# Oblivious Gap Matrix

Date: 2026-07-02

Status labels:

- Observed: production runtime code exists.
- Partial: runtime code exists but lacks production depth.
- Gap: not implemented or scaffold/fake/hardcoded/TODO/local-only.

No item is marked Proven.

| Module | Capability | Status | Evidence | Severity | Required to close |
| --- | --- | --- | --- | --- | --- |
| Relay | `/v1/chat/completions` live call | Partial | Relay mount in `src/server/internal/http/server.go:413-430`; policy supports chat in `src/server/internal/relay/handler/policy.go:470-472`. | P1 | Verify true upstream streaming, usage, quota, retry, provider error handling in target runtime. |
| Relay | Gateway proxy endpoint | Partial | `src/server/internal/http/gateway_handler.go` rewrites `/api/v1/gateway/proxy/*` to `/v1/*`, strips browser authorization, injects trusted Relay identity headers, and calls the configured Relay handler. | P1 | Prove target-runtime provider forwarding with request-log, usage, quota, route-decision, and streaming evidence. |
| Relay | Realtime API | Partial | Route policy still disables realtime unless `RealtimeCommercialLifecycleEnabled` is set; `src/server/internal/relay/relay.go` now passes that flag into the concrete handler; `src/server/internal/relay/handler/realtime.go` and `src/server/internal/relay/handler_new/realtime.go` default to `501 unsupported_api` before upstream dialing or billing-router calls, while explicit lifecycle-enabled tests cover streaming billing and usage capture. | P0 | Production prebill, abort settlement, request-log linkage, target-runtime provider proof, and deployed pricing/config evidence before enablement. |
| Relay | Batch API | Partial | Route policy still disables batch unless `BatchCommercialLifecycleEnabled` is set; `src/server/internal/relay/relay.go` now passes that flag into the concrete handler; `src/server/internal/relay/handler/batch.go` and `src/server/internal/relay/handler_new/batch.go` default to `501 unsupported_api` before upstream, billing-router, or polling-registrar work, while explicit lifecycle-enabled tests cover submit/list/get, billing routing, polling registration, durable settlement context, and polling/finalizer worker behavior. | P0 | Target-runtime polling worker proof, provider usage reconciliation, settlement/refund/audit proof, request-log joins, and deployed config evidence before enablement. |
| Relay | Files API lifecycle | Partial | Active runtime wires `handler.NewFilesHandler(...).WithMappingStore(cfg.FilesMappingStore)` in `src/server/internal/relay/relay.go`; `src/server/internal/relay/handler/files.go` requires trusted tenant identity, maps local file IDs to provider file IDs, filters lists to tenant mappings, and routes upload/list/get/delete/content through Relay billing; `src/server/internal/relay/store.go` persists save/get/list tenant-scoped mappings; HTTP server Relay config injects a request-log-backed route audit sink. | P1 | Add target-runtime provider reachability, request-log row proof, and tenant-isolation proof, plus explicit delete/tombstone lifecycle audit and reconciliation evidence. |
| Relay | Fine-tuning/Assistants/Threads/Runs | Observed | Route policy marks fine-tuning, assistants, threads, and runs disabled until lifecycle billing/governance/audit exists; both legacy `handler` and `handler_new` fine-tuning/assistants handlers now return `501 unsupported_api` directly instead of passthrough, so a route-policy bypass cannot forward these unsupported commercial surfaces upstream. | P0 | Keep disabled until full lifecycle, billing, governance, request-log, and target-runtime proof exists. |
| Relay | Pricing authority | Partial | Runtime prices load from `relay_pricing_entries`; `src/server/internal/relay/pricing.go` fails closed through `ErrRelayPriceNotConfigured`; production startup requires a non-empty pricing store; Relay usage rows persist immutable price snapshots; `/api/v1/admin/billing/reconciliation/relay-usage-prices` reports ledger-vs-snapshot drift; Admin Relay pricing catalog imports can create diffed pending batches, reject bad batches, approve audited SQL mutations, generate conflict-checked rollback drafts, and create pending imports from LiteLLM price JSON through `/api/v1/admin/pricing/relay-catalog/sync`; `relay_pricing_sync_runs` plus `/api/v1/admin/pricing/relay-catalog/sync-runs` record scheduled freshness and reconciliation run evidence; supported audio/moderation routes pass usage into billing and have seed catalog dimensions. | P1 | Add target-runtime provider price freshness proof, price freshness SLO evidence, and broader cross-system reconciliation across provider exports, request logs, checkout, webhook, quota, refund, and payout. |
| Chat | Persistent conversations/messages | Observed | `src/server/internal/http/routes_chat.go:67-90`; chat SQL store. | P2 | Keep; validate tenant scoping. |
| Chat | Live model response | Partial | Relay gateway exists; HTTP server and standalone Agent gateway fail closed in production when Relay is disabled; demo fallback remains non-production only. | P1 | Prove target-runtime live provider response, request-log join, provider usage, and streaming behavior. |
| Chat | Usage logging | Partial | `src/server/internal/chat/service.go:539-550`, `src/server/internal/chat/service.go:629-645`. | P1 | Use provider usage/Relay usage records, not estimates. |
| Chat | Streaming | Partial | Relay stream path exists; structured agent streaming simulated. | P1 | End-to-end SSE with cancellation and usage. |
| RAG | Upload/parse/chunk/embed | Partial | `src/server/internal/http/knowledge_handler.go:380-520`; `src/server/internal/http/routes_knowledge.go`; `src/server/internal/http/routes_knowledge_alias.go`; `src/server/internal/knowledge/ingestion_jobs.go`; `src/server/internal/knowledge/ingestion_job_store.go`; `src/server/migrations/0093_knowledge_ingestion_jobs.sql`. | P0 | Upload now parses once, enqueues durable ingestion jobs with status query, persists raw upload bytes/source metadata for parser replay, and avoids synchronous document/chunk creation; still prove worker recovery in target Postgres/Qdrant runtime. |
| RAG | Vector upsert/search | Partial | `src/server/internal/knowledge/qdrant_store.go:65-98`; Qdrant payload writes `document_title` at `src/server/internal/knowledge/qdrant_store.go:84`; durable vector index jobs have retry caps, owner-guarded processing leases, expired-lease reclaim, `dead_letter` terminal state, and SQL transactional enqueue for document create/update, chunk edit/split/merge, and delete vector cleanup intent in `src/server/internal/knowledge/index_jobs.go`, `src/server/internal/knowledge/index_job_store.go`, and `src/server/internal/knowledge/store.go`. | P1 | Add deeper retrieval debug, version filters, live Qdrant target proof, and keep outbox coverage as new write paths are added. |
| RAG | Document delete/reindex | Partial | Document vector cleanup in `src/server/internal/knowledge/service.go`; document-scoped replace before upsert; delete capability in `src/server/internal/knowledge/qdrant_store.go:46-63`; vector repair jobs retry and dead-letter; document create/update, chunk edit/split/merge, and delete SQL writes now enqueue vector jobs transactionally. | P1 | Prove target Postgres/Qdrant runtime recovery and retrieval cannot return deleted chunks after worker processing. |
| RAG | Citation metadata | Observed | Qdrant upsert/search preserves title/version/source/page metadata in `src/server/internal/knowledge/qdrant_store.go:65-145`. | P2 | Keep covered by payload/retrieval tests and target Qdrant proof. |
| Agent | Agent run persistence | Observed | Agent routes in `src/server/internal/http/router.go:290-335`; agent store/run files. | P2 | Keep; add target-runtime verification. |
| Agent | Tool approval gates | Partial | `src/server/internal/agent/runner.go:803-972` and approval routes; tool-enabled runs fail closed if structured replies are unavailable (`src/server/internal/agent/runner.go:868-871`, `src/server/internal/agent/runner.go:995-998`). | P1 | Complete live traces, cancellation, and target-runtime verification. |
| Agent | Simple run with tools | Partial | `src/server/internal/agent/runner.go` now fails closed from the plain `Run` path when tools are enabled and routes `RunStream` with enabled tools through `RunWithTools`, preserving structured tool calls, run status, tool result messages, and final streaming chunks. | P1 | Add target-runtime live traces, cancellation evidence, and deployed provider/tool execution proof before marking tool-enabled Agent runs commercially complete. |
| Agent | Structured streaming | Gap | `src/server/internal/agent/runner.go:937-947`, `src/server/internal/agent/runner.go:2123-2128`. | P1 | Stream provider deltas and tool calls live. |
| Agent | Custom Python sandbox | Partial | `src/server/internal/agent/executor.go` defines an injectable sandbox runner contract, keeps host Python non-production-only, fails closed in production without a runner, and delegates production Python to the runner with org/user/agent/run/tool/request/code/input/timeout metadata; `src/server/internal/agent/runner.go` and `src/server/internal/agent/service.go` fill durable Agent run/tool/request context for structured, plan-step, and approved persisted tool executions; `src/server/cmd/agent/main.go` wires the standalone Agent runtime to the Docker-backed Workflow sandbox when `WORKFLOW_SANDBOX_ENABLED=true`; `src/server/internal/workflow/sandbox/sandbox.go` supplies network-none, memory/CPU/PID, read-only FS, tmpfs, non-root, cap-drop, and no-new-privileges container policy, returns execution context in `Raw`, exposes it as `OBLIVIOUS_EXECUTION_CONTEXT`, classifies cancellation separately from timeout, and emits Raw execution evidence with timing, size, truncation, log-retention, and non-secret context metadata. | P0 | Target-runtime Docker/container proof, request-log/trace propagation into deployed collectors, persisted artifact/log retention, operator runbook, and production deployment evidence before marking custom code commercially ready. |
| Workflow | Workflow CRUD/execute API | Observed | `src/server/internal/http/routes_workflow.go:29-99`. | P2 | Keep; verify with live target runtime. |
| Workflow | Node registry | Partial | `src/server/internal/workflow/node_executor.go:38-86`. | P1 | Typed versioned registry with adapters and migration. |
| Workflow | Durable execution rows | Observed | `src/server/internal/workflow/store.go:327-358`. | P2 | Keep; add richer event log. |
| Workflow | Durable state history | Partial | `workflow_execution_events` persists service-level `created` and `status_changed` execution events; debug snapshots return those events. `executor.StateMachine` now supports a `TransitionSink` and refuses to update memory-only state if the sink fails (`src/server/internal/workflow/executor/state_machine.go:41-164`); service pause/resume/cancel routes through that StateMachine sink and the sink updates the store-backed event table (`src/server/internal/workflow/service.go:1414-1441`, `src/server/internal/workflow/service.go:1639-1641`, `src/server/internal/workflow/service.go:2030-2085`). | P1 | Extend the same durable transition path to remaining workflow transition surfaces, add replay/retention APIs, and prove DB-backed restart replay. |
| Workflow | Durable debugger | Observed | `workflow_debug_trace_entries` and `workflow_debug_variable_snapshots` persist trace entries and variable snapshots; SQL node execution creation writes those rows in the same transaction; `BuildExecutionDebugSnapshot` prefers durable rows, includes state replay, and exposes `/api/v1/workflows/{id}/executions/{executionID}/debug-snapshot`; `/api/v1/workflows/debug-retention/prune` prunes trace/snapshot rows by tenant and cutoff; the standalone in-memory `workflow/debug` tracer has been removed and no `workflow/debug`, `debug.Tracer`, or `NewTracer` references remain. | P1 | Add deployed target Postgres replay/retention smoke evidence before marking debugger commercially proven. |
| Billing | Stripe checkout/webhook | Observed | `src/server/internal/stripe/checkout.go:62-138`; `src/server/internal/stripe/webhook.go:54-92`. | P2 | Keep; add reconciliation/refund proof. |
| Billing | Non-Stripe providers | Partial | Default payment registry keeps Alipay/WeChatPay registered but unconfigured, so `Resolve` rejects them and `AvailableProviders` hides them until configured. Production config requires each domestic provider to supply checkout URL plus webhook secret together, now rejects non-HTTPS checkout URLs, hosted checkout registration validates the base URL before marking the provider configured, and console/marketplace surfaces only expose providers that are both registry-available and backed by checkout creators. | P1 | Prove real Alipay/WeChatPay target checkout, signed webhook lifecycle, refund/reconciliation, request-log linkage, and settlement evidence before marking commercial complete. |
| Billing | Quota settlement/reconciliation | Partial | Quota routes in `src/server/internal/http/router.go:570-619`. | P1 | Transactional lifecycle and reconciliation jobs. |
| Marketplace | Publish/review/install | Partial | `src/server/internal/http/marketplace_handler.go:69-108`; marketplace routes in `src/web/src/app/router.tsx:89-92`. | P1 | Manual review history, appeal, delist, ranking quality. |
| Marketplace | Paid install order/settlement | Partial | `src/server/internal/marketplace/settlement.go:218-368`. | P1 | Webhook/reconciliation/refund proof. |
| Marketplace | External payout | Partial | `src/server/internal/marketplace/settlement.go` now requires a configured payout provider, writes payout/settlement binding before provider dispatch, and retries queued rows with the same payout id; inbound payout lifecycle webhooks update payout/settlement state. | P0 | Prove real target payout provider dispatch, inbound webhook lifecycle, settlement reconciliation, refund/chargeback handling, and provider rail evidence. |
| Admin | Admin surfaces | Observed | `src/web/src/app/router.tsx:105-118`. | P2 | Keep. |
| Admin | Provider support | Observed | Admin channel creation validates providers against the Relay catalog and rejects planned/non-configurable providers; the provider catalog marks `status`, `configurable`, `installable`, and `runtimeReady`; `/api/v1/admin/channel-providers` now also returns explicit `supportedProviders` and `plannedProviders` groups; the Admin Channels UI filters forms and provider filters to supported providers that are installable and runtime-ready. | P1 | Keep catalog status tests and UI smoke coverage in release runs; add target admin route smoke evidence before claiming deployed-console completeness. |
| Observability | Metrics/alerts | Partial | Admin alert routes and alert delivery code. | P1 | End-to-end alert sinks and SLO policies. |
| Observability | Request log/cost/latency | Partial | Production rejects missing ClickHouse request-log backend, ClickHouse open/ping failures panic in production, and `request_logs` persists a dedicated `request_id` column for joining to usage/billing evidence (`src/server/internal/observability/request_log.go`, `src/server/migrations/clickhouse/0001_request_logs.sql`). | P0 | Add target ClickHouse ingest/query proof and target request-log to usage/billing join evidence. |
| Deployment/Security | Auth/session/org | Partial | Protected/admin routes in `src/web/src/app/router.tsx:138-150`; auth middleware. | P1 | Target-runtime hardening and tenant-isolation audit. |
| Deployment/Security | Secret protection | Partial | `src/server/internal/secretbox` protects stored secrets and rejects legacy unprotected values when `APP_ENV=production`. | P1 | Add target secret rotation/detection evidence and operator runbook. |
| Deployment/Security | WebSocket origin | Observed | `src/server/internal/relay/handler/realtime.go:17` delegates upgrade checks to the shared origin policy; realtime remains disabled by product policy until billing lifecycle is complete. | P2 | Keep origin policy covered while implementing realtime auth/billing/settlement. |

## Modules That Are Only Scaffolds Or Too Shallow To Count As Complete

- Gateway proxy target-runtime evidence.
- Realtime relay commercial lifecycle remains disabled by default and needs target-runtime prebill/abort settlement/request-log proof before enablement.
- Batch relay lifecycle remains disabled by default and needs target-runtime polling/settlement/refund/audit proof before enablement.
- Relay Files target-runtime request-log/lifecycle/tombstone proof.
- Fine-tuning/Assistants/Threads/Runs remain intentionally disabled, and direct handlers fail closed instead of passthrough until commercial lifecycle support exists.
- RAG upload enqueue/status raw parser replay target-runtime proof.
- Workflow debugger target Postgres replay/retention evidence.
- Marketplace payout target provider proof.
- Observability request-log/SLO loop by default.

## Dangerous Fake / Hardcoded / Local Logic

- Development-only Chat demo fallback must not be counted as commercial runtime evidence.
- Seeded relay price defaults now have manual import/approval/rollback, admin-triggered LiteLLM sync-to-pending-import audit loops, and a repository-local maintenance run ledger for scheduled freshness plus ledger-vs-snapshot reconciliation, but still need target provider-source price freshness proof and broader cross-system reconciliation beyond the usage price snapshot report.
- Batch submit now requires the request `model`, uses it for billing/polling registration, rejects missing-model requests before billing/upstream dispatch, and the direct Batch/Realtime handlers now fail closed with `501 unsupported_api` unless their commercial lifecycle flags are explicitly enabled; list/get passthrough avoids duplicate `/v1/batch` paths and preserves upstream base-path prefixes when enabled; Batch and Realtime remain blocked on target-runtime lifecycle, settlement/refund/abort, audit, usage capture, request-log joins, and provider proof.
- Marketplace payout local fallback is closed and repository-local transactional dispatch outbox exists; target external payout proof remains.
- Domestic Alipay/WeChatPay checkout providers now stay hidden unless configured with valid hosted checkout creators, and production rejects incomplete or non-HTTPS domestic payment config; target checkout/webhook/refund/reconciliation proof remains.
- Sandboxed custom Python now carries agent/run/tool/request execution context into the Docker runner and result metadata, classifies cancellation, and emits Raw retention/size/truncation evidence; target Docker proof, deployed trace/request-log propagation, persisted artifact/log retention, and operator runbook evidence remain.
- Workflow service-level pause/resume/cancel now uses a StateMachine transition sink backed by `workflow_execution_events`; debugger trace/snapshot rows, replay APIs, retention pruning, and legacy tracer removal are covered locally, but target Postgres replay/retention evidence remains.
- Production Relay request-log sink is fail-closed and ClickHouse rows now retain `request_id`; target ClickHouse row proof and target request-log to usage/billing join proof remain.
- RAG vector repair has retry/dead-letter state and transactional vector intent enqueue across document create/update, chunk edit/split/merge, and delete cleanup; upload now enqueues durable raw-payload ingestion jobs, but target Postgres/Qdrant worker recovery proof is still missing.
