# Current Implementation Depth

Date: 2026-07-02

Scope: production runtime code only. Excluded from scoring: `reference/`, `docs/`, tests, fixtures, mocks, generated OpenAPI/protobuf output, `evidence/`, `node_modules/`, `dist/`, `build/`, and `coverage/`.

No module is marked Proven. Scores are conservative implementation-depth scores from 0 to 5.

## Module Scores

| Module | Score | Production LOC estimate | Main production files | Runtime depth | Main gaps |
| --- | ---: | ---: | --- | --- | --- |
| Relay / API Gateway | 3/5 | ~15.5k | `src/server/internal/relay/**`, `src/server/internal/http/server.go`, admin channel/model/routes files | Real `/v1/*` relay mount (`src/server/internal/http/server.go:413-430`), authenticated Gateway proxy forwarding into Relay, channel pool, route policy/audit (`src/server/internal/relay/handler/policy.go:437-448`), quota hooks, usage logging, SQL-backed pricing catalog with production fail-fast, immutable usage price snapshots, admin-triggered LiteLLM price sync into pending imports, maintenance-worker run ledger for pricing freshness/reconciliation, health/config surfaces; Chat SSE in `handler_new` now flushes upstream chunks through the selected channel before upstream completion and avoids duplicate captured-body writes (`src/server/internal/relay/handler_new/chat.go:140-151`, `src/server/internal/relay/handler_new/chat.go:178-218`); Batch submit can persist polling jobs with request/user/org/token/billing context, the standalone and server Relay runtimes can start a polling worker when configured, completed Batch jobs can replace the original usage record and settle durable quota/API-token reservations, terminal Batch failures now write error usage/audit records before refund/dead-letter (`src/server/internal/relay/batch_polling_worker.go`, `src/server/internal/relay/store.go:440-623`), and active Realtime handler calls now reject disallowed WebSocket `Origin` values before billing/upstream dialing while Relay config forwards `CORS_ALLOWED_ORIGINS` into standalone and server-built Relay instances; production Realtime API-token authorization also receives the query-string model before the stream handler runs, streaming Realtime aborts now replace pending usage with a terminal error record plus request-log failure metadata, and opt-in production Realtime lifecycle startup fails closed unless active Realtime pricing exists for an enabled channel model. | Realtime/fine-tuning/assistants/threads/runs remain production-disabled or incomplete (`src/server/internal/relay/handler/policy.go:470-505`); Realtime still needs target price-source freshness, target abort/disconnect settlement, target request-log join proof, target-runtime proof, and final target proof before commercial enablement; target provider price freshness proof and cross-system reconciliation breadth are not yet settlement-grade; Batch target provider/result reconciliation and target request-log linkage are still required. |
| Chat | 2.5/5 | ~7.6k | `src/server/internal/chat/*`, `src/server/internal/http/routes_chat.go`, `src/web/src/routes/workspace/ChatPage.tsx` | User-visible chat UI and API routes exist (`src/server/internal/http/routes_chat.go:33-90`); SQL conversations/messages/config/personas/share/fork exist; production Chat/Agent gateway construction fails closed when Relay is disabled; Relay gateway supports streaming. | Non-production local demo fallback still exists for development only (`src/server/internal/chat/gateway.go:79-98`); usage is estimated, not provider-authoritative (`src/server/internal/chat/service.go:539-550`, `src/server/internal/chat/service.go:629-645`); real flow depends on Relay config and target provider evidence. |
| Knowledge / RAG | 3/5 | ~12.0k | `src/server/internal/knowledge/**`, `src/server/internal/http/knowledge_handler.go`, `src/web/src/routes/workspace/KnowledgePage.tsx` | Knowledge base/document/chunk/retrieval API exists; upload parser, chunking, embedding, Qdrant store, hybrid/rerank hooks, citations, retrieval test cases; document delete and document-id delete clean tenant-scoped Qdrant points; update/rebuild paths replace document-scoped Qdrant vectors before upsert; Qdrant payloads include document titles; durable vector index jobs now have retry caps, owner-guarded processing leases, expired-lease reclaim, terminal `dead_letter` state, and transactional SQL outbox creation for document create/update, chunk edit/split/merge, and delete vector cleanup intent; multipart upload now parses the file once, persists raw upload bytes/source metadata on `knowledge_ingestion_jobs`, returns `202 Accepted` without synchronous document/chunk creation, exposes `/documents/ingestion-jobs` status for operator/UI recovery, and `RAG_INGESTION_WORKER_*` can replay parsing from the persisted raw payload before chunk/embed/create. | Retrieval-debug evidence and target Postgres/Qdrant ingestion/vector runtime proof remain incomplete. |
| Agent | 3/5 | ~16.0k | `src/server/internal/agent/**`, `src/server/internal/http/agent_*`, `src/web/src/routes/workspace/AgentsPage.tsx`, `AgentPlanStepsPage.tsx` | Persistent agents/conversations/messages/runs/tool-runs/plan-steps; tool approvals/reject/retry routes (`src/server/internal/http/router.go:290-335`); ReAct/planning modes, budget checks, memory hooks, MCP/custom tools; service entrypoints route tool-enabled agents through `RunWithTools`, and direct lightweight `Runner.Run` misuse now fails closed before plain gateway execution (`src/server/internal/agent/runner.go:378-381`, `src/server/internal/agent/runner.go:872-876`, `src/server/internal/agent/runner.go:998-1002`); production custom Python remains fail-closed without a configured runner, but standalone Agent can now inject the Docker-backed Workflow sandbox when `WORKFLOW_SANDBOX_ENABLED=true` (`src/server/cmd/agent/main.go:141-188`). | The lightweight path remains intentionally no-tool for non-tool agents; structured streaming is simulated from full content (`src/server/internal/agent/runner.go:941-947`); custom Python still lacks target-runtime Docker/container evidence, cancellation/artifact retention proof, and audit-log joins before it can be counted as commercially complete. |
| Workflow | 3/5 | ~19.1k | `src/server/internal/workflow/**`, `src/server/internal/http/routes_workflow.go`, `src/server/internal/schedule/**`, `src/web/src/routes/workspace/WorkflowsPage.tsx` | Broad UI/API for workflow CRUD/execute/webhook/semantic/conversation/version/debug/state-replay/debug-retention/pause/resume/cancel (`src/server/internal/http/routes_workflow.go:12-108`); SQL `workflow_executions`, node executions, append-only execution status events, durable debug trace/variable snapshot tables, and a trigger replay ledger; every SQL node execution now writes trace/snapshot rows in the same transaction, debug snapshots prefer those persisted rows, state replay is exposed through a tenant-scoped API, debug trace/snapshot retention can be pruned through a CSRF-protected tenant-scoped API, signed webhook replay prevention survives handler reconstruction through `workflow_webhook_replay_keys`, duplicate chat-driven conversation/semantic trigger events are rejected across reconstructed dispatchers using persisted user message ids, scheduled trigger executions are idempotent on `scheduledTaskRunId` across rebuilt SQL services and unique-conflict paths, rebuilt services can resolve paused failure decisions and continue due auto-retry nodes from persisted SQL state, service pause/resume/cancel now transition through `executor.StateMachine` with a durable sink into `workflow_execution_events` (`src/server/internal/workflow/executor/state_machine.go:41-164`, `src/server/internal/workflow/service.go:1039-1072`, `src/server/internal/workflow/service.go:2030-2085`), and workflow telemetry release evidence now carries total/successful/failed execution counts matching the reported success rate. | Node registry is not a mature versioned adapter catalog (`src/server/internal/workflow/node_executor.go:38-86`); target workflow telemetry and deployed gRPC smoke are still missing, so this remains repository-local runtime and proof-schema hardening rather than a full commercial Workflow completion claim. |
| Billing / Quota | 3/5 | ~5.5k | `src/server/internal/billing/**`, `src/server/internal/quota/**`, `src/server/internal/stripe/**`, payment/webhook handlers | Real Stripe checkout (`src/server/internal/stripe/checkout.go:62-138`), signature-verified Stripe webhook ledger (`src/server/internal/stripe/webhook.go:54-92`), quota/package/topup/checkout routes (`src/server/internal/http/router.go:570-619`), relay runtime prices loaded from `relay_pricing_entries`, billable Relay `usage_records` persist immutable pricing snapshots, LiteLLM sync creates pending price imports for approval, and maintenance runs record provider price freshness/reconciliation status. | Non-Stripe providers are unconfigured by default (`src/server/internal/payment/provider.go:81-87`); legacy top-up path is not a real checkout; settlement/refund/reconciliation breadth is incomplete; target provider price freshness proof is not complete. |
| Marketplace | 2.5/5 | ~8.2k | `src/server/internal/marketplace/**`, `src/server/internal/http/marketplace_handler.go`, marketplace pages | Publish/search/detail/install/paid install routes and UI exist (`src/server/internal/http/marketplace_handler.go:69-108`, `src/web/src/app/router.tsx:89-92`); paid install creates payment intent/order in transaction (`src/server/internal/marketplace/settlement.go:218-248`); checkout-completed creates install/settlement (`src/server/internal/marketplace/settlement.go:259-368`); payout dispatch fails closed if no provider is configured; payout rows now act as a transactional outbox so provider dispatch happens only after payout/settlement SQL commit and queued dispatch retries reuse the same payout id; automated review scanner findings include stable rule IDs, policy version, and checksum metadata. | External payout provider target proof, payout webhook/reconciliation/refund breadth, external marketplace policy operations, abuse/risk signal depth, and appeal/delist/ranking maturity remain incomplete. |
| Admin | 3/5 | ~15.3k | `src/server/internal/admin/**`, `src/server/internal/http/admin_handler.go`, admin pages | Admin UI routes for channels/models/routes/plans/billing/API tokens/usage/alerts/settings/users/audit/reviews (`src/web/src/app/router.tsx:105-118`); channel mutations audit redacted changes (`src/server/internal/admin/channel_service.go:70-84`); channel provider catalog separates `supported` from `planned` and exposes `configurable`, `installable`, and `runtimeReady` readiness flags while admin channel creation and Relay adapter construction reject planned providers. | Runtime stats and review filters have shallow/incomplete behavior; admin cannot compensate for missing external integration or target provider proof. |
| Observability | 2.5/5 | ~6.3k | `src/server/internal/observability/**`, `src/server/internal/metrics/**`, admin alert routes, request-log release evidence scripts | `/metrics`, alert state/routing/provider/delivery APIs, request log row mapping, alert delivery history; production Relay rejects `OBSERVABILITY_REQUEST_LOG_BACKEND=none` (`src/server/internal/config/config.go:514-527`); ClickHouse sink open/ping failure panics in production (`src/server/internal/http/server.go:259-288`); ClickHouse `request_logs` now persists a dedicated `request_id` column for joining against Relay `usage_records.request_id` and billing evidence; HTTP recovery is explicitly audit-only, records planned remediation reasons, and prefers `OBSERVABILITY_HTTP_RECOVERY_AUDIT_*` envs while retaining legacy aliases; `/api/v1/admin/observability/latency-slo-proof` aggregates runtime alert states, delivery attempts, and recovery actions for release evidence; missing alert sinks are persisted as failed delivery attempts; request-log release evidence requires structured latency SLO window, triggered alert count, alert delivery success/failure counts, channels, and recovery-audit records. | Request logging still depends on target ClickHouse proof, including a target query proving request-log to usage/billing joins; `NoopRequestLogSink` remains for non-production/local paths (`src/server/internal/observability/request_log.go:45-49`); alert/recovery proof now has a runtime Admin endpoint but still needs target alert sink configuration and operator-review evidence before commercial completion. |
| Deployment / Security | 2.5/5 | ~6.6k | `src/server/cmd/server`, auth/security middleware, tenant/auth stores, `deploy/**` | Auth/session/org/workspace tables and guards, CSRF/rate-limit middleware, WebSocket origin policy, production plaintext-secret denial, production custom-Python fail-closed-by-default, health/metrics, Docker/Kubernetes-like assets; custom Python can reuse the Workflow Docker sandbox in the standalone Agent runtime when explicitly enabled; target secret-audit release proof must include `checkedAt`, required scope coverage, zero findings, nonzero scanned records, and zero plaintext/invalid/rotation-required counts. | No target-runtime proof in this audit; local/dev fallback paths remain; real target secret rotation/audit evidence is still required; production-grade custom code sandbox is wired only as a configured runtime path and still needs target isolation/log/cancellation/artifact proof. |

## Runtime Inventory By Module

### Relay / API Gateway

- API endpoints: `/v1/*` mounted through Relay, `/api/v1/gateway/proxy/*` forwards authenticated session requests into the Relay engine, plus aliases to chat/completions/embeddings/responses/images/audio/models via `combineHandlers` (`src/server/internal/http/server.go:413-430`).
- UI: Admin Channels, Models, Routes, API Tokens, Usage Logs (`src/web/src/app/router.tsx:105-118`) and Console Models/Usage/Access (`src/web/src/app/router.tsx:96-102`).
- Runtime state: channels, model routes, weights, API tokens, quota balances, usage records, semantic cache, tenant-scoped Relay file mappings.
- External integrations: OpenAI-compatible providers, Anthropic/Claude, Gemini, Bedrock, Vertex-like adapters in Relay/channel code.
- Incomplete flows: realtime target price-source freshness, target abort/disconnect settlement, target request-log join, and target-runtime proof; batch target provider/result reconciliation; target provider price-source freshness proof and cross-system reconciliation depth; Relay Files target-runtime lifecycle/tombstone proof; disabled assistant/thread/fine-tuning surfaces.

### Chat

- API endpoints: models/personas/conversations/messages/stream/share/export/fork/convert-to-task (`src/server/internal/http/routes_chat.go:33-90` and following route blocks).
- UI: Workspace Chat and Solo routes (`src/web/src/app/router.tsx:78-85`).
- Runtime state: conversations, conversation configs, messages, personas, shares, knowledge bindings.
- External integrations: Relay gateway when enabled.
- Incomplete flows: target provider evidence, estimated chat-service usage outside Relay ledger, structured tool/agent integration is not chat-native.

### Knowledge / RAG

- API endpoints: knowledge base CRUD, documents/upload async enqueue, documents/ingestion-jobs status, retrieval, chunk edit/split/merge, versions/test cases (`src/server/internal/http/routes_knowledge.go`).
- UI: Workspace Knowledge route (`src/web/src/app/router.tsx` around workspace entries).
- Runtime state: knowledge bases, documents, chunks, versions/history, retrieval tests, conversation bindings.
- Async workers: vector index repair worker exists for Qdrant upsert/rebuild/delete retry, owner-guarded lease reclaim, terminal dead-letter, and SQL transactional outbox jobs for document create/update, chunk edit/split/merge, and delete vector cleanup intent. A durable ingestion worker can consume `knowledge_ingestion_jobs`, replay parsing from persisted raw upload bytes, and create documents through the same chunk/embed/index path.
- External integrations: OpenAI-compatible embedder, Qdrant vector store, optional reranker.
- Incomplete flows: retrieval-debug plus target Postgres/Qdrant ingestion/vector runtime proof still need to be captured.

### Agent

- API endpoints: agent CRUD, conversations/messages/runs, tool-run approve/reject/retry, memories, plan-step operations (`src/server/internal/http/router.go:290-335`).
- UI: Agents, Agent Memories, Agent Plan Steps routes (`src/web/src/app/router.tsx:80-83`).
- Runtime state: agents, agent conversations/messages, runs, tool runs, memories, plan steps/executions.
- External integrations: Relay, MCP, custom API tools, local Python tools.
- Incomplete flows: custom Python is production fail-closed by default and non-production execution blocks imports, times out, and caps source/argument/output payloads; an explicit Docker sandbox runner path is now wired for standalone Agent when `WORKFLOW_SANDBOX_ENABLED=true`, but target container/runtime proof, cancellation/artifact retention, and audit-log joins are still missing; lightweight no-tool run path is intentionally plain chat only, structured streaming is simulated, and production-grade cancellation trace is not proven for every tool.

### Workflow

- API endpoints: workflow CRUD/execute/webhook/semantic/conversation/version/rollback/test-node/debug/pause/resume/cancel (`src/server/internal/http/routes_workflow.go:12-99` and following route blocks).
- UI: Workflows and Scheduled Tasks (`src/web/src/app/router.tsx:86-87`).
- Runtime state: workflows, workflow versions, executions, node executions, schedules, execution status events, durable debug trace entries, durable variable snapshots.
- Async workers: scheduled task worker starts only when configured.
- Incomplete flows: service pause/resume/cancel transitions now share the StateMachine rule path and write through the durable event table, SQL node-execution trace/snapshot storage is durable, state replay is exposed, the old standalone debug tracer helper has been removed, tenant-scoped debug retention pruning exists, signed webhook plus chat-driven conversation/semantic trigger replay prevention is backed by a PostgreSQL ledger, scheduled trigger execution idempotency is backed by `scheduledTaskRunId`, rebuilt-service paused-failure/due auto-retry continuation has PostgreSQL evidence, and release proof now rejects workflow telemetry without matching execution counts; target workflow telemetry source and deployed gRPC smoke are still incomplete.

### Billing / Quota

- API endpoints: quota/packages/topup/checkout and Stripe/Alipay/WeChatPay webhooks (`src/server/internal/http/router.go:570-619`).
- UI: Console Billing/Usage and Admin Billing/Plans (`src/web/src/app/router.tsx:96-111`).
- Runtime state: quota balances/packages/limits, payment intents, webhook ledger, billing lifecycle, marketplace orders/settlements.
- External integrations: Stripe real; Alipay/WeChatPay off by default.
- Incomplete flows: provider breadth, refund/reconciliation hardening, target provider price freshness proof and cross-system reconciliation.

### Marketplace

- API endpoints: list/search/detail/publish/update/delete/install/uninstall/my-agents/paid install (`src/server/internal/http/marketplace_handler.go:69-108` and settlement handler paths).
- UI: Marketplace home/detail/publish/my-agents (`src/web/src/app/router.tsx:89-92`).
- Runtime state: published agents, versions, installs, categories/tags, orders, settlements, payouts, audit retention.
- External integrations: checkout provider for paid install; explicit external payout provider configuration with repository-local transactional outbox/retry, but no default provider and no target payout rail proof.
- Incomplete flows: target external payout/reconciliation proof, appeal/delist depth, recommendation/ranking quality, and external marketplace policy/risk-signal operations beyond the deterministic local scanner.

### Admin

- API/UI: admin pages cover channels/models/routes/plans/billing/tokens/usage/alerts/settings/users/audit/reviews (`src/web/src/app/router.tsx:105-118`).
- Runtime state: users, channels, model inventory, plans, audit logs, marketplace reviews.
- Incomplete flows: partial review history filters and operational actions not always backed by real integrations; planned providers remain catalog metadata only until target implementation evidence promotes them to supported.

### Observability

- API/UI: metrics and admin observability alert routes (`src/server/internal/http/routes_observability_alert.go`).
- Runtime state: alert state/routing/provider configs/delivery history, optional request logs with dedicated `request_id` join column in ClickHouse.
- Incomplete flows: target ClickHouse persistence proof and target request-log to usage/billing join evidence are still required; release proof now can fetch runtime latency SLO proof with nonzero alerts, zero alert delivery failures, and persisted recovery audit records, but real target alert sink configuration and operator-review evidence are still missing.

### Deployment / Security

- Runtime state: auth/session/org/workspace/invitation/reset tokens.
- Security controls: session cookie, admin guard, CSRF, rate limits, secretbox, signed webhook checks in selected paths.
- Incomplete flows: real target secret rotation/audit evidence, local fallback paths, custom Python still lacks target-runtime proof for the newly wired Docker sandbox path, missing cancellation/artifact/log evidence.
