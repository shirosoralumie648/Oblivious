# Fusion Spec Evidence Pack

This pack is the current release-readiness evidence index for the four 2026-06-04 fusion specs. It is not a final completion claim. The source of truth remains `docs/reports/2026-06-07-fusion-spec-completion-matrix.md`; any row marked `Partial` remains open until the row-specific proof is recorded and rerun on the target environment where required.

## Current Boundary

- Current status: partial fusion-spec completion.
- Current matrix: `docs/reports/2026-06-07-fusion-spec-completion-matrix.md`.
- Historical commercial baseline: `docs/release/commercial-completion-audit.md` and `docs/release/commercial-gates.md`.
- This pack only indexes evidence that exists in the repository or must be supplied by a target deployment.
- A new full-project completion claim requires all matrix rows to be proven, then strict final verification with no environment skips.

## Requirement Evidence Index

| Spec area | Current evidence source | Required final proof |
| --- | --- | --- |
| Backend journey current delta | `scripts/verify-commercial-db-evidence.sh backend-journey`, `scripts/verify-commercial-db-evidence-profiles.sh`, `src/server/internal/http/commercial_journey_test.go`, `docs/release/commercial-completion-audit.md`, `docs/reports/2026-06-07-fusion-spec-completion-matrix.md` | Profile synchronization now fails if the no-skip backend journey profile stops using the exact anchored `backend_journey_pattern="^TestCommercialHTTPJourney$"` pattern. This pins repository-local DB-backed commercial journey evidence integrity only; target deploy, Kubernetes, backup/restore, live provider rails, target evidence, and final no-skip release proof remain external |
| Relay and API gateway | `docs/reports/2026-06-07-relay-requirement-audit.md`, `src/server/internal/relay/healthchecker_test.go`, `scripts/verify-relay-security.sh`, Relay/OpenAPI contract gates, `scripts/verify-commercial-db-evidence.sh relay-file-mapping-tenant-ownership`, `scripts/verify-commercial-db-evidence.sh admin-relay-channel-isolation`, `scripts/verify-commercial-db-evidence.sh admin-relay-read-isolation`, `scripts/verify-commercial-db-evidence.sh relay-runtime-channel-isolation`, `scripts/verify-commercial-db-evidence.sh core-sql-persistence`, Admin route browser payload proof in `src/web/e2e/admin-commercial-config.spec.ts`, Admin Channels browser proof in `src/web/e2e/admin-channels.spec.ts` with fail-closed fixture `src/web/e2e/fixtures/adminChannels.ts`, Admin Models browser proof in `src/web/e2e/admin-models.spec.ts` with fail-closed fixture `src/web/e2e/fixtures/adminModels.ts` | Focused Relay tests including realtime health probe `POST /v1/chat/completions`, upstream-error unhealthy behavior, and unknown-strategy fail-closed proof; Relay file upload/get/list tenant ownership proof; Admin Relay channel active-organization isolation; Admin Relay read-surface active-organization isolation; Relay runtime active-organization channel-selection and conversation-affinity persistence proof; Relay semantic-cache SQL persistence proof; Admin model-route browser CRUD payload proof; Admin channel create/edit/test/model-update/batch-disable/delete browser payload proof; Admin model inventory browser/API proof for provider/group/status/search/sort/limit/offset query propagation plus provider/channel/cost/margin rendering; `bash scripts/check.sh relay-security`; and `bash scripts/check.sh docs` on the release commit |
| Relay file-mapping current delta | `scripts/verify-commercial-db-evidence.sh relay-file-mapping-tenant-ownership`, `scripts/verify-commercial-db-evidence-profiles.sh`, `scripts/verify-commercial-db-evidence-profiles-fixtures.sh`, `src/server/internal/relay/store_test.go`, `src/server/internal/relay/relay_test.go`, matrix rows 21, 33, and 34 | Profile synchronization now fails if the no-skip Relay file-mapping profile omits SQL save/get/list tenant-ownership proof or the real NewRelay upload/get fail-closed route proof. The runner and guard now use explicit full test-name alternatives, with a negative fixture rejecting `TestRelayStoreSaveFileMappingPersistsTenantOwnershipV2`. This pins repository-local Relay file ownership evidence only; target tenant-isolation and final no-skip release proof remain external |
| Relay runtime current delta | `scripts/verify-commercial-db-evidence.sh relay-runtime-channel-isolation`, `scripts/verify-commercial-db-evidence-profiles.sh`, `scripts/verify-commercial-db-evidence-profiles-fixtures.sh`, `src/server/internal/relay/loadbalancer.go`, `src/server/internal/relay/router.go`, `src/server/internal/relay/store.go`, `src/server/internal/relay/handler/models.go`, matrix rows 21, 33, and 34 | Profile synchronization now fails if the no-skip Relay runtime channel-isolation profile omits model-route/default/fallback organization filtering, RouteWithBilling active-organization affinity replacement, conversation-affinity persistence/update behavior, SQL pool organization scope, runtime API-key at-rest/runtime hydration, or `/v1/models` active-organization scoping proof. The runner and guard now use explicit full test-name alternatives, with a negative fixture rejecting `TestLegacyLoadBalancerSelectModelForOrganizationFiltersModelRouteAndFallback`. This pins repository-local Relay runtime tenant-isolation evidence only; target provider reachability, target tenant-isolation evidence, and final no-skip release proof remain external |
| Core SQL current delta | `scripts/verify-commercial-db-evidence.sh core-sql-persistence`, `scripts/verify-commercial-db-evidence-profiles.sh`, `src/server/internal/chat/store_test.go`, `src/server/internal/channel/store_test.go`, `src/server/internal/relay/cache/semantic_cache_test.go`, `docs/release/commercial-completion-audit.md`, `docs/reports/2026-06-07-fusion-spec-completion-matrix.md` | Profile synchronization now fails if the no-skip core SQL persistence profile omits Chat SQL sharing/config/attachment/citation/fork evidence, Publishing channel config/message-log/retry/archive evidence, or Relay semantic-cache lookup/persistence evidence. This pins repository-local core Chat/Publishing/Relay evidence only; target traffic, live provider rails, and final no-skip release proof remain external |
| Admin Relay channel current delta | `scripts/verify-commercial-db-evidence.sh admin-relay-channel-isolation`, `scripts/verify-commercial-db-evidence-profiles.sh`, `scripts/verify-commercial-db-evidence-profiles-fixtures.sh`, `src/server/internal/http/admin_channel_secret_response_test.go`, matrix rows 20, 31, 33, and 34 | Profile synchronization now fails if the no-skip Admin Relay channel isolation profile omits `TestAdminRelayChannelHTTPRouteEnforcesActiveOrganizationIsolation`. The guard uses token-boundary full test-name matching, with a negative fixture rejecting `TestAdminRelayChannelHTTPRouteEnforcesActiveOrganizationIsolationV2`. This pins repository-local Admin Relay channel active-organization isolation proof only; target tenant-isolation evidence and final no-skip release proof remain external |
| Admin Relay read current delta | `scripts/verify-commercial-db-evidence.sh admin-relay-read-isolation`, `scripts/verify-commercial-db-evidence-profiles.sh`, `scripts/verify-commercial-db-evidence-profiles-fixtures.sh`, `src/server/internal/http/admin_channel_secret_response_test.go`, `src/server/internal/admin/service.go`, `src/server/internal/admin/model_inventory_store.go`, matrix rows 20, 26, 31, 33, and 34 | Profile synchronization now fails if the no-skip Admin Relay read-surface isolation profile omits `TestAdminRelayReadSurfacesScopeRuntimeStatsAndModelInventoryToActiveOrganization`. The guard uses token-boundary full test-name matching, with a negative fixture rejecting `TestAdminRelayReadSurfacesScopeRuntimeStatsAndModelInventoryToActiveOrganizationV2`. This pins repository-local runtime stats and model inventory active-organization isolation proof only; target tenant-isolation evidence, live provider inventory telemetry, and final no-skip release proof remain external |
| Workflow engine | `docs/reports/2026-06-07-workflow-requirement-audit.md`, `docs/reports/2026-06-08-workflow-success-rate-evidence.md`, `scripts/verify-workflow-success-rate-evidence.sh`, `scripts/verify-commercial-db-evidence.sh scheduled-task-runtime`, `scripts/verify-commercial-db-evidence.sh workflow-sql-isolation`, `scripts/verify-commercial-db-evidence.sh secret-response-safety`, `src/server/internal/workflow/agent_node_executor_test.go`, `src/server/internal/workflow/agent_client_test.go`, `src/server/internal/http/workflow_executor_test.go`, `src/server/internal/http/route_surface_test.go`, `src/server/pkg/workflow/grpc_server_test.go`, `src/server/cmd/workflow/main.go`, `src/server/cmd/workflow/main_test.go`, `src/server/cmd/grpc-smoke/main.go`, `scripts/target-grpc-smoke.sh`, `src/web/e2e/workflows.spec.ts`, `src/web/e2e/fixtures/workflows.ts`, `src/web/e2e/scheduled-tasks.spec.ts` | Repository load evidence, scheduled-task DB runtime proof including failed due-run `next_run_at` advancement, immediate-reclaim prevention, and HTTP active-organization isolation for run-history/status/run-now routes, Workflow SQL active-organization isolation proof, Workflow definition/version/execution-snapshot/node-execution secret response and at-rest proof, Workflow package and standalone runtime gRPC generated-client proof, default Workflow Agent executor wiring plus Workflow-to-Agent planning-control resume proof and standalone direct-client planning/ReAct parity proof, workflow browser proof for trigger/debug paths plus version history, rollback, branch create/publish/merge, resource-check payloads, paused-failure edited-retry decision payloads, and canvas context-menu test-node payload preservation, scheduled-task browser proof, plus target deployment telemetry for external success-rate claims |
| Workflow SQL current delta | `scripts/verify-commercial-db-evidence.sh workflow-sql-isolation`, `scripts/verify-commercial-db-evidence-profiles.sh`, `src/server/internal/workflow/store_test.go`, `src/server/internal/http/server_test.go`, matrix rows 23, 33, and 34 | Profile synchronization now fails if the no-skip Workflow SQL isolation profile omits `TestWorkflowStorePersistsDefinitionsAndExecutions`, `TestWorkflowStorePersistsVersionHistoryAndExecutionVersion`, or `TestCrossTenantWorkflowScopeDeniesReadWriteAndExecution`. The runner and guard now use explicit full test-name alternatives, with a negative fixture rejecting `TestCrossTenantWorkflowScopeDeniesReadWriteAndExecutionSuffix`. This pins repository-local Workflow SQL and HTTP tenant-isolation proof only; target workflow telemetry, deployed gRPC smoke, and final no-skip release proof remain external |
| Auth security current delta | `scripts/verify-commercial-db-evidence.sh auth-security-persistence`, `scripts/verify-commercial-db-evidence-profiles.sh`, `src/server/internal/auth/service_test.go`, `src/server/internal/http/server_test.go`, `src/server/internal/http/auth_response_test.go`, matrix row 33 | Profile synchronization now fails if the no-skip auth security profile omits password-policy/reset/session-revocation persistence, reset-token replay/expiry rejection, SQL rate-limit blocking, register/login/logout flow, repeated-failed-login rate limiting, password-reset confirmation/session revocation, non-test reset-request email non-enumeration, hashed-password storage, raw-password login against stored hashes, session-required `/me`, stable auth response contracts, or sensitive-action rate limiting. The runner and guard now use full test-name tokens for the HTTP route group, and negative fixtures reject suffixed tokens such as `MeRequiresSessionV2`, prefixed groups such as `TestWrongPrefixRegisterLoginMeLogoutFlow`, and whole-group suffix drift. This pins repository-local auth security proof only; broader tenant-isolation proof and final no-skip release proof remain external |
| Commercial profile pattern-shape current delta | `scripts/verify-commercial-db-evidence.sh`, `scripts/verify-commercial-db-evidence-profiles.sh`, `scripts/verify-commercial-db-evidence-profiles-fixtures.sh`, matrix row 34 | Profile synchronization now rejects any `_pattern` assignment that is not an exact anchored full Go test-name regex, either `^TestName$` or `^(TestA|TestB)$`. A negative fixture mutates the billing checkout/top-up HTTP pattern to append `WrongSuffix` after the grouped alternatives and proves the guard rejects that drift before token-only checks can accept it. This pins repository-local no-skip runner integrity only; target/live release evidence remains external |
| Dependency security current delta | `src/web/package.json`, `package-lock.json`, `pnpm-lock.yaml`, `src/web/package-lock.json`, `scripts/verify-dependency-security.sh`, `scripts/verify-quality-gates.sh`, `docs/release/commercial-completion-audit.md`, matrix rows 33 and 34 | The frontend workspace now pins `undici@7.28.0` after the audited `undici@7.27.2` path through `src/web`, `jsdom`, and `vitest` failed `pnpm audit` under vulnerable `<7.28.0` ranges. The dependency security gate now covers all tracked JavaScript lockfiles by running pnpm workspace audit, root `package-lock.json` npm audit, and `src/web/package-lock.json` npm audit before `govulncheck`, with `scripts/verify-quality-gates.sh` guarding those command forms. Current proof uses `pnpm --dir src/web why undici`, `pnpm audit --audit-level=moderate`, root and web `npm audit --audit-level=moderate`, `bash scripts/verify-quality-gates.sh`, and `bash scripts/check.sh security`; this pins repository-local dependency audit evidence only, while target secret audits, target/live evidence, and final no-skip release proof remain external |
| Migration current delta | `scripts/verify-commercial-db-evidence.sh migration-ledger-backfills`, `scripts/verify-commercial-db-evidence-profiles.sh`, `scripts/verify-commercial-db-evidence-profiles-fixtures.sh`, `src/server/cmd/migrate/main_test.go`, matrix rows 30 and 34 | Profile synchronization now fails if the no-skip migration-ledger/backfills profile omits migration ledger replay/skip behavior, checksum mismatch rejection, legacy tenant-scope backfill, or Marketplace category-ID backfill. The guard uses token-boundary full test-name matching, with a negative fixture rejecting `TestApplyMigrationsRecordsLedgerAndSkipsAppliedFilesV2`. This pins repository-local migration ledger and historical backfill compatibility proof only; target deployment migration execution, backup/restore, and final no-skip release proof remain external |
| Scheduled Task runtime current delta | `scripts/verify-commercial-db-evidence.sh scheduled-task-runtime`, `scripts/verify-commercial-db-evidence-profiles.sh`, `scripts/verify-commercial-db-evidence-profiles-fixtures.sh`, `src/server/internal/schedule/store_test.go`, `src/server/internal/http/routes_schedule_test.go`, `src/server/internal/http/routes_workflow_test.go`, matrix row 22 | Profile synchronization now fails if the no-skip scheduled-task runtime profile omits any of its 11 executed tests across scheduled-task SQL create/list/update/run-history/claim/complete/failure/schedule-sync semantics, HTTP task create/list/run-listing, or Workflow schedule-trigger synchronization. The runner and guard now use explicit full test-name alternatives, with a negative fixture rejecting `TestSQLStoreV2CreatesAndListsScheduledTasksByOrganization`. This pins repository-local PostgreSQL scheduled-task runtime proof only; target scheduler telemetry, deployed workflow success-rate evidence, and final no-skip release proof remain external |
| Knowledge and RAG | `docs/reports/2026-06-07-knowledge-requirement-audit.md`, Knowledge/RAG backend and frontend tests | Focused retrieval, chunking, citation, versioning, and UI tests for changed surfaces |
| Agent system | Agent service/store/runtime tests including service-level runtime config model-routing and skills proof, OpenAPI/Web product-path proof for runtime config create/edit controls, durable planning completion-counter service tests, structured plan-step `description`/`dependsOn` coverage, draft dependency-index preservation tests for insert/move/delete, `scripts/verify-commercial-db-evidence.sh agent-runtime-memory`, Agent route and UI tests including action ID alias conflict guards, `src/server/internal/workflow/agent_node_executor_test.go`, `src/server/internal/workflow/agent_client_test.go`, `src/server/internal/http/workflow_executor_test.go`, `src/web/e2e/agent-planning.spec.ts` for planning execution, adjust-plan, token-budget recovery, and advanced runtime config create/edit browser proof, `src/web/e2e/agent-memories.spec.ts`, `src/web/e2e/mcp-servers.spec.ts`, `api/proto/agent.proto`, `src/server/pkg/agent/grpc_server_test.go`, `src/server/cmd/agent/main.go`, `src/server/cmd/agent/main_test.go`, matrix row 24 | ReAct and planning runtime tests including direct PlanningEngine tool-step to ReAct handoff proof, persisted `Agent.Config.ModelRoutingRules` / `Skills` / `MaxSkills` behavior in the mature service `RunWithTools` path, no-skip profile coverage for ReAct model switching, skill selection, tool filtering, skill instruction injection, `call_agent` recursion guard, websearch provider fallback, and Agent ToolRun lifecycle/risk-level persistence, AgentConfig OpenAPI/TypeScript/Workspace create-update controls for model routing rules, skill bundles, and max skill caps, approval/resume tests, built-app token-budget recovery payload proof, durable planning completion-counter convergence tests, memory store/policy tests including LLM-assisted extraction plus `memory_key_consolidate` composition, structured plan-step dependency proof plus draft insert/move/delete dependency-index preservation guards, tool-run and plan-step snake/camel ID alias conflict guards, DB-backed run/tool-run/plan-step/config/memory persistence proof under `TEST_DATABASE_URL` or disposable pgvector PostgreSQL, authenticated gRPC proto/service-boundary tests for create/ReAct/tool approval, token-budget resume, planning continue/adjust, and plan-step approve/execute/skip/retry surfaces, Workflow agent-node resume integration for planning continue/adjust/budget/plan-step controls, standalone Workflow direct Agent client planning/ReAct dispatch and workspace session proof plus direct Workflow AgentClient planning-control adapter proof for continue/adjust/budget/plan-step reload boundaries, standalone Agent runtime registration and generated-client bufconn proof, MCP server browser lifecycle/tool-execution proof, and route/UI/browser tests for changed surfaces |
| Agent browser runtime-config current delta | `src/web/e2e/agent-planning.spec.ts`, `src/web/e2e/fixtures/agentPlanning.ts`, `src/web/src/routes/workspace/AgentsPage.tsx`, `src/web/src/routes/workspace/AgentsPage.test.tsx`, `src/web/src/features/agents/agentsApi.ts`, `src/web/src/features/agents/agentsApi.test.ts`, matrix row 24 | Built-app Playwright coverage now proves `/agents` can create a browser-seeded agent and then update advanced runtime config through fail-closed POST/PUT fixtures that pin `approvalMode`, `defaultExecutionMode`, long-term memory policies, `maxIterations`, `tokenBudget`, `maxSkills`, `modelRoutingRules`, and `skills` payloads, while preserving the existing planning/browser journey coverage. This is local browser/API-payload proof only; target deployed Agent compatibility, target gRPC smoke, and final no-skip release proof remain external |
| Agent runtime profile current delta | `scripts/verify-commercial-db-evidence.sh agent-runtime-memory`, `scripts/verify-commercial-db-evidence-profiles.sh`, `src/server/internal/agent/runner_react_test.go`, `src/server/internal/agent/runner_skill_test.go`, `src/server/internal/agent/store_test.go`, `src/server/internal/agent/tools/`, matrix row 24 | The Agent runtime/memory runner now uses exact test-name alternatives for its ReAct, tool, and SQL persistence groups, and profile synchronization now fails if the no-skip profile omits ReAct model routing/switching, skill selection/tool filtering/instruction injection, `call_agent` registration/recursion guards, websearch primary/fallback/error paths, Agent run/tool-run/plan-step SQL persistence, approval/runtime/memory-policy config persistence, or memory-store persistence evidence. This pins repository-local Agent runtime/profile evidence only; deployed Agent compatibility, target gRPC smoke, target secret audit, target workflow telemetry, and final no-skip release proof remain external |
| Publishing channels | Channel service/adapter/HTTP tests, `src/server/internal/http/channel_handler_test.go`, `scripts/verify-commercial-db-evidence.sh secret-response-safety`, `scripts/verify-commercial-db-evidence.sh publishing-channel-isolation`, `scripts/verify-commercial-db-evidence.sh core-sql-persistence`, built-app browser proof in `src/web/e2e/publishing-channels.spec.ts` with fail-closed payload fixture `src/web/e2e/fixtures/publishingChannels.ts`, and matrix row 25 | Adapter, worker, retry, fallback, archive, alert, SQL config/retry/archive persistence, DB-backed secret-response and active-organization isolation tests, and Workspace browser proof for channel create, edit with redacted secret marker preservation, manual send, forced fallback retry, message-log refresh, and delete/disabled state for changed surfaces |
| Publishing channels current delta | `scripts/verify-commercial-db-evidence.sh publishing-channel-isolation`, `scripts/verify-commercial-db-evidence-profiles.sh`, `scripts/verify-commercial-db-evidence-profiles-fixtures.sh`, `src/server/internal/http/channel_handler_test.go`, matrix row 25 | Profile synchronization now fails if the no-skip Publishing channel isolation profile omits `TestPublishingChannelHTTPRouteEnforcesActiveOrganizationIsolation` or widens its exact anchored `publishing_channel_isolation_pattern="^TestPublishingChannelHTTPRouteEnforcesActiveOrganizationIsolation$"` assignment. Negative fixtures reject both `TestPublishingChannelHTTPRouteEnforcesActiveOrganizationIsolationV2` and `^TestPublishingChannelHTTPRouteEnforcesActiveOrganizationIsolation.*$`. This pins repository-local active-organization isolation proof for Publishing channel list/read/mutation/message/retry routes only; target tenant-isolation evidence, live channel delivery, and final no-skip release proof remain external |
| Publishing Channels browser current delta | `src/web/e2e/publishing-channels.spec.ts`, `src/web/e2e/fixtures/publishingChannels.ts`, `src/web/src/routes/workspace/PublishingChannelsPage.tsx`, `src/web/src/features/layouts/WorkspaceLayout.tsx`, matrix rows 25, 28, and 31 | Built-app Playwright coverage now proves `/publishing` at a 390px mobile viewport keeps a long unbroken channel name, endpoint, edit/test/activate/delete action labels, the send-test channel selector, and long failed-delivery message ID/reason evidence inside the document and Workspace `main` width. The fixture includes a long webhook channel and failed delivery record so the action buttons and message visibility evidence cannot rely on short labels. This pins repository-local Publishing browser/mobile layout evidence only; live channel delivery, target deployment proof, and final no-skip release proof remain external |
| Billing and monetization | Admin/Billing/Relay quota/payment tests, Console API token usage sanitization tests, OpenAPI contract gates, `scripts/verify-commercial-db-evidence.sh`, `scripts/verify-commercial-db-evidence.sh billing-checkout-topup-http`, `scripts/verify-commercial-db-evidence.sh billing-provider-lifecycle`, `scripts/verify-commercial-db-evidence.sh admin-usage-analytics-db`, `scripts/verify-commercial-db-evidence.sh app-stateful-routes`, `scripts/verify-commercial-db-evidence.sh quota-sql-isolation`, `src/web/e2e/console-billing.spec.ts`, `src/web/e2e/console-access.spec.ts`, `src/web/e2e/console-usage.spec.ts`, `src/web/e2e/admin-api-tokens.spec.ts`, `src/web/e2e/admin-usage-logs.spec.ts`, `src/web/e2e/admin-billing-operator.spec.ts`, `src/web/e2e/admin-commercial-config.spec.ts`, matrix row 26 | DB-free handler proof where valid, DB-backed checkout, invoice, subscription, refund lifecycle, no-skip PostgreSQL Billing checkout/top-up HTTP route proof for session guards, tenant payment-intent persistence, provider selection/fail-closed behavior, direct top-up rejection, Stripe webhook ledger/subscription retry behavior, domestic top-up checkout/refund/subscription lifecycle webhooks, no-skip provider lifecycle proof for domestic checkout/refund/subscription bridge mapping and top-up refund status SQL synchronization, Stripe Marketplace install/refund webhook lifecycle proof, Marketplace settlement economics and lifecycle proof for segmented platform fees, spec revenue tiers, minimum-settlement cycle gating, paid-install settlement creation idempotency, refund settlement adjustment idempotency, selected-provider lifecycle idempotency keys, `marketplace_install` payment-intent kind migration contract, revenue-tier disclosure for segmented fees, and preservation of the legacy local/manual payout state boundary while configured payout adapters are covered, hosted Alipay/WeChat Pay checkout metadata parity for Marketplace paid installs, Marketplace publisher-stats HTTP PostgreSQL proof for gross, platform, net, refund, available, and pending settlement amounts, and Admin Billing webhook-event inspection that omits raw provider payloads while preserving sanitized ledger metadata, Admin usage daily aggregate refresh/query plus zero-token fallback, Quota SQL tenant-isolation and lifecycle aggregation proof for usage-limit settings store round-trip, Admin usage-limit settings route PostgreSQL persistence/listing, user-scoped balances, subscription request-cap fallback, no-credit-before-webhook top-up checkout, legacy direct top-up rejection, Admin user quota persistence/audit, Admin refund quota reversal, and Stripe top-up/refund balance accounting under `TEST_DATABASE_URL` or disposable pgvector PostgreSQL, user-visible Console usage proof without provider/channel route leakage, no-skip app-router PostgreSQL proof that recorded Chat requests are reflected in the Console usage summary, browser proof for Console subscription checkout and Console usage analytics/recent relay rendering, Admin API token filter/revoke browser proof, API-token lifecycle payload propagation, Admin usage filter/cost analytics, Admin Billing operator payout paid/failed plus mobile table/form containment and Stripe/domestic CNY top-up refund payload/state coverage, PostgreSQL proof that Admin top-up refund operator evidence is persisted to refund/lifecycle ledgers and that missing operator evidence does not mutate ledger or quota state, and Admin commercial configuration browser proof for plan request-token caps, user entitlement updates, user-level quota allocation, Relay pricing multipliers, and usage-limit request-token caps, plus provider proof for external rails |
| Relay pricing group multiplier current delta | `src/server/internal/relay/pricing.go`, `src/server/internal/relay/billing.go`, `src/server/internal/relay/router.go`, `src/server/internal/relay/pricing_test.go`, `src/server/internal/relay/billing_test.go`, `src/server/internal/relay/router_test.go`, `docs/product/pricing.md`, matrix row 26 | Admin-saved `groupMultipliers` are now stored in the Relay pricing runtime and applied to trusted user-group cost calculation. `BillingHook` pre/post billing uses `BillingSession.UserGroup`; `RouteWithBilling` reads `TrustedUserGroupFromContext` and applies the multiplier to quota preconsume, API-token quota preauthorization and settlement, and success usage cost records before channel cost multipliers. Verification: `GOCACHE=/tmp/oblivious-go-cache GOMODCACHE=/tmp/oblivious-go-mod-cache go test ./internal/relay -run 'PricingCalculateCostForGroup|BillingHook_.*GroupMultiplier|RouteWithBillingAppliesTrustedUserGroupPricingMultiplier' -count=1`, `GOCACHE=/tmp/oblivious-go-cache GOMODCACHE=/tmp/oblivious-go-mod-cache go test ./internal/relay -count=1`, docs gate, and diff hygiene |
| Console Billing browser current delta | `src/web/e2e/console-billing.spec.ts`, `src/web/e2e/fixtures/consoleBilling.ts`, `src/web/src/features/console/components/ConsoleWorkbenchLayout.tsx`, `src/web/src/routes/console/BillingPage.tsx`, matrix rows 26, 28, and 31 | Built-app Playwright coverage now proves `/console/billing` can start both subscription checkout with selected package/provider and top-up checkout with selected amount/provider. The fail-closed fixture rejects checkout requests unless subscription submits `kind=subscription`, `packageId=pkg_pro`, `provider=wechatpay`, and top-up submits `kind=topup`, `amount=37.5`, `provider=wechatpay`. The same route-level browser proof now covers a 390px mobile viewport, active Console navigation, semantic main landmark, visible billing controls, and no document/main overflow while the 640px invoice table is contained in its local horizontal-scroll viewport. This is local browser/API-payload/layout evidence only; live Stripe, Alipay, and WeChat Pay rails plus target reconciliation remain external proof |
| Console Access browser current delta | `src/web/e2e/console-access.spec.ts`, `src/web/e2e/fixtures/consoleAccess.ts`, `src/web/src/routes/console/AccessPage.tsx`, matrix rows 26, 28, 31, and 33 | Built-app Playwright coverage now proves `/console/access` at a 390px mobile viewport keeps API-token creation controls, token rows, long unbroken model-limit labels, raw token output, workspace/session labels, and token usage rows inside the document/main/Console canvas width. The fixture uses a long unbroken model identifier so token model-limit rendering cannot rely on short labels. This pins repository-local Console API-token browser/mobile layout evidence only; target tenant-isolation, target usage telemetry, live provider rails, and final no-skip release proof remain external |
| Console Notifications browser current delta | `src/web/e2e/console-notifications.spec.ts`, `src/web/e2e/fixtures/consoleNotifications.ts`, `src/web/src/routes/console/NotificationsPage.tsx`, matrix rows 28, 29, and 31 | Built-app Playwright coverage now proves `/console/notifications` at a 390px mobile viewport keeps unread-count controls, long unbroken alert titles, messages, categories, mark-read action labels, and delete action labels inside the document/main/Console canvas width. The fixture includes a long unread notification so the action buttons cannot rely on short titles. This pins repository-local notification browser/mobile layout evidence only; target live notification-provider operations, target deployment proof, and final no-skip release proof remain external |
| Billing checkout current delta | `scripts/verify-commercial-db-evidence.sh billing-checkout-topup-http`, `scripts/verify-commercial-db-evidence-profiles.sh`, `src/server/internal/http/stripe_handler_test.go`, `src/server/internal/http/payment_provider_config_test.go`, matrix rows 26, 31, and 34 | Profile synchronization now fails if the no-skip Billing checkout/top-up HTTP profile omits session guard proof, tenant payment-intent persistence, explicit/configured provider routing, Console provider discovery for configured checkout creators, unconfigured-provider and checkout-creator failure behavior, no-credit-before-webhook top-up handling, legacy direct top-up rejection, domestic webhook signature/event recording and top-up/refund/subscription lifecycle handling, or Stripe invalid-signature/event-ledger/subscription retry proof. The runner now uses explicit full test-name alternatives and the guard uses token-boundary matching so suffixed lookalikes such as `TestBillingCheckoutRequiresSessionV2` cannot satisfy checkout evidence. This pins repository-local Billing checkout/top-up HTTP evidence only; live provider checkout/refund/reconciliation and final no-skip release proof remain external |
| Billing provider lifecycle current delta | `scripts/verify-commercial-db-evidence.sh billing-provider-lifecycle`, `scripts/verify-commercial-db-evidence-profiles.sh`, `src/server/internal/stripe/lifecycle_test.go`, matrix row 26 | Profile synchronization now fails if the no-skip Billing provider lifecycle profile omits checkout-completed billing lifecycle metric emission, top-up refund status SQL synchronization, domestic checkout/marketplace/refund/subscription bridge proof, Stripe checkout subscription/top-up idempotency, invoice paid/failed transitions, subscription update/delete transitions, or refund quota-adjustment evidence. This pins repository-local provider lifecycle and metric proof only; live Stripe, Alipay, and WeChat Pay checkout/refund/payout/reconciliation rails, target observability telemetry, and final no-skip release proof remain external |
| Admin usage analytics current delta | `scripts/verify-commercial-db-evidence.sh admin-usage-analytics-db`, `scripts/verify-commercial-db-evidence-profiles.sh`, `src/server/internal/admin/usage_daily_aggregate_test.go`, matrix row 26 | Profile synchronization now fails if the no-skip Admin usage analytics DB profile omits PostgreSQL daily aggregate refresh/query, raw analytics zero-total-token fallback, or usage-log listing zero-total-token fallback evidence. The runner uses explicit full test-name alternatives so the guard pins the exact Go tests instead of only a grouped regex suffix. This pins repository-local usage analytics evidence integrity only; target usage telemetry, live provider-cost accuracy, and final no-skip release proof remain external |
| Console Usage current delta | `src/web/e2e/console-usage.spec.ts`, `src/web/e2e/fixtures/consoleUsage.ts`, `src/web/src/routes/console/UsagePage.tsx`, matrix rows 26, 28, and 31 | The Console Usage browser fixture now rejects unexpected query parameters on `GET /api/v1/console/usage`; a RED Playwright run showed `?period=30d` still returned `200` before the guard, and the GREEN run proves it returns the fixture contract-mismatch envelope while the normal built-app usage page still renders. Built-app Playwright proof now also covers a 390px mobile viewport with active Console navigation, semantic main landmark, By model/feature/user/daily analytics regions, locally scrollable analytics tables, and wrapped recent relay request identifiers so document/main do not gain horizontal overflow. This pins repository-local frontend/API fixture and mobile layout evidence only; target usage telemetry, live provider evidence, target deployment proof, and final no-skip release proof remain external |
| Console overview/models current delta | `src/web/e2e/console-overview.spec.ts`, `src/web/e2e/fixtures/consoleOverview.ts`, `src/web/src/routes/console/ModelsPage.tsx`, matrix rows 28 and 31 | Built-app Playwright proof now covers `/console` KPI drill-down rendering for billing, usage, top model, and access posture, then navigates to `/console/models` and proves active Console navigation, workspace context, model summaries, and access sibling navigation. The fixture also rejects unexpected query parameters on `GET /api/v1/console/models`. Built-app Playwright proof now additionally covers a 390px mobile viewport with a long unbroken model identifier, active Console navigation, semantic main landmark, workspace scope, access sibling navigation, and no document/main/Console canvas horizontal overflow after the Models list gained explicit wrapping boundaries. This pins repository-local Console overview/model browser and mobile layout evidence only; target usage telemetry, target deployment proof, and final no-skip release proof remain external |
| App stateful routes current delta | `scripts/verify-commercial-db-evidence.sh app-stateful-routes`, `scripts/verify-commercial-db-evidence-profiles.sh`, `scripts/verify-commercial-db-evidence-profiles-fixtures.sh`, `src/server/internal/http/server_test.go`, `src/server/internal/http/notification_handler_test.go`, `src/server/internal/http/task_handler_test.go`, `src/server/internal/http/route_surface_test.go`, matrix rows 26, 28, 31, 33, and 34 | Profile synchronization now fails if the no-skip app stateful routes profile omits Console API token or usage routes, organization session/invitation ownership flows, notification ownership, preferences initialization/update, conversation/message/config route-state persistence, Task approval workspace/status proof, or app-route session/CSRF surface proof. The runner and guard now use explicit full test-name alternatives, with a negative fixture rejecting `TestLegacyConsoleAPITokenCreateListAndRevoke`. This pins repository-local app stateful route persistence, ownership, and auth/CSRF evidence only; target usage telemetry, target tenant-isolation evidence, deployed gRPC smoke, target secret audit, and final no-skip release proof remain external |
| Quota SQL current delta | `scripts/verify-commercial-db-evidence.sh quota-sql-isolation`, `scripts/verify-commercial-db-evidence-profiles.sh`, `src/server/internal/quota/service_test.go`, `src/server/internal/http/server_test.go`, `src/server/internal/http/admin_marketplace_handler_test.go`, `src/server/internal/http/admin_billing_handler_test.go`, `src/server/internal/stripe/lifecycle_test.go`, matrix row 26 | Profile synchronization now fails if the no-skip quota SQL profile omits usage-limit settings persistence, user-scoped balance mode, billing-session organization scope, top-up mutation organization scope, subscription request-cap fallback, active public package listing, active-organization quota scope, Admin usage-limit persistence, Admin user quota persistence/audit, no-credit-before-webhook top-up checkout, legacy direct top-up rejection, Admin refund quota reversal, Stripe top-up fulfillment, or Stripe refund reversal. The runner and guard now use explicit full test-name alternatives, with a negative fixture rejecting `UsageLimitSettingsRoundTripV2`, so suffixed quota evidence names cannot satisfy the required SQL usage-limit proof. This pins repository-local quota lifecycle and tenant-isolation proof only; target quota enforcement, live provider rails, and final no-skip release proof remain external |
| Quota observability current delta | `scripts/verify-commercial-db-evidence.sh quota-sql-isolation`, `scripts/verify-commercial-db-evidence-profiles.sh`, `src/server/internal/quota/service_test.go`, matrix row 26 | Profile synchronization now fails if the no-skip quota SQL profile omits `TestQuotaObservabilityRecordsSettlementFailure`, and the profile runs it as a separate quota observability settlement-failure group. This pins repository-local quota settlement-failure metric evidence only; target quota telemetry, live provider rails, and final no-skip release proof remain external |
| Billing and Marketplace current delta | `scripts/verify-commercial-db-evidence.sh marketplace-money-movement`, `scripts/verify-commercial-db-evidence-profiles.sh`, `src/server/internal/admin/billing_store_test.go`, `src/server/internal/marketplace/settlement_test.go`, `src/server/internal/http/admin_billing_handler_test.go`, `src/server/internal/http/marketplace_payment_provider_test.go`, `src/server/internal/http/payment_provider_config_test.go`, matrix rows 26, 27, and 34 | Profile synchronization now body-pins every no-skip Marketplace money-movement test token across Admin Billing SQL-shape provider filters/refund SQL updates, Marketplace settlement checkout/selected-provider/currency/failure/economics/refund/audit/payout/publisher-stats lifecycle tests, Admin Billing money-movement state/filter/list-handler filter pass-through plus payout/refund/webhook-event routes, including payout paid/failed handler rejection before service mutation when provider payout ID or failure reason evidence is missing, Marketplace configured provider discovery, paid-install checkout fail-closed and no-install-before-webhook routes, hosted domestic checkout construction, Stripe Marketplace install/refund webhooks, and domestic Marketplace install/refund/payout webhooks, including handler-level domestic payout lifecycle adapter proof. The runner and guard now use explicit full test-name alternatives for the grouped Marketplace, Settlement, Admin Billing, and domestic Marketplace webhook families, with a negative fixture rejecting `TestDomesticPaymentWebhookRouteAppliesMarketplaceV2InstallSettlementOnce`. This is repository-local provider discovery, checkout-routing, inspection, settlement, refund, payout, webhook, and audit-retention evidence integrity only and does not replace live provider checkout/refund/payout/reconciliation proof |
| Marketplace governance current delta | `scripts/verify-commercial-db-evidence.sh marketplace-governance-review`, `scripts/verify-commercial-db-evidence-profiles.sh`, `src/server/internal/marketplace/governance_test.go`, `src/server/internal/http/admin_marketplace_handler_test.go`, matrix row 27 | The Marketplace governance/review runner now uses exact test-name alternatives for its service and HTTP route groups, and profile synchronization now fails if the no-skip profile omits takedown/appeal/abuse-report/publisher-notification/needs-changes/automated-review persistence, admin review SLA, governance route, abuse-report route, Admin review audit organization evidence, publish-time automated review, or admin abuse-report listing evidence. This pins repository-local governance/review/audit evidence only; live marketplace moderation, provider rails, target operations, and final no-skip release proof remain external |
| Marketplace ecosystem | Marketplace service/settlement/governance/search tests, `scripts/verify-commercial-db-evidence.sh`, `scripts/verify-commercial-db-evidence.sh marketplace-governance-review`, `scripts/verify-commercial-db-evidence.sh marketplace-recommendation-search`, `scripts/verify-commercial-db-evidence.sh marketplace-template-routes`, `src/server/migrations/0082_marketplace_audit_retention.sql`, Marketplace recommendation OpenAPI/schema contract checks, `src/web/e2e/admin-marketplace.spec.ts`, `src/web/e2e/admin-reviews.spec.ts`, and matrix row 27 | Provider checkout, settlement, payout, review, governance, UI, recommendation metadata, and DB-backed lifecycle proof for changed surfaces, including no-skip PostgreSQL governance/review/abuse-report/SLA evidence, no-skip PostgreSQL recommended search proof for content relevance, ranking signals, collaborative filtering, exploration, and governance demotion, no-skip PostgreSQL template route proof for create/list/detail/install persistence, no-skip paid audit-retention proof that publisher delete is rejected after order evidence exists, direct SQL delete is blocked by non-cascading audit FKs, buyer uninstall preserves paid order/settlement rows, no-skip settlement economics and lifecycle proof for segmented fees, revenue tiers, minimum-settlement gating, paid-install settlement creation, refund settlement adjustment, selected-provider lifecycle keys, `marketplace_install` payment-intent kind migration, revenue-tier disclosure, and local/manual payout state preservation, local Marketplace paid-install Alipay/WeChat Pay provider-selection and checkout-metadata parity across HTTP, hosted checkout creator, settlement, API, page, and browser tests, browser-level recommended-card metadata proof, browser-level Admin Reviews moderation/governance proof, and browser-level My Agents publisher mutation proof for template creation, settlement-cycle update, publisher delete, and installed-agent uninstall |
| Marketplace recommendation current delta | `scripts/verify-commercial-db-evidence.sh marketplace-recommendation-search`, `scripts/verify-commercial-db-evidence-profiles.sh`, `scripts/verify-commercial-db-evidence-profiles-fixtures.sh`, `src/server/internal/marketplace/search_test.go`, matrix row 27 | Profile synchronization now fails if the no-skip Marketplace recommendation-search profile omits content-relevance ranking, deterministic exploration fallback, ranking signals, requester collaborative filtering, or governance-demotion proof. The runner and guard now use explicit full test-name alternatives, with a negative fixture rejecting `TestSearchAgentsRecommendedV2RanksContentMatchesOverGenericHotAgents`. This pins repository-local PostgreSQL recommendation-search evidence only; live marketplace traffic, provider rails, target recommendation-quality telemetry, and final no-skip release proof remain external |
| Marketplace template current delta | `scripts/verify-commercial-db-evidence.sh marketplace-template-routes`, `scripts/verify-commercial-db-evidence-profiles.sh`, `src/server/internal/http/marketplace_templates_handler_test.go`, `src/server/internal/http/marketplace_route_surface_test.go`, matrix row 27 | Profile synchronization now fails if the no-skip Marketplace template profile omits `TestMarketplaceTemplateRoutesCreateListDetailAndInstall` or `TestMarketplaceRouterRegistersTemplateAndPublisherPreferenceRoutes`, and the guard uses token-boundary matching so suffixed lookalikes such as `MarketplaceTemplateRoutesCreateListDetailAndInstallV2` cannot satisfy the template route proof requirement. This pins repository-local template create/list/detail/install persistence plus template, publisher-preference, stats, my-agents, install, agent-stats, and admin abuse-report route-surface registration proof only; live marketplace operations and final no-skip release proof remain external |
| Marketplace template browser current delta | `src/web/e2e/admin-marketplace.spec.ts`, `src/web/e2e/fixtures/adminMarketplace.ts`, matrix rows 27, 28, and 31 | Built-app `/marketplace` browser proof now renders the Templates browse section and installs `Launch Browser Template` through canonical `POST /api/v1/marketplace/templates/template_launch_browser/install`. The fixture seeds the browse template list and only fulfills that canonical install route, so route drift fails closed through the fixture 404 path. This pins repository-local fixture-backed browser/API proof for Marketplace template install from the canonical browse route; live marketplace operations, target traffic, and final no-skip release proof remain external |
| Settings preferences browser current delta | `src/web/src/routes/workspace/SettingsPage.tsx`, `src/web/src/routes/workspace/SettingsPage.test.tsx`, `src/web/e2e/settings.spec.ts`, `src/web/e2e/fixtures/settings.ts`, matrix rows 28, 31, and 34 | Built-app `/settings` browser proof now saves editable workspace preferences while preserving extended `UserPreferences` fields from the authenticated session, including `defaultAgentModel`, `sidebarCollapsed`, and nested `notifications`. The fail-closed fixture only accepts `PUT /api/v1/app/me/preferences` when those fields and `onboardingCompleted` survive alongside the edited `defaultMode`, `modelStrategy`, and `networkEnabledHint` values. This pins repository-local fixture-backed browser/API proof for Settings preference-save payload shape only; target deployment proof and final no-skip release evidence remain external |
| Frontend shell and pages | Real app-router regression tests, Playwright browser journeys including `src/web/e2e/agent-planning.spec.ts`, `src/web/e2e/agent-memories.spec.ts`, `src/web/e2e/mcp-servers.spec.ts`, `src/web/e2e/chat-solo.spec.ts`, `src/web/e2e/workflows.spec.ts`, `src/web/e2e/scheduled-tasks.spec.ts`, `src/web/e2e/publishing-channels.spec.ts`, `src/web/e2e/console-overview.spec.ts`, `src/web/e2e/console-billing.spec.ts`, `src/web/e2e/console-access.spec.ts`, `src/web/e2e/console-usage.spec.ts`, `src/web/e2e/console-notifications.spec.ts`, `src/web/e2e/admin-api-tokens.spec.ts`, `src/web/e2e/admin-audit-log.spec.ts`, `src/web/e2e/admin-users.spec.ts`, `src/web/e2e/admin-usage-logs.spec.ts`, `src/web/e2e/admin-billing-operator.spec.ts`, `src/web/e2e/admin-commercial-config.spec.ts`, `src/web/e2e/admin-channels.spec.ts`, `src/web/e2e/admin-models.spec.ts`, `src/web/e2e/admin-marketplace.spec.ts`, `src/web/e2e/admin-reviews.spec.ts`, and `src/web/e2e/admin-alerts.spec.ts`, built-app Chat realtime WebSocket and message-action/mobile-rail proof in `src/web/e2e/chat-solo.spec.ts`, Chat realtime behavior in `src/web/src/routes/workspace/ChatPage.behavior.test.tsx`, page-focused Vitest suites, matrix row 28 | Route-level shell proof plus remaining page-specific workflow/browser tests, Chat mobile conversation-rail no-horizontal-overflow proof, Chat copy/edit/bookmark/share/fork/regenerate/delete browser proof, Publishing Channels create/edit/send/fallback-retry/delete browser proof, Admin API token filter/revoke browser proof, Admin Audit Log organizationID filter proof, Admin Users planID filter proof, Admin Channels create/edit/test/model detect/apply/batch-disable/delete browser proof, Admin Models inventory filter/sort/cost/margin browser proof, Admin Billing mobile no-horizontal-overflow proof for Admin shell, operator tables, and payout failure form, Console overview/model drill-down proof, Console usage analytics/recent relay browser proof, Console notification unread/read/delete controls, `pnpm --dir src/web exec tsc --noEmit`, targeted Vitest, and targeted Playwright for changed routes; target deployment proof still remains external |
| Observability and recovery | `docs/reports/2026-06-07-alert-requirement-audit.md`, `docs/release/recovery-platform-contract.md`, `src/server/internal/http/middleware_test.go`, `src/server/internal/http/server_test.go`, `src/server/internal/http/observability_alert_handler_test.go`, `src/server/internal/observability/recovery_test.go`, `scripts/verify-commercial-db-evidence.sh secret-response-safety`, `scripts/verify-commercial-db-evidence.sh observability-alert-recovery-persistence`, `src/web/e2e/admin-usage-logs.spec.ts`, `src/web/e2e/admin-alerts.spec.ts`, dashboard and deployment contract verifiers | Repository checks plus DB-backed alert routing, alert lifecycle, delivery history, notification throttle, and recovery cooldown persistence proof, Admin usage-log browser analytics proof, and Admin Alerts browser proof for filter/query, acknowledge/resolve, delivery history, recovery-action inspection, routing, and provider-test controls, while target platform proof is still required for true OOM/crash restart execution, scale-down, and failover claims |
| API contract | `docs/api/openapi.yaml`, `scripts/verify-openapi-contract.sh`, Chat realtime WebSocket schema refs for `ChatRealtimeClientMessage` and `ChatRealtimeEvent`, Chat realtime browser frame checks in `src/web/e2e/chat-solo.spec.ts`, Chat message action payload checks in `src/web/e2e/fixtures/chatSolo.ts`, route-surface manifest registration/security/admin-dispatch tests, Agent action ID alias conflict tests, Agent/Workflow/Task gRPC service-boundary tests, `src/web/e2e/console-overview.spec.ts` fixture query checks, `src/web/e2e/console-access.spec.ts` fixture payload checks, `src/web/e2e/console-usage.spec.ts` fail-closed usage route checks, `src/web/e2e/console-notifications.spec.ts` fixture query/payload checks, `src/web/e2e/admin-api-tokens.spec.ts` and `src/web/e2e/fixtures/adminApiTokens.ts` fixture query/revoke checks, `src/web/e2e/admin-audit-log.spec.ts` and `src/web/e2e/fixtures/adminAuditLog.ts` fixture query checks, `src/web/e2e/admin-users.spec.ts` and `src/web/e2e/fixtures/adminUsers.ts` fixture query checks, `src/web/e2e/admin-usage-logs.spec.ts` fixture query checks, `src/web/e2e/agent-memories.spec.ts` fixture query/payload checks, `src/web/e2e/mcp-servers.spec.ts` fixture query/payload checks, `src/web/e2e/workflows.spec.ts` fixture payload checks, `src/web/e2e/fixtures/publishingChannels.ts` fixture payload checks, `src/web/e2e/admin-billing-operator.spec.ts` fixture query/payload checks, `src/web/e2e/admin-commercial-config.spec.ts` fixture query/payload checks, `src/web/e2e/fixtures/adminChannels.ts` fixture query/create/update/test/model-update/batch/delete checks, `src/web/e2e/fixtures/adminModels.ts` fixture query/sort checks, `src/web/e2e/admin-marketplace.spec.ts` fixture query/payload checks, `src/web/e2e/admin-reviews.spec.ts` fixture query/payload checks, `src/web/e2e/admin-alerts.spec.ts` fixture query/payload checks | Contract gate plus focused route tests for every changed request/response family, including Chat realtime WebSocket client/server frame schemas and payload refs, Chat copy/edit/bookmark/share/fork/regenerate/delete browser payload drift checks, Console-only usage schemas that omit internal provider/channel route fields, Console overview/model browser proof for `GET /api/v1/console/models`, Console Usage browser proof for `GET /api/v1/console/usage`, `MarketplacePublishedAgent.recommendation` schema fidelity for score/reason recommendation metadata, browser payload checks for high-value Console notification unread-count/mark-read/mark-all-read/delete controls, Agent run tool/plan-step action snake/camel ID alias conflict checks, Agent Memory, MCP server create/connect/disconnect/diagnose/list-tools/execute/delete plus raw auth-token omission and SQL listing non-hydration, Workflow rollback/branch publish/merge/resource-check/paused-failure decisions, Publishing channel create/update/send/fallback-retry/delete payload checks with redacted secret marker preservation, Admin API token query/revoke propagation checks, Admin Audit Log `organizationID` query propagation checks, Admin Users `planID` query propagation checks, Admin Channels create/update/test/model-detect/model-apply/batch/delete payload checks, Admin Models inventory query propagation checks, Admin Billing, Admin Commercial Config, Marketplace My Agents and recommended search, Admin Reviews, and Admin Alerts mutations, Admin usage-log/analytics query propagation checks, Admin Billing payout/top-up query propagation checks, domestic top-up refund payload checks for `currency=cny` and omitted Stripe charge evidence, Admin Commercial Config plan/user/settings/model-route CRUD payload checks, Marketplace My Agents template/settlement/delete/uninstall payload checks, Admin Reviews review/abuse/governance payload checks, Admin Alerts filter/routing/provider payload checks, manifest-driven Admin route positive dispatch under signed admin sessions, Agent gRPC planning control-plane request/response mapping checks, Workflow/Task generated-client bufconn dispatch checks, and Workflow definition/execution-snapshot/node-execution secret redaction schema notes |
| Deployment and operations | `docs/release/v07-operations-evidence.md`, `docs/release/rc-checklist.md`, deployment and backup/restore scripts, `deploy/docker/Dockerfile.agent`, `deploy/docker/Dockerfile.workflow`, `deploy/docker/Dockerfile.task`, `docker-compose.yml`, tracked `deploy/kubernetes/*.yaml`, `deploy/kubernetes/network-policy.yaml`, `scripts/verify-deployment-operations-contract.sh`, `scripts/k8s-validate.sh`, `scripts/target-grpc-smoke.sh` | Repository deployment contract proof for Agent, Workflow, and Task HTTP plus gRPC port registration, Workflow/Task probes, Kafka broker wiring, static Secret/ConfigMap reference contract checks across tracked Kubernetes manifests, first-party pod/container security context gates, NetworkPolicy default-deny plus ingress/egress allowlist proof, then `scripts/deploy-validate.sh`, `OBLIVIOUS_K8S_SECRET_FILE=/path/outside/git/secret.yaml bash scripts/k8s-validate.sh`, `scripts/backup-restore-smoke.sh`, and validation-only generated-client target gRPC smoke on the target installation |
| Security and tenant isolation | Security row 33, auth/CSRF route-surface tests, tenant isolation tests, Agent action ID alias conflict guards, secret-redaction contracts, Admin Relay channel API key at-rest encryption proof, Observability alert-provider config at-rest encryption proof, Publishing channel config at-rest encryption proof, Workflow definition/runtime payload at-rest encryption proof, MCP auth-token at-rest protection proof, `scripts/verify-commercial-db-evidence.sh app-stateful-routes`, `scripts/verify-commercial-db-evidence.sh tenant-membership-lifecycle`, `scripts/verify-commercial-db-evidence.sh tenant-cross-surface`, `scripts/verify-commercial-db-evidence.sh marketplace-governance-review`, `scripts/verify-commercial-db-evidence.sh secret-response-safety`, `scripts/verify-commercial-db-evidence.sh auth-security-persistence`, `scripts/verify-commercial-db-evidence.sh relay-file-mapping-tenant-ownership`, `scripts/verify-commercial-db-evidence.sh relay-runtime-channel-isolation`, `scripts/verify-commercial-db-evidence.sh workflow-sql-isolation`, `scripts/verify-commercial-db-evidence.sh publishing-channel-isolation`, `scripts/verify-commercial-db-evidence.sh admin-relay-channel-isolation`, `scripts/verify-commercial-db-evidence.sh admin-relay-read-isolation`, `scripts/verify-commercial-db-evidence.sh observability-alert-recovery-persistence`, `scripts/verify-commercial-db-evidence.sh quota-sql-isolation`, `scripts/verify-commercial-db-evidence.sh core-sql-persistence`, `src/web/e2e/console-access.spec.ts` provider/channel omission checks, `src/web/e2e/mcp-servers.spec.ts` raw-token omission checks, `src/web/e2e/admin-alerts.spec.ts` routing/provider mutation payload checks | DB-free guards where valid, DB-backed auth-security, app-stateful Task approval state/workspace guard proof, tenant-isolation including Admin organization create/list/detail/update/archive/member route persistence, Chat share/fork/config/attachment/citation/persona persistence, Scheduled Task run-history/status/run-now active-organization denial and no-mutation proof, Marketplace governance/review route authorization and persistence, Agent run tool/plan-step action alias conflict fail-closed proof, Relay file ownership for upload, mapped get, and mapped list surfaces, Relay runtime active-organization channel selection plus Admin Relay API-key at-rest encryption/runtime decryption, Relay semantic-cache SQL persistence, Observability provider at-rest encryption/runtime decryption, Observability alert routing/recovery persistence, Publishing channel config/retry/archive persistence and active-organization isolation, Publishing channel config at-rest encryption/runtime decryption, Workflow definition/runtime payload at-rest encryption/runtime decryption, Workflow active-organization isolation, Admin Relay channel and read-surface active-organization isolation, Quota SQL tenant isolation including Admin usage-limit route session-organization override proof and Admin user quota route persistence/audit evidence, Admin Billing webhook-event raw-payload non-disclosure proof, browser-level Console usage sanitization proof, browser-level MCP auth-token non-disclosure proof plus PostgreSQL auth-token ciphertext/GetServer hydration/ListServers non-hydration proof, browser-level Admin Alerts routing/provider mutation proof, and secret-response proof for Observability, Publishing, Admin Relay, Workflow, and MCP persistence surfaces, plus dependency scans |
| Tenant membership current delta | `scripts/verify-commercial-db-evidence.sh tenant-membership-lifecycle`, `scripts/verify-commercial-db-evidence-profiles.sh`, `src/server/internal/tenant/service_test.go`, `src/server/internal/http/server_test.go`, `src/server/internal/http/tenant_handler_test.go`, matrix row 33 | Profile synchronization now fails if the no-skip Tenant membership lifecycle profile omits SQL organization lifecycle, SQL membership invitation ownership lifecycle, default organization/session creation, legacy-user default organization resolution, organization selection scope, invitation revoke rejection, session security on membership changes, member list/ownership transfer/remove routes, or Admin organization route persistence. The runner uses explicit full test-name alternatives so the guard pins exact Tenant and HTTP tests instead of only grouped regex suffixes. This pins repository-local tenant lifecycle evidence integrity only; target tenant-isolation evidence and final no-skip release proof remain external |
| Tenant cross-surface current delta | `scripts/verify-commercial-db-evidence.sh tenant-cross-surface`, `scripts/verify-commercial-db-evidence-profiles.sh`, `scripts/verify-commercial-db-evidence-profiles-fixtures.sh`, `src/server/internal/http/server_test.go`, `src/server/internal/http/agent_handler_test.go`, matrix row 33 | Profile synchronization now fails if the no-skip tenant cross-surface profile omits active-organization isolation proof for Chat, Knowledge, Console usage, Agent conversation/run/tool-run routes, Memory, MCP, Quota, Scheduled Task, Marketplace publisher, or Marketplace settlement preferences. The runner and guard now use explicit full test-name alternatives, with a negative fixture rejecting `TestLegacyCrossTenantChatScopeUsesActiveOrganization`. This pins repository-local cross-surface tenant-isolation evidence only; target tenant-isolation evidence, provider-secret audits, and final no-skip release proof remain external |
| Observability current delta | `scripts/verify-commercial-db-evidence.sh observability-alert-recovery-persistence`, `scripts/verify-commercial-db-evidence-profiles.sh`, `src/server/internal/observability/alert_state_sql_store_test.go`, matrix row 34 | Profile synchronization now fails if the no-skip observability alert/recovery profile omits routing-rule persistence, alert lifecycle/escalation persistence, alert state list/filter persistence, notification throttle/recovery cooldown persistence, or repeated delivery-batch evidence. The runner and profile guard now use full test-name tokens, so prefixed or suffixed lookalikes such as `TestSQLAlertV2RoutingRuleStorePersistsRoutingRules` or `TestSQLAlertStateStoreListsAlertStatesWithFiltersV2` cannot satisfy the proof requirement. This pins repository-local alert/recovery persistence proof only; target platform recovery and failover evidence remain external |
| Secret response current delta | `scripts/verify-commercial-db-evidence.sh secret-response-safety`, `scripts/verify-commercial-db-evidence-profiles.sh`, `scripts/verify-commercial-db-evidence-profiles-fixtures.sh`, `src/server/internal/http/observability_alert_handler_test.go`, `src/server/internal/http/channel_handler_test.go`, `src/server/internal/http/admin_channel_secret_response_test.go`, `src/server/internal/http/workflow_secret_response_test.go`, `src/server/internal/mcp/client_test.go`, matrix row 33 | Profile synchronization now fails if the no-skip secret-response profile omits Observability alert-provider SQL redaction, Publishing channel config redaction, Admin Relay API-key redaction, Workflow secret redaction, or MCP auth-token at-rest/listing protection. The runner and profile guard now use full test-name tokens plus an exact `mcp_auth_token_pattern="^TestSQLStoreProtectsAuthTokenWithPostgres$"` assignment, so prefixed, suffixed, or widened lookalikes such as `TestLegacyObservabilityAlertAdminRouteSQLProviderSecretsAreRedacted`, `TestObservabilityAlertAdminRouteSQLProviderSecretsAreRedactedV2`, or `^TestSQLStoreProtectsAuthTokenWithPostgres.*$` cannot satisfy the proof requirement. This pins repository-local redaction and at-rest evidence only; target secret audits remain external evidence |
| Migration and release readiness | `docs/reports/2026-06-08-migration-schema-audit.md`, migration verifiers, this evidence pack, `scripts/verify-commercial-db-evidence.sh core-sql-persistence`, `scripts/verify-commercial-db-evidence.sh migration-ledger-backfills` | Final strict verifier, release evidence logs, all DB-backed commercial evidence profiles including no-skip migration ledger/checksum/backfill proof, deployment validation, backup/restore smoke, unresolved-risk signoff |

API contract route-surface note: `src/server/internal/http/route_surface_test.go` now includes `TestRouteSurfaceRuntimeAPIRoutesAreDocumentedInManifestWithoutDatabase`, which parses active runtime router registrations and fails if a literal `/api/` `mux.Handle` or `mux.HandleFunc` registration is not covered by `docs/api/route-surface-manifest.json`. It also includes `TestRouteSurfaceDeclaredRouteRegistrarsAreMountedWithoutDatabase`, which fails if a non-test `routes_*.go` file declares a `register*Routes` helper that `NewRouterWithOptions` does not call. The OpenAPI contract gate then keeps the route-surface manifest synchronized with `docs/api/openapi.yaml`.

## Required Final Commands

Run the narrow tests for the changed slice first, then run the shared release checks below on the release candidate commit.

Target live evidence must reject wildcard environment endpoints such as `http://[::]:3000` and `http://0:3000`; `[::]` and `0` are bind addresses, not concrete release targets.

Current orchestration note: `scripts/verify-commercial-completion.sh` now includes dependency security, web TypeScript, the full server Go suite, all DB-backed commercial evidence profiles, the serial `TEST_DATABASE_URL` full server Go suite, deployment validation, target-gated Kubernetes validation, backup/restore smoke, target live evidence manifest validation, and `git diff --check`. The docs gate also runs `scripts/verify-target-release-evidence-fixtures.sh`, which proves the target manifest verifier rejects generated templates, missing provider and gRPC evidence collections, missing live rails, swapped provider evidence refs that point Stripe/Alipay/WeChat Pay rows at another provider's live-rail artifact, stale artifact commit/run lineage, missing strict final flags, non-canonical strict verifier commands including escape-hatch variable assignments, `env` wrappers, shell control operators, and trailing arguments, missing strict verifier logs, unfilled Kubernetes secret file classes, secret-like fields embedded under `secretAudit`, incomplete secret-audit scopes that do not cover Kubernetes, providers, and runtime, malformed gRPC smoke attachments, gRPC smoke artifact timestamps older than copied reports, loopback/local/wildcard target URLs, credential-bearing environment base URLs, environment base URLs with raw or percent-encoded secret-like query or fragment parameters such as `token=`, `password=`, `pass%77ord=`, or `%74oken=`, plus ordinary query values that still carry secret-like material, gRPC addresses including trailing-dot `localhost.`, IPv4-mapped IPv6 loopback hosts, abbreviated IPv4 loopback hosts such as `127.1`, and wildcard `0` hosts, URL-shaped gRPC addresses such as `https://host:port`, malformed workflow telemetry windows, workflow telemetry artifacts older than observation-window ends, incomplete artifact indexes, artifact entries that are not referenced by required evidence fields, free-form `evidenceRef` annotations that try to mask unused artifact entries, and artifact URIs with raw or percent-encoded secret-like query or fragment parameters such as `token=`, `password=`, `pass%77ord=`, or `%74oken=`, credentials in URI userinfo, local filesystem paths, `file:` URLs, loopback URLs, abbreviated IPv4 loopback URLs, wildcard `0` URLs, IPv4-mapped IPv6 loopback URLs, or inline/non-authority schemes such as `data:`. It also runs `scripts/verify-commercial-db-evidence-profiles.sh`, which keeps the commercial DB profile usage list, `case` dispatch, help text, and `all` aggregator synchronized. The standalone command list below remains the explicit release-candidate checklist and mirrors the direct command forms behind the strict verifier.

```bash
bash scripts/check.sh docs
bash scripts/check.sh relay-security
bash scripts/check.sh security
pnpm --dir src/web exec tsc --noEmit
pnpm --dir src/web test -- ChatPage SoloPage KnowledgePage MarketplacePage AdminHomePage AdminBillingPage AdminReviewsPage --runInBand
pnpm --dir src/web test:e2e --grep "commercial journey"
(cd src/server && GOCACHE=/tmp/oblivious-go-cache GOMODCACHE=/tmp/oblivious-go-mod-cache go test ./... -count=1)
GOCACHE=/tmp/oblivious-go-cache GOMODCACHE=/tmp/oblivious-go-mod-cache bash scripts/verify-commercial-db-evidence.sh all
(cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" GOCACHE=/tmp/oblivious-go-cache GOMODCACHE=/tmp/oblivious-go-mod-cache go test -p 1 ./... -count=1)
bash scripts/deploy-validate.sh
OBLIVIOUS_K8S_SECRET_FILE=/path/outside/git/secret.yaml bash scripts/k8s-validate.sh
bash scripts/backup-restore-smoke.sh
AGENT_GRPC_ADDR=agent:50063 WORKFLOW_GRPC_ADDR=workflow:50064 TASK_GRPC_ADDR=task:50065 bash scripts/target-grpc-smoke.sh > /path/outside/git/grpc-smoke.json
bash scripts/verify-target-release-evidence.sh --print-template > /path/outside/git/target-release-evidence.json
OBLIVIOUS_TARGET_EVIDENCE_FILE=/path/outside/git/target-release-evidence.json bash scripts/verify-target-release-evidence.sh
TEST_DATABASE_URL="$TEST_DATABASE_URL" \
OBLIVIOUS_K8S_SECRET_FILE=/path/outside/git/secret.yaml \
OBLIVIOUS_TARGET_EVIDENCE_FILE=/path/outside/git/target-release-evidence.json \
COMMERCIAL_COMPLETION_RUN_DEPLOY=true \
COMMERCIAL_COMPLETION_RUN_K8S=true \
COMMERCIAL_COMPLETION_RUN_BACKUP_RESTORE=true \
COMMERCIAL_COMPLETION_RUN_TARGET_EVIDENCE=true \
bash scripts/verify-commercial-completion.sh
git diff --check
```

The verifier treats IPv6 unspecified hosts such as `[::]`, abbreviated IPv4 loopback hosts such as `127.1`, and short wildcard hosts such as `0` as local/non-target addresses, the same release-evidence boundary as loopback and `0.0.0.0`.

The verifier repeatedly percent-decodes URI query and fragment parameter names before applying the secret-like name boundary, so target evidence rejects encoded and nested encoded forms such as `pass%77ord=`, `pass%2577ord=`, `%74oken=`, and `t%256fken=` the same way it rejects plain `password=` and `token=`.

The verifier treats the target provider live-rail set as closed: `providers[]` may contain only Stripe, Alipay, and WeChat Pay evidence rows, each backed by matching `provider-live-rail` artifact metadata.

The verifier also treats the target gRPC smoke service set as closed: `grpc[]` and `grpcSmokeReport.results[]` may contain only `agent`, `workflow`, and `task` generated-client evidence, with no extra service rows, and the copied smoke status must match the service contract: Agent `validation_error`, Workflow `validation_response`, and Task `validation_response`.

The target evidence manifest must record concrete environment `name`, `class`, and `baseUrl` values instead of TODO/example/sample placeholders, and `environment.baseUrl` must be an HTTP(S) URL that does not point at localhost, trailing-dot localhost, IPv4-mapped IPv6 loopback, abbreviated IPv4 loopback such as `127.1`, another loopback or wildcard target, a URI with embedded userinfo credentials, or a URI carrying raw or percent-encoded secret-like query or fragment parameter names or values such as `token=`, `password=`, `pass%77ord=`, `%74oken=`, or `evidence=target-secret-token`. It must include a concrete manifest `runId`, plus `providers[]` and `grpc[]` evidence collections, not only free-form notes. It must record `kubernetes.secretFileClass` as exactly `external-filled`, proving the manifest is tied to a filled external secret rather than a generated example or empty placeholder. It must record the canonical strict verifier command with all final gate flags immediately followed by `bash scripts/verify-commercial-completion.sh`: `COMMERCIAL_COMPLETION_RUN_DEPLOY=true`, `COMMERCIAL_COMPLETION_RUN_K8S=true`, `COMMERCIAL_COMPLETION_RUN_BACKUP_RESTORE=true`, and `COMMERCIAL_COMPLETION_RUN_TARGET_EVIDENCE=true`. It must not assign `COMMERCIAL_COMPLETION_ALLOW_ENV_SKIPS` or `OBLIVIOUS_TARGET_EVIDENCE_ALLOW_COMMIT_MISMATCH` in `strictVerifier.command`, even to shell-quoted, ANSI-C-quoted, or false-looking values, and it must not wrap the command with `env`, append shell control operators such as `|| true`, or pass the final flags as script arguments. It must include `strictVerifier.evidenceRef`, `startedAt`, and `completedAt` for the referenced no-skip verifier log. It must record `workflowTelemetry.successRate` as a proportion between `0.99` and `1.0`, and `workflowTelemetry.window` as an ISO-8601 `start/end` interval whose end is at or after start; the referenced workflow telemetry artifact must be recorded at or after that window end. It must also embed the structured `grpcSmokeReport` from `scripts/target-grpc-smoke.sh`, with a positive Go-duration `timeout`, Agent, Workflow, and Task pass results matching the manifest `grpc` addresses, and both manifest and smoke-result gRPC addresses must use plain `host:port` or `[ipv6]:port` non-local target service endpoints rather than URL-shaped, abbreviated loopback, or wildcard values; the referenced gRPC smoke artifact must be recorded at or after the copied smoke report timestamp. It must carry an `artifacts[]` index whose ids are unique, referenced by at least one required `evidenceRef`, whose entries provide concrete `kind`, `commit`, `runId`, `uri`, and `recordedAt` values for every non-placeholder required `evidenceRef`, whose `commit` equals the manifest commit, whose `runId` equals the manifest runId, whose kind matches the referencing evidence family, and whose provider live-rail entries carry `provider` metadata matching the referencing Stripe, Alipay, or WeChat Pay provider row. Free-form notes or unrelated `evidenceRef` keys do not count as artifact usage. Artifact URIs must be remote target artifact URIs with an authority/host rather than local filesystem paths, URI userinfo credentials, loopback, abbreviated IPv4 loopback, or wildcard URLs, IPv4-mapped IPv6 loopback URLs, `file:` URLs, inline schemes such as `data:`, or raw or percent-encoded secret-like query or fragment parameter names or values such as `token=`, `password=`, `pass%77ord=`, `%74oken=`, or `evidence=target-secret-token`. The manifest must not embed secret-like keys or values, including under `secretAudit`; `secretAudit.scope` must include `kubernetes`, `providers`, and `runtime`, and the secret-audit evidence itself belongs behind an artifact reference. If a command is intentionally skipped because the target environment lacks Docker, Kubernetes, database, browser, or payment-provider access, record the skip as residual risk. A skipped command is not successful proof.

## Unresolved Risk List

| Risk | Evidence still required | Current boundary |
| --- | --- | --- |
| External workflow success-rate proof | Deployed telemetry or load evidence from the target installation | Local deterministic workflow load evidence is repository proof only |
| Billing and Marketplace payment rails | Stripe, Alipay, and WeChat Pay checkout/refund/payout/reconciliation proof from the target installation | The no-skip `billing-checkout-topup-http`, `billing-provider-lifecycle`, and `marketplace-money-movement` profiles prove local PostgreSQL checkout, invoice, subscription, top-up, refund, settlement, payout, webhook state transitions, domestic checkout/refund/subscription bridge mapping, top-up refund status synchronization, Admin Billing payment-intent provider filters for top-up and Marketplace settlement inspection, Admin top-up refund operator-evidence ledger persistence and rejection preservation, domestic top-up refund payment-intent evidence propagation, Marketplace paid audit retention across publisher delete and buyer uninstall, local Stripe Marketplace install/refund webhook lifecycle, local Marketplace selected-provider lifecycle keys, `marketplace_install` payment-intent kind migration contract, revenue-tier disclosure, local/manual payout-state preservation, and local Marketplace paid-install Alipay/WeChat Pay provider-selection plus checkout-metadata parity; fake providers, DB-free handlers, and fixture-backed browser checkout/Admin Billing operator proof do not prove live provider rails. The target evidence manifest verifier now rejects final evidence unless Stripe, Alipay, and WeChat Pay each include live checkout, refund, payout, and reconciliation artifact refs, rejects extra provider rows outside that closed live-rail set, and requires those refs to resolve through the manifest `artifacts[]` index to `provider-live-rail` artifacts whose `provider` metadata matches the provider row |
| Deployment platform recovery | `scripts/k8s-validate.sh` with real cluster access, real secrets, Kubernetes Secret/ConfigMap reference review, NetworkPolicy enforcement, and failover/scale evidence | Local Docker compose deployment validation and backup/restore smoke have been refreshed for the current fusion-spec branch, including explicit Qdrant readiness, ClickHouse auth/init, Postgres TCP readiness, migration, server/web, health, metrics, app-route, Relay no-channel fail-closed, and commercial fixture restore proof. Static manifests and contract checks now scan tracked Kubernetes Secret/ConfigMap references, first-party workload pod/container security contexts, and NetworkPolicy default-deny/allowlist shape. The current Kubernetes smoke path applies `network-policy.yaml` alongside the release allowlist, but this still does not prove live Kubernetes failover, CNI enforcement behavior, ingress-controller namespace labels, or external platform recovery; other microservice manifests remain static/reference contracts until explicitly added to target smoke; `observability-alert-recovery-persistence` proves repository-local PostgreSQL alert/recovery state only |
| Agent/Workflow/Task gRPC deployed compatibility | `AGENT_GRPC_ADDR=... WORKFLOW_GRPC_ADDR=... TASK_GRPC_ADDR=... bash scripts/target-grpc-smoke.sh > /path/outside/git/grpc-smoke.json`, then reference that artifact and copy its structured results into `grpcSmokeReport` in the target evidence manifest | Repository proof now covers `cmd/agent`, `cmd/workflow`, and `cmd/task` registration into real `grpc.Server` listeners, full current Agent generated-client bufconn coverage for continue/adjust/budget/tool-approval/plan-step control RPCs, Workflow and Task package bufconn dispatch, standalone Workflow/Task generated-client command registration tests, Docker/Kubernetes/Compose Agent/Workflow/Task gRPC port configuration, `cmd/grpc-smoke`, `scripts/target-grpc-smoke.sh`, per-service smoke timeouts, and static deployment contract checks. The target evidence verifier now requires the manifest's `grpcSmokeReport.timeout` to be a positive Go duration string, requires `grpcSmokeReport.results` to include Agent, Workflow, and Task generated-client pass results whose addresses match the manifest `grpc` entries, requires those addresses to use expected ports and plain `host:port` or `[ipv6]:port` non-local target service endpoints instead of URLs, requires Agent status `validation_error`, Workflow status `validation_response`, and Task status `validation_response`, and requires the manifest `artifacts[]` index to hold the smoke artifact id referenced by those results. The smoke helper intentionally uses validation-only RPCs to prove generated-client reachability; it still does not prove DB, Kafka, business success paths, or live target reachability |
| Database-backed tenant isolation | Broader `TEST_DATABASE_URL` integration tests in CI or target release evidence | Focused disposable PostgreSQL app-stateful evidence now includes Task approval state/workspace no-mutation proof and Console usage summary proof that recorded Chat requests appear in the current user's Console usage aggregate, plus auth-security persistence, Relay file-mapping ownership for upload, mapped get, mapped list, and wrong-tenant empty-list/no-upstream behavior, Relay runtime channel selection, Workflow active-organization isolation, Publishing channel active-organization isolation, Admin Relay channel and read-surface active-organization isolation, Quota SQL tenant isolation and lifecycle aggregation including Admin user quota PATCH/audit persistence, user-scoped balance, top-up checkout/no-credit, Admin refund reversal, and Stripe top-up/refund accounting, tenant-membership lifecycle including Admin organization create/list/detail/update/archive/member route persistence, core Chat/Publishing/Relay SQL persistence, and tenant-cross-surface evidence covers selected auth/reset-token replay-expiry/non-enumeration/session/rate-limit/file-ownership/relay-runtime/workflow/publishing-channel/admin-relay-channel/admin-relay-read/quota/admin-user-quota/ownership/member/admin-organization and core app-surface SQL paths, but DB-free route guards do not prove all SQL persistence isolation |
| Provider/channel/workflow secret responses and at-rest secrets | SQL-backed HTTP redaction proof for newly added provider/channel/workflow route families, at-rest encryption proof for secret-bearing stores, plus target-environment secret audits | Admin Observability alert-provider, Publishing channel, Admin Relay channel, and Workflow definition/version/execution-snapshot/node-execution SQL-backed HTTP responses are covered; Admin Relay channel API keys, Observability alert-provider config secrets, Publishing channel config secrets, and Workflow definition/runtime secret-like payloads are now encrypted at rest with runtime/probe/webhook decryption proof. Target-environment secret audits remain open |
| Final release readiness | Strict final verifier with deploy, Kubernetes validation, backup/restore, generated-client target gRPC smoke, and target live evidence validation enabled with no environment skips | Current local release evidence includes no-skip DB profiles, Docker compose deployment validation, backup/restore smoke, Agent/Workflow/Task gRPC runtime/deployment contracts, `scripts/target-grpc-smoke.sh`, and a manifest validator plus template generator for target/live proof. The generated template is intentionally rejected until every `TODO` placeholder is replaced by concrete environment values and artifact refs, and it now contains separate Stripe, Alipay, and WeChat Pay provider evidence slots plus provider-labeled artifact metadata, strict verifier log metadata, a structured `grpcSmokeReport` section, a concrete manifest `runId`, and an `artifacts[]` index with per-artifact commit/runId lineage. The manifest rejects `strictVerifier.command` unless it exactly parses to the canonical strict verifier invocation with the deploy, Kubernetes, backup/restore, and target-evidence verifier flags followed by `bash scripts/verify-commercial-completion.sh`; rejects strict verifier commands that enable `COMMERCIAL_COMPLETION_ALLOW_ENV_SKIPS=true` even when `true` is shell-quoted; rejects strict verifier commands or strict wrapper environments that enable `OBLIVIOUS_TARGET_EVIDENCE_ALLOW_COMMIT_MISMATCH=true`; rejects `env` wrappers, shell-control suffixes, and final flags passed as script arguments; rejects missing/placeholder target environment values; rejects malformed, loopback, or wildcard target `environment.baseUrl` values including trailing-dot `localhost.`, bare host `0`, abbreviated IPv4 loopback such as `127.1`, IPv4-mapped IPv6 loopback, URI userinfo credentials, and raw or percent-encoded secret-like query or fragment parameters such as `token=`, `password=`, `pass%77ord=`, or `%74oken=`; rejects ordinary query values that still carry secret-like material; rejects any `kubernetes.secretFileClass` other than `external-filled`; rejects missing/placeholder strict verifier log artifacts or impossible start/end times; rejects artifact commit/runId values that do not match the manifest commit/runId; rejects `workflowTelemetry.successRate` values outside the `0.99` to `1.0` proportion range; rejects malformed or inverted `workflowTelemetry.window` intervals; rejects workflow telemetry artifacts recorded before the window end; rejects gRPC evidence unless the smoke report timeout is a positive Go duration string and the smoke report results are present, match the manifest addresses, target expected service ports, and use plain `host:port` or `[ipv6]:port` non-local target service endpoints including bare host `0`, abbreviated IPv4 loopback, IPv4-mapped IPv6 loopback, and URL-shaped address rejection; rejects gRPC smoke artifacts recorded before the copied smoke report timestamp; rejects evidence refs that do not resolve through `artifacts[]`; rejects artifact ids that are not referenced by evidence; rejects artifact URIs carrying raw or percent-encoded token/signature/API-key/password style query or fragment parameters, URI userinfo credentials, local filesystem paths, file URLs, loopback URLs, abbreviated IPv4 loopback URLs, wildcard `0` URLs, IPv4-mapped IPv6 loopback URLs, or inline/non-authority schemes; rejects secret-like keys under `secretAudit`; rejects `secretAudit.scope` unless it covers Kubernetes, provider, and runtime secrets; rejects evidence refs whose artifact kind does not match the evidence family; and rejects provider evidence refs whose provider-labeled artifact metadata does not match the Stripe, Alipay, or WeChat Pay row. Historical Phase 30 success does not automatically cover later fusion-spec changes, and the strict verifier still cannot be final until target Kubernetes validation, live provider rails, deployed gRPC reachability, target secret audit, target workflow telemetry, and all no-skip release checks run together |

## Update Rules

- Add a new matrix evidence line after each completed slice.
- Add exact commands, pass/fail result, skipped checks, and environment class.
- Keep rows marked `Partial` until their own evidence is complete.
- Do not use this pack to claim completion while unresolved risks remain.
