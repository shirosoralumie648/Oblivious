## 2026-09-10 - Use type=search for search inputs
**Learning:** The `<input type="search">` type offers built-in search clearing behaviors in some native browser contexts and improves keyboard semantics on mobile devices.
**Action:** Add `type="search"` to any search-specific inputs, including abstractions like `SearchBar` components.
