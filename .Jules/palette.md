## 2024-06-18 - [MCP Server Panel Button Loading States]
**Learning:** Adding immediate visual feedback to async actions, such as connecting or executing tools, significantly improves the perceived responsiveness of the UI.
**Action:** Always include a visual loading state (like a spinner) on buttons that trigger asynchronous operations to prevent users from wondering if their click registered.
