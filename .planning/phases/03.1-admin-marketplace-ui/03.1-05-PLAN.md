---
phase: 03.1-admin-marketplace-ui
plan: 05
type: execute
wave: 4
gap_closure: true
depends_on: [03.1-02, 03.1-03]
files_modified:
  - src/web/src/routes/admin/AdminPlansPage.tsx
  - src/web/src/routes/admin/AdminPlansPage.test.tsx
  - src/web/src/routes/admin/AdminUsersPage.tsx
  - src/web/src/routes/admin/AdminUsersPage.test.tsx
autonomous: true
requirements: [ADMIN-04]

must_haves:
  truths:
    - "Admin Plans page is no longer a placeholder"
    - "Plans page lists plan name, price, quota, model access, subscribers, active/public status, and actions"
    - "Plans page uses createAdminApi(createHttpClient()) with listPlans/createPlan/updatePlan/deactivatePlan"
    - "Plans add/edit flows use DrawerForm and current PlanCreateRequest/PlanUpdateRequest fields"
    - "Plan deactivate flow uses ConfirmDialog with destructive confirmation"
    - "Admin Users page replaces the legacy raw fetch implementation"
    - "Users page lists email/name, role, plan, status, usage stats, last login, and actions"
    - "Users page uses createAdminApi(createHttpClient()) with listUsers/updateUser/disableUser/enableUser"
    - "Users edit flow supports role, plan assignment, and status changes through DrawerForm"
    - "Both pages use useReducer + useEffect, DataTable, SearchBar, StatusBadge, EmptyState, and existing shadcn primitives"
  artifacts:
    - path: "src/web/src/routes/admin/AdminPlansPage.tsx"
      provides: "Admin plans CRUD page"
      exports: ["AdminPlansPage"]
    - path: "src/web/src/routes/admin/AdminUsersPage.tsx"
      provides: "Admin users lifecycle page"
      exports: ["AdminUsersPage"]
  key_links:
    - from: "AdminPlansPage"
      to: "createAdminApi → /api/v1/admin/plans"
      via: "useEffect + useReducer"
    - from: "AdminUsersPage"
      to: "createAdminApi → /api/v1/admin/users"
      via: "useEffect + useReducer"
---

<objective>
Close the UAT gap for the Admin Plans and Users pages. Replace placeholders/legacy raw-fetch UI with production admin pages that use the shared Phase 03.1 component and API foundation.

Purpose: Phase 03.1 UAT test 6 found that Plans is a placeholder and Users is a pre-03.1 implementation. This plan brings both pages onto the current Admin UI/API contract.
Output: Full Admin Plans and Users pages plus focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
- .planning/phases/03.1-admin-marketplace-ui/03.1-UAT.md
- .planning/phases/03.1-admin-marketplace-ui/03.1-02-SUMMARY.md
- .planning/phases/03.1-admin-marketplace-ui/03.1-03-SUMMARY.md
- src/web/src/features/admin/api.ts
- src/web/src/types/admin.ts
- src/web/src/components/shared/DataTable.tsx
- src/web/src/components/shared/DrawerForm.tsx
- src/web/src/components/shared/ConfirmDialog.tsx
- src/web/src/components/shared/SearchBar.tsx
- src/web/src/components/shared/StatusBadge.tsx
- src/web/src/components/shared/EmptyState.tsx
- src/web/src/routes/admin/AdminChannelsPage.tsx
- src/web/src/routes/admin/AdminRoutesPage.tsx
</read_first>

<api_contracts>
Use the live TypeScript contracts as authority:
- `PlanInfo`, `PlanCreateRequest`, `PlanUpdateRequest`
- `UserDetail`, `UserUpdateRequest`
- `AdminApi.listPlans`, `createPlan`, `updatePlan`, `deactivatePlan`
- `AdminApi.listUsers`, `updateUser`, `disableUser`, `enableUser`
</api_contracts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace AdminPlansPage with plan CRUD workflow</name>
  <files>src/web/src/routes/admin/AdminPlansPage.tsx, src/web/src/routes/admin/AdminPlansPage.test.tsx</files>
  <action>
Implement `AdminPlansPage` with:
- `useReducer` state for plans, loading/error, search/status filter, drawer form, delete/deactivate confirm state, and form errors.
- `createAdminApi(createHttpClient())` API integration.
- DataTable columns: Name, Price, Quota, Model Access, Subscribers, Active/Public, Actions.
- Toolbar: SearchBar and status/public filter controls.
- Add/Edit DrawerForm fields:
  - name
  - description
  - quotaAmount
  - tokenQuota
  - price
  - modelAccess as comma-separated input
  - agentLimit
  - durationDays
  - isPublic
  - sortOrder
- Save behavior:
  - create uses `api.createPlan(payload)`
  - edit uses `api.updatePlan(id, payload)`
  - reloads after success
- Deactivate behavior:
  - row action opens ConfirmDialog
  - confirm calls `api.deactivatePlan(id)` and reloads
- Empty copy: "No plans configured -- Create a pricing plan to make quota packages available."
- Error copy: "Something went wrong while loading this data."
  </action>
  <verification>
Add tests that prove:
- existing plans render with price/quota/public status
- Add Plan opens the drawer with fields
- deactivation confirms through `deactivatePlan`
  </verification>
</task>

<task type="auto">
  <name>Task 2: Replace AdminUsersPage with user lifecycle workflow</name>
  <files>src/web/src/routes/admin/AdminUsersPage.tsx, src/web/src/routes/admin/AdminUsersPage.test.tsx</files>
  <action>
Replace the existing raw `fetch`/`alert`/`confirm` implementation. Implement `AdminUsersPage` with:
- `useReducer` state for users, loading/error, search/role/status filters, drawer form, selected user, and action loading.
- `createAdminApi(createHttpClient())` API integration.
- DataTable columns: Email, Name, Role, Plan, Status, Usage, Last Login, Actions.
- Toolbar: SearchBar plus role/status filter controls.
- Edit DrawerForm fields:
  - role
  - planID
  - status
- Save behavior calls `api.updateUser(id, payload)`.
- Enable/Disable row actions call `api.enableUser(id)` or `api.disableUser(id)` and reload.
- Empty copy: "No users found -- Users will appear here after they register."
- Error copy: "Something went wrong while loading this data."
  </action>
  <verification>
Add tests that prove:
- users render with role/plan/status/usage values
- Edit User opens drawer and submits `updateUser`
- disable/enable actions call the correct API method
  </verification>
</task>

</tasks>

<verification_commands>
```bash
cd src/web && npx vitest run src/routes/admin/AdminPlansPage.test.tsx src/routes/admin/AdminUsersPage.test.tsx
cd src/web && npx tsc --noEmit
```
</verification_commands>

<summary_requirements>
Create `.planning/phases/03.1-admin-marketplace-ui/03.1-05-SUMMARY.md` with verification results, deviations, and any remaining gaps.
</summary_requirements>
