---
phase: 03.1-admin-marketplace-ui
plan: 06
type: execute
wave: 4
gap_closure: true
depends_on: [03.1-02, 03.1-03]
files_modified:
  - src/web/src/routes/admin/AdminAuditLogPage.tsx
  - src/web/src/routes/admin/AdminAuditLogPage.test.tsx
  - src/web/src/routes/admin/AdminReviewsPage.tsx
  - src/web/src/routes/admin/AdminReviewsPage.test.tsx
autonomous: true
requirements: [ADMIN-04, MARKET-02]

must_haves:
  truths:
    - "Admin Audit Log page is no longer a placeholder"
    - "Audit Log page lists actor, action, resource type/id, IP address, changes summary, and timestamp"
    - "Audit Log page supports filters for actor, action, resource type, and date range"
    - "Audit Log page uses createAdminApi(createHttpClient()) with listAuditLogs"
    - "Admin Reviews page is no longer a placeholder"
    - "Reviews page lists pending marketplace agents with owner, category, visibility, rating/install context, and actions"
    - "Reviews page supports approve and reject flows"
    - "Reject flow requires a reason before calling rejectAgent"
    - "Both pages use useReducer + useEffect, DataTable, SearchBar or filter controls, StatusBadge, ConfirmDialog/DrawerForm, and existing shadcn primitives"
  artifacts:
    - path: "src/web/src/routes/admin/AdminAuditLogPage.tsx"
      provides: "Filterable admin audit log page"
      exports: ["AdminAuditLogPage"]
    - path: "src/web/src/routes/admin/AdminReviewsPage.tsx"
      provides: "Marketplace review queue admin page"
      exports: ["AdminReviewsPage"]
  key_links:
    - from: "AdminAuditLogPage"
      to: "createAdminApi → /api/v1/admin/audit-logs"
      via: "useEffect + useReducer"
    - from: "AdminReviewsPage"
      to: "createAdminApi → /api/v1/admin/reviews"
      via: "useEffect + useReducer"
---

<objective>
Close the UAT gap for Admin Audit Log and Review Queue pages. Replace placeholders with functional pages wired to the current admin API client.

Purpose: Phase 03.1 UAT test 6 found Audit Log and Reviews still display Coming Soon placeholders. These pages are required for admin observability and marketplace governance.
Output: Full Audit Log and Reviews pages plus focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
- .planning/phases/03.1-admin-marketplace-ui/03.1-UAT.md
- .planning/phases/03.1-admin-marketplace-ui/03.1-02-SUMMARY.md
- .planning/phases/03.1-admin-marketplace-ui/03.1-03-SUMMARY.md
- src/web/src/features/admin/api.ts
- src/web/src/types/admin.ts
- src/web/src/components/shared/DataTable.tsx
- src/web/src/components/shared/DrawerForm.tsx
- src/web/src/components/shared/ConfirmDialog.tsx
- src/web/src/components/shared/SearchBar.tsx
- src/web/src/components/shared/StatusBadge.tsx
- src/web/src/components/shared/EmptyState.tsx
- src/web/src/routes/admin/AdminChannelsPage.tsx
- src/web/src/routes/admin/AdminRoutesPage.tsx
</read_first>

<api_contracts>
Use the live TypeScript contracts as authority:
- `AuditEntry`
- `PublishedAgent`
- `AdminApi.listAuditLogs`
- `AdminApi.listReviews`, `approveAgent`, `rejectAgent`
</api_contracts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace AdminAuditLogPage with filterable audit trail</name>
  <files>src/web/src/routes/admin/AdminAuditLogPage.tsx, src/web/src/routes/admin/AdminAuditLogPage.test.tsx</files>
  <action>
Implement `AdminAuditLogPage` with:
- `useReducer` state for entries, loading/error, filters, and pagination offset.
- `createAdminApi(createHttpClient())` API integration.
- DataTable columns: Actor, Action, Resource, Changes, IP Address, Timestamp.
- Filter controls for actor search, action, resourceType, startDate, endDate.
- Reload behavior when filters change.
- Changes rendering should be concise and resilient if `changes` is empty or JSON-like.
- Empty copy: "No audit entries found -- Administrative actions will appear here."
- Error copy: "Something went wrong while loading this data."
  </action>
  <verification>
Add tests that prove:
- audit entries render actor/action/resource values
- filters call `listAuditLogs` with expected query fields
- empty state renders when no entries are returned
  </verification>
</task>

<task type="auto">
  <name>Task 2: Replace AdminReviewsPage with approve/reject queue</name>
  <files>src/web/src/routes/admin/AdminReviewsPage.tsx, src/web/src/routes/admin/AdminReviewsPage.test.tsx</files>
  <action>
Implement `AdminReviewsPage` with:
- `useReducer` state for reviews, loading/error, status filter, selected agent, confirm approve state, reject drawer state, rejection reason, and action loading.
- `createAdminApi(createHttpClient())` API integration.
- DataTable columns: Agent, Owner, Category, Visibility, Status, Rating, Installs, Updated, Actions.
- Status filter defaulting to pending review.
- Approve flow:
  - row action opens ConfirmDialog
  - confirm calls `api.approveAgent(id)` and reloads
- Reject flow:
  - row action opens DrawerForm or dialog with reason textarea
  - submit requires non-empty reason
  - calls `api.rejectAgent(id, reason)` and reloads
- Empty copy: "No agents waiting for review -- Submitted agents will appear here."
- Error copy: "Something went wrong while loading this data."
  </action>
  <verification>
Add tests that prove:
- pending agents render with owner/category/status
- approve confirmation calls `approveAgent`
- reject requires reason and calls `rejectAgent`
  </verification>
</task>

</tasks>

<verification_commands>
```bash
cd src/web && npx vitest run src/routes/admin/AdminAuditLogPage.test.tsx src/routes/admin/AdminReviewsPage.test.tsx
cd src/web && npx tsc --noEmit
```
</verification_commands>

<summary_requirements>
Create `.planning/phases/03.1-admin-marketplace-ui/03.1-06-SUMMARY.md` with verification results, deviations, and any remaining gaps.
</summary_requirements>
