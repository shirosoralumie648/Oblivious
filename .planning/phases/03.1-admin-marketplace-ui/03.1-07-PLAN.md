---
phase: 03.1-admin-marketplace-ui
plan: 07
type: execute
wave: 5
gap_closure: true
depends_on: [03.1-02, 03.1-03]
files_modified:
  - src/web/src/app/router.tsx
  - src/web/src/routes/marketplace/MarketplaceHomePage.tsx
  - src/web/src/routes/marketplace/MarketplaceAgentDetailPage.tsx
  - src/web/src/routes/marketplace/MarketplacePublishPage.tsx
  - src/web/src/routes/marketplace/MarketplaceMyAgentsPage.tsx
  - src/web/src/routes/marketplace/MarketplacePage.test.tsx
autonomous: true
requirements: [MARKET-02]

must_haves:
  truths:
    - "Marketplace UI no longer renders the old static MCP server catalog for /marketplace"
    - "Marketplace home/search page consumes createMarketplaceApi(createHttpClient())"
    - "Marketplace home/search page supports category, price, rating, and text search filters"
    - "Agent detail page shows metadata, tags, rating, install count, versions/reviews, and install/review actions"
    - "Publish page supports agent metadata, tools, examples, visibility, pricing, version, and changelog fields"
    - "My Agents page lists the signed-in user's published agents and installed agents"
    - "Routes are registered for /marketplace, /marketplace/agents/:agentId, /marketplace/publish, and /marketplace/my-agents"
    - "Pages use the shared component foundation where applicable: DataTable, FilterPanel, RatingStars, EmptyState, DrawerForm/ConfirmDialog"
  artifacts:
    - path: "src/web/src/routes/marketplace/MarketplaceHomePage.tsx"
      provides: "Agent marketplace browse/search page"
      exports: ["MarketplaceHomePage"]
    - path: "src/web/src/routes/marketplace/MarketplaceAgentDetailPage.tsx"
      provides: "Agent marketplace detail/install/review page"
      exports: ["MarketplaceAgentDetailPage"]
    - path: "src/web/src/routes/marketplace/MarketplacePublishPage.tsx"
      provides: "Agent publish/edit form page"
      exports: ["MarketplacePublishPage"]
    - path: "src/web/src/routes/marketplace/MarketplaceMyAgentsPage.tsx"
      provides: "Publisher and installed agents management page"
      exports: ["MarketplaceMyAgentsPage"]
  key_links:
    - from: "Marketplace pages"
      to: "createMarketplaceApi → /api/v1/marketplace/*"
      via: "useEffect + useReducer"
    - from: "router.tsx"
      to: "Marketplace route group"
      via: "ProtectedRoute children"
---

<objective>
Close the UAT gap for Agent Marketplace UI. Replace or route around the old MCP server catalog so Phase 03.1 exposes the intended Agent Marketplace workflows backed by the marketplace API client.

Purpose: Phase 03.1 UAT test 7 found `/marketplace` is still a static MCP server catalog and does not consume `/api/v1/marketplace/*`. This plan implements the Agent Marketplace surface promised by MARKET-02.
Output: Marketplace browse/search, detail/install/review, publish, and my-agents pages plus route registrations and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
- .planning/phases/03.1-admin-marketplace-ui/03.1-UAT.md
- .planning/phases/03.1-admin-marketplace-ui/03.1-02-SUMMARY.md
- .planning/phases/03.1-admin-marketplace-ui/03.1-03-SUMMARY.md
- src/web/src/features/marketplace/api.ts
- src/web/src/features/admin/api.ts
- src/web/src/types/admin.ts
- src/web/src/components/shared/DataTable.tsx
- src/web/src/components/shared/FilterPanel.tsx
- src/web/src/components/shared/RatingStars.tsx
- src/web/src/components/shared/EmptyState.tsx
- src/web/src/components/shared/DrawerForm.tsx
- src/web/src/components/shared/ConfirmDialog.tsx
- src/web/src/app/router.tsx
- src/web/src/routes/workspace/MarketplacePage.tsx
</read_first>

<api_contracts>
Use the live TypeScript contracts in `src/web/src/features/marketplace/api.ts` as authority:
- `MarketplaceAgent`
- `Category`
- `AgentPublishRequest`
- `AgentInstall`
- `AgentReview`
- `MarketplaceApi.searchAgents`, `getFeatured`, `getCategories`, `getAgent`, `publishAgent`, `updateAgent`, `installAgent`, `uninstallAgent`, `getInstalledAgents`, `getMyAgents`, `getReviews`, `submitReview`, `getVersions`
</api_contracts>

<routing_note>
Preserve unrelated router changes in the dirty worktree. The intended Phase 03.1 routes are:
- `/marketplace`
- `/marketplace/agents/:agentId`
- `/marketplace/publish`
- `/marketplace/my-agents`

If the old MCP server catalog must remain accessible, move it to a distinct future route such as `/settings/mcp-marketplace` only if the existing router/layout has an obvious place for it. Otherwise leave the old component file unused and document the replacement in the summary.
</routing_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Marketplace home/search page</name>
  <files>src/web/src/routes/marketplace/MarketplaceHomePage.tsx</files>
  <action>
Implement browse/search with:
- `useReducer` state for featured/search results/categories, loading/error, query, category, price/rating filters, sort, and pagination.
- `createMarketplaceApi(createHttpClient())`.
- Header actions linking to publish and my-agents.
- Featured or search result cards showing name, owner, category, tags, rating, install count, pricing, and status.
- FilterPanel or compact filter controls for category, price, rating, and sort.
- Empty copy: "No agents found -- Adjust your filters or publish the first agent."
- Error copy: "Something went wrong while loading marketplace agents."
  </action>
</task>

<task type="auto">
  <name>Task 2: Build Agent detail page</name>
  <files>src/web/src/routes/marketplace/MarketplaceAgentDetailPage.tsx</files>
  <action>
Implement detail workflow with:
- Route param `agentId`.
- Load `getAgent(agentId)`, `getVersions(agentId)`, and `getReviews(agentId)`.
- Show name, owner, description, category, tags, tools summary, examples summary, rating, install count, pricing, visibility/status, and version.
- Install button calls `installAgent(agentId, selectedVersionId?)`.
- Review form uses RatingStars and calls `submitReview`.
- Loading, error, and empty/not-found states.
  </action>
</task>

<task type="auto">
  <name>Task 3: Build publish and my-agents pages</name>
  <files>src/web/src/routes/marketplace/MarketplacePublishPage.tsx, src/web/src/routes/marketplace/MarketplaceMyAgentsPage.tsx</files>
  <action>
Implement:
- `MarketplacePublishPage`
  - Drawer-style or page form fields for name, description, category, tags, tools, exampleConversations, systemPrompt, visibility, pricingType, pricingAmount, version, changelog.
  - Submit calls `publishAgent`.
  - Validation for required name/description/version and non-negative price.
- `MarketplaceMyAgentsPage`
  - Load `getMyAgents()` and `getInstalledAgents()`.
  - Show published agents and installed agents in tabs or adjacent sections.
  - Provide uninstall action for installed agents.
  - Link published agents to detail/edit route where possible.
  </action>
</task>

<task type="auto">
  <name>Task 4: Register routes and add tests</name>
  <files>src/web/src/app/router.tsx, src/web/src/routes/marketplace/MarketplacePage.test.tsx</files>
  <action>
Register marketplace routes under the existing protected app route tree:
- `/marketplace` → `MarketplaceHomePage`
- `/marketplace/agents/:agentId` → `MarketplaceAgentDetailPage`
- `/marketplace/publish` → `MarketplacePublishPage`
- `/marketplace/my-agents` → `MarketplaceMyAgentsPage`

Add focused tests that mock `createMarketplaceApi` and prove:
- home page renders agents/categories and invokes search filters
- detail page loads an agent and calls install
- publish page submits publish payload
- my-agents page renders published/installed agents and calls uninstall
  </action>
</task>

</tasks>

<verification_commands>
```bash
cd src/web && npx vitest run src/routes/marketplace/MarketplacePage.test.tsx
cd src/web && npx tsc --noEmit
```
</verification_commands>

<summary_requirements>
Create `.planning/phases/03.1-admin-marketplace-ui/03.1-07-SUMMARY.md` with verification results, deviations, and any remaining gaps.
</summary_requirements>
