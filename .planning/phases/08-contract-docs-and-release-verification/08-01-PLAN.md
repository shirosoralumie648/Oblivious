---
phase: 08-contract-docs-and-release-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/API.md
  - docs/architecture/current-system-contracts.md
  - docs/release/rc-checklist.md
  - docs/release/deployment-runtime-remediation.md
autonomous: true
requirements: [DOC-02]
user_setup: []

must_haves:
  truths:
    - "D-01: docs/API.md stays the canonical routed HTTP index and must mirror the live /api/v1 and /v1 surfaces from src/server/internal/http/router.go and src/server/internal/relay/handler/router.go."
    - "D-02: docs/architecture/current-system-contracts.md stays the long-form behavior contract and must match config.go, scripts/check.sh, and scripts/test.sh without turning into another route index."
    - "D-03: README.md remains summary-level and points readers to docs/API.md and docs/release/rc-checklist.md instead of repeating the full route tables."
    - "D-04: docs/release/rc-checklist.md and docs/release/deployment-runtime-remediation.md remain separate docs with distinct roles."
    - "D-05: Phase-facing prose should describe the current consolidated mainline, while preserving v03.2 wording only for archived evidence and the already-validated deployment path."
    - "D-09: Keep the validated restricted-network scripts/deploy-validate.sh command verbatim, including the OBLIVIOUS_* overrides and the /healthz smoke target."
    - "D-10: If deployment smoke is mentioned, keep /healthz and the restricted-network override form explicit."
    - "D-11: Do not pull CURRENT_STATUS.md, root ROADMAP.md, ARCHAEOLOGY_REPORT.md, or docs/superpowers/* into the docs commit."
    - "D-12: Do not absorb src/web/src/theme/tokens.css, src/web/tailwind.config.ts, or src/web/src/routes/workspace/MarketplacePage.tsx into this phase."
    - "D-13: Knowledge and SOLO boundary docs may be cited as support, but this phase must not expand product scope."
  artifacts:
    - path: "docs/API.md"
      provides: "canonical HTTP route index"
      contains:
        - "## Auth Endpoints"
        - "## Agent Endpoints"
        - "## Memory Endpoints"
        - "## MCP Server Endpoints"
        - "## Notification Endpoints"
        - "## Quota Endpoints"
        - "## Admin Endpoints"
        - "## Marketplace Endpoints"
        - "## Relay /v1 Endpoints"
    - path: "docs/architecture/current-system-contracts.md"
      provides: "behavior contract and env matrix"
      contains:
        - "### 6.8 Agent, Memory, MCP, Notification, And Quota"
        - "### 6.9 Admin"
        - "### 6.10 Marketplace"
        - "### 6.11 WebSocket And Relay"
        - "RELAY_ENABLED"
        - "RELAY_DEFAULT_MODEL"
        - "OPENAI_API_KEY"
        - "OPENAI_BASE_URL"
    - path: "README.md"
      provides: "summary entrypoint and doc navigation"
      contains:
        - "docs/API.md"
        - "docs/release/rc-checklist.md"
    - path: "docs/release/rc-checklist.md"
      provides: "release gate ledger"
      contains:
        - "bash scripts/check.sh docs"
        - "bash scripts/test.sh server"
        - "TEST_DATABASE_URL"
        - "restricted-network"
    - path: "docs/release/deployment-runtime-remediation.md"
      provides: "host-specific deployment remediation"
      contains:
        - "OBLIVIOUS_IMAGE_REGISTRY_PREFIX=docker.m.daocloud.io/library/"
        - "OBLIVIOUS_GOPROXY=https://mirrors.aliyun.com/goproxy/,direct"
        - "OBLIVIOUS_GOSUMDB=sum.golang.google.cn"
        - "http://127.0.0.1:8080/healthz"
        - "/tmp/go-build/.../api-server"
  key_links:
    - from: "src/server/internal/http/router.go"
      to: "docs/API.md"
      via: "live /api/v1 route inventory"
    - from: "src/server/internal/relay/handler/router.go"
      to: "docs/API.md"
      via: "live /v1 relay route inventory"
    - from: "src/server/internal/config/config.go"
      to: "docs/architecture/current-system-contracts.md"
      via: "env contract and defaults"
    - from: "scripts/check.sh and scripts/test.sh"
      to: "docs/architecture/current-system-contracts.md and docs/release/rc-checklist.md"
      via: "docs/env consistency and skip semantics"
    - from: "scripts/deploy-validate.sh and docker-compose.yml"
      to: "docs/release/deployment-runtime-remediation.md and docs/release/rc-checklist.md"
      via: "restricted-network deploy proof"
---

<objective>
Reconcile the public contract docs with the live route and command surface already shipped in Phases 5-7.

Purpose: make README, API index, current system contracts, RC checklist, and deployment remediation point to the same live routes, env vars, and release gates without re-opening frontend or runtime scope.
Output: updated docs that describe the current consolidated mainline and keep the validated restricted-network deployment path verbatim.
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
@$HOME/.Codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
- .planning/phases/08-contract-docs-and-release-verification/08-CONTEXT.md
- .planning/phases/08-contract-docs-and-release-verification/08-DISCUSSION-LOG.md
- .planning/ROADMAP.md
- .planning/STATE.md
- README.md
- docs/API.md
- docs/architecture/current-system-contracts.md
- docs/architecture/knowledge-evolution-decision.md
- docs/architecture/solo-runtime-decision.md
- docs/release/rc-checklist.md
- docs/release/deployment-runtime-remediation.md
- config/.env.example
- src/server/internal/http/router.go
- src/server/internal/relay/handler/router.go
- src/server/internal/config/config.go
- scripts/check.sh
- scripts/test.sh
- scripts/deploy-validate.sh
- docker-compose.yml
- Dockerfile.web
</read_first>
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| Route docs drift away from the live app or relay surface and mislead readers about what is actually shipped. | High | Reconcile docs/API.md directly against src/server/internal/http/router.go and src/server/internal/relay/handler/router.go. |
| README or release docs overstate the project state and hide the live release gate commands or skip semantics. | High | Keep README summary-level and make rc-checklist/deployment-remediation carry the exact commands and skip reasons. |
| Current-system-contracts drifts into future design instead of describing the current behavior contract. | Medium | Keep the file anchored to live routes, env defaults, and documented verification commands only. |
| Historical/reference files or unrelated frontend cleanup debt get swept into the docs commit. | Medium | Keep the plan scoped to the five docs files in files_modified and treat other dirty files as deferred. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile route and environment contracts in docs/API.md and docs/architecture/current-system-contracts.md</name>
  <files>docs/API.md, docs/architecture/current-system-contracts.md</files>
  <read_first>
  - docs/API.md
  - docs/architecture/current-system-contracts.md
  - docs/architecture/knowledge-evolution-decision.md
  - docs/architecture/solo-runtime-decision.md
  - src/server/internal/http/router.go
  - src/server/internal/relay/handler/router.go
  - src/server/internal/config/config.go
  - scripts/check.sh
  - scripts/test.sh
  </read_first>
  <action>
  Update `docs/API.md` so the canonical route index mirrors the current live surface exactly:
  - Keep the public auth section limited to `/healthz`, `/metrics`, `/api/v1/auth/login`, and `/api/v1/auth/register`.
  - Keep the app route tables for chat, agent, memory, mcp, notification, quota, console, preferences, tasks, knowledge, websocket, admin, marketplace, and relay.
  - Preserve the `GET /v1/models` note under "Not Routed In This Release Candidate".

  Update `docs/architecture/current-system-contracts.md` so it matches the current consolidated mainline:
  - Reframe the scope and current-state prose to the v03.3 mainline consolidation state instead of the older v03.2 RC framing.
  - Keep sections 6.8 through 6.11 aligned with the live route registry and the browser route matrix in src/web/src/app/router.tsx.
  - Add the current backend env variables `RELAY_ENABLED`, `RELAY_DEFAULT_MODEL`, `OPENAI_API_KEY`, and `OPENAI_BASE_URL` to the environment matrix with their current defaults.
  - Keep the release gate commands section explicit about `bash scripts/check.sh docs`, `bash scripts/check.sh all`, and `bash scripts/test.sh all`.
  - Preserve the Knowledge Beta and SOLO runtime boundary statements from the supporting architecture decision docs instead of implying a deeper feature scope.
  </action>
  <verify>
  <automated>rg -n "## Relay /v1 Endpoints|### 6.8 Agent, Memory, MCP, Notification, And Quota|### 6.11 WebSocket And Relay|RELAY_ENABLED|RELAY_DEFAULT_MODEL|OPENAI_API_KEY|OPENAI_BASE_URL|bash scripts/check.sh docs|bash scripts/test.sh all|GET /v1/models" docs/API.md docs/architecture/current-system-contracts.md</automated>
  </verify>
  <acceptance_criteria>
  - `docs/API.md` contains `## Relay /v1 Endpoints`.
  - `docs/API.md` still contains the note that `GET /v1/models` is not routed inbound in this release candidate.
  - `docs/architecture/current-system-contracts.md` contains `RELAY_ENABLED`.
  - `docs/architecture/current-system-contracts.md` contains `RELAY_DEFAULT_MODEL`.
  - `docs/architecture/current-system-contracts.md` contains `OPENAI_API_KEY`.
  - `docs/architecture/current-system-contracts.md` contains `OPENAI_BASE_URL`.
  - `docs/architecture/current-system-contracts.md` contains `### 6.11 WebSocket And Relay`.
  - The docs do not introduce any new product scope beyond the current consolidated mainline.
  </acceptance_criteria>
  <done>Docs/API.md and current-system-contracts.md are reconciled against the live route and env surface without rewriting the product boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile README.md and release docs without mixing cleanup debt</name>
  <files>README.md, docs/release/rc-checklist.md, docs/release/deployment-runtime-remediation.md</files>
  <read_first>
  - README.md
  - docs/release/rc-checklist.md
  - docs/release/deployment-runtime-remediation.md
  - docs/API.md
  - docs/architecture/current-system-contracts.md
  - scripts/deploy-validate.sh
  - scripts/check.sh
  - docker-compose.yml
  - Dockerfile.web
  </read_first>
  <action>
  Keep `README.md` summary-level:
  - Point readers to `docs/API.md` for the routed HTTP surface.
  - Point readers to `docs/release/rc-checklist.md` for release-candidate evidence.
  - Keep the mainline boundary short and avoid duplicating route tables.

  Keep the release docs separate and factual:
  - Preserve exact command strings and skip semantics in `docs/release/rc-checklist.md`.
  - Preserve the validated restricted-network `scripts/deploy-validate.sh` form in `docs/release/deployment-runtime-remediation.md`.
  - Keep the remediation note host-specific and factual, including the `/tmp/go-build/.../api-server` 8080 listener note and the explicit `/healthz` smoke target.
  - Do not move unrelated frontend cleanup debt or historical/reference docs into these files.
  </action>
  <verify>
  <automated>rg -n "docs/API.md|docs/release/rc-checklist.md|bash scripts/check.sh docs|bash scripts/deploy-validate.sh|OBLIVIOUS_IMAGE_REGISTRY_PREFIX|http://127.0.0.1:8080/healthz|TEST_DATABASE_URL" README.md docs/release/rc-checklist.md docs/release/deployment-runtime-remediation.md</automated>
  </verify>
  <acceptance_criteria>
  - `README.md` contains `docs/API.md`.
  - `README.md` contains `docs/release/rc-checklist.md`.
  - `docs/release/rc-checklist.md` contains `bash scripts/check.sh docs`.
  - `docs/release/rc-checklist.md` contains the validated restricted-network `bash scripts/deploy-validate.sh` form.
  - `docs/release/deployment-runtime-remediation.md` contains `OBLIVIOUS_IMAGE_REGISTRY_PREFIX=docker.m.daocloud.io/library/`.
  - `docs/release/deployment-runtime-remediation.md` contains `http://127.0.0.1:8080/healthz`.
  - These files do not absorb unrelated cleanup paths from the current dirty worktree.
  </acceptance_criteria>
  <done>README and the release docs point to the live canonical contract surfaces without flattening the docs hierarchy or hiding the deployment caveats.</done>
</task>

</tasks>

<verification_commands>
```bash
rg -n "docs/API.md|docs/release/rc-checklist.md|RELAY_ENABLED|RELAY_DEFAULT_MODEL|OPENAI_API_KEY|OPENAI_BASE_URL|bash scripts/deploy-validate.sh|http://127.0.0.1:8080/healthz" README.md docs/API.md docs/architecture/current-system-contracts.md docs/release/rc-checklist.md docs/release/deployment-runtime-remediation.md
bash scripts/check.sh docs
```
</verification_commands>

<success_criteria>
- README and release docs point readers at the canonical doc layers instead of repeating them.
- docs/API.md and current-system-contracts.md match the live route and env surface.
- The validated restricted-network deployment command remains verbatim in the remediation note.
- No historical/reference files or unrelated frontend cleanup files are pulled into this docs commit.
</success_criteria>

<summary_requirements>
Create `.planning/phases/08-contract-docs-and-release-verification/08-01-SUMMARY.md` with the files changed, commands run, pass/fail status, and any docs or release debt intentionally left outside this plan.
</summary_requirements>
