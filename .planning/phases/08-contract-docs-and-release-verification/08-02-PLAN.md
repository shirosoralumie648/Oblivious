---
phase: 08-contract-docs-and-release-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - .planning/phases/08-contract-docs-and-release-verification/08-VERIFICATION.md
autonomous: true
requirements: [VERIFY-01]
user_setup: []

must_haves:
  truths:
    - "D-06: Phase 8 verification should be docs-first: bash scripts/check.sh docs plus targeted grep and route checks against the live code and script surfaces."
    - "D-07: If broader gates are rerun, record them as confirmation of already-shipped behavior rather than re-proving Phase 7 runtime smoke."
    - "D-08: TEST_DATABASE_URL absence is an intentional skip for bash scripts/test.sh server, not a failure."
    - "D-09: The validated restricted-network scripts/deploy-validate.sh command is the deployment proof baseline and should be referenced verbatim."
    - "D-10: If deployment smoke is mentioned, keep the /healthz target and restricted-network override form explicit."
    - "D-11: Do not fold historical/reference docs into the verification artifact."
    - "D-12: Do not pull src/web/src/theme/tokens.css, src/web/tailwind.config.ts, or src/web/src/routes/workspace/MarketplacePage.tsx into this verification step."
    - "D-13: Knowledge and SOLO boundary docs remain supporting references only; verification does not reopen those scope boundaries."
  artifacts:
    - path: ".planning/phases/08-contract-docs-and-release-verification/08-VERIFICATION.md"
      provides: "phase verification evidence"
      contains:
        - "bash scripts/check.sh docs"
        - "bash scripts/check.sh all"
        - "bash scripts/test.sh all"
        - "TEST_DATABASE_URL"
        - "OBLIVIOUS_IMAGE_REGISTRY_PREFIX=docker.m.daocloud.io/library/"
        - "http://127.0.0.1:8080/healthz"
  key_links:
    - from: "scripts/check.sh and scripts/test.sh"
      to: "08-VERIFICATION.md"
      via: "recording what passed, what skipped, and why"
    - from: "scripts/deploy-validate.sh"
      to: "08-VERIFICATION.md"
      via: "preserving the validated restricted-network deployment proof baseline"
    - from: "07-VERIFICATION.md"
      to: "08-VERIFICATION.md"
      via: "reuse prior runtime proof instead of pretending Phase 7 did not happen"
---

<objective>
Capture the final release-verification evidence for the consolidated mainline after the docs are reconciled.

Purpose: prove the documentation changes line up with the real command surface, record which checks passed or were skipped, and note any remaining external prerequisites instead of treating them as completed gates.
Output: a phase verification artifact that distinguishes passed checks, intentional skips, and any blockers or external prerequisites.
</objective>

<execution_context>
@$HOME/.Codex/get-shit-done/workflows/execute-plan.md
@$HOME/.Codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
- .planning/phases/08-contract-docs-and-release-verification/08-CONTEXT.md
- .planning/phases/08-contract-docs-and-release-verification/08-DISCUSSION-LOG.md
- .planning/phases/08-contract-docs-and-release-verification/08-01-SUMMARY.md
- README.md
- docs/API.md
- docs/architecture/current-system-contracts.md
- docs/release/rc-checklist.md
- docs/release/deployment-runtime-remediation.md
- scripts/check.sh
- scripts/test.sh
- scripts/deploy-validate.sh
- docker-compose.yml
- Dockerfile.web
</read_first>
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| Verification claims pass without recording which checks were intentionally skipped. | High | Capture pass, skip, and blocker status for each command in 08-VERIFICATION.md. |
| The phase re-proves Phase 7 runtime smoke instead of treating it as existing validated evidence. | Medium | Reference the existing restricted-network deploy proof baseline and only confirm it when relevant. |
| TEST_DATABASE_URL is absent but the skip is omitted, causing the verification report to overstate coverage. | High | Record the explicit server integration skip reason verbatim. |
| Historical/reference docs or unrelated frontend cleanup files get mixed into the verification artifact. | Medium | Keep the verification scope limited to the docs and commands in files_modified and the current docs/runbook surface. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run the docs and release verification command set and capture the results</name>
  <files>.planning/phases/08-contract-docs-and-release-verification/08-VERIFICATION.md</files>
  <read_first>
  - .planning/phases/08-contract-docs-and-release-verification/08-CONTEXT.md
  - .planning/phases/08-contract-docs-and-release-verification/08-DISCUSSION-LOG.md
  - docs/API.md
  - docs/architecture/current-system-contracts.md
  - docs/release/rc-checklist.md
  - docs/release/deployment-runtime-remediation.md
  - scripts/check.sh
  - scripts/test.sh
  - scripts/deploy-validate.sh
  - .planning/phases/07-frontend-e2e-and-deployment-gate-alignment/07-VERIFICATION.md
  </read_first>
  <action>
  Run the Phase 8 verification set after the docs have been reconciled:
  - `bash scripts/check.sh docs`
  - `bash scripts/check.sh all`
  - `bash scripts/test.sh all`
  - targeted `rg` checks against `README.md`, `docs/API.md`, `docs/architecture/current-system-contracts.md`, `docs/release/rc-checklist.md`, and `docs/release/deployment-runtime-remediation.md`

  Capture the exact status for each command:
  - pass / fail
  - any intentional skip reason
  - any external prerequisite still needed

  Treat `bash scripts/test.sh server` integration skipping as expected when `TEST_DATABASE_URL` is unset.
  Treat the validated restricted-network `scripts/deploy-validate.sh` path as the deployment proof baseline, not a new Phase 8 runtime experiment.
  </action>
  <verify>
  <automated>bash scripts/check.sh docs</automated>
  <automated>bash scripts/check.sh all</automated>
  <automated>bash scripts/test.sh all</automated>
  <automated>rg -n "docs/API.md|docs/release/rc-checklist.md|bash scripts/deploy-validate.sh|TEST_DATABASE_URL|http://127.0.0.1:8080/healthz|OBLIVIOUS_IMAGE_REGISTRY_PREFIX" README.md docs/API.md docs/architecture/current-system-contracts.md docs/release/rc-checklist.md docs/release/deployment-runtime-remediation.md</automated>
  </verify>
  <acceptance_criteria>
  - `bash scripts/check.sh docs` exits 0.
  - `bash scripts/check.sh all` exits 0.
  - `bash scripts/test.sh all` exits 0 and records the intentional integration skip if `TEST_DATABASE_URL` is absent.
  - The verification artifact records which checks passed, which were skipped, and why.
  - The verification artifact retains the validated restricted-network deploy command as the baseline reference.
  </acceptance_criteria>
  <done>Verification commands have been run and the results are ready for the phase-level verification artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Write the phase verification artifact</name>
  <files>.planning/phases/08-contract-docs-and-release-verification/08-VERIFICATION.md</files>
  <read_first>
  - .planning/phases/08-contract-docs-and-release-verification/08-CONTEXT.md
  - .planning/phases/08-contract-docs-and-release-verification/08-DISCUSSION-LOG.md
  - .planning/phases/08-contract-docs-and-release-verification/08-01-SUMMARY.md
  - .planning/phases/07-frontend-e2e-and-deployment-gate-alignment/07-VERIFICATION.md
  - docs/release/rc-checklist.md
  - docs/release/deployment-runtime-remediation.md
  </read_first>
  <action>
  Create `.planning/phases/08-contract-docs-and-release-verification/08-VERIFICATION.md` as the authoritative phase verification artifact for DOC-02 and VERIFY-01.

  The file must record:
  - the command list run in this phase
  - which commands passed
  - which commands were intentionally skipped
  - the explicit `TEST_DATABASE_URL` skip reason if server integration tests were not run
  - the fact that the restricted-network `scripts/deploy-validate.sh` command remains the validated deployment baseline from Phase 7
  - any remaining blocker that prevents calling the phase fully complete

  Keep the artifact factual and specific. Do not restate the full docs changes or re-open unrelated cleanup files.
  </action>
  <verify>
  <automated>test -f .planning/phases/08-contract-docs-and-release-verification/08-VERIFICATION.md</automated>
  <automated>rg -n "passed|skipped|TEST_DATABASE_URL|OBLIVIOUS_IMAGE_REGISTRY_PREFIX|/healthz|blocked|baseline" .planning/phases/08-contract-docs-and-release-verification/08-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
  - `08-VERIFICATION.md` exists.
  - `08-VERIFICATION.md` records command outcomes with pass/skip/fail status.
  - `08-VERIFICATION.md` records the explicit integration-test skip reason when `TEST_DATABASE_URL` is absent.
  - `08-VERIFICATION.md` records the validated restricted-network deployment baseline and `/healthz` smoke target.
  - `08-VERIFICATION.md` does not mix in unrelated frontend or historical-reference cleanup work.
  </acceptance_criteria>
  <done>The phase verification artifact exists and distinguishes completed checks from intentional skips or remaining blockers.</done>
</task>

</tasks>

<verification_commands>
```bash
bash scripts/check.sh docs
bash scripts/check.sh all
bash scripts/test.sh all
```
</verification_commands>

<success_criteria>
- Phase 8 verification evidence is captured in `08-VERIFICATION.md`.
- The report identifies which checks passed, which were skipped, and why.
- The report keeps the validated restricted-network deploy path explicit.
- No unrelated cleanup or historical-reference work is folded into the verification artifact.
</success_criteria>

<summary_requirements>
Create `.planning/phases/08-contract-docs-and-release-verification/08-02-SUMMARY.md` with the files changed, commands run, pass/fail status, and any remaining blockers or intentional skips.
</summary_requirements>
