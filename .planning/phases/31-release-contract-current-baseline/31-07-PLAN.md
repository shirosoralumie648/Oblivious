---
phase: 31-release-contract-current-baseline
plan: "07"
type: execute
wave: 6
depends_on: ["31-06"]
files_modified:
  - config/release/contract.schema.json
  - config/release/contract.v1.json
  - src/server/internal/releasecontract/contract.go
  - src/server/internal/releasecontract/contract_test.go
  - .planning/phases/31-release-contract-current-baseline/31-01-PLAN.md
  - .planning/phases/31-release-contract-current-baseline/31-03-PLAN.md
  - .planning/phases/31-release-contract-current-baseline/31-04-PLAN.md
  - .planning/phases/31-release-contract-current-baseline/31-05-PLAN.md
  - .planning/phases/31-release-contract-current-baseline/31-06-PLAN.md
autonomous: true
requirements: [RELS-01]
must_haves:
  truths:
    - "Every authored deployment profile carries the same required refreshIntervalSeconds, maxAgeSeconds, and allowedFutureSkewSeconds timing contract through JSON Schema, the typed Go model, checked-in contract values, canonical fixtures, and embedded-provider mutation proof."
    - "The Phase 31 contract and build-identity suites pass from committed clean source; dirty-worktree overlays cannot supply fields that the trusted commit lacks."
    - "All six Phase 31 plans pass both canonical artifact and key-link verification without manual waivers, invalid regular expressions, or patterns that contradict delegated ownership."
    - "Phase 31 remains repository-local foundation evidence only; RELS-01 stays pending until later readiness, parity, target, and live evidence phases close it."
  artifacts:
    - path: "config/release/contract.schema.json"
      provides: "Required bounded deployment-profile timing fields"
      contains: '"allowedFutureSkewSeconds"'
    - path: "config/release/contract.v1.json"
      provides: "Authored timing values for committed and excluded profiles"
      contains: '"maxAgeSeconds": 120'
    - path: "src/server/internal/releasecontract/contract.go"
      provides: "Typed deployment-profile timing contract"
      contains: "RefreshIntervalSeconds"
    - path: "src/server/internal/releasecontract/contract_test.go"
      provides: "Schema, typed-model, checked-in-policy, and missing-field regression coverage"
      contains: "assertDeploymentProfileTiming"
  key_links:
    - from: "config/release/contract.v1.json"
      to: "config/release/contract.schema.json"
      via: "all authored profiles satisfy the required timing schema"
      pattern: "refreshIntervalSeconds|maxAgeSeconds|allowedFutureSkewSeconds"
    - from: "src/server/internal/releasecontract/contract.go"
      to: "src/server/internal/releasecontract/contract_test.go"
      via: "typed timing fields are serialized and asserted for every profile"
      pattern: "RefreshIntervalSeconds|assertDeploymentProfileTiming"
    - from: "src/server/internal/buildinfo/provider_test.go"
      to: "config/release/contract.v1.json"
      via: "embedded provider mutates an authored timing field and detects digest drift"
      pattern: "maxAgeSeconds"
---

<objective>
Close the two blocking Phase 31 verification gaps by committing one coherent deployment-profile timing contract and repairing structural-verifier metadata to match the implemented semantic links.

Purpose: Make the exact committed snapshot pass its own Stage A, full Go regression, and plan-structure checks without borrowing a dirty Phase 31.1 overlay or accepting manual waivers.
Output: Timing-complete release contract/model/tests, automatically verifiable Phase 31 plan metadata, and fresh repository-local verification evidence ready for an exact-final-HEAD Stage B run.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-release-contract-current-baseline/31-CONTEXT.md
@.planning/phases/31-release-contract-current-baseline/31-RESEARCH.md
@.planning/phases/31-release-contract-current-baseline/31-REVIEW.md
@.planning/phases/31-release-contract-current-baseline/31-VALIDATION.md
@.planning/phases/31-release-contract-current-baseline/31-SECURITY.md
@.planning/phases/31-release-contract-current-baseline/31-VERIFICATION.md
@.planning/phases/31-release-contract-current-baseline/31-06-SUMMARY.md

<interfaces>
The checked-in digest fixtures and `TestEmbeddedProviderRecomputesContractDigest` already consume these authored JSON field names:

```text
refreshIntervalSeconds
maxAgeSeconds
allowedFutureSkewSeconds
```

The typed model uses exact Go fields:

```go
RefreshIntervalSeconds   int64 `json:"refreshIntervalSeconds"`
MaxAgeSeconds            int64 `json:"maxAgeSeconds"`
AllowedFutureSkewSeconds int64 `json:"allowedFutureSkewSeconds"`
```

The structural gates are invoked for every Phase 31 plan with:

```bash
node /home/shirosora/.codex/gsd-core/bin/gsd-tools.cjs query verify.artifacts <plan>
node /home/shirosora/.codex/gsd-core/bin/gsd-tools.cjs query verify.key-links <plan>
```
</interfaces>
</context>

## Artifacts this phase produces

- Modified schema fields: required `refreshIntervalSeconds` (integer, minimum 1), `maxAgeSeconds` (integer, minimum 1), and `allowedFutureSkewSeconds` (integer, minimum 0) on every deployment profile.
- Modified Go fields: `DeploymentProfile.RefreshIntervalSeconds`, `DeploymentProfile.MaxAgeSeconds`, and `DeploymentProfile.AllowedFutureSkewSeconds`.
- Authored values: `30`, `120`, and `30` respectively for `dual`, `microservices`, `monolith`, and `split`; commitments and profile inclusion remain unchanged.
- Test helpers: `assertDeploymentProfileTiming`; no new runtime service, endpoint, migration, environment variable, or release claim.
- Planning metadata: corrected `contains` and `pattern` values in Plans 31-01, 31-03, 31-04, 31-05, and 31-06; Plan 31-02 remains unchanged because both gates already pass.

<tasks>

<task type="auto">
  <name>Task 1: Commit the deployment-profile timing contract as one coherent revision</name>
  <read_first>
    - config/release/contract.schema.json
    - config/release/contract.v1.json
    - src/server/internal/releasecontract/contract.go
    - src/server/internal/releasecontract/contract_test.go
    - src/server/internal/releasecontract/digest_test.go
    - src/server/internal/releasecontract/testdata/canonical-equivalent-a.json
    - src/server/internal/releasecontract/testdata/canonical-equivalent-b.json
    - src/server/internal/releasecontract/testdata/canonical-semantic-change.json
    - src/server/internal/buildinfo/provider_test.go
  </read_first>
  <files>config/release/contract.schema.json, config/release/contract.v1.json, src/server/internal/releasecontract/contract.go, src/server/internal/releasecontract/contract_test.go</files>
  <behavior>
    - Every profile must contain the three timing fields; missing values fail schema validation before typed use.
    - `refreshIntervalSeconds` and `maxAgeSeconds` are positive integers; `allowedFutureSkewSeconds` permits zero but rejects negative values.
    - All four checked-in profiles retain their prior commitment/reason/topology semantics while authoring timing values 30/120/30.
    - Canonical-equivalent fixtures retain the same digest and the semantic fixture changes only maxAgeSeconds; embedded-provider mutation changes 120 to 121 and returns contract_digest_mismatch.
  </behavior>
  <action>Take ownership of the already-authorized four-file timing overlay. In the JSON Schema add all three exact camelCase fields to the deployment-profile `required` array and properties with the integer minimums above. In `contract.v1.json` author 30/120/30 on all four profiles without promoting excluded profiles or changing operation arrays. In `DeploymentProfile` add the exact int64 JSON fields. Keep the existing schema-baseline assertion, typed JSON-field assertion, checked-in per-profile timing assertion, missing-field negative case, and model fixture values in `contract_test.go`; preserve unrelated semantic negative cases already present. Run the canonical digest tests and retain `sha256:2baeda420ab178420e0fcb64072ce3c91bc3b821ed8dd1eede6eac073864cbfa` only if the freshly computed canonical fixture output matches it; never update a golden merely to hide a semantic mismatch.</action>
  <acceptance_criteria>
    - `go test ./internal/releasecontract ./internal/buildinfo -count=1` exits 0.
    - Removing any required timing field from a profile returns `contract_schema_invalid`.
    - `TestCanonicalBytesGolden`, `TestDigestEquivalentFormatting`, `TestDigestSemanticMutation`, and `TestEmbeddedProviderRecomputesContractDigest` all pass without modifying their intended mutations.
    - `git diff -- config/release/contract.v1.json` shows timing additions only; profile commitments and the default profile remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd src/server &amp;&amp; go test ./internal/releasecontract ./internal/buildinfo -count=1</automated>
  </verify>
  <done>The trusted commit contains every field already required by its canonical and embedded-provider proof paths, so clean-source behavior no longer depends on an uncommitted overlay.</done>
</task>

<task type="auto">
  <name>Task 2: Repair the eight structural-verifier metadata failures</name>
  <read_first>
    - .planning/phases/31-release-contract-current-baseline/31-01-PLAN.md
    - .planning/phases/31-release-contract-current-baseline/31-02-PLAN.md
    - .planning/phases/31-release-contract-current-baseline/31-03-PLAN.md
    - .planning/phases/31-release-contract-current-baseline/31-04-PLAN.md
    - .planning/phases/31-release-contract-current-baseline/31-05-PLAN.md
    - .planning/phases/31-release-contract-current-baseline/31-06-PLAN.md
    - src/server/internal/buildinfo/provider.go
    - src/server/internal/surfacereport/build.go
    - scripts/build-release-image.sh
    - scripts/verify-release-build-fixtures.sh
    - scripts/verify-release-contract-fixtures.sh
    - scripts/verify-release-contract.sh
  </read_first>
  <files>.planning/phases/31-release-contract-current-baseline/31-01-PLAN.md, .planning/phases/31-release-contract-current-baseline/31-03-PLAN.md, .planning/phases/31-release-contract-current-baseline/31-04-PLAN.md, .planning/phases/31-release-contract-current-baseline/31-05-PLAN.md, .planning/phases/31-release-contract-current-baseline/31-06-PLAN.md</files>
  <behavior>
    - Plan 31-01 recognizes the authored `release-profile-operation.sh` path.
    - Plan 31-03 recognizes `releasecontract.Digest` and the `rev-parse` HEAD commit/tree calls with valid regular expressions.
    - Plan 31-04 recognizes the concrete `identities.Resolve` call.
    - Plan 31-05 recognizes the actual `git -C ... status --porcelain=v1 --untracked-files=all` command and delegated build-fixture call.
    - Plan 31-06 recognizes delegated dirty-source proof through `verify-release-build-fixtures.sh` and the Stage B build-wrapper call.
  </behavior>
  <action>Change only the failing `must_haves` metadata. Remove YAML double-escaping from `release-profile-operation.sh`, `releasecontract.Digest`, and `build-release-image.sh`; replace the invalid HEAD brace regex with `rev-parse|HEAD`; replace interface-shaped `IdentityProvider.Resolve` with concrete `identities.Resolve`; replace the Plan 31-05 artifact literal with `status --porcelain=v1 --untracked-files=all`; and replace Plan 31-06's incorrectly local `source_worktree_dirty` artifact assertion with `verify-release-build-fixtures.sh`, which is the actual delegated owner invoked by that script. Do not weaken paths, delete links, add waivers, or modify Plan 31-02.</action>
  <acceptance_criteria>
    - The six `verify.artifacts` invocations report `all_passed: true`, totaling 21/21 artifacts.
    - The six `verify.key-links` invocations report `all_verified: true`, totaling 21/21 links.
    - No result contains `Invalid regex pattern`, `Missing pattern`, or `not found in source or target`.
    - Only frontmatter `contains`/`pattern` values change in the five existing plans.
  </acceptance_criteria>
  <verify>
    <automated>for plan in .planning/phases/31-release-contract-current-baseline/31-0{1,2,3,4,5,6}-PLAN.md; do node /home/shirosora/.codex/gsd-core/bin/gsd-tools.cjs query verify.artifacts "$plan" | jq -e '.all_passed == true'; node /home/shirosora/.codex/gsd-core/bin/gsd-tools.cjs query verify.key-links "$plan" | jq -e '.all_verified == true'; done</automated>
  </verify>
  <done>Every pre-existing Phase 31 artifact and semantic link is machine-verifiable with metadata that matches the implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full repository-local gap-closure gate</name>
  <read_first>
    - scripts/verify-release-contract.sh
    - scripts/check.sh
    - .planning/phases/31-release-contract-current-baseline/31-VERIFICATION.md
    - .planning/phases/31-release-contract-current-baseline/31-REVIEW.md
    - .planning/phases/31-release-contract-current-baseline/31-VALIDATION.md
    - .planning/phases/31-release-contract-current-baseline/31-SECURITY.md
  </read_first>
  <files>.planning/phases/31-release-contract-current-baseline/31-REVIEW.md, .planning/phases/31-release-contract-current-baseline/31-VALIDATION.md, .planning/phases/31-release-contract-current-baseline/31-SECURITY.md, .planning/phases/31-release-contract-current-baseline/31-VERIFICATION.md</files>
  <behavior>
    - Stage A and full Go regression must pass from the same source revision with no Phase 31 failure.
    - DB-backed integration absence is reported separately and cannot be promoted to Phase 31 or target/live proof.
    - Review has no critical/warning issue, validation is Nyquist compliant, security has threats_open 0, and verification is passed only after fresh evidence.
    - Stage B is rerun from a fresh detached worktree at the exact final tracking commit with zero skips before push.
  </behavior>
  <action>After Tasks 1 and 2 are committed, run `bash scripts/verify-release-contract.sh --stage-a`, `cd src/server &amp;&amp; go test ./... -count=1`, `bash scripts/check.sh docs`, all twelve structural checks, and `git diff --check`. Refresh 31-REVIEW, 31-VALIDATION, 31-SECURITY, and 31-VERIFICATION from these exact results while keeping evidence class repository-local and RELS-01 pending. Commit the final tracking artifacts, create a fresh detached worktree at that exact HEAD, require empty porcelain including untracked files, and run `bash scripts/verify-release-contract.sh --clean-head --profile monolith`. Record the exact commit/tree/contract digest, environment, command, migration state, pass/fail, zero skips, and residual risks. Any post-Stage-B commit invalidates the tuple and requires a fresh run; push only the exact passing HEAD.</action>
  <acceptance_criteria>
    - Stage A, full Go regression, docs check, structural checks, and diff check all exit 0.
    - `31-REVIEW.md` has no open critical/warning finding, `31-VALIDATION.md` is compliant, `31-SECURITY.md` reports `threats_open: 0`, and `31-VERIFICATION.md` reports `status: passed` with current evidence.
    - Final detached worktree porcelain is empty and Stage B exits 0 with zero skips at the exact push candidate HEAD.
    - No documentation claims target/live or commercial release readiness, and RELS-01 is not marked complete.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/verify-release-contract.sh --stage-a &amp;&amp; (cd src/server &amp;&amp; go test ./... -count=1) &amp;&amp; bash scripts/check.sh docs &amp;&amp; git diff --check</automated>
  </verify>
  <done>Fresh repository-local evidence closes both Phase 31 gaps, and the exact final clean HEAD has a non-skipped Stage B tuple eligible for push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authored JSON -> schema/typed model | Timing values must be required, bounded, and represented identically before they affect later readiness logic. |
| dirty worktree -> trusted commit | Existing uncommitted timing fields cannot count as clean-source proof until incorporated into the exact committed contract revision. |
| plan metadata -> structural verifier | Patterns are proof selectors, not manual assertions; malformed or fictitious selectors must fail planning/verification. |
| final tracking commit -> Stage B/push | Any commit after Stage B changes the trusted tuple and invalidates the prior result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31-07-01 | Tampering | canonical contract digest | high | mitigate | Require timing fields in schema/model/authored JSON, retain semantic mutation tests, and rerun Stage A plus full regression from committed source. |
| T-31-07-02 | Spoofing | embedded build identity | high | mitigate | Mutate the authored maxAgeSeconds value and require recomputed digest mismatch from EmbeddedProvider. |
| T-31-07-03 | Repudiation | structural plan proof | medium | mitigate | Require 21/21 artifacts and 21/21 key links through canonical machine checks with no waiver. |
| T-31-07-04 | Tampering | final release tuple | high | mitigate | Run Stage B only in a fresh detached clean worktree at the final tracking commit; rerun after any commit. |
| T-31-07-05 | Information Disclosure | release logs | medium | mitigate | Keep evidence repository-local/ignored, report stable codes, and never read or emit real secrets or target artifacts. |
</threat_model>

<verification>
- Contract packages: `cd src/server &amp;&amp; go test ./internal/releasecontract ./internal/buildinfo -count=1`.
- Phase gate: `bash scripts/verify-release-contract.sh --stage-a`.
- Full regression: `cd src/server &amp;&amp; go test ./... -count=1`.
- Documentation and metadata: `bash scripts/check.sh docs`, all six artifact checks, all six key-link checks, and `git diff --check`.
- Exact-final-HEAD gate after tracking commit: empty porcelain plus `bash scripts/verify-release-contract.sh --clean-head --profile monolith` from a fresh detached worktree.
</verification>

<success_criteria>
- The canonical contract, typed model, checked-in profiles, golden vectors, and embedded-provider mutation agree on the three timing fields.
- Stage A and full Go regression pass without a dirty overlay.
- Structural verification reports 21/21 artifacts and 21/21 key links.
- Review, Nyquist validation, security, and canonical verification are clean with fresh repository-local evidence.
- The exact final clean HEAD passes Stage B with zero skips and is pushed without a subsequent identity-changing commit.
- RELS-01 remains pending for later readiness, parity, target, and live proof.
</success_criteria>

<output>
Create `.planning/phases/31-release-contract-current-baseline/31-07-SUMMARY.md` when done.
</output>
