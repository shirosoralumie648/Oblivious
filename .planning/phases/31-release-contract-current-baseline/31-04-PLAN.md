---
phase: 31-release-contract-current-baseline
plan: "04"
type: execute
wave: 4
depends_on: ["31-03"]
files_modified:
  - src/server/internal/surfacereport/report.go
  - src/server/internal/surfacereport/registry.go
  - src/server/internal/surfacereport/build.go
  - src/server/internal/surfacereport/writer.go
  - src/server/internal/surfacereport/report_test.go
  - src/server/internal/surfacereport/registry_test.go
  - src/server/internal/surfacereport/writer_test.go
autonomous: true
requirements: [RELS-01]
must_haves:
  truths:
    - "Every producer shares one nested SurfaceReportV1 whose top level contains only schemaVersion, releaseIdentity, surfaceIdentity, drift, evidence, and outcome (D-FND-09)."
    - "evidence.details is accepted only through a typed registry; the foundation registers and emits surface ID `build-identity`, while duplicate/unregistered schemas fail (D-FND-09)."
    - "releaseIdentity is resolved by the shared trusted provider plus the validated authored-profile resolver; raw, unknown, conditional, or excluded profile strings cannot enter a report (D-FND-03, D-FND-10)."
    - "Validated reports are written with same-directory temp, file sync, close, rollback-backed atomic replace, parent sync, cleanup, and byte-for-byte prior-destination preservation on every injected failure (D-FND-10)."
    - "Report evidence is repository-local and contains explicit residual risks without readiness/parity/E3/E4 claims (D-FND-12)."
  artifacts:
    - path: "src/server/internal/surfacereport/report.go"
      provides: "Nested SurfaceReportV1 envelope and validation"
      exports: ["SurfaceReportV1", "NewReport", "Validate"]
    - path: "src/server/internal/surfacereport/registry.go"
      provides: "Typed evidence.details registry API"
      exports: ["DetailsRegistry", "NewDetailsRegistry", "RegisterDetails"]
    - path: "src/server/internal/surfacereport/build.go"
      provides: "Registered build-identity details and trusted report construction"
      exports: ["BuildIdentitySurfaceID", "BuildIdentityDetails", "NewBuildIdentityReport"]
    - path: "src/server/internal/surfacereport/writer.go"
      provides: "Fault-injectable atomic report writer"
      exports: ["ReportWriter", "AtomicWriter"]
  key_links:
    - from: "src/server/internal/surfacereport/build.go"
      to: "src/server/internal/buildinfo/provider.go"
      via: "producer resolves release identity internally"
      pattern: "IdentityProvider\\.Resolve"
    - from: "src/server/internal/surfacereport/build.go"
      to: "src/server/internal/releasecontract/load.go"
      via: "producer resolves the explicit deployment profile from the validated authored contract"
      pattern: "ResolveCommittedProfile"
    - from: "src/server/internal/surfacereport/report.go"
      to: "src/server/internal/surfacereport/registry.go"
      via: "evidence.details decode/validate by surface ID"
      pattern: "DetailsRegistry"
    - from: "src/server/internal/surfacereport/writer.go"
      to: "SurfaceReportV1"
      via: "validate before same-directory temp and rename"
      pattern: "Validate|Rename|Sync"
---

<objective>
Create the shared nested report protocol, typed details registry, foundation `build-identity` producer contract, and atomic writer required by D-FND-09/D-FND-10, bounded by D-FND-12.

Purpose: Let later phases add only their owned details schemas/producers while reusing one trusted identity and persistence boundary.
Output: SurfaceReportV1 types/validation, extensible typed registry, build details/report constructor, and failure-safe atomic writer.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-release-contract-current-baseline/31-CONTEXT.md
@.planning/phases/31-release-contract-current-baseline/31-RESEARCH.md
@.planning/phases/31-release-contract-current-baseline/31-PATTERNS.md
@.planning/phases/31-release-contract-current-baseline/31-03-SUMMARY.md

<interfaces>
From Plan 31-03:

```go
type IdentityProvider interface {
    Resolve(ctx context.Context, repoRoot, contractPath, schemaPath string) (buildinfo.BuildIdentityV1, error)
}
```

From Plan 31-01:

```go
type ProfileResolver interface {
    ResolveCommittedProfile(ctx context.Context, repoRoot, contractPath, schemaPath, profileID string) (releasecontract.DeploymentProfile, error)
}
```

This plan creates:

```go
type SurfaceReportV1 struct {
    SchemaVersion string `json:"schemaVersion"`
    ReleaseIdentity ReleaseIdentity `json:"releaseIdentity"`
    SurfaceIdentity SurfaceIdentity `json:"surfaceIdentity"`
    Drift Drift `json:"drift"`
    Evidence Evidence `json:"evidence"`
    Outcome Outcome `json:"outcome"`
}

func RegisterDetails[T any](r *DetailsRegistry, surfaceID string, validate func(T) error) error
func NewBuildIdentityReport(ctx context.Context, identities buildinfo.IdentityProvider, profiles releasecontract.ProfileResolver, repoRoot, contractPath, schemaPath, profileID string, details BuildIdentityDetails, outcome Outcome) (SurfaceReportV1, error)
type ReportWriter interface { Write(context.Context, string, SurfaceReportV1) error }
```
</interfaces>
</context>

## Artifacts this phase produces

- New files: `src/server/internal/surfacereport/report.go`, `registry.go`, `build.go`, `writer.go`, `report_test.go`, `registry_test.go`, and `writer_test.go`.
- New exported symbols: `SurfaceReportV1`, `ReleaseIdentity`, `SurfaceIdentity`, `Drift`, `Evidence`, `Outcome`, `Result`, `NewReport`, `Validate`, `DetailsRegistry`, `NewDetailsRegistry`, `RegisterDetails`, `BuildIdentitySurfaceID`, `BuildIdentityDetails`, `BinaryInspection`, `OCIInspection`, `PackagedContractInspection`, `NewBuildIdentityReport`, `ReportWriter`, `AtomicWriter`, `NewAtomicWriter`, and `PreserveProducerError`.
- Exact build-report signature: `NewBuildIdentityReport(ctx, identities, profiles, repoRoot, contractPath, schemaPath, profileID, details, outcome)`. Both resolvers receive the same explicit authority paths; the report copies `resolvedProfile.ID`, never the raw `profileID`, into `releaseIdentity.deploymentProfile`.
- New report fields: top-level `schemaVersion`, `releaseIdentity`, `surfaceIdentity`, `drift`, `evidence`, `outcome`; nested identity fields `releaseCommit`, `sourceTree`, `contractDigest`, `deploymentProfile`, `dirty`, `evidenceClass`; surface fields `surface`, `canonicalSource`, `consumer`, `version`, `sourceDigest`, `consumerDigest`; drift fields `missing`, `extra`, `incompatible`; evidence fields `class`, `environment`, `mode`, `checkedAt`, `toolVersions`, `details`; outcome fields `result`, `errorCodes`, `skippedChecks`.
- New `build-identity` details fields: `binaries`, `oci`, `packagedContract`, with per-component names/paths/digests/identity tuples and comparison results. No CLI flags are introduced by this plan.

<tasks>

<task type="auto">
  <name>Task 1: Define the nested envelope and typed details registry</name>
  <read_first>
    - .planning/phases/31-release-contract-current-baseline/31-CONTEXT.md
    - .planning/phases/31-release-contract-current-baseline/31-RESEARCH.md
    - src/server/internal/buildinfo/identity.go
  </read_first>
  <files>src/server/internal/surfacereport/report.go, src/server/internal/surfacereport/registry.go, src/server/internal/surfacereport/report_test.go, src/server/internal/surfacereport/registry_test.go</files>
  <behavior>
    - Exact nested top-level fields validate; legacy flat/unknown fields fail strict decode.
    - drift accepts only sorted unique missing/extra/incompatible entries.
    - environment/mode/tool versions/details live only under evidence; errors/skips live only under outcome.
    - Unregistered, duplicate, type-mismatched, or arbitrary details fail.
  </behavior>
  <action>Implement D-FND-09 as closed typed structs plus strict JSON decode/validation. Enforce schema `surface-report/v1`, exact evidence/result enums, RFC3339Nano UTC checkedAt, sorted/unique drift/error/skip arrays, `pass` only with empty drift/errors/skips, and release/evidence class consistency. Implement `DetailsRegistry` with an internal surface-ID map and generic `RegisterDetails[T]` helper that records the concrete decoder/validator; duplicate registration and unknown surface IDs return `surface_schema_invalid`. Registry callers may extend an instance with owned schemas in later phases, but cannot replace envelope validation or the foundation registration.</action>
  <acceptance_criteria>
    - Tests reject flat identity fields, unknown top-level/nested fields, misplaced evidence/outcome data, invalid pass outcomes, and non-deterministic arrays.
    - Registry rejects duplicate/unregistered/type-confused details.
    - Validation never accepts an open `map[string]any` details authority.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/surfacereport '^(TestSurfaceReportV1NestedValidation|TestSurfaceReportV1RejectsFlatAndMisplacedFields|TestDetailsRegistryRejectsDuplicateUnknownAndWrongType)$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>All current and future producers share one closed envelope and one typed details-registration protocol.</done>
</task>

<task type="auto">
  <name>Task 2: Register and construct the trusted build-identity surface</name>
  <read_first>
    - src/server/internal/surfacereport/report.go
    - src/server/internal/surfacereport/registry.go
    - src/server/internal/buildinfo/provider.go
    - src/server/internal/releasecontract/load.go
    - Dockerfile.server
  </read_first>
  <files>src/server/internal/surfacereport/build.go, src/server/internal/surfacereport/report_test.go, src/server/internal/surfacereport/registry_test.go</files>
  <behavior>
    - Foundation registry contains exactly one built-in registration for surface ID `build-identity`.
    - Producer observations contain component evidence but no releaseIdentity input.
    - Report build fields come from IdentityProvider, while deploymentProfile comes only from ProfileResolver's committed result; raw profile input is never copied.
    - Omitted, unknown, conditional, or excluded profiles fail; with the checked-in contract, only explicit committed `monolith` can construct a passing report.
    - Component mismatch details make outcome fail.
  </behavior>
  <action>Complete D-FND-03/D-FND-09/D-FND-10 by defining `BuildIdentityDetails` for the three active binaries, OCI labels, and packaged contract inspection, registering it under constant `BuildIdentitySurfaceID = "build-identity"`, and constructing its report through the exact `NewBuildIdentityReport(ctx, identities, profiles, repoRoot, contractPath, schemaPath, profileID, details, outcome)` signature. Resolve the build tuple through `IdentityProvider.Resolve` and the deployment profile through `ProfileResolver.ResolveCommittedProfile` using the same explicit repo/contract/schema authority. Require a non-empty requested ID, reject unknown/conditional/excluded profiles, compare the two resolver contexts, and copy only `resolvedProfile.ID` into `releaseIdentity`; never copy the raw string. The checked-in contract therefore permits a passing report only for explicit committed `monolith`. Set surface identity to `build-identity`, canonical source `config/release/contract.v1.json`, consumer `binary-oci-packaged-contract-inspector`, version `v1`. Reject caller identity-shaped fields in details JSON, missing components, duplicate binary names, identity/digest mismatch, nonempty committed skips, and E3/E4/readiness/full-RELS-01 wording in structured claim fields. Tests include unknown/excluded substitution and a valid `monolith` result sourced from the resolver.</action>
  <acceptance_criteria>
    - A valid three-binary/OCI/package detail value produces a schema-valid nested report with provider identity.
    - Caller identity substitution, raw unknown/excluded/conditional profile input, and any component mismatch fail deterministically; the emitted profile equals the resolver-returned committed ID.
    - Registry lookup for `build-identity` succeeds; unrelated details remain unregistered.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/surfacereport '^(TestBuildIdentityDetailsRegistration|TestNewBuildIdentityReportUsesTrustedIdentityAndCommittedProfile|TestNewBuildIdentityReportRejectsUnknownExcludedAndConditionalProfile|TestBuildIdentityReportRejectsCallerIdentityAndMismatch)$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>The foundation owns and emits one trusted `build-identity` report contract that downstream aggregation can consume without rebuilding the envelope.</done>
</task>

<task type="auto">
  <name>Task 3: Write validated reports atomically and preserve producer failures</name>
  <read_first>
    - src/server/internal/surfacereport/report.go
    - src/server/internal/surfacereport/registry.go
    - .planning/phases/31-release-contract-current-baseline/31-VALIDATION.md
  </read_first>
  <files>src/server/internal/surfacereport/writer.go, src/server/internal/surfacereport/writer_test.go</files>
  <behavior>
    - Missing parent directories are created and a valid report replaces destination atomically.
    - Injected write/file-sync/close/rename and post-rename directory-sync failures remove staging/rollback files and preserve prior valid bytes; an initially absent destination remains absent.
    - Unwritable destinations return `report_output_unwritable`.
    - Producer error remains authoritative when best-effort failure-report writing also fails.
  </behavior>
  <action>Implement D-FND-10 with `AtomicWriter` and a narrow one-shot failpoint filesystem boundary used only for deterministic failure tests. Validate report/registry before touching the destination and capture the previous destination bytes/absence. Create parents, create a uniquely named staging file in the destination directory, write complete canonical JSON, file sync, and close. When a destination exists, create and sync a same-directory rollback snapshot of its exact bytes and sync the parent before replacement. Atomically rename staging over the destination, then sync the parent directory. If any failure occurs before rename, remove staging/rollback artifacts and return with the old destination untouched. If the post-rename directory sync fails, atomically restore the synced rollback snapshot over the destination (or remove the newly created destination when none existed), sync the parent through the non-injected rollback path, read back and byte-compare the restored destination/absence, remove staging/rollback artifacts, and only then return the original nonzero writer error. A successful commit removes the rollback snapshot after the parent sync and leaves no residue. The writer must never return a normal write error after rename unless prior bytes/absence have been restored and verified. `PreserveProducerError` retains the original producer error/status and joins the sanitized report-write code only as secondary context.</action>
  <acceptance_criteria>
    - Success produces complete valid JSON with no temp residue.
    - Every named one-shot failpoint, including the post-rename parent-directory sync, leaves an existing valid destination byte-identical (or an initially absent destination absent) with no staging/rollback residue.
    - Producer plus writer failure cannot become success or replace the primary code.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/surfacereport '^(TestAtomicWriterCreatesParentAndReplaces|TestAtomicWriterPreservesDestinationOnInjectedFailures|TestAtomicWriterRollsBackPostRenameDirectorySyncFailure|TestPreserveProducerErrorKeepsPrimaryStatus)$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>Shared reports cannot be partially written, identity-spliced, or converted from producer failure to success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| producer observations -> shared report | Surface-specific data enters a release evidence envelope. |
| identity provider -> report releaseIdentity | Trusted build identity is attached without caller ownership. |
| report -> filesystem | Validated evidence replaces a durable prior destination. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31-04-01 | Spoofing | report releaseIdentity | high | mitigate | Construct build fields only from IdentityProvider and deploymentProfile only from the validated committed ProfileResolver result; reject raw identity/profile substitution and mismatches. |
| T-31-04-02 | Tampering | evidence.details/envelope | high | mitigate | Closed structs, typed per-surface registry, duplicate/unregistered rejection, deterministic validation. |
| T-31-04-03 | Tampering | atomic writer | high | mitigate | Validate first, same-directory staging plus synced rollback snapshot, sync/close/rename/dir-sync, post-rename rollback, byte verification, and one-shot failure injection. |
| T-31-04-04 | Repudiation | producer/write failures | medium | mitigate | Preserve primary producer status and attach stable secondary writer code. |
| T-31-04-05 | Information Disclosure | report evidence | medium | mitigate | Typed allowlisted details and sanitized errors exclude credentials, internal addresses, and raw target evidence. |
</threat_model>

<verification>
- Run all three non-vacuous matched-test selectors and `git diff --check`.
- Writer failure tests are Stage A repository-local and use isolated temporary directories.
- Phase 31.1/31.2 may register owned details schemas but must reuse these envelope/identity/writer APIs.
</verification>

<success_criteria>
- SurfaceReportV1 has exactly the approved nested ownership boundaries.
- `build-identity` is registered and constructed with trusted release identity.
- Atomic output preserves prior valid evidence under all injected failures.
- Claims remain repository-local and do not close RELS-01 or RELS-02.
</success_criteria>

<output>
Create `.planning/phases/31-release-contract-current-baseline/31-04-SUMMARY.md` when done.
</output>
