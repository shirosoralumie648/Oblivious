---
phase: 06-backend-mainline-integration-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/internal/http/route_surface_test.go
  - src/server/internal/http/auth_middleware_test.go
autonomous: true
requirements: [ROUTE-01]

must_haves:
  truths:
    - "D-01: Keep `src/server/internal/http/router.go` as the active backend composition root unless the plan deliberately completes the route-split refactor."
    - "D-03: All app route groups introduced or changed in this slice must require a session: Agent, Memory, MCP, Notification, Quota, Console, Preferences, Chat, Knowledge, Task, and WebSocket."
    - "D-04: Admin routes must continue using `requireAdmin`; Phase 6 must not weaken role checks while normalizing session/user fields."
    - "D-05: `/healthz` and `/metrics` are not changed by default in this phase."
    - "D-07: WebSocket remains cookie-session authenticated via `authMiddleware.currentSession` and user ID binding."
    - "D-21: Do not commit the backend integration slice until the exact staged backend paths match the Phase 5 commit-boundary group."
  artifacts:
    - path: "src/server/internal/http/route_surface_test.go"
      provides: "route surface regression coverage for app/admin/session boundaries"
      contains:
        - "/api/v1/app/agents"
        - "/api/v1/app/memory/documents"
        - "/api/v1/app/mcp-servers"
        - "/api/v1/app/notifications"
        - "/api/v1/app/quota"
        - "/api/v1/console/usage"
        - "/api/v1/app/me/preferences"
        - "/api/v1/app/knowledge-bases"
        - "/api/v1/app/tasks"
        - "/api/v1/ws"
        - "/api/v1/admin/stats"
    - path: "src/server/internal/http/auth_middleware_test.go"
      provides: "auth middleware guard regression coverage"
      contains:
        - "requireAdmin"
        - "currentSession"
  key_links:
    - from: "route surface tests"
      to: "src/server/internal/http/router.go"
      via: "live route registry and middleware boundaries"
    - from: "admin route tests"
      to: "src/server/internal/http/auth_middleware.go"
      via: "requireAdmin"
---

<objective>
Prove the live backend route registry enforces the intended session and admin boundaries for ROUTE-01 without switching the router composition strategy yet.

Purpose: make the current route surface executable as a regression contract before any metadata or auth payload work lands.
Output: focused HTTP middleware and route-surface tests that fail if Agent, Memory, MCP, Notification, Quota, Console, Preferences, Chat, Knowledge, Task, or WebSocket routes drift away from the intended auth model.
</objective>

<execution_context>
@$HOME/.qwen/get-shit-done/workflows/execute-plan.md
@$HOME/.qwen/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
- .planning/phases/06-backend-mainline-integration-hardening/06-CONTEXT.md
- .planning/phases/05-dirty-worktree-triage-and-commit-boundary/05-COMMIT-BOUNDARIES.md
- .planning/phases/05-dirty-worktree-triage-and-commit-boundary/05-WORKTREE-INVENTORY.md
- .planning/ROADMAP.md
- .planning/STATE.md
- src/server/internal/http/router.go
- src/server/internal/http/auth_middleware.go
- src/server/internal/http/server_test.go
- src/server/internal/http/auth_middleware_test.go
- src/server/internal/http/notification_handler.go
- src/server/internal/ws/handler.go
</read_first>
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| Route drift makes a session-required path public again | High | Add table-driven route-surface tests over the live router for the high-risk app route groups. |
| Admin-only APIs accidentally downgrade to a normal session | High | Keep explicit `requireAdmin` regression coverage in `auth_middleware_test.go`. |
| WebSocket access becomes anonymous or token-based | High | Cover `/api/v1/ws` with a session-gate test and keep cookie-session auth as the only path. |
| Health/metrics accidentally change during route cleanup | Medium | Explicitly exclude `/healthz` and `/metrics` from this plan. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add route-surface regression coverage for session and admin boundaries</name>
  <files>src/server/internal/http/route_surface_test.go</files>
  <read_first>
  - src/server/internal/http/router.go
  - src/server/internal/http/server_test.go
  - src/server/internal/http/auth_middleware.go
  - src/server/internal/http/response.go
  - src/server/internal/ws/handler.go
  </read_first>
  <action>
  Create `src/server/internal/http/route_surface_test.go` as a table-driven integration test over `NewRouter(testConfig(), testDatabase(t))`.

  The test must:
  - Assert that unauthenticated requests to these app routes return `401 Unauthorized`: `/api/v1/app/agents`, `/api/v1/app/memory/documents`, `/api/v1/app/mcp-servers`, `/api/v1/app/notifications`, `/api/v1/app/quota`, `/api/v1/console/usage`, `/api/v1/app/me/preferences`, `/api/v1/app/knowledge-bases`, `/api/v1/app/tasks`, and `/api/v1/ws`.
  - Assert that a normal user session receives `403 Forbidden` from admin routes such as `/api/v1/admin/stats`, `/api/v1/admin/channels`, `/api/v1/admin/routes`, `/api/v1/admin/plans`, `/api/v1/admin/users`, `/api/v1/admin/audit-logs`, and `/api/v1/admin/reviews`.
  - Preserve the public auth behavior for `/api/v1/auth/login` and `/api/v1/auth/register`.
  - Keep `/healthz` and `/metrics` out of the auth checks.
  - Reuse the package-local `testDatabase` helper and explicit `httptest` requests rather than introducing new test infrastructure.
  </action>
  <acceptance_criteria>
  - `src/server/internal/http/route_surface_test.go` contains `/api/v1/app/agents`.
  - `src/server/internal/http/route_surface_test.go` contains `/api/v1/app/mcp-servers`.
  - `src/server/internal/http/route_surface_test.go` contains `/api/v1/app/notifications`.
  - `src/server/internal/http/route_surface_test.go` contains `/api/v1/ws`.
  - `src/server/internal/http/route_surface_test.go` contains `/api/v1/admin/reviews`.
  - `src/server/internal/http/route_surface_test.go` contains `401`.
  - `src/server/internal/http/route_surface_test.go` contains `403`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Pin the auth middleware guard primitive</name>
  <files>src/server/internal/http/auth_middleware_test.go</files>
  <read_first>
  - src/server/internal/http/auth_middleware.go
  - src/server/internal/http/auth_middleware_test.go
  - src/server/internal/http/route_surface_test.go
  </read_first>
  <action>
  Extend `auth_middleware_test.go` with a direct `requireAdmin` regression test that uses a non-admin `auth.Session` and proves the middleware returns `403 Forbidden` before the handler runs.

  Keep the existing cookie-signing coverage intact. Do not change the cookie scheme or the `currentSession` signing semantics in this plan.
  </action>
  <acceptance_criteria>
  - `src/server/internal/http/auth_middleware_test.go` contains `requireAdmin`.
  - `src/server/internal/http/auth_middleware_test.go` contains `403`.
  - `src/server/internal/http/auth_middleware_test.go` still contains `currentSession`.
  </acceptance_criteria>
</task>

</tasks>

<verification_commands>
```bash
cd src/server && go test ./internal/http -count=1
```
</verification_commands>

<summary_requirements>
Create `src/server/internal/http/route_surface_test.go` and update `auth_middleware_test.go` with the executed commands, pass/fail status, and any ROUTE-01 gaps that remain after the route-surface regression suite.
</summary_requirements>
