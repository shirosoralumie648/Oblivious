---
phase: 19-marketplace-settlement-and-governance
plan: 01
type: execute
wave: 1
depends_on:
  - 18-subscription-invoice-topup-refund-state-machine
files_modified:
  - src/server/migrations/0030_marketplace_settlement_governance.sql
  - src/server/internal/marketplace/types.go
  - src/server/internal/marketplace/settlement.go
  - src/server/internal/marketplace/settlement_test.go
  - src/server/internal/marketplace/governance.go
  - src/server/internal/marketplace/governance_test.go
  - src/server/internal/marketplace/store.go
  - src/server/internal/marketplace/publisher_analytics.go
  - src/server/internal/stripe/checkout.go
  - src/server/internal/stripe/lifecycle.go
  - src/server/internal/stripe/lifecycle_test.go
  - src/server/internal/http/marketplace_handler.go
  - src/server/internal/http/admin_handler.go
  - src/server/internal/http/router.go
  - src/server/internal/http/server_test.go
  - src/server/internal/http/stripe_handler_test.go
  - src/server/internal/http/admin_marketplace_handler_test.go
  - docs/release/commercial-gates.md
  - .planning/phases/19-marketplace-settlement-and-governance/19-01-SUMMARY.md
autonomous: true
requirements: [MARKET-03, MARKET-04]
user_setup:
  - "No live Stripe keys or payout provider credentials are required. Tests use fake checkout creators and Stripe signed webhook fixtures."
  - "Use TEST_DATABASE_URL with OBLIVIOUS_REQUIRE_TEST_DATABASE=true for DB-backed HTTP tests."
  - "Use restricted-network Go proxy overrides if module downloads fail: GOPROXY=https://mirrors.aliyun.com/goproxy/,direct and GOSUMDB=sum.golang.google.cn."
must_haves:
  truths:
    - "T-01: Free Marketplace installs continue to work through the existing install path."
    - "T-02: Paid Marketplace installs do not create agent_installs before verified payment evidence."
    - "T-03: Paid Marketplace checkout creates one pending marketplace order and one pending payment intent with organization, buyer, publisher, agent, version, gross, platform fee, and publisher net fields."
    - "T-04: checkout.session.completed for checkout_kind=marketplace_install marks the order paid, creates the install, creates one settlement, and is idempotent across duplicate webhook delivery."
    - "T-05: Refund events update Marketplace order refund state and reverse or adjust settlement state exactly once."
    - "T-06: Payout state is modeled in local tables but no external payout API is called."
    - "T-07: Admin takedown prevents new installs without deleting orders, settlements, or prior installs."
    - "T-08: Publisher appeal and admin reinstate are recorded as governance events and do not bypass admin decision state."
    - "T-09: Abuse reports have open/review/resolved/dismissed states and append governance events."
    - "T-10: Phase 20 admin billing inspection and v06 closeout remain future work."
  artifacts:
    - path: "src/server/migrations/0030_marketplace_settlement_governance.sql"
      provides: "Marketplace order, settlement, payout, governance, and abuse-report persistence"
      contains:
        - "marketplace_orders"
        - "marketplace_settlements"
        - "marketplace_payouts"
        - "marketplace_governance_events"
        - "marketplace_abuse_reports"
    - path: "src/server/internal/marketplace/settlement.go"
      provides: "Paid install checkout, settlement, refund, and payout-state service boundary"
      contains:
        - "type SettlementService"
        - "CreatePaidInstallCheckout"
        - "ApplyPaidInstallCheckoutCompleted"
        - "ApplyMarketplaceRefund"
    - path: "src/server/internal/marketplace/governance.go"
      provides: "Takedown, appeal, reinstate, and abuse-report workflow boundary"
      contains:
        - "TakedownAgent"
        - "AppealAgent"
        - "ReinstateAgent"
        - "ReportAbuse"
    - path: "src/server/internal/http/stripe_handler_test.go"
      provides: "DB-backed paid Marketplace webhook lifecycle tests"
      contains:
        - "TestMarketplacePaidInstallDoesNotInstallBeforeWebhook"
        - "TestStripeWebhookRouteAppliesMarketplaceInstallSettlementOnce"
        - "TestStripeRefundUpdatesMarketplaceSettlementOnce"
    - path: "src/server/internal/http/admin_marketplace_handler_test.go"
      provides: "Route tests for takedown, appeal, reinstate, abuse reports, and publisher financial stats"
      contains:
        - "TestMarketplaceGovernanceTakedownAppealAndReinstate"
        - "TestMarketplaceAbuseReportLifecycle"
        - "TestMarketplacePublisherStatsIncludesSettlementAmounts"
  key_links:
    - from: "src/server/internal/http/marketplace_handler.go"
      to: "src/server/internal/marketplace/settlement.go"
      via: "Paid install requests create pending orders and checkout sessions instead of immediate installs"
    - from: "src/server/internal/stripe/lifecycle.go"
      to: "src/server/internal/marketplace/settlement.go"
      via: "Verified Stripe events apply paid install and refund effects through a MarketplaceSettlementApplier interface"
    - from: "marketplace_settlements"
      to: "src/server/internal/marketplace/publisher_analytics.go"
      via: "Publisher stats expose revenue, platform fee, net, refund, and payout state"
    - from: "marketplace_governance_events"
      to: "docs/release/commercial-gates.md"
      via: "MARKET-04 evidence must show auditable Marketplace governance"
---

<objective>
Implement Phase 19 by modeling paid Marketplace settlement, platform fees, payout state, refund impact, takedown, appeal, abuse, and governance audit trails.

Purpose: close `MARKET-03` and `MARKET-04` without enabling Phase 20 admin billing UI, v07 operations, or v08 product completeness prematurely.
</objective>

<context>
<read_first>
- .planning/phases/19-marketplace-settlement-and-governance/19-CONTEXT.md
- .planning/phases/18-subscription-invoice-topup-refund-state-machine/18-01-SUMMARY.md
- .planning/PROJECT.md
- .planning/REQUIREMENTS.md
- .planning/ROADMAP.md
- .planning/STATE.md
- docs/superpowers/specs/2026-05-27-commercial-complete-program-design.md
- docs/release/commercial-gates.md
- src/server/internal/marketplace/service.go
- src/server/internal/marketplace/store.go
- src/server/internal/marketplace/types.go
- src/server/internal/marketplace/publisher_analytics.go
- src/server/internal/http/marketplace_handler.go
- src/server/internal/http/admin_handler.go
- src/server/internal/stripe/checkout.go
- src/server/internal/stripe/lifecycle.go
- src/server/internal/http/billing_handler.go
- src/server/migrations/0023_marketplace.sql
- src/server/migrations/0027_tenant_scope_core_domains.sql
- src/server/migrations/0029_billing_lifecycle.sql
</read_first>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add Marketplace settlement/governance schema and test DB setup</name>
  <files>src/server/migrations/0030_marketplace_settlement_governance.sql, src/server/internal/http/server_test.go</files>
  <action>
  Add persistence for `marketplace_orders`, `marketplace_settlements`, `marketplace_payouts`, `marketplace_governance_events`, and `marketplace_abuse_reports`. Extend the HTTP test database schema with the same tables and indexes.
  </action>
  <schema_contract>
  - `marketplace_orders`: buyer organization/user, publisher organization/user, agent/version, payment intent, checkout session, install, gross amount, platform fee amount, publisher net amount, refunded amount, currency, status, timestamps.
  - `marketplace_settlements`: order, publisher, agent, gross/platform/net/refund amounts, payout ID, status, hold-until, timestamps.
  - `marketplace_payouts`: publisher, amount, currency, provider, provider payout ID, status, metadata, timestamps.
  - `marketplace_governance_events`: actor, organization, agent, action, from/to status, reason, metadata, created_at.
  - `marketplace_abuse_reports`: reporter, agent, reason, details, status, resolution, reviewer, timestamps.
  </schema_contract>
  <verify_red>
  <automated>cd src/server && go test ./internal/marketplace -run 'Settlement|Governance|Abuse|Payout|PublisherStats' -count=1</automated>
  <expected>Initially no Phase 19 settlement/governance tests exist. Add schema first, then continue to RED tests.</expected>
  </verify_red>
</task>

<task type="tdd">
  <name>Task 2: Add paid Marketplace settlement RED tests</name>
  <files>src/server/internal/marketplace/settlement_test.go</files>
  <action>
  Add failing tests:
  - `TestSettlementCreatePaidInstallCheckoutCreatesPendingOrderAndIntent`
  - `TestSettlementApplyPaidInstallCheckoutCompletedCreatesInstallAndSettlementOnce`
  - `TestSettlementApplyRefundAdjustsOrderAndSettlementOnce`
  - `TestSettlementPayoutStateIsLocalOnly`
  </action>
  <verify_red>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test ./internal/marketplace -run 'Settlement' -count=1</automated>
  <expected>FAIL because settlement service/store types and SQL methods do not exist.</expected>
  </verify_red>
</task>

<task type="tdd">
  <name>Task 3: Implement Marketplace settlement service/store</name>
  <files>src/server/internal/marketplace/types.go, src/server/internal/marketplace/settlement.go, src/server/internal/marketplace/store.go</files>
  <action>
  Implement `SettlementService`, `SettlementStore`, paid install checkout creation, checkout-completed application, refund application, local payout state transitions, fee calculation, and idempotency by payment intent/order/refund keys.
  </action>
  <implementation_contract>
  - Default platform fee: `2000` basis points.
  - Pending paid order status: `pending_payment`.
  - Paid order status: `paid`.
  - Refund statuses: `partially_refunded`, `refunded`.
  - Settlement statuses: `pending`, `available`, `payout_pending`, `paid_out`, `partially_refunded`, `reversed`.
  - Paid install completion must create `agent_installs` exactly once for `(organization_id, agent_id, user_id)`.
  - Payout state changes only update local records; they must not call Stripe Connect or any external payout API.
  </implementation_contract>
  <verify_green>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test ./internal/marketplace -run 'Settlement' -count=1</automated>
  <expected>PASS.</expected>
  </verify_green>
</task>

<task type="tdd">
  <name>Task 4: Wire paid install checkout and Stripe lifecycle application</name>
  <files>src/server/internal/http/marketplace_handler.go, src/server/internal/http/router.go, src/server/internal/stripe/checkout.go, src/server/internal/stripe/lifecycle.go, src/server/internal/stripe/lifecycle_test.go, src/server/internal/http/stripe_handler_test.go</files>
  <action>
  Add DB-backed route/lifecycle tests and wire paid Marketplace install into checkout and verified webhook application.
  </action>
  <tests>
  - `TestMarketplacePaidInstallDoesNotInstallBeforeWebhook`
  - `TestStripeWebhookRouteAppliesMarketplaceInstallSettlementOnce`
  - `TestStripeRefundUpdatesMarketplaceSettlementOnce`
  </tests>
  <implementation_contract>
  - Free agents keep the existing install behavior.
  - Paid agents return a checkout response and leave `agent_installs` empty until webhook completion.
  - Checkout metadata includes `checkout_kind=marketplace_install`, `agent_id`, `version_id`, `marketplace_order_id`, `publisher_user_id`, and `publisher_organization_id`.
  - `LifecycleService` uses a `MarketplaceSettlementApplier` interface so `stripe` does not depend directly on marketplace package internals.
  - Duplicate webhook delivery creates one order effect, one install, one settlement, and one lifecycle transition.
  </implementation_contract>
  <verify_green>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test ./internal/http -run 'Marketplace.*Paid|Stripe.*Marketplace|Refund.*Marketplace' -count=1</automated>
  <expected>PASS.</expected>
  </verify_green>
</task>

<task type="tdd">
  <name>Task 5: Add governance and abuse workflow RED tests</name>
  <files>src/server/internal/marketplace/governance_test.go, src/server/internal/http/admin_marketplace_handler_test.go</files>
  <action>
  Add failing tests:
  - `TestGovernanceTakedownPreventsNewInstallsAndPreservesHistory`
  - `TestGovernanceAppealAndReinstateRecordEvents`
  - `TestGovernanceAbuseReportLifecycle`
  - `TestMarketplaceGovernanceTakedownAppealAndReinstate`
  - `TestMarketplaceAbuseReportLifecycle`
  </action>
  <verify_red>
  <automated>cd src/server && go test ./internal/marketplace -run 'Governance|Abuse' -count=1</automated>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test ./internal/http -run 'Marketplace.*(Takedown|Appeal|Abuse)' -count=1</automated>
  <expected>FAIL because takedown, appeal, reinstate, and abuse-report handlers/services do not exist.</expected>
  </verify_red>
</task>

<task type="tdd">
  <name>Task 6: Implement governance and abuse workflows</name>
  <files>src/server/internal/marketplace/governance.go, src/server/internal/marketplace/store.go, src/server/internal/http/marketplace_handler.go, src/server/internal/http/admin_handler.go, src/server/internal/http/router.go</files>
  <action>
  Implement admin takedown, publisher appeal, admin reinstate, abuse report submission, abuse report resolution/dismissal, governance event recording, and install blocking for unavailable paid/free agents.
  </action>
  <route_contract>
  - `POST /api/v1/admin/marketplace/agents/:agentId/takedown`
  - `POST /api/v1/admin/marketplace/agents/:agentId/reinstate`
  - `POST /api/v1/marketplace/agents/:agentId/appeal`
  - `POST /api/v1/marketplace/agents/:agentId/abuse-reports`
  - `POST /api/v1/admin/marketplace/abuse-reports/:reportId/resolve`
  - `POST /api/v1/admin/marketplace/abuse-reports/:reportId/dismiss`
  </route_contract>
  <verify_green>
  <automated>cd src/server && go test ./internal/marketplace -run 'Governance|Abuse' -count=1</automated>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test ./internal/http -run 'Marketplace.*(Takedown|Appeal|Abuse)' -count=1</automated>
  <expected>PASS.</expected>
  </verify_green>
</task>

<task type="tdd">
  <name>Task 7: Extend publisher stats with settlement amounts</name>
  <files>src/server/internal/marketplace/publisher_analytics.go, src/server/internal/marketplace/types.go, src/server/internal/http/admin_marketplace_handler_test.go</files>
  <action>
  Add settlement-backed publisher financial stats: gross revenue, platform fees, net revenue, refunded amount, pending settlement, available settlement, paid-out amount, and payout-pending amount.
  </action>
  <verify_green>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test ./internal/http -run 'MarketplacePublisherStatsIncludesSettlementAmounts' -count=1</automated>
  <expected>PASS.</expected>
  </verify_green>
</task>

<task type="execute">
  <name>Task 8: Update evidence docs and close MARKET-03/MARKET-04 only after verification</name>
  <files>docs/release/commercial-gates.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md, .planning/phases/19-marketplace-settlement-and-governance/19-01-SUMMARY.md</files>
  <action>
  After all RED/GREEN tests pass, update v06 evidence, mark `MARKET-03` and `MARKET-04` complete, move state to Phase 20 ready to plan, and write `19-01-SUMMARY.md` with exact commands and residual Phase 20/v07/v08 work.
  </action>
  <verify>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test ./internal/marketplace -run 'Settlement|Governance|Abuse|Payout|PublisherStats' -count=1</automated>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test ./internal/http -run 'Marketplace.*(Paid|Settlement|Refund|Takedown|Appeal|Abuse|PublisherStats)|Stripe.*Marketplace' -count=1</automated>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test ./internal/marketplace ./internal/stripe ./internal/http -count=1</automated>
  <automated>bash scripts/check.sh docs</automated>
  <automated>GOPROXY=https://mirrors.aliyun.com/goproxy/,direct GOSUMDB=sum.golang.google.cn bash scripts/check.sh all</automated>
  <automated>git diff --check</automated>
  </verify>
</task>

</tasks>

<completion_criteria>
Phase 19 is complete only when MARKET-03 and MARKET-04 have current DB-backed evidence for paid Marketplace order creation, settlement/platform fee/payout-state modeling, refund impact, takedown, appeal, abuse workflow, and append-only governance events. Do not mark v06 complete, do not build Phase 20 admin billing UI, and do not claim final commercial readiness.
</completion_criteria>
