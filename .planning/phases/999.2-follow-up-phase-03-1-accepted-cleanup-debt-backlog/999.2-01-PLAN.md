---
phase: 999.2-follow-up-phase-03-1-accepted-cleanup-debt-backlog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/src/routes/workspace/MarketplacePage.tsx
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - docs/release/rc-checklist.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "D-01: Treat src/web/src/routes/workspace/MarketplacePage.tsx as obsolete duplicate code, not as the active /marketplace implementation."
    - "D-02: Prefer deleting src/web/src/routes/workspace/MarketplacePage.tsx if reference checks prove no active imports, routes, tests, or live docs depend on it."
    - "D-03: If deletion is unsafe because active references still exist, do not rewire the page; stop and record the blocker instead of migrating product behavior."
    - "D-04: Do not wire the old workspace page back into the router. /marketplace stays on src/web/src/routes/marketplace/*."
    - "D-05: Do not migrate the static MCP server catalog into Agent Marketplace in this phase."
    - "D-06: Keep .planning/REQUIREMENTS.md as a living cross-phase context file for this repository."
    - "D-07: Future milestone completion should archive snapshots under .planning/milestones/ and should not reset or delete the living requirements file without a dedicated GSD policy migration."
    - "D-08: If traceability is updated, use small additive rows or notes instead of broad .planning/REQUIREMENTS.md rewrites."
    - "D-09: Keep unrelated dirty files out of this phase, especially src/web/src/theme/tokens.css, src/web/tailwind.config.ts, root historical docs, reference reports, and docs/superpowers/*."
    - "D-10: Phase 999.2 remains cleanup-policy work; no MCP marketplace redesign, Agent Marketplace feature work, or broad GSD workflow reform."
  artifacts:
    - path: "src/web/src/app/router.tsx"
      provides: "active marketplace route evidence"
      contains:
        - "MarketplaceHomePage"
        - "MarketplaceAgentDetailPage"
        - "MarketplacePublishPage"
        - "MarketplaceMyAgentsPage"
    - path: ".planning/REQUIREMENTS.md"
      provides: "living requirements close policy"
      contains:
        - "living"
        - ".planning/milestones"
        - "dedicated GSD policy migration"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 999.2 completion traceability"
      contains:
        - "Phase 999.2"
        - "MarketplacePage"
        - "REQUIREMENTS.md"
    - path: ".planning/STATE.md"
      provides: "current GSD routing state"
      contains:
        - "Phase 999.2"
        - "complete"
  key_links:
    - from: ".planning/phases/999.2-follow-up-phase-03-1-accepted-cleanup-debt-backlog/999.2-CONTEXT.md"
      to: "src/web/src/routes/workspace/MarketplacePage.tsx"
      via: "D-01 through D-05 decide delete-over-migrate cleanup behavior"
    - from: ".planning/phases/999.2-follow-up-phase-03-1-accepted-cleanup-debt-backlog/999.2-CONTEXT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "D-06 through D-08 decide living requirements close policy"
    - from: "src/web/src/app/router.tsx"
      to: ".planning/ROADMAP.md and .planning/STATE.md"
      via: "route evidence supports closing the legacy marketplace cleanup backlog"
---

<objective>
Resolve the Phase 03.1 accepted cleanup debt captured as backlog Phase 999.2.

Purpose: remove or explicitly block the obsolete workspace marketplace surface, record the repository's living requirements close policy, and close the backlog traceability without changing active Agent Marketplace behavior or unrelated dirty worktree files.
Output: the legacy `src/web/src/routes/workspace/MarketplacePage.tsx` cleanup decision, a narrow requirements-policy note, updated active backlog routing, and a Phase 999.2 summary.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
- .planning/phases/999.2-follow-up-phase-03-1-accepted-cleanup-debt-backlog/999.2-CONTEXT.md
- .planning/phases/999.2-follow-up-phase-03-1-accepted-cleanup-debt-backlog/999.2-DISCUSSION-LOG.md
- .planning/ROADMAP.md
- .planning/STATE.md
- .planning/PROJECT.md
- .planning/REQUIREMENTS.md
- .planning/codebase/CONCERNS.md
- .planning/codebase/STRUCTURE.md
- .planning/phases/03.1-admin-marketplace-ui/03.1-07-SUMMARY.md
- .planning/phases/07-frontend-e2e-and-deployment-gate-alignment/07-01-SUMMARY.md
- .planning/phases/08-contract-docs-and-release-verification/08-CONTEXT.md
- src/web/src/app/router.tsx
- src/web/src/routes/workspace/MarketplacePage.tsx
- docs/release/rc-checklist.md
</read_first>
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| Deleting a file that is still actively imported or routed breaks the frontend. | High | Require reference search and router proof before removing `src/web/src/routes/workspace/MarketplacePage.tsx`; stop and record a blocker if active references exist. |
| The cleanup phase migrates the static MCP catalog into Agent Marketplace, creating new product scope. | High | Explicitly forbid MCP catalog migration, `/marketplace` rewiring, and active marketplace feature changes. |
| The requirements policy update rewrites historical requirements or drops cross-phase context. | Medium | Add a narrow living-requirements policy note only; do not reset, delete, or broadly rewrite `.planning/REQUIREMENTS.md`. |
| Existing unrelated dirty files get staged or modified during cleanup. | Medium | Limit files to the `files_modified` list and require explicit-path staging plus final status review. |
| Active release/debt docs remain stale after Phase 999.2 is closed. | Low | Update only the current accepted-debt line in `docs/release/rc-checklist.md` if it still points to unresolved backlog 999.2. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Prove the legacy workspace MarketplacePage is orphaned and remove it</name>
  <files>src/web/src/routes/workspace/MarketplacePage.tsx, docs/release/rc-checklist.md</files>
  <read_first>
  - .planning/phases/999.2-follow-up-phase-03-1-accepted-cleanup-debt-backlog/999.2-CONTEXT.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/STRUCTURE.md
  - .planning/phases/03.1-admin-marketplace-ui/03.1-07-SUMMARY.md
  - .planning/phases/07-frontend-e2e-and-deployment-gate-alignment/07-01-SUMMARY.md
  - src/web/src/app/router.tsx
  - src/web/src/routes/workspace/MarketplacePage.tsx
  - docs/release/rc-checklist.md
  </read_first>
  <action>
  Run a preflight reference audit before changing the legacy page:
  - `git status --short -- src/web/src/routes/workspace/MarketplacePage.tsx`
  - `git ls-files --error-unmatch src/web/src/routes/workspace/MarketplacePage.tsx`
  - `rg -n "MarketplacePage|routes/workspace/MarketplacePage|workspace/MarketplacePage|MCP Server Marketplace|/api/v1/app/mcp-servers" src/web/src src/web/e2e .planning docs README.md package.json`
  - `rg -n "MarketplaceHomePage|MarketplaceAgentDetailPage|MarketplacePublishPage|MarketplaceMyAgentsPage" src/web/src/app/router.tsx src/web/src/routes/marketplace src/web/src/features/marketplace`

  Classify the reference audit:
  - Active blocking references are imports, router elements, tests, or source files under `src/web/src` or `src/web/e2e` that still require `src/web/src/routes/workspace/MarketplacePage.tsx`.
  - Non-blocking references are planning/debt notes, historical milestone archives, docs describing MCP HTTP routes, and untracked reference material under `docs/superpowers/*`.

  If no active blocking references exist, remove `src/web/src/routes/workspace/MarketplacePage.tsx` from the worktree. Do not create an archived replacement unless the preflight audit shows deletion is unsafe. Do not change `src/web/src/app/router.tsx` except to verify that it already routes `/marketplace` to `src/web/src/routes/marketplace/*`.

  If active blocking references do exist, stop this task without deleting the file and record the exact blocker in the Phase 999.2 summary. Do not migrate the MCP static catalog and do not wire the old workspace page back into `/marketplace`.

  If `docs/release/rc-checklist.md` still says the legacy workspace MarketplacePage cleanup is tracked as unresolved backlog 999.2 after deletion, update only that accepted-debt bullet so it records that Phase 999.2 resolved the cleanup. Leave unrelated release checklist content untouched.
  </action>
  <verify>
  <automated>test ! -e src/web/src/routes/workspace/MarketplacePage.tsx</automated>
  <automated>! rg -n "from ['\\\"].*routes/workspace/MarketplacePage|from ['\\\"].*/MarketplacePage|<MarketplacePage|element: <MarketplacePage" src/web/src src/web/e2e</automated>
  <automated>rg -n "MarketplaceHomePage|MarketplaceAgentDetailPage|MarketplacePublishPage|MarketplaceMyAgentsPage" src/web/src/app/router.tsx</automated>
  <automated>COREPACK_HOME=.tmp/corepack pnpm --dir src/web test -- src/routes/marketplace/MarketplacePage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - The preflight audit output is recorded in the summary.
  - `src/web/src/routes/workspace/MarketplacePage.tsx` no longer exists if no active blocking references were found.
  - No source file imports `src/web/src/routes/workspace/MarketplacePage.tsx`.
  - `src/web/src/app/router.tsx` still maps `/marketplace`, `/marketplace/agents/:agentId`, `/marketplace/publish`, and `/marketplace/my-agents` to `src/web/src/routes/marketplace/*`.
  - `docs/release/rc-checklist.md` no longer describes legacy `MarketplacePage.tsx` cleanup as unresolved backlog 999.2 after the cleanup is complete.
  - No MCP static catalog entries are migrated into active Agent Marketplace.
  </acceptance_criteria>
  <done>The obsolete workspace marketplace page is removed or a precise blocker is recorded without changing active marketplace behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Record the living REQUIREMENTS.md close policy</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
  - .planning/phases/999.2-follow-up-phase-03-1-accepted-cleanup-debt-backlog/999.2-CONTEXT.md
  - .planning/REQUIREMENTS.md
  - .planning/milestones/v03.2-REQUIREMENTS.md
  - .planning/STATE.md
  </read_first>
  <action>
  Add a narrow policy note to `.planning/REQUIREMENTS.md` that records the Phase 999.2 closeout decision:
  - `.planning/REQUIREMENTS.md` remains a living cross-phase context file for this repository.
  - Milestone completion should archive requirements snapshots under `.planning/milestones/`.
  - Future milestone completion should not reset or delete the living requirements file unless a dedicated GSD policy migration explicitly owns that change.
  - Historical traceability updates should be additive rows or notes, not broad rewrites.

  Place the note near the existing `## Traceability` or coverage section so future milestone closeout work can find it. Do not rewrite completed requirements, historical requirement lists, future requirements, or out-of-scope tables.
  </action>
  <verify>
  <automated>rg -n "living cross-phase context|\\.planning/milestones|dedicated GSD policy migration|additive rows or notes" .planning/REQUIREMENTS.md</automated>
  <automated>git diff --check -- .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
  - `.planning/REQUIREMENTS.md` contains the phrase `living cross-phase context`.
  - `.planning/REQUIREMENTS.md` names `.planning/milestones/` as the snapshot archive location.
  - `.planning/REQUIREMENTS.md` contains `dedicated GSD policy migration`.
  - `.planning/REQUIREMENTS.md` contains `additive rows or notes`.
  - Existing completed requirement IDs remain present, including `ADMIN-04`, `MARKET-02`, `DOC-02`, and `VERIFY-01`.
  </acceptance_criteria>
  <done>The requirements close policy is recorded without resetting or rewriting the living requirements file.</done>
</task>

<task type="auto">
  <name>Task 3: Close Phase 999.2 planning traceability</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
  - .planning/phases/999.2-follow-up-phase-03-1-accepted-cleanup-debt-backlog/999.2-CONTEXT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - docs/release/rc-checklist.md
  </read_first>
  <action>
  After Tasks 1 and 2 are complete, update only the active GSD traceability needed to close Phase 999.2:
  - In `.planning/ROADMAP.md`, mark both Phase 999.2 items complete and add notes that the legacy workspace MarketplacePage cleanup was resolved and living `.planning/REQUIREMENTS.md` is the repository policy.
  - In `.planning/STATE.md`, record Phase 999.2 as executed or ready for verification, with next suggested step pointing to `$gsd:verify-work` or phase completion instead of `$gsd:plan-phase 999.2`.
  - Preserve the completed Phase 999.1 status and do not reopen mainline Phase 5 through Phase 8 scope.

  Do not modify root `ROADMAP.md`, `CURRENT_STATUS.md`, `ARCHAEOLOGY_REPORT.md`, `docs/reports/*`, `docs/superpowers/*`, `src/web/src/theme/tokens.css`, or `src/web/tailwind.config.ts`.
  </action>
  <verify>
  <automated>rg -n "Phase 999.2|MarketplacePage|REQUIREMENTS.md|resolved|complete|verify-work" .planning/ROADMAP.md .planning/STATE.md</automated>
  <automated>git diff --check -- .planning/ROADMAP.md .planning/STATE.md docs/release/rc-checklist.md</automated>
  </verify>
  <acceptance_criteria>
  - `.planning/ROADMAP.md` marks both Phase 999.2 backlog items complete after execution.
  - `.planning/STATE.md` no longer routes the project back to `$gsd:plan-phase 999.2`.
  - `.planning/STATE.md` records the cleanup and requirements-policy outcomes.
  - Phase 999.1 remains resolved.
  - Unrelated dirty files stay unstaged and unmodified.
  </acceptance_criteria>
  <done>GSD routing and backlog traceability reflect that Phase 999.2 has been executed and is ready for verification or closeout.</done>
</task>

</tasks>

<verification_commands>
```bash
test ! -e src/web/src/routes/workspace/MarketplacePage.tsx
! rg -n "from ['\\\"].*routes/workspace/MarketplacePage|from ['\\\"].*/MarketplacePage|<MarketplacePage|element: <MarketplacePage" src/web/src src/web/e2e
rg -n "MarketplaceHomePage|MarketplaceAgentDetailPage|MarketplacePublishPage|MarketplaceMyAgentsPage" src/web/src/app/router.tsx
rg -n "living cross-phase context|\\.planning/milestones|dedicated GSD policy migration|additive rows or notes" .planning/REQUIREMENTS.md
rg -n "Phase 999.2|MarketplacePage|REQUIREMENTS.md|resolved|complete" .planning/ROADMAP.md .planning/STATE.md docs/release/rc-checklist.md
COREPACK_HOME=.tmp/corepack pnpm --dir src/web test -- src/routes/marketplace/MarketplacePage.test.tsx
git diff --check -- src/web/src/routes/workspace/MarketplacePage.tsx .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md docs/release/rc-checklist.md
```
</verification_commands>

<success_criteria>
- Legacy workspace `MarketplacePage.tsx` is removed when preflight proves no active references depend on it, or a precise blocker is recorded without reintroducing the route.
- The active `/marketplace` route tree remains under `src/web/src/routes/marketplace/*`.
- `.planning/REQUIREMENTS.md` records the living requirements and milestone snapshot policy.
- `.planning/ROADMAP.md` and `.planning/STATE.md` stop routing Phase 999.2 as unplanned backlog after execution.
- No unrelated dirty worktree files are modified or staged.
</success_criteria>

<summary_requirements>
Create `.planning/phases/999.2-follow-up-phase-03-1-accepted-cleanup-debt-backlog/999.2-01-SUMMARY.md` with the files changed, commands run, pass/fail status, the preflight reference-audit result, whether the legacy file was deleted or blocked, the requirements-policy note added, and confirmation that unrelated dirty files were left untouched.
</summary_requirements>
