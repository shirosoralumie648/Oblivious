---
phase: 20-billing-admin-evidence-and-v06-closeout
plan: 01
type: execute
wave: 1
depends_on:
  - 19-marketplace-settlement-and-governance
files_modified:
  - src/server/internal/admin/types.go
  - src/server/internal/admin/store.go
  - src/server/internal/admin/service.go
  - src/server/internal/admin/billing_store.go
  - src/server/internal/admin/billing_service.go
  - src/server/internal/http/admin_handler.go
  - src/server/internal/http/router.go
  - src/server/internal/http/admin_billing_handler_test.go
  - src/web/src/types/admin.ts
  - src/web/src/features/admin/api.ts
  - src/web/src/features/admin/api.test.ts
  - src/web/src/features/layouts/AdminSidebar.tsx
  - src/web/src/features/layouts/AdminSidebar.test.tsx
  - src/web/src/app/router.tsx
  - src/web/src/app/router.test.tsx
  - src/web/src/routes/admin/AdminBillingPage.tsx
  - src/web/src/routes/admin/AdminBillingPage.test.tsx
  - docs/API.md
  - docs/release/commercial-gates.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/20-billing-admin-evidence-and-v06-closeout/20-VERIFICATION.md
  - .planning/phases/20-billing-admin-evidence-and-v06-closeout/20-01-SUMMARY.md
autonomous: true
requirements: [ADMIN-BILL-01, DOC-05]
user_setup:
  - "No live Stripe keys, Stripe network access, or payout provider credentials are required."
  - "Use TEST_DATABASE_URL with OBLIVIOUS_REQUIRE_TEST_DATABASE=true for DB-backed HTTP tests."
  - "Use go test -p 1 for DB-backed multi-package tests against the shared PostgreSQL test database."
  - "Use restricted-network Go proxy overrides if module downloads fail: GOPROXY=https://mirrors.aliyun.com/goproxy/,direct and GOSUMDB=sum.golang.google.cn."
must_haves:
  truths:
    - "T-01: Admin billing inspection is read-only and does not mutate payment, quota, settlement, or payout state."
    - "T-02: All Phase 20 admin billing routes require an admin session."
    - "T-03: Admin can inspect Relay billing sessions and Stripe payment intents separately."
    - "T-04: Admin can inspect Stripe webhook events including status and errors."
    - "T-05: Admin can inspect subscriptions, top-ups, invoices, and refunds."
    - "T-06: Admin can inspect Marketplace settlements and payout state."
    - "T-07: The Admin Billing UI exposes every required billing surface without placeholder-only content."
    - "T-08: v06 closeout evidence maps PAY-01, PAY-02, PAY-03, MARKET-03, MARKET-04, ADMIN-BILL-01, and DOC-05 to files, tests, and runtime/database proof."
    - "T-09: v07 production operations and v08 product completeness remain open after v06 closeout."
  artifacts:
    - path: "src/server/internal/admin/billing_store.go"
      provides: "Read-only Admin billing inspection SQL queries"
      contains:
        - "type BillingInspectionStore"
        - "ListBillingSessions"
        - "ListWebhookEvents"
        - "ListMarketplaceSettlements"
    - path: "src/server/internal/http/admin_billing_handler_test.go"
      provides: "DB-backed route evidence for ADMIN-BILL-01"
      contains:
        - "TestAdminBillingRoutesRequireAdmin"
        - "TestAdminBillingSummaryIncludesMoneyMovementState"
        - "TestAdminBillingListsExposeAllRequiredSurfaces"
    - path: "src/web/src/routes/admin/AdminBillingPage.tsx"
      provides: "Admin Billing inspection UI"
      contains:
        - "AdminBillingPage"
        - "Billing summary"
        - "surface tabs"
    - path: ".planning/phases/20-billing-admin-evidence-and-v06-closeout/20-VERIFICATION.md"
      provides: "v06 closeout verification evidence"
      contains:
        - "ADMIN-BILL-01"
        - "DOC-05"
        - "Residual v07/v08 work"
  key_links:
    - from: "src/server/internal/http/router.go"
      to: "src/server/internal/admin/billing_service.go"
      via: "Admin-only billing inspection routes"
    - from: "src/web/src/routes/admin/AdminBillingPage.tsx"
      to: "src/web/src/features/admin/api.ts"
      via: "Admin Billing frontend API methods"
    - from: "docs/release/commercial-gates.md"
      to: ".planning/phases/20-billing-admin-evidence-and-v06-closeout/20-VERIFICATION.md"
      via: "v06 billing gate evidence and residual v07/v08 boundaries"
---

<objective>
Implement Phase 20 by adding read-only Admin billing inspection APIs/UI and closing v06 Billing And Marketplace Operations with reproducible evidence.

Purpose: close `ADMIN-BILL-01` and `DOC-05` without claiming v07 production operations, v08 product completeness, or final commercial readiness.
</objective>

<context>
<read_first>
- .planning/phases/20-billing-admin-evidence-and-v06-closeout/20-CONTEXT.md
- .planning/phases/19-marketplace-settlement-and-governance/19-01-SUMMARY.md
- .planning/phases/18-subscription-invoice-topup-refund-state-machine/18-01-SUMMARY.md
- .planning/phases/17-stripe-payment-authority-and-webhook-ledger/17-01-SUMMARY.md
- .planning/PROJECT.md
- .planning/REQUIREMENTS.md
- .planning/ROADMAP.md
- .planning/STATE.md
- docs/superpowers/specs/2026-05-27-commercial-complete-program-design.md
- docs/release/commercial-gates.md
- src/server/internal/admin/service.go
- src/server/internal/admin/store.go
- src/server/internal/admin/types.go
- src/server/internal/http/admin_handler.go
- src/server/internal/http/router.go
- src/server/internal/http/server_test.go
- src/web/src/features/admin/api.ts
- src/web/src/types/admin.ts
- src/web/src/features/layouts/AdminSidebar.tsx
- src/web/src/routes/admin/AdminAuditLogPage.tsx
- src/web/src/routes/admin/AdminPlansPage.tsx
</read_first>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add Admin billing route RED tests</name>
  <files>src/server/internal/http/admin_billing_handler_test.go</files>
  <action>
  Add DB-backed failing tests:
  - `TestAdminBillingRoutesRequireAdmin`
  - `TestAdminBillingSummaryIncludesMoneyMovementState`
  - `TestAdminBillingListsExposeAllRequiredSurfaces`
  </action>
  <test_contract>
  Seed rows in the existing HTTP test database for:
  - `billing_sessions`
  - `payment_intents`
  - `stripe_webhook_events`
  - `subscriptions`
  - `topup_orders`
  - `billing_invoices`
  - `billing_refunds`
  - `marketplace_orders`
  - `marketplace_settlements`
  - `marketplace_payouts`
  The tests must call `/api/v1/admin/billing/summary` and each list route through `NewRouterWithOptions`, prove non-admin users receive `403`, and prove admin responses include the seeded IDs/status/amounts.
  </test_contract>
  <verify_red>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test -p 1 ./internal/http -run 'AdminBilling|BillingAdmin' -count=1</automated>
  <expected>FAIL because `/api/v1/admin/billing/*` routes do not exist.</expected>
  </verify_red>
</task>

<task type="tdd">
  <name>Task 2: Implement read-only Admin billing store and service</name>
  <files>src/server/internal/admin/types.go, src/server/internal/admin/store.go, src/server/internal/admin/service.go, src/server/internal/admin/billing_store.go, src/server/internal/admin/billing_service.go</files>
  <action>
  Add typed billing inspection DTOs, a `BillingInspectionStore` interface, service delegation methods, and SQL list/summary queries over the existing money-movement tables.
  </action>
  <implementation_contract>
  - Define `BillingInspectionFilter` with `OrganizationID`, `UserID`, `Status`, `Kind`, `Provider`, `Limit`, and `Offset`.
  - Define a collection type for each inspected table instead of returning raw `map[string]any`.
  - Clamp default `Limit` to 50 and max `Limit` to 100.
  - Summary totals must use `COUNT(*)` and `COALESCE(SUM(...), 0)` so empty databases return zero values.
  - SQL must be read-only: no `INSERT`, `UPDATE`, or `DELETE` in the new store.
  </implementation_contract>
  <verify_green>
  <automated>cd src/server && go test ./internal/admin -run 'Billing' -count=1</automated>
  <expected>PASS if package-level tests exist; otherwise compile through HTTP tests in Task 3.</expected>
  </verify_green>
</task>

<task type="tdd">
  <name>Task 3: Wire Admin billing HTTP routes</name>
  <files>src/server/internal/http/admin_handler.go, src/server/internal/http/router.go, src/server/internal/http/admin_billing_handler_test.go</files>
  <action>
  Add handler methods for summary and list endpoints, parse query filters with existing helpers, and mount `/api/v1/admin/billing/*` under `requireAdmin`.
  </action>
  <route_contract>
  - `GET /api/v1/admin/billing/summary`
  - `GET /api/v1/admin/billing/sessions`
  - `GET /api/v1/admin/billing/payment-intents`
  - `GET /api/v1/admin/billing/webhook-events`
  - `GET /api/v1/admin/billing/subscriptions`
  - `GET /api/v1/admin/billing/topups`
  - `GET /api/v1/admin/billing/invoices`
  - `GET /api/v1/admin/billing/refunds`
  - `GET /api/v1/admin/billing/settlements`
  - `GET /api/v1/admin/billing/payouts`
  </route_contract>
  <response_contract>
  Use collection keys:
  - `sessions`
  - `paymentIntents`
  - `webhookEvents`
  - `subscriptions`
  - `topups`
  - `invoices`
  - `refunds`
  - `settlements`
  - `payouts`
  Each response also includes `total`.
  </response_contract>
  <verify_green>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test -p 1 ./internal/http -run 'AdminBilling|BillingAdmin' -count=1</automated>
  <expected>PASS.</expected>
  </verify_green>
</task>

<task type="tdd">
  <name>Task 4: Add Admin Billing frontend API and route RED tests</name>
  <files>src/web/src/features/admin/api.test.ts, src/web/src/routes/admin/AdminBillingPage.test.tsx, src/web/src/features/layouts/AdminSidebar.test.tsx, src/web/src/app/router.test.tsx</files>
  <action>
  Add failing Vitest coverage for the frontend contract:
  - `createAdminApi().getBillingSummary()`
  - `createAdminApi().listBillingSurface(surface, filters)`
  - `AdminBillingPage` renders summary metrics and a seeded billing sessions table.
  - Sidebar exposes a `Billing` nav entry.
  - Router renders `/admin/billing`.
  </action>
  <verify_red>
  <automated>cd src/web && npx vitest run src/features/admin/api.test.ts src/routes/admin/AdminBillingPage.test.tsx src/features/layouts/AdminSidebar.test.tsx src/app/router.test.tsx</automated>
  <expected>FAIL because Admin Billing API methods/page/route do not exist.</expected>
  </verify_red>
</task>

<task type="tdd">
  <name>Task 5: Implement Admin Billing frontend API and page</name>
  <files>src/web/src/types/admin.ts, src/web/src/features/admin/api.ts, src/web/src/features/layouts/AdminSidebar.tsx, src/web/src/app/router.tsx, src/web/src/routes/admin/AdminBillingPage.tsx</files>
  <action>
  Add typed API methods, sidebar entry, router route, and a compact Admin Billing page with summary tiles, surface selector buttons, filters, and a `DataTable` for the active surface.
  </action>
  <implementation_contract>
  - Use existing `DataTable`, `StatusBadge`, `SearchBar`, `Button`, and `Input` primitives.
  - Keep route path `/admin/billing`.
  - Surface selector labels: Sessions, Payment Intents, Webhooks, Subscriptions, Top-ups, Invoices, Refunds, Settlements, Payouts.
  - Filters: organization ID, user ID, status, kind, provider.
  - Do not add explanatory marketing copy; the page is an operational inspection surface.
  - Text must fit in compact Admin layout on desktop and mobile; use table cells with short labels and wrapped IDs where needed.
  </implementation_contract>
  <verify_green>
  <automated>cd src/web && npx vitest run src/features/admin/api.test.ts src/routes/admin/AdminBillingPage.test.tsx src/features/layouts/AdminSidebar.test.tsx src/app/router.test.tsx</automated>
  <expected>PASS.</expected>
  </verify_green>
</task>

<task type="execute">
  <name>Task 6: Update API docs and commercial gate evidence</name>
  <files>docs/API.md, docs/release/commercial-gates.md</files>
  <action>
  Document the new Admin billing inspection routes and update commercial gates to say v06 Billing And Marketplace Operations can close only after Phase 20 verification passes. Keep v07 and v08 open as required future gates.
  </action>
  <verify>
  <automated>bash scripts/check.sh docs</automated>
  <expected>PASS.</expected>
  </verify>
</task>

<task type="execute">
  <name>Task 7: Write v06 closeout artifacts and update planning state</name>
  <files>.planning/phases/20-billing-admin-evidence-and-v06-closeout/20-VERIFICATION.md, .planning/phases/20-billing-admin-evidence-and-v06-closeout/20-01-SUMMARY.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md, .planning/milestones/v06-ROADMAP.md, .planning/milestones/v06-REQUIREMENTS.md, .planning/milestones/v06-STATE.md</files>
  <action>
  After backend, frontend, docs, and broad checks pass, mark `ADMIN-BILL-01` and `DOC-05` complete, mark v06 complete, archive v06 milestone snapshots, and route the project to v07 Production Operations planning. Preserve the final objective as open because v07 and v08 remain required.
  </action>
  <verify>
  <automated>rg -n "v07 Production Operations|v08 Product Completeness|final commercial|commercial readiness" .planning docs/release/commercial-gates.md</automated>
  <expected>Output confirms v07/v08 remain required and final commercial readiness is not claimed.</expected>
  </verify>
</task>

</tasks>

<verification>
Run these commands before closing Phase 20:

1. `cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test -p 1 ./internal/http -run 'AdminBilling|BillingAdmin' -count=1`
2. `cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test -p 1 ./internal/admin ./internal/http ./internal/stripe ./internal/marketplace -count=1`
3. `cd src/web && npx vitest run src/features/admin/api.test.ts src/routes/admin/AdminBillingPage.test.tsx src/features/layouts/AdminSidebar.test.tsx src/app/router.test.tsx`
4. `bash scripts/check.sh docs`
5. `GOPROXY=https://mirrors.aliyun.com/goproxy/,direct GOSUMDB=sum.golang.google.cn bash scripts/check.sh all`
6. `git diff --check`
</verification>

<success_criteria>
Phase 20 is complete only when Admin users can inspect billing sessions, payment intents, webhook events, subscriptions, top-ups, invoices, refunds, Marketplace settlements, and payout state from admin-only APIs and the Admin Billing UI; docs and verification artifacts map all v06 money-movement requirements to concrete evidence; v06 is closed; and v07/v08 remain visible as required future work. Do not claim final commercial readiness.
</success_criteria>
