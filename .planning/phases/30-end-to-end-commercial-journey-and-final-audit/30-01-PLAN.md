# Phase 30 Plan - End-to-End Commercial Journey and Final Audit

---
phase: 30-end-to-end-commercial-journey-and-final-audit
plan: 01
type: execute
wave: 1
depends_on:
  - 29-public-docs-onboarding-pricing-and-operator-guides
autonomous: true
requirements: [PROD-06, AUDIT-01]
user_setup:
  - "DB-backed HTTP integration proof requires TEST_DATABASE_URL and OBLIVIOUS_REQUIRE_TEST_DATABASE=true."
  - "Deployment and backup/restore proof require Docker plus a pgvector-capable PostgreSQL image or explicit external disposable databases."
  - "No live provider keys or live Stripe keys are required; provider and payment paths use Relay/fake checkout/signed fixture evidence."
must_haves:
  truths:
    - "T-01: Phase 30 must prove, with fresh commands, signup, organization, provider/channel, subscription/top-up, Chat, Agent, Knowledge, Marketplace, billing, deploy, backup, and restore journeys."
    - "T-02: All AI journey evidence must preserve the Relay-only invariant."
    - "T-03: Final audit must map every commercial readiness gate to files, commands, environment class, pass/fail status, skipped checks, and residual risk."
    - "T-04: Product Completeness Gate and final commercial readiness may close only after Phase 30 verification passes."
    - "T-05: Missing Docker, missing TEST_DATABASE_URL, missing backup/restore smoke, or missing E2E proof is not accepted as final readiness."
  artifacts:
    - path: "src/server/internal/http/commercial_journey_test.go"
      provides: "DB-backed backend commercial journey evidence"
      contains:
        - "TestCommercialHTTPJourney"
        - "registerHTTPUser"
        - "Relay"
        - "Marketplace"
        - "Admin billing"
    - path: "src/web/e2e/commercial-journey.spec.ts"
      provides: "Browser commercial journey evidence"
      contains:
        - "commercial journey"
        - "Chat"
        - "Knowledge"
        - "Marketplace"
        - "Admin"
    - path: "scripts/verify-commercial-completion.sh"
      provides: "Final commercial verification orchestrator"
      contains:
        - "commercial completion"
        - "scripts/check.sh docs"
        - "verify-relay-security"
        - "commercial-journey"
        - "backup-restore-smoke"
    - path: "docs/release/commercial-completion-audit.md"
      provides: "Final audit mapping"
      contains:
        - "AUDIT-01"
        - "Tenant And Identity Gate"
        - "Relay Authority Gate"
        - "Product Completeness Gate"
        - "Verification Gate"
    - path: ".planning/phases/30-end-to-end-commercial-journey-and-final-audit/30-VERIFICATION.md"
      provides: "Fresh Phase 30 verification record"
      contains:
        - "PROD-06"
        - "AUDIT-01"
        - "scripts/verify-commercial-completion.sh"
---

<objective>
Implement Phase 30 by proving the final commercial journey and completion audit for the full multi-tenant AI SaaS objective.

Purpose: close `PROD-06` and `AUDIT-01` only when current evidence proves end-to-end commercial deployability. This is the only phase allowed to close Product Completeness Gate and the thread goal, and only after the full verification record supports that claim.
</objective>

<context>
<read_first>
- .planning/phases/30-end-to-end-commercial-journey-and-final-audit/30-CONTEXT.md
- .planning/PROJECT.md
- .planning/REQUIREMENTS.md
- .planning/ROADMAP.md
- .planning/STATE.md
- docs/superpowers/specs/2026-05-27-commercial-complete-program-design.md
- docs/release/commercial-gates.md
- docs/release/v07-operations-evidence.md
- docs/product/public-overview.md
- docs/product/onboarding.md
- docs/product/pricing.md
- docs/product/operator-guide.md
- scripts/check.sh
- scripts/test.sh
- scripts/deploy-validate.sh
- scripts/backup-restore-smoke.sh
- src/server/internal/http/server_test.go
- src/server/internal/http/stripe_handler_test.go
- src/server/internal/http/admin_billing_handler_test.go
- src/server/internal/http/admin_marketplace_handler_test.go
- src/server/internal/http/knowledge_handler_test.go
- src/server/internal/http/agent_handler_test.go
- src/web/e2e/admin-marketplace.spec.ts
- src/web/e2e/fixtures/adminMarketplace.ts
- src/web/src/routes/workspace/ChatPage.behavior.test.tsx
- src/web/src/routes/workspace/SoloPage.test.tsx
- src/web/src/routes/workspace/KnowledgePage.test.tsx
- src/web/src/routes/marketplace/MarketplacePage.test.tsx
- src/web/src/routes/admin/AdminBillingPage.test.tsx
</read_first>
</context>

<tasks>

<task type="test">
  <name>Task 1: Add RED backend commercial HTTP journey test</name>
  <files>src/server/internal/http/commercial_journey_test.go</files>
  <action>Add a DB-backed HTTP test that fails until the commercial journey evidence is wired through the same router and test database.</action>
  <test_contract>
  - Test name: `TestCommercialHTTPJourney`.
  - Use `testDatabase(t)` and `NewRouterWithOptions` with `fakeCheckoutCreator`.
  - Register a commercial user with `registerHTTPUser`.
  - Query the user organization with `queryHTTPUserScope`.
  - Exercise organization evidence through `createHTTPOrganization` or direct Admin organization route if needed.
  - Insert or call Admin channel/route/plan setup so provider/channel configuration is visible through Admin endpoints.
  - Exercise subscription checkout and top-up checkout through `/api/v1/billing/checkout` and `/api/v1/app/quota/topup`, using CSRF and fake checkout evidence.
  - Exercise Chat conversation create/send/list with Relay-enabled config and assert the response is not a direct-provider bypass.
  - Exercise Knowledge base/document/retrieve and assert `embedding_rag` plus source fields when test embedder data is available; if the router-local Relay embedder cannot run inside the test, seed chunk evidence and use the retrieve endpoint in the same organization.
  - Exercise durable Agent run status and approve/retry endpoints using the Phase 26 helper pattern.
  - Exercise Marketplace publish/review/install/paid settlement/refund evidence using signed webhook helpers from `stripe_handler_test.go`.
  - Exercise Admin billing summary/list endpoints and assert billing sessions, payment intents, webhook events, subscriptions/top-ups, settlements, and payouts are visible.
  - Assert all touched records share the expected organization ID.
  </test_contract>
  <verify_red>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test ./internal/http -run TestCommercialHTTPJourney -count=1</automated>
  <expected>FAIL before implementation because `commercial_journey_test.go` does not exist.</expected>
  </verify_red>
</task>

<task type="code">
  <name>Task 2: Implement backend commercial HTTP journey proof</name>
  <files>src/server/internal/http/commercial_journey_test.go</files>
  <action>Implement the DB-backed commercial journey test with existing test helpers and signed payment fixtures.</action>
  <implementation_contract>
  - Reuse helper functions already present in `server_test.go`, `stripe_handler_test.go`, `agent_handler_test.go`, and Admin/Marketplace tests.
  - Do not introduce live provider or live Stripe dependencies.
  - Keep the test one coherent journey with named substeps using `t.Run` or clear helper functions.
  - Fail on missing tenant organization IDs, missing CSRF on mutating requests, direct provider bypass signals, missing billing evidence, missing Marketplace settlement evidence, missing Admin inspection evidence, or cross-tenant leakage.
  </implementation_contract>
  <verify_green>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test ./internal/http -run TestCommercialHTTPJourney -count=1</automated>
  <expected>PASS after backend journey proof is implemented.</expected>
  </verify_green>
</task>

<task type="test">
  <name>Task 3: Add RED browser commercial journey test</name>
  <files>src/web/e2e/commercial-journey.spec.ts, src/web/e2e/fixtures/commercialJourney.ts</files>
  <action>Add a Playwright journey that fails until route fixtures cover onboarding, Chat, Knowledge, Agent/SOLO, Marketplace, Admin, and billing context.</action>
  <test_contract>
  - Start at `/onboarding` or `/chat` as an authenticated commercial user.
  - Verify onboarding completion state and organization context.
  - Navigate Chat, send a mocked Relay-backed message, bind Knowledge, and verify quota/Relay context.
  - Navigate Knowledge, create/select a knowledge base, retrieve RAG results, and verify source citation text.
  - Navigate SOLO/Agent and verify commercial run readiness, budget, authorization, tool, approval, and retry context.
  - Navigate Marketplace detail/publish/my-agents and verify paid/free review and settlement boundaries.
  - Navigate Admin dashboard/billing/reviews and verify commercial operations and billing inspection context.
  </test_contract>
  <verify_red>
  <automated>COREPACK_HOME=.tmp/corepack pnpm --dir src/web test:e2e --grep "commercial journey"</automated>
  <expected>FAIL before implementation because the spec/fixture do not exist.</expected>
  </verify_red>
</task>

<task type="code">
  <name>Task 4: Implement browser commercial journey fixture and spec</name>
  <files>src/web/e2e/commercial-journey.spec.ts, src/web/e2e/fixtures/commercialJourney.ts</files>
  <action>Implement deterministic browser journey evidence with mocked API routes.</action>
  <implementation_contract>
  - Reuse fixture style from `src/web/e2e/fixtures/adminMarketplace.ts`.
  - Keep API payloads aligned with current frontend types.
  - Include Relay/quota/budget/settlement copy that Phase 28 requires.
  - Do not add visible instructional text to application code; this is test fixture/spec work only.
  </implementation_contract>
  <verify_green>
  <automated>COREPACK_HOME=.tmp/corepack pnpm --dir src/web test:e2e --grep "commercial journey"</automated>
  <expected>PASS after Playwright journey fixture/spec are implemented.</expected>
  </verify_green>
</task>

<task type="code">
  <name>Task 5: Add final commercial completion verifier</name>
  <files>scripts/verify-commercial-completion.sh</files>
  <action>Create an orchestration script for final commercial readiness verification.</action>
  <implementation_contract>
  - Run `bash scripts/check.sh docs`.
  - Run `bash scripts/check.sh relay-security`.
  - Run targeted frontend commercial suites: `COREPACK_HOME=.tmp/corepack pnpm --dir src/web test -- ChatPage SoloPage KnowledgePage MarketplacePage AdminHomePage AdminBillingPage AdminReviewsPage --runInBand`.
  - Run Playwright commercial journey: `COREPACK_HOME=.tmp/corepack pnpm --dir src/web test:e2e --grep "commercial journey"`.
  - Run DB-backed backend commercial journey: `cd src/server && TEST_DATABASE_URL=... OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test ./internal/http -run TestCommercialHTTPJourney -count=1`.
  - Run deployment validation only when `COMMERCIAL_COMPLETION_RUN_DEPLOY=true`; otherwise exit non-zero unless `COMMERCIAL_COMPLETION_ALLOW_ENV_SKIPS=true` is set.
  - Run backup/restore smoke only when `COMMERCIAL_COMPLETION_RUN_BACKUP_RESTORE=true`; otherwise exit non-zero unless `COMMERCIAL_COMPLETION_ALLOW_ENV_SKIPS=true` is set.
  - Print a concise evidence summary with skipped checks and make skipped checks impossible to count as final readiness.
  </implementation_contract>
  <verify>
  <automated>bash scripts/verify-commercial-completion.sh --help</automated>
  <expected>PASS and document required environment variables without running heavyweight checks.</expected>
  </verify>
</task>

<task type="docs">
  <name>Task 6: Add final commercial completion audit</name>
  <files>docs/release/commercial-completion-audit.md</files>
  <action>Create the audit document for `AUDIT-01`.</action>
  <documentation_contract>
  - Include a gate matrix for Tenant And Identity, Relay Authority, Billing And Monetization, Product Completeness, Security, Operations, and Verification.
  - For each gate, list files, commands, environment class, latest result, skipped checks, residual risk, and pass/fail.
  - Include a user-objective matrix for Chat, Agent, Knowledge, Relay, Admin, Marketplace, tenant isolation, billing/rate-limit/monitoring, and direct commercial deployability.
  - State final readiness is blocked until the verifier script runs without environment skips and Phase 30 verification records it.
  </documentation_contract>
</task>

<task type="docs">
  <name>Task 7: Wire Phase 30 into docs, gates, and planning state</name>
  <files>scripts/verify-quality-gates.sh, docs/release/commercial-gates.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Require Phase 30 evidence assets and route final completion only after verification passes.</action>
  <documentation_contract>
  - Quality gates assert Phase 30 context, plan, verifier script, audit doc, backend journey test, browser journey test, verification, and summary files.
  - Commercial gates describe Phase 30 evidence but do not close Product Completeness until `30-VERIFICATION.md` records full pass.
  - Planning files mark Phase 30 as executing before implementation, then complete only after all verification passes.
  </documentation_contract>
  <verify_docs>
  <automated>bash scripts/check.sh docs</automated>
  <expected>PASS after docs and gate updates.</expected>
  </verify_docs>
</task>

<task type="execute">
  <name>Task 8: Run final verification and close Product Completeness only if evidence passes</name>
  <files>.planning/phases/30-end-to-end-commercial-journey-and-final-audit/30-VERIFICATION.md, .planning/phases/30-end-to-end-commercial-journey-and-final-audit/30-01-SUMMARY.md</files>
  <action>Record fresh Phase 30 evidence and close final readiness only if every required check passes without hidden skips.</action>
  <evidence_contract>
  - Run `bash scripts/verify-commercial-completion.sh` in the strict mode required for final readiness.
  - Run `bash scripts/check.sh docs`.
  - Run `git diff --check`.
  - Record DB URL class, Docker/deployment environment class, backup/restore environment class, skipped checks, and residual risk.
  - If any required check is skipped or fails, keep `PROD-06`, `AUDIT-01`, Product Completeness Gate, and thread goal open.
  - Only after strict verification passes, update `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and `docs/release/commercial-gates.md` to close v08 Product Completeness and final commercial readiness.
  </evidence_contract>
</task>

</tasks>

<verification>
Minimum strict Phase 30 verification before completion:
- `bash scripts/verify-commercial-completion.sh`
- `bash scripts/check.sh docs`
- `git diff --check`

Final readiness is not proven if `scripts/verify-commercial-completion.sh` ran with `COMMERCIAL_COMPLETION_ALLOW_ENV_SKIPS=true`.
</verification>
