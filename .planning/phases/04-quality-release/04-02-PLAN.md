---
phase: 04-quality-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/web/package.json
  - src/web/playwright.config.ts
  - src/web/e2e/admin-marketplace.spec.ts
  - src/web/e2e/fixtures/adminMarketplace.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "Release owner can run a headless browser E2E command for Admin and Marketplace workflows"
    - "E2E tests use stable mocked or seeded API responses and do not call live LLM providers, Stripe, or external services"
    - "E2E flows cover Admin navigation and Marketplace browse/detail/install/publish/my-agents routes"
    - "CI either runs the E2E command or documents why it is intentionally separate from default PR checks"
  artifacts:
    - path: "src/web/playwright.config.ts"
      provides: "Playwright browser test configuration"
      exports: []
      contains: "webServer"
    - path: "src/web/e2e/admin-marketplace.spec.ts"
      provides: "Admin and Marketplace browser workflow specs"
      exports: []
      contains: "test("
    - path: "src/web/e2e/fixtures/adminMarketplace.ts"
      provides: "Mock API responses for E2E tests"
      exports: ["registerAdminMarketplaceRoutes"]
  key_links:
    - from: "Playwright tests"
      to: "src/web/src/app/router.tsx"
      via: "browser navigation to /admin and /marketplace routes"
    - from: "E2E fixtures"
      to: "/api/v1/admin/* and /api/v1/marketplace/*"
      via: "page.route mocked responses"
---

<objective>
Add a headless browser E2E gate for the primary Admin and Marketplace workflows that v03.1 shipped. The gate must prove browser route wiring and user-facing flows without requiring live provider keys or external payment/LLM services.

Purpose: Close TEST-02 with a repeatable E2E command suitable for release-candidate validation.
Output: Playwright setup, mocked Admin/Marketplace E2E fixtures, workflow specs, and script/CI wiring.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
- .planning/phases/04-quality-release/04-CONTEXT.md
- src/web/package.json
- package.json
- src/web/src/app/router.tsx
- src/web/src/features/auth/ProtectedRoute.tsx
- src/web/src/features/auth/AdminRoute.tsx
- src/web/src/features/admin/api.ts
- src/web/src/features/marketplace/api.ts
- src/web/src/routes/admin/AdminHomePage.tsx
- src/web/src/routes/marketplace/MarketplaceHomePage.tsx
- src/web/src/routes/marketplace/MarketplaceAgentDetailPage.tsx
- src/web/src/routes/marketplace/MarketplacePublishPage.tsx
- src/web/src/routes/marketplace/MarketplaceMyAgentsPage.tsx
- .github/workflows/ci.yml
</read_first>
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| E2E tests become flaky by depending on live APIs or external services | High | Use `page.route` fixtures for all `/api/v1/*` calls needed by the flows. |
| Auth/admin setup blocks browser tests | Medium | Mock `/api/v1/auth/me` with an admin session fixture and keep route protection exercised. |
| CI downloads browsers unpredictably or slows every PR beyond acceptable cost | Medium | Add a named E2E command and document whether CI runs it by default or as a release gate. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Playwright as the browser E2E runner</name>
  <files>src/web/package.json, package.json, src/web/playwright.config.ts</files>
  <read_first>
  - src/web/package.json
  - package.json
  - src/web/vite.config.ts
  - src/web/tsconfig.json
  </read_first>
  <action>
  Add Playwright in the existing Vite/React toolchain:
  - Add `@playwright/test` to `src/web/package.json` devDependencies.
  - Add script `"test:e2e": "playwright test"` to `src/web/package.json`.
  - Add root script `"test:e2e": "COREPACK_HOME=.tmp/corepack pnpm --dir src/web test:e2e"` to `package.json`.
  - Create `src/web/playwright.config.ts` with:
    - `testDir: './e2e'`
    - `use.baseURL: 'http://127.0.0.1:4173'`
    - `webServer.command: 'pnpm preview --host 127.0.0.1 --port 4173'`
    - `webServer.reuseExistingServer: !process.env.CI`
    - chromium project only for initial Phase 4 gate
  - If `pnpm preview` needs a build first, set `webServer.command` to `pnpm build && pnpm preview --host 127.0.0.1 --port 4173`.
  </action>
  <acceptance_criteria>
  - `src/web/package.json` contains `"test:e2e": "playwright test"`.
  - `src/web/package.json` contains `"@playwright/test"`.
  - `package.json` contains `"test:e2e": "COREPACK_HOME=.tmp/corepack pnpm --dir src/web test:e2e"`.
  - `src/web/playwright.config.ts` contains `testDir: './e2e'`.
  - `src/web/playwright.config.ts` contains `webServer`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build deterministic Admin/Marketplace E2E fixtures</name>
  <files>src/web/e2e/fixtures/adminMarketplace.ts</files>
  <read_first>
  - src/web/src/features/admin/api.ts
  - src/web/src/features/marketplace/api.ts
  - src/web/src/types/admin.ts
  - src/web/src/services/http/envelope.ts
  </read_first>
  <action>
  Create `src/web/e2e/fixtures/adminMarketplace.ts` exporting `registerAdminMarketplaceRoutes(page: Page): Promise<void>`.

  The fixture must intercept and fulfill these paths with JSON envelopes matching the frontend client expectations:
  - `GET **/api/v1/auth/me` with an admin user/session/workspace response.
  - `GET **/api/v1/admin/stats`.
  - `GET **/api/v1/admin/channels`.
  - `GET **/api/v1/admin/routes`.
  - `GET **/api/v1/admin/plans`.
  - `GET **/api/v1/admin/users`.
  - `GET **/api/v1/admin/audit-logs`.
  - `GET **/api/v1/admin/reviews`.
  - `GET **/api/v1/marketplace/featured`.
  - `GET **/api/v1/marketplace/categories`.
  - `GET **/api/v1/marketplace/search`.
  - `GET **/api/v1/marketplace/agents/*`.
  - `POST **/api/v1/marketplace/installs/*`.
  - `POST **/api/v1/marketplace/agents`.
  - `GET **/api/v1/marketplace/my-agents`.
  - `GET **/api/v1/marketplace/installs`.

  Use deterministic IDs such as `agent_release_helper`, `channel_openai_primary`, and `user_admin`.
  </action>
  <acceptance_criteria>
  - `src/web/e2e/fixtures/adminMarketplace.ts` contains `registerAdminMarketplaceRoutes`.
  - `src/web/e2e/fixtures/adminMarketplace.ts` contains `/api/v1/auth/me`.
  - `src/web/e2e/fixtures/adminMarketplace.ts` contains `/api/v1/admin/channels`.
  - `src/web/e2e/fixtures/adminMarketplace.ts` contains `/api/v1/marketplace/search`.
  - `src/web/e2e/fixtures/adminMarketplace.ts` does not contain `OPENAI_API_KEY`.
  - `src/web/e2e/fixtures/adminMarketplace.ts` does not contain `STRIPE_SECRET`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add Admin and Marketplace browser workflow specs</name>
  <files>src/web/e2e/admin-marketplace.spec.ts</files>
  <read_first>
  - src/web/e2e/fixtures/adminMarketplace.ts
  - src/web/src/app/router.tsx
  - src/web/src/routes/admin/AdminHomePage.tsx
  - src/web/src/routes/admin/AdminChannelsPage.tsx
  - src/web/src/routes/admin/AdminRoutesPage.tsx
  - src/web/src/routes/admin/AdminPlansPage.tsx
  - src/web/src/routes/admin/AdminUsersPage.tsx
  - src/web/src/routes/admin/AdminReviewsPage.tsx
  - src/web/src/routes/marketplace/MarketplaceHomePage.tsx
  - src/web/src/routes/marketplace/MarketplaceAgentDetailPage.tsx
  - src/web/src/routes/marketplace/MarketplacePublishPage.tsx
  - src/web/src/routes/marketplace/MarketplaceMyAgentsPage.tsx
  </read_first>
  <action>
  Create `src/web/e2e/admin-marketplace.spec.ts` with these tests:
  - `admin navigation exposes release management pages`: register fixtures, visit `/admin`, verify visible dashboard content, navigate or directly visit `/admin/channels`, `/admin/routes`, `/admin/plans`, `/admin/users`, `/admin/audit-log`, `/admin/reviews`, and assert each page shows fixture data.
  - `marketplace browse detail and install workflow works`: visit `/marketplace`, search or filter for the fixture agent, open `/marketplace/agents/agent_release_helper`, click install, and assert success feedback or installed state.
  - `marketplace publish and my agents workflow works`: visit `/marketplace/publish`, submit required fields for a fixture agent, assert publish request is sent, then visit `/marketplace/my-agents` and assert published/installed fixture content.
  </action>
  <acceptance_criteria>
  - `src/web/e2e/admin-marketplace.spec.ts` contains `test(`.
  - `src/web/e2e/admin-marketplace.spec.ts` contains `/admin/channels`.
  - `src/web/e2e/admin-marketplace.spec.ts` contains `/marketplace/agents/agent_release_helper`.
  - `src/web/e2e/admin-marketplace.spec.ts` contains `/marketplace/publish`.
  - `cd src/web && npx playwright test --list` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Wire E2E into release verification without hiding cost</name>
  <files>.github/workflows/ci.yml, docs/release/rc-checklist.md</files>
  <read_first>
  - .github/workflows/ci.yml
  - docs/release/rc-checklist.md
  - src/web/package.json
  </read_first>
  <action>
  Add E2E to release verification:
  - In `.github/workflows/ci.yml`, either add a separate `e2e` job that runs `pnpm --dir src/web exec playwright install --with-deps chromium` and `pnpm --dir src/web test:e2e`, or document in `docs/release/rc-checklist.md` that E2E is a manual RC command if CI browser install is too expensive.
  - Prefer a separate `e2e` job if dependency installation is reliable in GitHub Actions.
  - Update `docs/release/rc-checklist.md` with exact command `COREPACK_HOME=.tmp/corepack pnpm --dir src/web test:e2e`.
  </action>
  <acceptance_criteria>
  - `docs/release/rc-checklist.md` contains `pnpm --dir src/web test:e2e`.
  - `.github/workflows/ci.yml` contains either `test:e2e` or the plan summary records why CI did not run E2E.
  - `.github/workflows/ci.yml` does not contain real provider secrets.
  </acceptance_criteria>
</task>

</tasks>

<verification_commands>
```bash
COREPACK_HOME=.tmp/corepack pnpm --dir src/web install --frozen-lockfile
COREPACK_HOME=.tmp/corepack pnpm --dir src/web exec playwright install chromium
COREPACK_HOME=.tmp/corepack pnpm --dir src/web test:e2e
COREPACK_HOME=.tmp/corepack pnpm --dir src/web test
COREPACK_HOME=.tmp/corepack pnpm --dir src/web build
```
</verification_commands>

<summary_requirements>
Create `.planning/phases/04-quality-release/04-02-SUMMARY.md` with E2E command output, any CI decision, and remaining TEST-02 gaps.
</summary_requirements>
