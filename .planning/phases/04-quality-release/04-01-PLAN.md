---
phase: 04-quality-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check.sh
  - scripts/test.sh
  - .github/workflows/ci.yml
  - src/server/internal/http/admin_marketplace_handler_test.go
  - src/server/internal/admin/service_test.go
  - src/server/internal/marketplace/service_test.go
  - src/server/internal/relay/billing_test.go
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Maintainers can run a broad backend release test gate with repo-local Go caches"
    - "scripts/check.sh server and scripts/test.sh server no longer hide release-risk packages behind a narrow package subset"
    - "HTTP integration tests document TEST_DATABASE_URL behavior and explicit skip conditions"
    - "Admin, Marketplace, Relay billing, Agent/Memory/Quota boundary coverage is represented by focused tests or existing test commands"
    - "CI server job uses the same script path documented for local release checks"
  artifacts:
    - path: "scripts/check.sh"
      provides: "server release check entry point"
      contains: "go test ./..."
    - path: "scripts/test.sh"
      provides: "server release test entry point and TEST_DATABASE_URL integration gate"
      contains: "TEST_DATABASE_URL"
    - path: ".github/workflows/ci.yml"
      provides: "CI server gate aligned to scripts/check.sh and scripts/test.sh"
      contains: "bash scripts/test.sh server"
  key_links:
    - from: "scripts/test.sh server"
      to: "src/server/internal/http"
      via: "TEST_DATABASE_URL-gated integration package"
    - from: "CI server job"
      to: "local release gate"
      via: "same scripts/check.sh server and scripts/test.sh server commands"
---

<objective>
Make the backend release test gate honest and broad enough for v03.2. Phase 4 should prove the already shipped Admin, Marketplace, Relay, Agent, Memory, and Quota boundaries instead of relying on a narrow fast-test slice.

Purpose: Close TEST-01 by turning current backend test scripts and CI into a release-candidate gate with explicit integration-test skip semantics.
Output: Updated backend scripts/CI plus focused integration/regression tests or documented coverage hooks for the critical service boundaries.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
- .planning/phases/04-quality-release/04-CONTEXT.md
- .planning/codebase/TESTING.md
- scripts/check.sh
- scripts/test.sh
- .github/workflows/ci.yml
- src/server/internal/http/admin_marketplace_handler_test.go
- src/server/internal/admin/service_test.go
- src/server/internal/marketplace/service.go
- src/server/internal/marketplace/store.go
- src/server/internal/relay/billing_test.go
- src/server/internal/agent/service_test.go
- src/server/internal/memory/embedder_test.go
- src/server/internal/quota/service_test.go
</read_first>
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| Release gate false positive because narrow scripts skip changed packages | High | Update server scripts to run broad `go test ./... -count=1` or a named release gate that includes all active backend packages. |
| Integration tests silently skipped because `TEST_DATABASE_URL` is absent | Medium | Keep the skip explicit in script output and make the release checklist require recording the skip reason. |
| Tests call live LLM/Stripe/provider services | High | Use fake stores/gateways and avoid real provider keys in tests. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Broaden backend release scripts without losing explicit integration gating</name>
  <files>scripts/check.sh, scripts/test.sh</files>
  <read_first>
  - scripts/check.sh
  - scripts/test.sh
  - .planning/phases/04-quality-release/04-CONTEXT.md
  </read_first>
  <action>
  Update the server script behavior to make release-risk package coverage explicit:
  - In `scripts/check.sh`, change `run_server_checks` so the command executed from `src/server` includes `go test ./... -count=1`.
  - In `scripts/test.sh`, change the "server unit tests" command so it includes `go test ./... -count=1`.
  - Preserve repo-local cache exports: `GOCACHE="$repo_root/.tmp/go-build"` and `GOMODCACHE="$repo_root/.tmp/go-mod"`.
  - Preserve the `TEST_DATABASE_URL` branch in `scripts/test.sh`.
  - Make the skip message exactly include `Skipping server integration tests: TEST_DATABASE_URL not set.`
  - Do not add real secrets or provider credentials to scripts.
  </action>
  <acceptance_criteria>
  - `scripts/check.sh` contains `go test ./... -count=1`.
  - `scripts/test.sh` contains `go test ./... -count=1`.
  - `scripts/test.sh` contains `TEST_DATABASE_URL`.
  - `scripts/test.sh` contains `Skipping server integration tests: TEST_DATABASE_URL not set.`
  - `bash scripts/check.sh server` exits 0 or the failure is recorded in the plan summary with the exact failing package/test.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add backend boundary tests for Admin and Marketplace release surfaces</name>
  <files>src/server/internal/http/admin_marketplace_handler_test.go, src/server/internal/admin/service_test.go, src/server/internal/marketplace/service_test.go</files>
  <read_first>
  - src/server/internal/http/admin_marketplace_handler_test.go
  - src/server/internal/http/router.go
  - src/server/internal/http/auth_middleware.go
  - src/server/internal/http/response.go
  - src/server/internal/admin/service_test.go
  - src/server/internal/marketplace/service.go
  - src/server/internal/marketplace/types.go
  </read_first>
  <action>
  Add or extend tests that prove:
  - Admin routes reject non-admin sessions and accept `auth.Session{User: auth.User{Role: "admin"}}`.
  - `/api/v1/admin/channels`, `/api/v1/admin/routes`, `/api/v1/admin/plans`, `/api/v1/admin/users`, `/api/v1/admin/audit-logs`, and `/api/v1/admin/reviews` have handler-level coverage through `httptest`.
  - Public marketplace browse endpoints (`/api/v1/marketplace/featured`, `/api/v1/marketplace/categories`, `/api/v1/marketplace/search`, `/api/v1/marketplace/agents`) do not require a session.
  - Session-required marketplace endpoints (`/api/v1/marketplace/agents` POST, `/api/v1/marketplace/my-agents`, `/api/v1/marketplace/installs`, `/api/v1/marketplace/publisher/stats`) reject unauthenticated requests.
  - Marketplace service tests cover publish, install, review, and my-agents behavior using fake stores or in-memory fixtures.
  </action>
  <acceptance_criteria>
  - `src/server/internal/http/admin_marketplace_handler_test.go` contains `requireAdmin` or an explicit admin-role authorization assertion.
  - `src/server/internal/http/admin_marketplace_handler_test.go` contains `/api/v1/admin/channels`.
  - `src/server/internal/http/admin_marketplace_handler_test.go` contains `/api/v1/marketplace/search`.
  - `src/server/internal/marketplace/service_test.go` exists and contains `Test`.
  - `cd src/server && go test ./internal/http ./internal/admin ./internal/marketplace -count=1` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Protect Relay billing, Agent, Memory, and Quota collaboration paths</name>
  <files>src/server/internal/relay/billing_test.go, src/server/internal/agent/service_test.go, src/server/internal/memory/embedder_test.go, src/server/internal/quota/service_test.go</files>
  <read_first>
  - .planning/phases/02-agent-memory-enhancement/02-CONTEXT.md
  - src/server/internal/relay/billing_test.go
  - src/server/internal/relay/billing.go
  - src/server/internal/agent/service_test.go
  - src/server/internal/agent/service.go
  - src/server/internal/memory/embedder_test.go
  - src/server/internal/memory/embedder.go
  - src/server/internal/quota/service_test.go
  - src/server/internal/quota/service.go
  </read_first>
  <action>
  Extend existing tests to assert the release-critical collaboration contracts:
  - Relay billing preconsume/settle/refund calls quota service paths and remains idempotent for duplicate sessions.
  - Agent service continues to route generated replies through the configured gateway rather than bypassing Relay.
  - Memory embedder uses the Relay embedding path and does not require live provider credentials in tests.
  - Quota service tests cover insufficient balance, successful preconsume, settlement, and refund.
  </action>
  <acceptance_criteria>
  - `src/server/internal/relay/billing_test.go` contains `PreConsume`.
  - `src/server/internal/relay/billing_test.go` contains `Refund`.
  - `src/server/internal/memory/embedder_test.go` contains `Relay`.
  - `src/server/internal/quota/service_test.go` contains `insufficient` or `Insufficient`.
  - `cd src/server && go test ./internal/relay ./internal/agent ./internal/memory ./internal/quota -count=1` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Align CI server job with local release gate</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
  - .github/workflows/ci.yml
  - scripts/check.sh
  - scripts/test.sh
  </read_first>
  <action>
  Confirm `.github/workflows/ci.yml` server job uses the same script entry points as local release verification:
  - Keep `bash scripts/check.sh server`.
  - Keep `bash scripts/test.sh server`.
  - Do not add direct `go test` commands in CI that diverge from scripts.
  - If script changes require env documentation, add comments or step names only; do not add secrets.
  </action>
  <acceptance_criteria>
  - `.github/workflows/ci.yml` contains `bash scripts/check.sh server`.
  - `.github/workflows/ci.yml` contains `bash scripts/test.sh server`.
  - `.github/workflows/ci.yml` does not contain `OPENAI_API_KEY`.
  - `.github/workflows/ci.yml` does not contain `STRIPE_SECRET`.
  </acceptance_criteria>
</task>

</tasks>

<verification_commands>
```bash
bash scripts/check.sh server
bash scripts/test.sh server
cd src/server && go test ./internal/http ./internal/admin ./internal/marketplace ./internal/relay ./internal/agent ./internal/memory ./internal/quota -count=1
```
</verification_commands>

<summary_requirements>
Create `.planning/phases/04-quality-release/04-01-SUMMARY.md` with commands run, pass/fail status, any integration skip reason, and remaining TEST-01 gaps.
</summary_requirements>
