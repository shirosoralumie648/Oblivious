---
phase: 04-quality-release
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - docs/API.md
  - docs/architecture/current-system-contracts.md
  - docs/release/rc-checklist.md
  - scripts/verify-quality-gates.sh
  - README.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "docs/API.md lists the current routed HTTP surface, including Admin, Marketplace, Memory, MCP, Quota, Notifications, Agent, Chat, Console, Auth, and Relay /v1 routes"
    - "current-system-contracts.md reflects verified current behavior, not future intended behavior"
    - "rc-checklist.md names exact release commands, env prerequisites, E2E gate, integration-test skip handling, and known accepted debt"
    - "scripts/verify-quality-gates.sh asserts the new release documentation anchors so docs drift is caught"
  artifacts:
    - path: "docs/API.md"
      provides: "canonical current API index"
      contains: "Admin Endpoints"
      contains: "Marketplace Endpoints"
    - path: "docs/architecture/current-system-contracts.md"
      provides: "mainline behavior contract"
      contains: "/api/v1/admin"
      contains: "/api/v1/marketplace"
    - path: "docs/release/rc-checklist.md"
      provides: "release-candidate checklist"
      contains: "TEST_DATABASE_URL"
  key_links:
    - from: "docs/API.md"
      to: "src/server/internal/http/router.go"
      via: "route matrix reconciliation"
    - from: "rc-checklist.md"
      to: "scripts/check.sh and scripts/test.sh"
      via: "exact command references"
---

<objective>
Reconcile and harden the release documentation for the current routed API and v03.2 release-candidate gates.

Purpose: Close DOC-01 by making docs/API.md, current-system-contracts.md, and rc-checklist.md usable as release evidence rather than stale intent.
Output: Updated API docs, system contract docs, RC checklist, and docs quality-gate assertions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
- .planning/phases/04-quality-release/04-CONTEXT.md
- docs/API.md
- docs/architecture/current-system-contracts.md
- docs/release/rc-checklist.md
- scripts/verify-quality-gates.sh
- scripts/check.sh
- scripts/test.sh
- .github/workflows/ci.yml
- src/server/internal/http/router.go
- src/server/internal/relay/handler/router.go
- src/web/src/app/router.tsx
- config/.env.example
- README.md
</read_first>
</context>

<threat_model>
## Threat Model

| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| Docs describe future endpoints not present in router | High | Reconcile endpoint tables against `src/server/internal/http/router.go` and relay handler routes. |
| Release checklist accepts skipped integration/E2E gates without evidence | Medium | Require explicit skip reason and evidence artifact for `TEST_DATABASE_URL` and E2E. |
| Secrets leak into docs | High | Use env var names and placeholders only. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild docs/API.md around the live route surface</name>
  <files>docs/API.md</files>
  <read_first>
  - docs/API.md
  - src/server/internal/http/router.go
  - src/server/internal/relay/handler/router.go
  - src/server/internal/http/response.go
  </read_first>
  <action>
  Update `docs/API.md` so it includes these sections and route groups:
  - `## Base URLs` with app API `http://localhost:8080/api/v1` and Relay API `http://localhost:8080/v1`.
  - `## Response Envelope` with `ok`, `data`, and `error`.
  - `## Auth Endpoints`.
  - `## Chat Endpoints`.
  - `## Agent Endpoints`.
  - `## Memory Endpoints`.
  - `## MCP Server Endpoints`.
  - `## Notification Endpoints`.
  - `## Quota Endpoints`.
  - `## Console Endpoints`.
  - `## Admin Endpoints` covering `/api/v1/admin/stats`, `/channels`, `/channels/batch`, `/channels/:id/test`, `/channels/:id/health`, `/routes`, `/plans`, `/users`, `/audit-logs`, `/reviews`, approve/reject.
  - `## Marketplace Endpoints` covering featured, curated, categories, search, agents, my-agents, installs, publisher stats, reviews/versions if routed.
  - `## Relay /v1 Endpoints` covering chat completions, responses, embeddings, images, audio, moderations, completions, files, fine tuning, assistants/threads/runs families that are registered.
  Do not document endpoints that cannot be found in `router.go` or `relay/handler/router.go` unless they are explicitly marked as not routed.
  </action>
  <acceptance_criteria>
  - `docs/API.md` contains `## Admin Endpoints`.
  - `docs/API.md` contains `/api/v1/admin/channels`.
  - `docs/API.md` contains `## Marketplace Endpoints`.
  - `docs/API.md` contains `/api/v1/marketplace/search`.
  - `docs/API.md` contains `## Relay /v1 Endpoints`.
  - `docs/API.md` contains `/v1/chat/completions`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update current system contracts with verified current behavior</name>
  <files>docs/architecture/current-system-contracts.md</files>
  <read_first>
  - docs/architecture/current-system-contracts.md
  - src/server/internal/http/router.go
  - src/web/src/app/router.tsx
  - config/.env.example
  </read_first>
  <action>
  Update `docs/architecture/current-system-contracts.md` to reflect current v03.2 baseline:
  - Keep the HTTP envelope and session cookie sections.
  - Add route matrix rows for Admin and Marketplace routes that are registered in `router.go`.
  - Add frontend route matrix rows for `/admin`, `/admin/channels`, `/admin/routes`, `/admin/plans`, `/admin/users`, `/admin/audit-log`, `/admin/reviews`, `/marketplace`, `/marketplace/agents/:agentId`, `/marketplace/publish`, and `/marketplace/my-agents`.
  - Add a section `## Release Gate Commands` listing:
    - `bash scripts/check.sh docs`
    - `bash scripts/check.sh web`
    - `bash scripts/check.sh server`
    - `bash scripts/test.sh web`
    - `bash scripts/test.sh server`
    - `COREPACK_HOME=.tmp/corepack pnpm --dir src/web test:e2e`
  - State that `lobehub/` and `new-api/` remain reference trees, not root workspace members.
  </action>
  <acceptance_criteria>
  - `docs/architecture/current-system-contracts.md` contains `/api/v1/admin/stats`.
  - `docs/architecture/current-system-contracts.md` contains `/api/v1/marketplace/featured`.
  - `docs/architecture/current-system-contracts.md` contains `/marketplace/my-agents`.
  - `docs/architecture/current-system-contracts.md` contains `COREPACK_HOME=.tmp/corepack pnpm --dir src/web test:e2e`.
  - `docs/architecture/current-system-contracts.md` contains `lobehub/`.
  - `docs/architecture/current-system-contracts.md` contains `new-api/`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Strengthen RC checklist with evidence and skip semantics</name>
  <files>docs/release/rc-checklist.md, README.md</files>
  <read_first>
  - docs/release/rc-checklist.md
  - README.md
  - scripts/check.sh
  - scripts/test.sh
  - .planning/STATE.md
  </read_first>
  <action>
  Update `docs/release/rc-checklist.md`:
  - Add exact command checklist for docs, web build, server check, web tests, server tests, E2E tests, and deployment smoke.
  - Add `TEST_DATABASE_URL` requirement and explicit skip recording rule.
  - Add "Evidence" column or bullet for each gate, requiring command output, CI URL, or summary file path.
  - Add known accepted debt from `.planning/STATE.md`: Phase 01 missing summary artifact and legacy workspace MarketplacePage cleanup.
  - Add "No live provider keys required" note for tests/E2E.
  Update `README.md` Quick Start or release section to point to `docs/release/rc-checklist.md` and `docs/API.md`.
  </action>
  <acceptance_criteria>
  - `docs/release/rc-checklist.md` contains `TEST_DATABASE_URL`.
  - `docs/release/rc-checklist.md` contains `pnpm --dir src/web test:e2e`.
  - `docs/release/rc-checklist.md` contains `Known accepted debt`.
  - `docs/release/rc-checklist.md` contains `No live provider keys required`.
  - `README.md` contains `docs/release/rc-checklist.md`.
  - `README.md` contains `docs/API.md`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Extend docs quality gate assertions</name>
  <files>scripts/verify-quality-gates.sh</files>
  <read_first>
  - scripts/verify-quality-gates.sh
  - docs/API.md
  - docs/architecture/current-system-contracts.md
  - docs/release/rc-checklist.md
  </read_first>
  <action>
  Add assertions to `scripts/verify-quality-gates.sh`:
  - `docs/API.md` contains `## Admin Endpoints`.
  - `docs/API.md` contains `## Marketplace Endpoints`.
  - `docs/API.md` contains `## Relay /v1 Endpoints`.
  - `docs/architecture/current-system-contracts.md` contains `/api/v1/admin/stats`.
  - `docs/architecture/current-system-contracts.md` contains `/api/v1/marketplace/featured`.
  - `docs/release/rc-checklist.md` contains `TEST_DATABASE_URL`.
  - `docs/release/rc-checklist.md` contains `pnpm --dir src/web test:e2e`.
  </action>
  <acceptance_criteria>
  - `scripts/verify-quality-gates.sh` contains `## Admin Endpoints`.
  - `scripts/verify-quality-gates.sh` contains `## Marketplace Endpoints`.
  - `scripts/verify-quality-gates.sh` contains `## Relay /v1 Endpoints`.
  - `scripts/verify-quality-gates.sh` contains `TEST_DATABASE_URL`.
  - `bash scripts/check.sh docs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification_commands>
```bash
bash scripts/check.sh docs
rg -n "## Admin Endpoints|## Marketplace Endpoints|## Relay /v1 Endpoints" docs/API.md
rg -n "/api/v1/admin/stats|/api/v1/marketplace/featured|/marketplace/my-agents" docs/architecture/current-system-contracts.md
```
</verification_commands>

<summary_requirements>
Create `.planning/phases/04-quality-release/04-03-SUMMARY.md` with docs changed, commands run, any docs drift discovered, and remaining DOC-01 gaps.
</summary_requirements>
