# Phase 28 Plan — Commercial UX and Journey Hardening

---
phase: 28-commercial-ux-and-journey-hardening
plan: 01
type: execute
wave: 1
depends_on:
  - 27-knowledge-product-promise-alignment
autonomous: true
requirements: [PROD-04]
user_setup:
  - "No live provider keys are required. Frontend tests use mocked APIs."
  - "Do not change Phase 29 public docs/pricing/onboarding scope or Phase 30 final E2E audit scope."
must_haves:
  truths:
    - "T-01: Active Chat, SOLO/Agent, Knowledge, Admin, and Marketplace routes show explicit loading/error/empty/action states instead of silently swallowing customer-impacting failures."
    - "T-02: Customer-facing copy must not claim fake provider, fake billing, fake settlement, fake tool, or fake RAG behavior."
    - "T-03: Chat and Agent/SOLO journeys expose quota/budget/authorization/tool-boundary context where the user can make a commercial decision."
    - "T-04: Marketplace install, publish, review, and uninstall actions expose paid/free/review/settlement boundaries and recoverable errors."
    - "T-05: Admin commercial operation pages remain routed and prove channels, routes, plans, billing, users, audit, and review queue coverage."
    - "T-06: Phase 28 closes only PROD-04. Public docs/pricing/onboarding, final end-to-end journeys, and AUDIT-01 remain open."
  artifacts:
    - path: "src/web/src/routes/workspace/ChatPage.behavior.test.tsx"
      provides: "Chat commercial journey regression tests"
      contains:
        - "quota"
        - "Relay"
        - "Unable to send"
        - "Start in SOLO"
    - path: "src/web/src/routes/workspace/SoloPage.test.tsx"
      provides: "Agent/SOLO commercial run-state regression tests"
      contains:
        - "Commercial run readiness"
        - "Budget consumed"
        - "Authorization scope"
        - "Unable to"
    - path: "src/web/src/routes/marketplace/MarketplacePage.test.tsx"
      provides: "Marketplace commercial action-state regression tests"
      contains:
        - "settlement"
        - "Unable to install"
        - "submitted for review"
        - "Unable to uninstall"
    - path: "src/web/src/routes/admin/AdminHomePage.test.tsx"
      provides: "Admin commercial operations dashboard evidence"
      contains:
        - "Commercial operations"
        - "Channels"
        - "Billing"
        - "Review Queue"
    - path: "scripts/verify-quality-gates.sh"
      provides: "Active-route anti-placeholder gate"
      contains:
        - "PROD-04"
        - "Commercial UX"
        - "placeholder"
        - "fake"
---

<objective>
Implement Phase 28 by hardening the active commercial customer journeys for Chat, Agent/SOLO, Knowledge, Admin, and Marketplace.

Purpose: close `PROD-04` only. The product must stop presenting enabled customer-facing surfaces as commercially ready when they lack action feedback, quota/budget context, or clear settlement/review/error boundaries.
</objective>

<context>
<read_first>
- .planning/phases/28-commercial-ux-and-journey-hardening/28-CONTEXT.md
- .planning/REQUIREMENTS.md
- .planning/ROADMAP.md
- .planning/STATE.md
- docs/superpowers/specs/2026-05-27-commercial-complete-program-design.md
- src/web/src/routes/workspace/ChatPage.tsx
- src/web/src/routes/workspace/ChatPage.behavior.test.tsx
- src/web/src/routes/workspace/SoloPage.tsx
- src/web/src/routes/workspace/SoloPage.test.tsx
- src/web/src/routes/workspace/KnowledgePage.tsx
- src/web/src/routes/workspace/KnowledgePage.test.tsx
- src/web/src/routes/marketplace/MarketplaceHomePage.tsx
- src/web/src/routes/marketplace/MarketplaceAgentDetailPage.tsx
- src/web/src/routes/marketplace/MarketplacePublishPage.tsx
- src/web/src/routes/marketplace/MarketplaceMyAgentsPage.tsx
- src/web/src/routes/marketplace/MarketplacePage.test.tsx
- src/web/src/routes/admin/AdminHomePage.tsx
- src/web/src/routes/admin/AdminBillingPage.tsx
- src/web/src/routes/admin/AdminReviewsPage.tsx
- src/web/src/routes/admin/*.test.tsx
- src/web/src/components/shared/DataTable.tsx
- src/web/src/components/shared/EmptyState.tsx
- docs/API.md
- docs/release/commercial-gates.md
- scripts/verify-quality-gates.sh
</read_first>
</context>

<tasks>

<task type="test">
  <name>Task 1: Add RED Chat commercial journey tests</name>
  <files>src/web/src/routes/workspace/ChatPage.behavior.test.tsx</files>
  <action>Add failing tests for the active Chat customer journey.</action>
  <test_contract>
  - `it('shows a retryable chat workspace error when initial loading fails')` rejects `listConversations` and expects visible text `Unable to load chat workspace.` plus a `Retry chat workspace` button.
  - `it('shows quota and Relay errors when message send fails')` rejects `sendMessage` with `new Error('quota preauthorization failed through Relay')` and expects that exact commercial failure text to be visible without clearing the draft.
  - `it('shows SOLO handoff errors without opening a fake handoff')` rejects `convertConversationToTask` and expects `Unable to prepare SOLO handoff.` while `SOLO task goal` remains absent.
  - `it('renders one Convert to SOLO task heading')` opens a successful handoff and asserts `screen.getAllByRole('heading', { name: 'Convert to SOLO task' })` has length `1`.
  </test_contract>
  <verify_red>
  <automated>COREPACK_HOME=.tmp/corepack pnpm --dir src/web test -- ChatPage.behavior --runInBand</automated>
  <expected>FAIL before implementation because ChatPage currently swallows load/send/handoff failures and has duplicate handoff heading text.</expected>
  </verify_red>
</task>

<task type="code">
  <name>Task 2: Harden Chat journey loading, send, and SOLO handoff states</name>
  <files>src/web/src/routes/workspace/ChatPage.tsx</files>
  <action>Implement explicit customer-facing Chat action states.</action>
  <implementation_contract>
  - Add `error`, `isLoading`, `isSending`, `isCreatingConversation`, `isUpdatingKnowledgeBinding`, `isPreparingHandoff`, and `isStartingSolo` state.
  - When initial loading fails, show `Unable to load chat workspace.` and the thrown error message if present; render a `Retry chat workspace` button that reruns the same load.
  - Disable `Send message` while sending and preserve `messageDraft` if `sendMessage` fails.
  - Show thrown send errors, including quota and Relay wording, in a visible `role="alert"` region.
  - Wrap create conversation, knowledge binding, SOLO handoff, and start-in-SOLO actions in explicit try/catch blocks with visible action errors.
  - Remove the duplicate `Convert to SOLO task` heading.
  - Keep existing successful create, bind, send, and handoff behavior unchanged.
  </implementation_contract>
  <verify_green>
  <automated>COREPACK_HOME=.tmp/corepack pnpm --dir src/web test -- ChatPage.behavior --runInBand</automated>
  <expected>PASS after Chat journey hardening.</expected>
  </verify_green>
</task>

<task type="test">
  <name>Task 3: Add RED SOLO/Agent commercial run-state tests</name>
  <files>src/web/src/routes/workspace/SoloPage.test.tsx</files>
  <action>Add tests that lock commercial Agent/SOLO state and recovery behavior.</action>
  <test_contract>
  - `it('renders commercial run readiness with budget, authorization, tools, and knowledge scope')` loads a running task and expects `Commercial run readiness`, `Budget consumed: 6 / 20`, `Authorization scope: workspace_tools`, `Current enabled tools`, and the selected knowledge base.
  - `it('surfaces start failures without clearing the task form')` rejects `startTask` with `new Error('quota budget exceeded')` and expects the error text plus the original task goal still in the textarea.
  - `it('surfaces budget update failures while keeping active run visible')` rejects `updateTaskBudget` and expects `Unable to update task budget.` plus the current run status still visible.
  - `it('shows approval and retry recovery actions for awaiting and failed states')` loads `awaiting_confirmation` and failed task examples and expects approval/retry actions with visible run boundary copy.
  </test_contract>
  <verify_red>
  <automated>COREPACK_HOME=.tmp/corepack pnpm --dir src/web test -- SoloPage --runInBand</automated>
  <expected>FAIL before implementation because SOLO lacks the commercial readiness summary and does not expose all action failures with run context preserved.</expected>
  </verify_red>
</task>

<task type="code">
  <name>Task 4: Harden SOLO/Agent run-state UX</name>
  <files>src/web/src/routes/workspace/SoloPage.tsx</files>
  <action>Expose commercial run readiness and recoverable action errors.</action>
  <implementation_contract>
  - Add an explicit `Commercial run readiness` section when `startedTask` exists.
  - Include status, execution mode, authorization scope, budget consumed/limit, knowledge source count, allowed tools, blocked tools, and current recovery state in that section.
  - Preserve the task creation form when create/start fails and render thrown error text when available.
  - Keep active task details visible when pause, resume, approve, cancel, retry, budget update, export, or continue-in-chat actions fail.
  - Render approval boundary copy for `awaiting_confirmation`.
  - Render retry recovery copy and action for failed/stopped tasks that can be restarted with `startTask`.
  - Keep existing successful task grouping, task detail, export, and continue-in-chat tests green.
  </implementation_contract>
  <verify_green>
  <automated>COREPACK_HOME=.tmp/corepack pnpm --dir src/web test -- SoloPage --runInBand</automated>
  <expected>PASS after SOLO/Agent journey hardening.</expected>
  </verify_green>
</task>

<task type="test">
  <name>Task 5: Add RED Marketplace commercial action-state tests</name>
  <files>src/web/src/routes/marketplace/MarketplacePage.test.tsx</files>
  <action>Add tests that lock paid/free install, review, publish, and uninstall boundaries.</action>
  <test_contract>
  - Extend the fixture with a paid agent and assert the detail page shows `Paid installs create a checkout-backed marketplace order before workspace installation.`.
  - `it('surfaces install failures without showing installed success')` rejects `installAgent` and expects `Unable to install agent.` plus no `Agent installed.` message.
  - `it('surfaces review failures and keeps review text')` rejects `submitReview` and expects `Unable to submit review.` plus the typed review text.
  - `it('describes review and settlement boundaries for paid publisher submissions')` selects paid pricing on publish, submits, and expects success text `Agent submitted for review. Paid installs remain checkout-backed until approval and settlement evidence exist.`
  - `it('surfaces uninstall failures on my agents')` rejects `uninstallAgent` and expects `Unable to uninstall agent.` while the installed agent row remains visible.
  </test_contract>
  <verify_red>
  <automated>COREPACK_HOME=.tmp/corepack pnpm --dir src/web test -- MarketplacePage --runInBand</automated>
  <expected>FAIL before implementation because Marketplace action errors and settlement boundary copy are incomplete.</expected>
  </verify_red>
</task>

<task type="code">
  <name>Task 6: Harden Marketplace install, review, publish, and uninstall states</name>
  <files>src/web/src/routes/marketplace/MarketplaceAgentDetailPage.tsx, src/web/src/routes/marketplace/MarketplacePublishPage.tsx, src/web/src/routes/marketplace/MarketplaceMyAgentsPage.tsx</files>
  <action>Implement commercial Marketplace action feedback.</action>
  <implementation_contract>
  - Add action loading and action error state to `MarketplaceAgentDetailPage`.
  - Disable install/review buttons while their action is in flight.
  - Catch install/review errors and show visible text with the thrown message if present.
  - Show paid-install settlement boundary copy for non-free agents and keep free agents labeled as direct installs.
  - In `MarketplacePublishPage`, show copy that public/paid submissions enter review before paid operation and settlement; success copy must mention review and settlement boundaries for paid submissions.
  - In `MarketplaceMyAgentsPage`, catch uninstall errors, keep the installed row visible, and show a retryable action error.
  </implementation_contract>
  <verify_green>
  <automated>COREPACK_HOME=.tmp/corepack pnpm --dir src/web test -- MarketplacePage --runInBand</automated>
  <expected>PASS after Marketplace hardening.</expected>
  </verify_green>
</task>

<task type="test">
  <name>Task 7: Add RED Admin commercial operations journey tests</name>
  <files>src/web/src/routes/admin/AdminHomePage.test.tsx, src/web/src/routes/admin/AdminBillingPage.test.tsx, src/web/src/routes/admin/AdminReviewsPage.test.tsx</files>
  <action>Add Admin tests that prove routed commercial operation modules are not placeholders.</action>
  <test_contract>
  - `AdminHomePage.test.tsx` expects a `Commercial operations` section that lists Channels, Routes, Plans, Billing, Users, Audit Log, and Review Queue as routed modules.
  - `AdminBillingPage.test.tsx` expects empty billing surfaces to render `No billing records found for this commercial surface.` instead of a generic empty table message.
  - `AdminReviewsPage.test.tsx` expects each pending review row to include pricing type/amount and a governance status hint before approval/rejection.
  </test_contract>
  <verify_red>
  <automated>COREPACK_HOME=.tmp/corepack pnpm --dir src/web test -- AdminHomePage AdminBillingPage AdminReviewsPage --runInBand</automated>
  <expected>FAIL before implementation because Admin does not yet expose the Phase 28 commercial operations checklist and review pricing/governance hints.</expected>
  </verify_red>
</task>

<task type="code">
  <name>Task 8: Harden Admin commercial operations UI</name>
  <files>src/web/src/routes/admin/AdminHomePage.tsx, src/web/src/routes/admin/AdminBillingPage.tsx, src/web/src/routes/admin/AdminReviewsPage.tsx</files>
  <action>Expose routed commercial operations modules and review/billing context.</action>
  <implementation_contract>
  - Add a `Commercial operations` section to `AdminHomePage` with links or labels for Channels, Routes, Plans, Billing, Users, Audit Log, and Review Queue.
  - Keep existing metric cards and charts.
  - In `AdminBillingPage`, use a commercial-specific empty state per selected surface.
  - In `AdminReviewsPage`, show pricing type/amount, visibility, and governance status in the review queue so admins can evaluate commercial impact before approval.
  - Preserve approve/reject confirmation behavior and existing tests.
  </implementation_contract>
  <verify_green>
  <automated>COREPACK_HOME=.tmp/corepack pnpm --dir src/web test -- AdminHomePage AdminBillingPage AdminReviewsPage --runInBand</automated>
  <expected>PASS after Admin hardening.</expected>
  </verify_green>
</task>

<task type="docs">
  <name>Task 9: Update commercial gates and quality checks for PROD-04</name>
  <files>docs/release/commercial-gates.md, scripts/verify-quality-gates.sh, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Document Phase 28 evidence requirements without claiming final readiness.</action>
  <documentation_contract>
  - `docs/release/commercial-gates.md` adds Phase 28 as the `PROD-04` evidence path and states Phase 29-30 remain required.
  - `scripts/verify-quality-gates.sh` asserts Phase 28 context/plan files and active-route anti-placeholder checks for customer-facing routes.
  - `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` route Phase 28 as ready to execute before implementation, then complete only after verification.
  - Do not mark `PROD-05`, `PROD-06`, `AUDIT-01`, Product Completeness Gate, or final commercial readiness complete.
  </documentation_contract>
  <verify_docs>
  <automated>bash scripts/check.sh docs</automated>
  <expected>PASS after docs and gate updates.</expected>
  </verify_docs>
</task>

<task type="execute">
  <name>Task 10: Verify Phase 28 and close only PROD-04</name>
  <files>.planning/phases/28-commercial-ux-and-journey-hardening/28-VERIFICATION.md, .planning/phases/28-commercial-ux-and-journey-hardening/28-01-SUMMARY.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Record evidence and route next work to Phase 29 only after all checks pass.</action>
  <evidence_contract>
  - Record focused Chat tests.
  - Record focused SOLO/Agent tests.
  - Record focused Marketplace tests.
  - Record focused Admin tests.
  - Record full relevant web test command: `COREPACK_HOME=.tmp/corepack pnpm --dir src/web test -- ChatPage SoloPage KnowledgePage MarketplacePage AdminHomePage AdminBillingPage AdminReviewsPage --runInBand`.
  - Record docs gate: `bash scripts/check.sh docs`.
  - Record diff hygiene: `git diff --check`.
  - Mark `PROD-04` complete only after all behavior and docs gates pass.
  - Route next work to Phase 29 Public Docs Onboarding Pricing and Operator Guides.
  - Preserve the boundary that v08 and final commercial readiness remain open.
  </evidence_contract>
  <verify_green>
  <automated>COREPACK_HOME=.tmp/corepack pnpm --dir src/web test -- ChatPage SoloPage KnowledgePage MarketplacePage AdminHomePage AdminBillingPage AdminReviewsPage --runInBand</automated>
  <automated>bash scripts/check.sh docs</automated>
  <automated>git diff --check</automated>
  <expected>All PASS before Phase 28 can close.</expected>
  </verify_green>
</task>

</tasks>
