---
phase: 31.1-readiness-fail-closed
plan: "04"
subsystem: background-workers
tags: [go, readiness, fail-closed, relay, channel, schedule, archive]

# Dependency graph
requires:
  - phase: 31.1-01
    provides: "Manager-backed Guard, typed EffectID bindings, RuntimeAuthorities, and EffectRegistrar"
provides:
  - "Claim/effect readiness checks for scheduled, channel, archive, and Relay batch workers"
  - "Independent batch provider retrieval, finalizer, and terminal transition guards"
  - "Stable readiness-code retry bookkeeping without provider-like effects after denial"
affects: [31.1-02, 31.1-03, 31.1-05, 31.1-06, 31.1-07, 31.1-09, 31.1-10, phase-31.2]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Worker constructors resolve typed capabilities once from the startup RuntimeAuthorities carrier"
    - "Every irreversible worker step re-reads the current manager-backed Guard; no generation is pinned"

key-files:
  created: []
  modified:
    - src/server/internal/schedule/worker.go
    - src/server/internal/schedule/worker_test.go
    - src/server/internal/channel/service_legacy.go
    - src/server/internal/channel/service_test.go
    - src/server/internal/channel/archive.go
    - src/server/internal/channel/archive_test.go
    - src/server/internal/relay/batch_polling_worker.go
    - src/server/internal/relay/batch_polling_worker_test.go
    - src/server/internal/http/workflow_executors.go
    - src/server/internal/releasecontract/catalog.go
    - src/server/internal/releasecontract/catalog_test.go

key-decisions:
  - "Readiness-enabled worker constructors are explicit fail-closed entrypoints; legacy constructors remain available until Plan 31.1-02 owns production composition."
  - "Relay batch finalization and succeeded/dead-letter transitions use a distinct typed EffectID binding while preserving the existing provider capability policy."
  - "Readiness denials persist only the stable ReadinessCode and use bounded retry bookkeeping; they never imply provider settlement, refund, or dispatch."

patterns-established:
  - "Claim checks precede durable claim calls, outbound checks precede provider/channel delivery, and worker-effect checks precede finalizers and terminal state writes."
  - "A prior successful check is never reused for a later effect; each transition invokes the same injected Guard again."

requirements-completed: []

coverage:
  - id: D1
    description: "Scheduled task and channel/archive workers guard claims and each downstream effect with shared current readiness."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "bash scripts/run-go-tests-matched.sh ./internal/schedule '^TestScheduledTaskReadinessGuardContract$'"
        status: pass
      - kind: unit
        ref: "bash scripts/run-go-tests-matched.sh ./internal/channel '^TestChannelRetryAndArchiveReadinessGuardContract$'"
        status: pass
    human_judgment: false
  - id: D2
    description: "Relay batch polling rechecks readiness before claim, provider retrieval, finalizers, and succeeded/dead-letter transitions."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "bash scripts/run-go-tests-matched.sh ./internal/relay '^TestBatchPollingReadinessGuardContract$'"
        status: pass
      - kind: integration
        ref: "cd src/server && go test ./internal/relay ./internal/releasecontract -count=1"
        status: pass
    human_judgment: false
  - id: D3
    description: "All plan changes preserve the repository Go regression suite and clean patch formatting."
    requirement: RELS-01
    verification:
      - kind: integration
        ref: "cd src/server && go test ./... -count=1"
        status: pass
      - kind: other
        ref: "git diff --check"
        status: pass
    human_judgment: false

# Metrics
duration: 20 min
completed: 2026-07-18
status: complete
---

# Phase 31.1 Plan 04: Worker Fail-Closed Guard Summary

**Scheduled, channel/archive, and Relay batch workers now re-evaluate the shared readiness manager before every claim and irreversible effect, with bounded denial bookkeeping and zero provider-like calls after expiry.**

## Performance

- **Duration:** 20 min (continuation after the Task 3 model-capacity interruption)
- **Started:** 2026-07-18T09:35:00+08:00
- **Completed:** 2026-07-18T09:55:00+08:00
- **Tasks:** 3
- **Files modified:** 11

## Accomplishments

- Task 1 guards scheduled task claims, workflow creation/continuation, and agent starts through injected `Guard`, `RuntimeAuthorities`, and `EffectRegistrar`; the workflow tool adapter remains delegation-only.
- Task 2 guards channel retry claims and every outbound send, plus archive listing, sink writes, and source deletion independently.
- Task 3 guards Relay batch claims, provider retrieval, completion/failure finalizers, and succeeded/dead-letter transitions. Expiry after a claim records only a stable readiness code for retry and does not settle, refund, dispatch, or mark a provider result terminal.

## Task Commits

Each task was committed atomically:

1. **Task 1: Guard scheduled claims and workflow/agent execution effects** - `8032604` (`feat(31.1-04)`)
2. **Task 2: Guard channel retry sends and archive object/source effects** - `9da2648` (`feat(31.1-04)`)
3. **Task 3: Guard batch claim, provider retrieval and terminal finalizers** - `08745fe` (`feat(31.1-04)`)

**Plan metadata:** created after this summary and state updates; recorded in the completion report.

## Files Created/Modified

- `src/server/internal/schedule/worker.go` - readiness-enabled scheduled worker constructor and claim/effect guards.
- `src/server/internal/channel/service_legacy.go` - retry claim and per-delivery guard boundaries.
- `src/server/internal/channel/archive.go` - independent archive claim, sink, and delete guards.
- `src/server/internal/relay/batch_polling_worker.go` - batch claim/retrieval/finalizer/terminal guard boundaries and stable denial bookkeeping.
- `src/server/internal/releasecontract/catalog.go` - typed Relay batch finalizer effect binding.
- `src/server/internal/http/workflow_executors.go` - injected delegation-only workflow tool executor seam.
- Matching `*_test.go` files - named contract suites, fake-clock expiry cases, zero-call spies, and authorization-preserving regressions.

## Decisions Made

- Keep readiness-enabled constructors explicit and fail closed while preserving compatibility constructors until the startup composition owner in Plan 31.1-02 switches production wiring.
- Resolve all worker capabilities from the startup `RuntimeAuthorities` carrier and register stable descriptors at construction; workers never reload contract/profile data or derive local capability IDs.
- Treat denied claims/effects as bounded idempotent retry/release bookkeeping only. Batch finalizers and terminal transitions require their own current guard check.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Persist only stable readiness codes for denied Relay batch jobs**
- **Found during:** Task 3 (batch claim/provider/finalizer guards)
- **Issue:** `ReadinessError.Error()` can include a diagnostic field, which would make retry bookkeeping unstable and expose internal detail.
- **Fix:** Added typed `ReadinessError` extraction with a non-readiness fallback for `recordReadinessDeniedJob`; added a field-bearing denial test.
- **Files modified:** `src/server/internal/relay/batch_polling_worker.go`, `src/server/internal/relay/batch_polling_worker_test.go`
- **Verification:** `TestBatchPollingReadinessGuardContract`, relay package tests, and full Go suite pass.
- **Committed in:** `08745fe`

**Total deviations:** 1 auto-fixed (1 bug).
**Impact on plan:** Required to preserve the declared stable denial-code contract; no scope expansion.

## Issues Encountered

- Task 3 resumed with uncommitted implementation after a model-capacity interruption. The partial diff was inspected and completed in place; prior Task 1/2 commits were not redone or altered.
- An initial helper invocation used an unanchored test regex and was rejected by the helper; the exact anchored suite command was rerun successfully.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 31.1-02 can switch runtime composition to the readiness-enabled constructors using the same startup `RuntimeAuthorities` and manager-backed `Guard`.
- Plan 31.1-03 can discover the stable claim/effect descriptors and exact typed binding joins after all consumer producers are present.
- `RELS-01` remains pending at the phase level until the remaining consumers, deployment wiring, and repository-local readiness evidence are complete.

## Known Stubs

None. Compatibility constructors intentionally remain until Plan 31.1-02 owns production composition; readiness-enabled constructors are fail-closed and tested.

## Self-Check: PASSED

- Summary file exists and all three task commits are present: `8032604`, `9da2648`, and `08745fe`.
- Focused suites for schedule, channel, and Relay batch readiness guards pass.
- `cd src/server && go test ./... -count=1` passes across all Go packages.
- `git diff --check` passes for the pending orchestrator-only config change and committed task tree.
- No tracked files were deleted by the Task 3 commit.

---
*Phase: 31.1-readiness-fail-closed*
*Completed: 2026-07-18*
