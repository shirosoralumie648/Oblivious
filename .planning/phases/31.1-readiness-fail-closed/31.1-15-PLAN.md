---
phase: 31.1-readiness-fail-closed
plan: "15"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/internal/agent/executor.go
  - src/server/internal/agent/service.go
  - src/server/internal/agent/executor_test.go
  - src/server/internal/agent/executor_test_helpers_test.go
  - src/server/internal/agent/runner_skill_test.go
  - src/server/internal/agent/runner_react_test.go
  - src/server/internal/agent/service_test.go
  - src/server/internal/http/workflow_executor_test.go
autonomous: true
gap_closure: true
requirements: [RELS-01]
must_haves:
  truths:
    - "The one-argument compatibility `NewToolExecutor` denies builtin, HTTP, process, sandbox, and MCP effects before downstream calls (D-RDY-11, D-RDY-14)."
    - "Effect-capable test construction exists only through an authority-backed `_test.go` helper; production Agent/HTTP composition has an AST-enforced authorized-constructor path and no permissive bypass (D-RDY-20, D-RDY-22)."
    - "Every behavior test that intentionally needs an effect-capable executor, including both HTTP workflow `NewToolExecutor(nil)` callers and `TestWorkflowToolExecutorAdapterRunsBuiltinTool`, uses an explicit authority-backed test fixture rather than the deny-only compatibility constructor (D-RDY-11, D-RDY-14)."
  artifacts:
    - path: "src/server/internal/agent/executor.go"
      provides: "Fail-closed compatibility ToolExecutor"
      contains: "NewToolExecutor"
    - path: "src/server/internal/agent/executor_test_helpers_test.go"
      provides: "Test-only explicit authority-backed fixtures"
      contains: "newAuthorizedToolExecutorForTest"
    - path: "src/server/internal/http/workflow_executor_test.go"
      provides: "Authority-backed HTTP workflow ToolExecutor fixtures for both compatibility callers"
      contains: "TestWorkflowToolExecutorAdapterRunsBuiltinTool"
  key_links:
    - from: "NewToolExecutor"
      to: "ToolExecutor.authorizeTool"
      via: "missing runtime authority returns readiness_unavailable before dispatch"
      pattern: "authorizeTool"
    - from: "workflow executor tests"
      to: "NewAuthorizedToolExecutor"
      via: "one explicit HTTP-package fixture supplies valid Guard, RuntimeAuthorities, Effects, and HTTP client"
      pattern: "authorizedWorkflowToolExecutor"
---

<objective>
Close WR-03 by denying compatibility Agent effects and migrating every intentional behavior fixture, including the two HTTP workflow callers, to explicit authority-backed construction.

Purpose: The one-argument constructor must be safe when wiring regresses, while tests that exercise real tool behavior must state and provide their authority explicitly.
Output: Fail-closed compatibility behavior, authority-backed test fixtures, complete caller migration, and AST production proof.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-VERIFICATION.md
@.planning/phases/31.1-readiness-fail-closed/31.1-10-SUMMARY.md
@src/server/internal/agent/executor.go
@src/server/internal/agent/service.go
@src/server/internal/http/workflow_executor_test.go

<constraints>
- Do not add an exported allow-all constructor or environment flag.
- Test-only effect-capable helpers must call the real authorized constructor from `_test.go` and may not be referenced by production source.
- Both `agent.NewToolExecutor(nil)` calls in `workflow_executor_test.go` must be migrated; leaving either call behind is a failed plan, even if the focused builtin test passes.
- CR-06/CR-09 remain Phase 34, WR-02 remains Phase 35, and the two readiness OpenAPI Envelope definitions remain Phase 31.2.
- Evidence is repository-local E1 only.
</constraints>

<interfaces>
Keep `NewToolExecutor(*mcp.Client) *ToolExecutor` source-compatible, but its returned value has no authority and therefore denies effects. `NewAuthorizedToolExecutor` remains the only effect-capable constructor. Test fixtures may wrap that constructor only from `_test.go` and must use a valid authored contract/profile, Guard, duplicate-rejecting registrar, and test HTTP client; they cannot set private fields or add an allow-all production constructor.
</interfaces>
</context>

## Artifacts this phase produces

- Deny-only compatibility ToolExecutor.
- Explicit authority-backed test-only constructor and complete Agent/HTTP fixture migration.
- AST regression for production constructor choice.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make compatibility ToolExecutor deny every effect</name>
  <files>src/server/internal/agent/executor.go, src/server/internal/agent/service.go, src/server/internal/agent/executor_test.go, src/server/internal/agent/executor_test_helpers_test.go, src/server/internal/agent/runner_skill_test.go, src/server/internal/agent/runner_react_test.go, src/server/internal/agent/service_test.go</files>
  <read_first>
    - src/server/internal/agent/executor.go
    - src/server/internal/agent/service.go
    - src/server/internal/agent/executor_test.go
    - src/server/internal/agent/runner_skill_test.go
    - src/server/internal/agent/runner_react_test.go
    - src/server/internal/agent/service_test.go
    - .planning/phases/31.1-readiness-fail-closed/31.1-REVIEW.md
  </read_first>
  <behavior>
    - Compatibility executor returns `readiness_unavailable` for each effect category and every downstream counter is zero.
    - Authorized executor behavior is unchanged.
    - Production `agent` and `http` source contains no effect-capable call to the compatibility constructor.
  </behavior>
  <action>Change the no-authority branch of `authorizeTool` to return a stable readiness error instead of success. Add a clearly named helper in a new `_test.go` file that calls `NewAuthorizedToolExecutor` with a real test Guard, RuntimeAuthorities, duplicate-rejecting registrar, and HTTP client; do not mutate private fields to simulate permissive authority. Keep fail-closed tests on `NewToolExecutor`. Add an AST test that walks production `internal/agent` and `internal/http` call sites and proves effect-capable runtime construction uses `NewAuthorizedToolExecutor`/`NewServiceWithRuntimeOptions`; fallback `NewToolExecutor` values are deny-only and cannot be mistaken for production authorization.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/agent '^TestCompatibilityToolExecutorFailClosedContract$|^TestProductionToolExecutorConstructionContract$'</automated>
  </verify>
  <acceptance_criteria>
    - All five compatibility effect categories deny with zero downstream calls.
    - No exported or production-compiled permissive helper exists.
    - Existing behavior tests use the explicit test-only helper and pass.
    - AST production construction test passes.
  </acceptance_criteria>
  <done>Compatibility construction is source-compatible but cannot authorize any effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate every intentional Agent and HTTP behavior fixture</name>
  <files>src/server/internal/agent/executor_test.go, src/server/internal/agent/executor_test_helpers_test.go, src/server/internal/agent/runner_skill_test.go, src/server/internal/agent/runner_react_test.go, src/server/internal/agent/service_test.go, src/server/internal/http/workflow_executor_test.go</files>
  <read_first>
    - src/server/internal/agent/executor_test_helpers_test.go
    - src/server/internal/agent/executor_test.go
    - src/server/internal/agent/runner_skill_test.go
    - src/server/internal/agent/runner_react_test.go
    - src/server/internal/agent/service_test.go
    - src/server/internal/http/workflow_executor_test.go
  </read_first>
  <behavior>
    - Legacy Agent tests that intentionally execute builtin, HTTP, process, sandbox, or MCP effects use the authority-backed `_test.go` helper and retain their original behavioral assertions.
    - Both HTTP-package `agent.NewToolExecutor(nil)` callers are replaced by one local authority-backed fixture, including `TestWorkflowToolExecutorAdapterRunsBuiltinTool`.
    - A repository AST/count assertion finds zero effect-capable behavior fixtures using `NewToolExecutor`; only named fail-closed tests retain that constructor.
  </behavior>
  <action>Mechanically migrate only tests whose expected outcome requires a concrete effect to the authority-backed helper. In `workflow_executor_test.go`, add an HTTP-package fixture that returns an authorized executor and migrate both current `agent.NewToolExecutor(nil)` expressions, explicitly preserving `TestWorkflowNodeExecutorRegistryIncludesRealLLMKnowledgeAndAgentExecutors` and `TestWorkflowToolExecutorAdapterRunsBuiltinTool`. Keep compatibility denial tests on `NewToolExecutor`. Extend the production-construction AST/count regression to classify every remaining one-argument call by file and test name, fail on any unapproved behavior caller, and require the exact two HTTP legacy calls to be absent.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/agent '^TestAuthorizedToolExecutorFixtureMigrationContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./internal/http '^TestWorkflowNodeExecutorRegistryIncludesRealLLMKnowledgeAndAgentExecutors$|^TestWorkflowToolExecutorAdapterRunsBuiltinTool$'</automated>
  </verify>
  <acceptance_criteria>
    - Every intended behavior test passes through explicit authority and no exported/test-permissive bypass exists.
    - Both HTTP workflow callers use the local authorized fixture; no `agent.NewToolExecutor(nil)` remains in that file.
    - Remaining compatibility-constructor calls are enumerated fail-closed cases or non-effect metadata cases and are checked structurally.
  </acceptance_criteria>
  <done>Changing compatibility construction to deny-only does not silently weaken or invalidate intentional Agent/HTTP behavior coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Legacy constructor -> concrete tool effect | Missing runtime authority previously became implicit allow-all. |
| Test fixture -> effect-capable executor | A permissive or private-field fixture can conceal missing runtime authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-15-01 | Elevation of Privilege | Compatibility ToolExecutor | high | mitigate | Deny missing authority for all effects; permit test behavior only in `_test.go`. |
| T-31.1-15-02 | Spoofing | Authority-backed test fixture | high | mitigate | Test helper calls the real authorized constructor with valid authority; AST/count proof rejects leftover behavior callers. |
| T-31.1-15-03 | Spoofing | Production constructor selection | high | mitigate | AST test proves production-capable paths use authorized construction. |
</threat_model>

<verification>
- Run both focused selectors.
- Run `cd src/server &amp;&amp; go test ./internal/agent ./internal/http -count=1`.
- Run `git diff --check` and confirm no permissive/private-field helper exists; the authority-backed helper is `_test.go` only.
</verification>

<success_criteria>
- The compatibility canonical gap and every affected Agent/HTTP test caller are closed.
- Existing authorized behavior remains covered without an exported bypass.
</success_criteria>

<commit_policy>
Create one atomic commit: `fix(31.1-15): fail closed compatibility tools`. Do not push. Plan 20 ends with a HARD EXTERNAL/MANUAL POST-CLOSEOUT GATE handoff; no GSD executor, verifier, tracker, hook, or autonomous orchestrator step owns final Stage B or push.
</commit_policy>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-15-SUMMARY.md` with compatibility, AST, and complete Agent/HTTP fixture-migration evidence.
</output>
