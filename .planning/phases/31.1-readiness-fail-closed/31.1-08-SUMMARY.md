---
phase: 31.1-readiness-fail-closed
plan: "08"
subsystem: deployment-readiness-harness
tags: [docker, postgres, readiness, deployment, reports, kubernetes]
requires:
  - phase: 31.1-readiness-fail-closed
    provides: "Manager-backed readiness endpoints and typed readiness/deployment report producers"
provides:
  - "Self-contained monolith image with release-contract inspector, trusted labels, packaged contract/schema and output-only audit path"
  - "Canonical Compose/Kubernetes monolith wiring with /livez startup/liveness, /readyz readiness and emptyDir audit output"
  - "Mandatory disposable standalone-build and aggregate-consume E2 harnesses with exact identity, migration, stale-refresh and report evidence"
affects: [31.1-verification, 31.2-contract-surface-parity-gate, release-evidence]
requirements-completed: []
coverage:
  - id: D1
    description: "Deployment report details are typed, distinct from readiness details, profile/workload/probe/audit/migration/harness bounded, and reject unknown fields, skips and claim inflation."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/surfacereport/deployment_test.go#TestDeploymentSurfaceContract"
        status: pass
      - kind: other
        ref: "bash scripts/verify-readiness-deployment-contract.sh"
        status: pass
    human_judgment: false
  - id: D2
    description: "Dockerfile.server, Compose and app-deployment.yaml use one explicit monolith profile, packaged identity/contract, output-only audit storage and the exact /livez vs /readyz probe split; server.yaml is excluded from release inventory."
    requirement: RELS-01
    verification:
      - kind: other
        ref: "python scripts/verify_deployment_operations_contract.py ."
        status: pass
      - kind: other
        ref: "bash scripts/verify-readiness-deployment-contract.sh"
        status: pass
    human_judgment: false
  - id: D3
    description: "Disposable standalone-build E2 starts the exact immutable image, applies real PostgreSQL migrations, proves healthy/blocked/stale/restored manager transitions, preserves generation/times across malformed refreshes, and writes two identity-bound reports with zero skips."
    requirement: RELS-01
    verification:
      - kind: integration
        ref: "/var/tmp/oblivious-31-1-stage-a-final-6b12fce/harness-result.json"
        status: pass
      - kind: integration
        ref: "/var/tmp/oblivious-31-1-stage-a-final-6b12fce/readiness-report.json and deployment-report.json"
        status: pass
    human_judgment: false
  - id: D4
    description: "Aggregate-consume consumes the supplied release-artifact-bundle/v1 identity/tag/digest with buildInvocationCount=0 and reruns the same disposable runtime proof without rebuilding."
    requirement: RELS-01
    verification:
      - kind: integration
        ref: "/var/tmp/oblivious-31-1-aggregate-6b12fce/harness-result.json"
        status: pass
      - kind: other
        ref: "bash scripts/verify-readiness-contract-fixtures.sh"
        status: pass
    human_judgment: false
---

# Phase 31.1 Plan 08: Canonical Deployment and Readiness Harness Summary

Canonical monolith deployment assets now consume the same packaged contract, profile and trusted image identity as runtime readiness. The mandatory harness provisions a disposable PostgreSQL database, applies the real migration binary, starts the exact image with an output-only audit volume, and records dynamic readiness transitions through separate typed report CLI producers.

## Task Commits

1. **Task 1: Lock typed deployment report contract** - `6df31c1`
2. **Task 2: Wire canonical image, Compose, Kubernetes and mutation gate** - `e9b1a4c`
3. **Task 3: Add standalone/aggregate harness and Stage A/B gates** - `0ca951a`
4. **Runtime composition hardening: exact shared effect registration is idempotent** - `b494e87`
5. **Readiness report CLI output parsing and offline snapshot validation** - `b0a3806`, `e3361aa`
6. **Shared typed report registry and failure evidence** - `6b12fce`, `877c99f`

## Accomplishments

- Added `oblivious-release-contract` to the image, preserved build identity/OCI labels, packaged contract/schema, created non-root-writable `/var/lib/oblivious/audit`, and set explicit monolith/contract/audit environment paths.
- Made `app-deployment.yaml` the only canonical application workload in release validation; startup and liveness use `/livez`, readiness uses `/readyz`, and Kubernetes audit evidence uses `emptyDir` output storage. Compose uses a dedicated named audit volume without snapshot/report input mounts.
- Added a source-configured `OBLIVIOUS_READINESS_PROBE_BASE_URL` composition seam for normal typed `HTTPDependencyProbe` instances. Missing configuration retains the blocked fail-closed behavior; the harness uses an atomic control protocol with healthy, blocked and structural modes.
- Added strict standalone-build and aggregate-consume harness modes. Docker, DB, migration, image digest/tag, bundle, audit, report and cleanup failures are hard failures. Aggregate mode records zero builder invocations and consumes only the supplied immutable bundle.
- Fixed production composition defects exposed only by the disposable image: exact shared `chat.provider.dispatch` registrations are idempotent, readiness report output parsing handles common flags, offline reports validate at snapshot time while enforcing current freshness, and the default typed report registry includes readiness details.

## Verification

- `bash scripts/verify-readiness-contract-fixtures.sh` passed, including static deployment mutations, invalid mode, missing bundle and Docker-unavailable fail-closed cases.
- Stage A passed on exact clean fixture HEAD `6b12fce712f912042bd0e484d3842e1a8afd567e`: image digest `sha256:9ac15932d56a6c3a221b7e295ac2ab2cb7b45c0631b201ebfed89bdcf2944f3a`, build count `1`, migration `applied_and_validated`, two report surfaces, empty skips, and healthy/blocked/stale/restored runtime proof.
- Aggregate-consume passed with the same image digest and build count `0` using a transient `release-artifact-bundle/v1`.
- Focused deployment/readiness/report tests and `git diff --check` passed. Full `cd src/server && go test ./... -count=1` remains the final pre-close verification command.

## Evidence Boundary

All evidence is repository-local E1/E2 disposable Docker proof. No target Kubernetes, target provider, profile parity, E3/E4, or commercial release claim is made. Residual risks remain external target inspection, supply-chain attestations, and non-monolith parity.

---
*Phase: 31.1-readiness-fail-closed*
*Completed: 2026-07-18*
