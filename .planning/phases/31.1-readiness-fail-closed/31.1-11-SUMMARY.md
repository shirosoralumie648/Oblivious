---
phase: 31.1-readiness-fail-closed
plan: "11"
subsystem: release-readiness
tags: [go, readiness, concurrency, race-detector, fail-closed]
requires:
  - phase: 31.1-readiness-fail-closed
    provides: "Plan 01 ReadinessManager, bounded probe coordinator, immutable generation publication, and authored timing contract"
provides:
  - "Linearizable generation-one bootstrap with exactly one successful concurrent caller"
  - "Capacity-one per-dependency probe execution lanes that bound non-cooperative work"
  - "Cancellation-precedence arbitration that discards enabled observations returned after probe expiry"
  - "Canceled refreshes cannot acquire or strand a dependency lane"
  - "Bootstrap is the only lifecycle authority allowed to publish initial generation one"
  - "Race-detector regressions for bootstrap winner counts, timeout reuse, complete candidates, and late-result isolation"
  - "Nil bootstrap contexts fail closed before probe or audit work"
affects: [31.1-17, 31.1-19, 31.1-20, release-readiness]
tech-stack:
  added: []
  patterns:
    - "Dedicated private bootstrap transition mutex"
    - "Per-managed-probe capacity-one in-flight channel"
    - "Pre- and post-receive context checks around probe completion"
    - "Pre- and post-acquire context checks with token return on cancellation"
key-files:
  created: []
  modified:
    - src/server/internal/releasecontract/readiness_manager.go
    - src/server/internal/releasecontract/readiness_manager_test.go
key-decisions:
  - "Bootstrap serialization spans the pre-publication state check through the generation-one refresh and publication."
  - "An occupied dependency lane synthesizes dependency_unproven for the current candidate without launching another Probe.Run invocation."
  - "Observed probe cancellation takes precedence over an already-ready completion before any observation is accepted."
  - "Ordinary and periodic refresh require an existing generation; only Bootstrap may transition nil state to generation one."
  - "Bootstrap rejects a nil context with the stable readiness_unavailable/context error before entering the serialized transition."
patterns-established:
  - "Late probe completion remains candidate-local and releases its dependency lane only after the real invocation exits."
  - "Tests wait on observable lane emptiness instead of fixed release sleeps."
requirements-completed: []
coverage:
  - id: D1
    description: "Concurrent bootstrap has one winner, 63 stable fail-closed losers, exactly one complete probe cycle, generation one, and one audit publication."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/releasecontract/readiness_manager_test.go#TestReadinessManagerBootstrapConcurrencyContract"
        status: pass
      - kind: other
        ref: "bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerBootstrapConcurrencyContract$' -race"
        status: pass
    human_judgment: false
  - id: D2
    description: "A context-ignoring dependency consumes at most one in-flight invocation while repeated refreshes remain bounded, publish complete blocked candidates, reject late results, and reuse the lane after release."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/releasecontract/readiness_manager_test.go#TestReadinessManagerNonCooperativeProbeBoundContract"
        status: pass
      - kind: other
        ref: "bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerNonCooperativeProbeBoundContract$' -race"
        status: pass
      - kind: integration
        ref: "cd src/server && go test ./internal/releasecontract -count=1"
        status: pass
    human_judgment: false
  - id: D3
    description: "When probe completion and deadline cancellation are simultaneously ready, cancellation wins and an enabled post-deadline observation is discarded before candidate publication."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/releasecontract/readiness_manager_test.go#TestReadinessManagerProbeDeadlinePrecedenceContract"
        status: pass
      - kind: other
        ref: "bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerProbeDeadlinePrecedenceContract$' -race"
        status: pass
    human_judgment: false
  - id: D4
    description: "An already-canceled refresh starts zero dependency probes, returns any acquired token before Probe.Run, preserves the current generation, and leaves the lane reusable by a healthy refresh."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/releasecontract/readiness_manager_test.go#TestReadinessManagerCanceledRefreshDoesNotStartProbeContract"
        status: pass
      - kind: other
        ref: "bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerCanceledRefreshDoesNotStartProbeContract$' -race -count=20"
        status: pass
    human_judgment: false
  - id: D5
    description: "An ordinary refresh racing an in-progress Bootstrap cannot probe, publish generation one, or write audit; the gated Bootstrap remains the sole generation-one publisher."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/releasecontract/readiness_manager_test.go#TestReadinessManagerBootstrapOwnsInitialGenerationContract"
        status: pass
      - kind: other
        ref: "bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerBootstrapOwnsInitialGenerationContract$' -race -count=20"
        status: pass
    human_judgment: false
  - id: D6
    description: "Bootstrap(nil) is deterministic and fail-closed: it returns readiness_unavailable with context field, starts zero probes, writes zero audits, and publishes no generation."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/releasecontract/readiness_manager_test.go#TestReadinessManagerBootstrapNilContextContract"
        status: pass
      - kind: other
        ref: "bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerBootstrapNilContextContract$' -race"
        status: pass
    human_judgment: false
duration: 30 min
completed: 2026-07-19
status: complete
---

# Phase 31.1 Plan 11: Bootstrap and Probe Work Bounds Summary

**Generation-one bootstrap is the sole initial publisher, every dependency has one cancellation-safe capacity-bounded probe lane, and deadline cancellation wins before completion can authorize a candidate.**

## Performance

- **Duration:** 30 min active execution
- **Started:** 2026-07-19T03:58:42Z
- **Completed:** 2026-07-19T05:17:57Z
- **Tasks:** 2 planned tasks + 4 reviewed remediations
- **Files modified:** 2

## Accomplishments

- Added a dedicated bootstrap transition mutex covering the initial-state check through generation-one refresh and publication. The 64-caller race contract proves one success, 63 stable `readiness_unavailable` failures, generation 1, one complete applicable probe set, and one audit write.
- Compiled a capacity-one in-flight lane for every applicable dependency. Occupied lanes synthesize `dependency_unproven` without launching another goroutine, while other dependencies continue producing structurally complete candidates.
- Proved that releasing a stuck invocation does not mutate an older generation and makes the dependency lane reusable by a later refresh.
- Added explicit completion arbitration that checks cancellation before waiting and again after receiving a result, preventing an enabled result returned after expiry from authorizing the candidate.
- Added cancellation checks before and immediately after lane acquisition, returning the token before `Probe.Run` when cancellation wins and preserving healthy lane reuse.
- Reserved nil-to-generation-one publication for Bootstrap; ordinary and periodic refresh now require an existing generation before probing.
- Replaced the late-result regression's fixed release sleep with bounded observation of the private lane becoming empty.
- Added a nil-context Bootstrap guard and deterministic regression coverage; invalid lifecycle input now returns stable `readiness_unavailable`/`context` without probes, audit writes, or generation publication.

## Task Commits

Each task was committed atomically:

1. **Task 1: Serialize the bootstrap state transition** - `8e7d7b8`
2. **Task 2: Bound non-cooperative probe work per dependency** - `21a8a1e`
3. **Reviewed remediation: Prioritize probe cancellation over completion** - `9ad7ad9`
4. **Reviewed remediation A: Reject canceled lane acquisition** - `3c9f437`
5. **Reviewed remediation B: Reserve initial generation for Bootstrap** - `bf22b67`
6. **Reviewed remediation C: Wait for observable lane release** - `c0a8ab0`

## Files Created/Modified

- `src/server/internal/releasecontract/readiness_manager.go` - Adds Bootstrap-only initial publication, cancellation-safe capacity-one execution lanes, and cancellation-precedence completion arbitration.
- `src/server/internal/releasecontract/readiness_manager_test.go` - Adds concurrent bootstrap, canceled acquisition, initial-generation ownership, non-cooperative probe, and simultaneous-ready deadline race regressions.

## Test Evidence

- RED: `TestReadinessManagerBootstrapConcurrencyContract` failed against the prior implementation with `64 succeeded, 0 failed; want 1 and 63`.
- GREEN: `bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerBootstrapConcurrencyContract$' -race` passed with one concrete matched test.
- RED: `TestReadinessManagerNonCooperativeProbeBoundContract` failed against the prior implementation with `non-cooperative probe calls before release = 6, want 1`.
- GREEN: `bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerNonCooperativeProbeBoundContract$' -race` passed with one concrete matched test.
- REVIEW RED: `TestReadinessManagerProbeDeadlinePrecedenceContract` exposed the missing explicit arbitration point as `undefined: awaitProbeResult` before production changes.
- REVIEW GREEN: `bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerProbeDeadlinePrecedenceContract$' -race -count=20` passed deterministic helper and manager integration subtests.
- REVIEW A RED: `TestReadinessManagerCanceledRefreshDoesNotStartProbeContract` observed `1 new postgres probes, want zero`.
- REVIEW A GREEN: its focused selector passed under `-race -count=20`, including later healthy lane reuse.
- REVIEW B RED: `TestReadinessManagerBootstrapOwnsInitialGenerationContract` observed a nil error from the racing ordinary refresh, proving it had published initial state.
- REVIEW B GREEN: its focused selector passed under `-race -count=20`, with zero racing audit writes and Bootstrap generation exactly 1.
- REVIEW C: `TestReadinessManagerNonCooperativeProbeBoundContract` passed under `-race -count=20` with bounded lane-empty condition waiting and no fixed release sleep.
- RED follow-up: `TestReadinessManagerBootstrapNilContextContract` reproduced a nil dereference in `runBoundedProbes` at `readiness_manager.go:361` when `Bootstrap(nil)` reached `ctx.Err()`.
- GREEN follow-up: `bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerBootstrapNilContextContract$' -race` passed after the minimal Bootstrap context guard.
- Follow-up focused suite: `bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^(TestReadinessManagerBootstrapNilContextContract|TestReadinessManagerBootstrapConcurrencyContract|TestReadinessManagerCanceledRefreshDoesNotStartProbeContract|TestReadinessManagerBootstrapOwnsInitialGenerationContract|TestReadinessManagerNonCooperativeProbeBoundContract|TestReadinessManagerProbeDeadlinePrecedenceContract)$' -race -count=20` passed all six selectors.
- `cd src/server && go test ./internal/releasecontract -race -count=1` passed.
- `cd src/server && go vet ./internal/releasecontract` passed.
- `git diff --check -- src/server/internal/releasecontract/readiness_manager.go src/server/internal/releasecontract/readiness_manager_test.go` passed after the follow-up.

## Decisions Made

- Bootstrap uses a dedicated transition mutex rather than overloading publication locking, so generation-one ownership is explicit while ordinary publication behavior remains unchanged.
- Each managed dependency owns one nonblocking capacity-one lane. An occupied lane produces the same typed blocked observation as a timeout and never permits a late result to cross candidate boundaries.
- Probe completion is accepted only after cancellation checks on both sides of the blocking receive; this gives observed deadline cancellation deterministic precedence when both cases are ready.
- Lane acquisition checks cancellation before token selection and immediately afterward; the second check returns the token before any probe goroutine starts.
- Ordinary refresh cannot create initial state. Bootstrap alone calls the initial-capable candidate pipeline while holding the bootstrap transition mutex.

## Deviations from Plan

- The plan's commit policy requested one combined implementation commit. The active executor contract required one atomic commit per task, so the two verified tasks were committed separately. File scope and runtime behavior were unchanged.

## TDD Gate Compliance

- Both task-level `tdd="true"` cycles demonstrated focused RED before production edits and focused GREEN under `-race` after the minimal implementation.
- RED and GREEN were kept in each task's atomic outcome commit per the active executor instruction; no separate test-only RED commits were created.

## Issues Encountered

- Spec review identified that Go `select` may pseudo-randomly choose an enabled completion when both it and `probeCtx.Done()` are ready. A private arbitration helper and manager-level post-deadline-enabled regression close that High gap without changing public APIs or lane ownership.
- Code-quality review found the same simultaneous-ready risk at lane acquisition and a lifecycle race between Bootstrap and ordinary refresh. Both now fail closed at their authority boundaries; the late-result test also observes actual lane release rather than elapsed time.

## User Setup Required

None - no external service configuration required.

## Boundary and Residual Risk

- A probe that never returns may retain one goroutine and occupy its dependency lane until process restart. The change bounds this to one invocation per dependency and keeps all later refresh callers bounded and fail closed.
- A nil context is rejected at the Bootstrap boundary; callers must provide a non-nil context to begin lifecycle work.
- Evidence is E1 repository-local unit/race proof. Database migrations, container runtime, target/live dependencies, provider/payment rails, and commercial release were not exercised.
- `RELS-01` remains pending. Plans 12-22 and the canonical Phase 31.1 verifier still own the remaining readiness gaps and closeout evidence.

## Follow-up Commit

- `cc5bb26 fix(31.1-11): reject nil bootstrap context`
- Summary/docs follow-up commit is recorded after `verify-summary`, coverage classification, and scoped self-check.

## Next Phase Readiness

- Ready for gap Plan 31.1-12.
- No Plan 11 blocker remains; final Stage B and push remain outside this executor.

---
*Phase: 31.1-readiness-fail-closed*
*Completed: 2026-07-19*

## Self-Check: PASSED

- Summary file and all seven implementation/test commits, including `cc5bb26`, are present.
- All six focused race selectors at repeated count, package race, `go vet`, coverage classification, and scoped diff checks passed.
- The nil-context RED/GREEN regression is represented in coverage D6 and does not alter public interfaces or authored timing.
- Only the two plan-owned source files and this summary changed; protected `.planning/config.json` and `.planning/intel/` remain untouched and unstaged.
