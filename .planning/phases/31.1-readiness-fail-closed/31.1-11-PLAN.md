---
phase: 31.1-readiness-fail-closed
plan: "11"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/internal/releasecontract/readiness_manager.go
  - src/server/internal/releasecontract/readiness_manager_test.go
autonomous: true
gap_closure: true
requirements: [RELS-01]
must_haves:
  truths:
    - "Concurrent Bootstrap calls have one winner, all other callers fail closed, and the published generation remains exactly 1 (D-RDY-05, D-RDY-06)."
    - "A probe that ignores cancellation can strand at most one invocation per dependency; later refreshes remain deadline-bounded and do not create unbounded probe work (D-RDY-09)."
    - "Timed-out dependency work still publishes a complete blocked observation when structurally possible, while malformed successful observations never publish a generation (D-RDY-08, D-RDY-09)."
  artifacts:
    - path: "src/server/internal/releasecontract/readiness_manager.go"
      provides: "Serialized bootstrap transition and bounded per-dependency probe execution lanes"
      contains: "Bootstrap"
    - path: "src/server/internal/releasecontract/readiness_manager_test.go"
      provides: "Exact concurrent-bootstrap and repeated non-cooperative-probe race regressions"
      contains: "TestReadinessManagerBootstrapConcurrencyContract"
  key_links:
    - from: "Manager.Bootstrap"
      to: "Manager.refresh"
      via: "one serialized bootstrap state transition encloses probe construction through generation-one publication"
      pattern: "Bootstrap|publication"
    - from: "Manager.runBoundedProbes"
      to: "Probe.Run"
      via: "one capacity-one in-flight lane per managed dependency prevents repeated leaked invocations"
      pattern: "probeDeadline|inFlight"
---

<objective>
Close WR-01 and WR-05 by making generation-one bootstrap a single serialized transition and bounding context-ignoring probe work without weakening fail-closed observations.

Purpose: The manager is the sole runtime authority; duplicate bootstrap publication or unbounded orphaned probes undermine both monotonic generations and availability.
Output: A race-safe manager implementation and focused regression coverage.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-VERIFICATION.md
@.planning/phases/31.1-readiness-fail-closed/31.1-REVIEW.md
@.planning/phases/31.1-readiness-fail-closed/31.1-01-SUMMARY.md
@src/server/internal/releasecontract/readiness_manager.go
@src/server/internal/releasecontract/readiness_manager_test.go

<constraints>
- Preserve D-RDY-01: audit files remain output-only and cannot seed bootstrap or authorization.
- Preserve authored 30s/120s/30s timing and exact UTC nanosecond evaluation; this plan adds no runtime override.
- CR-06 and CR-09 remain Phase 34, WR-02 remains Phase 35, and the two readiness OpenAPI Envelope definitions remain Phase 31.2.
- Evidence is repository-local E1 only; this plan makes no target/live or commercial-release claim.
</constraints>

<interfaces>
Keep the existing public `ReadinessManager` interface and `Probe.Run(context.Context)` contract. Add only private manager/managed-probe synchronization state. A non-cooperative probe lane may remain occupied forever, but it must prevent another invocation for that dependency and must not delay the outer refresh beyond `probeDeadline`.
</interfaces>
</context>

## Artifacts this phase produces

- Serialized generation-one bootstrap behavior.
- Capacity-one per-dependency probe lanes with bounded caller latency.
- Race-detector proof for concurrent bootstrap and repeated timeout attempts.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serialize the bootstrap state transition</name>
  <files>src/server/internal/releasecontract/readiness_manager.go, src/server/internal/releasecontract/readiness_manager_test.go</files>
  <read_first>
    - src/server/internal/releasecontract/readiness_manager.go
    - src/server/internal/releasecontract/readiness_manager_test.go
    - .planning/phases/31.1-readiness-fail-closed/31.1-VERIFICATION.md
  </read_first>
  <behavior>
    - 64 callers released simultaneously produce exactly one nil result and 63 stable fail-closed bootstrap errors.
    - The single published snapshot has generation 1, not 2, and the applicable probe set runs once.
    - A second Bootstrap after the winner returns fails without starting probes or changing snapshot bytes.
  </behavior>
  <action>Add a dedicated bootstrap transition lock/state that is acquired before checking current state and remains authoritative through the generation-one refresh/publication. Do not reuse a pre-probe nil check or let a competing bootstrap fall through to ordinary refresh. Keep periodic `refresh` serialization and immutable publication semantics intact. Write the failing concurrent test first, synchronize caller start with a barrier, count exact outcomes/probe calls, and run the case under the race detector.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerBootstrapConcurrencyContract$' -race</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one concurrent Bootstrap returns nil.
    - Evaluation generation is exactly 1 and probe calls equal one complete applicable set.
    - Every losing caller returns a stable readiness error and performs no publication or audit write.
  </acceptance_criteria>
  <done>The bootstrap transition is linearizable and generation one cannot be skipped by concurrent callers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound non-cooperative probe work per dependency</name>
  <files>src/server/internal/releasecontract/readiness_manager.go, src/server/internal/releasecontract/readiness_manager_test.go</files>
  <read_first>
    - src/server/internal/releasecontract/readiness_manager.go
    - src/server/internal/releasecontract/readiness_manager_test.go
    - .planning/phases/31.1-readiness-fail-closed/31.1-PATTERNS.md
  </read_first>
  <behavior>
    - A cancellation-ignoring probe times out within the configured deadline and yields `dependency_unproven`.
    - Five later refresh attempts do not start a second invocation for the occupied dependency.
    - Other dependency lanes complete normally and each candidate remains structurally complete.
    - Releasing the stuck probe makes its lane reusable without publishing a late result into an older candidate.
  </behavior>
  <action>Give each compiled managed probe a private capacity-one execution lane or equivalent single-flight state. Acquire the lane before starting `Probe.Run`; when occupied, return a typed timeout/unproven result for that candidate without launching another goroutine. Release only when the real invocation exits, and keep candidate-local result channels so a late completion cannot enter a newer generation. Extend tests with call counters, repeated refreshes, a release channel, bounded elapsed assertions, and a final successful reuse.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerNonCooperativeProbeBoundContract$' -race</automated>
  </verify>
  <acceptance_criteria>
    - Repeated timeout cycles leave the non-cooperative probe call count at 1 until release.
    - Each refresh returns within a bounded deadline and never publishes a partial observation set.
    - No late result changes an already published generation.
  </acceptance_criteria>
  <done>Context-ignoring probes can consume only bounded work and cannot corrupt candidate or publication ordering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent callers -> manager bootstrap | Uncoordinated callers attempt to create the first authorization generation. |
| Dependency probe -> refresh coordinator | A probe may ignore cancellation or return after its candidate has expired. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-11-01 | Tampering | Bootstrap generation transition | high | mitigate | Serialize pre-check, probe work, and first publication; race-test exact winner/generation counts. |
| T-31.1-11-02 | Denial of Service | Non-cooperative probe execution | high | mitigate | Capacity-one dependency lanes bound orphaned work and preserve outer deadlines. |
| T-31.1-11-03 | Elevation of Privilege | Late probe result | high | mitigate | Candidate-local result ownership prevents late completion from authorizing a later generation. |
</threat_model>

<verification>
- Run both focused selectors under `-race`.
- Run `cd src/server &amp;&amp; go test ./internal/releasecontract -count=1`.
- Run `git diff --check` and inspect only the two owned files.
</verification>

<success_criteria>
- Canonical gap 1 is closed with exact winner, generation, call-count, deadline, and late-result assertions.
- No public authority interface, authored timing, or audit authority changes.
</success_criteria>

<commit_policy>
After focused and package verification, create one atomic commit: `fix(31.1-11): serialize bootstrap and bound probe work`. Do not push. Plan 20 ends with a HARD EXTERNAL/MANUAL POST-CLOSEOUT GATE handoff; no GSD executor, verifier, tracker, hook, or autonomous orchestrator step owns final Stage B or push.
</commit_policy>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-11-SUMMARY.md` with repository-local evidence, exact commands, commit hash, and residual risk.
</output>
