---
phase: 31.1-readiness-fail-closed
plan: "20"
type: execute
wave: 3
depends_on: ["31.1-11", "31.1-12", "31.1-13", "31.1-14", "31.1-15", "31.1-16", "31.1-17", "31.1-18", "31.1-19", "31.1-21", "31.1-22"]
files_modified:
  - .planning/phases/31.1-readiness-fail-closed/31.1-REVIEW.md
  - .planning/phases/31.1-readiness-fail-closed/31.1-SECURITY.md
  - .planning/phases/31.1-readiness-fail-closed/31.1-VALIDATION.md
  - scripts/verify-phase-31-1-audit.py
  - scripts/verify-phase-31-1-audit-fixtures.sh
autonomous: true
gap_closure: true
requirements: [RELS-01]
must_haves:
  truths:
    - "All focused gap selectors, full Go regression, frontend test/build, readiness fixture/deployment gates, and patch checks pass with zero skips before closeout (D-RDY-27)."
    - "The main checkout's direct OpenAPI command fails with exactly the two deferred readiness Envelope rows, while a temporary clean checkout with only those two references applied passes the complete `bash scripts/check.sh docs` sequence; no later docs failure is hidden by OpenAPI's early exit."
    - "SECURITY uses one strict parsable row schema for the exact unique high-threat set from continuous Plans 11-22; every row has a source boundary, command, observed result, and commit identity, while all T-31.1-20 rows remain explicitly unobserved/pending until the external gate."
    - "VALIDATION derives normalized task IDs and selector/assertion IDs from continuous Plans 11-22 and compares its designated table bidirectionally with exact row/pair counts, uniqueness, and mutation rejection; stale 10-plan/28-task and 10-gap-plan/22-task maps plus appended token lists fail."
    - "Ordinary execute-plan creates only pre-closeout proof and committed audit artifacts. Standard GSD then creates the Plan 20 SUMMARY, canonical VERIFICATION, phase.complete tracking, and any configured post-completion commits before returning."
    - "Final tracking-HEAD Stage B and push are a HARD EXTERNAL/MANUAL POST-CLOSEOUT GATE, not a task, checkpoint, hook, or autonomous GSD action. Phase/push eligibility remains blocked until the operator runs the exact handoff command as the last repository action (D-RDY-28)."
  artifacts:
    - path: ".planning/phases/31.1-readiness-fail-closed/31.1-REVIEW.md"
      provides: "Fresh code review bound to the audited implementation HEAD/tree with canonical gap dispositions"
      contains: "audited_implementation_head"
    - path: ".planning/phases/31.1-readiness-fail-closed/31.1-SECURITY.md"
      provides: "Strict pre-closeout security audit rows plus explicit pending post-closeout rows"
      contains: "post_closeout_high_threats_pending"
    - path: ".planning/phases/31.1-readiness-fail-closed/31.1-VALIDATION.md"
      provides: "Exact 26-row Plans 11-22 gap-task map with bidirectional selector/assertion coverage"
      contains: "mapped_gap_tasks: 26"
    - path: "scripts/verify-phase-31-1-audit.py"
      provides: "Structured SECURITY and VALIDATION parser enforcing exact sets, schema, identity, and table-only mappings"
      contains: "pending_external_gate"
    - path: "scripts/verify-phase-31-1-audit-fixtures.sh"
      provides: "Mutation matrix proving missing, extra, duplicate, blank, stale, pending-map, and appended-token inputs are rejected"
      contains: "mutation"
    - path: "/var/tmp/oblivious-31-1-preclose-stage-b-<head>/harness-result.json"
      provides: "External exact audited-implementation-HEAD repository-local E2 pre-closeout result"
      contains: "repository-local"
  key_links:
    - from: "scripts/verify-phase-31-1-audit.py"
      to: "Plans 11-22, SECURITY, and VALIDATION"
      via: "structured table parsing and bidirectional set comparison make stale/duplicate/vacuous audit text fail"
      pattern: "31.1-20"
    - from: "temporary two-reference checkout"
      to: "scripts/check.sh docs"
      via: "the exact deferred diff removes only the known early failure so every later docs check must execute and pass"
      pattern: "Envelope"
    - from: "HARD EXTERNAL/MANUAL POST-CLOSEOUT GATE"
      to: "final tracking HEAD and origin/commercial-target-release-runner"
      via: "operator-only clean detached Stage B, JSON assertions, unchanged source identity, exact SHA push, and remote equality"
      pattern: "expected-head"
---

<objective>
Re-audit the Phase 31.1 gap implementation, prove the complete docs chain behind the deferred OpenAPI rows, and produce strict identity-bound security/Nyquist artifacts before handing the final tracking-HEAD Stage B and push to an explicit manual boundary.

Purpose: Standard GSD must write SUMMARY, canonical verification, phase tracking, and optional closeout artifacts after Plan 20. Therefore no autonomous plan or invented hook can certify or push the eventual final HEAD.
Output: Complete read-only gates, temporary-checkout full docs proof, strict audit validator plus mutation fixtures, fresh REVIEW/SECURITY/VALIDATION, pre-closeout E2 proof, and an exact operator command that remains blocked until all standard repository writers finish.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-VERIFICATION.md
@.planning/phases/31.1-readiness-fail-closed/31.1-REVIEW.md
@.planning/phases/31.1-readiness-fail-closed/31.1-SECURITY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-VALIDATION.md
@.planning/phases/31.1-readiness-fail-closed/31.1-UAT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-11-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-12-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-13-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-14-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-15-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-16-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-17-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-18-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-19-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-21-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-22-SUMMARY.md

<constraints>
- Treat `31.1-UAT.md` 35/35 as historical conversational acceptance, not production-composition proof.
- The prior `31.1-SECURITY.md threats_open: 0` and original `31.1-VALIDATION.md` 10-plan/28-task pending map are superseded inputs, not closure evidence.
- The only main-checkout docs exception is exactly `GET /api/v1/app/readiness/capabilities 200` and `GET /api/v1/admin/readiness 200` missing `#/components/schemas/Envelope`; ownership remains Phase 31.2 RELS-02. Full later docs checks must still pass in the temporary two-reference checkout.
- CR-06 strict Relay Files and CR-09 batch provider identity remain Phase 34; WR-02 MCP Connect semantics remains Phase 35. Do not implement or count them as Phase 31.1 gaps.
- Preserve unrelated dirty `.planning/config.json`, `.planning/STATE.md`, `.planning/intel/`, user changes, secrets, and external target state. Temporary proof uses fresh detached worktrees and removes only those worktrees.
- No E3/E4, target/live, profile parity, signed release, or commercial-readiness claim.
- Plan 20 may create only the two audit verifier files and update REVIEW/SECURITY/VALIDATION. Any product/test/manifest/deployment/OpenAPI defect stops and routes to its owner in Plans 11-19 or 21-22, or to Phase 31.2 for the deferred Envelope rows.
- Execute-plan must not run final Stage B or push. After execute-plan, standard GSD owns SUMMARY, canonical VERIFICATION, `phase.complete`, and configured post-completion writes. The external gate is unavailable until that workflow fully returns.
- T-31.1-20-* evidence is post-closeout-only. Before the external gate, those SECURITY rows must say `post_closeout_external`, `NOT_OBSERVED_PRE_CLOSEOUT`, `PENDING_FINAL_TRACKING_HEAD`, and `pending_external_gate`; they cannot be marked pass/closed/observed in a committed artifact.
</constraints>

<interfaces>
`scripts/verify-phase-31-1-audit.py` accepts `--phase-dir`, `--expected-implementation-head`, `--security`, and `--validation`. It parses markdown tables structurally, derives expected high threats and plan task/evidence mappings from every continuous `31.1-11-PLAN.md` through `31.1-22-PLAN.md`, and exits nonzero on any schema or set mismatch. `scripts/verify-phase-31-1-audit-fixtures.sh` runs the valid baseline plus isolated negative mutations.

Stage B remains `bash scripts/verify-readiness-contract.sh stage-b --output-dir <outside-git-dir> --expected-head <40-char-head>`. Task 1 may run it only at the audited implementation/pre-closeout HEAD. The final invocation is solely the manual command after `gsd-execute-phase` fully returns; it is deliberately outside `<tasks>` and all GSD workflow extension points.
</interfaces>
</context>

## Artifacts this phase produces

- Complete focused/full repository-local gate transcript.
- Direct-main exact two-row OpenAPI deferral proof and temporary two-reference full docs pass.
- Strict SECURITY/VALIDATION schemas with mutation-tested structured validation.
- External pre-closeout Stage B bundle with exact JSON assertions.
- A hard operator handoff after ordinary GSD closeout; no autonomous final proof or push claim.

<tasks>

<task type="auto">
  <name>Task 1: Run complete read-only gates and pre-closeout identity proof</name>
  <files>None (read-only verification plus temporary external worktrees; defects route to their owners)</files>
  <read_first>
    - scripts/run-go-tests-matched.sh
    - scripts/verify-readiness-contract.sh
    - scripts/verify-readiness-contract-fixtures.sh
    - scripts/verify-readiness-deployment-contract.sh
    - scripts/verify-openapi-contract.sh
    - scripts/check.sh
    - docs/api/openapi.yaml
    - .planning/phases/31.1-readiness-fail-closed/31.1-VERIFICATION.md
  </read_first>
  <assertion_ids>ASSERT-OPENAPI-MAIN-EXACT-TWO, ASSERT-DOCS-DEFERRED-TWO-REF-FULL-PASS, ASSERT-PRECLOSE-STAGE-B-IDENTITY</assertion_ids>
  <action>Capture `audited_head=$(git rev-parse HEAD^{commit})` and `audited_tree=$(git rev-parse HEAD^{tree})`, then run every anchored selector/assertion from implementation Plans 11-19 and 21-22, full `go test ./... -count=1`, frontend test/build and focused agents API test, readiness fixture/deployment/Kubernetes-runner gates, and `git diff --check`. This task is diagnostic outside temporary worktrees: any failure stops without repair and reports the owning plan and exact command.

Run `bash scripts/verify-openapi-contract.sh` directly on the main checkout with stdout/stderr and numeric status recorded under `/var/tmp/oblivious-31-1-openapi-main-&lt;audited_head&gt;.*`. Require status 1, one Envelope diagnostic header, and exactly the two allowed method/path/status rows in the authored order, with no third diagnostic. This direct command, not `check.sh docs`, is the authoritative main-checkout deferral assertion.

Create a clean detached worktree at `audited_head`. Apply an exact patch to that temporary checkout only which changes the schema of those two named readiness GET 200 responses from `type: object` to `$ref: "#/components/schemas/Envelope"`. Before running checks, require the temporary diff has exactly one path (`docs/api/openapi.yaml`), exactly two additions and two deletions, exactly two added Envelope references, exactly two removed object-type rows, and hunks rooted in the two named operation blocks; reject any other content, path, or whitespace change. Save `git diff --unified=12` to `/var/tmp/oblivious-31-1-docs-temp-&lt;audited_head&gt;.diff`, `git diff --name-only` to the matching `.paths`, and `git diff --numstat` to the matching `.numstat`. Run the full `bash scripts/check.sh docs` there, save combined output and numeric status to matching `.log`/`.status`, and require exit 0, thereby exercising every docs check after OpenAPI. Remove the worktree and reassert that main `docs/api/openapi.yaml`, main HEAD, and main status snapshot are unchanged.

Create a second clean detached worktree at `audited_head`, run Stage B into `/var/tmp/oblivious-31-1-preclose-stage-b-&lt;audited_head&gt;`, and require exact JSON fields: releaseCommit, pass result, repository-local class, applied-and-validated migration state, empty skips, and the exact E2-only claim. Require detached and source HEAD remain `audited_head`, remove only the temporary worktree, and do not push.</action>
  <verify>
    <automated>bash -c 'set -euo pipefail; head=$(git rev-parse HEAD^{commit}); main=/var/tmp/oblivious-31-1-openapi-main-${head}; docs=/var/tmp/oblivious-31-1-docs-temp-${head}; test "$(cat ${main}.status)" -eq 1; test "$(grep -Fc "[openapi-contract] /api JSON responses must reference #/components/schemas/Envelope:" "${main}.log")" -eq 1; mapfile -t rows &lt; &lt;(sed -n "s/^  - //p" "${main}.log"); test "${#rows[@]}" -eq 2; test "${rows[0]}" = "GET /api/v1/app/readiness/capabilities 200"; test "${rows[1]}" = "GET /api/v1/admin/readiness 200"; test "$(cat ${docs}.status)" -eq 0; test -f "${docs}.diff"; test "$(cat "${docs}.paths")" = "docs/api/openapi.yaml"; test "$(awk '\''$1 ~ /^[0-9]+$/ {a+=$1; d+=$2} END {print a ":" d}'\'' "${docs}.numstat")" = "2:2"; test "$(grep -Fc '\''+                $ref: "#/components/schemas/Envelope"'\'' "${docs}.diff")" -eq 2; test "$(grep -Fc '\''-                type: object'\'' "${docs}.diff")" -eq 2; test "$(grep -Fc "  /api/v1/app/readiness/capabilities:" "${docs}.diff")" -eq 1; test "$(grep -Fc "  /api/v1/admin/readiness:" "${docs}.diff")" -eq 1; test "$(grep -Ec '\''^[+-][^+-]'\'' "${docs}.diff")" -eq 4; jq -e --arg head "$head" '\''.releaseCommit == $head and .result == "pass" and .evidenceClass == "repository-local" and .migrationState == "applied_and_validated" and .skippedChecks == [] and .claim == "repository-local E2 only; no E3/E4, target, or commercial release claim"'\'' "/var/tmp/oblivious-31-1-preclose-stage-b-${head}/harness-result.json" &gt;/dev/null'</automated>
  </verify>
  <acceptance_criteria>
    - Every focused selector and full Go/frontend/readiness gate passes with zero skips.
    - Direct main OpenAPI output is exactly the two Phase 31.2 rows and no other error.
    - A clean temporary checkout differing only by those two exact Envelope references passes the entire docs aggregate, proving no hidden later docs failure.
    - Pre-closeout Stage B binds the exact clean implementation HEAD and repository-local E2 claim; main files/status are unchanged and nothing is pushed.
  </acceptance_criteria>
  <done>The implementation HEAD has complete repository-local proof, including the docs checks that the main OpenAPI failure otherwise masks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build strict security and Nyquist audit validation</name>
  <files>.planning/phases/31.1-readiness-fail-closed/31.1-REVIEW.md, .planning/phases/31.1-readiness-fail-closed/31.1-SECURITY.md, .planning/phases/31.1-readiness-fail-closed/31.1-VALIDATION.md, scripts/verify-phase-31-1-audit.py, scripts/verify-phase-31-1-audit-fixtures.sh</files>
  <read_first>
    - .planning/phases/31.1-readiness-fail-closed/31.1-REVIEW.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-SECURITY.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-VALIDATION.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-11-PLAN.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-12-PLAN.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-13-PLAN.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-14-PLAN.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-15-PLAN.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-16-PLAN.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-17-PLAN.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-18-PLAN.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-19-PLAN.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-20-PLAN.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-21-PLAN.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-22-PLAN.md
  </read_first>
  <assertion_ids>ASSERT-SECURITY-SCHEMA-MUTATIONS, ASSERT-NYQUIST-BIDIRECTIONAL-MUTATIONS, ASSERT-AUDIT-IDENTITY</assertion_ids>
  <behavior>
    - The valid SECURITY/VALIDATION pair passes against the exact audited implementation HEAD and continuous Plans 11-22.
    - SECURITY mutations for missing, extra/stale, duplicate, blank boundary, blank command, blank result, wrong commit, or prematurely observed T-31.1-20 row all fail.
    - VALIDATION mutations for missing/duplicate task, missing/extra evidence pair, old 10-plan/28-task/pending metadata, duplicate evidence in a cell, or an expected token appended outside the designated table all fail.
  </behavior>
  <action>Write `verify-phase-31-1-audit.py` with a real markdown table parser, not grep. Derive the expected high-threat IDs by parsing the STRIDE tables of exactly continuous Plans 11-22 and selecting severity `high`; reject malformed plan rows, duplicate expected IDs, absent plans, or plan numbers outside the continuous 11..22 set. Parse SECURITY only from `## High-Threat Audit Rows` using the exact header `Threat ID | Severity | Evidence phase | Source boundary | Evidence command | Observed result | Commit identity | Status`. Compare IDs bidirectionally and require one unique row per expected threat, no extras, nonblank/non-placeholder boundary/command/result/identity, and no duplicate cells. The command cell must contain the actual executable selector, mutation command, or JSON assertion rather than a label/TBD; encode literal shell pipes as `&#124;` in markdown and decode them before validation. Plans 11-19 and 21-22 rows require `pre_closeout`, exact observed result `PASS`, the exact 40-character audited implementation HEAD, and `mitigated`. Every T-31.1-20 row instead requires `post_closeout_external`, `NOT_OBSERVED_PRE_CLOSEOUT`, `PENDING_FINAL_TRACKING_HEAD`, and `pending_external_gate`. SECURITY frontmatter must require `high_threat_rows: 42`, `precloseout_high_threats_open: 0`, `post_closeout_high_threats_pending: 6`, `post_closeout_gate_status: pending_external`, and `release_eligible: false`; all counts are re-derived and equality-checked rather than trusted.

Parse each Plan 11-22 task structurally and normalize task ordinals as `31.1-NN-TT`. For each task, derive evidence IDs only from `Test[A-Za-z0-9_]+` tokens inside its automated verification field and comma-separated uppercase IDs inside its assertion-ID field; tokens in action/read-first/prose do not count. Parse VALIDATION only from `## Gap Task Validation Map` with exact columns `Task ID | Plan | Task ordinal | Selector/assertion IDs | Automated result | Audited commit`. Require exactly 12 continuous source plans, 26 unique task rows, 43 unique task/evidence pairs, 40 globally distinct evidence IDs, no duplicate ID within a cell, exact audited commit/result on each row, and bidirectional equality of task IDs and `(task,evidence)` pairs. Reject any normalized gap task ID or expected evidence ID outside that designated table so appended token lists cannot satisfy the check. Reject legacy `target_plans: 10`, `28 executable tasks`, `28/28`, `target_plans: 20`, `gap_plans: 10`, `mapped_gap_tasks: 22`, `mapped_gap_evidence_pairs: 35`, `unique_gap_evidence_ids: 32`, `wave_0_complete: false`, `pending implementation`, and any legacy Plans 01-10 task row; require `target_plans: 22`, `gap_plans: 12`, `mapped_gap_tasks: 26`, `mapped_gap_evidence_pairs: 43`, `unique_gap_evidence_ids: 40`, and `wave_0_complete: true`.

Write the fixture shell script to copy the valid plans/audits to disposable directories, run the positive case, apply each named mutation independently, require every mutation to fail, and report exact case counts. Then rebuild REVIEW/SECURITY/VALIDATION against Task 1's `audited_head`/`audited_tree`, run the validator and fixtures, and preserve the four deferrals plus E1/E2 ceiling. Do not edit VERIFICATION, SUMMARY, ROADMAP, REQUIREMENTS, or STATE.</action>
  <verify>
    <automated>bash scripts/verify-phase-31-1-audit-fixtures.sh &amp;&amp; bash -c 'set -euo pipefail; phase=.planning/phases/31.1-readiness-fail-closed; head=$(sed -n "s/^audited_implementation_head: //p" "$phase/31.1-SECURITY.md" | head -1); test "${#head}" -eq 40; python scripts/verify-phase-31-1-audit.py --phase-dir "$phase" --expected-implementation-head "$head" --security "$phase/31.1-SECURITY.md" --validation "$phase/31.1-VALIDATION.md"; git diff --check'</automated>
  </verify>
  <acceptance_criteria>
    - REVIEW and SECURITY name the exact Task 1 implementation HEAD/tree and fresh audit run identity.
    - SECURITY has exactly 42 unique high-threat rows and strict nonblank evidence fields; Plans 11-19 and 21-22 are observed at the audited commit while all six Plan 20 rows remain explicitly pending external observation.
    - VALIDATION has exactly 26 normalized task rows, 43 task/evidence pairs, and 40 distinct evidence IDs derived from Plans 11-22, with exact bidirectional equality and no out-of-table token escape.
    - Positive validation passes and every security/Nyquist mutation fails for the intended reason.
    - No canonical verification, tracking, SUMMARY, final Stage B, push, or product repair occurs inside this task.
  </acceptance_criteria>
  <done>Committed audit artifacts are exact, parsable, identity-bound, mutation-tested, and honest that the post-closeout gate has not yet been observed.</done>
</task>

</tasks>

## HARD EXTERNAL/MANUAL POST-CLOSEOUT GATE

This section is intentionally **not** a GSD task, checkpoint, hook, finalizer, or autonomous continuation. `execute-plan.md` ignores it. Execute the gap set with the supported command `$gsd-execute-phase 31.1 --gaps-only --no-transition`; `--no-transition` still performs standard verification/completion commits but prevents automatic Phase 31.2 work from invalidating the handoff. After Task 2, that standard workflow must first:

1. create and commit `31.1-20-SUMMARY.md`;
2. run the canonical verifier and commit `31.1-VERIFICATION.md` with exact `status: passed` for the repository-local Phase 31.1 implementation;
3. run `phase.complete 31.1` and commit ROADMAP/REQUIREMENTS/STATE;
4. finish every configured post-completion writer and return control to the operator.

The execute-phase response must then say: **Phase 31.1 repository implementation verified, but final tracking-HEAD Stage B and push remain BLOCKED. Run the exact operator command below as the last repository action. Do not start Phase 31.2 or run any writer first.** No autonomous completion/push claim is allowed.

From the repository root, the operator runs exactly:

```bash
bash -c '
set -euo pipefail
phase=.planning/phases/31.1-readiness-fail-closed
branch=commercial-target-release-runner
summary=$phase/31.1-20-SUMMARY.md
verification=$phase/31.1-VERIFICATION.md
security=$phase/31.1-SECURITY.md

git ls-files --error-unmatch "$summary" >/dev/null
grep -Fxq "status: passed" "$verification"
grep -Eq "^- \[x\] \*\*Phase 31\.1:" .planning/ROADMAP.md
grep -Eq "^\| RELS-01 \| Phase 31, Phase 31\.1 \| (Complete|Completed) \|$" .planning/REQUIREMENTS.md
grep -Eq "^current_phase: ['\''\"]?31\.2['\''\"]?$" .planning/STATE.md
grep -Fxq "release_eligible: false" "$security"
grep -Fxq "post_closeout_gate_status: pending_external" "$security"

audited_head=$(sed -n "s/^audited_implementation_head: //p" "$security" | head -1)
final_head=$(git rev-parse HEAD^{commit})
test "${#audited_head}" -eq 40
test "${#final_head}" -eq 40
git merge-base --is-ancestor "$audited_head" "$final_head"

allowed=$(mktemp /var/tmp/oblivious-31-1-allowed.XXXXXX)
actual=$(mktemp /var/tmp/oblivious-31-1-actual.XXXXXX)
before=$(mktemp /var/tmp/oblivious-31-1-status-before.XXXXXX)
after=$(mktemp /var/tmp/oblivious-31-1-status-after.XXXXXX)
printf "%s\n" \
  .planning/ROADMAP.md \
  .planning/REQUIREMENTS.md \
  .planning/STATE.md \
  .planning/PROJECT.md \
  "$phase/31.1-20-SUMMARY.md" \
  "$phase/31.1-REVIEW.md" \
  "$phase/31.1-SECURITY.md" \
  "$phase/31.1-VALIDATION.md" \
  "$phase/31.1-VERIFICATION.md" \
  scripts/verify-phase-31-1-audit-fixtures.sh \
  scripts/verify-phase-31-1-audit.py | LC_ALL=C sort -u >"$allowed"
git diff --name-only "$audited_head..$final_head" | LC_ALL=C sort -u >"$actual"
test ! -s <(comm -23 "$actual" "$allowed")
git status --porcelain=v1 -uall >"$before"

wt=$(mktemp -d /var/tmp/oblivious-31-1-final-wt.XXXXXX)
rmdir "$wt"
cleanup() {
  git worktree remove --force "$wt" >/dev/null 2>&1 || true
  rm -f "$allowed" "$actual" "$before" "$after"
}
trap cleanup EXIT
git worktree add --detach "$wt" "$final_head" >/dev/null
test -z "$(git -C "$wt" status --porcelain=v1 -uall)"
test "$(git -C "$wt" rev-parse HEAD^{commit})" = "$final_head"

out=/var/tmp/oblivious-31-1-final-stage-b-$final_head
rm -rf "$out"
(cd "$wt" && bash scripts/verify-readiness-contract.sh stage-b --output-dir "$out" --expected-head "$final_head")
jq -e --arg head "$final_head" '\''
  .releaseCommit == $head and
  .result == "pass" and
  .evidenceClass == "repository-local" and
  .migrationState == "applied_and_validated" and
  .skippedChecks == [] and
  .claim == "repository-local E2 only; no E3/E4, target, or commercial release claim"
'\'' "$out/harness-result.json" >/dev/null
test "$(git rev-parse HEAD^{commit})" = "$final_head"
test "$(git -C "$wt" rev-parse HEAD^{commit})" = "$final_head"
git status --porcelain=v1 -uall >"$after"
cmp -s "$before" "$after"

git push origin "$final_head:refs/heads/$branch"
remote_head=$(git ls-remote origin "refs/heads/$branch" | awk "NR == 1 {print \$1}")
test "$remote_head" = "$final_head"

evidence=/var/tmp/oblivious-31-1-postclose-security-$final_head.tsv
printf "Threat ID\tSeverity\tEvidence phase\tSource boundary\tEvidence command\tObserved result\tCommit identity\tStatus\n" >"$evidence"
for id in T-31.1-20-01 T-31.1-20-02 T-31.1-20-03 T-31.1-20-04 T-31.1-20-05 T-31.1-20-06; do
  printf "%s\thigh\tpost_closeout_external\tstandard_closeout_to_stage_b_and_origin\tmanual_operator_gate\tstage_b_pass_and_remote_sha_equal\t%s\tobserved\n" "$id" "$final_head" >>"$evidence"
done
printf "FINAL_HEAD=%s\nSTAGE_B=%s\nPOST_CLOSEOUT_SECURITY=%s\nREMOTE_HEAD=%s\n" "$final_head" "$out" "$evidence" "$remote_head"
'
```

Any failed precondition or changed HEAD/status stops without push. After a successful push, do not edit, format, generate, commit, amend, merge, rebase, or start Phase 31.2 in this checkout until the final SHA, Stage B directory, external security TSV, and remote equality have been reported. Because the evidence is intentionally outside git and no later repository write is allowed, committed SECURITY remains an honest pre-gate record with Plan 20 rows pending.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main OpenAPI failure -> later docs aggregate | `set -e` can conceal every docs check after the first deferred row. |
| Plans/audit prose -> closure claim | Token greps can accept stale, duplicated, extra, or appended evidence. |
| Standard GSD commits -> final Stage B identity | SUMMARY/verifier/tracking/post-completion writes necessarily change HEAD after execute-plan. |
| Local repository proof -> external readiness claim | E1/E2 evidence must not be inflated into target/live or commercial claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-20-01 | Spoofing | Security sign-off | high | mitigate | Parse the exact plan-derived threat set and reject missing/extra/duplicate/stale/blank rows with mutation fixtures. |
| T-31.1-20-02 | Tampering | Final release tuple | high | mitigate | Keep committed evidence pending; only the operator proves the fixed final tracking HEAD after every standard writer finishes. |
| T-31.1-20-03 | Repudiation | Docs/check result | high | mitigate | Assert direct-main exact rows and full temporary-checkout docs success after only two explicit Envelope references. |
| T-31.1-20-04 | Elevation of Privilege | Push eligibility | high | mitigate | External command pushes only the exact zero-skip Stage B SHA and asserts remote equality; autonomous workflows cannot push. |
| T-31.1-20-05 | Information Disclosure | Evidence bundles | high | mitigate | Keep raw Stage B and post-closeout evidence outside git and expose only sanitized identity/status fields. |
| T-31.1-20-06 | Tampering | Nyquist/execute scope | high | mitigate | Bidirectional table parsing plus mutation tests reject stale maps/token appendices; implementation defects route to owners. |
</threat_model>

<verification>
- Plans 11-19 and 21-22 selectors/assertions, full Go/frontend/readiness gates, and diff checks pass.
- Direct main OpenAPI has exactly two deferred rows; a clean temporary checkout with only those two references passes all of `check.sh docs`.
- SECURITY's exact 42 high-threat rows and VALIDATION's exact 26-task/43-pair/40-ID map pass the structured validator and every negative mutation is rejected.
- Ordinary execute-plan ends after pre-closeout proof and audit commits; it does not run final Stage B or push.
- The standard workflow owns SUMMARY, canonical VERIFICATION, `phase.complete`, and configured closeout commits, then returns a blocking manual handoff.
- Only the operator command may create final Stage B and push, and it proves unchanged HEAD/status plus remote SHA equality.
</verification>

<success_criteria>
- Canonical verification can move from `gaps_found` to repository-local `passed` for actionable Phase 31.1 implementation truths without pretending the later manual release identity has already been observed.
- RELS-01 implementation/tracking closes and Phase 31.2 remains next for RELS-02, while phase/push eligibility stays blocked until the exact external command succeeds.
- Final evidence is non-vacuous repository-local E1/E2 and no later repository write invalidates the pushed tuple.
</success_criteria>

<commit_policy>
Task 2 commits only REVIEW, SECURITY, VALIDATION, and the two phase-audit validator files. The executor then creates/commits `31.1-20-SUMMARY.md`; canonical verifier, phase.complete, and configured standard closeout steps own subsequent commits. Run the gap set via `$gsd-execute-phase 31.1 --gaps-only --no-transition` so the supported workflow returns before Phase 31.2 starts. No GSD component runs final Stage B or push. The operator command is external/manual and runs only after the standard workflow fully returns.
</commit_policy>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-20-SUMMARY.md` through the normal execute-plan output step. Record the audited implementation HEAD/tree, direct-main exact rows, temporary-checkout full docs pass, pre-closeout Stage B tuple, validator/mutation results, and the exact HARD EXTERNAL/MANUAL POST-CLOSEOUT GATE handoff. The next execution command is `$gsd-execute-phase 31.1 --gaps-only --no-transition`. The SUMMARY must say final Stage B/push are pending and must not claim canonical verification, phase.complete, final Stage B, remote equality, or phase/push eligibility already occurred.
</output>
