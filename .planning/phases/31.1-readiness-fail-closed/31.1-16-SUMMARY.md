---
phase: 31.1-readiness-fail-closed
plan: "16"
subsystem: http-readiness
tags: [readiness, fail-closed, http, freshness, security, tdd]

# Dependency graph
requires:
  - phase: 31.1-readiness-fail-closed
    provides: "Evaluator-owned nanosecond freshness, ReadinessManager last-known diagnostics, and Admin/App readiness projections"
provides:
  - "Evaluation-error-first Admin and App handlers that return the existing sanitized readiness error before projecting rows"
  - "Non-vacuous stale, unavailable, and identity-mismatch leakage coverage with enabled last-known values"
  - "Real evaluator coverage for the exact validUntil and validUntil + 1ns boundary through both endpoints"
affects: [31.1-20 phase closeout, 31.2 app projection integration, RELS-01]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "HTTP readiness projections consume Evaluation.ErrorCode before sorting, copying identity, or rendering capability state"
    - "Endpoint boundary tests derive freshness only through releasecontract.Evaluator and retain last-known rows solely to prove handler rejection"

key-files:
  created: []
  modified:
    - src/server/internal/http/readiness_handler.go
    - src/server/internal/http/readiness_handler_test.go

key-decisions:
  - "Admin and App reuse writeReadinessHTTPError for every evaluation-level error so no stale inventory or diagnostic detail crosses the response boundary."
  - "Handlers do not compare time or add a grace period; the evaluator remains the only source of the exact freshness verdict."
  - "No frontend caller, route, copy, or visual implementation is added because the approved UI scope is a backend response-state contract."

patterns-established:
  - "Last-known capability values remain internal diagnostics whenever Evaluation.ErrorCode is non-empty."
  - "Freshness endpoint tests use the checked-in authored contract/profile and identical observations at the exact nanosecond boundary."

requirements-completed: []

coverage:
  - id: D1
    description: "Admin and App reject stale, unavailable, and identity-mismatched evaluations before exposing enabled rows, identity fields, dependencies, raw endpoints, or secrets."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/http/readiness_handler_test.go#TestReadinessProjectionEvaluationErrorContract"
        status: pass
      - kind: integration
        ref: "bash scripts/run-go-tests-matched.sh ./internal/http '^TestReadinessProjectionEvaluationErrorContract$'"
        status: pass
      - kind: integration
        ref: "go test -race ./internal/http -run '^(TestReadinessProjectionEvaluationErrorContract|TestReadinessProjectionFreshnessBoundaryContract)$' -count=1"
        status: pass
    human_judgment: false
  - id: D2
    description: "The real evaluator keeps now == validUntil fresh and returns readiness_stale at validUntil + 1ns; both endpoints fail the stale projection closed."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/http/readiness_handler_test.go#TestReadinessProjectionFreshnessBoundaryContract"
        status: pass
      - kind: integration
        ref: "bash scripts/run-go-tests-matched.sh ./internal/http '^TestReadinessProjectionFreshnessBoundaryContract$'"
        status: pass
      - kind: integration
        ref: "go test ./internal/http ./internal/releasecontract -count=1"
        status: pass
    human_judgment: false

# Metrics
duration: 3 min
completed: 2026-07-19
status: complete
---

# Phase 31.1 Plan 16: Fail-Closed Readiness Projections Summary

**Admin and App projections now reject evaluation-level failures before copying last-known readiness data, while exact evaluator-owned nanosecond tests prove the fresh-to-stale boundary.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-07-19T11:28:13Z
- **Completed:** 2026-07-19T11:31:08Z
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments

- Admin and App check `evaluation.ErrorCode` immediately after `ReadinessManager.Evaluate` and route failures through the existing sanitized `writeReadinessHTTPError` response before sorting, identity projection, digest generation, or row construction.
- Table-driven stale, unavailable, and build-identity-mismatch cases seed an enabled last-known row plus a raw internal endpoint and secret; both endpoints return stable 503 codes without leaking any seeded value.
- The checked-in contract and authored monolith profile produce identical enabled observations at `now == validUntil` and `validUntil + 1ns`; the real evaluator accepts the former and emits `readiness_stale` for the latter, and both endpoints reject the stale last-known projection.
- Existing fresh projection behavior and Admin/App inventory separation remain covered by the full HTTP package regression.
- No frontend implementation file, production caller, route, navigation item, copy, or visual state changed. The approved backend-only UI scope required no screenshot or visual-regression artifact.

## Task Commits

The plan-required single implementation commit contains both TDD task outcomes:

1. **Tasks 1-2: Fail evaluation-level errors closed and prove exact expiry** - `0114832` (`fix`)

## TDD Evidence

- **RED evaluation errors:** `TestReadinessProjectionEvaluationErrorContract` failed for all six Admin/App cases because each returned HTTP 200 and exposed the seeded enabled row. Admin also exposed the raw dependency endpoint and secret reason; App exposed the raw identity/profile values and enabled row.
- **RED expiry boundary:** `TestReadinessProjectionFreshnessBoundaryContract` first proved the exact `validUntil` result fresh, then proved the real evaluator returned `readiness_stale` at `validUntil + 1ns`; both stale endpoint cases still returned HTTP 200 with enabled rows before the handler fix.
- **GREEN:** After the two minimal error-first branches, both exact anchored selectors, focused race, full HTTP/releasecontract packages, scoped vet, and diff hygiene passed.
- No separate RED commit was created because the plan commit policy requires one atomic `fix(31.1-16)` implementation commit; the observed failing output is recorded above.

## Files Created/Modified

- `src/server/internal/http/readiness_handler.go` - Rejects evaluation-level errors before Admin or App projection work.
- `src/server/internal/http/readiness_handler_test.go` - Adds sanitized leakage cases, authored authority/observation fixtures, and exact evaluator boundary endpoint coverage.

## Decisions Made

- Reuse the established readiness error envelope instead of forcing disabled rows. An error response cannot be mistaken for a current inventory and exposes only the stable readiness code.
- Preserve the manager/evaluator ownership split: the manager may retain last-known diagnostics and set `ErrorCode`, but HTTP projections never interpret timestamps or freshness locally.
- Retain read-only `capabilityId` and all mutation behavior unchanged; frontend integration remains Phase 31.2.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The first pre-commit base assertion used an incorrectly expanded full SHA for the supplied short base `a3501b5`; it failed before staging. The live HEAD was verified as `a3501b5708d8e299f7b567feb7a47350cf0f8dcb`, the short-base assertion passed, and only the two planned backend files were staged.

## User Setup Required

None - no external service or configuration changes are required.

## Known Stubs

None.

## Threat Surface

- No endpoint, auth path, network call, file access, schema, or frontend surface was added.
- The existing server-error trust boundary is narrowed: raw last-known identity, endpoint, dependency, reason, and secret-like test values no longer cross evaluation-level error responses.

## Next Phase Readiness

- Plan 31.1-16 closes CR-08 with repository-local E1 automated evidence; Plan 31.1-17 is next.
- `RELS-01` remains phase-level pending until the remaining gap plans, full phase verification, exact clean-HEAD Stage B, and hard manual push gate complete.
- No frontend screenshot was required, and no provider-live, target/live, payment, Kubernetes, migration, E3/E4, or commercial-readiness claim is made.

## Test Evidence

- `bash scripts/run-go-tests-matched.sh ./internal/http '^TestReadinessProjectionEvaluationErrorContract$'` passed and matched one concrete anchored test.
- `bash scripts/run-go-tests-matched.sh ./internal/http '^TestReadinessProjectionFreshnessBoundaryContract$'` passed and matched one concrete anchored test.
- `cd src/server && go test ./internal/http ./internal/releasecontract -count=1` passed.
- `cd src/server && go test -race ./internal/http -run '^(TestReadinessProjectionEvaluationErrorContract|TestReadinessProjectionFreshnessBoundaryContract)$' -count=1` passed.
- `cd src/server && go vet ./internal/http ./internal/releasecontract` passed.
- `git diff --check` passed before the implementation commit.

---
*Phase: 31.1-readiness-fail-closed*
*Plan: 16*
*Completed: 2026-07-19*

## Self-Check: PASSED

- SUMMARY exists at the declared output path and implementation commit `0114832` exists with no deletions.
- Both anchored selectors, full HTTP/releasecontract packages, focused race, scoped vet, and diff check pass.
- Coverage classifies both deliverables as automated passing evidence.
- No frontend production file changed and no screenshot was required by the approved UI scope fence.
- Protected user changes remain unstaged; `RELS-01` remains phase-level pending.
