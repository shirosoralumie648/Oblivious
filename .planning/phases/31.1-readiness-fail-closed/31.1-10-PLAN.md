---
phase: 31.1-readiness-fail-closed
plan: "10"
type: execute
wave: 3
depends_on: ["31.1-01", "31.1-04", "31.1-06"]
files_modified:
  - src/server/internal/http/agent_web_search.go
  - src/server/internal/http/agent_web_search_test.go
  - src/server/internal/mcp/web_search_provider.go
  - src/server/internal/mcp/web_search_provider_test.go
  - src/server/internal/mcp/builtin.go
  - src/server/internal/agent/executor.go
  - src/server/internal/agent/executor_test.go
  - src/server/internal/agent/service.go
  - src/web/src/features/agents/agentsApi.ts
autonomous: true
requirements: [RELS-01]
must_haves:
  truths:
    - "Direct Tavily、configured Chain 与 builtin web-search package constructors 基于 Plan 01 RuntimeAuthorities 完成独立 package tests；它们不写 server.go、不依赖尚未完成的 BuildRuntime composition，也不本地重读 contract/profile、硬编码 capability 或使用 package-global authority（D-RDY-11, D-RDY-14, D-RDY-20, D-RDY-21）。"
    - "真实 production agent.ToolExecutor，而不是未接入 production 的 agent/tools Registry，由 NewAuthorizedToolExecutor 在无副作用构造期 resolve/Register 五类稳定 typed descriptors 恰好一次；builtin、custom API HTTP、local Python process、Python sandbox 与 MCP 每个新 effect 紧前只使用同一 current RuntimeAuthorities/CatalogAuthorizer/Guard（D-RDY-11, D-RDY-14, D-RDY-16, D-RDY-21）。"
    - "Service runner、plan-step、direct ExecuteTool 与 workflow adapter 均复用 authority-required ToolExecutor；missing/zero/mismatched carrier、unknown/deleted/ambiguous/runtime mismatch、stale/blocked/unavailable 或 descriptor failure 在真实 HTTP/process/sandbox/MCP/builtin/provider 前拒绝且具名计数为零（D-RDY-20, D-RDY-21, D-RDY-22）。"
    - "Live `agent.ToolDefinition` response JSON 与 frontend `AgentToolDefinition` 携带 server-derived read-only `capabilityId`；create/update mutation 严格拒绝该字段，frontend normalization 保留 response identity 而 mutation serialization 显式省略它（D-RDY-20, D-RDY-21, D-RDY-22）。"
  artifacts:
    - path: "src/server/internal/http/agent_web_search.go"
      provides: "Monolith-only authority-preserving direct Tavily/configured Chain factory"
      contains: "buildAgentWebSearchProvider"
    - path: "src/server/internal/mcp/web_search_provider.go"
      provides: "Guarded direct Tavily transport using current catalog authority"
      contains: "NewTavilyWebSearchProvider"
    - path: "src/server/internal/mcp/builtin.go"
      provides: "Instance-scoped builtin web-search execution without production global authority"
      contains: "NewWebSearchTool"
    - path: "src/server/internal/agent/executor.go"
      provides: "Construction-time one-shot typed descriptor registration plus live builtin/custom HTTP/local Python/sandbox/MCP current guard before every effect"
      contains: "ToolRuntimeOptions"
    - path: "src/server/internal/agent/executor_test.go"
      provides: "Named live-dispatch current-generation and per-effect zero-call suite"
      contains: "TestLiveAgentToolExecutorReadinessContract"
    - path: "src/web/src/features/agents/agentsApi.ts"
      provides: "Exact AgentToolDefinition capabilityId response normalization and mutation omission"
      contains: "capabilityId"
  key_links:
    - from: "src/server/internal/http/agent_web_search.go"
      to: "src/server/internal/mcp/websearch/websearch.go"
      via: "package factory passes the same Plan 01-based runtime options consumed by Plan 06 Chain.Search; Plan 02 later owns BuildRuntime wiring"
      pattern: "NewProviderFromConfig"
    - from: "src/server/internal/agent/executor.go"
      to: "src/server/internal/releasecontract/catalog.go"
      via: "constructor resolves/registers stable typed descriptors once; live branches resolve only current subjects and guard immediately before concrete effects"
      pattern: "ResolveAndRequire|CapabilityBindings"
    - from: "src/server/internal/agent/service.go"
      to: "src/server/internal/agent/executor.go"
      via: "runner, plan-step and direct ExecuteTool delegate to one authority-required ToolExecutor instead of duplicating dispatch"
      pattern: "ToolExecutor"
---

<objective>
先独立闭合 committed monolith 的 web-search package constructors，再直接保护真实 Agent `ToolExecutor` 的 builtin/custom HTTP/local Python/sandbox/MCP dispatch，使每个新 effect 消费同一 current authority。

Purpose: Plan 06 的 Registry 不是 production consumer，不能替代 `agent/executor.go` 安全证明；本计划补齐真实 dispatcher，同时让 Plan 02 在后续 wave 独占 server.go composition。
Output: authority-preserving web-search factories、guarded direct transport、instance-scoped builtin、live ToolExecutor/Service wiring，以及 HTTP/process/sandbox/MCP/builtin/provider 具名 zero-call suites。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-RESEARCH.md
@.planning/phases/31.1-readiness-fail-closed/31.1-PATTERNS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-01-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-06-SUMMARY.md
@src/server/internal/http/agent_web_search.go
@src/server/internal/mcp/web_search_provider.go
@src/server/internal/mcp/builtin.go
@src/server/internal/agent/executor.go
@src/server/internal/agent/executor_test.go
@src/server/internal/agent/service.go
@src/web/src/features/agents/agentsApi.ts

<interfaces>
Consume the exact Plan 01 carrier and Plan 02 runtime handoff without another constructor:

```go
type EffectID string
type CapabilityBindings struct { /* private immutable lookup */ }
func (b CapabilityBindings) Resolve(effect EffectID) (CapabilityID, error)
type RuntimeAuthorities struct {
    CatalogAuthorizer CatalogAuthorizer
    CapabilityBindings CapabilityBindings
}
func NewRuntimeAuthorities(contract AuthoredContractV1, profile DeploymentProfile, guard Guard) (RuntimeAuthorities, error)
type WebSearchRuntimeOptions struct {
    Authorities releasecontract.RuntimeAuthorities
    Guard releasecontract.Guard
    Effects releasecontract.EffectRegistrar
}
func buildAgentWebSearchProvider(cfg config.Config, options mcp.WebSearchRuntimeOptions) (mcp.WebSearchProvider, error)
type ToolRuntimeOptions struct {
    Authorities releasecontract.RuntimeAuthorities
    Guard releasecontract.Guard
    Effects releasecontract.EffectRegistrar
    HTTPClient *http.Client
}
func NewAuthorizedToolExecutor(mcpClient *mcp.Client, options ToolRuntimeOptions) (*ToolExecutor, error)
```

`websearch.NewProviderFromConfig` and `Chain` use this exact `WebSearchRuntimeOptions` after Plan 06; Plan 10 must not introduce another web-search authority type. `NewAuthorizedToolExecutor` is the only production-capable ToolExecutor constructor. Any retained one-argument test compatibility constructor has zero authority and its `Execute` must fail before every effect; production source under `src/server/internal/agent` and `src/server/internal/http` must not call it. Plan 02 later passes the exact startup carrier into these already-tested constructors from its sole `server.go` ownership.

The exact catalog response contract is the live `agent.ToolDefinition` plus required `CapabilityID string \`json:"capabilityId"\`` and the frontend `AgentToolDefinition.capabilityId: string`. OpenAI/provider conversion must omit it. Agent tool create/update request DTOs do not contain capability identity and strict decoding rejects that member. `normalizeToolDefinition` preserves only server-returned capability identity, while mutation serialization selects business fields and cannot spread a response object.
</interfaces>
</context>

## Artifacts this phase produces

- Modified files: the exact nine existing factory/provider/builtin/live-Agent/frontend API paths in `files_modified`; `workflow_executors.go` is owned by Plan 04 and `server.go` remains Plan 02's sole owner.
- New exact options/function surface: `mcp.WebSearchRuntimeOptions`, `agent.ToolRuntimeOptions`, authority-required direct Tavily/configured factory overloads, instance-scoped `NewWebSearchTool`, and `NewAuthorizedToolExecutor`.
- New top-level suites: `TestTavilyWebSearchReadinessDispatchContract`, `TestWebSearchProviderFactoryAuthorityContract`, and `TestLiveAgentToolExecutorReadinessContract`; Plan 06's `TestWebsearchChainReadinessPerProviderContract` remains the configured Chain attempt owner and is invoked as an upstream assertion.
- Typed descriptors: Plan 01-owned IDs for builtin execute, custom API HTTP, local Python process, Python sandbox and Agent MCP dispatch. `NewAuthorizedToolExecutor`, direct Tavily and WebSearchTool/provider constructors register their owned descriptors exactly once; live calls never register. Plan 03 later creates expected rows and joins them with real `executor.go` AST seams.
- No new contract/profile reader, capability constant outside Plan 01 `EffectID`, readiness evaluator, global authority, duration, report, deployment asset or evidence claim.

<tasks>

<task type="auto">
  <name>Task 1: Build authority-required direct Tavily and configured Chain packages</name>
  <read_first>
    - src/server/internal/http/agent_web_search.go
    - src/server/internal/http/agent_web_search_test.go
    - src/server/internal/mcp/web_search_provider.go
    - src/server/internal/mcp/web_search_provider_test.go
    - src/server/internal/mcp/websearch/websearch.go
    - src/server/internal/mcp/websearch/websearch_test.go
    - src/server/internal/releasecontract/catalog.go
  </read_first>
  <files>src/server/internal/http/agent_web_search.go, src/server/internal/http/agent_web_search_test.go, src/server/internal/mcp/web_search_provider.go, src/server/internal/mcp/web_search_provider_test.go</files>
  <behavior>
    - Test V31.1-R07/T-RDY-03: `buildAgentWebSearchProvider` and direct/configured package constructors require exactly one Plan 01-based WebSearchRuntimeOptions carrier; missing/zero/mismatched values fail before provider construction or outbound calls without any BuildRuntime/server.go dependency.
    - Test V31.1-R07/T-RDY-07: direct Tavily reconstructs the canonical `web_search` plus `tavily` runtime subject and calls current `CatalogAuthorizer.ResolveAndRequire` immediately before each HTTP attempt; denial makes RoundTripper count zero.
    - Test V31.1-R07/T-RDY-03: comma-configured Chain receives the same options; primary failure followed by fake-clock denial leaves fallback Search/HTTP count zero through Plan 06's exact Chain suite.
    - Test V31.1-R09/T-RDY-03: Tavily and factory-owned WebSearchTool/provider constructors resolve/register stable descriptors once; two legal Search calls on one instance keep registrar count fixed and guard/HTTP counts at two, expiry after the first keeps the second HTTP delta zero, and duplicate owner construction fails.
  </behavior>
  <action>Define the single `mcp.WebSearchRuntimeOptions{Authorities,Guard,Effects}` carrier and change `buildAgentWebSearchProvider` to accept it directly and return `(provider,error)`, failing on missing authority or malformed configured provider rather than logging and continuing. Do not edit or import `server.go`; Plan 02 owns the later composition handoff after this package contract is passing. `NewTavilyWebSearchProvider` and the factory-created WebSearchTool/provider owners resolve their stable Plan 01 `EffectID` mappings and call the strict EffectRegistrar exactly once during construction; duplicate/unknown registration fails construction. The Tavily instance retains only immutable descriptor wiring plus business configuration, never generation/verdict. Its `Search` reconstructs server-owned `CatalogSubject{Kind:"builtin",ID:"web_search",Runtime:"tavily"}` and invokes `Authorities.CatalogAuthorizer.ResolveAndRequire(ctx,subject,BoundaryOutbound)` as the final authorization step before `HTTPClient.Do`; Search never calls `Register`. The configured branch passes the identical options into Plan 06 `websearch.NewProviderFromConfig`, whose constructor owns its one-shot descriptor registration; do not add a second Chain guard or mapping. Prove two legal calls on one instance produce registration/guard/HTTP counts 1/2/2, then first-pass/current-expiry produces 1/2/1; a second constructor claiming the same descriptor fails duplicate registration. Update existing direct-provider/factory tests for required options and exact denial behavior. Package tests prove there is no local Load/ResolveCommittedProfile call, hard-coded capability ID, global authority or implicit BuildRuntime dependency. Only after the production changes above are complete, add and run `TestTavilyWebSearchReadinessDispatchContract` in `web_search_provider_test.go` and `TestWebSearchProviderFactoryAuthorityContract` in `agent_web_search_test.go` as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Both exact package-local new suites and Plan 06's exact Chain suite pass through the matched helper at task completion, before Plan 02 begins.
    - Direct Tavily current denial/expiry produces zero HTTP calls; configured primary failure plus denial produces zero fallback calls.
    - Each owner constructor registers its descriptor exactly once; repeated same-instance searches do not re-register, and duplicate construction remains a strict failure.
    - The factory accepts one carrier unchanged, and every missing/mismatched mapping fails before provider construction; Plan 02 later verifies the BuildRuntime-to-factory handoff.
    - Environment/provider name selects business configuration only; it cannot select capability, Guard, profile, identity or generation.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/mcp '^TestTavilyWebSearchReadinessDispatchContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./internal/mcp/websearch '^TestWebsearchChainReadinessPerProviderContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./internal/http '^TestWebSearchProviderFactoryAuthorityContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.1-R07 and T-RDY-03/T-RDY-07 cover the committed monolith direct Tavily and configured Chain construction/attempt paths.</done>
</task>

<task type="auto">
  <name>Task 2: Guard every live Agent ToolExecutor effect</name>
  <read_first>
    - src/server/internal/mcp/builtin.go
    - src/server/internal/agent/executor.go
    - src/server/internal/agent/executor_test.go
    - src/server/internal/agent/service.go
    - src/server/internal/http/workflow_executors.go
    - src/server/internal/agent/tools/registry.go
    - src/server/internal/releasecontract/catalog.go
  </read_first>
  <files>src/server/internal/mcp/builtin.go, src/server/internal/agent/executor.go, src/server/internal/agent/executor_test.go, src/server/internal/agent/service.go, src/web/src/features/agents/agentsApi.ts</files>
  <behavior>
    - Test V31.1-R07/T-RDY-03: named subtests `builtin`, `custom_api_http`, `custom_python_process`, `custom_python_sandbox`, and `mcp` each advance to a new current-generation denial immediately before their concrete effect and observe the corresponding builtin/HTTP/process/sandbox/MCP counter remain zero.
    - Test V31.1-R07/T-RDY-07: `NewAuthorizedToolExecutor` resolves/registers the five Plan 01 typed descriptors exactly once; live `ToolExecutor` constructs a server-owned CatalogSubject from persisted Tool type/name/runtime/server metadata and calls current CatalogAuthorizer/Guard immediately before every new effect without registering again. Caller/persisted capability-like fields are ignored.
    - Test V31.1-R07/T-RDY-03: Agent Service runner, plan-step and direct `ExecuteTool` all delegate to the same authority-required ToolExecutor; the Plan 04-owned workflow adapter contract requires injection and never creates a production-capable zero-authority fallback.
    - Test V31.1-R07/T-RDY-03: instance-scoped WebSearchTool and configured Chain recheck independently; primary failure followed by denial blocks fallback through the real ToolExecutor path, while Plan 06 Registry calls remain at zero to prove the live path does not rely on that unconsumed API.
    - Test V31.1-R09/T-RDY-03: for each of the five effect classes, two legal calls on one executor leave its descriptor registration count at one and guard/downstream counts at two; first success followed by generation expiry leaves the second downstream delta zero; constructing a duplicate executor owner against the same registry fails before execution.
    - Test V31.1-R07/T-RDY-07: live `ToolDefinition` and normalized `AgentToolDefinition` retain the exact server-derived `capabilityId`; provider conversion and every create/update mutation omit it, and a server mutation body containing it is rejected before persistence/effect.
  </behavior>
  <action>Introduce `ToolRuntimeOptions{Authorities,Guard,Effects,HTTPClient}` and `NewAuthorizedToolExecutor`; missing/zero/mismatched values fail construction. In `NewAuthorizedToolExecutor`, resolve the five stable branch-specific Plan 01 `EffectID` mappings through `Authorities.CapabilityBindings` and register each typed descriptor exactly once before returning; any unknown or duplicate descriptor fails construction. Retain immutable descriptors only as wiring evidence and never retain generation/verdict. If the existing one-argument `NewToolExecutor` must remain so unrelated tests compile, make it a test-compatible zero-authority value whose `Execute` always denies before any effect, and dynamically prove through executed construction paths that production Agent/HTTP composition uses only `NewAuthorizedToolExecutor`. Inject process starter, HTTP client, sandbox runner, MCP client and builtin/provider fakes so each concrete counter is independently observable. In `ToolExecutor.Execute`, derive a fresh server-owned subject from the selected persisted Tool and branch runtime and call current `Authorities.CatalogAuthorizer.ResolveAndRequire` immediately before the concrete action: builtin `tool.Execute`/provider.Search, custom API `HTTPClient.Do`, local Python process start, sandbox `RunCustomPython`, or MCP `CallTool`. Execute never calls `Register`; every retry/fallback/new process re-guards without pinning capability/generation. Replace the zero-field/global-backed web-search behavior with `NewWebSearchTool(provider, WebSearchRuntimeOptions)`, whose constructor owns one-shot registration, and remove production use of `SetWebSearchProviderForTest`. Refactor `Service` so `initRunner`, plan-step, direct `ExecuteTool`, and setters retain one authorized executor/options rather than duplicate builtin/MCP switches; Plan 04 owns the workflow adapter source and Plan 02 later injects this executor. Extend the live `ToolDefinition` response shape with server-derived `capabilityId`, ensure `ToOpenAITool` never emits it, update `agentsApi.ts` normalization to require/preserve it, and serialize create/update mutations by explicit business-field selection so capabilityId is omitted even when the input originated from a response. Strict server mutation decoding rejects capabilityId. Explicitly keep `agent/tools.Registry` independent: neither Service nor ToolExecutor routes through it, and its tests are not cited as production safety proof. For every effect class prove same-instance registration/guard/downstream counts 1/2/2 on two legal calls and 1/2/1 when generation expires after the first; a second `NewAuthorizedToolExecutor` using the same strict EffectRegistry fails duplicate owner registration. Unknown/deleted/ambiguous/runtime mismatch, missing authority, descriptor failure, stale/blocked/unavailable or expiry after selection returns a sanitized stable error and keeps the exact downstream counter zero. Only after the production changes above are complete, add and run `TestLiveAgentToolExecutorReadinessContract` in `executor_test.go` as implementation-complete automated verification using a table with exact named subtests for builtin execution, custom API `HTTPClient.Do`, local Python `exec.CommandContext` start, production Python sandbox `RunCustomPython`, and MCP `CallTool`, plus Service runner/plan-step/direct-ExecuteTool and the Plan 04-owned workflow-adapter injection contract.</action>
  <acceptance_criteria>
    - The exact live suite covers enabled success plus current denial for all five builtin/custom-HTTP/local-Python/sandbox/MCP effect classes and for runner, plan-step, direct Service, and workflow-adapter paths.
    - Each denied subtest names and proves its own builtin/HTTP/process/sandbox/MCP counter is zero; one authorized effect cannot authorize the next effect or fallback.
    - Constructor registration stays exactly once per owned descriptor across repeated effects; duplicate executor/WebSearchTool construction remains a strict owner-conflict failure.
    - No production path writes/reads global web-search provider or authority, uses the one-argument compatibility constructor, or treats `agent/tools.Registry` as a production guard.
    - Every effect uses a descriptor already registered once by its owner constructor, then performs immediately preceding current catalog/Guard authorization without re-registration or generation pinning; existing enabled business result formatting remains compatible.
    - Tool response JSON and frontend normalization preserve server-derived capabilityId, while provider conversion and all mutation payloads omit it and strict server mutation decoding rejects injection.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/agent '^TestLiveAgentToolExecutorReadinessContract$' &amp;&amp; pnpm --dir src/web exec vitest run src/features/agents/agentsApi.test.ts --reporter=verbose &amp;&amp; pnpm --dir src/web exec tsc --noEmit &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.1-R07 and T-RDY-03/T-RDY-07 prove current-generation live Agent builtin/custom HTTP/local Python/sandbox/MCP execution without Registry substitution, global state, or locally reconstructed authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Plan 01 carrier -> web-search/ToolExecutor package constructors | Package consumers must be independently testable before later BuildRuntime composition. |
| persisted Agent Tool -> live ToolExecutor dispatch | Server-owned type/name/runtime becomes a current catalog subject; caller capability metadata is untrusted. |
| side-effect-free constructor -> EffectRegistry | Stable descriptors register exactly once; duplicate owner construction fails while repeated effects on the same instance do not touch the registry. |
| ToolExecutor branch -> builtin/HTTP/process/sandbox/MCP effect | Tool selection or a prior attempt cannot authorize the next concrete effect; each call re-guards only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-RDY-03 | Elevation of Privilege | monolith web-search chain | high | mitigate | Independently tested carrier-required package constructors, per-layer current checks and downstream zero-call tests; Plan 02 owns later composition. |
| T-RDY-07 | Tampering | provider/capability selection | high | mitigate | Server-owned subjects, immutable RuntimeAuthorities and no request/config capability authority. |
| T-RDY-03 | Elevation of Privilege | live Agent custom/MCP dispatch | high | mitigate | One-shot constructor descriptor registration plus current guard immediately before HTTP/process/sandbox/MCP, with five named double-call/expiry zero-call subtests. |
| T-RDY-03 | Spoofing | Registry/global/compatibility substitute | high | mitigate | Live ToolExecutor suite, instance-scoped options, no production Registry reliance, no production compatibility constructor and global-swap removal. |
</threat_model>

<verification>
- All plan-local/upstream exact suite invocations use the Phase 31 matched helper; direct source scans only bind the already dynamic tested live path.
- Plan 06 remains sole owner of generic configured Chain and independent Registry implementation/tests; Plan 04 owns `workflow_executors.go`, Plan 10 owns direct Tavily/builtin/live Agent packages plus `agentsApi.ts`, and Plan 02 exclusively owns `server.go` plus complete composition after these suites pass.
- `TestLiveAgentToolExecutorReadinessContract` has named builtin/custom-API/local-Python/sandbox/MCP subtests with independent concrete effect counters; Registry calls remain zero and are not accepted as proof.
- The suite separately asserts constructor registrar counts, per-call guard counts, duplicate-constructor failure and downstream counts; no dispatch path calls `Register`.
- Evidence is E1 repository-local behavior proof only; no provider-live, target or commercial-readiness claim is allowed.
</verification>

<success_criteria>
- Direct Tavily, configured Chain and builtin packages expose authority-required constructors with exact current-generation zero-call proof before Plan 02 composition.
- `NewAuthorizedToolExecutor` registers the five Plan 01 typed descriptors once; the live Agent ToolExecutor only re-resolves current subjects and guards immediately before builtin, custom API HTTP, local Python process, Python sandbox and MCP effects.
- Stable descriptors are registered once by each owner constructor; two effects on the same instance re-guard twice without duplicate registration, while a duplicate owner constructor fails.
- Service runner/plan-step/direct-tool and workflow adapter cannot bypass or replace the authorized ToolExecutor with the independent Registry or global/compatibility state.
- No production global, local contract/profile reload, hard-coded capability or missing-authority fallback can dispatch web search or another Agent effect.
</success_criteria>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-10-SUMMARY.md` when done.
</output>
