---
phase: 31.1-readiness-fail-closed
plan: "06"
subsystem: api
tags: [mcp, agent-tools, websearch, readiness, fail-closed]

requires:
  - phase: 31.1-readiness-fail-closed
    provides: "Plan 01 RuntimeAuthorities, digest-bound catalog resolver, typed EffectID bindings, Guard, and EffectRegistrar"
provides:
  - "Strict authority-bound MCP transport constructor with per-request catalog resolution immediately before HTTP transport"
  - "Independent builtin/custom/MCP Registry API with one-time typed descriptor registration and per-execution re-resolution"
  - "Per-provider web-search fallback authorization and strict MCP mutation DTO decoding"
  - "Repository-local E1 zero-call suites for transport, Registry, outer web search, Chain fallback, and MCP mutation"
affects: [31.1-03-effect-inventory, 31.1-10-agent-tool-dispatch, MCP, web-search]

tech-stack:
  added: []
  patterns:
    - "Startup RuntimeAuthorities is immutable wiring evidence; CatalogAuthorizer.ResolveAndRequire is repeated immediately before each effect"
    - "EffectRegistrar registration is constructor-only and duplicate owners fail closed"
    - "MCP request DTOs use DisallowUnknownFields plus trailing-input rejection"

key-files:
  created:
    - src/server/internal/agent/tools/registry_readiness_test.go
  modified:
    - src/server/internal/mcp/client.go
    - src/server/internal/mcp/client_test.go
    - src/server/internal/agent/tools/registry.go
    - src/server/internal/agent/tools/websearch.go
    - src/server/internal/mcp/websearch/websearch.go
    - src/server/internal/mcp/websearch/websearch_test.go
    - src/server/internal/http/mcp_handler.go
    - src/server/internal/http/mcp_handler_test.go

key-decisions:
  - "MCP transport and tool categories resolve only server-owned runtime/tool subjects; caller metadata, capability-like Extra values, and persisted verdicts remain non-authoritative."
  - "The Registry suite explicitly covers only the independent reusable Registry API; live agent.ToolExecutor dispatch remains Plan 10 ownership."
  - "MCP mutation validates strict JSON before store access, then authorizes the server-owned runtime.mcp subject before CreateServer."

patterns-established:
  - "Per-attempt fallback checks: a primary provider failure does not authorize a fallback; the fallback performs its own current catalog and Guard check."
  - "Stable descriptor IDs are resolved from CapabilityBindings once and never registered from Execute, Search, or request handlers."

requirements-completed: [RELS-01]

coverage:
  - id: D1
    description: "MCP JSON-RPC transport re-resolves current runtime.mcp authority before every HTTP attempt and blocks denied retries with zero transport calls."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/mcp/client_test.go#TestMCPReadinessDispatchContract"
        status: pass
    human_judgment: false
  - id: D2
    description: "Independent builtin/custom/MCP Registry execution re-resolves current subjects and rejects stale/caller capability metadata."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/agent/tools/registry_readiness_test.go#TestToolRegistryReadinessExecutionContract"
        status: pass
    human_judgment: false
  - id: D3
    description: "Outer web-search fallback and internal Chain authorize every provider attempt, including fallback after primary failure."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/agent/tools/registry_readiness_test.go#TestWebsearchReadinessFallbackContract"
        status: pass
      - kind: unit
        ref: "src/server/internal/mcp/websearch/websearch_test.go#TestWebsearchChainReadinessPerProviderContract"
        status: pass
    human_judgment: false
  - id: D4
    description: "MCP create/execute DTOs reject capabilityId, unknown fields, and trailing JSON before persistence or transport effects."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/http/mcp_handler_test.go#TestMCPCatalogMutationContract"
        status: pass
    human_judgment: false

duration: 40min
completed: 2026-07-18
status: complete
---

# Phase 31.1 Plan 06: MCP, Registry, and Web-Search Readiness Summary

**MCP transport, independent tool Registry, web-search fallback, and MCP mutations now use server-derived current catalog authority with constructor-only effect registration and zero-call denial tests.**

## Performance

- **Duration:** 40 min
- **Started:** 2026-07-18T03:02:57Z
- **Completed:** 2026-07-18T03:42:29Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments

- Added strict `RuntimeAuthorities` carriers for MCP Client, independent Registry, outer WebsearchTool, provider Chain, and MCP handler constructors. Each resolves typed effect bindings and registers descriptors exactly once; duplicate registration fails construction.
- Guarded each MCP JSON-RPC transport attempt with a fresh `runtime.mcp` catalog subject and current `BoundaryOutbound` authorization. Registry categories reconstruct builtin/custom/MCP subjects on every Execute call, and web-search primary/fallback attempts repeat authorization independently.
- Added strict MCP mutation decoding with unknown-field and trailing-input rejection, including stable zero-call tests for `capabilityId`, readiness denial, runtime mismatch, duplicate descriptor construction, and denied fallback.
- Full backend regression passed: `go test ./... -count=1`. All five plan selectors passed with `scripts/run-go-tests-matched.sh` and `git diff --check`.

## Task Commits

Each implementation/test slice was committed atomically:

1. **Task 1: Guard every MCP JSON-RPC dispatch** - `79ec481` (feat), `79d68f3` (test)
2. **Task 2: Re-resolve independent Registry API** - `8cc46d3` (feat), `518ab90` (test)
3. **Task 3: Guard web-search fallback and strict MCP mutation** - `56d79fa` (feat), `cff5f5b` (test), `f04502d` (test), `d8119a5` (test), `a7d5da4` (test consolidation)

**Plan metadata:** pending docs/state commit.

## Files Created/Modified

- `src/server/internal/mcp/client.go` - strict MCP runtime options, one-time transport descriptor, and per-request catalog authorization.
- `src/server/internal/agent/tools/registry.go` - independent Registry readiness carrier, category descriptors, and execution-time re-resolution.
- `src/server/internal/agent/tools/websearch.go` - outer web-search descriptor and per-provider authorization.
- `src/server/internal/mcp/websearch/websearch.go` - guarded provider/Chain construction and per-attempt checks.
- `src/server/internal/http/mcp_handler.go` - strict JSON boundary and pre-store runtime subject authorization.
- `*_test.go` paths above - dynamic resolver/Guard/registrar/downstream zero-call suites.

## Decisions Made

- Dynamic catalog subjects are the only effect authority. Capability-like request/metadata values are ignored or rejected and no generation/verdict is retained by consumers.
- Registry tests are explicitly independent API proof and do not claim coverage for the production `agent.ToolExecutor`; Plan 10 owns that dispatcher seam and Plan 03 owns exact static/runtime effect joining.
- The compatibility constructors remain for existing non-readiness unit callers; production composition must use the strict `WithOptions` constructors when runtime authority wiring is available.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Consolidated the outer web-search suite into the planned Registry test file**
- **Found during:** Summary file inventory
- **Issue:** A temporary standalone test file would have expanded the plan's exact nine-file artifact list.
- **Fix:** Moved `TestWebsearchReadinessFallbackContract` and its probe into `registry_readiness_test.go` and removed the temporary file.
- **Verification:** Focused Agent tools tests and full Go regression passed.
- **Committed in:** `a7d5da4`

**Total deviations:** 1 auto-fixed (Rule 3)
**Impact on plan:** No behavior or scope change; final artifact list matches the plan.

## Issues Encountered

- Frontend `pnpm --dir src/web test` and `pnpm --dir src/web build` started long-lived Vitest/TypeScript worker trees without producing completion or exit output in this environment. Those task-owned processes were terminated after inspection; frontend verification is residual/unverified. Backend full regression and all plan-focused suites passed.
- Existing router/agent composition still uses compatibility constructors in current mainline callers; strict startup wiring is exposed through `*WithOptions` constructors for the runtime composition work owned by Plans 02/10.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 03 can inventory the typed descriptors and perform the exact expected/static/runtime join without depending on this plan to read an expected manifest.
- Plan 10 can inject the same `RuntimeAuthorities` carrier into the live `agent.ToolExecutor` and configured monolith web-search path; this plan intentionally does not claim that production dispatcher proof.
- Evidence ceiling is E1 repository-local unit proof; no target/live deployment or profile-parity claim is made.

---
*Phase: 31.1-readiness-fail-closed*
*Completed: 2026-07-18*

## Self-Check: PASSED

- Summary file exists at the expected phase path.
- All nine implementation/test commits are present in repository history.
- No generated or unintended files remain; `.planning/config.json` remains intentionally unstaged.
