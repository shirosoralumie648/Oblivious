---
phase: 31.1-readiness-fail-closed
plan: "18"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/internal/releasecontract/effect_registry.go
  - src/server/internal/releasecontract/effect_registry_test.go
  - config/release/readiness-effect-surface.v1.json
autonomous: true
gap_closure: true
requirements: [RELS-01]
must_haves:
  truths:
    - "One explicit descriptor specification map is the sole registry allowlist and descriptor-to-EffectID mapping; owner substrings, prefix wildcards, and fallback casts cannot bless unknown runtime descriptors (D-RDY-11, D-RDY-20)."
    - "The checked-in manifest contains every genuinely construction-reachable Admin, Marketplace, MCP, Agent, Chat, web-search, billing, Relay, worker, channel, and finance runtime descriptor exactly once with descriptor, registration, guard, effect-call, and configuration-selection metadata; independent package APIs are classified but cannot be promoted into the monolith runtime projection without a real owner (D-RDY-11, D-RDY-14, D-RDY-15)."
    - "Static discovery is based on parsed constructor/method call structure, not comments, string tokens, enum mentions, dead constants, or owner-name heuristics; removed or relocated guard/effect calls fail discovery (D-RDY-11)."
    - "VerifyEffectCoverage rejects missing/invalid RuntimeAuthorities, contract, or selected profile instead of treating authority verification as optional (D-RDY-11, D-RDY-20)."
  artifacts:
    - path: "src/server/internal/releasecontract/effect_registry.go"
      provides: "Strict descriptor specification map, structural guard-to-effect discovery, and authority-required coverage verification"
      contains: "runtimeDescriptorSpecs"
    - path: "src/server/internal/releasecontract/effect_registry_test.go"
      provides: "Marker-preserving guard/effect relocation mutations and invalid-authority tests"
      contains: "TestStructuralEffectDiscoveryMutationContract"
    - path: "config/release/readiness-effect-surface.v1.json"
      provides: "Complete independent expected inventory with strict descriptor and call-chain metadata"
      contains: "admin.channel.model.mutation"
  key_links:
    - from: "runtimeDescriptorSpecs"
      to: "EffectRegistry.Register/effectIDForDescriptor"
      via: "one exact descriptor ID lookup validates expected owner/boundary and resolves its authored EffectID"
      pattern: "runtimeDescriptorSpecs"
    - from: "readiness-effect-surface.v1.json"
      to: "DiscoverEffectSurfaces"
      via: "every applicable expected row exact-joins descriptor ID, owner, registration symbol, guard call, concrete effect call, and call order"
      pattern: "astCall"
    - from: "VerifyEffectCoverage"
      to: "RuntimeAuthorities.CapabilityBindings"
      via: "valid authorities are mandatory for every runtime descriptor/capability join"
      pattern: "authorities.Valid"
---

<objective>
Close the contract/static/authority portions of CR-01 by making one plan own the complete descriptor allowlist, effect mapping, expected manifest, structural guard-to-effect analysis, and mandatory authority validation.

Purpose: The production exact join cannot succeed when real constructor descriptors are absent from either the registry allowlist or expected inventory, and dead text must not certify a removed guard/effect.
Output: Complete strict descriptor contract, structural discovery, authority-required exact join, and mutation-driven tests.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-RESEARCH.md
@.planning/phases/31.1-readiness-fail-closed/31.1-VERIFICATION.md
@.planning/phases/31.1-readiness-fail-closed/31.1-03-SUMMARY.md
@src/server/internal/releasecontract/effect_registry.go
@src/server/internal/releasecontract/effect_registry_test.go
@src/server/internal/agent/executor.go
@src/server/internal/agent/tools/registry.go
@config/release/readiness-effect-surface.v1.json

<constraints>
- Use Go parser/AST/token APIs and repository types; add no package dependency or string-marker fallback.
- The expected manifest classifies seams but cannot author capability commitment/profile truth.
- Excluded seams remain classified statically and register zero runtime descriptors for monolith.
- CR-06/CR-09 remain Phase 34, WR-02 remains Phase 35, and OpenAPI Envelope parity remains Phase 31.2.
- Evidence is repository-local E1 only.
</constraints>

<interfaces>
Create one private `runtimeDescriptorSpecs` map keyed by the exact runtime descriptor ID. Each value names the authored `EffectID`, exact owner, boundary, and any configuration-selection rule; `EffectRegistry.Register`, `effectIDForDescriptor`, and runtime joining must all consume it. Remove prefix acceptance, owner-substring inference, and `EffectID(descriptor.ID)` fallback. The genuinely live web-search projection is `agent.tool.builtin` plus `mcp.websearch.builtin` plus exactly one configured provider row: `mcp.websearch.tavily`, `mcp.websearch.chain`, or `mcp.websearch.provider`. Do not expect `agent.tool.web_search`: it is registered only by the independent `agent/tools.WebsearchTool`, and the inspected BuildRuntime path has no such owner. Likewise, any independent Registry seam without a production construction owner must remain explicitly classified as non-runtime/excluded rather than being instantiated by tests to satisfy the join. The map/manifest must still cover every genuinely constructed Admin, Marketplace, Billing, MCP transport, Chat, Relay, worker, channel, finance, and ToolExecutor descriptor.

Extend `EffectSurface` with strict `descriptorId`, `registrationSymbol`, `guardCall`, `effectCall`, and optional configuration-selector metadata (or equivalently named typed fields). Preserve strict JSON/EOF decoding. Every committed/conditional runtime row must populate the structural fields and exact owner/boundary/capability data; mutually exclusive provider rows must project according to the real BuildRuntime configuration, while excluded entrypoint/GRPC/independent-API rows register zero monolith descriptors. Parse the exact constructor descriptor tables/composite literals and owning effect functions, and require the resolver/guard call to precede the concrete effect call. `VerifyEffectCoverage` must require valid Contract, selected committed Profile, RuntimeAuthorities, and the selected runtime configuration projection before any join succeeds.
</interfaces>
</context>

## Artifacts this phase produces

- Structural ToolExecutor/Registry static discovery.
- Sole strict descriptor allowlist/effect mapping for every production descriptor.
- Complete manifest metadata for every applicable registration, guard, and concrete effect call.
- Marker-preserving source mutation tests.
- Mandatory contract/profile/authority validation.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make one strict descriptor contract complete</name>
  <files>src/server/internal/releasecontract/effect_registry.go, src/server/internal/releasecontract/effect_registry_test.go, config/release/readiness-effect-surface.v1.json</files>
  <read_first>
    - src/server/internal/releasecontract/effect_registry.go
    - src/server/internal/releasecontract/effect_registry_test.go
    - config/release/readiness-effect-surface.v1.json
    - src/server/internal/admin/service.go
    - src/server/internal/http/billing_handler.go
    - src/server/internal/http/marketplace_handler.go
    - src/server/internal/chat/relay_gateway.go
    - src/server/internal/mcp/client.go
    - src/server/internal/mcp/web_search_provider.go
    - src/server/internal/mcp/websearch/websearch.go
    - src/server/internal/agent/executor.go
    - src/server/internal/agent/tools/registry.go
    - src/server/internal/agent/tools/websearch.go
    - .planning/phases/31.1-readiness-fail-closed/31.1-REVIEW.md
  </read_first>
  <behavior>
    - The descriptor spec map and manifest contain identical exact applicable descriptor IDs, owners, boundaries, capabilities/effects, with no prefix/owner fallback.
    - Admin model mutation, Marketplace and Billing checkout, MCP transport, `agent.tool.builtin`, `mcp.websearch.builtin`, the three mutually exclusive provider alternatives, and both Chat descriptors are present in addition to every existing genuinely constructed runtime descriptor.
    - `agent.tool.web_search` and any ownerless Registry descriptor are absent from the monolith runtime projection and cannot be made present by constructing an independent tool/registry in a coverage fixture.
    - Every committed/conditional runtime manifest row has nonblank descriptor, registration, guard, and concrete effect metadata.
    - Removing any manifest/spec row reports missing; adding an unregistered row reports extra; duplicate/unknown owner/boundary/effect mappings fail stably.
  </behavior>
  <action>Write `TestEffectSurfaceManifestRuntimeDescriptorContract` first. Replace `isKnownEffectDescriptorID`, `effectIDForDescriptor`, owner-substring inference, and prefix acceptance with one `runtimeDescriptorSpecs` table keyed by exact descriptor ID and carrying exact `EffectID`, owner, boundary, and configuration selector. Make registry validation and authority joining use only this table. Extend `EffectSurface` strict decoding/validation with descriptor/registration/guard/effect/selection metadata. Update the manifest as this plan's sole contract write: include every genuinely applicable runtime descriptor, model `mcp.websearch.tavily`/`chain`/`provider` as mutually exclusive selected alternatives, retain `agent.tool.builtin` and `mcp.websearch.builtin`, and classify ownerless independent Registry/WebsearchTool seams as zero-runtime rather than adding `agent.tool.web_search` to the production expectation. Add table-driven missing/extra/duplicate/unknown/mismatched map, manifest, and selected-projection mutations, including a fixture that tries to construct the independent tool only for coverage and is rejected. Do not modify producer source to fit the contract; real composition changes belong to Plan 19 or the earlier owning gap plan.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestEffectSurfaceManifestRuntimeDescriptorContract$'</automated>
  </verify>
  <acceptance_criteria>
    - One explicit map is the only descriptor allowlist/effect mapping.
    - The exact map/manifest projection includes every actual production constructor descriptor and no wildcard/prefix allowance.
    - The selected web-search projection has the generic builtin, guarded MCP builtin, and one provider row; it excludes `agent.tool.web_search` and ownerless Registry rows.
    - Every applicable manifest row has complete structural metadata.
    - Missing and extra mutations for both map and manifest are rejected non-vacuously.
  </acceptance_criteria>
  <done>The expected/runtime descriptor contract is complete, strict, and owned by one plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove every expected guard-to-effect chain structurally</name>
  <files>src/server/internal/releasecontract/effect_registry.go, src/server/internal/releasecontract/effect_registry_test.go, config/release/readiness-effect-surface.v1.json</files>
  <read_first>
    - src/server/internal/releasecontract/effect_registry.go
    - src/server/internal/releasecontract/effect_registry_test.go
    - config/release/readiness-effect-surface.v1.json
    - src/server/internal/agent/executor.go
    - src/server/internal/agent/tools/registry.go
    - src/server/internal/http/router.go
    - src/server/internal/http/server.go
  </read_first>
  <behavior>
    - Current production source discovers every expected applicable descriptor registration and owning guard/effect chain.
    - Adding only marker comments/strings/constants discovers nothing.
    - Removing registration/guard/effect calls or moving a guard after its concrete effect fails the exact affected seam.
    - Static discovery reports both missing expected and extra production seams and remains nonzero.
  </behavior>
  <action>Delete `appendExecutorEffectSurfaces` raw file reads and all marker/string fallback logic. Parse constructor registrations, descriptor tables/composite literals, resolver/current-guard calls, and concrete effect calls for every applicable manifest row. Match exact package/function/receiver ownership and require registration at construction plus guard before effect in the owning effect method. For indirect callbacks, follow only explicit local typed call edges named by manifest metadata; do not infer from comments or owner strings. Build temp-repo mutations that retain old tokens while deleting or relocating registration, guard, and effect AST nodes for each producer family (worker, Relay/Chat, Admin/Marketplace/Billing, MCP/web-search, ToolExecutor/Registry). Assert exact missing/static-drift/extra codes.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestStructuralEffectDiscoveryMutationContract$'</automated>
  </verify>
  <acceptance_criteria>
    - No raw token, prefix, or owner-name fallback remains in structural discovery.
    - Every producer family has marker-preserving registration/guard/effect mutations.
    - Missing, reordered, and extra call chains are rejected with the affected descriptor ID.
    - Current manifest/static exact join passes and is nonzero.
  </acceptance_criteria>
  <done>Static coverage proves actual production registration and guard-before-effect structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Require contract, profile, and RuntimeAuthorities</name>
  <files>src/server/internal/releasecontract/effect_registry.go, src/server/internal/releasecontract/effect_registry_test.go</files>
  <read_first>
    - src/server/internal/releasecontract/effect_registry.go
    - src/server/internal/releasecontract/effect_registry_test.go
    - src/server/internal/releasecontract/catalog.go
    - config/release/contract.v1.json
  </read_first>
  <behavior>
    - Zero authorities, empty contract, or missing/non-committed profile fails coverage.
    - Unknown effect/capability, mismatch, duplicate, extra, missing, excluded registration, and zero runtime rows all fail stably.
    - Valid monolith contract/profile/authorities joins every runtime descriptor.
  </behavior>
  <action>Make Contract, selected Profile, and valid RuntimeAuthorities mandatory in `VerifyEffectCoverage`; remove the conditional authority block. Validate that the profile belongs to the contract and is committed, then resolve every runtime descriptor through its exact `runtimeDescriptorSpecs` EffectID and `CapabilityBindings`. Exact-match capability, boundary, owner, and profile projection. Add table-driven negative cases for each omitted/invalid input and preserve stable `effect_coverage_*` errors.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessEffectCoverageContract$|^TestEffectCoverageRequiresRuntimeAuthoritiesContract$'</automated>
  </verify>
  <acceptance_criteria>
    - Coverage cannot pass without valid RuntimeAuthorities, contract, and selected committed profile.
    - All mismatch/duplicate/missing/extra/zero cases are non-vacuous and stable.
    - Valid authority joins every runtime descriptor capability exactly.
  </acceptance_criteria>
  <done>The exact join always includes independent expected/static data and trusted runtime authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production Go source -> static coverage evidence | Source structure is interpreted as proof that guards/effects exist. |
| Runtime descriptor -> authored authority | Descriptor capability claims must resolve through trusted startup authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-18-00 | Spoofing | Descriptor allowlist/effect mapping | high | mitigate | One exact specification map owns ID/effect/owner/boundary validation; missing/extra and wildcard mutations fail. |
| T-31.1-18-01 | Tampering | Static effect discovery | high | mitigate | Parse guard-to-effect AST structure and reject marker-preserving mutations. |
| T-31.1-18-02 | Elevation of Privilege | Optional authority join | high | mitigate | Require valid contract/profile/RuntimeAuthorities unconditionally. |
| T-31.1-18-03 | Spoofing | Descriptor capability | high | mitigate | Resolve each effect through CapabilityBindings and exact-match authored capability. |
</threat_model>

<verification>
- Run all three focused releasecontract selectors.
- Run `cd src/server &amp;&amp; go test ./internal/releasecontract -count=1`.
- Run `git diff --check`.
</verification>

<success_criteria>
- CR-01 allowlist, incomplete-manifest, structural, and optional-authority gaps are closed with mutation proof.
- No package install or second commitment/profile authority is introduced.
</success_criteria>

<commit_policy>
Create one atomic commit: `fix(31.1-18): complete structural effect contract`. Do not push. Plan 20 ends with a HARD EXTERNAL/MANUAL POST-CLOSEOUT GATE handoff; no GSD executor, verifier, tracker, hook, or autonomous orchestrator step owns final Stage B or push.
</commit_policy>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-18-SUMMARY.md` with seam/mutation counts and authority-negative matrix.
</output>
