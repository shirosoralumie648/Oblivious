---
phase: 31.1-readiness-fail-closed
plan: "12"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/internal/relay/router.go
  - src/server/internal/relay/router_test.go
autonomous: true
gap_closure: true
requirements: [RELS-01]
must_haves:
  truths:
    - "Relay resolves and requires the current server-owned model before semantic-cache lookup, cache success, usage, metrics, quota, or provider work (D-RDY-14, D-RDY-21)."
    - "A stale, blocked, deleted, disabled, or unknown model cannot return a cached successful response and produces zero usage/quota/provider deltas (D-RDY-16, D-RDY-22)."
    - "Cache misses retain the separate provider guard immediately before each provider callback; cache authorization does not pin a generation for later effects (D-RDY-14, D-RDY-16)."
  artifacts:
    - path: "src/server/internal/relay/router.go"
      provides: "Current model authorization before semantic-cache and retained immediate provider guard"
      contains: "RouteWithBilling"
    - path: "src/server/internal/relay/router_test.go"
      provides: "Expiry-on-cache-hit zero-call and provider-boundary regression"
      contains: "TestRelaySemanticCacheReadinessContract"
  key_links:
    - from: "Router.RouteWithBilling"
      to: "routerReadiness.requireModel"
      via: "model authorization precedes cache lookup and is repeated before provider effects"
      pattern: "requireModel"
    - from: "semantic cache hit"
      to: "usage/quota/metrics"
      via: "no success accounting is reachable after readiness denial"
      pattern: "semantic_cache"
---

<objective>
Close CR-05 by placing current model authority ahead of semantic-cache success while retaining immediate per-provider authorization.

Purpose: Cached content is still a billable AI result and cannot outlive the authored capability/catalog readiness state.
Output: Correct Relay ordering and focused stale-cache zero-call tests.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-VERIFICATION.md
@.planning/phases/31.1-readiness-fail-closed/31.1-05-SUMMARY.md
@src/server/internal/relay/router.go
@src/server/internal/relay/router_test.go

<constraints>
- Preserve distinct model/catalog and provider-effect guards; do not treat cache authorization as provider authorization.
- CR-06 strict Files routing and CR-09 persisted batch provider identity remain Phase 34.
- WR-02 remains Phase 35; readiness OpenAPI Envelope parity remains Phase 31.2.
- Evidence is repository-local E1 only; no provider-live or target claim is permitted.
</constraints>

<interfaces>
Keep `RouteWithBilling` and `routerReadiness.requireModel` signatures. The current model check must occur before reading a semantic cache. The existing second check immediately before `fn(ch)` remains mandatory because quota/rate-limit/usage work can cross a freshness boundary.
</interfaces>
</context>

## Artifacts this phase produces

- Relay semantic-cache authorization ordering.
- Denied cache-hit usage/quota/provider zero-call proof.
- Regression proof that provider dispatch still rechecks current readiness.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deny semantic-cache results before any success side effect</name>
  <files>src/server/internal/relay/router.go, src/server/internal/relay/router_test.go</files>
  <read_first>
    - src/server/internal/relay/router.go
    - src/server/internal/relay/router_test.go
    - .planning/phases/31.1-readiness-fail-closed/31.1-REVIEW.md
  </read_first>
  <behavior>
    - A seeded cache hit with stale readiness returns the stable readiness error.
    - Cache-success response, success usage, metrics, quota pre-consume/settle/refund, and provider counters remain zero.
    - Unknown/deleted model subjects fail before cache lookup can become an observable success.
  </behavior>
  <action>Move the first `requireModel(ctx, model)` to the start of `RouteWithBilling` after trusted metadata extraction and before `SemanticCacheRequestFromContext` lookup. Do not record a readiness denial as a successful or failed provider usage row. Add spies for usage, quota, provider, and cache-success metrics; seed a real in-memory semantic cache; advance the readiness guard to stale immediately before the request; assert the exact zero deltas.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/relay '^TestRelaySemanticCacheReadinessContract$'</automated>
  </verify>
  <acceptance_criteria>
    - Denied cache hit returns no cached response.
    - Usage, quota, provider, and success-metric counters remain unchanged.
    - Stable readiness codes contain no provider URL, credentials, or cache payload.
  </acceptance_criteria>
  <done>Semantic-cache success cannot bypass current catalog/readiness authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve immediate provider reauthorization on cache miss</name>
  <files>src/server/internal/relay/router.go, src/server/internal/relay/router_test.go</files>
  <read_first>
    - src/server/internal/relay/router.go
    - src/server/internal/relay/router_test.go
    - .planning/phases/31.1-readiness-fail-closed/31.1-PATTERNS.md
  </read_first>
  <behavior>
    - Cache miss can pass the first model check, expire during billing work, and be denied immediately before the provider callback.
    - The provider counter remains zero and existing safe quota refund bookkeeping is retained.
    - Retry/fallback attempts repeat both catalog and provider boundaries.
  </behavior>
  <action>Keep the existing pre-provider `requireModel` call adjacent to the provider callback and add an ordered guard spy that permits the pre-cache check but denies the later effect check. Assert no provider call and the existing bounded compensation behavior. Do not collapse the two checks or cache a verdict/generation in the Router.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/relay '^TestRelaySemanticCacheProviderRecheckContract$'</automated>
  </verify>
  <acceptance_criteria>
    - The guard trace contains a pre-cache model check and a later pre-provider check.
    - Expiry between the checks yields zero provider calls.
    - Existing cache-hit and cache-miss success tests continue to pass when readiness is current.
  </acceptance_criteria>
  <done>Cache and provider boundaries each consume current authority at their own side-effect point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted/cache data -> Relay response | Cached model output crosses into a billable success path. |
| Relay billing work -> provider callback | Time may advance after initial catalog authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-12-01 | Elevation of Privilege | Semantic-cache hit | high | mitigate | Require current server model before lookup/success and assert zero accounting on denial. |
| T-31.1-12-02 | Tampering | Cached model identity | high | mitigate | Resolve the submitted server-owned model through current RuntimeAuthorities, never cache capability authority. |
| T-31.1-12-03 | Repudiation | Usage/quota records | medium | mitigate | Denied cache responses produce no success record; focused counter assertions prove ordering. |
</threat_model>

<verification>
- Run both focused Relay selectors.
- Run `cd src/server &amp;&amp; go test ./internal/relay -count=1`.
- Run `git diff --check`.
</verification>

<success_criteria>
- Canonical cache gap is closed without changing deferred Files or batch-provider behavior.
- Both cache and provider effect boundaries have explicit current-generation tests.
</success_criteria>

<commit_policy>
Create one atomic commit after verification: `fix(31.1-12): authorize relay cache hits`. Do not push. Plan 20 ends with a HARD EXTERNAL/MANUAL POST-CLOSEOUT GATE handoff; no GSD executor, verifier, tracker, hook, or autonomous orchestrator step owns final Stage B or push.
</commit_policy>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-12-SUMMARY.md` with exact counter evidence and repository-local claim limits.
</output>
