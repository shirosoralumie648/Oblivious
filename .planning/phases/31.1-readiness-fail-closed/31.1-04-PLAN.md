---
phase: 31.1-readiness-fail-closed
plan: "04"
type: execute
wave: 2
depends_on: ["31.1-01"]
files_modified:
  - src/server/internal/schedule/worker.go
  - src/server/internal/schedule/worker_test.go
  - src/server/internal/channel/service_legacy.go
  - src/server/internal/channel/service_test.go
  - src/server/internal/relay/batch_polling_worker.go
  - src/server/internal/relay/batch_polling_worker_test.go
  - src/server/internal/channel/archive.go
  - src/server/internal/channel/archive_test.go
  - src/server/internal/http/workflow_executors.go
autonomous: true
requirements: [RELS-01]
must_haves:
  truths:
    - "每个 worker 在 claim 前及每个不可逆 effect 前重新调用同一 manager-backed guard；通过过的 generation 不会被 pin（D-RDY-11, D-RDY-13, D-RDY-16）。"
    - "claim 后 readiness 失效允许现有幂等 release/fail/retry bookkeeping，但 Provider/tool/channel/object/execution/settlement 新 effect 为零（D-RDY-13, D-RDY-16）。"
    - "channel retry 的每次 Send 和 archive sink/source mutation 都重新 guard；ingress 或 loop-entry success 不授权后续 dispatch（D-RDY-14）。"
    - "`workflowToolExecutorAdapter` 只保留 injected executor delegation/fail-closed seam，不构造 compatibility/global ToolExecutor；Plan 10 提供 authority-required executor，Plan 02 完成 composition（D-RDY-11, D-RDY-20）。"
  artifacts:
    - path: "src/server/internal/schedule/worker.go"
      provides: "Guarded scheduled claim plus workflow/agent effects"
    - path: "src/server/internal/relay/batch_polling_worker.go"
      provides: "Guarded batch claim/retrieve/finalizer transitions"
    - path: "src/server/internal/channel/service_legacy.go"
      provides: "Guarded initial/retry channel transport"
    - path: "src/server/internal/channel/archive.go"
      provides: "Guarded archive list/sink/delete effects"
    - path: "src/server/internal/http/workflow_executors.go"
      provides: "Workflow adapter injection seam with no local ToolExecutor construction"
  key_links:
    - from: "src/server/internal/schedule/worker.go"
      to: "src/server/internal/releasecontract/readiness.go"
      via: "BoundaryWorkerClaim and BoundaryWorkerEffect"
      pattern: "BoundaryWorker"
    - from: "src/server/internal/channel/service_legacy.go"
      to: "src/server/internal/channel/webhook_adapter.go"
      via: "guard immediately before outboundDeliverer.DeliverOutbound"
      pattern: "DeliverOutbound"
---

<objective>
为 scheduled task、channel retry、Relay batch polling 和 channel archive 建立 claim/effect 双 guard，并证明失效发生在 claim 后时不会产生新外部或不可逆 effect。

Purpose: 关闭 background work 绕过 ingress readiness 的路径，同时保留现有安全 bookkeeping。
Output: 四个 worker family 的 constructor-injected guard、stable descriptors 与 zero-call tests。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-01-SUMMARY.md
@src/server/internal/schedule/worker.go
@src/server/internal/channel/service_legacy.go
@src/server/internal/relay/batch_polling_worker.go
@src/server/internal/channel/archive.go
@src/server/internal/http/workflow_executors.go

<interfaces>
Every constructor consumes the exact Plan 31.1-01 `Guard`, `EffectRegistrar`, and startup-built carrier:

```go
type EffectID string
type CapabilityBindings struct { /* private immutable lookup */ }
func (b CapabilityBindings) Resolve(effect EffectID) (CapabilityID, error)
type RuntimeAuthorities struct {
    CatalogAuthorizer CatalogAuthorizer
    CapabilityBindings CapabilityBindings
}
func NewRuntimeAuthorities(contract AuthoredContractV1, profile DeploymentProfile, guard Guard) (RuntimeAuthorities, error)
```

Worker constructors receive that same value and resolve only typed Plan 01 `EffectID` constants. Missing/unknown mappings fail construction; no worker reloads contract/profile, hard-codes a capability ID, uses package-global authority, or installs a local deny-all substitute.
</interfaces>
</context>

## Artifacts this phase produces

- Modified files: the eight existing worker/source test files plus the workflow ToolExecutor adapter listed in frontmatter.
- New constructor/config fields: `Guard releasecontract.Guard`, `Authorities releasecontract.RuntimeAuthorities`, and `Effects releasecontract.EffectRegistrar`; claim/effect IDs come only from `Authorities.CapabilityBindings.Resolve(typedEffectID)`.
- New top-level suites: `TestScheduledTaskReadinessGuardContract`, `TestChannelRetryAndArchiveReadinessGuardContract`, `TestBatchPollingReadinessGuardContract`.

<tasks>

<task type="auto">
  <name>Task 1: Guard scheduled claims and workflow/agent execution effects</name>
  <read_first>
    - src/server/internal/schedule/worker.go
    - src/server/internal/schedule/worker_test.go
    - src/server/internal/schedule/service.go
    - src/server/internal/releasecontract/readiness.go
    - src/server/internal/http/workflow_executors.go
    - src/server/internal/http/workflow_executor_test.go
  </read_first>
  <files>src/server/internal/schedule/worker.go, src/server/internal/schedule/worker_test.go, src/server/internal/http/workflow_executors.go</files>
  <behavior>
    - Test V31.1-R05: denial before claim makes ClaimDueScheduledTaskRuns, workflow starter and agent starter call counts zero.
    - Test V31.1-R05/T-RDY-03: after claim, clock expiry blocks startClaimedWorkflow, RunExecutionUntilBlocked, and StartRun independently.
    - Test V31.1-R05: denial may mark/release the claimed run through existing idempotent store semantics but cannot start another target.
    - Existing `TestWorkflowToolExecutorAdapterRunsBuiltinTool` continues to prove delegation through an explicitly supplied executor; adapter source contains no production executor constructor or global fallback.
  </behavior>
  <action>Inject the shared Guard/RuntimeAuthorities/EffectRegistrar into Worker/Service and resolve typed schedule-claim/workflow-effect/agent-effect IDs during construction; missing mappings fail before a loop starts. Call `BoundaryWorkerClaim` immediately before `ClaimDueScheduledTaskRuns`; for every claimed item call current `BoundaryWorkerEffect` immediately before workflow create, workflow continue, and agent StartRun. Do not pass a snapshot/generation into the job. On denial, invoke only existing fail/release bookkeeping, store the stable readiness code, and continue without target calls. Register distinct descriptors from those bindings. Keep `workflowToolExecutorAdapter` as an injected delegation-only seam: it may call the supplied executor but may not instantiate `agent.NewToolExecutor`, read a global, or provide a zero-authority production fallback. Plan 10 supplies the authorized implementation and Plan 02 owns final composition. Tests prove startup-carrier preservation and zero contract/profile/global lookups. Only after the production changes above are complete, add and run `TestScheduledTaskReadinessGuardContract` as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Tables cover disabled/blocked/stale/unknown/unavailable/identity mismatch/nil guard with zero downstream effect calls.
    - A fake clock expires between claim and each target effect and proves re-evaluation rather than generation pinning.
    - Existing success/failure scheduling semantics remain passing.
  </acceptance_criteria>
  <verify><automated>bash scripts/run-go-tests-matched.sh ./internal/schedule '^TestScheduledTaskReadinessGuardContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./internal/http '^TestWorkflowToolExecutorAdapterRunsBuiltinTool$' &amp;&amp; git diff --check</automated></verify>
  <done>V31.1-R05/T-RDY-03 proves scheduled claim/effect denial at the actual boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Guard channel retry sends and archive object/source effects</name>
  <read_first>
    - src/server/internal/channel/service_legacy.go
    - src/server/internal/channel/service_test.go
    - src/server/internal/channel/archive.go
    - src/server/internal/channel/archive_test.go
    - src/server/internal/channel/webhook_adapter.go
  </read_first>
  <files>src/server/internal/channel/service_legacy.go, src/server/internal/channel/service_test.go, src/server/internal/channel/archive.go, src/server/internal/channel/archive_test.go</files>
  <behavior>
    - Test V31.1-R05/R07: retry claim denial reads/updates/sends zero times; expiry after claim blocks every Service.Send/DeliverOutbound attempt.
    - Test V31.1-R05: archive claim/list, sink write, and source delete each read current guard; expiry between them prevents later effects.
    - Test T-RDY-03: safe retry/release metadata may change on denial, but transport/sink/delete counters remain zero where not yet authorized.
  </behavior>
  <action>Add Guard/RuntimeAuthorities/Effects fields to channel Service and ArchiveWorker, then resolve typed retry-claim/channel-send/archive-write/archive-delete IDs once from `Authorities.CapabilityBindings`; any missing mapping fails construction. Guard retry claim before `ClaimDueRetryMessages`, then guard each outbound dispatch immediately before `DeliverOutbound`, including fallback and retries. Guard archive list as claim, then independently guard sink `ArchiveMessageLogs` and source `DeleteArchivedMessageLogs`; never treat a prior check as permission for the next step. Preserve existing retry/dead-letter bookkeeping only after explicit denial and never call channel-health outbound remediation. Use only the startup carrier, not a catalogBindings reload, local constant, global or deny-all fallback. Only after the production changes above are complete, add and run `TestChannelRetryAndArchiveReadinessGuardContract` as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Fake clocks expire between claim/send, list/sink, and sink/delete with exact zero-call assertions.
    - Initial sends and retry/fallback sends all hit the same current manager guard.
    - Existing archive and retry behavior passes when every guard check authorizes.
  </acceptance_criteria>
  <verify><automated>bash scripts/run-go-tests-matched.sh ./internal/channel '^TestChannelRetryAndArchiveReadinessGuardContract$' &amp;&amp; git diff --check</automated></verify>
  <done>V31.1-R05/V31.1-R07 and T-RDY-03 cover channel/archive claim plus each irreversible effect.</done>
</task>

<task type="auto">
  <name>Task 3: Guard batch claim, provider retrieval and terminal finalizers</name>
  <read_first>
    - src/server/internal/relay/batch_polling_worker.go
    - src/server/internal/relay/batch_polling_worker_test.go
    - src/server/internal/relay/router.go
    - src/server/internal/releasecontract/readiness.go
  </read_first>
  <files>src/server/internal/relay/batch_polling_worker.go, src/server/internal/relay/batch_polling_worker_test.go</files>
  <behavior>
    - Test V31.1-R05: claim denial calls ClaimBatchPollingJobs/RetrieveBatch/finalizers zero times.
    - Test V31.1-R05/R06: expiry after claim blocks RetrieveBatch; expiry after retrieval blocks completion/failure finalizer and terminal state effect.
    - Test T-RDY-03/T-RDY-08: denial permits only bounded retry/release bookkeeping and never settles/refunds or marks a provider result as dispatched/completed.
  </behavior>
  <action>Extend config/worker with Guard, the same RuntimeAuthorities and descriptors; resolve typed batch-claim/retrieve/finalize IDs from `Authorities.CapabilityBindings` and fail construction if any mapping is absent. Require `BoundaryWorkerClaim` before store claim, `BoundaryOutbound` before each `RetrieveBatch`, and `BoundaryWorkerEffect` before each completion/failure finalizer and irreversible succeeded/dead-letter transition. Read the manager anew at every check. A readiness denial records a stable retry/failure state only when existing idempotent semantics require it; it cannot invoke usage/quota settlement/refund as if provider work occurred. No local contract read, hard-coded ID, global authority or constructed fallback is permitted. Only after the production changes above are complete, add and run `TestBatchPollingReadinessGuardContract` as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Named table covers every denial class and expiry at each transition with independent spy counts.
    - No retained generation authorizes a later batch step.
    - Existing pricing/usage/quota finalizer tests remain passing under an authorizing guard.
  </acceptance_criteria>
  <verify><automated>bash scripts/run-go-tests-matched.sh ./internal/relay '^TestBatchPollingReadinessGuardContract$' &amp;&amp; git diff --check</automated></verify>
  <done>V31.1-R05/V31.1-R06 and T-RDY-03/T-RDY-08 close batch claim/retrieval/finalization bypasses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| persisted jobs -> worker claim | A background loop acquires work that may later expire. |
| claimed work -> external/irreversible effect | Current readiness must be checked for every new effect. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-RDY-03 | Elevation of Privilege | worker generation pinning | high | mitigate | Claim/effect checks, fake-clock expiry between steps, explicit descriptors, and zero-call spies. |
| T-RDY-04 | Denial of Service | worker shutdown | medium | mitigate | Context-owned loops and bounded denial bookkeeping; Plan 31.1-02 owns lifecycle shutdown. |
| T-RDY-08 | Tampering | finalizer/financial state | high | mitigate | Separate finalizer guard and prohibition on provider-like terminal state after denial. |
</threat_model>

<verification>
- Run each exact suite through the declared helper; no static token scan substitutes for spy counts.
- D-RDY-16 permits only existing idempotent/cancellation bookkeeping, never a new external or irreversible effect.
</verification>

<success_criteria>
- All four worker families re-read current manager before claim and every effect.
- Expiry between steps yields zero new effects without permanently locking work.
- Runtime descriptors identify claim/effect boundaries for Plan 31.1-03 exact join.
- Every worker family receives the same startup RuntimeAuthorities and resolves typed mappings without a local authority path.
</success_criteria>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-04-SUMMARY.md` when done.
</output>
