---
phase: 31.1-readiness-fail-closed
plan: "15"
subsystem: agent
tags: [readiness, fail-closed, agent-tools, authorization, ast, tdd]

# Dependency graph
requires:
  - phase: 31.1-readiness-fail-closed
    provides: "RuntimeAuthorities, catalog Guard, effect bindings, and the authorized Agent ToolExecutor constructor"
provides:
  - "Source-compatible compatibility ToolExecutor that denies all five effect categories before downstream calls"
  - "Authority-backed Agent and HTTP test fixtures using the real authorized constructors"
  - "AST/count contracts for production constructor selection and every remaining compatibility call"
  - "Mutation-backed constructor inventory for aliases, parenthesized calls, and direct ToolExecutor composite literals"
affects: [31.1-19 production effect registry, 31.1-20 phase closeout, RELS-01]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Compatibility constructors retain source shape but provide deny-only values when runtime authority is absent"
    - "Effect behavior tests construct real authored authority through test-only helpers instead of private-field or allow-all bypasses"

key-files:
  created:
    - src/server/internal/agent/executor_test_helpers_test.go
  modified:
    - src/server/internal/agent/executor.go
    - src/server/internal/agent/executor_test.go
    - src/server/internal/agent/runner_skill_test.go
    - src/server/internal/agent/runner_react_test.go
    - src/server/internal/agent/service_test.go
    - src/server/internal/http/workflow_executor_test.go
    - src/server/internal/http/agent_runs_handler_test.go
    - src/server/internal/http/routes_agent_runs_test.go

key-decisions:
  - "NewToolExecutor remains source-compatible but authorizeTool returns readiness_unavailable whenever its runtime authority is absent."
  - "MCP dispatch now follows the same unconditional authorization-before-effect order as builtin, HTTP, local process, and sandbox effects."
  - "NewAuthorizedToolExecutor and NewServiceWithRuntimeOptions remain the only effect-capable constructors; test helpers call those real constructors with authored authority."
  - "The AST proof classifies only ToolExecutor construction and leaves full registry ownership/exactness to Plan 19."

patterns-established:
  - "Dynamic zero-call tests cover each effect category while AST counts prevent compatibility callers from drifting into behavior fixtures."
  - "HTTP package tests use one local authority fixture for workflow tools and indirect Agent service effects."

requirements-completed: []

coverage:
  - id: D1
    description: "The one-argument compatibility ToolExecutor returns readiness_unavailable before builtin, HTTP, process, sandbox, and MCP downstream calls."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/agent/executor_test.go#TestCompatibilityToolExecutorFailClosedContract"
        status: pass
      - kind: integration
        ref: "bash scripts/run-go-tests-matched.sh ./internal/agent '^TestCompatibilityToolExecutorFailClosedContract$|^TestProductionToolExecutorConstructionContract$'"
        status: pass
      - kind: integration
        ref: "go test -race ./internal/agent -run '^TestCompatibilityToolExecutorFailClosedContract$|^TestProductionToolExecutorConstructionContract$|^TestAuthorizedToolExecutorFixtureMigrationContract$' -count=1"
        status: pass
    human_judgment: false
  - id: D2
    description: "Intentional Agent and HTTP effect behavior fixtures use real authority-backed test constructors, and structural counts reject compatibility-call drift."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/agent/executor_test_helpers_test.go#TestAuthorizedToolExecutorFixtureMigrationContract"
        status: pass
      - kind: unit
        ref: "src/server/internal/agent/executor_test_helpers_test.go#TestToolExecutorConstructorInventoryMutationContract"
        status: pass
      - kind: integration
        ref: "bash scripts/run-go-tests-matched.sh ./internal/http '^TestWorkflowNodeExecutorRegistryIncludesRealLLMKnowledgeAndAgentExecutors$|^TestWorkflowToolExecutorAdapterRunsBuiltinTool$'"
        status: pass
      - kind: integration
        ref: "go test ./internal/agent ./internal/http -count=1"
        status: pass
      - kind: integration
        ref: "go test -race ./internal/http -run '^TestWorkflowNodeExecutorRegistryIncludesRealLLMKnowledgeAndAgentExecutors$|^TestWorkflowToolExecutorAdapterRunsBuiltinTool$' -count=1"
        status: pass
    human_judgment: false

# Metrics
duration: 42 min
completed: 2026-07-19
status: complete
---

# Phase 31.1 Plan 15: Fail-Closed Compatibility Tools Summary

**Compatibility Agent tools now deny every effect without runtime authority, while all intended behavior coverage uses explicit authored authority through real production-capable constructors.**

## Performance

- **Duration:** 42 min
- **Started:** 2026-07-19T09:38:00Z
- **Completed:** 2026-07-19T10:20:00Z
- **Tasks:** 2 completed
- **Files modified:** 9

## Accomplishments

- `NewToolExecutor(*mcp.Client)` remains source-compatible but returns stable `readiness_unavailable` before builtin, custom HTTP, local process, sandbox, or MCP effects; five independent downstream counters remain zero.
- MCP dispatch now always calls `authorizeTool` before accessing the MCP client, removing its former non-strict authorization skip.
- `newAuthorizedToolExecutorForTest` and the HTTP-package fixture load the real authored contract/profile, build `RuntimeAuthorities` with a live Guard, use duplicate-rejecting registrars and real HTTP clients, and call `NewAuthorizedToolExecutor` or `NewServiceWithRuntimeOptions`.
- Every intended Agent and HTTP behavior fixture, including both former `agent.NewToolExecutor(nil)` workflow callers and `TestWorkflowToolExecutorAdapterRunsBuiltinTool`, now uses explicit authority.
- AST/count tests enumerate the two production deny-only fallbacks and every retained one-argument test call, require the authorized production composition path, require the test helpers to call real authorized constructors, and reject any HTTP workflow compatibility caller.
- Spec-review hardening resolves local aliases for all three constructors, unwraps parenthesized constructor references and calls, rejects direct test `ToolExecutor` literals, and permits only the two canonical production literals in `executor.go`.

## Task Commits

The plan-required single implementation commit contains both task outcomes:

1. **Tasks 1-2: Fail closed compatibility tools and migrate authority-backed fixtures** - `21bdeb0` (`fix`)
2. **Spec review: Harden constructor inventory proof** - `83484ea` (`test`)

## TDD Evidence

- **RED behavior:** `TestCompatibilityToolExecutorFailClosedContract` failed in all five subtests because builtin, HTTP, process, sandbox, and MCP returned `err=<nil>` instead of `readiness_unavailable`; the configured downstream paths were reachable.
- **RED inventory:** `TestAuthorizedToolExecutorFixtureMigrationContract` listed every old effect-capable `NewToolExecutor` caller, including both calls in `workflow_executor_test.go`. The production contract initially also caught the expected function-name inventory mismatch before its expected names were corrected.
- **GREEN:** After the minimal authorization change and fixture migration, the two anchored Agent selectors, the HTTP workflow selector, focused race tests, full Agent/HTTP package tests, and scoped vet all passed.
- **Spec-review RED:** `TestToolExecutorConstructorInventoryMutationContract` found only the direct constructor call and missed the expected `compatibilityAlias` and `parenthesized` callers; authorized/service aliases and both composite literals were also absent from the collector.
- **Spec-review GREEN:** The mutation selector records direct, parenthesized, aliased, and parenthesized-alias calls at their actual function names, plus both `ToolExecutor{}` and `&ToolExecutor{}` forms. Existing exact inventories remain unchanged.
- No separate RED commit was created because the plan commit policy requires one atomic `fix(31.1-15)` implementation commit; the observed RED output is preserved here.

## Compatibility Call Classification

- Production retains exactly two one-argument calls: the `defaultPlanStepExecutor.ExecutePlanStep` and `Service.initRunner` fallbacks. Both now produce deny-only executors and cannot execute effects.
- The compatibility contract itself retains five calls, one per effect category.
- Retained non-effect calls cover production-disabled Python, tool-definition metadata, oversized source/argument rejection, and two tool-definition-only service tests.
- `workflow_executor_test.go` contains zero `agent.NewToolExecutor` calls.
- The production AST proof requires `NewServiceWithRuntimeOptions` in both HTTP composition paths and `NewAuthorizedToolExecutor` inside `NewServiceWithRuntimeOptions`.
- Production composite construction is exact: `executor.go:NewToolExecutor` and `executor.go:NewAuthorizedToolExecutor` each contain one canonical literal; every test package has zero direct `ToolExecutor` literals.

## Files Created/Modified

- `src/server/internal/agent/executor.go` - Denies missing authority and authorizes MCP before client access.
- `src/server/internal/agent/executor_test.go` - Five-category zero-call fail-closed contract and migrated effect behavior tests.
- `src/server/internal/agent/executor_test_helpers_test.go` - Real authority fixture plus production/test AST and count contracts.
- `src/server/internal/agent/runner_skill_test.go` - Authority-backed skill runner fixture.
- `src/server/internal/agent/runner_react_test.go` - Authority-backed ReAct runner fixtures.
- `src/server/internal/agent/service_test.go` - Authority-backed direct, runner, service, policy, and provider behavior fixtures.
- `src/server/internal/http/workflow_executor_test.go` - Shared HTTP authority options plus both required workflow migrations.
- `src/server/internal/http/agent_runs_handler_test.go` - Five indirect Agent effect fixtures use the authorized HTTP service helper.
- `src/server/internal/http/routes_agent_runs_test.go` - Three route-level indirect Agent effect fixtures use the authorized HTTP service helper.

## Decisions Made

- Missing authority has one stable error contract across all ToolExecutor effect categories; compatibility no longer means permissive.
- Test authority is explicit and realistic. No exported constructor, environment flag, package-global switch, private-field authority assignment, or allow-all Guard was introduced.
- Denied commercial web-search coverage constructs an authorized web-search tool and then removes provider availability through the existing setter, preserving the original disabled-before-provider-call assertion without inventing catalog authority.
- The structural proof intentionally does not assert Plan 19's global registry exactness or duplicate-owner join.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Migrated indirect HTTP Agent service effect fixtures**

- **Found during:** Full Agent/HTTP package verification after Tasks 1-2
- **Issue:** Eight handler/route tests constructed `agent.NewService`, whose compatibility runner correctly became deny-only and returned HTTP 500 before intended datetime effects.
- **Fix:** Reused the HTTP package's real-authority options fixture through `NewServiceWithRuntimeOptions` for only the eight failing effect fixtures.
- **Files modified:** `src/server/internal/http/agent_runs_handler_test.go`, `src/server/internal/http/routes_agent_runs_test.go`, `src/server/internal/http/workflow_executor_test.go`
- **Verification:** `go test ./internal/http -count=1`, focused HTTP selector, focused race, and scoped vet pass.
- **Committed in:** `21bdeb0`

**2. [Rule 3 - Blocking] Migrated indirect Agent Service and policy fixtures exposed by full regression**

- **Found during:** Full Agent package verification after direct caller migration
- **Issue:** Legacy `NewService` and zero-value `ToolExecutor` fixtures expected concrete effects or post-authorization commercial policy behavior and now correctly stopped at missing authority.
- **Fix:** Migrated only the failing effect fixtures to `NewServiceWithRuntimeOptions` or the real authorized ToolExecutor helper, retaining explicit compatibility calls for fail-closed and non-effect cases.
- **Files modified:** `src/server/internal/agent/service_test.go`, `src/server/internal/agent/executor_test.go`
- **Verification:** `go test ./internal/agent -count=1`, all anchored selectors, focused race, and scoped vet pass.
- **Committed in:** `21bdeb0`

**Total deviations:** 2 blocking test-fixture migrations required by the new fail-closed compatibility contract.
**Impact on plan:** No production scope expansion beyond the planned compatibility/MCP authorization change; additional files are test-only and preserve full package regression coverage.

## Issues Encountered

- The first focused race command was invoked from the repository root and failed with `go: cannot find main module`; it was rerun from `src/server` and passed.
- Strict catalog authorization rejects unauthored `lorem_ipsum` before commercial policy evaluation. The regression now asserts `capability_unknown` and zero downstream calls, matching the authored catalog boundary.

## Spec Review Fix

- **Finding:** MEDIUM - the first AST inventory recognized only direct `Ident`/`SelectorExpr` calls, so local constructor aliases, parenthesized calls, and direct `ToolExecutor` composite literals could bypass classification.
- **Fix:** Added per-function local alias resolution for `NewToolExecutor`, `NewAuthorizedToolExecutor`, and `NewServiceWithRuntimeOptions`; recursively unwraps `ParenExpr`; collects `ToolExecutor` composite literals; and enforces exact production/test literal inventories.
- **Non-vacuous proof:** Synthetic parsed source includes direct, parenthesized, aliased, parenthesized-alias, authorized alias, service alias, value composite, and pointer composite mutations. Counts are asserted at each actual caller.
- **Scope:** Test-only AST analysis. No `go/types`, production code, permissive private-field fixture, registry exactness claim, or tracking change.
- **Commit:** `83484ea`

## User Setup Required

None - no external service configuration required.

## Known Stubs

None.

## Next Phase Readiness

- Plan 31.1-15 closes WR-03 with repository-local E1 behavior and structural evidence; Plan 31.1-16 is next.
- Plan 19 still owns production registry ownership and full manifest/static/runtime/profile/authority exactness.
- `RELS-01` remains phase-level pending until all gap plans, full phase verification, clean-HEAD Stage B, and the hard manual push gate complete.
- No provider-live, target/live, payment, Kubernetes, migration, or commercial-readiness claim is made.

## Test Evidence

- Both exact Agent task selectors passed and matched their anchored tests.
- The exact constructor mutation selector passed and matched one concrete test.
- The exact HTTP workflow selector passed and matched two concrete tests.
- Focused Agent and HTTP `-race` commands passed.
- `cd src/server && go test ./internal/agent ./internal/http -count=1` passed.
- `cd src/server && go vet ./internal/agent ./internal/http` passed.
- `git diff --check` passed before the implementation commit.

---
*Phase: 31.1-readiness-fail-closed*
*Plan: 15*
*Completed: 2026-07-19*

## Self-Check: PASSED

- SUMMARY exists at the declared output path and implementation commit `21bdeb0` exists with no deletions.
- Spec-review test commit `83484ea` exists and changes only `executor_test_helpers_test.go`.
- All anchored selectors, both full packages, focused race, scoped vet, and diff check pass.
- Coverage classifies both deliverables as automated passing evidence.
- Protected user changes remain unstaged; `RELS-01` remains phase-level pending.
