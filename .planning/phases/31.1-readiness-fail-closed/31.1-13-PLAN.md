---
phase: 31.1-readiness-fail-closed
plan: "13"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/internal/http/router.go
  - src/server/internal/http/router_readiness_composition_test.go
  - src/server/internal/http/marketplace_handler.go
  - src/server/internal/http/billing_readiness_test.go
  - src/server/internal/admin/channel_service.go
  - src/server/internal/admin/service_test.go
autonomous: true
gap_closure: true
requirements: [RELS-01]
must_haves:
  truths:
    - "Strict Router composition always creates Admin through `admin.NewReadinessService` with the startup Guard, RuntimeAuthorities, and EffectRegistrar; missing authority or a caller-supplied `options.AdminService` fails before route publication (D-RDY-11, D-RDY-20, D-RDY-21)."
    - "Strict Marketplace checkout construction requires the same financial carrier before a handler can call local intent or provider checkout effects (D-RDY-15)."
    - "Denied Admin sync/detect/apply and Marketplace checkout make zero upstream reads/probes, store mutations, local orders, and provider calls through the production constructor helpers (D-RDY-14, D-RDY-15, D-RDY-22)."
    - "Admin SyncChannelModels, DetectChannelModelUpdates, and ApplyChannelModelUpdates require the generic model-mutation readiness guard before the first GetChannel or TestChannel; later model-specific resolution remains current and unknown returned models cause zero writes (D-RDY-14, D-RDY-22)."
  artifacts:
    - path: "src/server/internal/http/router.go"
      provides: "Strict authority-required Admin and Marketplace composition"
      contains: "NewReadinessService"
    - path: "src/server/internal/http/marketplace_handler.go"
      provides: "Fail-closed strict Marketplace handler constructor"
      contains: "withMarketplaceFinancialReadiness"
    - path: "src/server/internal/http/router_readiness_composition_test.go"
      provides: "Production constructor-carrier and denial zero-call proof"
      contains: "TestStrictRouterReadinessCompositionContract"
    - path: "src/server/internal/admin/channel_service.go"
      provides: "Readiness-before-read/probe Admin model mutation flow"
      contains: "SyncChannelModels"
  key_links:
    - from: "NewRouterWithOptions strict branch"
      to: "admin.NewReadinessService"
      via: "ModelCatalogRuntimeOptions is created from RouterOptions before Admin mutation routes exist"
      pattern: "NewReadinessService"
    - from: "strict Marketplace handler"
      to: "CheckoutCreator.CreateCheckoutSession"
      via: "billingFinancialReadiness.require runs before local/provider dispatch"
      pattern: "withMarketplaceFinancialReadiness"
---

<objective>
Close CR-02 and CR-03 by making strict Router construction authority-required for Admin model mutations and Marketplace provider checkout.

Purpose: Package-level readiness constructors already exist, but production composition currently omits them and silently permits nil carriers.
Output: Strict constructor wiring plus dynamic production-helper zero-call tests.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-VERIFICATION.md
@.planning/phases/31.1-readiness-fail-closed/31.1-05-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-07-SUMMARY.md
@src/server/internal/http/router.go
@src/server/internal/http/marketplace_handler.go
@src/server/internal/admin/service.go
@src/server/internal/admin/channel_service.go
@src/server/internal/http/billing_readiness_test.go

<constraints>
- Keep legacy/non-strict constructors source-compatible for isolated tests, but strict construction must never treat nil readiness as success.
- `RouterOptions.AdminService` is compatibility/test injection only. `NewReadinessRouterWithOptions` and the production `BuildRuntime` path must reject it rather than trusting an unproven service.
- Signed provider reconciliation remains reconciliation-only; do not add outbound behavior to webhook completion paths.
- CR-06 and CR-09 remain Phase 34, WR-02 remains Phase 35, and readiness OpenAPI Envelope parity remains Phase 31.2.
- Evidence is repository-local E1; no live payment or commercial-readiness claim.
</constraints>

<interfaces>
Use existing `admin.NewReadinessService(store, admin.ModelCatalogRuntimeOptions{Guard, Authorities, Effects}, opts...)` and `marketplace.FinancialReadiness{Guard, Authorities, Effects}`. The strict branch must construct Admin internally every time and reject non-nil `RouterOptions.AdminService`; compatibility tests may inject it only through the non-strict entrypoint, and an AST regression must prove no non-test production caller sets that field. Add a narrow strict Marketplace constructor/helper that returns `(marketplaceHandler, error)` after validating its readiness option; do not infer strictness from a nil receiver during request handling.
</interfaces>
</context>

## Artifacts this phase produces

- Strict Admin service composition with current model authority.
- Strict Marketplace handler composition with current financial authority.
- Router-owned denial tests covering upstream, store, intent, and provider counters.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make strict Admin construction authority-required</name>
  <files>src/server/internal/http/router.go, src/server/internal/http/router_readiness_composition_test.go, src/server/internal/admin/channel_service.go, src/server/internal/admin/service_test.go</files>
  <read_first>
    - src/server/internal/http/router.go
    - src/server/internal/admin/service.go
    - src/server/internal/admin/channel_service.go
    - src/server/internal/admin/service_test.go
    - .planning/phases/31.1-readiness-fail-closed/31.1-REVIEW.md
  </read_first>
  <behavior>
    - Strict composition without Guard, Authorities, or Effects fails before route publication.
    - Strict composition with a caller-supplied `options.AdminService`, including an allow-open spy service, fails before that service or any route dependency is called.
    - Strict composition registers `admin.channel.model.mutation` exactly once.
    - Readiness denial on sync, detect, and apply occurs before the first upstream read/probe and leaves all store-read/store-update counters at zero.
  </behavior>
  <action>In the strict Router branch, reject non-nil `options.AdminService` with a stable construction error, then always construct Admin with `admin.NewReadinessService` and the exact startup carrier before creating Marketplace or Admin handlers. Keep `AdminService` injection reachable only from the non-strict compatibility entrypoint used by `_test.go` callers; add an AST assertion that no non-test `internal/http` or `cmd/server` source sets the field and that `BuildRuntime` reaches only `NewReadinessRouterWithOptions`. In `admin/channel_service.go`, add a private generic model-mutation readiness check using the startup-bound `admin.channel.model.mutation` capability and ensure each public `SyncChannelModels`, `DetectChannelModelUpdates`, and `ApplyChannelModelUpdates` path performs that check before its first `GetChannel` or `TestChannel`; factor a private checked detect helper if needed so Apply cannot bypass the check or accidentally run it twice. Retain the later current `requireModels` calls after probe results and before persistence so unknown returned models still produce zero writes. Exercise a malicious allow-open injected service and require strict construction to fail with zero service/store/probe calls. Extend the real readiness-aware Admin mutation table with denial-first sync, detect, and apply cases and assert the guard runs before the first read/probe while all upstream reads, probes, and writes remain zero.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestStrictRouterAdminReadinessCompositionContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./internal/admin '^TestModelCatalogMutationContract$'</automated>
  </verify>
  <acceptance_criteria>
    - Production strict Router source reaches `admin.NewReadinessService`, never `admin.NewService`, for its default service.
    - Missing carrier and any caller-supplied Admin service fail strict construction before route publication or service calls.
    - Non-test production source contains no `RouterOptions.AdminService` injection.
    - Denied sync/detect/apply fail before the first GetChannel/TestChannel and produce no upstream read, probe, or persistence mutation.
  </acceptance_criteria>
  <done>Strict Admin model mutation cannot exist without the startup model/readiness authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make strict Marketplace checkout authority-required</name>
  <files>src/server/internal/http/router.go, src/server/internal/http/marketplace_handler.go, src/server/internal/http/router_readiness_composition_test.go, src/server/internal/http/billing_readiness_test.go</files>
  <read_first>
    - src/server/internal/http/router.go
    - src/server/internal/http/marketplace_handler.go
    - src/server/internal/http/billing_readiness_test.go
    - src/server/internal/marketplace/settlement.go
    - .planning/phases/31.1-readiness-fail-closed/31.1-07-SUMMARY.md
  </read_first>
  <behavior>
    - Strict Marketplace construction without financial readiness returns an error.
    - Router passes one startup `FinancialReadiness` to local settlement and provider checkout owners.
    - Denial leaves order intent, checkout provider, session update, and failed-state store counters at zero.
  </behavior>
  <action>Add a strict Marketplace handler constructor that applies checkout and financial readiness options, validates readiness configuration, and returns an error rather than retaining a nil/failed carrier. Use it from the strict Router path before route registration; keep the legacy value constructor only in non-strict compatibility paths. Build the router-level test with spy settlement/checkout/store dependencies and a denied guard, then call the same strict handler path and assert every downstream counter remains zero.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestStrictRouterMarketplaceReadinessCompositionContract$'</automated>
  </verify>
  <acceptance_criteria>
    - `withMarketplaceFinancialReadiness` is present in strict production construction before checkout handling.
    - Missing or invalid financial carrier prevents handler/route construction.
    - Denial causes zero local intent, provider, session, and failure-state calls.
  </acceptance_criteria>
  <done>Marketplace checkout is fail closed at both local intent and external provider boundaries in strict composition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Startup authority -> Router-owned services | Strict production composition decides whether readiness-aware services exist. |
| Marketplace request -> payment provider | Customer input can trigger local financial intent and an external checkout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-13-01 | Elevation of Privilege | Admin model mutation | high | mitigate | Strict constructor requires RuntimeAuthorities and denial-first zero-call tests cover sync/detect/apply. |
| T-31.1-13-04 | Spoofing | Caller-supplied Admin service | high | mitigate | Strict composition rejects injected services and AST proof confines compatibility injection to `_test.go`. |
| T-31.1-13-02 | Elevation of Privilege | Marketplace checkout | high | mitigate | Strict handler construction requires FinancialReadiness before routes/effects exist. |
| T-31.1-13-03 | Repudiation | Checkout state | high | mitigate | Denial asserts no premature order/session/failure mutation that could imply dispatch. |
</threat_model>

<verification>
- Run the three focused selectors and `cd src/server &amp;&amp; go test ./internal/http ./internal/admin -count=1`.
- Run `git diff --check`.
- Inspect strict versus compatibility branches in the diff.
</verification>

<success_criteria>
- Canonical Admin and Marketplace composition gaps are closed with construction failure and zero-call evidence.
- No signed reconciliation, deferred finance routing, or public API surface is expanded.
</success_criteria>

<commit_policy>
Create one atomic commit: `fix(31.1-13): wire strict admin and marketplace readiness`. Do not push. Plan 20 ends with a HARD EXTERNAL/MANUAL POST-CLOSEOUT GATE handoff; no GSD executor, verifier, tracker, hook, or autonomous orchestrator step owns final Stage B or push.
</commit_policy>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-13-SUMMARY.md` with exact constructor and zero-call evidence.
</output>
