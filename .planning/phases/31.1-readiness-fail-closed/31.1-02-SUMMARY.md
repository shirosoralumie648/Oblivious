---
phase: 31.1-readiness-fail-closed
plan: "02"
subsystem: runtime-composition
tags: [go, startup, readiness, lifecycle, immutable-handoff, fail-closed]
requires:
  - phase: 31.1-readiness-fail-closed
    provides: "ReadinessManager, Guard, EffectRegistrar, RuntimeAuthorities, catalog authority, and strict consumer constructors from Plans 01/04/05/06/07/10"
provides:
  - "One explicit immutable contract/profile/build preflight handoff for server, agent, billing, channel, and chat roots"
  - "Side-effect-free BuildRuntime with strict authority composition and explicit StartBackground/Close lifecycle ownership"
  - "Ordered server startup with DB ping and migrations before listener, generation-one bootstrap before authority construction, and listener-before-refresh/workers"
affects: [31.1-03, 31.1-09, release-contract, deployment]
tech-stack:
  added: []
  patterns:
    - "RunEntrypoint resolves the authored contract, committed profile, and trusted build identity once, then exposes only defensive value copies"
    - "Runtime construction allocates strict consumers without starting goroutines; one lifecycle owner starts and cancels all background loops"
    - "Startup failures return before the next mutable dependency and preserve the primary failure boundary"
key-files:
  created:
    - .planning/phases/31.1-readiness-fail-closed/31.1-02-SUMMARY.md
  modified:
    - src/server/pkg/config/common.go
    - src/server/internal/config/config.go
    - src/server/cmd/server/main.go
    - src/server/cmd/server/main_test.go
    - src/server/cmd/agent/main.go
    - src/server/cmd/billing/main.go
    - src/server/cmd/channel/main.go
    - src/server/cmd/chat/main.go
    - src/server/internal/http/server.go
    - src/server/internal/http/router.go
    - src/server/internal/relay/relay.go
key-decisions:
  - "The source-authored entrypoint literal is supplied at each real root's first fallible statement; no executable name, argv, cwd, audit file, or defaultProfile can select it."
  - "BuildRuntime is the production composition owner; legacy NewServer remains only as a side-effect-free compatibility constructor for existing tests."
  - "Strict Relay, Router, Agent, MCP, financial, and channel constructors receive the same startup-built RuntimeAuthorities value; no local allow-all or replacement authority is installed."
  - "The default bounded bootstrap emits blocked observations until deployment-owned probes are supplied, keeping readiness fail-closed while preserving control-plane construction."
requirements-completed: [RELS-01]
coverage:
  - id: D1
    description: "Group-one roots reject missing, unknown, conditional, excluded, entrypoint-missing, and identity-mismatched startup before continuation/DB/listener/provider effects, and successful callbacks receive one defensive immutable handoff."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/cmd/server/main_test.go#TestStandaloneProfilePreflightGroupOneContract"
        status: pass
      - kind: unit
        ref: "bash scripts/run-go-tests-matched.sh ./cmd/server '^TestStandaloneProfilePreflightGroupOneContract$'"
        status: pass
    human_judgment: false
  - id: D2
    description: "Strict runtime construction validates RuntimeAuthorities without starting workers or health checks; StartBackground has one owner, rejects duplicates, and Close is idempotent."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/cmd/server/main_test.go#TestRuntimeBuildAndBackgroundLifecycleContract"
        status: pass
      - kind: integration
        ref: "go test ./internal/http ./internal/relay ./cmd/server -count=1"
        status: pass
    human_judgment: false
  - id: D3
    description: "Server startup consumes one handoff and records contract/config, DB open/ping, migrations, bootstrap generation 1, authority construction, BuildRuntime, listener, Serve, refresh, and background order with fail-closed cutoffs."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/cmd/server/main_test.go#TestServerStartupOrderContract"
        status: pass
      - kind: integration
        ref: "cd src/server && go test ./... -count=1"
        status: pass
    human_judgment: false
duration: 40 min
completed: 2026-07-18
status: complete
---

# Phase 31.1 Plan 02: Runtime Composition and Ordered Startup Summary

**Committed-profile preflight now hands one defensive contract/profile/build tuple into strict runtime composition, while server readiness bootstrap and all background effects follow listener ownership.**

## Performance

- **Duration:** approximately 40 min
- **Started:** 2026-07-18T05:55:00Z
- **Completed:** 2026-07-18T06:35:55Z
- **Tasks:** 3
- **Files modified:** 11 implementation/test files plus this summary

## Accomplishments

- Added shared `RunEntrypoint` policy and immutable `ResolvedEntrypointInputs`; all five group-one roots provide explicit source-authored IDs and no dependency construction occurs on denied preflight.
- Replaced production composition with `BuildRuntime` and explicit `StartBackground`/`Close` ownership. Strict Relay, Chat, Agent, MCP, financial, channel, schedule, batch, and archive consumers receive the startup authority carrier without constructor-time runs.
- Reworked server continuation around one handoff and explicit startup sequencing: DB open/ping, migrations, bounded generation-one readiness bootstrap, one `NewRuntimeAuthorities` call, BuildRuntime, synchronous listener, Serve, then refresh/workers.
- Added dynamic preflight, lifecycle, AST first-call, event-order, and failure-cutoff proofs in `cmd/server/main_test.go`.

## Task Commits

1. **Task 1: Reject untrusted or excluded startup before mutable dependencies** - `1349908`
2. **Task 2: Split server construction from background lifecycle** - `4114f21`
3. **Task 3: Enforce complete startup with one immutable preflight handoff** - `03ef62b`

**Plan metadata:** pending docs commit.

## Files Created/Modified

- `src/server/pkg/config/common.go` - shared preflight interfaces, validation, deep-copy handoff, and continuation runner.
- `src/server/internal/config/config.go` - internal config-family aliases and wrapper for the shared runner.
- `src/server/cmd/server/main.go` - trusted handoff continuation, startup dependency seams, bounded manager bootstrap, ordered listener lifecycle, and effect registry.
- `src/server/cmd/server/main_test.go` - dynamic group-one zero-call/AST proof, runtime lifecycle proof, and event-order/failure matrix.
- `src/server/cmd/agent/main.go`, `cmd/billing/main.go`, `cmd/channel/main.go`, `cmd/chat/main.go` - explicit source-authored preflight wrappers.
- `src/server/internal/http/server.go` - `Runtime`, `RuntimeOptions`, `BuildRuntime`, strict consumer composition, and lifecycle ownership.
- `src/server/internal/http/router.go` - strict router composition path that consumes the exact startup carrier.
- `src/server/internal/relay/relay.go` - strict `NewRelayWithOptions` predecessor constructor for authority-bound Relay routing.

## Decisions Made

- Resolver outputs are checked against the one loaded contract and identity digest before the continuation; accessors return isolated copies so callback mutation cannot alter later composition.
- The server's audit destination is deployment-owned through `OBLIVIOUS_READINESS_AUDIT_PATH`; the default manager refuses an absent path rather than selecting a local fallback.
- `Serve` returning `ErrServerClosed` is treated as a controlled termination and still drains runtime resources through `Close`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Missing critical predecessor surface] Added strict Relay constructor entrypoint**
- **Found during:** Task 2
- **Issue:** Plan 05 exposed strict Relay router constructors, but the top-level Relay builder had no way for composition to pass the startup authority carrier.
- **Fix:** Added `relay.NewRelayWithOptions` and routed it to `NewRouterWithBillingOptions`; health checks remain lifecycle-owned by `BuildRuntime`.
- **Files modified:** `src/server/internal/relay/relay.go`
- **Verification:** `go test ./internal/relay ./internal/http -count=1`
- **Committed in:** `4114f21`

**2. [Rule 3 - Missing critical predecessor wiring] Added strict Router consumer path and shared-service reuse**
- **Found during:** Task 2
- **Issue:** `RouterOptions` carried readiness fields but legacy router construction ignored them, rebuilding unguarded Agent/MCP/Chat/finance/channel paths.
- **Fix:** Added `NewReadinessRouterWithOptions`, strict consumer branches, and explicit reuse of the startup-built Agent/MCP/channel services to avoid duplicate descriptor registration.
- **Files modified:** `src/server/internal/http/router.go`, `src/server/internal/http/server.go`
- **Verification:** `go test ./internal/http ./internal/relay ./cmd/server -count=1`
- **Committed in:** `4114f21`

**Total deviations:** 2 auto-fixed (Rule 3 predecessor gaps). **Impact:** Required to make the declared Plan 01/04/05/06/07/10 strict constructor contracts effective in the production composition; no compatibility authority or allow-all fallback was introduced.

## Issues Encountered

- The mounted checkout remained writable for code and Git operations; no mirror pivot was needed. The user-owned `.planning/config.json` change stayed untouched and unstaged.
- Full verification is repository-local E1/E2 only. No live provider, payment, target deployment, or target migration evidence was available or claimed.

## User Setup Required

None - no external service configuration was changed. Production startup requires deployment-owned `OBLIVIOUS_DEPLOYMENT_PROFILE` and `OBLIVIOUS_READINESS_AUDIT_PATH` values; omitted values fail closed.

## Next Phase Readiness

- Plan 09 can apply the same `RunEntrypoint` callback contract to remaining standalone roots.
- Plan 03 can exact-join runtime descriptors now that strict router, worker, Relay, Agent, MCP, and finance composition paths are constructed from one authority carrier.
- Residual risk is limited to repository-local proof: the default bounded bootstrap intentionally publishes blocked observations until deployment supplies real dependency probes; no target/live readiness claim is made.

---
*Phase: 31.1-readiness-fail-closed*
*Completed: 2026-07-18*

## Self-Check: PASSED

- Summary file exists at the required phase path.
- Task commits `1349908`, `4114f21`, and `03ef62b` are present.
- `bash scripts/run-go-tests-matched.sh ./cmd/server '^TestStandaloneProfilePreflightGroupOneContract$|^TestRuntimeBuildAndBackgroundLifecycleContract$|^TestServerStartupOrderContract$'` passed.
- `cd src/server && go test ./... -count=1` passed with no failures.
- `git diff --check` passed and `.planning/config.json` remains intentionally unstaged.
