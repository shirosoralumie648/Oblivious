---
phase: 31.1-readiness-fail-closed
plan: "02"
type: execute
wave: 4
depends_on: ["31.1-01", "31.1-04", "31.1-05", "31.1-06", "31.1-07", "31.1-10"]
files_modified:
  - src/server/cmd/server/main.go
  - src/server/cmd/server/main_test.go
  - src/server/internal/http/server.go
  - src/server/internal/config/config.go
  - src/server/pkg/config/common.go
  - src/server/cmd/agent/main.go
  - src/server/cmd/billing/main.go
  - src/server/cmd/channel/main.go
  - src/server/cmd/chat/main.go
autonomous: true
requirements: [RELS-01]
must_haves:
  truths:
    - "Startup 顺序固定为 trusted contract/profile/build -> DB open/ping -> migration apply/ledger -> bounded bootstrap -> generation 1 -> listener/control plane -> workers/refresh（D-RDY-07）。"
    - "DB ping 或 migration failure 在 listener 前 fatal；其他 dependency failure 仍可发布 blocked generation 1（D-RDY-08, D-RDY-09）。"
    - "NewServer/BuildRuntime 构造不启动 Relay health checks 或 worker goroutine；background lifecycle 只在 listener 成功后显式启动并受 shutdown context 所有（D-RDY-10）。"
    - "excluded standalone profile 在 config/DB/listener/outbound 构造前由 Phase 31 committed-profile policy 拒绝（D-RDY-11）。"
    - "RunEntrypoint 将唯一一次解析出的完整 validated contract、resolver-returned profile 与 trusted build identity 封装为 immutable ResolvedEntrypointInputs 并按值交给 continuation；server 不得从 env/global/cwd/disk 重载，且只从该 handoff 构造一次 RuntimeAuthorities（D-RDY-01, D-RDY-07, D-RDY-11, D-RDY-20, D-RDY-21）。"
  artifacts:
    - path: "src/server/cmd/server/main.go"
      provides: "Explicit readiness-aware composition and listener ownership"
      contains: "net.Listen"
    - path: "src/server/internal/http/server.go"
      provides: "Side-effect-free BuildRuntime plus explicit StartBackground lifecycle"
      exports: ["Runtime", "RuntimeOptions", "BuildRuntime"]
    - path: "src/server/pkg/config/common.go"
      provides: "Shared authored-entrypoint preflight runner and immutable validated-input handoff for every root"
      exports: ["ResolvedEntrypointInputs", "EntrypointPreflightOptions", "RunEntrypoint"]
    - path: "src/server/cmd/server/main_test.go"
      provides: "Dynamic continuation-zero plus AST first-call proof for server and standalone group one"
      contains: "TestStandaloneProfilePreflightGroupOneContract"
  key_links:
    - from: "src/server/cmd/server/main.go"
      to: "src/server/internal/releasecontract/readiness_manager.go"
      via: "Bootstrap before listener and StartRefresh after listener"
      pattern: "Bootstrap|StartRefresh"
    - from: "src/server/internal/http/server.go"
      to: "src/server/internal/http/router.go"
      via: "RuntimeOptions passes the exact startup-built RuntimeAuthorities into RouterOptions and every service constructor"
      pattern: "RuntimeAuthorities|RouterOptions"
    - from: "src/server/pkg/config/common.go"
      to: "src/server/cmd/server/main.go"
      via: "RunEntrypoint passes one immutable ResolvedEntrypointInputs value into normalStartup; runMain consumes its contract/profile/identity without reload"
      pattern: "ResolvedEntrypointInputs"
---

<objective>
重构 monolith composition/lifecycle，使 trusted identity/profile 在 DB 前失败，bootstrap 在 listener 前完成，而所有 background effects 在 listener 成功后才启动。

Purpose: 关闭构造期 goroutine 和 excluded standalone entrypoint 两个最早的 authorization bypass。
Output: 可测试的 startup orchestrator、无后台副作用的 Runtime builder、显式 background owner 和共享 standalone preflight。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-PATTERNS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-01-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-04-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-05-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-06-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-07-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-10-SUMMARY.md
@.planning/phases/31-release-contract-current-baseline/31-03-PLAN.md
@src/server/cmd/server/main.go
@src/server/internal/http/server.go
@src/server/internal/http/router.go

<interfaces>
Consume `ReadinessManager`, `Guard`, and `Boundary` exactly from Plan 31.1-01. Consume Phase 31 `IdentityProvider.Resolve(ctx, repoRoot, contractPath, schemaPath)` and `ProfileResolver.ResolveCommittedProfile(ctx, repoRoot, contractPath, schemaPath, profileID)` with explicit paths.

```go
type Runtime struct {
    Server *http.Server
    StartBackground func(context.Context) error
    Close func(context.Context) error
}
type RuntimeOptions struct {
    Readiness releasecontract.ReadinessManager
    Guard releasecontract.Guard
    Effects releasecontract.EffectRegistrar
    Authorities releasecontract.RuntimeAuthorities
}
func BuildRuntime(config.Config, *sql.DB, RuntimeOptions) (*Runtime, error)
type EntrypointPreflightOptions struct {
    RepoRoot, ContractPath, SchemaPath, ProfileID string
    Contracts ContractLoader
    Identities buildinfo.IdentityProvider
    Profiles releasecontract.ProfileResolver
}
type ContractLoader interface {
    Load(context.Context, string, string, string) (releasecontract.AuthoredContractV1, error)
}
type ResolvedEntrypointInputs struct {
    contract releasecontract.AuthoredContractV1
    profile releasecontract.DeploymentProfile
    identity buildinfo.BuildIdentityV1
}
func (i ResolvedEntrypointInputs) Contract() releasecontract.AuthoredContractV1
func (i ResolvedEntrypointInputs) Profile() releasecontract.DeploymentProfile
func (i ResolvedEntrypointInputs) Identity() buildinfo.BuildIdentityV1
type EntrypointContinuation func(context.Context, ResolvedEntrypointInputs) error
func RunEntrypoint(ctx context.Context, id releasecontract.EntrypointID, options EntrypointPreflightOptions, normalStartup EntrypointContinuation) error
```

The authority carrier is consumed verbatim from Plan 01:

```go
type EffectID string
type CapabilityBindings struct { /* private immutable lookup */ }
func (b CapabilityBindings) Resolve(effect EffectID) (CapabilityID, error)
type RuntimeAuthorities struct {
    CatalogAuthorizer CatalogAuthorizer
    CapabilityBindings CapabilityBindings
}
func NewRuntimeAuthorities(contract AuthoredContractV1, profile DeploymentProfile, guard Guard) (RuntimeAuthorities, error)
```
</interfaces>
</context>

## Artifacts this phase produces

- Modified files: the nine paths in `files_modified`; Phase 31-created `cmd/server/main_test.go` is extended, not assumed present before Phase 31 execution.
- New exported symbols: `Runtime`, `RuntimeOptions`, `BuildRuntime`, `ContractLoader`, immutable `ResolvedEntrypointInputs`, `EntrypointContinuation`, `EntrypointPreflightOptions`, and config-family `RunEntrypoint`; `RuntimeOptions{Readiness,Guard,Effects,Authorities}` passes the exact value into Plan 01 `RouterOptions{Readiness,Guard,Effects,Authorities}`.
- `ResolvedEntrypointInputs` stores the complete validated `AuthoredContractV1`, resolver-returned `DeploymentProfile`, and trusted `BuildIdentityV1` behind private fields; its accessors return defensive value copies, so a root receives one provenance-bound handoff without shared mutable collections, path/global lookup, or reload.
- Source-authored entrypoint literals owned here: `server`, `agent`, `billing`, `channel`, and `chat`; Plan 31.1-09 owns the remaining eight standalone roots.
- New runtime inputs: explicit `OBLIVIOUS_DEPLOYMENT_PROFILE`; contract/schema/audit locations are deployment-owned paths, never identity or verdict inputs. The authored `defaultProfile` is never a runtime fallback.

<tasks>

<task type="auto">
  <name>Task 1: Reject untrusted or excluded startup before mutable dependencies</name>
  <read_first>
    - src/server/cmd/server/main.go
    - src/server/internal/config/config.go
    - src/server/pkg/config/common.go
    - src/server/cmd/agent/main.go
    - src/server/cmd/billing/main.go
    - src/server/cmd/channel/main.go
    - src/server/cmd/chat/main.go
    - src/server/cmd/server/main_test.go
    - src/server/internal/releasecontract/load.go
    - src/server/internal/buildinfo/provider.go
  </read_first>
  <files>src/server/cmd/server/main.go, src/server/internal/config/config.go, src/server/pkg/config/common.go, src/server/cmd/agent/main.go, src/server/cmd/billing/main.go, src/server/cmd/channel/main.go, src/server/cmd/chat/main.go, src/server/cmd/server/main_test.go</files>
  <behavior>
    - Test V31.1-R03: each real group-one root supplies its exact source-authored entrypoint ID as the first fallible call; missing/mismatched identity, unknown/conditional/excluded profile, or profile without that entrypoint leaves its continuation/DB/listener/provider spies at zero.
    - Test V31.1-R09: monolith is accepted only when explicitly supplied and resolver-confirmed committed; candidate assets never select a profile.
    - Test T-RDY-01/T-RDY-03: environment may select a profile ID/path but cannot supply entrypoint/commit/tree/digest/dirty identity; argv, executable name/path and audit snapshot cannot select an entrypoint.
    - Test V31.1-R03/T-RDY-01: ContractLoader, ProfileResolver, and IdentityProvider are each invoked exactly once; the successful continuation receives one `ResolvedEntrypointInputs` whose complete contract/profile/identity equal those three validated results and whose defensive accessors cannot mutate the retained handoff.
  </behavior>
  <action>Implement one shared `RunEntrypoint(ctx,id,options,normalStartup)` policy behind thin internal/pkg config wrappers: require explicit repoRoot/contract/schema/profile and non-nil `ContractLoader`, `IdentityProvider`, and `ProfileResolver`; invoke each exactly once; validate that the trusted identity and resolver-returned committed profile belong to the one complete loaded `AuthoredContractV1`; call Plan 01 `RequireProfileEntrypoint` with the source-provided ID; freeze the complete contract/profile/identity into private fields of one `ResolvedEntrypointInputs`; then invoke `normalStartup(ctx, inputs)` once only on success. Accessors return defensive deep value copies for collection-bearing contract/profile values, and there is no constructor or setter available to callers. Refactor the real `server`, `agent`, `billing`, `channel`, and `chat` mains in this task: each first fallible statement passes its literal authored ID to the runner, and its complete current config, DB, migration, listener, worker, gRPC and outbound startup body is enclosed in the two-argument continuation. For `server`, introduce the outer literal wrapper now, before this task's suite runs; Task 3 may refactor only the continuation internals and must preserve this first-call boundary and captured handoff. The suite dynamically executes the runner for every ID with loader/resolver/identity/continuation/DB/listener/provider counters, asserts 1/1/1 resolution and exact immutable handoff on success, and uses Go AST only to bind that already-dynamic contract to each live main's first statement/literal; AST is never accepted without the zero-call execution table. Reject reload or derivation from environment, globals, cwd, argv, `os.Executable`, binary basename, asset existence, audit snapshot, or defaultProfile. Plan 09 applies this exact runner/callback contract to all remaining roots before Plan 03 exact-join verification. Only after the production changes above are complete, add and run automated verification with `TestStandaloneProfilePreflightGroupOneContract` in `cmd/server/main_test.go`.</action>
  <acceptance_criteria>
    - The exact suite dynamically asserts zero continuation/DB/listener/provider calls for every group-one root under omitted, unknown, conditional, excluded, entrypoint-missing and identity-mismatched cases.
    - Exact validated contract, trusted identity, resolver-confirmed profile plus matching authored entrypoint invoke their three resolvers once and reach that root's continuation once with the same immutable `ResolvedEntrypointInputs` value.
    - AST first-call/literal binding and dynamic zero-call proof both pass for all five real roots, including `server`; neither source order alone nor generic profile resolution is sufficient.
    - Mutating a contract/profile copy returned by the callback accessors cannot alter a second accessor result or the values later consumed by server composition.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./cmd/server '^TestStandaloneProfilePreflightGroupOneContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.1-R03/V31.1-R09 and T-RDY-01/T-RDY-03 deny excluded/mismatched startup before effects.</done>
</task>

<task type="auto">
  <name>Task 2: Split server construction from background lifecycle</name>
  <read_first>
    - src/server/internal/http/server.go
    - src/server/cmd/server/main_test.go
    - src/server/internal/http/router.go
    - src/server/internal/schedule/worker.go
    - src/server/internal/relay/batch_polling_worker.go
    - src/server/internal/channel/archive.go
    - .planning/phases/31.1-readiness-fail-closed/31.1-04-SUMMARY.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-05-SUMMARY.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-06-SUMMARY.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-07-SUMMARY.md
    - .planning/phases/31.1-readiness-fail-closed/31.1-10-SUMMARY.md
  </read_first>
  <files>src/server/internal/http/server.go, src/server/cmd/server/main_test.go</files>
  <behavior>
    - Test V31.1-R03/T-RDY-04: BuildRuntime creates router/services/workers but starts zero goroutines, health checks, provider calls, claims, or archive effects.
    - Test V31.1-R03: StartBackground starts each enabled loop exactly once under the supplied context; cancellation/shutdown stops all and Close is idempotent.
    - Test V31.1-R03: a blocked bootstrap can still build the control plane while customer effects remain guard-denied.
    - Test V31.1-R03/T-RDY-07: BuildRuntime rejects zero/mismatched RuntimeAuthorities and proves the exact startup-created value reaches RouterOptions, Relay/Chat, worker, MCP/tool/web-search and finance constructor spies without local contract/profile reads.
    - Test V31.1-R03/T-RDY-07: every Plan 04/05/06/07 and Plan 10 production constructor already compiles and passes its independent package contract before BuildRuntime wiring; composition uses those exact APIs and introduces no compatibility constructor or same-wave file dependency.
  </behavior>
  <action>Consume the completed independent package constructor surfaces from Plans 04/05/06/07/10; if a declared constructor differs, correct that owning predecessor rather than adding a compatibility overload in `server.go`. Replace effectful `NewServer` ownership with `BuildRuntime`. Move Relay `StartHealthChecks`, schedule worker, batch polling, channel retry, and archive goroutine starts into one `StartBackground(ctx)` closure; construction may allocate clients/workers and register shutdown callbacks but cannot call their Run/Start methods or make Provider calls. Require non-nil manager/guard/effect registrar and a nonzero `RuntimeAuthorities`; production construction fails rather than installing a local deny-all/global fallback. Pass `RuntimeOptions.Authorities` by value, unchanged, into exact `RouterOptions{Readiness,Guard,Effects,Authorities}` and the already-built worker, Relay/Chat, MCP/tool/web-search, live Agent `ToolExecutor`, and finance constructor APIs. Composition spies compare catalog/binding behavior, not pointer identity, and prove no consumer reloads contract/profile or substitutes hard-coded capability IDs. Tie all cancellations to the root context, preserve existing closers, make duplicate start an error, and make Close wait/cleanup without granting new effects. This plan is the sole `server.go` owner; Plan 10 owns only package constructors and live Agent dispatch, so there is no write overlap or reverse dependency. Only after the production changes above are complete, add and run `TestRuntimeBuildAndBackgroundLifecycleContract` in `cmd/server/main_test.go` as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Constructor spies remain zero through BuildRuntime success/failure, including Relay health and four worker families.
    - StartBackground has one owner, rejects duplicate start, and all loops stop after root cancellation/Close.
    - Existing production fail-closed Relay configuration remains intact; no development allow-all guard is introduced.
    - Missing/zero RuntimeAuthorities fails BuildRuntime before router/service construction; the valid carrier remains immutable and behaviorally identical at every consumer spy.
    - BuildRuntime compiles after each task against predecessor-owned constructor signatures; no task requires an uncompleted same-wave file or adds a local compatibility path.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./cmd/server '^TestRuntimeBuildAndBackgroundLifecycleContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>D-RDY-10 and V31.1-R03/T-RDY-04 have dynamic goroutine/effect ordering proof.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce complete startup with one immutable preflight handoff</name>
  <read_first>
    - src/server/cmd/server/main.go
    - src/server/cmd/server/main_test.go
    - src/server/internal/http/server.go
    - src/server/internal/db/db.go
    - src/server/internal/migrations/migrations.go
    - src/server/internal/buildinfo/inspect.go
  </read_first>
  <files>src/server/cmd/server/main.go, src/server/cmd/server/main_test.go</files>
  <behavior>
    - Test V31.1-R03 records contract/profile/build, DB open, DB ping, migration apply/ledger validation, probes/bootstrap generation 1, BuildRuntime, net.Listen, Serve, refresh, workers in exact order.
    - Test V31.1-R03/T-RDY-04 cuts off at every failure; identity/profile fails before DB, DB/migration fails before listener, bind fails before any background start.
    - Test V31.1-R03: non-DB blocked observations still reach listener/control plane, while shutdown cancels refresh/workers and drains server.
    - Test V31.1-R03/T-RDY-07: ContractLoader, ProfileResolver, and IdentityProvider each run once in `RunEntrypoint`; `runMain` receives that same immutable `ResolvedEntrypointInputs`, performs no reload, and passes its exact contract/profile to manager/bootstrap and one `NewRuntimeAuthorities` call.
    - Test V31.1-R03/T-RDY-07: after bootstrap, startup calls `NewRuntimeAuthorities(inputs.Contract(), inputs.Profile(), guard)` exactly once and passes that value to BuildRuntime; constructor failure stops before listener and no later consumer reconstructs authority.
  </behavior>
  <action>Preserve Task 1's outer `RunEntrypoint(ctx, EntrypointID("server"), options, func(ctx context.Context, inputs ResolvedEntrypointInputs) error { ... })` as the first fallible call, and refactor only that continuation behind package-local `runMain(ctx,args,inputs,deps)` whose injected deps record every step without opening real dependencies. The continuation must use `inputs.Contract()`, `inputs.Profile()`, and `inputs.Identity()` from the same immutable handoff for manager/probe/authority construction; it must not call `Load`, `ResolveCommittedProfile`, `IdentityProvider.Resolve`, read environment authority, consult globals/cwd, or reopen contract/profile paths. Open DB then call `PingContext`; apply migrations and validate the existing ledger; build applicable bounded probes from the handed-off values; call `Bootstrap` and require generation 1; construct the manager-backed Guard and call `NewRuntimeAuthorities(inputs.Contract(),inputs.Profile(),guard)` exactly once; pass the result in RuntimeOptions; synchronously call `net.Listen`; start Serve; only after listener success call `StartRefresh(rootCtx)` and `StartBackground(rootCtx)`. Resolver counters assert contract/profile/identity were each resolved exactly once by preflight, the callback captured the same contract digest/profile/identity tuple, and mutations to returned defensive copies cannot affect later composition. Preserve Phase 31 identity inspection within the trusted preflight as a no-side-effect step. On errors, close already-created resources in reverse order and preserve the primary stable failure code. Only after the production changes above are complete, add and run automated verification by extending the Phase 31-created server main test with `TestServerStartupOrderContract`.</action>
  <acceptance_criteria>
    - The exact event-recorder sequence passes and every failure table asserts all later spy counts remain zero.
    - DB ping/migration errors are fatal; non-DB dependency blocked observations do not prevent liveness/control-plane construction.
    - Failed bind starts no refresh/worker; signal cancellation shuts down the listener and all background owners once.
    - RuntimeAuthorities construction failure stops before BuildRuntime/listener, and the event recorder observes exactly one contract/profile/identity resolution, one immutable `ResolvedEntrypointInputs` callback handoff, one successful authority construction, and one unchanged carrier handoff.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./cmd/server '^TestServerStartupOrderContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>D-RDY-07/D-RDY-08/D-RDY-10 and V31.1-R03 are executable, ordered, and fail closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| env/argv -> profile preflight | Caller selects a profile but cannot author commitment or identity. |
| immutable preflight inputs -> startup composition | Validated contract/profile/identity provenance must cross into DB/bootstrap/runtime wiring without reload or mutable aliasing. |
| startup composition -> DB/listener/workers | Ordering decides whether mutable dependencies can run without authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-RDY-01 | Spoofing | startup identity/profile | high | mitigate | Phase 31 trusted resolvers, explicit profile, no default/audit fallback, and zero-call preflight tests. |
| T-RDY-07 | Tampering | preflight-to-composition handoff | high | mitigate | Private immutable `ResolvedEntrypointInputs`, exactly-once resolver counters, defensive-copy mutation tests, and no reload seams. |
| T-RDY-04 | Elevation of Privilege | constructor/background ordering | high | mitigate | Pure builder, synchronous listener, exact event suite, shared cancellation, and failed-bind zero starts. |
| T-RDY-03 | Tampering | excluded standalone entrypoints | high | mitigate | Both shared config families enforce committed-profile preflight before dependency construction. |
</threat_model>

<verification>
- Use only the Phase 31 matched-test helper for focused selectors.
- Run related package suites and `git diff --check` after each task; no source-order scan substitutes for event-recorder behavior.
- Plan 02 executes only after Plans 04/05/06/07/10 package constructors are complete; its task commands therefore compile against real predecessor APIs and never depend on same-wave files.
- This plan proves repository runtime ordering only, not deployment readiness or target operation.
</verification>

<success_criteria>
- Runtime identity/profile validation occurs before DB and no authored default is used as runtime input.
- DB/migration fatal behavior and blocked non-DB bootstrap behavior match D-RDY-08.
- Listener owns the transition to refresh/workers; constructor and failed bind have zero background effects.
- The server first-call preflight resolves contract/profile/identity once, hands one immutable `ResolvedEntrypointInputs` value into composition, and one RuntimeAuthorities value reaches every runtime consumer.
</success_criteria>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-02-SUMMARY.md` when done.
</output>
