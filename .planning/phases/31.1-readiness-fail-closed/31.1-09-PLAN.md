---
phase: 31.1-readiness-fail-closed
plan: "09"
type: execute
wave: 5
depends_on: ["31.1-02"]
files_modified:
  - src/server/cmd/admin/main.go
  - src/server/cmd/gateway/main.go
  - src/server/cmd/marketplace/main.go
  - src/server/cmd/observability/main.go
  - src/server/cmd/rag/main.go
  - src/server/cmd/relay/main.go
  - src/server/cmd/task/main.go
  - src/server/cmd/workflow/main.go
  - src/server/cmd/server/standalone_preflight_test.go
autonomous: true
requirements: [RELS-01]
must_haves:
  truths:
    - "每个 standalone composition root 的第一个 fallible call 提交源码固定 EntrypointID 给 Plan 02 shared runner；resolver-confirmed monolith 不包含该 entrypoint 时，config/DB/worker/gRPC/listener/provider/effect registration 全部为零（D-RDY-07, D-RDY-11）。"
    - "Entrypoint identity 不能从 environment、argv、os.Executable、binary basename、asset existence、defaultProfile 或旧 audit 推导；generic profile resolution 不能代替 exact entrypoint membership（D-RDY-01, D-RDY-11）。"
    - "动态 continuation-zero 表与 Go AST first-call/literal binding 必须同时通过；source scan 单独不能证明 preflight 执行，excluded roots 也不能注册 runtime effect descriptor（D-RDY-11）。"
    - "每个 root continuation 接收 Plan 02 唯一的 immutable ResolvedEntrypointInputs；contract/profile/identity resolver 各调用一次，root 不得从 env/global/cwd/path 重载或替换其 validated contract、resolver-returned profile 与 trusted identity（D-RDY-01, D-RDY-07, D-RDY-11）。"
  artifacts:
    - path: "src/server/cmd/server/standalone_preflight_test.go"
      provides: "Central dynamic zero-effect and AST first-call proof for eight real standalone roots"
      contains: "TestStandaloneProfilePreflightGroupTwoContract"
    - path: "src/server/cmd/relay/main.go"
      provides: "Relay standalone authored-entrypoint preflight before DB, health checks and batch worker"
    - path: "src/server/cmd/workflow/main.go"
      provides: "Workflow standalone authored-entrypoint preflight before DB, gRPC and runtime construction"
  key_links:
    - from: "src/server/cmd/server/standalone_preflight_test.go"
      to: "src/server/pkg/config/common.go"
      via: "dynamic RunEntrypoint resolver counters, immutable ResolvedEntrypointInputs capture, and continuation/DB/listener/provider spies"
      pattern: "RunEntrypoint|ResolvedEntrypointInputs"
    - from: "src/server/cmd/relay/main.go"
      to: "src/server/internal/releasecontract/readiness.go"
      via: "literal relay EntrypointID checked against resolver-confirmed profile"
      pattern: "EntrypointID"
---

<objective>
把 Plan 02 的 authored-entrypoint preflight 接到剩余八个真实 standalone composition roots，确保 committed monolith 下它们在任何依赖或 effect 构造前稳定拒绝。

Purpose: profile 本身是 monolith 并不能标识当前运行 binary；每个 root 必须主动提交自己的源码身份并被当前 profile 明确允许。
Output: 八个 first-call root wiring 和两组非 vacuous continuation/DB/listener/provider/effect-registration zero-call suites。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-RESEARCH.md
@.planning/phases/31.1-readiness-fail-closed/31.1-PATTERNS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-01-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-02-SUMMARY.md
@src/server/internal/config/config.go
@src/server/pkg/config/common.go

<interfaces>
Consume these exact Plan 01/02 contracts without a second resolver or identity source:

```go
type EntrypointID string
func RequireProfileEntrypoint(profile DeploymentProfile, entrypointID EntrypointID) error
type ContractLoader interface {
    Load(context.Context, string, string, string) (releasecontract.AuthoredContractV1, error)
}
type EntrypointPreflightOptions struct {
    RepoRoot, ContractPath, SchemaPath, ProfileID string
    Contracts ContractLoader
    Identities buildinfo.IdentityProvider
    Profiles releasecontract.ProfileResolver
}
type ResolvedEntrypointInputs struct { /* private immutable complete contract/profile/identity */ }
func (i ResolvedEntrypointInputs) Contract() releasecontract.AuthoredContractV1
func (i ResolvedEntrypointInputs) Profile() releasecontract.DeploymentProfile
func (i ResolvedEntrypointInputs) Identity() buildinfo.BuildIdentityV1
type EntrypointContinuation func(context.Context, ResolvedEntrypointInputs) error
func RunEntrypoint(ctx context.Context, id releasecontract.EntrypointID, options EntrypointPreflightOptions, normalStartup EntrypointContinuation) error
```

Every owned root passes a package-source literal ID. `normalStartup(ctx, inputs)` receives the same immutable complete values resolved once by Plan 02 and encloses every existing config load, DB open/ping, migration, client/provider factory, worker/gRPC/router/listener construction and start. Missing/nil trusted providers fail before the closure. Standalone roots do not reload or reconstruct the contract, profile, identity, Guard, or authority from paths/environment/globals/cwd.

`RuntimeAuthorities{CatalogAuthorizer,CapabilityBindings}` is constructed only inside an authorized normal-startup continuation by Plan 02. An excluded standalone root must stop before any carrier, Guard, registrar or local/global fallback is created.
</interfaces>
</context>

## Artifacts this phase produces

- New file: `src/server/cmd/server/standalone_preflight_test.go`.
- Modified roots: `admin`, `gateway`, `marketplace`, `observability`, `rag`, `relay`, `task`, and `workflow` main files listed in `files_modified`.
- New exact suites: `TestStandaloneProfilePreflightGroupTwoContract` and `TestStandaloneProfilePreflightEffectfulRootsContract`.
- No new config authority, profile default, executable-name discovery, readiness evaluator, effect registry, deployment asset or evidence claim.

<tasks>

<task type="auto">
  <name>Task 1: Preflight Admin, Gateway, Marketplace and Observability roots</name>
  <read_first>
    - src/server/cmd/admin/main.go
    - src/server/cmd/gateway/main.go
    - src/server/cmd/marketplace/main.go
    - src/server/cmd/observability/main.go
    - src/server/internal/config/config.go
    - src/server/pkg/config/common.go
    - src/server/internal/releasecontract/readiness.go
  </read_first>
  <files>src/server/cmd/admin/main.go, src/server/cmd/gateway/main.go, src/server/cmd/marketplace/main.go, src/server/cmd/observability/main.go, src/server/cmd/server/standalone_preflight_test.go</files>
  <behavior>
    - Test V31.1-R03/T-RDY-03: each root's missing/mismatched identity, omitted/unknown/conditional/excluded profile, and resolver-confirmed profile without its exact ID return before config continuation, DB, listener, provider/client and descriptor registration spies.
    - Test V31.1-R09: committed monolith rejects the four standalone IDs even though the generic profile resolver succeeds; no candidate asset or service-specific database URL promotes them.
    - Test T-RDY-01: environment/argv/executable-name/path substitutions cannot change the literal ID observed by the dynamic runner.
    - Test V31.1-R03/T-RDY-01: loader/profile/identity counters are exactly one and the continuation captures the exact immutable `ResolvedEntrypointInputs`; denial leaves the continuation at zero and success cannot mutate/reload the handed-off values.
  </behavior>
  <action>Refactor each real main into a package-local `func(context.Context, ResolvedEntrypointInputs) error` normal-startup closure invoked only through Plan 02 `RunEntrypoint` as described below. The first fallible root statement must pass literal IDs `admin`, `gateway`, `marketplace`, and `observability`; all current config/DB/migration/provider/gRPC/HTTP/worker code moves inside the closure without semantic changes. The exact suite dynamically invokes the shared runner for every literal with contract-loader/profile-resolver/identity-provider counters plus continuation/DB/listener/provider/effect-registrar spies, proving all denial counters zero and exactly 1/1/1 resolution plus exact immutable handoff on success. Each root may use the received complete contract/profile/identity but cannot call the resolvers again, load from paths, consult environment/global/cwd authority, or mutate the retained handoff. The suite also parses the four live main files with Go AST to require the runner call/literal precedes the closure; this structural binding supplements rather than replaces dynamic zero-call proof. Reject environment, argv, executable path/name, defaultProfile, DB-mode and asset-based identity. Do not construct/register a readiness manager or effect descriptor for an excluded root. Only after the production changes above are complete, add and run the group table in `standalone_preflight_test.go` as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Four real roots have exact source literals and no fallible/effectful statement before `RunEntrypoint`.
    - Dynamic table proves generic monolith profile resolution followed by exact entrypoint denial with every continuation/effect spy zero.
    - Success proves one contract/profile/identity resolution and one exact immutable callback value; a defensive-copy mutation cannot alter a later accessor result.
    - AST/literal mismatch, moved preflight, nil provider, identity mismatch and profile substitution each fail the named suite.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./cmd/server '^TestStandaloneProfilePreflightGroupTwoContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.1-R03/V31.1-R09 and T-RDY-01/T-RDY-03 close authored-entrypoint preflight for group two.</done>
</task>

<task type="auto">
  <name>Task 2: Preflight RAG, Relay, Task and Workflow effectful roots</name>
  <read_first>
    - src/server/cmd/rag/main.go
    - src/server/cmd/relay/main.go
    - src/server/cmd/task/main.go
    - src/server/cmd/workflow/main.go
    - src/server/cmd/server/standalone_preflight_test.go
    - src/server/internal/config/config.go
    - src/server/pkg/config/common.go
  </read_first>
  <files>src/server/cmd/rag/main.go, src/server/cmd/relay/main.go, src/server/cmd/task/main.go, src/server/cmd/workflow/main.go, src/server/cmd/server/standalone_preflight_test.go</files>
  <behavior>
    - Test V31.1-R03/T-RDY-04: excluded `rag`, `relay`, `task`, and `workflow` stop before HTTP/gRPC listen, Relay health checks, batch/schedule workers, DB/migration, provider/search/sandbox and goroutine starts.
    - Test V31.1-R09/T-RDY-03: each root produces zero runtime descriptors under committed monolith, while Plan 03 expected inventory retains an explicit expected-excluded row.
    - Test T-RDY-01: exact authored ID/profile/identity is evaluated on every process start and cannot be restored from audit or another root's successful result.
    - Test V31.1-R03/T-RDY-01: each allowed fixture root receives exactly the contract/profile/identity returned by the single preflight resolution; the continuation performs no reload before any later construction.
  </behavior>
  <action>Apply the same shared runner to literal IDs `rag`, `relay`, `task`, and `workflow` as the first fallible statement in each live main. Enclose every existing DB/migration, HTTP/gRPC listener, Relay `StartHealthChecks`, batch/schedule worker, provider/web-search/sandbox factory and goroutine start in `func(ctx context.Context, inputs ResolvedEntrypointInputs) error`. Dynamic per-root dependency spies must stay zero for generic-monolith-success followed by exact entrypoint denial, identity mismatch and profile omission; a fake resolver-confirmed fixture profile containing the exact ID reaches the continuation once with the exact immutable contract/profile/identity value and without granting any later readiness verdict. Assert the contract loader, profile resolver, and identity provider each ran once and that the closure never calls them or reloads from environment/global/cwd/path. AST binds each literal/first call to the real root, but behavior counters remain the acceptance authority. Register no runtime descriptor before successful preflight; Plan 03 inventory classifies each root expected-excluded for monolith. Only after the production changes above are complete, extend `standalone_preflight_test.go`, add `TestStandaloneProfilePreflightEffectfulRootsContract`, and run it as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - All four high-effect roots deny before every named dependency/goroutine and register zero descriptors under monolith.
    - Exact-ID fixture success invokes only the injected continuation once; it does not start real dependencies or cache authorization.
    - Exact-ID fixture success captures the one immutable preflight handoff, and all resolver/reload counters remain exactly 1/1/1/0.
    - The named suite fails for wrong/copied literal, preflight after config, any nonzero denial spy, audit fallback or runtime registration.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./cmd/server '^TestStandaloneProfilePreflightEffectfulRootsContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.1-R03/V31.1-R09 and T-RDY-01/T-RDY-03/T-RDY-04 close all remaining standalone composition roots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source-authored root ID -> committed profile | Current binary identity must be independently allowed by the resolved profile. |
| preflight -> immutable inputs -> normal startup closure | Any reload, mutable alias, dependency, or effect before successful membership and exact handoff is an authorization bypass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-RDY-03 | Elevation of Privilege | standalone root | high | mitigate | Source literal, exact profile membership, first-call AST binding and dynamic continuation/effect zero counters. |
| T-RDY-01 | Spoofing | entrypoint identity | high | mitigate | No env/argv/executable/default/audit derivation; trusted identity/profile providers plus authored ID. |
| T-RDY-07 | Tampering | standalone callback inputs | high | mitigate | Exactly-once resolver counters, private immutable `ResolvedEntrypointInputs`, defensive-copy mutation tests, and zero reload calls. |
| T-RDY-04 | Denial of Service | excluded worker/listener starts | high | mitigate | Entire startup enclosed in continuation and high-effect root spy matrix. |
</threat_model>

<verification>
- Both focused selectors use the Phase 31 matched-test helper and exact top-level names.
- Go AST proves only live first-call/literal binding; dynamic resolver/continuation/DB/listener/provider/worker/descriptor counters prove behavior.
- Dynamic success cases also prove the complete validated contract, resolver-returned profile, and trusted identity cross the callback once without reload or mutable aliasing.
- This plan creates no runtime success evidence for standalone profiles; committed monolith denial is repository-local E1 proof only.
</verification>

<success_criteria>
- All twelve standalone roots across Plans 02/09 submit exact source-authored identities before effects.
- Generic committed-profile success cannot authorize a root missing from that profile.
- Committed monolith yields zero dependency/effect/descriptor calls for every excluded standalone root.
- No mutable/caller-derived entrypoint identity or audit fallback exists.
- All root callbacks consume the same immutable `ResolvedEntrypointInputs` contract/profile/identity resolved once by the shared preflight.
</success_criteria>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-09-SUMMARY.md` when done.
</output>
