---
phase: 31.1-readiness-fail-closed
plan: "19"
subsystem: release-contract
tags: [go, readiness, effect-coverage, runtime-registry, fail-closed, chat]

# Dependency graph
requires:
  - phase: 31.1-readiness-fail-closed
    provides: "Plan 18 frozen effect-surface manifest/spec map, RuntimeAuthorities, and strict producer contracts"
provides:
  - "Strict production EffectRegistry construction with a post-construction defensive Runtime descriptor snapshot"
  - "Real BuildRuntime exact-join coverage for Tavily, fallback-chain, and single-provider web search compositions"
  - "Pre-harness production exact-join selectors in both readiness gate stages"
affects: [31.1-20, 31.1-22, release-contract, readiness-verification]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Production construction uses releasecontract.NewEffectRegistry and rejects every duplicate descriptor"
    - "Runtime captures a sorted copy only after strict constructors complete and returns defensive evidence copies"
    - "Shared Chat consumers retain per-dispatch guards while one router-owned constructor owns descriptor registration"
    - "Phase gates run the same non-vacuous real-composition exact join before deployment harness work"

key-files:
  created:
    - src/server/internal/http/runtime_effect_coverage_test.go
  modified:
    - src/server/cmd/server/main.go
    - src/server/cmd/server/main_test.go
    - src/server/internal/http/server.go
    - src/server/internal/chat/relay_gateway.go
    - src/server/internal/chat/relay_gateway_test.go
    - scripts/verify-readiness-contract.sh
    - scripts/verify-readiness-contract-fixtures.sh

key-decisions:
  - "The shipped monolith passes releasecontract.NewEffectRegistry directly; the permissive byte-identical acceptance path is removed."
  - "The schedule Agent gateway is a shared Chat consumer and skips only evidence registration; the router gateway remains the single chat.provider.dispatch owner, while every consumer still performs its current-generation guard."
  - "Runtime.EffectDescriptors is evidence-only, sorted, defensive, and populated only after all strict constructors return successfully."
  - "The production selector is anchored before Docker, deployment, or identity-bearing harness work in both Stage A and Stage B."

patterns-established:
  - "Three explicit web-search configurations select exactly one provider descriptor: mcp.websearch.tavily, mcp.websearch.chain, or mcp.websearch.provider."
  - "Coverage fixtures assert producer-family exact-once counts and zero rows for excluded Registry/WebsearchTool families."
  - "Mutation tables cover manifest, structural static discovery, runtime, profile, RuntimeAuthorities, and provider-selection drift with stable error codes."

requirements-completed: []

coverage:
  - id: D1
    description: "Strict BuildRuntime exposes a sorted defensive descriptor snapshot and rejects duplicate construction."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/http/runtime_effect_coverage_test.go#TestRuntimeEffectRegistrySnapshotContract"
        status: pass
      - kind: unit
        ref: "src/server/cmd/server/main_test.go#TestRuntimeEffectRegistryRejectsExactDuplicateContract"
        status: pass
    human_judgment: false
  - id: D2
    description: "Real monolith composition exactly joins 17 selected descriptors for direct Tavily, fallback chain, and another single provider, with excluded and ownerless families absent."
    requirement: RELS-01
    verification:
      - kind: integration
        ref: "src/server/internal/http/runtime_effect_coverage_test.go#TestProductionEffectCoverageContract"
        status: pass
      - kind: integration
        ref: "bash scripts/run-go-tests-matched.sh ./internal/chat '^TestChatRelayReadinessDispatchContract$'"
        status: pass
      - kind: integration
        ref: "bash scripts/run-go-tests-matched.sh ./cmd/server '^TestRuntimeBuildAndBackgroundLifecycleContract$'"
        status: pass
    human_judgment: false
  - id: D3
    description: "Readiness Stage A and Stage B invoke the production exact join before harness work and fixture selectors reject manifest/static/authority/selection drift."
    requirement: RELS-01
    verification:
      - kind: integration
        ref: "bash scripts/verify-readiness-contract-fixtures.sh"
        status: pass
      - kind: other
        ref: "bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessEffectCoverageContract$'"
        status: pass
    human_judgment: false

# Metrics
duration: 39 min
completed: 2026-07-21
status: complete
---

# Phase 31.1 Plan 19: Production Effect Coverage Summary

**Strict real-composition effect coverage with a defensive runtime snapshot and a mandatory pre-harness exact-join gate**

## Performance

- **Duration:** 39 min
- **Started:** 2026-07-21T01:35:14Z
- **Completed:** 2026-07-21T02:14:20Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments

- Replaced the permissive server-local registry with `releasecontract.NewEffectRegistry`, rejecting byte-identical duplicates and exposing only a sorted defensive Runtime snapshot after successful strict construction.
- Verified real `BuildRuntime` composition for direct Tavily, a configured fallback chain, and another supported single provider: each selected projection contains exactly 17 descriptors across six producer families, including `agent.tool.builtin`, `mcp.websearch.builtin`, and exactly one provider row; excluded Registry/WebsearchTool rows remain absent.
- Added a 17-case mutation matrix covering manifest, structural static, runtime missing/extra/duplicate/excluded/owner drift, profile, RuntimeAuthorities, and provider-selection failures, plus an AST guard against coverage-only Registry/WebsearchTool construction.
- Anchored `TestProductionEffectCoverageContract` before deployment and identity work for both readiness gate stages; fixture verification now runs manifest/spec, structural, authority, selection, production, and deployment fail-closed selectors.

## Task Commits

The plan explicitly requires one atomic production commit covering all three tasks:

1. **Task 1: Expose a strict real-composition descriptor snapshot** - `aa80f5f` (`fix(31.1-19): gate production effect coverage`)
2. **Task 2: Match the frozen contract from real strict composition** - `aa80f5f` (`fix(31.1-19): gate production effect coverage`)
3. **Task 3: Invoke the six-input production exact join from the phase gate** - `aa80f5f` (`fix(31.1-19): gate production effect coverage`)

## Files Created/Modified

- `src/server/cmd/server/main.go` - Instantiates the strict production EffectRegistry.
- `src/server/cmd/server/main_test.go` - Requires exact duplicate rejection.
- `src/server/internal/http/server.go` - Captures and defensively exposes post-construction descriptor evidence; assigns Chat registration ownership.
- `src/server/internal/http/runtime_effect_coverage_test.go` - Real BuildRuntime provider matrix, producer-family counts, AST guard, and six-input mutation matrix.
- `src/server/internal/chat/relay_gateway.go` - Supports shared guarded consumers that do not duplicate evidence registration.
- `src/server/internal/chat/relay_gateway_test.go` - Proves skipped registration still guards transport and makes zero network calls on denial.
- `scripts/verify-readiness-contract.sh` - Runs the production exact-join selector before both deployment stages.
- `scripts/verify-readiness-contract-fixtures.sh` - Runs and anchors all effect-contract fixture selectors.

## Decisions Made

- Registry snapshots are evidence only and cannot authorize runtime effects or replace current per-dispatch guards.
- The router owns shared Chat descriptor registration; the schedule Agent gateway uses the same authority/guard carrier with registration disabled, preventing duplicate IDs without weakening authorization.
- Plan 18 manifest/spec and `effect_registry.go` remain read-only inputs; no expected row or mapping was changed.

## Deviations from Plan

None - plan executed exactly as written. The duplicate Chat registration discovered during the real composition test was the producer-ownership correction explicitly required by Task 2.

## Issues Encountered

- Initial real strict construction failed on duplicate `chat.provider.dispatch` from the schedule Agent and router Chat compositions. The fix assigned one construction owner and was verified by the Chat dispatch contract and the full real exact join.
- No authentication gate or external target/live evidence was required. Evidence remains repository-local E1/E2 only.

## Evidence

- `bash scripts/run-go-tests-matched.sh ./internal/http '^TestRuntimeEffectRegistrySnapshotContract$'` passed.
- `bash scripts/run-go-tests-matched.sh ./cmd/server '^TestRuntimeEffectRegistryRejectsExactDuplicateContract$'` passed.
- `bash scripts/run-go-tests-matched.sh ./internal/http '^TestProductionEffectCoverageContract$'` passed for all three provider configurations and mutations.
- `bash scripts/run-go-tests-matched.sh ./internal/chat '^TestChatRelayReadinessDispatchContract$'` passed.
- `bash scripts/run-go-tests-matched.sh ./cmd/server '^TestRuntimeBuildAndBackgroundLifecycleContract$'` passed.
- `bash scripts/verify-readiness-contract-fixtures.sh` passed, including deployment fail-closed fixtures.
- `cd src/server && go test ./internal/http ./internal/chat ./internal/releasecontract ./cmd/server -count=1` passed.
- `cd src/server && go test -race ./internal/http ./internal/chat ./internal/releasecontract ./cmd/server -count=1` passed.
- `cd src/server && go vet ./internal/http ./internal/chat ./internal/releasecontract ./cmd/server` passed.
- `git diff --check` passed before the production commit.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 19's repository-local production descriptor gate is ready for Plan 20/22 closeout work. The phase still has pending plans and RELS-01 remains open until the remaining readiness closeout and external/manual release gates are completed; this summary does not claim target/live or commercial release proof.

---
*Phase: 31.1-readiness-fail-closed*
*Completed: 2026-07-21*

## Self-Check: PASSED

- Summary artifact exists and coverage metadata classifies all three deliverables as automated pass.
- Production commit `aa80f5f` exists with exactly the eight Plan 19 implementation/test/gate files.
- Protected Plan 18 registry files, `.planning/config.json`, and `.planning/intel/` were not staged or committed.
