---
phase: 31.1-readiness-fail-closed
plan: "22"
type: execute
wave: 2
depends_on: ["31.1-17"]
files_modified:
  - scripts/k8s-validate.sh
  - deploy/kubernetes/kafka.yaml
  - scripts/verify-readiness-deployment-contract.sh
  - scripts/verify-k8s-readiness-runner-fixtures.sh
autonomous: true
gap_closure: true
requirements: [RELS-01]
must_haves:
  truths:
    - "The supported `scripts/k8s-validate.sh` applies the canonical Kafka manifest and waits for the Kafka StatefulSet to become ready before applying/waiting for the server and before any `/readyz` success proof (D-RDY-24, D-RDY-25, D-RDY-26)."
    - "A Kubernetes ConfigMap that advertises `KAFKA_BROKERS` without a matching applied Kafka service/StatefulSet and ordered wait is rejected by the deployment contract (D-RDY-09, D-RDY-25)."
    - "Mutation and fake-kubectl behavior fixtures prove exact apply/wait/readiness order without requiring a live cluster, while the real runner retains its external secret/cluster gate and repository-local evidence ceiling (D-RDY-26, D-RDY-27, D-RDY-28)."
  artifacts:
    - path: "scripts/k8s-validate.sh"
      provides: "Supported canonical Kubernetes apply/wait/readiness sequence including Kafka"
      contains: "statefulset/kafka"
    - path: "deploy/kubernetes/kafka.yaml"
      provides: "Canonical Kafka Service and StatefulSet consumed by the supported runner"
      contains: "kind: StatefulSet"
    - path: "scripts/verify-readiness-deployment-contract.sh"
      provides: "Static ordered-link contract for ConfigMap address, Kafka manifest, runner apply/wait, and /readyz"
      contains: "k8s-validate.sh"
    - path: "scripts/verify-k8s-readiness-runner-fixtures.sh"
      provides: "Positive fake-kubectl trace plus missing-deploy/wait/order mutation matrix"
      contains: "k8s readiness runner fixtures passed"
  key_links:
    - from: "deploy/kubernetes/configmap.yaml KAFKA_BROKERS"
      to: "deploy/kubernetes/kafka.yaml Service"
      via: "exact in-cluster service host/port is backed by the manifest the runner applies"
      pattern: "oblivious-kafka"
    - from: "scripts/k8s-validate.sh"
      to: "statefulset/kafka"
      via: "apply Kafka, then wait for readiness, then apply/wait server, then require /readyz"
      pattern: "rollout status statefulset/kafka"
---

<objective>
Complete the Kubernetes half of CR-07 by wiring Kafka into the supported canonical runner and proving an advertised broker address cannot pass without deployment and ordered readiness.

Purpose: Configuring `KAFKA_BROKERS` is hollow when the supported validation command never deploys or waits for Kafka; the server then cannot produce a healthy generation from the shipped topology.
Output: Kafka-aware runner, exact manifest/address/order contract, and mutation-tested fake-kubectl behavior evidence.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-VERIFICATION.md
@.planning/phases/31.1-readiness-fail-closed/31.1-17-SUMMARY.md
@scripts/k8s-validate.sh
@deploy/kubernetes/kafka.yaml
@deploy/kubernetes/configmap.yaml
@deploy/kubernetes/app-deployment.yaml
@scripts/verify-readiness-deployment-contract.sh

<constraints>
- `deploy/kubernetes/app-deployment.yaml` remains the sole canonical server workload and is read-only here; Kafka is a dependency manifest, not a second application authority.
- Preserve the existing three-broker KRaft topology, service identity, persistent storage, external secret gate, namespace cleanup behavior, `/livez` liveness/startup, `/readyz` readiness, and output-only audit volume.
- Fake-kubectl fixtures prove command/order behavior only and cannot be described as target/live Kubernetes evidence.
- CR-06/CR-09 remain Phase 34, WR-02 remains Phase 35, and readiness OpenAPI Envelope parity remains Phase 31.2.
</constraints>

<interfaces>
The ConfigMap broker must resolve to the client Service declared by `deploy/kubernetes/kafka.yaml`. `scripts/k8s-validate.sh` renders/applies that manifest in its data-service stage, then waits with a bounded configurable timeout for `statefulset/kafka`/its ready replicas before applying the app workload. After server rollout and port-forward establishment, the runner must explicitly require `GET /readyz` success before `deploy-smoke.sh`; `/healthz` alone is not readiness proof. The static verifier parses these links and relative order. The fixture script runs the real runner against PATH-injected `kubectl`/`curl` fakes and checks a complete ordered trace.
</interfaces>
</context>

## Artifacts this phase produces

- Supported Kafka apply/wait wiring in `k8s-validate.sh`.
- Exact ConfigMap-to-Service/StatefulSet address contract.
- Fake-kubectl ordered execution trace and negative mutation suite.
- Explicit `/readyz` proof after dependency and server readiness.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply and wait for canonical Kafka before server readiness</name>
  <files>scripts/k8s-validate.sh, deploy/kubernetes/kafka.yaml, scripts/verify-readiness-deployment-contract.sh</files>
  <read_first>
    - scripts/k8s-validate.sh
    - deploy/kubernetes/kafka.yaml
    - deploy/kubernetes/configmap.yaml
    - deploy/kubernetes/app-deployment.yaml
    - scripts/verify-readiness-deployment-contract.sh
    - .planning/phases/31.1-readiness-fail-closed/31.1-17-SUMMARY.md
  </read_first>
  <assertion_ids>ASSERT-K8S-KAFKA-APPLY-WAIT-ORDER, ASSERT-K8S-READYZ-AFTER-KAFKA</assertion_ids>
  <behavior>
    - Runner applies the Kafka Service/StatefulSet in the data-service stage and uses a bounded Kafka timeout.
    - Runner records Kafka ready before app workload apply/server rollout and records explicit `/readyz` success only afterward.
    - ConfigMap broker host/port exactly matches the Kafka client Service and container port selected by the manifest.
  </behavior>
  <action>Update the supported runner to render/apply `deploy/kubernetes/kafka.yaml` with the other data services, then wait for `statefulset/kafka` and all expected ready replicas using `OBLIVIOUS_K8S_KAFKA_TIMEOUT` or an equivalently bounded setting. Keep Kafka wait before app workload apply, server rollout, port-forward, and `/readyz`. Add an explicit `curl -fsS /readyz` gate after port-forward startup and before `deploy-smoke.sh`; `/healthz` may prove the forward is live but cannot satisfy readiness. Normalize only the Kafka manifest fields required for deterministic Service/StatefulSet identity and readiness while preserving three brokers, KRaft, resources, and persistent data. Extend the deployment contract parser to exact-join ConfigMap address, Service port, StatefulSet selector/name, runner apply, runner wait, and later `/readyz` in source order.</action>
  <verify>
    <automated>bash scripts/verify-readiness-deployment-contract.sh &amp;&amp; bash -n scripts/k8s-validate.sh</automated>
  </verify>
  <acceptance_criteria>
    - The supported runner applies the canonical Kafka manifest exactly once and waits for the named StatefulSet with a bounded timeout.
    - Kafka wait precedes app apply/server rollout, and explicit `/readyz` proof follows both.
    - ConfigMap, Service, StatefulSet, and runner identities/ports exact-join.
    - Existing secret, cluster, cleanup, profile, live/ready, and audit gates remain fail closed.
  </acceptance_criteria>
  <done>The supported Kubernetes command can actually satisfy the Kafka-backed readiness contract it advertises.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject address-only and misordered Kafka readiness wiring</name>
  <files>scripts/k8s-validate.sh, deploy/kubernetes/kafka.yaml, scripts/verify-readiness-deployment-contract.sh, scripts/verify-k8s-readiness-runner-fixtures.sh</files>
  <read_first>
    - scripts/k8s-validate.sh
    - deploy/kubernetes/kafka.yaml
    - deploy/kubernetes/configmap.yaml
    - scripts/verify-readiness-deployment-contract.sh
    - scripts/deploy-smoke.sh
  </read_first>
  <assertion_ids>ASSERT-K8S-KAFKA-ADDRESS-WITHOUT-DEPLOYMENT, ASSERT-K8S-KAFKA-RUNNER-MUTATIONS</assertion_ids>
  <behavior>
    - The unmodified asset set passes static validation and a fake-kubectl runner trace contains apply Kafka, wait Kafka, apply app, wait server, and `/readyz` in exact order.
    - A ConfigMap Kafka address with runner apply removed fails; apply without wait, wrong StatefulSet/service/port, wait after app, and `/healthz`-only proof each fail independently.
    - Zero matching apply/wait commands, duplicate Kafka apply, and a fixture that never executes the real runner cannot pass vacuously.
  </behavior>
  <action>Create a fail-closed fixture script that copies only the runner, Kafka manifest, ConfigMap, app manifest, and verifier inputs to disposable directories. Run the real runner with PATH-injected `kubectl` and `curl` fakes, a disposable non-placeholder secret, bounded loops, and a trace file; the fakes must emulate cluster-info, apply, wait/rollout, port-forward lifetime, `/healthz`, and `/readyz` without contacting a cluster. Assert the exact ordered trace and nonzero command counts. Apply isolated mutations for address-without-deployment, missing wait, duplicate apply, wrong service/port/StatefulSet, wait after app, `/healthz` substituted for `/readyz`, and verifier/runner not executed; require every case to fail for its intended stable reason. Clean all temp state with a trap.</action>
  <verify>
    <automated>bash scripts/verify-k8s-readiness-runner-fixtures.sh &amp;&amp; bash scripts/verify-readiness-deployment-contract.sh</automated>
  </verify>
  <acceptance_criteria>
    - Positive fake-kubectl trace proves the real supported runner's exact Kafka-before-app-before-ready order.
    - Every named mutation fails independently, including the explicit Kafka-address-without-deployment case.
    - Fixtures use no live cluster, committed secret, provider, or target evidence and leave no temp processes/files.
  </acceptance_criteria>
  <done>Canonical Kubernetes readiness wiring is non-vacuous, ordered, and mutation-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kubernetes configuration -> deployed dependency | A broker address can claim a service that the supported runner never creates. |
| Dependency rollout -> server readiness | App startup may race ahead of Kafka and publish only blocked observations. |
| Fake command trace -> repository evidence | A fixture can pass without executing the real runner or checking command order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-22-01 | Spoofing | Kafka address/deployment link | high | mitigate | Exact-join ConfigMap host/port to applied Service/StatefulSet and reject address-only mutations. |
| T-31.1-22-02 | Tampering | Apply/wait/readiness order | high | mitigate | Parse source order and assert fake-kubectl trace places Kafka ready before app/server and `/readyz`. |
| T-31.1-22-03 | Denial of Service | Kafka rollout wait | high | mitigate | Bounded configurable timeout and StatefulSet readiness stop app validation on unavailable Kafka. |
| T-31.1-22-04 | Repudiation | Kubernetes fixture proof | high | mitigate | Execute the real runner with non-vacuous command counts and reject a disconnected fixture mutation. |
</threat_model>

<verification>
- Run `bash -n scripts/k8s-validate.sh scripts/verify-readiness-deployment-contract.sh scripts/verify-k8s-readiness-runner-fixtures.sh`.
- Run both contract/fixture scripts and require every named mutation to fail.
- Run `git diff --check` and inspect only the four owned files.
</verification>

<success_criteria>
- Canonical Kubernetes cannot advertise Kafka readiness without deploying and waiting for the matching Kafka workload.
- The supported runner proves `/readyz` only after Kafka and server readiness, with repository-local fake-command mutation evidence and no target/live claim.
</success_criteria>

<commit_policy>
Create one atomic commit: `fix(31.1-22): wire kafka into k8s readiness runner`. Do not push. Plan 20 ends with a HARD EXTERNAL/MANUAL POST-CLOSEOUT GATE handoff; no GSD executor, verifier, tracker, hook, or autonomous orchestrator step owns final Stage B or push.
</commit_policy>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-22-SUMMARY.md` with exact apply/wait/ready trace, mutation case counts, commands, and repository-local claim ceiling.
</output>
