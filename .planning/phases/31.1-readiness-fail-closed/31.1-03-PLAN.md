---
phase: 31.1-readiness-fail-closed
plan: "03"
type: execute
wave: 6
depends_on: ["31.1-02", "31.1-04", "31.1-05", "31.1-06", "31.1-07", "31.1-09", "31.1-10"]
files_modified:
  - src/server/internal/http/router.go
  - src/server/internal/http/readiness_handler.go
  - src/server/internal/http/readiness_handler_test.go
  - config/release/readiness-effect-surface.v1.json
  - src/server/internal/releasecontract/effect_registry.go
  - src/server/internal/releasecontract/effect_registry_test.go
  - src/server/cmd/release-contract/main.go
  - src/server/cmd/release-contract/main_test.go
  - scripts/verify-readiness-contract-fixtures.sh
autonomous: true
requirements: [RELS-01]
must_haves:
  truths:
    - "HTTP dispatch 每次在业务 handler 前读取 current manager；excluded 不注册，disabled/blocked/stale/unknown/identity mismatch 以稳定脱敏码零调用拒绝（D-RDY-11, D-RDY-12）。"
    - "Admin full inventory 与 authenticated app-safe projection 是不同 DTO；/livez 只表示 control-plane liveness，/readyz 直接消费 current evaluation（D-RDY-17, D-RDY-18）。"
    - "Plan 03 后置创建 Expected inventory，并与 Go AST static discovery、selected-profile runtime descriptors 做 exact three-way join；上游 consumer plans 只注册 typed descriptors，不依赖该 manifest，missing/extra/duplicate/unknown/unclassified/zero rows 失败（D-RDY-11）。"
    - "Inspector 可以离线验证 runtime-produced snapshot 并写 readiness report，但 runtime 永不读取报告或 snapshot 授权（D-RDY-01, D-RDY-19）。"
    - "Router 与 exact-join verifier 消费 startup 构造的同一 RuntimeAuthorities；catalog 和 typed effect mapping 不得由 handler/worker/tool/finance 本地重建（D-RDY-11, D-RDY-20, D-RDY-21）。"
  artifacts:
    - path: "config/release/readiness-effect-surface.v1.json"
      provides: "Independent expected inventory for all static effect seams and profile dispositions"
      contains: '"schemaVersion": "readiness-effect-surface/v1"'
    - path: "src/server/internal/releasecontract/effect_registry.go"
      provides: "Go AST discovery, runtime registry, profile projection and exact join"
      exports: ["EffectRegistry", "DiscoverEffectSurfaces", "VerifyEffectCoverage"]
    - path: "src/server/internal/http/readiness_handler.go"
      provides: "live/ready/Admin/app projections and stable readiness errors"
      exports: ["NewReadinessHandlers"]
    - path: "scripts/verify-readiness-contract-fixtures.sh"
      provides: "Dynamic one-field mutation and no-skip E1 contract suite"
      contains: "readiness-effect-surface/v1"
  key_links:
    - from: "src/server/internal/http/router.go"
      to: "src/server/internal/releasecontract/catalog.go"
      via: "RouterOptions carries the exact startup-built RuntimeAuthorities and current Guard to every effectful dispatch"
      pattern: "RuntimeAuthorities|BoundaryHTTP"
    - from: "src/server/internal/releasecontract/effect_registry.go"
      to: "config/release/readiness-effect-surface.v1.json"
      via: "Plan 03 creates the manifest only after all descriptor producers, then owns the expected/static/runtime exact join projected through committed profile"
      pattern: "VerifyEffectCoverage"
    - from: "src/server/internal/releasecontract/effect_registry.go"
      to: "src/server/internal/agent/executor.go"
      via: "static discovery and expected rows cover the real builtin/custom-HTTP/local-Python/sandbox/MCP ToolExecutor seams, not the unconsumed Registry alone"
      pattern: "ToolExecutor|RunCustomPython|CallTool"
---

<objective>
完成 current manager 的 control-plane/HTTP consumption、后置创建的独立 effect coverage authority，以及 evidence-only CLI，使遗漏 guard、前向 manifest 依赖或泄漏 projection 不能形成假绿。

Purpose: 在所有 consumer wiring 完成后，用行为测试与三方 exact join 证明 selected monolith 的每个 effect seam 被分类并注册。
Output: HTTP guards/views/health、effect inventory/AST/runtime verifier、readiness CLI 与 mutation fixtures。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-VALIDATION.md
@.planning/phases/31.1-readiness-fail-closed/31.1-01-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-02-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-04-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-05-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-06-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-07-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-09-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-10-SUMMARY.md
@src/server/internal/http/router.go
@src/server/internal/http/server.go
@src/server/cmd/release-contract/main.go

<interfaces>
Use the exact Plan 31.1-01 `ReadinessManager`, `Guard`, `Boundary`, `EffectDescriptor`, `EffectRegistrar`, and `RouterOptions{Readiness, Guard, Effects, Authorities}` unchanged. The authority carrier contract is:

```go
type EffectID string
type CapabilityBindings struct { /* private immutable lookup */ }
func (b CapabilityBindings) Resolve(effect EffectID) (CapabilityID, error)
type RuntimeAuthorities struct {
    CatalogAuthorizer CatalogAuthorizer
    CapabilityBindings CapabilityBindings
}
func NewRuntimeAuthorities(contract AuthoredContractV1, profile DeploymentProfile, guard Guard) (RuntimeAuthorities, error)
```

The readiness constructor is side-effect-free; the CLI persists its returned value through Phase 31's writer:

```go
func NewReadinessReport(ctx context.Context, identities buildinfo.IdentityProvider, profiles releasecontract.ProfileResolver, repoRoot, contractPath, schemaPath, profileID string, input ReadinessInspection, outcome Outcome) (SurfaceReportV1, error)
type ReportWriter interface { Write(context.Context, string, SurfaceReportV1) error }
```
</interfaces>
</context>

## Artifacts this phase produces

- New files: `readiness_handler.go`, `readiness_handler_test.go`, Plan 03-owned expected inventory, `effect_registry.go`, `effect_registry_test.go`, and `verify-readiness-contract-fixtures.sh`.
- Modified files: `router.go` and Phase 31-created `cmd/release-contract/main.go`/`main_test.go`.
- New exported symbols: `AdminReadinessInventory`, `AppCapabilityAvailability`, `NewReadinessHandlers`, `EffectRegistry`, `NewEffectRegistry`, `DiscoverEffectSurfaces`, `VerifyEffectCoverage`.
- Router/control-plane construction consumes one nonzero `RuntimeAuthorities` value; effect inventory is created only in this later plan and verifies predecessor-registered typed bindings against expected/static/runtime rows without becoming a second runtime authority.
- New CLI subcommands: `inspect-readiness --repo --contract --schema --profile --snapshot`, `report-readiness --repo --contract --schema --profile --snapshot --output`, and `verify-readiness-snapshot --repo --contract --schema --profile --snapshot`; no identity, verdict, generation, or timing override flag.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serve manager-backed liveness, readiness, Admin and app views</name>
  <read_first>
    - src/server/internal/http/router.go
    - src/server/internal/http/server.go
    - src/server/internal/http/auth_middleware.go
    - src/server/internal/http/admin_handler.go
    - src/server/internal/releasecontract/readiness.go
  </read_first>
  <files>src/server/internal/http/router.go, src/server/internal/http/readiness_handler.go, src/server/internal/http/readiness_handler_test.go</files>
  <behavior>
    - Test V31.1-R04: `/livez` stays 200 for blocked/stale dependencies and makes zero DB/probe/audit reads; `/readyz` fails required blocked/unknown/unavailable/stale but permits intentionally disabled conditional capability.
    - Test V31.1-R04/T-RDY-03: effectful HTTP handlers are called zero times for disabled, blocked, stale, unavailable, unknown, identity mismatch, and nil manager.
    - Test V31.1-R04/T-RDY-06: Admin includes full committed/conditional/excluded inventory and remediation/evidence refs; app response omits excluded, raw observations, credentials, URLs, file paths, and internal detail.
    - Test V31.1-R04/T-RDY-07: zero/mismatched RouterOptions.Authorities fails before route registration; a valid router uses only `Authorities.CatalogAuthorizer`/`Authorities.CapabilityBindings` plus the shared Guard and performs zero contract/profile reloads.
  </behavior>
  <action>Use RED first with one `TestReadinessControlPlaneContract` suite, then implement D-RDY-12/D-RDY-17/D-RDY-18. Register `/livez`, replace current DB-only `/readyz`, and add authenticated Admin full inventory plus authenticated app-safe projection using separate concrete DTOs. All views call the in-memory manager and never read audit files. Require the exact startup-provided `RouterOptions.Authorities`; build route registration only from `Authorities.CapabilityBindings.Resolve(typedEffectID)` or `Authorities.CatalogAuthorizer.ResolveAndRequire` as appropriate. Excluded routes are not registered, while every registered effectful route uses the shared `Guard.Require(ctx, capabilityID, BoundaryHTTP)` immediately before the business handler and registers one runtime descriptor. Missing/zero/mismatched authorities fail router construction; handler code cannot load contract/profile, hard-code IDs, access a global authority, or install a fallback. Map disabled to `capability_disabled`, blocked to `503 capability_blocked`, and all other non-authorizing states to stable non-success codes through existing `writeError`; never echo probe errors. Keep the exact operation/schema parity expansion out of this plan and Phase 31.2.</action>
  <acceptance_criteria>
    - The named suite dynamically proves live/ready matrix, HTTP zero calls, auth boundaries, and distinct serialized DTO field sets.
    - `/livez` performs no DB/provider/probe/audit work; `/readyz` uses current manager generation on every request.
    - Excluded routes return 404 because they are absent, not a visible disabled response.
    - Composition assertions prove RouterOptions receives the same immutable authority behavior built by startup and exposes no local reload/hard-code/global fallback.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestReadinessControlPlaneContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.1-R04 and T-RDY-03/T-RDY-05/T-RDY-06 prove current-generation control-plane behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce expected/static/runtime effect coverage exactness</name>
  <read_first>
    - .planning/phases/31.1-readiness-fail-closed/31.1-RESEARCH.md
    - src/server/internal/releasecontract/contract.go
    - src/server/internal/releasecontract/readiness.go
    - src/server/internal/http/router.go
    - src/server/internal/schedule/worker.go
    - src/server/internal/relay/router.go
    - src/server/internal/marketplace/settlement.go
    - src/server/internal/agent/executor.go
    - src/server/internal/agent/tools/registry.go
  </read_first>
  <files>config/release/readiness-effect-surface.v1.json, src/server/internal/releasecontract/effect_registry.go, src/server/internal/releasecontract/effect_registry_test.go</files>
  <behavior>
    - Test V31.1-R09: expected inventory and Go AST discovery match every HTTP/gRPC, worker claim/effect, outbound, financial, and operation seam exactly once.
    - Test V31.1-R09: runtime registry equals the selected monolith applicable projection; excluded standalone/gRPC seams remain inventoried with zero runtime registration.
    - Test V31.1-R09/T-RDY-03: all twelve real standalone roots from Plans 02/09 appear as authored expected-excluded entrypoint rows whose source literal matches the contract entrypoint ID and whose runtime descriptor count is zero.
    - Test V31.1-R09/T-RDY-03: live `src/server/internal/agent/executor.go` builtin, custom API HTTP, local Python process, Python sandbox, and MCP seams each have distinct expected/static/runtime rows; `agent/tools.Registry` rows remain separately classified and cannot substitute for them.
    - Test V31.1-R09/T-RDY-03: EffectRegistry rejects unknown and duplicate registration, including a duplicate owner constructor; two valid effects on one constructed instance do not mutate the registry and remain valid because each call re-guards independently.
    - Test V31.1-R09/T-RDY-03: zero rows, missing/extra/duplicate/unknown capability, owner/signature drift, profile applicability drift, and unclassified boundary each fail.
  </behavior>
  <action>After all Plan 04/05/06/07/10 descriptor producers and Plan 02/09 roots are complete, use RED first with `TestReadinessEffectCoverageContract`. Create `readiness-effect-surface/v1` here as the sole expected coverage authority; no predecessor may require its existence. Each row contains stable seam ID, repo-relative owner package and symbol, one Phase 31 capability ID, boundary enum, AST-call signature, authored entrypoint ID where applicable, and explicit profile disposition; it cannot define commitment or profile facts. Implement Go parser/AST discovery for the recognized production calls and a concurrency-safe `EffectRegistry` that rejects every duplicate or unknown descriptor. Runtime registration is construction-time wiring evidence only: each side-effect-free owner constructor registers its stable descriptor once, while dispatch/Search/Execute/request paths never register and only re-resolve current subject plus manager-backed Guard. A duplicate constructor against the same registry must fail, but two legal effects on the original instance leave its registry snapshot byte-for-byte unchanged and re-guard twice. `VerifyEffectCoverage` must first exact-join every discovered seam to expected inventory, then resolve the committed profile and compare runtime descriptors to only the applicable expected projection. For every applicable typed seam, additionally require the startup-built `RuntimeAuthorities.CapabilityBindings.Resolve(EffectID)` result to equal the expected/runtime descriptor capability; for catalog seams require the same `Authorities.CatalogAuthorizer` binding behavior. Include all twelve live standalone roots wired by Plans 02/09, Plan 10 direct Tavily/configured Chain/builtin web-search, distinct live `agent.ToolExecutor` builtin/custom-HTTP/local-Python/sandbox/MCP effects, the separately classified non-production Registry API, and excluded gRPC dispatches. Fixture cases delete each live executor row, duplicate one constructor descriptor, and substitute a Registry row to prove omission, repeated ownership, or API substitution cannot pass. Never infer current entrypoint/applicability, reload runtime authority, or derive capability from asset/file existence, environment or executable name.</action>
  <acceptance_criteria>
    - The checked-in inventory covers all seven boundary classes and references only IDs in the validated Phase 31 contract.
    - Every one-field mismatch family produces a distinct stable nonzero error in the named Go suite.
    - A successful join has nonzero applicable rows and exact set equality; ordering cannot hide duplicates.
    - The twelve standalone expected-excluded rows have exact root/entrypoint ownership and zero runtime descriptors under monolith.
    - Five live ToolExecutor effect classes exact-join independently from Registry rows; deleting any live row or presenting Registry-only coverage fails.
    - EffectRegistry remains unchanged across repeated effects on one instance, rejects duplicate constructors/descriptors, and never participates in per-call authorization.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessEffectCoverageContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.1-R09 and T-RDY-03 make guard coverage independently machine-checkable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose evidence-only CLI and one-field readiness fixtures</name>
  <read_first>
    - src/server/cmd/release-contract/main.go
    - src/server/cmd/release-contract/main_test.go
    - src/server/internal/surfacereport/readiness.go
    - src/server/internal/surfacereport/writer.go
    - scripts/verify-release-contract-fixtures.sh
    - scripts/run-go-tests-matched.sh
  </read_first>
  <files>src/server/cmd/release-contract/main.go, src/server/cmd/release-contract/main_test.go, scripts/verify-readiness-contract-fixtures.sh</files>
  <behavior>
    - Test V31.1-R10/T-RDY-01: offline inspector validates runtime-produced snapshot with trusted resolvers/evaluator and cannot call a manager mutation or authorize runtime.
    - Test V31.1-R10: report output uses registered readiness details, trusted identity/profile, atomic writer, empty skips, and repository-local claim class.
    - Fixture V31.1-R01/R02/R09/R10: one-field identity/timing/snapshot/details/inventory/claim mutations each fail with the expected stable code.
  </behavior>
  <action>Use RED first with `TestRunReadinessInspectionContract`, then extend the existing Phase 31 CLI without changing its identity/profile/report contracts. Require explicit repo/contract/schema/profile/snapshot paths, bound all reads, use `FileProfileResolver` plus trusted `IdentityProvider`, and delegate freshness only to the Go evaluator/`NewReadinessReport`; do not accept time, generation, availability, identity, or capability overrides. `inspect-readiness` emits sanitized evidence, `report-readiness` uses Phase 31 `AtomicWriter`, and `verify-readiness-snapshot` returns stable nonzero status. Create the shell fixture with temp Git/filesystem cleanup and dynamic invocations of real Go tests/CLI for seeded-file authority, one-nanosecond boundaries, structural refresh retention, report details, effect exact join, sensitive-field leakage, identity splice, non-empty skip, and claim ceiling. The fixture must assert stable codes and zero-effect counters, not source tokens.</action>
  <acceptance_criteria>
    - CLI tests dynamically reject all authority/identity/timing override flags and prove manager state is unchanged.
    - Fixture script runs the Phase 31 matched-test helper using its actual `<package> <anchored-regex> [args...]` interface, with no `--` or caller `-run` form.
    - Passing output records E1/repository-local, exact profile/identity/generation/times, empty skips, and residual risk; E3/E4/commercial claims fail.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./cmd/release-contract '^TestRunReadinessInspectionContract$' &amp;&amp; bash -n scripts/verify-readiness-contract-fixtures.sh &amp;&amp; bash scripts/verify-readiness-contract-fixtures.sh &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.1-R01/R02/R09/R10 and T-RDY-01/T-RDY-02/T-RDY-03/T-RDY-05/T-RDY-06/T-RDY-10 have executable fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| HTTP caller -> guarded business handler | Untrusted dispatch must cross current manager verdict. |
| predecessor runtime descriptors/live AST -> Plan 03 manifest/verifier | The later independent expected set proves every real effect is classified without becoming a forward build dependency. |
| runtime snapshot -> offline CLI/report | Evidence input may be inspected but never promoted to authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-RDY-03 | Elevation of Privilege | unguarded effect seam | high | mitigate | Expected/AST/runtime exact join, nonzero set, descriptor uniqueness, and HTTP zero-call tests. |
| T-RDY-05 | Spoofing | health/audit classification | high | mitigate | Separate live/ready semantics, current manager only, typed report failures, and no file fallback. |
| T-RDY-06 | Information Disclosure | app projection/errors | high | mitigate | Separate DTOs, serialization allowlists, auth, and dynamic forbidden-field assertions. |
| T-RDY-10 | Repudiation | evidence class/skips | high | mitigate | Empty committed skips, repository-local claim validation, stable codes, and mutation fixtures. |
</threat_model>

<verification>
- All dynamic Go selectors use `scripts/run-go-tests-matched.sh <package> <anchored-regex> [args...]` exactly.
- Text/AST inventory supports coverage verification but never substitutes for zero-call runtime behavior tests.
- Plan 03 is the first and only creator of the expected manifest; Plan 06/10 dynamic package suites must already pass without it.
- `scripts/verify-quality-gates.sh` remains untouched; Phase 31.2 owns aggregate integration.
</verification>

<success_criteria>
- Current manager controls HTTP/control-plane views with stable sanitized outcomes.
- Expected, discovered and runtime effect sets join exactly for monolith, while excluded seams register zero times.
- The exact join includes RuntimeAuthorities typed/catalog bindings, the complete Plan 10 monolith web-search path, and every real live ToolExecutor builtin/custom-HTTP/local-Python/sandbox/MCP seam separately from Registry API rows.
- Offline evidence tooling cannot become runtime authorization and cannot overclaim beyond E1/E2.
</success_criteria>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-03-SUMMARY.md` when done.
</output>
