---
phase: 31.1-readiness-fail-closed
plan: "16"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/internal/http/readiness_handler.go
  - src/server/internal/http/readiness_handler_test.go
autonomous: true
gap_closure: true
requirements: [RELS-01]
must_haves:
  truths:
    - "Admin and App projections reject/sanitize evaluation-level stale, unavailable, and identity errors before rendering capability rows (D-RDY-02, D-RDY-17, D-RDY-18)."
    - "At `now == validUntil` fresh enabled rows remain available; at `validUntil + 1ns` neither response can contain an enabled row (D-RDY-04)."
    - "App projection continues to omit excluded inventory and both surfaces expose no raw probe detail, endpoint, credential, or secret (D-RDY-17)."
  artifacts:
    - path: "src/server/internal/http/readiness_handler.go"
      provides: "Evaluation-error-first Admin/App projection behavior"
      contains: "evaluation.ErrorCode"
    - path: "src/server/internal/http/readiness_handler_test.go"
      provides: "Exact expiry and sanitized stale/unavailable projection tests"
      contains: "TestReadinessProjectionFreshnessBoundaryContract"
  key_links:
    - from: "ReadinessManager.Evaluate"
      to: "ReadinessHandlers.Admin/App"
      via: "top-level ErrorCode is checked before iterating last-known capability values"
      pattern: "ErrorCode"
    - from: "Evaluator.ValidUntil"
      to: "Admin/App enabled rows"
      via: "the Go evaluator alone decides exact freshness; handlers copy no time logic"
      pattern: "ValidUntil"
---

<objective>
Close CR-08 and enforce the approved narrow UI state contract without adding frontend routes or design work.

Purpose: Last-known capability values are diagnostic data, not current permission when evaluation itself is stale or unavailable.
Output: Fail-closed projection handlers and exact nanosecond boundary tests.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-UI-SPEC.md
@.planning/phases/31.1-readiness-fail-closed/31.1-VERIFICATION.md
@.planning/phases/31.1-readiness-fail-closed/31.1-03-SUMMARY.md
@src/server/internal/http/readiness_handler.go
@src/server/internal/http/readiness_handler_test.go
@src/server/internal/releasecontract/readiness.go

<constraints>
- The Go evaluator remains the only freshness implementation; handlers must not compare clocks or add a grace period.
- No frontend production caller, page, route, navigation item, dashboard, copy, or visual change is in scope.
- Preserve read-only `capabilityId` response behavior; mutations remain unchanged.
- CR-06/CR-09 remain Phase 34, WR-02 remains Phase 35, and Envelope schema parity remains Phase 31.2.
- Evidence is repository-local E1 only.
</constraints>

<interfaces>
Choose one approved server behavior consistently for evaluation-level errors: return the existing sanitized readiness error response, or return a top-level stale/unavailable state with every row forced disabled. Prefer the existing error response because it cannot be misread as current inventory. Exact freshness comes from `Evaluator.Evaluate`; handlers consume only `Evaluation.ErrorCode`.
</interfaces>
</context>

## Artifacts this phase produces

- Evaluation-error-first Admin/App projections.
- Exact validUntil and validUntil+1ns endpoint tests.
- Sanitized stale/unavailable/identity-mismatch assertions.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail projections closed on evaluation-level errors</name>
  <files>src/server/internal/http/readiness_handler.go, src/server/internal/http/readiness_handler_test.go</files>
  <read_first>
    - src/server/internal/http/readiness_handler.go
    - src/server/internal/http/readiness_handler_test.go
    - .planning/phases/31.1-readiness-fail-closed/31.1-UI-SPEC.md
    - src/server/internal/releasecontract/readiness.go
  </read_first>
  <behavior>
    - Stale, unavailable, and identity-mismatched evaluations cannot return enabled rows.
    - Errors use stable sanitized readiness codes.
    - Admin/App never expose stale capability arrays, observations, endpoint details, or secrets.
  </behavior>
  <action>Immediately after `manager.Evaluate()` in Admin and App, check `evaluation.ErrorCode` and route it through `writeReadinessHTTPError` before sorting or projecting capability values. Do not add handler-side time comparisons or reuse prior projections. Add table-driven error-code cases with an intentionally enabled stale capability in the stub evaluation so the test proves no enabled row leaks.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestReadinessProjectionEvaluationErrorContract$'</automated>
  </verify>
  <acceptance_criteria>
    - All evaluation-level error cases return non-success or zero enabled rows.
    - Response bodies contain stable codes and none of the seeded raw detail/secret strings.
    - Fresh projection behavior and Admin/App inventory separation remain unchanged.
  </acceptance_criteria>
  <done>Last-known capability values cannot be advertised as current after evaluation failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the exact expiry boundary through both endpoints</name>
  <files>src/server/internal/http/readiness_handler_test.go</files>
  <read_first>
    - src/server/internal/http/readiness_handler_test.go
    - src/server/internal/releasecontract/readiness_test.go
    - .planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
  </read_first>
  <behavior>
    - Evaluator result at exact validUntil has no error and both projections may include the fresh enabled row.
    - Evaluator result one nanosecond later is `readiness_stale` and neither projection returns an enabled row.
  </behavior>
  <action>Construct the two evaluations through the real `releasecontract.Evaluator` using the authored profile and identical observations at the exact boundary and one nanosecond later. Feed each result to the same Admin/App handlers. Assert exact status/body semantics and no handler-local freshness calculation.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestReadinessProjectionFreshnessBoundaryContract$'</automated>
  </verify>
  <acceptance_criteria>
    - Exact boundary remains fresh.
    - `validUntil + 1ns` fails closed for both endpoints.
    - No frontend implementation file changes.
  </acceptance_criteria>
  <done>The server and approved UI contract share the exact evaluator-owned nanosecond boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-memory evaluation -> Admin/App DTO | Last-known values cross into operator and user-visible choices. |
| Server error -> authenticated client | Diagnostic codes must not disclose probe endpoints or secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-16-01 | Elevation of Privilege | Stale projection | high | mitigate | Reject top-level evaluation errors before projecting enabled rows. |
| T-31.1-16-02 | Information Disclosure | Readiness error response | high | mitigate | Reuse sanitized stable codes and assert raw detail/secret absence. |
| T-31.1-16-03 | Tampering | Freshness boundary | high | mitigate | Use real evaluator results at exact nanosecond boundaries; no handler clock logic. |
</threat_model>

<verification>
- Run both focused handler selectors and `cd src/server &amp;&amp; go test ./internal/http ./internal/releasecontract -count=1`.
- Re-run the unchanged frontend capability normalization test during Plan 20.
- Run `git diff --check`.
</verification>

<success_criteria>
- Canonical projection gap and UI-SPEC stale/unavailable contract are closed backend-only.
- Exact-boundary and sanitized-error evidence is non-vacuous.
</success_criteria>

<commit_policy>
Create one atomic commit: `fix(31.1-16): fail stale readiness projections closed`. Do not push. Plan 20 ends with a HARD EXTERNAL/MANUAL POST-CLOSEOUT GATE handoff; no GSD executor, verifier, tracker, hook, or autonomous orchestrator step owns final Stage B or push.
</commit_policy>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-16-SUMMARY.md` and record that no frontend implementation change or screenshot was required by the approved UI scope fence.
</output>
