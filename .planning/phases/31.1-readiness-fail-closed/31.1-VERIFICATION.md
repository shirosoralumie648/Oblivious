---
phase: 31.1-readiness-fail-closed
verified: 2026-07-18T18:13:55Z
status: gaps_found
score: "1/5 must-haves verified"
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "Runtime validates trusted contract/profile/build, publishes generation 1 after bounded bootstrap, and refreshes without audit-file authority."
    status: partial
    reason: "The normal path and isolated lifecycle test pass, but concurrent Bootstrap callers can both pass the pre-check and publish generations 1 and 2; context-ignoring probes also leak an inner goroutine on every timed-out refresh."
    artifacts:
      - path: "src/server/internal/releasecontract/readiness_manager.go"
        issue: "Bootstrap checks current state before probe work and outside publicationMu; runBoundedProbes starts an unbounded inner goroutine for each call."
    missing:
      - "Serialize the bootstrap state transition so exactly one Bootstrap succeeds and generation remains 1."
      - "Prevent repeated leaked work from probes that ignore cancellation, and add the missing concurrent-bootstrap regression case."
  - truth: "Every new HTTP/gRPC, worker, Provider/model/tool/channel, and financial side effect rechecks the same current manager and is covered by an independent exact-join authority."
    status: failed
    reason: "The production exact-join is not gated, strict router composition omits financial readiness for Marketplace checkout, production Agent web search is injected after authorized executor construction, and Relay semantic-cache hits return success before current model authorization."
    artifacts:
      - path: "src/server/internal/releasecontract/effect_registry.go"
        issue: "ToolExecutor discovery is marker-string based and RuntimeAuthorities verification is optional."
      - path: "scripts/verify-readiness-contract.sh"
        issue: "The phase gate never invokes VerifyEffectCoverage against production descriptors and authorities."
      - path: "src/server/internal/http/router.go"
        issue: "Strict Marketplace handler construction omits withMarketplaceFinancialReadiness."
      - path: "src/server/internal/http/server.go"
        issue: "WebSearchProvider is set only after NewAuthorizedToolExecutor has cloned builtin tools."
      - path: "src/server/internal/relay/router.go"
        issue: "Semantic-cache success and usage recording precede requireModel."
    missing:
      - "Wire the strict production carriers before every consumer is constructed and add router/runtime composition zero-call tests."
      - "Make production effect discovery structural, require authorities, expose strict production descriptors, and invoke the exact join in the phase gate."
      - "Authorize model/catalog state before semantic-cache success or usage recording."
  - truth: "Admin/app projections, /livez, /readyz, audit export, Compose, and canonical Kubernetes consume the same current manager and identity."
    status: failed
    reason: "Canonical Compose/Kubernetes do not configure a real readiness probe source, so startup emits only dependency_unproven blocked observations; Admin and App projections ignore evaluation.ErrorCode and advertise stale capability values as Enabled."
    artifacts:
      - path: "src/server/cmd/server/main.go"
        issue: "Without OBLIVIOUS_READINESS_PROBE_BASE_URL every dependency receives startupBlockedProbe."
      - path: "docker-compose.yml"
        issue: "No supported readiness probe endpoint or real dependency probe wiring is supplied."
      - path: "deploy/kubernetes/app-deployment.yaml"
        issue: "No supported readiness probe endpoint or sidecar/service is supplied."
      - path: "src/server/internal/http/readiness_handler.go"
        issue: "Admin and App render evaluation.Capabilities without first rejecting evaluation.ErrorCode."
    missing:
      - "Build probes from shipped dependency configuration or deploy/configure the supported probe endpoint in every canonical asset."
      - "Fail or force-disable Admin/App projection rows on stale/unavailable evaluation and add exact-expiry tests."
  - truth: "Server-derived catalogBindings are read-only metadata and mutation plus execution always re-resolve current authority."
    status: failed
    reason: "The isolated ModelOption/ToolDefinition tests pass, but strict router composition creates admin.NewService instead of admin.NewReadinessService, leaving model sync/detect/apply authorization conditional; compatibility ToolExecutor remains allow-open, and chat strict decoding accepts trailing JSON values."
    artifacts:
      - path: "src/server/internal/http/router.go"
        issue: "Strict production composition does not construct the authority-required Admin service."
      - path: "src/server/internal/admin/channel_service.go"
        issue: "Model catalog authorization is skipped whenever modelReadiness is nil."
      - path: "src/server/internal/agent/executor.go"
        issue: "NewToolExecutor creates strict=false and authorizeTool then returns success."
      - path: "src/server/internal/http/chat_handler.go"
        issue: "decodeStrictChatMutation performs one Decode and does not reject a trailing JSON value."
    missing:
      - "Require the readiness-aware Admin constructor in strict composition and prove denied model sync/detect/apply makes zero probe/store calls."
      - "Make compatibility ToolExecutor deny effects and require EOF after strict mutation decoding."
  - truth: "Phase completion has an exact clean current-HEAD E1/E2 Stage B result with zero skips."
    status: failed
    reason: "Stage B passed at acd2fe84a9abd69a6b96f8455d9207386b921801, but current HEAD is a66e11f3e58443a846e99507f6583437b3388879 with three later planning-only commits and a dirty .planning/config.json. The older Stage B is useful historical E2 evidence but cannot certify current HEAD."
    artifacts:
      - path: "/var/tmp/oblivious-31-1-stage-b-acd2fe8/harness-result.json"
        issue: "Identity-bound to acd2fe84a9abd69a6b96f8455d9207386b921801, not current HEAD."
    missing:
      - "After code gaps and documentation gate failures are closed, commit the final tree and rerun Stage B at that exact clean HEAD."
deferred:
  - truth: "The two readiness GET 200 responses reference the shared OpenAPI Envelope schema."
    addressed_in: "Phase 31.2"
    evidence: "Phase 31.2 goal and success criterion 1 explicitly own OpenAPI/runtime bidirectional parity and drift blocking."
  - truth: "Strict Relay Files operations dispatch without inventing a model, and persisted batch jobs use their originating provider/channel."
    addressed_in: "Phase 34"
    evidence: "Phase 34 owns supported /v1 compatibility, Relay provider routing, retries, usage, and request-to-audit lifecycle correctness."
  - truth: "MCP Connect requires initialized notification and initial tool discovery before persisting connected."
    addressed_in: "Phase 35"
    evidence: "Phase 35 owns the real MCP registration/tool customer journey and operational behavior."
---

# Phase 31.1: Dynamic Readiness and Continuous Fail-Closed Verification Report

**Phase Goal:** Current runtime uses one in-process dynamic ReadinessManager to continuously compute availability and fail closed before every new side effect.
**Verified:** 2026-07-18T18:13:55Z
**Status:** gaps_found
**Re-verification:** No - initial canonical verification
**Evidence ceiling:** Repository-local E1/E2 only; no target/live, profile-parity, E3/E4, or commercial-release claim.

## Goal Achievement

### Observable Truths

| # | ROADMAP truth | Status | Evidence |
|---|---|---|---|
| 1 | Trusted startup performs DB/migration/bootstrap, publishes generation 1, refreshes, and never authorizes from audit files. | FAILED | Normal startup/lifecycle tests pass, but `Bootstrap` is not serialized and concurrent callers can publish generation 2; timed-out context-ignoring probes leak inner goroutines. |
| 2 | Authored 30s refresh, 120s max age, and 30s future skew are evaluated by one Go evaluator. | VERIFIED | Checked-in monolith profile contains exactly 30/120/30; evaluator, manager lifecycle, and publication race tests pass, including nanosecond boundaries. |
| 3 | Every new dispatch/claim/effect rechecks one guard and denial produces zero downstream calls. | FAILED | Package-level tests pass, but production effect coverage is not gated and real Marketplace/web-search/cache composition contains bypasses or missing carrier wiring. |
| 4 | Admin/app/health/audit/deployment surfaces consume the same current manager and identity. | FAILED | `/readyz` is manager-backed, but stale Admin/App projections remain enabled and shipped deployments have no configured dependency probe source outside the harness. |
| 5 | Server catalog bindings are read-only and re-resolved at mutation and execution. | FAILED | Isolated API/frontend tests pass, but production Admin model mutation can run without `modelReadiness`; compatibility execution remains allow-open. |

**Score:** 1/5 truths verified

### Required Artifacts

The structured artifact query found all 45/45 PLAN-declared artifacts and all 29/29 pattern-based key links. Presence is not goal proof; substantive/wiring results are below.

| Plan slice | Artifact status | Functional status | Details |
|---|---|---|---|
| 31.1-01 evaluator/manager/catalog/report | Present and substantive | PARTIAL | Core evaluator, immutable snapshots, reports, and race test work; concurrent Bootstrap and probe leak remain. |
| 31.1-02 startup/runtime composition | Present and substantive | PARTIAL | Ordered startup test passes; strict Admin, Marketplace, and web-search carrier wiring is incomplete. |
| 31.1-03 control plane/effect coverage/CLI | Present and substantive | FAILED WIRING | Reports and routes work in isolation; exact coverage is marker-based, optional-authority, and absent from the phase gate. |
| 31.1-04 worker guards | Present and substantive | VERIFIED LOCALLY | Scheduled/channel/archive/batch focused zero-call suites pass; production-wide exact join is still missing. |
| 31.1-05 Relay/Chat/model | Present and substantive | PARTIAL | Per-attempt suite passes, but semantic-cache success precedes model authorization. |
| 31.1-06 MCP/Registry/web search | Present and substantive | VERIFIED LOCALLY | Package suites pass; MCP connection lifecycle is deferred to Phase 35. |
| 31.1-07 finance | Present and substantive | FAILED WIRING | Settlement tests pass, but strict Marketplace HTTP composition omits provider-dispatch readiness. |
| 31.1-08 deployment/harness | Present and substantive | PARTIAL | Historical standalone/aggregate/Stage B E2 evidence passes; canonical assets cannot become ready without harness-only probe configuration. |
| 31.1-09 standalone entrypoints | Present and substantive | VERIFIED | Group-two and effectful-root preflight tests pass. |
| 31.1-10 live Agent/tools/frontend | Present and substantive | PARTIAL | Live executor/frontend tests pass in isolation; production web search is injected too late to install the authorized instance-scoped tool. |

### Key Link Verification

| From | To | Status | Details |
|---|---|---|---|
| Authored monolith profile | Go evaluator/manager | VERIFIED | Exact 30/120/30 values, trusted identity/profile closure, and current evaluation are exercised. |
| `RunEntrypoint` | server startup and `RuntimeAuthorities` | VERIFIED | Group-one/group-two and startup-order tests pass. |
| Startup `RuntimeAuthorities` | all production consumers | FAILED | Admin service, Marketplace handler, and Agent web-search construction do not receive/use the required carrier correctly. |
| Effect manifest/static discovery | production descriptors/authorities | NOT WIRED | `VerifyEffectCoverage` is only exercised with a synthetic runtime set and is not called by the release gate. |
| Manager evaluation | `/readyz` | VERIFIED | Evaluation-level errors fail readiness. |
| Manager evaluation | Admin/App projections | PARTIAL | Real data flows, but evaluation-level stale/unavailable errors are ignored. |
| Runtime audit | readiness/deployment reports | VERIFIED HISTORICALLY | Separate typed reports were produced with zero skips at the prior E2 tuple. |

### Data-Flow Trace

| Data | Source | Consumer | Status |
|---|---|---|---|
| Readiness timing and commitments | `config/release/contract.v1.json` | evaluator/manager | FLOWING |
| Dependency availability | canonical deployment configuration | manager probes | HOLLOW FOR SHIPPED TOPOLOGY - absent harness endpoint becomes `dependency_unproven` for every dependency |
| Current evaluation | manager | `/readyz` | FLOWING AND FAIL-CLOSED |
| Current evaluation | manager | Admin/App DTOs | FLOWING BUT STALE VALUES RENDER ENABLED |
| Startup authorities | server continuation | strict consumers | PARTIAL/DISCONNECTED at Admin, Marketplace provider dispatch, and Agent web search |
| Runtime descriptors | constructors | exact-join release gate | DISCONNECTED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full backend regression | `cd src/server && go test ./... -count=1` | Fresh orchestrator run passed | PASS |
| Authored evaluator | `bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessEvaluatorContract$'` | 1 concrete test passed | PASS |
| Manager lifecycle | `bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerLifecycleContract$'` | 1 concrete test passed | PASS |
| Publication race | `bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerPublicationRace$' -race` | 1 concrete test passed under race detector | PASS |
| Startup order | `bash scripts/run-go-tests-matched.sh ./cmd/server '^TestServerStartupOrderContract$'` | 1 concrete test passed | PASS |
| Effect coverage unit contract | `bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessEffectCoverageContract$'` | Synthetic/unit contract passed; production gate remains unwired | PASS, INSUFFICIENT |
| Control plane | `bash scripts/run-go-tests-matched.sh ./internal/http '^TestReadinessControlPlaneContract$'` | Handler contract passed; no stale Admin/App case | PASS, INSUFFICIENT |
| Live Agent executor | `bash scripts/run-go-tests-matched.sh ./internal/agent '^TestLiveAgentToolExecutorReadinessContract$'` | Isolated live-dispatch suite passed | PASS, INSUFFICIENT FOR COMPOSITION |
| Frontend capability identity | `pnpm --dir src/web exec vitest run src/features/agents/agentsApi.test.ts --reporter=dot` | 1 file, 9 tests passed | PASS |
| Static deployment mutation gate | `bash scripts/verify-readiness-deployment-contract.sh` | 3 mutations rejected; gate passed | PASS, MISSES PROBE SOURCE |
| OpenAPI contract | `bash scripts/verify-openapi-contract.sh` | Exit 1: both readiness GET 200 responses lack `#/components/schemas/Envelope` | FAIL, DEFERRED TO 31.2 |
| Patch formatting | `git diff --check` | Passed | PASS |

### Probe Execution

No PLAN/SUMMARY declares a conventional `scripts/**/tests/probe-*.sh`, and none exists. The phase uses named Go suites plus the deployment harness rather than verifier probe scripts.

### Requirements Coverage

| Requirement | Source | Status | Evidence |
|---|---|---|---|
| RELS-01 | ROADMAP Phase 31.1 and all ten PLAN files | BLOCKED | The manifest/evaluator foundation exists, but current runtime enforcement and canonical deployment consumption are incomplete at production composition boundaries. |

### Review Claim Revalidation

All 14 REVIEW claims still describe current HEAD. Their phase disposition is independent of the review labels.

| ID | Current evidence | Disposition |
|---|---|---|
| CR-01 | `effect_registry.go:269-312,425`; `verify-readiness-contract.sh:26-28`; `main.go:239-261` | CURRENT BLOCKER |
| CR-02 | `router.go:348-351` creates `admin.NewService`; channel checks are conditional at `channel_service.go:247,307,359` | CURRENT BLOCKER |
| CR-03 | `router.go:381-391` omits `withMarketplaceFinancialReadiness`; nil readiness returns success | CURRENT BLOCKER |
| CR-04 | `server.go:125-155` sets provider after authorized executor construction; `executor.go:132-145` installs the authorized tool only during construction | CURRENT BLOCKER |
| CR-05 | `relay/router.go:396-418` returns cache success before `requireModel` at line 439 | CURRENT BLOCKER |
| CR-06 | strict Files paths call `RouteWithBilling(..., model="")`, which is rejected | VALID, DEFERRED TO PHASE 34 |
| CR-07 | `main.go:291-295` uses blocked probes unless the harness-only base URL is set; canonical assets omit it | CURRENT BLOCKER |
| CR-08 | Admin/App methods at `readiness_handler.go:132-174` never check `evaluation.ErrorCode` | CURRENT BLOCKER |
| CR-09 | one first-enabled adapter serves all batch jobs, and persisted jobs contain no provider/channel identity | VALID, DEFERRED TO PHASE 34 |
| WR-01 | `readiness_manager.go:335-355` can strand an inner probe goroutine per timeout | CURRENT GAP |
| WR-02 | `mcp/client.go:223-237` ignores initialized/list failure then records connected | VALID, DEFERRED TO PHASE 35 |
| WR-03 | `executor.go:67-72,223-225` keeps compatibility execution allow-open | CURRENT GAP |
| WR-04 | `chat_handler.go:130-133` does not require EOF after the first JSON object | CURRENT GAP |
| WR-05 | `Bootstrap` pre-check is outside `publicationMu`; concurrent calls can publish generation 2 | CURRENT GAP |

### UAT and Security Artifacts

- `31.1-UAT.md` records 35/35 accepted checkpoints, including five user-approved checks. These validate isolated and conversational acceptance but do not exercise the production composition paths above.
- `31.1-SECURITY.md` records 35 threats closed at ASVS L1. That conclusion is superseded for phase closure by the observable current production gaps in CR-01 through CR-05, CR-07, CR-08, WR-01, WR-03, WR-04, and WR-05.

### Current Evidence Identity

- Historical exact clean Stage B: commit `acd2fe84a9abd69a6b96f8455d9207386b921801`, profile `monolith`, migration `applied_and_validated`, image digest `sha256:60fed20ed99938b74ef60586eebd3cdd0ae27251f9a5c2232bde2d2799c44474`, `skippedChecks: []`.
- Current HEAD: `a66e11f3e58443a846e99507f6583437b3388879`; implementation paths are unchanged from `acd2fe8`, but three later planning artifacts changed the tree and `.planning/config.json` is dirty.
- Therefore prior Stage B remains historical repository-local E2 evidence, not current-HEAD certification.

## Gaps Summary

Phase 31.1 has a substantive evaluator/manager foundation and broad isolated zero-call coverage, but the goal is not achieved in the production runtime. The primary failure pattern is that strict package constructors exist while server/router composition either omits them, supplies authority too late, or never verifies the resulting production descriptor set.

Next actions:

1. Repair current production composition for Admin, Marketplace checkout, Agent web search, and Relay cache authorization; add router/BuildRuntime tests that exercise the real constructed services.
2. Replace marker-based optional effect proof with a structural, authority-required exact join over production descriptors and invoke it from the phase gate.
3. Supply real dependency probe wiring in canonical Compose/Kubernetes and fail stale Admin/App projections closed.
4. Close manager/bootstrap and compatibility-decoder gaps, then rerun focused tests, full Go/frontend/docs gates, commit once, and run exact clean-HEAD Stage B with zero skips.

---

_Verified: 2026-07-18T18:13:55Z_
_Verifier: Codex (gsd-verifier role)_
