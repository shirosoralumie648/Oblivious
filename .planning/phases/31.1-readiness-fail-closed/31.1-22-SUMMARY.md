---
phase: 31.1-readiness-fail-closed
plan: "22"
subsystem: kubernetes-readiness
tags: [kubernetes, kafka, readiness, fail-closed, fixtures, release-evidence]

# Dependency graph
requires:
  - phase: 31.1-readiness-fail-closed
    provides: "Plan 17 deployment-owned Kafka readiness probe and canonical broker address"
provides:
  - "Canonical Kubernetes runner applies and bounded-waits the Kafka StatefulSet before the server workload"
  - "Static exact join from ConfigMap broker DNS/port through Kafka Service, StatefulSet, and runner order"
  - "Real-runner fake-kubectl trace plus ten fail-closed deployment/order/anti-vacuity mutations"
affects: [31.1-20, RELS-01, kubernetes-readiness, release-evidence]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Advertised Kubernetes dependency addresses exact-join a canonical applied workload and bounded rollout wait"
    - "Repository fixtures execute the real runner with PATH-injected command fakes and reject empty proof"

key-files:
  created:
    - scripts/verify-k8s-readiness-runner-fixtures.sh
  modified:
    - scripts/k8s-validate.sh
    - scripts/verify-readiness-deployment-contract.sh

key-decisions:
  - "Use the existing three-broker kafka StatefulSet unchanged because its Service DNS, selectors, replica count, and client port already satisfy the canonical contract."
  - "Keep /healthz only as the port-forward liveness check and require an independent /readyz success before deploy-smoke."
  - "Treat verifier and runner completion as non-vacuous evidence: no-op verifier output and an empty runner trace both fail."

patterns-established:
  - "Kubernetes dependency order is apply Kafka, bounded wait Kafka, apply app, wait server, require /readyz, then smoke."
  - "Fixture-only static validation emits a required completion marker before evidence can count as passing."

requirements-completed: []

coverage:
  - id: D1
    description: "The supported Kubernetes runner applies canonical Kafka exactly once, bounded-waits statefulset/kafka before app rollout, and requires /readyz before smoke."
    requirement: RELS-01
    verification:
      - kind: integration
        ref: "bash scripts/verify-readiness-deployment-contract.sh"
        status: pass
      - kind: other
        ref: "bash -n scripts/k8s-validate.sh scripts/verify-readiness-deployment-contract.sh scripts/verify-k8s-readiness-runner-fixtures.sh"
        status: pass
    human_judgment: false
  - id: D2
    description: "A fake-kubectl run of the real runner proves exact Kafka-before-app-before-ready order and ten independent mutations fail closed."
    requirement: RELS-01
    verification:
      - kind: integration
        ref: "bash scripts/verify-k8s-readiness-runner-fixtures.sh"
        status: pass
    human_judgment: false

# Metrics
duration: 16 min
completed: 2026-07-21
status: complete
---

# Phase 31.1 Plan 22: Kafka-Aware Kubernetes Readiness Summary

**The supported Kubernetes runner now deploys and bounded-waits canonical Kafka before server rollout, then proves `/readyz` before smoke with non-vacuous repository fixtures.**

## Performance

- **Duration:** 16 min
- **Started:** 2026-07-21T02:35:00Z
- **Completed:** 2026-07-21T02:51:11Z
- **Tasks:** 2 completed
- **Files modified:** 3

## Accomplishments

- Added the canonical `deploy/kubernetes/kafka.yaml` to the data-service apply stage and bounded `statefulset/kafka` rollout with `OBLIVIOUS_K8S_KAFKA_TIMEOUT` defaulting to `300s` before any app workload apply.
- Added a distinct `/readyz` success gate after server rollout and port-forward liveness, before `deploy-smoke.sh`; `/healthz` no longer serves as readiness proof.
- Exact-joined `KAFKA_BROKERS` Service DNS/port to the Kafka client Service, StatefulSet namespace/selectors, three replicas, and named client container port.
- Executed the real copied runner with fake `kubectl` and `curl`, proving exact command order without a cluster, target secret, provider, or live environment.
- Rejected ten isolated mutations, including both verifier and runner anti-vacuity cases.

## Ordered Runner Trace

1. `kubectl apply -f deploy/kubernetes/kafka.yaml`
2. `kubectl -n oblivious rollout status statefulset/kafka --timeout=300s`
3. `kubectl apply -f /tmp/oblivious-k8s-validate.<id>/app-deployment.yaml`
4. `kubectl -n oblivious rollout status deployment/oblivious-server`
5. Port-forward `/healthz` succeeds.
6. `curl -fsS http://127.0.0.1:18080/readyz` succeeds.
7. The real `deploy-smoke.sh` runs `/healthz`, `/metrics`, app-route, and Relay-route checks.

## Mutation Matrix

| Mutation | Stable rejection boundary |
|---|---|
| Address advertised without Kafka apply | Missing unique ordered Kafka apply |
| Kafka apply without wait | Missing unique bounded StatefulSet wait |
| Duplicate Kafka apply | Exact-once command count |
| Wrong Kafka Service | ConfigMap address cannot resolve canonical Service |
| Wrong Kafka port | Broker port differs from client Service port |
| Wrong StatefulSet wait target | Required `statefulset/kafka` wait is absent |
| Kafka wait after app apply | Ordered source positions disagree |
| `/healthz` substituted for `/readyz` | Explicit readiness command is absent |
| Verifier not executed | Required verifier completion marker is absent |
| Runner not executed | Runner trace is empty |

**Mutation result:** 10/10 rejected for their intended stable reasons.

## Task Commit

The plan required one atomic production commit for both tasks:

1. **Task 1: Apply and wait for canonical Kafka before server readiness** - `8bfa593` (`fix(31.1-22): wire kafka into k8s readiness runner`)
2. **Task 2: Reject address-only and misordered Kafka readiness wiring** - `8bfa593` (`fix(31.1-22): wire kafka into k8s readiness runner`)

## TDD Evidence

- After adding the exact static contract but before changing the runner, `bash scripts/verify-readiness-deployment-contract.sh` failed with exit 1 and `Kubernetes runner must contain exactly one ordered command: kubectl apply -f deploy/kubernetes/kafka.yaml`.
- The minimal runner apply/wait/readyz implementation then made Task 1's verifier command pass.
- The real-runner fixture subsequently passed its positive trace and all ten negative cases.

## Files Created/Modified

- `scripts/k8s-validate.sh` - Applies Kafka, bounded-waits its StatefulSet, and explicitly requires `/readyz` before smoke.
- `scripts/verify-readiness-deployment-contract.sh` - Parses exact address/workload/runner links and source order; exposes a fixture-only assets check marker.
- `scripts/verify-k8s-readiness-runner-fixtures.sh` - Runs real runner bytes with command fakes and the ten-case mutation matrix.
- `deploy/kubernetes/kafka.yaml` - Validated as the canonical three-broker input and intentionally unchanged.

## Decisions Made

- `rollout status statefulset/kafka` with an explicit timeout is the bounded all-replicas-ready gate; app apply cannot begin until it succeeds.
- Kafka manifest fields were not normalized gratuitously: the existing client Service, StatefulSet selectors, namespace, three replicas, KRaft settings, resources, and persistent storage already exact-join.
- Fake command evidence is repository-local behavior proof only and cannot substitute for a target cluster run.

## Deviations from Plan

None - plan executed as written. The no-op verifier case is the explicit verifier half of the planned verifier/runner anti-vacuity mutation.

## Verification

- `bash -n scripts/k8s-validate.sh scripts/verify-readiness-deployment-contract.sh scripts/verify-k8s-readiness-runner-fixtures.sh` - PASS.
- `bash scripts/verify-k8s-readiness-runner-fixtures.sh` - PASS; positive real-runner trace plus 10/10 rejected mutations.
- `bash scripts/verify-readiness-deployment-contract.sh` - PASS; existing Docker/Compose/Kubernetes mutations also rejected.
- `git diff --check` - PASS before the production commit.
- Production commit `8bfa593` contains only the three Plan 22 scripts; protected user paths were not staged.

## Issues Encountered

None. No live cluster, target secret, external credential, or authentication gate was required.

## Repository-Local Claim Limit

This plan provides E1 repository-local static and fake-command behavior evidence only. It does not prove target Kubernetes apply/wait, live Kafka availability, E3/E4, profile parity, provider/payment behavior, final Stage B eligibility, commercial readiness, or push eligibility. `RELS-01` remains pending.

## User Setup Required

None for repository-local verification. A real target secret and reachable target cluster remain external release gates.

## Next Phase Readiness

Plan 31.1-22 is complete. Plan 31.1-20 is the only remaining phase plan and owns closeout re-audit plus the hard external/manual final Stage B and push handoff; this executor did not run either action.

---
*Phase: 31.1-readiness-fail-closed*
*Completed: 2026-07-21*

## Self-Check: PASSED

- Production commit `8bfa593` exists with exactly the three owned script paths.
- Kafka apply, bounded wait, app apply, server rollout, `/readyz`, and smoke are uniquely ordered in both static source proof and the fake-command trace.
- Coverage metadata classifies both Plan 22 deliverables as automated pass; `RELS-01` remains pending.
- Final Stage B was not run and no commit was pushed.
