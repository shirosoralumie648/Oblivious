---
phase: 31.1-readiness-fail-closed
plan: "01"
type: execute
wave: 1
depends_on: ["31-06"]
files_modified:
  - src/server/internal/releasecontract/readiness.go
  - src/server/internal/releasecontract/readiness_test.go
  - src/server/internal/releasecontract/readiness_manager.go
  - src/server/internal/releasecontract/readiness_manager_test.go
  - src/server/internal/releasecontract/catalog.go
  - src/server/internal/releasecontract/catalog_test.go
  - src/server/internal/surfacereport/readiness.go
  - src/server/internal/surfacereport/readiness_test.go
  - src/server/internal/http/router.go
autonomous: true
requirements: [RELS-01]
must_haves:
  truths:
    - "ReadinessManager 是唯一 runtime capability authorization authority；审计 snapshot 只写不读，离线 inspector 也不能授权 runtime（D-RDY-01, D-RDY-02, D-RDY-19）。"
    - "唯一 Go evaluator 使用 authored monolith profile 的 30s/120s/30s 合同和 UTC epoch nanoseconds；边界相等有效，超过一纳秒即拒绝（D-RDY-03, D-RDY-04）。"
    - "只有完整可评估 refresh 才发布单调 immutable generation；结构失败保留原 snapshot 且不延长其有效期（D-RDY-05, D-RDY-06, D-RDY-09）。"
    - "catalogBindings 与 typed effect mappings 只在 startup 从 validated contract 和 resolver-confirmed profile 编译为 immutable RuntimeAuthorities；unknown/ambiguous/runtime-class/profile mismatch fail closed，consumer 不得本地重读、硬编码或使用 global fallback（D-RDY-20, D-RDY-21, D-RDY-22）。"
    - "每个 composition root 提供源码固定的 authored EntrypointID；resolver-confirmed profile 不包含该 ID 时在依赖构造前拒绝，不能由 profile membership alone 推断当前 binary（D-RDY-07, D-RDY-11）。"
  artifacts:
    - path: "src/server/internal/releasecontract/readiness.go"
      provides: "ReadinessSnapshotV1, exact evaluator, stable verdict/reason codes, Guard contracts"
      exports: ["ReadinessManager", "Evaluation", "ReadinessSnapshotV1", "Guard", "Boundary"]
    - path: "src/server/internal/releasecontract/readiness_manager.go"
      provides: "Bounded probe coordination and atomic immutable generation publication"
      exports: ["Manager", "NewManager", "Probe", "HTTPDependencyProbe", "Clock"]
    - path: "src/server/internal/releasecontract/catalog.go"
      provides: "Server-authored catalog resolver plus immutable startup-compiled runtime authority carrier"
      exports: ["CatalogSubject", "CatalogResolver", "CatalogAuthorizer", "EffectID", "CapabilityBindings", "RuntimeAuthorities", "NewRuntimeAuthorities"]
    - path: "src/server/internal/surfacereport/readiness.go"
      provides: "Typed readiness details registration and side-effect-free evidence report constructor"
      exports: ["ReadinessSurfaceID", "ReadinessDetails", "NewReadinessReport"]
    - path: "src/server/internal/http/router.go"
      provides: "Shared RouterOptions injection seam before runtime consumers split"
      contains: "Readiness"
  key_links:
    - from: "src/server/internal/releasecontract/readiness_manager.go"
      to: "src/server/internal/buildinfo/provider.go"
      via: "Phase 31 trusted IdentityProvider is resolved before manager construction"
      pattern: "IdentityProvider"
    - from: "src/server/internal/releasecontract/readiness.go"
      to: "config/release/contract.v1.json"
      via: "validated DeploymentProfile readiness requirement supplies 30s/120s/30s"
      pattern: "refreshIntervalSeconds|maxAgeSeconds|allowedFutureSkewSeconds"
    - from: "src/server/internal/releasecontract/catalog.go"
      to: "src/server/internal/http/router.go"
      via: "NewRuntimeAuthorities compiles one immutable CatalogAuthorizer/CapabilityBindings value for RouterOptions"
      pattern: "RuntimeAuthorities"
    - from: "src/server/internal/surfacereport/readiness.go"
      to: "src/server/internal/surfacereport/registry.go"
      via: "Phase-owned typed details registration and shared validator/writer"
      pattern: "RegisterDetails"
---

<objective>
建立 Phase 31.1 的唯一动态授权内核：精确 evaluator、不可变 generation manager、server-authored catalog resolver，以及只用于证据的 readiness report adapter。

Purpose: 先锁定所有并行消费者共享的 verdict、时间、identity、catalog 和 audit 合同，避免各 handler/worker 自行解释 readiness。
Output: 可并发读取的 manager/guard、bounded probes、严格 catalog resolver、ReadinessSnapshotV1 与 readiness SurfaceReportV1 producer。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-RESEARCH.md
@.planning/phases/31.1-readiness-fail-closed/31.1-PATTERNS.md
@.planning/phases/31-release-contract-current-baseline/31-01-PLAN.md
@.planning/phases/31-release-contract-current-baseline/31-02-PLAN.md
@.planning/phases/31-release-contract-current-baseline/31-03-PLAN.md
@.planning/phases/31-release-contract-current-baseline/31-04-PLAN.md

<interfaces>
Phase 31 is an unexecuted hard dependency at planning time. Before implementation, verify its implementation provides these exact declared contracts; if any differs, stop and correct Phase 31 rather than adding a second 31.1 authority:

```go
func Load(ctx context.Context, repoRoot, contractPath, schemaPath string) (AuthoredContractV1, error)
func LoadBytes(ctx context.Context, repoRoot string, contractBytes, schemaBytes []byte) (AuthoredContractV1, error)
type ProfileResolver interface {
    ResolveCommittedProfile(ctx context.Context, repoRoot, contractPath, schemaPath, profileID string) (DeploymentProfile, error)
}
type IdentityProvider interface {
    Resolve(ctx context.Context, repoRoot, contractPath, schemaPath string) (buildinfo.BuildIdentityV1, error)
}
func RegisterDetails[T any](r *DetailsRegistry, surfaceID string, validate func(T) error) error
type ReportWriter interface { Write(context.Context, string, SurfaceReportV1) error }
```

This plan locks the shared 31.1 consumer contracts:

```go
type ReadinessManager interface {
    Bootstrap(context.Context) error
    StartRefresh(context.Context)
    Require(capabilityID string) error
    Evaluate() Evaluation
    ExportAudit(path string) error
}
type Guard interface {
    Require(context.Context, string, Boundary) error
}
type EffectDescriptor struct {
    ID           string
    CapabilityID string
    Boundary     Boundary
    Owner        string
}
type EffectRegistrar interface {
    Register(EffectDescriptor) error
}
type EntrypointID string
func RequireProfileEntrypoint(profile DeploymentProfile, entrypointID EntrypointID) error
func (a CatalogAuthorizer) ResolveAndRequire(context.Context, CatalogSubject, Boundary) (CapabilityID, error)
type EffectID string
type CapabilityBindings struct { /* private immutable lookup */ }
func (b CapabilityBindings) Resolve(effect EffectID) (CapabilityID, error)
type RuntimeAuthorities struct {
    CatalogAuthorizer CatalogAuthorizer
    CapabilityBindings CapabilityBindings
}
func NewRuntimeAuthorities(contract AuthoredContractV1, profile DeploymentProfile, guard Guard) (RuntimeAuthorities, error)
func NewReadinessReport(ctx context.Context, identities buildinfo.IdentityProvider, profiles releasecontract.ProfileResolver, repoRoot, contractPath, schemaPath, profileID string, input ReadinessInspection, outcome surfacereport.Outcome) (surfacereport.SurfaceReportV1, error)
```

`NewReadinessReport` is a side-effect-free constructor. It validates and returns one report value; it accepts no `ReportWriter` or output path. Plan 31.1-03 owns CLI persistence through Phase 31 `AtomicWriter` after construction.
</interfaces>
</context>

## Artifacts this phase produces

- New files: the eight readiness/release-report files listed in `files_modified`; `src/server/internal/http/router.go` is modified to establish the shared injection seam.
- New exported symbols: `Availability`, `Observation`, `CapabilityEvaluation`, `Evaluation`, `ReadinessSnapshotV1`, `Evaluator`, `Clock`, `Probe`, `HTTPDependencyProbe`, `ReadinessManager`, `Manager`, `NewManager`, `Boundary`, `Guard`, `ManagerGuard`, `EffectDescriptor`, `EffectRegistrar`, `EntrypointID`, `RequireProfileEntrypoint`, `CatalogSubject`, `CapabilityID`, `CatalogResolver`, `CatalogAuthorizer`, `EffectID`, `CapabilityBindings`, `RuntimeAuthorities`, `NewRuntimeAuthorities`, `ReadinessSurfaceID`, `ReadinessDetails`, and `NewReadinessReport`; `RouterOptions` gains `Readiness`, `Guard`, `Effects`, and `Authorities`.
- Typed `EffectID` constants cover worker claim/effect, Relay/Chat provider, MCP/tool/web-search, HTTP mutation, checkout, Admin refund, Marketplace payout/settlement, and the live Agent `ToolExecutor` builtin, custom-API HTTP, local-Python process, Python-sandbox, and MCP dispatch seams. Only `NewRuntimeAuthorities` maps them to authored capability IDs.
- Exact authored timing inputs consumed from Phase 31: `refreshIntervalSeconds=30`, `maxAgeSeconds=120`, `allowedFutureSkewSeconds=30`; 31.1 creates no environment/request/default substitute.
- Stable public denial codes consumed by all later plans: `build_identity_missing`, `build_identity_mismatch`, `profile_required`, `profile_excluded`, `readiness_unavailable`, `readiness_stale`, `capability_unknown`, `capability_disabled`, `capability_blocked`, `report_output_unwritable`.

<tasks>

<task type="auto">
  <name>Task 1: Lock the exact evaluator and immutable snapshot contract</name>
  <read_first>
    - .planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
    - docs/superpowers/specs/2026-07-15-phase-31-release-contract-design.md
    - src/server/internal/releasecontract/contract.go
    - src/server/internal/releasecontract/load.go
    - src/server/internal/buildinfo/identity.go
  </read_first>
  <files>src/server/internal/releasecontract/readiness.go, src/server/internal/releasecontract/readiness_test.go</files>
  <behavior>
    - Test V31.1-R01: trusted identity, explicit committed profile, generation greater than zero, complete observation set, and known reason codes are mandatory.
    - Test V31.1-R01: exact max-age and future-skew boundaries pass; one nanosecond beyond either boundary fails with the stable authored code.
    - Test V31.1-R01/T-RDY-02: request, environment, snapshot file, and caller-provided duration attempts cannot replace 30s/120s/30s.
    - Test V31.1-R03/T-RDY-03: `RequireProfileEntrypoint` rejects empty/unknown/excluded/current-profile-missing IDs, while an exact authored monolith entrypoint passes; no caller/env/executable-name fallback exists.
  </behavior>
  <action>Consume Phase 31 `AuthoredContractV1`, `DeploymentProfile`, and `buildinfo.BuildIdentityV1` directly; require the selected profile to be the resolver-confirmed committed profile. Read the three authored second fields from that profile and reject their absence or any value other than 30/120/30 for the authored monolith contract instead of inventing defaults. Normalize every instant to UTC and compare `UnixNano`; derive `ValidUntil` from the oldest applicable observation plus max age. Validate capability/reference closure, generation monotonic shape, availability enum, reason-code membership, identity/profile equality, observation completeness, and future skew in one evaluator. Define immutable result types with private construction/copy helpers so published maps/slices cannot be mutated by readers. In the same shared contract file, define `Boundary`, manager-backed `Guard`, `EffectDescriptor{ID, CapabilityID, Boundary, Owner}`, the sole `EffectRegistrar.Register(EffectDescriptor) error`, and `EntrypointID` plus `RequireProfileEntrypoint(profile,id)`. Entrypoint authorization requires a non-empty source-authored literal to be present/applicable in the resolver-confirmed profile; it never derives ID from environment, argv, executable path/name, asset existence, or profile membership alone. Per D-RDY-01 and T-RDY-01, provide no snapshot-file loader in the runtime package. Only after the production changes above are complete, add and run one top-level `TestReadinessEvaluatorContract` table for D-RDY-02 through D-RDY-04 as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - `TestReadinessEvaluatorContract` dynamically covers identity/profile/generation, enabled/disabled/blocked/unknown, exact nanosecond freshness/future boundaries, and unknown reason rejection.
    - The only timing values accepted for checked-in monolith are obtained from the validated authored profile and equal 30s/120s/30s.
    - No evaluator API accepts a duration override, audit path, caller identity, or raw profile string as authority.
    - Entrypoint tests prove exact source-authored ID membership and all forbidden derivation paths before Plan 02/09 wire real roots.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessEvaluatorContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.1-R01/V31.1-R03 and T-RDY-01/T-RDY-02/T-RDY-03 have non-vacuous evaluator plus authored-entrypoint implementation/verification proof.</done>
</task>

<task type="auto">
  <name>Task 2: Publish bounded immutable generations through the sole manager</name>
  <read_first>
    - src/server/internal/releasecontract/readiness.go
    - src/server/internal/releasecontract/contract.go
    - src/server/internal/surfacereport/writer.go
    - src/server/internal/http/server.go
  </read_first>
  <files>src/server/internal/releasecontract/readiness_manager.go, src/server/internal/releasecontract/readiness_manager_test.go</files>
  <behavior>
    - Test V31.1-R02: bootstrap publishes generation 1; a complete refresh publishes exactly current+1 and readers see only complete immutable snapshots.
    - Test V31.1-R02/T-RDY-04: a dependency timeout/error becomes a typed blocked observation within deadline, while structural evaluation failure preserves generation/checkedAt/validUntil byte-for-byte.
    - Test V31.1-R02: fake time triggers no early refresh, then refreshes at each authored 30-second cadence; retained old state naturally becomes stale.
    - Test V31.1-R02/T-RDY-01: a seeded audit file is ignored, excluded probes have zero calls, and audit write failure cannot change memory authorization.
    - Test V31.1-R02/T-RDY-10: an HTTP dependency probe transport failure publishes blocked, while a transport-success response with malformed availability/reason is structural and preserves the prior generation/times exactly.
  </behavior>
  <action>`NewManager` accepts only already validated contract, trusted identity, resolver-returned profile, evaluator, clock, applicable probes, deadline policy, and a write-only atomic audit sink; nil dependencies fail closed. Build every candidate outside publication, bound each probe with child context, synthesize typed blocked observations for timeout/error, reject incomplete/malformed candidates, deep-freeze collections, and serialize generation updates under one publication lock before `atomic.Pointer.Store`. Add `HTTPDependencyProbe` for normal configured external dependency health endpoints: timeout/non-success/transport error returns an authored blocked observation, while HTTP success with a malformed/unknown availability or reason produces a structurally invalid observation that the manager rejects without publication. This strict distinction is the production path exercised by Plan 08's external fault service; there is no harness-only verdict or timing override. `Require` and `Evaluate` load the current pointer and ask the same evaluator about current clock time; they never probe, mutate, pin a generation, or read disk. `StartRefresh` owns exactly one 30-second loop bound to its context. Export strict deterministic `ReadinessSnapshotV1` after successful publication using a same-directory atomic write compatible with Phase 31 writer durability; `report_output_unwritable` is observable to operations/evidence while the in-memory snapshot remains authoritative and unchanged. Only after the production changes above are complete, add and run `TestReadinessManagerLifecycleContract` and `TestReadinessManagerPublicationRace` for D-RDY-01/D-RDY-05/D-RDY-06/D-RDY-09/D-RDY-19 as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Dependency failure publishes a new blocked generation; structural failure preserves the prior generation and original validity exactly.
    - A cancellation-ignoring probe cannot hold bootstrap/refresh beyond its deadline, and its test releases the fake goroutine.
    - `TestReadinessManagerPublicationRace` passes through the helper with `-race` and observes no partial/decreasing generation.
    - Seeded audit bytes, audit write failure, and excluded candidate assets have zero effect on `Require` verdicts or probe calls.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerLifecycleContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerPublicationRace$' -race &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.1-R02 and T-RDY-01/T-RDY-02/T-RDY-04/T-RDY-05/T-RDY-10 prove immutable continuous authorization, including strict HTTP structural-refresh retention, without file fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Compile catalog authority and emit evidence-only readiness reports</name>
  <read_first>
    - src/server/internal/releasecontract/readiness.go
    - src/server/internal/releasecontract/readiness_manager.go
    - src/server/internal/releasecontract/contract.go
    - src/server/internal/surfacereport/report.go
    - src/server/internal/surfacereport/registry.go
    - src/server/internal/surfacereport/writer.go
  </read_first>
  <files>src/server/internal/releasecontract/catalog.go, src/server/internal/releasecontract/catalog_test.go, src/server/internal/surfacereport/readiness.go, src/server/internal/surfacereport/readiness_test.go, src/server/internal/http/router.go</files>
  <behavior>
    - Test V31.1-R07: fixed model/builtin IDs and strict custom/MCP runtime classes resolve to one authored capability; missing, duplicate, deleted, or class-mismatched subjects return capability_unknown and make zero guard calls.
    - Test V31.1-R07/T-RDY-07: ResolveAndRequire always resolves current subject before reading the current manager generation; caller/persisted capability fields are ignored as authority.
    - Test V31.1-R07/T-RDY-07: `NewRuntimeAuthorities` accepts only the validated contract, resolver-confirmed profile and current Guard; it rejects missing/duplicate/profile-inapplicable catalog or typed effect mappings, including every live Agent `ToolExecutor` builtin/custom-HTTP/local-Python/sandbox/MCP seam, deep-copies lookup state, and produces the only `RouterOptions.Authorities` value with exact `CatalogAuthorizer` and `CapabilityBindings` fields.
    - Test V31.1-R10: readiness details accept exactly generation/checkedAt/validUntil and build release identity only through Phase 31 trusted identity/profile resolvers; construction returns a validated value without filesystem writes.
    - Test T-RDY-01/T-RDY-05: offline snapshot inspection can emit a failure/pass report but cannot seed or mutate any manager.
  </behavior>
  <action>For D-RDY-20 through D-RDY-22, compile the validated contract `catalogBindings` into immutable `(Kind, ID, Runtime)` lookups keyed by contract digest; reject duplicate/ambiguous keys and require exact runtime class for custom/MCP subjects. Define the closed typed `EffectID` set for worker, provider, MCP/tool/web-search, HTTP mutation, finance, and the production Agent `ToolExecutor` builtin/custom-API HTTP/local-Python process/Python-sandbox/MCP seams, then make `NewRuntimeAuthorities(contract,profile,guard)` compile every applicable `EffectID -> CapabilityID` exactly once from that same validated contract/profile. `CapabilityBindings` keeps a private deep-copied lookup and exposes only `Resolve(EffectID)`; unknown, missing, duplicate or profile-inapplicable mappings fail construction. `RuntimeAuthorities` contains that immutable value and the `CatalogAuthorizer` bound to the same current Guard. Extend `RouterOptions` with `Authorities RuntimeAuthorities`; zero/missing authorities fail production construction. No consumer may reload contract/profile, hard-code a capability ID, install a package global, or substitute a deny-all value after startup. `CatalogAuthorizer.ResolveAndRequire` resolves the supplied business subject then delegates to the shared Guard; it accepts no capability ID from request or persistence. For D-RDY-19, register `ReadinessSurfaceID = "readiness"` through Phase 31 `RegisterDetails` with exactly `generation`, `checkedAt`, and `validUntil`. `NewReadinessReport` receives `IdentityProvider`, `ProfileResolver`, the same explicit repoRoot/contract/schema/profile inputs, and either an online manager evaluation or an offline runtime-produced snapshot; it reuses the evaluator for schema/identity/freshness, copies only resolver-returned profile ID, constructs and validates nested `SurfaceReportV1`, then returns that value without accepting a writer/output path or touching the filesystem. Plan 31.1-03's CLI is the sole owner of Phase 31 `AtomicWriter.Write` for readiness output. Keep offline input behind the evidence adapter; no runtime constructor/guard accepts it. Only after the production changes above are complete, add and run automated verification with `TestCatalogAuthorityContract`, `TestRuntimeAuthoritiesContract`, and the report suite.</action>
  <acceptance_criteria>
    - Catalog tests prove unknown/ambiguous/deleted/runtime-class mismatch and stale persisted capability data fail before dispatch.
    - Runtime-authority tests prove one startup-compiled value carries the same Guard-backed catalog plus typed mappings through RouterOptions, cannot be mutated through source maps/copies, and rejects every absent/unknown mapping before consumer construction.
    - Request/persisted models expose no API that can supply the resolved capability to `ResolveAndRequire`.
    - Readiness registry rejects missing/unknown details, identity/profile splice, arbitrary details, non-empty committed skips, and E3/E4 claim classes.
    - Online and offline inspector reports use surface `readiness`; construction performs no write and neither path can alter `Manager.current` or authorize a capability.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestCatalogAuthorityContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestRuntimeAuthoritiesContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./internal/surfacereport '^TestReadinessSurfaceContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.1-R07/V31.1-R10 and T-RDY-01/T-RDY-05/T-RDY-07 establish one immutable startup authority carrier and evidence-only reporting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase 31 contract/identity -> readiness evaluator | Authored and derived trusted values become dynamic authorization inputs. |
| probes/clock -> immutable generation | Dependency observations and time determine current availability. |
| runtime snapshot -> report/audit filesystem | In-memory authority is serialized as evidence without becoming an input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-RDY-01 | Spoofing | identity/profile/audit input | high | mitigate | Trusted Phase 31 resolvers only, explicit profile, seeded-file zero-influence tests, and no runtime read API. |
| T-RDY-02 | Tampering | time/generation/freshness | high | mitigate | One nanosecond evaluator, authored durations only, atomic immutable publication, and old-expiry retention proof. |
| T-RDY-04 | Denial of Service | probe coordinator | high | mitigate | Per-probe deadlines, non-cooperative probe test, bounded candidate construction, and context-owned refresh. |
| T-RDY-07 | Elevation of Privilege | catalog capability selection | high | mitigate | Immutable server binding lookup, exact runtime class, current re-resolution, and zero-call unknown tests. |
</threat_model>

<verification>
- All focused Go suites use the Phase 31 declared helper contract: package pattern, anchored regex, optional Go args.
- Confirm Phase 31 actually delivered explicit `repoRoot`, trusted `IdentityProvider`, committed-profile resolver, typed registry, and atomic writer before implementation.
- Evidence remains E1 repository-local; no runtime/deployment/E3/E4 completion claim is allowed in this plan.
</verification>

<success_criteria>
- One manager/evaluator owns every readiness verdict and exact freshness comparison.
- Immutable generations and bounded probes pass normal plus race suites.
- Catalog binding and readiness report adapters cannot accept caller identity/capability authority.
- One immutable RuntimeAuthorities value carries catalog and typed effect bindings from startup into every later consumer.
- All plan-owned tests are non-vacuous and Phase 31 APIs are reused without a parallel implementation.
</success_criteria>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-01-SUMMARY.md` when done.
</output>
