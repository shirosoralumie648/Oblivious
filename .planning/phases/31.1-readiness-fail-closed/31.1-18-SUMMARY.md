---
phase: 31.1-readiness-fail-closed
plan: "18"
subsystem: release-contract
tags: [go-ast, readiness, effect-coverage, runtime-authority, fail-closed]

# Dependency graph
requires:
  - phase: 31.1-readiness-fail-closed
    provides: "RuntimeAuthorities, guarded effect producers, and the committed monolith profile consumed by the exact coverage join"
provides:
  - "One exact 39-row descriptor allowlist and descriptor-to-EffectID mapping"
  - "Strict 53-row effect-surface manifest with structural metadata and configuration selectors"
  - "Go AST registration and guard-before-effect discovery with no text-marker fallback"
  - "Mandatory contract, committed profile, RuntimeAuthorities, and capability-binding validation"
affects: [31.1-19, 31.1-20, release-contract, readiness-verification]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Exact descriptor specification map is the sole runtime registry authority"
    - "Static proof uses parsed registration and guard-before-effect call structure"
    - "Runtime coverage joins expected manifest, source structure, selected configuration, and authored authority"

key-files:
  created: []
  modified:
    - config/release/readiness-effect-surface.v1.json
    - src/server/internal/releasecontract/effect_registry.go
    - src/server/internal/releasecontract/effect_registry_test.go

key-decisions:
  - "The 39-entry runtimeDescriptorSpecs map is the only descriptor allowlist and EffectID mapping; prefixes, owner substrings, and EffectID(descriptor.ID) fallback are rejected."
  - "The monolith web-search projection contains agent.tool.builtin, mcp.websearch.builtin, and exactly one of tavily, chain, or provider."
  - "agent.tool.web_search, the three independent Registry rows, and python sandbox remain statically classified but excluded from monolith runtime registration."
  - "VerifyEffectCoverage cannot succeed without the authored contract, its selected committed profile, matching RuntimeAuthorities, and exact capability bindings."

patterns-established:
  - "Manifest rows that can project into runtime carry descriptorId, effectId, registrationSymbol, guardCall, effectCall, and any configurationSelector."
  - "Marker-preserving mutations must fail when registration, ownership, boundary, EffectID binding, guard, effect, or call order drifts."
  - "Source registration discovery enumerates direct, helper-bound, generated-struct, and generated-map descriptors before consulting the 39-row specification map."
  - "Guard/effect call paths use receiver-qualified function identities, so same-named methods cannot lend evidence across types."
  - "Known descriptors validate the exact source registration metadata and nearest same-object Resolve binding that feeds Register; constructor siblings and unused literals are never rescanned as substitute proof."
  - "Revision 3 requires an explicit EffectRuntimeConfiguration projection, exact expression paths plus AST object identity, provenance before Register/range consumption, and sorted first failures."
  - "Revision 4 binds each registration to an exact authority resolver and registrar path/object, follows only reachable helper edges, and uses the last reaching descriptor assignment or collection state before consumption."
  - "Revision 5 makes scalar and collection provenance scope-exact through ast.Object identity and fails closed on ambiguous conditional writes, resets, and appends before Register or range consumption."
  - "Revision 6 merges zero-iteration and loop-carried Range/For states, evaluates For.Post, and accepts generated range provenance only when the range cardinality is statically known."
  - "Revision 7 rejects loop descriptor provenance when branch or successful-return control can bypass a descriptor write or Register, while preserving exact optional-sandbox and failure-return producer paths."
  - "Revision 8 binds the optional-sandbox continue whitelist to a direct unlabeled continue in the current registration loop; labels, nested loops, and function literals cannot lend that proof."
  - "Revision 9 binds the sandbox comparison, readiness error, resolver result, and descriptor capability to exact same-row AST objects before permitting the direct continue."
  - "Revision 10 selects the last preceding authoritative Resolve for the checked error object before exact-matching that binding's capability and current-row effect objects."
  - "Revision 11 rejects ordinary, tuple, and conditionally ambiguous writes after the selected Resolve binding before sandbox condition or descriptor use."
  - "Revision 12 rejects mutation of the current sandbox Range row or its selector after authoritative Resolve and before descriptor construction."
  - "Revision 13 rejects address-taking escapes of the current sandbox Range row or selector after authoritative Resolve and before descriptor construction."
  - "Revision 14 starts sandbox row address-escape scanning at the current Range body or effect declaration, so pre-Resolve aliases cannot evade the barrier."
  - "Revision 15 rejects current-row-rooted method calls as implicit sandbox row escapes, including pointer receivers and pointer-returning value methods."
  - "Revision 16 rejects current-row-rooted method-value selectors used as aliases, while allowing only exact live effect.id Resolve/comparison reads and the effect.key capability-map index."
  - "Revision 17 extends descriptor escape protection through descriptor.End(), permits only exact effect.id/effect.owner descriptor fields, and unwraps only unshadowed predeclared string conversions."
  - "Revision 18 binds capability provenance only through direct identifiers or unshadowed predeclared string conversions; arbitrary one-argument wrappers cannot borrow authored capability fallback."
  - "Revision 19 aggregates package AST declarations before conversion proof, so a top-level string shadow in another file invalidates every package string conversion."
  - "Revision 20 requires checked denial control to dominate every effect path, rejects dead closures/branches and mutable ranged registration rows, and analyzes returned option closures only when exact package structure proves invocation."
  - "Revision 21 rejects goto as denial termination, treats exact range-row closure capture as escape, and expands only latest exact local FuncLit bindings at actual call sites."
  - "Revision 22 recursively expands exact invoked local FuncLit bindings with scope-first lookup, captured-outer fallback, path-local cycle guards, and direct-return guard proxying only for nested closure calls."
  - "Revision 23 resolves invoked FuncLit bindings through explicit nearest-outward lexical frames, freezes each frame at its exact dynamic call-site boundary, and drops non-lexical caller frames before recursive expansion."
  - "Revision 24 carries discarded dynamic caller frames only as reaching-write evidence and fails guard proof closed when they rebind the exact ast.Object captured by an outer-defined invoked FuncLit."
  - "Revision 25 unifies captured-object provenance across direct writes, Range assignment, address/object escape, and recursively invoked local FuncLits while pruning uninvoked helpers and writes after the call boundary."
  - "Revision 26 carries full lexical ancestry in discarded provenance, resolves stable Ident-to-Ident-to-FuncLit callable aliases, models defer/go completion timing, and rejects exact-object writer callbacks passed to opaque calls."
  - "Revision 27 includes registered deferred effects in dominance and denial reachability, and resolves exact local callback carriers through slices, structs, interfaces, and one-expression factories."
  - "Revision 28 preserves named ast.Fun identity through stable callable aliases and recursively resolves exact nested callback carriers with bounded object/factory traversal."

requirements-completed: []

coverage:
  - id: D1
    description: "The exact descriptor specification map and strict manifest cover every classified effect seam without wildcard, prefix, owner-name, or fallback-cast authority."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/releasecontract/effect_registry_test.go#TestEffectSurfaceManifestRuntimeDescriptorContract"
        status: pass
      - kind: integration
        ref: "cd src/server && go test ./internal/releasecontract -count=1"
        status: pass
    human_judgment: false
  - id: D2
    description: "Parsed Go registration and guard-before-effect structure rejects marker-only, removed, relocated, reordered, or mismatched producer evidence."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/releasecontract/effect_registry_test.go#TestStructuralEffectDiscoveryMutationContract"
        status: pass
      - kind: integration
        ref: "cd src/server && go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10"
        status: pass
    human_judgment: false
  - id: D3
    description: "Effect coverage requires the authored contract, committed selected profile, matching RuntimeAuthorities, and exact capability bindings before joining runtime descriptors."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/releasecontract/effect_registry_test.go#TestReadinessEffectCoverageContract"
        status: pass
      - kind: unit
        ref: "src/server/internal/releasecontract/effect_registry_test.go#TestEffectCoverageRequiresRuntimeAuthoritiesContract"
        status: pass
      - kind: integration
        ref: "cd src/server && go test ./... -count=1"
        status: pass
    human_judgment: false

# Metrics
duration: 2h 5m
completed: 2026-07-20
status: complete
---

# Phase 31.1 Plan 18: Structural Effect Contract Summary

**The release contract now proves an exact descriptor inventory, parsed guard-to-effect source structure, selected runtime configuration, and mandatory authored authority before any monolith effect coverage can pass.**

## Performance

- **Duration:** 2h 5m
- **Started:** 2026-07-19T14:49:35Z
- **Completed:** 2026-07-19T16:54:15Z
- **Tasks:** 3 completed
- **Files modified:** 3

## Accomplishments

- Replaced prefix wildcards, owner substring inference, raw marker discovery, capability inference, and `EffectID(descriptor.ID)` fallback with the sole exact `runtimeDescriptorSpecs` map.
- Expanded the strict manifest contract with `descriptorId`, `effectId`, `registrationSymbol`, `guardCall`, `effectCall`, and `configurationSelector`, while preserving strict JSON and EOF validation.
- Added Go parser/AST proof for exact registration identity and guard-before-effect order, including explicit local typed call paths.
- Made the contract, selected committed profile, matching `RuntimeAuthorities`, selected runtime configuration, and capability bindings mandatory for coverage.
- Kept independently reachable APIs classified without promoting them into the shipped monolith projection.
- Revision 1 enumerates source registrations before the specification join, so unknown registered descriptors become `effect_coverage_extra_static` while unused descriptor literals remain non-authoritative.
- Revision 1 keys methods by receiver and validates each exact descriptor literal/capability binding, preventing sibling descriptors and same-named receiver methods from lending evidence.
- Revision 2 carries the exact registered literal/helper-call provenance and resolved EffectID into known-ID validation, eliminating the remaining constructor rescan and generated-row heuristic paths.
- Revision 3 makes selected runtime configuration mandatory, enforces all-or-none optional cohorts and exactly one web-search provider, rejects statically false or shadowed receiver proofs, and sorts descriptor/selector failures deterministically.
- Revision 4 binds resolver/registrar identity to exact AST objects, excludes statically unreachable registration/helper paths, and tracks the latest reaching scalar or collection state before consumption.
- Revision 5 adds scope-exact scalar and collection reaching definitions: inner shadow bindings cannot lend evidence to an outer Register/range, and unknown-condition writes, resets, or appends are rejected unless every path has the same proven state.
- Revision 6 makes loop provenance conservative: unknown or empty Range bodies cannot create definite membership, `For.Post` participates in scalar and collection reaching state, and ambiguous loop-carried writes fail closed.
- Revision 7 makes loop-body control explicit: `break`, `continue`, `goto`, `fallthrough`, and successful returns before a descriptor write/consumption invalidate provenance instead of allowing later lexical statements to certify it.
- Revision 8 rejects labeled or nested-loop continues even when they reuse the optional-sandbox condition; only the exact direct unlabeled production continue is permitted.
- Revision 9 rejects shadowed effect/error names, unrelated errors, and errors from another Resolve result; the sandbox exception now requires one exact same-row authority binding.
- Revision 10 rejects stale earlier Resolve evidence when the same error, or the same capability/error pair, is overwritten before the sandbox condition.
- Revision 11 treats any later reachable write to the selected error or capability object as stale/ambiguous, including ordinary assignments and tuple writes that are not themselves Resolve bindings.
- Revision 12 rejects later writes that mutate the current Range row object or its selector, including direct, tuple, and conditional row writes after the selected Resolve binding.
- Revision 13 rejects taking the address of the current Range row or its selector between Resolve and descriptor construction, covering helper calls, local pointer aliases, and conditional helper escapes without relying on alias analysis.
- Revision 14 extends that escape barrier to the current loop/declaration start, rejecting row, selector, and conditional helper aliases created before the authoritative Resolve.
- Revision 15 treats any method call whose receiver/root is the current row or selector as an implicit escape across the same window, while preserving plain field reads and the production projection.
- Revision 16 treats current-row-rooted selector values as implicit escapes unless AST context proves the exact `effect.id` Resolve/comparison read or production `effect.key` capability-map index.
- Revision 20 replaces lexical guard-before-effect ordering with checked denial dominance, prunes uninvoked closures and statically dead branches, requires ranged descriptor rows to remain unmodified/unescaped through Register, and restores only exact invoked returned-option registrations.
- Revision 21 rejects every `goto` as fail-closed guard termination, invalidates a ranged row when any intervening FuncLit captures its exact AST object, and exposes guard/effect calls only from an exact latest identifier-to-FuncLit binding that is actually invoked.
- Revision 22 recursively expands exact invoked local FuncLit bindings from the current closure block before captured outer state, terminates self-reference/cycles with path-local `visited[*ast.FuncLit]`, and proxies nested guard proof only through prior FuncLit bindings whose final statement directly returns the invoked guard call.
- Revision 23 replaces the current-to-top shortcut with explicit lexical scope frames, resolves exact AST-object bindings nearest-outward using per-frame call-site cutoffs, and retains only the defining frame plus its lexical outers when expanding a closure.
- Revision 24 marks exact captured-object rebindings in discarded dynamic caller frames as ambiguous guard evidence, while preserving no-rewrite calls, different-object shadows, nearest-outward lexical lookup, and cycle termination.
- Revision 25 replaces the narrow discarded-frame rebinding check with one exact-object provenance proof that rejects direct/Range writes, object escape, and writes inside actually invoked local FuncLits before a captured helper call, while preserving `:=` shadows, post-call writes, uninvoked writers, and invoked read-only helpers.
- Revision 26 retains each discarded dynamic frame with its static lexical ancestry and per-frame cutoff, resolves stable exact-object callable aliases nearest-outward with a cycle guard, distinguishes current-frame defer from invoked-helper completion, treats goroutine writers as ambiguous, and inspects only writer/escape callbacks passed to opaque calls.
- Revision 27 records deferred effect registration in source-call dominance without executing same-frame unresolved deferred callees early, and resolves exact local callback carriers through slice indexes, struct selectors, interface assertions, local factory returns, and direct invocation of returned callables.
- Revision 28 carries named `ast.Fun`/source-target identity through Ident, Paren, TypeAssert, and IndexList aliases, projects exact named callbacks at opaque call sites, and recursively normalizes nested Index/Selector/Call/Composite/map carriers with lexical cutoffs, cycle guards, and a bounded depth.

## Inventory And Projection

| Class | Count | Meaning |
|---|---:|---|
| Manifest rows | 53 | 13 entrypoints, 39 descriptor rows, and the remaining non-descriptor classified row |
| Exact descriptor specifications | 39 | Byte-exact descriptor IDs shared by the map and manifest |
| Excluded descriptor specifications | 5 | Python sandbox, three independent Registry rows, and independent Agent web search |
| Optional configuration rows | 15 | Whole cohorts that project only when their real runtime owner is enabled |
| Web-search one-of rows | 3 | `mcp.websearch.tavily`, `mcp.websearch.chain`, or `mcp.websearch.provider` |
| Deterministic test projection | 17 | Committed non-optional rows plus the selected `provider` alternative |

The web-search projection always retains `agent.tool.builtin` and `mcp.websearch.builtin`, then requires exactly one configured provider alternative. `agent.tool.web_search`, `agent.tool.registry.*`, and `agent.tool.python_sandbox` register zero descriptors in the committed monolith projection.

## Negative Evidence Matrix

### Structural Matrix: 198 Cases + 5 Standalone Invariants

The verified structural matrix contains 198 scoped cases: 33 marker-preserving producer mutations across schedule registration/capability binding, channel retry/delivery, archive, Relay batch, Chat, Admin, marketplace settlement/payout, billing and marketplace checkout, MCP transport/builtin/Tavily/Chain/provider, ToolExecutor, Registry, generated ToolExecutor/Registry Resolve bindings, and independent WebsearchTool; 12 ranged-row immutability/escape mutations; 3 statically unreachable registration/helper mutations; 5 exact call-path/object cases; 48 checked guard-dominance, invoked-local-closure, and deferred-effect cases; 4 closure/static-branch reachability cases; 48 scalar/collection provenance and loop-control cases; 10 callback-carrier provenance cases; 14 named-function callable-identity cases; 14 nested callback-carrier cases; 5 descriptor literal escape/evaluator mutations; and 2 cross-file package-string-shadow conversion mutations.

The provenance cases include post-Register assignment, post-range append, outer-vs-inner scalar and collection shadow bindings, unknown-condition scalar write, unknown-condition collection reset/append, unknown Range append, unknown Range reset-plus-wrong-append, `For.Post` scalar write, `For.Post` collection append/reset, known Range `break`/`continue`/`goto`/successful-return bypass, scalar assignment/Register bypass, nested/labeled/inner-loop continue-target bypass, shadowed effect/error objects, unrelated/second/stale Resolve errors, same-object capability/error reassignment, ordinary error/capability writes, tuple reassignment, conditional ordinary writes, current-row selector/object/tuple/conditional writes, current-row/helper pointer escape, local pointer alias mutation, conditional selector/helper escape, pre-Resolve row/selector aliases, conditional pre-Resolve helper alias, pointer-receiver method escape, value-method pointer escape, conditional pointer-receiver escape, setter/pointer/conditional method-value aliases, last-reaching scalar/collection state, descriptor ID/Owner method or arbitrary-wrapper escapes plus arbitrary scalar wrappers, nested/direct capability wrapper fallback mutations, package-wide string-shadow conversion rejection, captured-helper Range assignment/address escape/invoked nested rebinding, full-ancestry outer writers, stable and ambiguous callable aliases, current/helper defer timing, deferred effect registration before/after guards and in denial helpers, named effect and non-effect aliases through immediate/deferred/opaque/Paren/TypeAssert/IndexList paths, goroutine and immediate-literal writers, opaque writer callbacks, slice/struct/interface/factory callback carriers, direct factory-result invocation, nested slice/selector/factory/composite/type-asserted carriers, exact map-key selection, and their shadow/post-call/uninvoked/read-only controls. The exact call-path set includes one live control and four marker-preserving negative receiver/reachability mutations; all cases passed in the fresh `-v` run.

Five standalone checks additionally prove:

1. Marker-only source with the old `builtin` string cannot certify a seam.
2. Production discovery returns every one of the 39 descriptor IDs exactly once.
3. A registered unknown descriptor is discovered and rejected as `effect_coverage_extra_static`, while an unused descriptor literal in the same function is ignored.
4. A guard moved after its effect cannot satisfy the ordered call contract.
5. A wrong descriptor that actually feeds `Register` cannot borrow an otherwise exact unused literal in the same constructor.

Each mutation retains the former token in a comment where applicable, so rejection is controlled by parsed registration, receiver, binding, and call structure rather than text presence.

### Authority-Negative Cases: 7

| Mutation | Stable rejection |
|---|---|
| Zero `RuntimeAuthorities` | `effect_coverage_authorities_required` |
| Empty authored contract | `effect_coverage_contract_required` |
| Missing selected profile | `effect_coverage_profile_invalid` |
| Selected non-committed profile | `effect_coverage_profile_invalid` |
| Authority/contract digest mismatch | `effect_coverage_authorities_mismatch` |
| Missing EffectID binding | `effect_coverage_unknown_effect` |
| Mismatched capability binding | `effect_coverage_capability_mismatch` |

### Runtime-Join Negative Cases: 10

- Zero runtime rows, missing row, extra row, duplicate row, excluded-row registration, owner drift, capability drift, missing provider selection, multiple provider selection, and a partial optional cohort all fail with stable `effect_coverage_*` codes.

## Task Commits

1. **Tasks 1-3: Exact descriptor contract, structural discovery, and authority-required join** - `c6e2f19` (`fix`)
2. **Revision 1: Source-first enumeration, exact receiver paths, and per-descriptor capability binding** - `87ca711` (`fix`)
3. **Revision 2: Exact registered-literal provenance and generated Resolve data flow** - `79006fe` (`fix`)
4. **Revision 3: Explicit selected cohorts, exact call/object proof, ordered provenance, and deterministic errors** - `3538769` (`fix`)
5. **Revision 4: Authority resolver identity, reachable registration paths, and reaching-definition provenance** - `0e24d68` (`fix`)
6. **Revision 5: Scope-exact scalar/collection provenance and ambiguous-condition rejection** - `9c7cd0c` (`fix`)
7. **Revision 6: Zero/multiple-iteration Range/For state and For.Post provenance** - `056e19f` (`fix`)
8. **Revision 7: Branch/return-aware loop provenance** - `a6f3707` (`fix`)
9. **Revision 8: Exact current-loop continue target binding** - `fc4e725` (`fix`)
10. **Revision 9: Same-row sandbox/Resolve AST identity binding** - `1508df9` (`fix`)
11. **Revision 9 follow-up: Second Resolve result regression** - `4de4717` (`test`)
12. **Revision 10: Last-reaching Resolve selection for sandbox errors** - `68876fd` (`fix`)
13. **Revision 11: Stale ordinary-write rejection after Resolve** - `1a0da99` (`fix`)
14. **Revision 12: Sandbox row-stability rejection after Resolve** - `a0e24fd` (`fix`)
15. **Revision 13: Sandbox row address-escape rejection after Resolve** - `87267c1` (`fix`)
16. **Revision 14: Pre-Resolve sandbox row alias rejection** - `4f51422` (`fix`)
17. **Revision 15: Implicit sandbox row escape rejection** - `f0f1c21` (`fix`)
18. **Revision 16: Sandbox row method-value rejection** - `be22376` (`fix`)
19. **Revision 17: Descriptor literal escape-window closure and strict string evaluation** - `c1b8794` (`fix`)
20. **Revision 18: Exact capability expression provenance and wrapper rejection** - `de7512b` (`fix`)
21. **Revision 19: Package-wide string shadow rejection** - `6036dcc` (`fix`)
22. **Revision 20: Guard dominance, reachable option registration, and immutable ranged rows** - `ec1c56b` (`fix`)
23. **Revision 21: Goto rejection and exact local-closure provenance** - `d0814a8` (`fix`)
24. **Revision 22: Recursive invoked-closure provenance** - `a493132` (`fix`)
25. **Revision 23: Nearest-outward lexical scope provenance** - `a181412` (`fix`)
26. **Revision 24: Captured closure rebinding rejection** - `1c3753b` (`fix`)
27. **Revision 25: Unified captured-object provenance** - `ade004a` (`fix`)
28. **Revision 26: Callable alias, completion timing, and opaque callback provenance** - `d6ffbac` (`fix`)
29. **Revision 27: Deferred effect and callback carrier provenance** - `3b7139c` (`fix`)
30. **Revision 28: Named callable and nested carrier identity** - `28352e3` (`fix`)

## TDD Evidence

- **RED descriptor contract:** `TestEffectSurfaceManifestRuntimeDescriptorContract` reported ten missing required descriptors and no `runtimeDescriptorSpecs` authority.
- **RED structural discovery:** `TestStructuralEffectDiscoveryMutationContract` showed marker-only `"builtin"` text incorrectly certifying `agent.tool.builtin`.
- **RED authority requirement:** `TestEffectCoverageRequiresRuntimeAuthoritiesContract` showed zero authorities returning `<nil>`.
- **Revision 1 RED:** the same structural selector returned `discovered=[]` for `unknown.source.effect`, incorrectly certified sibling capability substitutions for schedule, settlement, archive, and Relay batch, and accepted Chain/provider guard/effect evidence from same-named methods on the wrong receiver.
- **Revision 2 RED:** the structural selector certified a wrong registered schedule descriptor by borrowing an unused correct literal, and certified ToolExecutor/Registry builtin rows after their row-dependent Resolve calls were replaced with fixed MCP effects.
- **GREEN focused selectors:** all three plan selectors passed after the exact map, strict manifest, AST discovery, and mandatory authority join were implemented.
- **Revision 1 GREEN:** all 39 descriptors are source-enumerated exactly once; unknown registered seams, unused literals, sibling bindings, and receiver collisions have explicit passing regressions.
- **Revision 2 GREEN:** known rows compare the exact registration's descriptor, EffectID, capability, owner, boundary, package, and call path while retaining its source position as identity evidence; generated rows use the nearest preceding Resolve assignment for the same AST object.
- No separate RED commit was created; the exact observed failures and selector are recorded above.
- **Revision 5 RED limitation:** the interrupted recovery writer inherited the implementation already present in the worktree; the required focused selector was run immediately and passed, so the earlier RED output for the five new shadow/conditional cases was not recoverable and is intentionally not invented here.
- **Revision 6 RED:** the structural selector certified `range.append` from an unknown Range body, certified the loop-body `range.reset` wrong append, and ignored scalar/collection `For.Post` writes by retaining the pre-loop correct descriptor. After correcting the scalar fixture to assign a predeclared wrong descriptor in Post, all five cases failed by returning an incorrectly certified row before GREEN.
- **Revision 7 RED:** five valid Go fixtures were incorrectly certified before GREEN: known one-element Range bodies with `break`, `continue`, `goto`, or `return nil` before append, plus `continue` before scalar assignment and same-loop Register.
- **Revision 8 RED:** three valid fixtures were incorrectly certified by the recursive safeContinue whitelist: nested `continue outer`, labeled current-loop continue, and unlabeled continue in a nested inner loop.
- **Revision 9 RED:** three fixtures were incorrectly certified when the sandbox comparison used a shadowed Range value, the readiness check used a shadowed error, or it used an unrelated error. A fourth GREEN regression covers an error returned by a second authoritative Resolve while the descriptor retains the first Resolve capability.
- **Revision 10 RED:** the structural selector accepted an earlier matching Resolve after the same error object was overwritten by another effect, and accepted the earlier binding after the same capability/error objects were reassigned with `=`.
- **Revision 11 RED:** four fixtures were incorrectly certified after ordinary error/capability writes, tuple reassignment, and conditional error reassignment followed the selected Resolve.
- **Revision 12 RED:** four fixtures were incorrectly certified after reassigning the current sandbox Range selector, current `effect` row object, tuple row, or conditionally writing the row selector after the selected Resolve.
- **Revision 12 GREEN:** `a0e24fd` makes optional sandbox continuation fail closed when the current Range row or its selector is mutated before descriptor construction; the original structural GREEN passed all 74 matrix cases plus 5 standalone invariants in 1.757s.
- **Revision 13 RED:** three fixtures were incorrectly certified and each returned one discovered row before GREEN: `sandbox.row.helper.escape@sandbox.RowHelperEscape` after `mutateRow(&effect)`, `sandbox.row.alias.escape@sandbox.RowAliasEscape` after a local `pointer := &effect` alias mutation, and `sandbox.row.conditional.escape@sandbox.RowConditionalEscape` after a conditional helper received `&effect.id`.
- **Revision 13 GREEN:** `87267c1` makes optional sandbox continuation fail closed on any reachable address-taking expression rooted at the current Range row between the authoritative Resolve and descriptor construction. The structural selector passed all 77 matrix cases plus 5 standalone invariants while preserving exactly 39 positive production descriptors.
- **Revision 14 RED:** three fixtures were incorrectly certified and each returned one discovered row before GREEN: `sandbox.row.pre.resolve.alias@sandbox.RowPreResolveAlias` after `pointer := &effect` before Resolve and alias mutation after it, `sandbox.row.pre.resolve.selector@sandbox.RowPreResolveSelector` after taking `&effect.id` before Resolve, and `sandbox.row.conditional.pre.resolve@sandbox.RowConditionalPreResolve` after a conditional helper received `&effect.id` before Resolve.
- **Revision 14 GREEN:** `4f51422` starts exact-object address-escape scanning at the current Range body or effect declaration rather than the Resolve assignment. The structural selector passed all 80 matrix cases plus 5 standalone invariants while preserving exactly 39 positive production descriptors.
- **Revision 15 RED:** three fixtures were incorrectly certified and each returned one discovered row before GREEN: `sandbox.row.pointer.receiver@sandbox.RowPointerReceiver` after `effect.setSandbox()`, `sandbox.row.value.method@sandbox.RowValueMethod` after `pointer := effect.ptr()`, and `sandbox.row.conditional.receiver@sandbox.RowConditionalReceiver` after a conditional pointer-receiver call.
- **Revision 15 GREEN:** `f0f1c21` extends the exact-object escape barrier to method-call receivers rooted at the current row or selector. The structural selector passed all 83 matrix cases plus 5 standalone invariants while preserving exactly 39 positive production descriptors.
- **Revision 16 RED:** three fixtures were incorrectly certified and each returned one discovered row before GREEN: `sandbox.row.setter.value@sandbox.RowSetterValue` after `setter := effect.setSandbox`, `sandbox.row.pointer.value@sandbox.RowPointerValue` after `pointerFactory := effect.ptr`, and `sandbox.row.conditional.value@sandbox.RowConditionalValue` after a conditional method-value alias.
- **Revision 16 GREEN:** `be22376` adds a parent-aware selector-value barrier. The structural selector passed all 86 matrix cases plus 5 standalone invariants while preserving exactly 39 positive production descriptors and the live `effect.key` map-index read.
- **Revision 17 RED:** five marker-preserving fixtures were incorrectly certified and each returned one discovered row before GREEN: descriptor `ID` after `effect.mutateAndID(...)`, owner-first descriptor `Owner` after `effect.mutateAndOwner(...)`, descriptor `ID` through `fake(effect.id)`, descriptor `Owner` through `fake(effect.owner)`, and scalar `ID` through arbitrary `fake("scalar.wrapper")` evaluation.
- **Revision 17 GREEN:** `c1b8794` extends row write/escape protection through `descriptor.End()`, limits descriptor-safe reads to exact `effect.id`/`effect.owner` fields and the production `effect.key` index, and unwraps only unshadowed predeclared `string(...)`. The structural selector passed all 91 matrix cases plus 5 standalone invariants while preserving exactly 39 positive production descriptors.
- **Revision 17 verification:** the five focused selectors, `go test ./internal/releasecontract -count=1`, `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, `git diff --cached --check`, the five-test race suite with `-count=10` (170.345s), and `go test ./... -count=1` all exited 0.
- **Revision 18 RED:** three marker-preserving ToolExecutor fixtures were incorrectly certified before GREEN: nested `string(fake(capability))`, helper/pointer-shaped `string(pointer(capability))`, and direct `fake(capability)` that borrowed the authored capability after the runtime expression became opaque.
- **Revision 18 GREEN:** `de7512b` restricts `expressionReference` and `sourceExpressionObject` to direct identifiers or the exact unshadowed predeclared `string(...)` conversion, preventing arbitrary wrappers from matching Resolve bindings or triggering authored-capability fallback. The structural selector passed all 94 matrix cases plus 5 standalone invariants while preserving exactly 39 positive production descriptors.
- **Revision 18 verification:** the five focused selectors, `go test ./internal/releasecontract -count=1`, `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, `git diff --cached --check`, the five-test race suite with `-count=10` (173.823s), and `go test ./... -count=1` all exited 0.
- **Revision 19 RED:** two valid two-file fixtures were incorrectly certified before GREEN: a package-level `var string` declared in `shadow.go` did not invalidate `ID: string(descriptorID)` in `registration.go`, nor `CapabilityID: string(capability)` with an authored fallback.
- **Revision 19 GREEN:** `6036dcc` aggregates package files before conversion analysis, detects top-level `var`/`const`/`type`/`func string` declarations, and marks unresolved `string` identifiers as shadowed across the package. The structural selector passed all 96 matrix cases plus 5 standalone invariants while preserving exactly 39 positive production descriptors.
- **Revision 19 verification:** the five focused selectors, `go test ./internal/releasecontract -count=1`, `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, `git diff --cached --check`, the five-test race suite with `-count=10` (171.903s), and `go test ./... -count=1` all exited 0.

## Revision 20 Evidence

- **RED A - guard control:** the old lexical order accepted five unsafe shapes: runtime-conditional guard bypass, ignored guard result, unchecked guard result, denial fallthrough, and the effect executing inside the denial branch. Four checked return/continue/break and exact optional-readiness controls remained valid positives.
- **RED B - reachability:** an uninvoked local closure and an `if true` dead else exposed guard/register/effect calls, while a real uninvoked closure registration was discovered. A direct IIFE remained the required reachable positive.
- **RED C - ranged registration stability:** all nine mutations were incorrectly certified before GREEN: schedule ID, CapabilityID, Owner, and Boundary writes; schedule whole-row reassignment, address escape, and method escape; plus generated-row reassignment and method escape.
- **GREEN implementation:** `ec1c56b` (`fix(31.1-18): prove guard dominance and immutable registrations`) records call AST nodes, requires checked non-nil denial branches to terminate and dominate every effect/helper target in the same ordered block/case/communication clause, prunes statically dead branches and uninvoked closures, and rejects exact ranged-row writes or escapes before Register.
- **Returned option proof:** generic closure pruning remains fail closed. Registration analysis exposes only a top-level returned FuncLit whose result is directly invoked or flows in the same block through a stable exact collection into a variadic consumer that calls the exact ranged option object. This restored `http.marketplace.checkout`, `marketplace.settlement.intent`, and `marketplace.payout.dispatch` without admitting the dead-closure fixture.
- **Matrix and positive control:** the final committed implementation passed 127 nested verbose RUN records: 118 scoped matrix cases, 5 standalone invariants, and 4 organizational parent `t.Run` records. Production discovery remained exactly 39 descriptor rows.
- **Profile-bound optimization:** the first correct implementation rebuilt package parent maps/call walks per returned-option producer; one bounded cache pass reduced the single structural package time from 6.922s to 3.462s and the package suite from 11.691s to 6.217s while narrowing expansion to the production top-level `return func` shape.
- **Final focused/package/vet/manifest/diff:** the matched helper selected exactly five concrete tests and passed them in 5.470s; `go test ./internal/releasecontract -count=1` passed in 6.438s; `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` all exited 0.
- **Final race:** the five focused tests passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 243.582s package time and 268.90s wall.
- **Final full backend:** `cd src/server && go test ./... -count=1` passed on the first run in 34.97s wall. The run included the existing unstaged Plan 21 Chat changes; those files were not edited or staged by Revision 20.

## Revision 21 Evidence

- **RED A - goto termination:** both new denial-control cases were incorrectly accepted before GREEN. A `goto` reached a labeled effect and a safe-looking `goto` reached a denial-return label, but both produced `guard dominance proof=true want=false`; Revision 21 conservatively rejects all `goto` tokens as termination proof.
- **RED B - range-row closure capture:** three invoked closure mutations were incorrectly certified for `worker.schedule.claim`: closure-local descriptor ID write, `&descriptor` helper escape, and descriptor method escape. Each reported `mutable ranged registration certified worker.schedule.claim` before GREEN.
- **RED C - invoked local closure visibility:** an effect executed from an invoked denial closure was invisible and produced `true want=false`, while a guard directly returned from an invoked local closure was invisible and produced `false want=true`.
- **GREEN implementation:** `d0814a8` (`fix(31.1-18): reject goto and closure provenance bypasses`) removes `goto` from structural denial termination, visits a FuncLit node without generically descending its body so exact-object capture invalidates ranged-row stability, and expands calls only when the invoked identifier's latest unambiguous reaching value is that exact FuncLit.
- **Exact guard proxy:** calls inside an invoked local closure are visible at execution time, but a guard is proxied to the checked call site only when the closure body is exactly one direct `return guard()` statement. Uninvoked closures remain pruned, direct IIFEs remain reachable, and arbitrary closure wrappers cannot lend checked-result proof.
- **Matrix and positive control:** the final implementation passed 134 nested verbose RUN records: 125 scoped matrix cases, 5 standalone invariants, and 4 organizational parent `t.Run` records. All Revision 1-20 cases passed and production discovery remained exactly 39 descriptor rows.
- **Final focused/package/vet/manifest/diff:** the matched helper selected exactly five concrete tests and passed them in 6.272s; `go test ./internal/releasecontract -count=1` passed in 7.012s; `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` all exited 0.
- **Final race:** the five focused tests passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 237.146s package time and 248.50s wall.
- **Final full backend:** `cd src/server && go test ./... -count=1` passed on the first run in 33.33s wall. The run included the existing unstaged Plan 21 Chat changes; those files were not edited or staged by Revision 21.

## Revision 22 Evidence

- **RED:** three nested invoked-closure fixtures exposed missing recursive provenance: a denial effect hidden in a nested closure returned `true want=false`, a three-level denial effect returned `true want=false`, and a three-level returned guard closure returned `false want=true`. The self-referenced denial closure termination control passed, proving the cycle case was isolated.
- **GREEN implementation:** commit `a493132` (`fix(31.1-18): recursively prove invoked closure provenance`) recursively expands only exact invoked local FuncLit bindings. Binding lookup is scope-first in the current closure block, then falls back to captured outer function state; each expansion carries a path-local `visited[*ast.FuncLit]` set to terminate self-reference and cycles.
- **Nested guard proof:** a guard call in a nested closure is proxied to the invoking call site only when prior statements bind FuncLits and the final statement directly returns the invoked guard call. Uninvoked closures remain pruned, while direct IIFEs, returned-option consumers, conservative `goto` rejection, exact descriptor-capture rejection, and the production 39-descriptor positive control remain unchanged.
- **Matrix and positive control:** the final implementation passed 138 raw nested `=== RUN` records: 129 scoped matrix cases, 5 standalone invariants, and 4 organizational parent `t.Run` records. All Revision 1-21 cases remained green, and production discovery remained exactly 39 descriptor rows.
- **Structural/focused verification:** the structural GREEN passed in 4.540s; the matched five-selector run passed in 5.952s.
- **Package/quality verification:** `cd src/server && go test ./internal/releasecontract -count=1` passed in 8.663s; `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` all exited 0.
- **Race:** the five focused selectors passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 247.177s package time and 258.02s wall.
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first run in 42.14s wall. The run included the existing unstaged Plan 21 Chat changes; those files were not edited or staged by Revision 22.

## Revision 23 Evidence

- **RED:** the test-only structural run failed exactly three new cases before any registry edit: an enclosing denial helper returned `true want=false`, the nearest enclosing shadowed denial helper returned `true want=false`, and a cross-enclosing three-level returned guard returned `false want=true`. The new cross-scope self and mutual cycle controls passed, as did the prior self-cycle control and every Revision 1-22 case. The selector exited 1 in 3.696s package time and 10.61s wall.
- **GREEN implementation:** commit `a181412` (`fix(31.1-18): resolve invoked closures by lexical scope`) replaces the current-block-to-top-level shortcut with explicit `sourceLexicalScope` frames. Exact AST-object lookup walks nearest-outward, each retained frame is bounded before its dynamic invocation call site, ambiguous nearest-frame state fails closed, and recursive expansion drops dynamic caller frames that are not lexical parents.
- **Cycle and guard proof:** the top-level function body is the outermost frame; path-local FuncLit visiting still adds before recursion and deletes on return. Cross-scope self/mutual cycles terminate, uninvoked closures remain pruned, and direct-return proxying carries the cross-enclosing guard to the checked call site without name-based special cases.
- **Matrix and positive control:** the final implementation produced 144 total JSON RUN records including the root test, or 143 nested RUN records: 134 scoped matrix cases, 5 standalone invariants, and 4 organizational parent `t.Run` records. Production discovery remained exactly 39 descriptor rows.
- **Structural/focused verification:** the first lexical-frame structural GREEN passed in 5.683s package time and 19.71s wall; the matched five-selector run selected exactly five concrete tests and passed in 6.317s package time and 9.07s wall.
- **Package/quality verification:** `cd src/server && go test ./internal/releasecontract -count=1` passed in 8.008s package time and 9.13s wall; `go vet ./internal/releasecontract` passed in 1.36s wall; `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` all exited 0.
- **Race:** the five focused selectors passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 248.732s package time and 259.22s wall.
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first run in 33.32s wall. The run included the existing unstaged Plan 21 Chat changes; those files were not edited or staged by Revision 23.

## Revision 24 Evidence

- **RED:** before any registry edit, the captured helper rebound in its dynamic caller was incorrectly certified with `guard dominance proof=true want=false`. The no-rewrite positive, different-object shadow control, all cycle controls, and every Revision 1-23 case passed. The selector exited 1 in 4.432s package time and 10.68s wall.
- **GREEN implementation:** commit `1c3753b` (`fix(31.1-18): reject captured closure rebinding`) retains discarded dynamic caller frames only as reaching-write evidence. When a local FuncLit call target captures an exact `ast.Object`, any reachable write to that object before the discarded frame's invocation call site marks the source call ambiguous and makes guard dominance fail closed.
- **GREEN recovery:** the first broad ambiguity marker also promoted pre-existing lexical loop state ambiguity and regressed the valid checked `continue` and `break` controls to `false want=true` in 4.564s package time and 17.54s wall. Restricting the marker to `sourceObjectReboundBefore(discarded, object)` restored both controls while retaining the new rebinding rejection.
- **Narrow fail-closed boundary:** no-write capture remains provable, a same-named different-object shadow does not poison the captured helper, and ordinary lexical ambiguity remains non-expanding without becoming a global rejection. This preserves checked loop `continue`/`break`, nearest-outward lexical resolution, uninvoked pruning, direct IIFEs, returned options, conservative `goto`, range capture, cycle termination, and the 39-descriptor positive control.
- **Matrix and positive control:** the final implementation produced 147 total JSON RUN records including the root test, or 146 nested RUN records: 137 scoped matrix cases, 5 standalone invariants, and 4 organizational parent `t.Run` records. Production discovery remained exactly 39 descriptor rows.
- **Structural/focused verification:** the narrowed structural GREEN passed in 4.600s package time and 8.67s wall; the matched five-selector run selected exactly five concrete tests and passed in 9.050s package time and 11.74s wall.
- **Package/quality verification:** `cd src/server && go test ./internal/releasecontract -count=1` passed in 7.553s package time and 9.38s wall; `go vet ./internal/releasecontract` passed in 1.86s wall; `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` all exited 0.
- **Race:** the five focused selectors passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 253.169s package time and 264.91s wall.
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first run in 38.78s wall. The run included the existing unstaged Plan 21 Chat changes; those files were not edited or staged by Revision 24.

## Revision 25 Evidence

- **RED:** before any registry edit, the test-only structural run failed exactly three new ambiguity cases, each with `true want=false`: captured-helper Range assignment, captured-helper address escape, and an actually invoked nested helper rebinding the captured helper. The `:=` different-object Range shadow, post-call write, uninvoked writer, invoked read-only helper, existing safe outer binding, and all Revision 1-24 cases passed. The selector exited 1 in 4.530s package time and 12.47s wall.
- **GREEN implementation:** commit `ade004a` (`fix(31.1-18): prove captured object provenance`) extracts `astNodeWritesObject`, replaces the narrow rebinding predicate with `capturedObjectStableBeforeCall` and recursive `objectProvenanceStableBefore`, and rejects exact-object direct/Range writes, address/object escape, or writes in actually invoked local FuncLits before the captured call boundary. `invokedLiteralLexicalScopes` shares the exact lexical-frame construction; discarded dynamic frames remain provenance-only and never participate in lexical lookup.
- **Fail-closed boundary:** uninvoked FuncLits remain pruned, different-object `:=` shadows do not poison the captured helper, writes after `middle()` remain outside the proof window, invoked read-only helpers remain provable, and ambiguous/cyclic invoked-helper provenance fails closed. No name-based special case or producer change was added.
- **Matrix and positive control:** the final implementation produced 154 total JSON RUN records including the root test, or 153 nested RUN records: 144 scoped matrix cases, 5 standalone invariants, and 4 organizational parent `t.Run` records. Production discovery remained exactly 39 descriptor rows.
- **Structural/focused verification:** the first structural GREEN passed without a recovery edit in 6.246s package time and 23.42s wall; the matched five-selector run selected exactly five concrete tests and passed in 6.929s package time and 9.99s wall.
- **Package/quality verification:** `cd src/server && go test ./internal/releasecontract -count=1` passed in 8.863s package time and 10.52s wall; `go vet ./internal/releasecontract` passed in 1.88s wall; `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` all exited 0.
- **Race:** the five focused selectors passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 256.094s package time and 267.91s wall.
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first and only run in 40.64s wall; `internal/releasecontract` completed in 8.148s. The run included the existing unstaged Plan 21 Chat changes; those files were not edited or staged by Revision 25.

## Revision 26 Evidence

- **Base RED:** with only the first eight tests added and the registry unchanged, the structural selector failed exactly five new cases. Outer-lexical writer invocation, a stable callable alias, a conditional callable binding, and an alias rebound to an unproven local callable each returned `true want=false`; same-frame `defer rewrite(); middle()` returned `false want=true`. Invoked-helper defer, goroutine writer, immediate IIFE writer, direct writer, uninvoked/read-only controls, every Revision 1-25 case, and the 39 production descriptors passed. The selector exited 1 in 6.225s package time and 15.71s wall.
- **Base GREEN model:** discarded provenance now retains the dynamic frame plus its complete static lexical ancestry and call-site cutoffs. `resolveLocalFunctionLiteral` walks nearest-outward exact-object state through stable `Ident -> Ident -> FuncLit` aliases with an object-cycle guard; conditional/nonunique/in-scope unproven bindings fail closed, while direct parameter/receiver/global/import calls without local binding candidates remain ordinary. Parent-aware execution skips a current-frame defer before a later same-frame call, includes defers when an invoked helper completes, and treats goroutine writers as potentially immediate.
- **Base GREEN recovery:** the first implementation made production `agent.tool.local_python` missing by treating `defer cancel()` as immediate (2.657s package, 21.11s wall). Applying completion timing to source-call discovery restored it but exposed direct Router callback parameter `fn` (3.057s package, 10.98s wall). Narrowing unresolved-callee ambiguity to local binding candidates restored all 39 descriptors but initially propagated generic loop ambiguity to global `effect`, regressing checked `continue`/`break` (6.944s package, 18.92s wall). Requiring an in-scope binding candidate for that ambiguity produced the first complete base GREEN in 4.929s package time and 11.67s wall.
- **Callback RED:** after the base GREEN, the registry diff remained byte-identical with SHA-256 `e32d08d0f329b31c414fbf5f34910f6d92797f75845ab057c1f2e59176ace0c0` while four callback tests were added. The direct writer FuncLit and stable writer alias passed to opaque `invoke` each returned `true want=false`; the opaque read-only callback and unpassed/uninvoked writer passed. The selector exited 1 in 8.545s package time and 25.55s wall.
- **Callback GREEN implementation:** commit `d6ffbac` (`fix(31.1-18): prove callable aliases and deferred writes`) inspects only arguments of an opaque/unexpanded call, resolves direct FuncLits or stable exact local aliases, and recursively rejects exact-object writes, address-taking, selector/method escape, and nested proven writer paths. Pure reads and unpassed callbacks remain pruned. The structural selector passed in 7.128s package time and 24.15s wall with exactly 39 production descriptors.
- **Matrix and positive control:** the final implementation produced 166 total JSON RUN records including the root test, or 165 nested RUN records: 156 scoped matrix cases, 5 standalone invariants, and 4 organizational parent `t.Run` records. Production discovery remained exactly 39 descriptor rows.
- **Focused/package/quality verification:** the matched helper selected exactly five concrete tests and passed in 6.635s package time and 8.91s wall; `cd src/server && go test ./internal/releasecontract -count=1` passed in 9.255s package time and 10.65s wall; `go vet ./internal/releasecontract` passed in 1.43s wall. `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` all exited 0.
- **Race:** the five focused selectors passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 280.850s package time and 293.28s wall.
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first and only run in 73.81s wall; `internal/releasecontract` completed in 16.539s. The run included the existing unstaged Plan 21 Chat changes; those files were not edited or staged by Revision 26.

## Revision 27 Evidence

- **RED:** with the registry unchanged, its diff from the Revision 26 baseline was empty with SHA-256 `e3b0c44298fc1c149afbf4f5f34910f6d92797f75845ab057c1f2e59176ace0c0`. The structural selector failed exactly seven new cases: an effect deferred after the guard was not dominated, a denial-branch deferred effect was incorrectly accepted, and exact writer callbacks carried through a slice index, struct selector, interface type assertion, opaque-call factory result, or directly invoked factory result were missed. It exited 1 in 6.972s package time and 13.38s wall. Deferred registration before the guard, an invoked helper's deferred effect, every carrier read-only counterpart, all Revision 1-26 cases, and the 39-descriptor production invariant remained GREEN.
- **First structural GREEN:** source-effect discovery now includes registered deferred effects for dominance and denial reachability while unresolved same-frame deferred callees do not inherit object-state ambiguity. Callback resolution follows exact local `ast.Object` provenance through direct FuncLits, stable identifiers, slice indexes, struct selectors, interface assertions, exact one-expression local factory returns, and direct invocation of returned callables. Same-frame deferred rebinding remains excluded before a later call, and local ambiguous/unproven carriers fail closed. The first implementation passed without a recovery edit in 6.874s package time and 24.20s wall.
- **Matrix and positive control:** the final implementation produced 180 total JSON RUN records including the root test, or 179 nested RUN records: 170 scoped matrix cases, 5 standalone invariants, and 4 organizational parent `t.Run` records. The raw matrix completed in 5.25s wall, and production discovery remained exactly 39 descriptor rows.
- **Focused/package/quality verification:** the matched helper selected exactly five concrete tests and passed in 6.380s package time and 9.36s wall; `cd src/server && go test ./internal/releasecontract -count=1` passed in 6.839s package time and 7.96s wall; `go vet ./internal/releasecontract` passed in 1.12s wall. `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` all exited 0. A final pre-commit freshness run also passed the package in 6.813s package time and 9.33s wall and vet in 1.09s wall.
- **Race:** the five focused selectors passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 248.837s package time and 260.24s wall.
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first and only run in 44.84s wall; `internal/releasecontract` completed in 8.494s. The run included the existing unstaged Plan 21 Chat changes; those files were not edited or staged by Revision 27.
- **Implementation:** commit `3b7139c` (`fix(31.1-18): prove deferred effects and callback carriers`) changes only `effect_registry.go` and `effect_registry_test.go`; paused Plan 21 Chat files, `.planning/config.json`, and `.planning/intel/` remained outside the commit.

## Revision 28 Evidence

- **RED:** before any registry edit, `effect_registry.go` had content SHA-256 `8aedadda68a753345930e6ed09ab685db0d2bc6f85279530f67e91a7243477cc` and an empty diff with SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`. The structural selector failed exactly 15 of 28 new cases: named effect aliases reached denial through immediate, deferred, opaque direct/alias, reassigned, and parenthesized paths; named non-effect aliases through TypeAssert and IndexList were rejected; nested slice/selector/factory/composite/type-asserted writers were missed; and an exact map-key read-only callback was over-rejected. It exited 1 in 3.710s package time and 10.67s wall. Thirteen new controls, all Revision 1-27 cases, and the 39-descriptor production invariant remained GREEN.
- **GREEN model:** callable resolution now returns one typed identity: a local FuncLit with execution scopes, an exact named `ast.Fun` target, or ambiguity. Stable aliases preserve the named target for source-call matching, named callbacks passed to opaque calls are projected at the call site, and a local alias with a real unresolved/nonunique binding fails closed. Carrier resolution recursively normalizes Paren, TypeAssert, IndexList, Index, Selector, Call, Composite, and exact map/array/struct selection with exact lexical/call-site cutoffs, object/factory visiting guards, and a depth limit of 64.
- **GREEN recovery:** the first implementation exposed three compatibility boundaries before reaching the new assertions: ordinary receiver parameters without local binding candidates made Admin model mutation missing (5.782s package, 19.53s wall); opaque method receivers sourced from a map index made Agent builtin missing (6.517s package, 22.29s wall); and the direct Relay callback parameter `fn` was treated as an unresolved alias (5.008s package, 17.47s wall). Separating immediate source-call selectors from callback carriers and limiting ambiguity to actual local binding candidates restored all 39 rows. The next run passed every named identity and writer negative but rejected six exact read-only Index carriers (10.214s package, 21.96s wall); requiring the generic Index probe to return early only for an actual named `ast.Fun` produced the first complete structural GREEN in 13.827s package time and 33.62s wall.
- **Matrix and positive control:** the committed implementation produced 208 total JSON RUN records including the root test, or 207 nested RUN records: 198 scoped matrix cases, 5 standalone invariants, and 4 organizational parent `t.Run` records. The timed raw count completed in 11.28s wall, and production discovery remained exactly 39 descriptor rows.
- **Focused/package/quality verification:** the matched helper selected exactly five concrete tests and passed in 18.417s package time and 32.13s wall; `cd src/server && go test ./internal/releasecontract -count=1` passed in 17.139s package time and 19.41s wall; `go vet ./internal/releasecontract` passed in 11.39s wall. `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` all exited 0.
- **Race:** the five focused selectors passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 562.725s package time and 573.88s wall.
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first and only run in 72.58s wall; `internal/releasecontract` completed in 26.620s. The run included the existing unstaged Plan 21 Chat changes; those files were not edited or staged by Revision 28.
- **Implementation:** commit `28352e3` (`fix(31.1-18): preserve named callable and nested carrier identity`) changes only `effect_registry.go` and `effect_registry_test.go`; paused Plan 21 Chat files, `.planning/config.json`, and `.planning/intel/` remained outside the commit.

## Revision 3 Evidence

- **RED configuration contract:** the required focused selectors initially failed to compile because `EffectRuntimeConfiguration` and `EffectCoverageOptions.Selection` were absent.
- **RED path/provenance/determinism:** mutation cases cover exact live paths, `if false` guards, wrong and shadowed receivers, post-Register assignment, post-range append, and reversed invalid inputs.
- **GREEN implementation:** commit `3538769` (`fix(31.1-18): enforce selected cohorts and path-safe structural proof`).
- **Focused selectors:** all five tests passed, including `TestEffectCoverageFirstFailureIsDeterministic`.
- **Package and vet:** `cd src/server && go test ./internal/releasecontract -count=1` and `go vet ./internal/releasecontract` passed.
- **Race:** the five focused selectors passed under `go test -race ... -count=10` in 158.427s.
- **Full backend:** `cd src/server && go test ./... -count=1` passed.
- **Manifest and diff hygiene:** `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` passed.

## Revision 4 Evidence

- **RED authority/registration identity:** the structural selector initially certified `agent.tool.builtin` after replacing `options.Authorities.CapabilityBindings.Resolve(effect.id)` with `fake.Resolve(effect.id)`, certified `worker.schedule.claim` after replacing `effects.Register(descriptor)` with `other.Register(descriptor)`, and certified `http.billing.checkout` after moving the helper edge to `fake.newCheckoutFinancialReadiness(...)`.
- **RED reachability/provenance:** `if false` registration, range, and helper calls were counted; stale correct descriptor assignments and collection appends were reported alongside the later wrong reaching values.
- **GREEN implementation:** commit `0e24d68` (`fix(31.1-18): bind authority resolvers and reachable registration paths`) records exact resolver/registrar paths and AST root objects, excludes statically unreachable branches, requires exact helper callee edges, and tracks the latest descriptor definition/state before Register or range consumption.
- **Focused selectors:** manifest, structural mutation (39 positive descriptors plus all Revision 4 mutations), coverage/authority, and deterministic first-failure suites passed.
- **Package and vet:** `cd src/server && go test ./internal/releasecontract -count=1` and `go vet ./internal/releasecontract` passed.
- **Race:** the five focused selectors passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 167.918s.
- **Full backend:** `cd src/server && go test ./... -count=1` passed. The run included the existing unstaged Plan 21 Chat changes; those files were not edited or staged by Plan 18.
- **Manifest and diff hygiene:** `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` passed.

## Revision 5 Evidence

- **Implementation:** commit `9c7cd0c` (`fix(31.1-18): make descriptor provenance scope exact`) changes only `effect_registry.go` and `effect_registry_test.go`; paused Plan 21 Chat files, `.planning/config.json`, and `.planning/intel/` remained outside the commit.
- **Initial recovery selector:** after `gofmt` on only the two releasecontract files, `bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestStructuralEffectDiscoveryMutationContract$'` passed (1 matched test, 1.928s). No prior RED output survived the interrupted writer.
- **Focused selectors:** all five concrete tests passed through `bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$'`.
- **Package/vet/manifest/diff:** `cd src/server && go test ./internal/releasecontract -count=1`, `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` all passed before the implementation commit.
- **Race:** the five focused tests passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 167.695s.
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first run; no startup-order flake or rerun was needed.
- **Mutation count:** `go test ./internal/releasecontract -run '^TestStructuralEffectDiscoveryMutationContract$' -count=1 -v` enumerated the 47 matrix cases and 5 standalone invariants recorded above; all passed.

## Revision 6 Evidence

- **Implementation:** commit `056e19f` (`fix(31.1-18): merge loop descriptor provenance safely`) changes only `effect_registry.go` and `effect_registry_test.go`; paused Plan 21 Chat files, `.planning/config.json`, and `.planning/intel/` remained outside the commit.
- **GREEN structural selector:** `bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestStructuralEffectDiscoveryMutationContract$'` passed with the production 39-descriptor invariant and all Revision 1-6 cases (1 matched test, 1.862s).
- **Focused/package/vet/manifest/diff:** all five concrete focused tests passed; `cd src/server && go test ./internal/releasecontract -count=1`, `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` passed before the implementation commit.
- **Race:** the five focused tests passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 169.089s.
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first run; no startup-order flake or rerun was needed.
- **Mutation count:** the fresh verbose structural run passed and enumerated 52 scoped matrix cases plus the 5 standalone invariants recorded above.

## Revision 7 Evidence

- **Implementation:** commit `a6f3707` (`fix(31.1-18): fail closed on loop branch provenance`) changes only `effect_registry.go` and `effect_registry_test.go`; paused Plan 21 Chat files, `.planning/config.json`, and `.planning/intel/` remained outside the commit.
- **Control model:** loop-local branch/return nodes before the target descriptor/Register mark provenance ambiguous. The existing Agent generated-descriptor loop remains provable only through its exact sandbox capability-unknown `continue`; `return ..., err` is accepted as failure-only only when the same AST error object is dominated by `err != nil`.
- **GREEN recovery:** the first implementation run exposed a typed-nil `*ast.BlockStmt` panic in loop lookup; adding the explicit nil guard produced a clean structural-selector pass with the production 39-descriptor invariant and all Revision 1-7 cases (1 matched test, 1.895s). The final tightened condition/object implementation passed again.
- **Focused/package/vet/manifest/diff:** all five concrete focused tests passed; `cd src/server && go test ./internal/releasecontract -count=1`, `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` passed before the implementation commit.
- **Race:** the five focused tests passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 168.940s.
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first run; no startup-order flake or rerun was needed.
- **Mutation count:** the fresh verbose structural run passed and enumerated 57 scoped matrix cases plus the 5 standalone invariants recorded above.

## Revision 8 Evidence

- **Implementation:** commit `fc4e725` (`fix(31.1-18): bind loop continue targets exactly`) changes only `effect_registry.go` and `effect_registry_test.go`; paused Plan 21 Chat files, `.planning/config.json`, and `.planning/intel/` remained outside the commit.
- **Control model:** `safeContinue` now requires a one-statement optional-sandbox condition body containing an unlabeled `continue`, and the AST parent chain must reach the current loop body without crossing another `Range`, `For`, or function literal. Labeled and nested continue targets remain unsafe.
- **Focused/package/vet/manifest/diff:** all five concrete focused tests passed; `cd src/server && go test ./internal/releasecontract -count=1`, `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` passed before the implementation commit.
- **Race:** the five focused tests passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 170.286s.
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first run; no startup-order flake or rerun was needed.
- **Mutation count:** the fresh verbose structural run passed and enumerated 60 scoped matrix cases plus the 5 standalone invariants recorded above.

## Revision 9 Evidence

- **Implementation:** commit `1508df9` (`fix(31.1-18): bind sandbox continue identities exactly`) changes only `effect_registry.go` and the three RED fixtures in `effect_registry_test.go`. Follow-up commit `4de4717` adds the second-Resolve-result regression only. Paused Plan 21 Chat files, `.planning/config.json`, and `.planning/intel/` remained outside both commits.
- **Identity model:** the sandbox comparison selector base must be the current Range value object. The readiness error and descriptor capability must be the error/capability LHS objects from the same preceding authoritative `CapabilityBindings.Resolve(effect.id)` assignment; the Resolve effect selector must use that same Range object.
- **Focused/package/vet/manifest/diff:** all five concrete focused tests passed; `cd src/server && go test ./internal/releasecontract -count=1`, `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` passed on the final code/test tree.
- **Race:** after the test-only follow-up, the five focused tests passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 166.652s.
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the final tree on the first run; no startup-order flake or rerun was needed.
- **Mutation count:** the fresh verbose structural run passed and enumerated 64 scoped matrix cases plus the 5 standalone invariants recorded above.

## Revision 10 Evidence

- **Implementation:** commit `68876fd` (`fix(31.1-18): bind sandbox errors to reaching resolves`) changes only `effect_registry.go` and `effect_registry_test.go`; paused Plan 21 Chat files, `.planning/config.json`, and `.planning/intel/` remained outside the commit.
- **Reaching-binding model:** for the exact checked error object, only the source-position-latest authoritative Resolve before `IsReadinessCode` is eligible. That single binding must then exact-match the descriptor capability object, current Range effect object, and `effect.id` reference.
- **Focused/package/vet/manifest/diff:** all five concrete focused tests passed; `cd src/server && go test ./internal/releasecontract -count=1`, `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` passed on the final code/test tree.
- **Race:** the five focused tests passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 165.413s.
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first run; no startup-order flake or rerun was needed.
- **Mutation count:** the fresh verbose structural run passed and enumerated 66 scoped matrix cases plus the 5 standalone invariants recorded above.

## Revision 11 Evidence

- **Implementation:** commit `1a0da99` (`fix(31.1-18): reject stale sandbox binding writes`) changes only `effect_registry.go` and `effect_registry_test.go`; paused Plan 21 Chat files, `.planning/config.json`, and `.planning/intel/` remained outside the commit.
- **Write-barrier model:** after selecting the latest Resolve for the checked error object, discovery rejects any reachable write to that error before `IsReadinessCode` or to that capability before descriptor construction. Tuple, conditional, and loop writes are covered by exact AST object identity.
- **Focused/package/vet/manifest/diff:** all five concrete focused tests passed; `cd src/server && go test ./internal/releasecontract -count=1`, `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` passed on the final code/test tree.
- **Race:** the five focused tests passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 164.620s.
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first run; no startup-order flake or rerun was needed.
- **Mutation count:** the fresh verbose structural run passed and enumerated 70 scoped matrix cases plus the 5 standalone invariants recorded above.

## Revision 12 Evidence

- **Implementation:** commit `a0e24fd` (`fix(31.1-18): reject mutated sandbox rows`) changes only `effect_registry.go` and `effect_registry_test.go`; paused Plan 21 Chat files, `.planning/config.json`, and `.planning/intel/` remained outside the commit.
- **Row-stability model:** after selecting the authoritative Resolve, discovery requires the current Range row object and its `effect.id` selector to remain unchanged through the sandbox condition and descriptor construction. Direct row-selector reassignment, current-row object reassignment, tuple row reassignment, and conditional selector writes all fail closed by exact AST object identity.
- **RED:** all four new fixtures incorrectly produced discovered `EffectSurface` rows before the row-stability check was implemented.
- **Focused/package/vet/manifest/diff:** the matched-test helper selected exactly five concrete tests and passed them in 3.672s (7.15s wall); `cd src/server && go test ./internal/releasecontract -count=1` passed in 4.514s (6.69s wall), and `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` passed on the final code/test tree.
- **Race:** the five focused tests passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 165.568s (167.99s wall) on the fresh closeout rerun.
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first run in 20.81s wall; no startup-order flake or rerun was needed.
- **Mutation count:** the fresh verbose structural run passed in 1.781s (2.96s wall) and emitted 79 nested `=== RUN` records, matching the 74 scoped matrix cases plus the 5 standalone invariants recorded above.

## Revision 13 Evidence

- **Implementation:** commit `87267c1` (`fix(31.1-18): reject sandbox row address escapes`) changes only `effect_registry.go` and `effect_registry_test.go`; paused Plan 21 Chat files, `.planning/config.json`, and `.planning/intel/` remained outside the commit.
- **Escape-barrier model:** after selecting the authoritative Resolve, discovery rejects any reachable unary `&` rooted at the current Range row object before descriptor construction. This covers `&effect`, `&effect.id`, helper arguments, local pointer aliases, and conditional helper calls conservatively without interprocedural alias analysis.
- **RED:** `go test ./internal/releasecontract -run '^TestStructuralEffectDiscoveryMutationContract$' -count=1 -v` failed exactly the three new address-escape subtests; every prior subtest passed.
- **Focused/package/vet/manifest/diff:** the matched-test helper selected exactly five concrete tests and passed them in 3.761s (6.88s wall); `cd src/server && go test ./internal/releasecontract -count=1` passed in 4.428s (6.35s wall), and `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` passed before the implementation commit.
- **Race:** the five focused tests passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 165.637s (175.26s wall).
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first run in 30.28s wall; no startup-order flake or rerun was needed.
- **Mutation count:** the fresh verbose structural run passed and emitted 82 nested `=== RUN` records, matching the 77 scoped matrix cases plus the 5 standalone invariants recorded above; the production positive control remained exactly 39 descriptor rows.

## Revision 14 Evidence

- **Implementation:** commit `4f51422` (`fix(31.1-18): reject pre-resolve sandbox aliases`) changes only `effect_registry.go` and `effect_registry_test.go`; paused Plan 21 Chat files, `.planning/config.json`, and `.planning/intel/` remained outside the commit.
- **Escape-window model:** the optional sandbox exception now carries the current `RangeStmt` into the capability check and begins exact-object address scanning at the earlier of the loop-body entry or effect declaration. Any current-row or selector address escape before or after Resolve and before descriptor use fails closed.
- **RED:** `go test ./internal/releasecontract -run '^TestStructuralEffectDiscoveryMutationContract$' -count=1 -v` failed exactly the three new pre-Resolve alias subtests; every prior subtest passed.
- **Focused/package/vet/manifest/diff:** the matched-test helper selected exactly five concrete tests and passed them in 3.815s (7.15s wall); `cd src/server && go test ./internal/releasecontract -count=1` passed in 4.483s (6.58s wall), and `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` passed before the implementation commit.
- **Race:** the five focused tests passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 163.876s (173.74s wall).
- **Full backend:** `cd src/server && go test ./... -count=1` passed on the first run in 31.41s wall; no startup-order flake or rerun was needed.
- **Mutation count:** the fresh verbose structural run passed and emitted 85 nested `=== RUN` records, matching the 80 scoped matrix cases plus the 5 standalone invariants recorded above; the production positive control remained exactly 39 descriptor rows.

## Revision 15 Evidence

- **Implementation:** commit `f0f1c21` (`fix(31.1-18): reject implicit sandbox row escapes`) changes only `effect_registry.go` and `effect_registry_test.go`; paused Plan 21 Chat files, `.planning/config.json`, and `.planning/intel/` remained outside the commit.
- **Implicit-escape model:** the conservative escape scan now rejects `CallExpr` method selectors whose receiver/root resolves to the current Range row object, including pointer receivers, value methods returning pointers, and conditional calls. Plain field reads remain accepted.
- **RED:** `go test ./internal/releasecontract -run '^TestStructuralEffectDiscoveryMutationContract$' -count=1 -v` failed exactly the three new implicit-method subtests; every prior subtest passed.
- **Focused/package/vet/manifest/diff:** the matched-test helper selected exactly five concrete tests and passed them in 3.712s (6.62s wall); `cd src/server && go test ./internal/releasecontract -count=1` passed in 4.515s (6.13s wall), and `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` passed before the implementation commit.
- **Race:** the five focused tests passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 165.782s (175.32s wall).
- **Full backend:** the first `cd src/server && go test ./... -count=1` attempt encountered the unrelated existing `cmd/server` startup-order scheduling flake (`listen, refresh, background, serve` vs `listen, serve, refresh, background`); the targeted startup-order subtest passed 20/20 unchanged, and the immediate full-suite rerun passed in 21.76s wall. No server, Chat, config, or tracking file was changed.
- **Mutation count:** the fresh verbose structural run passed and emitted 88 nested `=== RUN` records, matching the 83 scoped matrix cases plus the 5 standalone invariants recorded above; the production positive control remained exactly 39 descriptor rows.

## Revision 16 Evidence

- **Implementation:** commit `be22376` (`fix(31.1-18): reject sandbox row method values`) changes only `effect_registry.go` and `effect_registry_test.go`; paused Plan 21 Chat files, `.planning/config.json`, and `.planning/intel/` remained outside the commit.
- **Selector-value model:** the escape scan builds one AST parent map and allows only `effect.id` as the exact authoritative Resolve argument or sandbox comparison operand, plus `effect.key` as the production capability-map index. Other current-row selectors used as values, including method aliases, fail closed.
- **RED:** `go test ./internal/releasecontract -run '^TestStructuralEffectDiscoveryMutationContract$' -count=1 -v` failed exactly the three new method-value subtests; every prior subtest passed.
- **Focused/package/vet/manifest/diff:** the matched-test helper selected exactly five concrete tests and passed them in 3.936s (6.89s wall); `cd src/server && go test ./internal/releasecontract -count=1` passed in 4.590s (6.36s wall), and `go vet ./internal/releasecontract`, `jq empty config/release/readiness-effect-surface.v1.json`, `git diff --check`, and `git diff --cached --check` passed before the implementation commit.
- **Race:** the five focused tests passed under `go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract|TestEffectCoverageFirstFailureIsDeterministic)$' -count=10` in 167.708s (177.61s wall).
- **Full backend:** the first `cd src/server && go test ./... -count=1` attempt reproduced the unrelated existing `cmd/server` startup-order scheduling flake (`listen, refresh, background, serve` vs `listen, serve, refresh, background`); the immediate full-suite rerun passed in 20.93s wall, and the targeted startup-order subtest passed 20/20 unchanged. No server, Chat, config, or tracking file was changed.
- **Mutation count:** the fresh verbose structural run passed and emitted 91 nested `=== RUN` records, matching the 86 scoped matrix cases plus the 5 standalone invariants recorded above; the production positive control remained exactly 39 descriptor rows.

## Historical Verification

Historical Revision 2 verification on the implementation tree committed as `79006fe`:

- `bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestEffectSurfaceManifestRuntimeDescriptorContract$'` - pass
- `bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestStructuralEffectDiscoveryMutationContract$'` - pass
- `bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessEffectCoverageContract$|^TestEffectCoverageRequiresRuntimeAuthoritiesContract$'` - pass
- `cd src/server && go test ./internal/releasecontract -count=1` - pass
- `cd src/server && go vet ./internal/releasecontract` - pass
- `cd src/server && go test -race ./internal/releasecontract -run '^(TestEffectSurfaceManifestRuntimeDescriptorContract|TestStructuralEffectDiscoveryMutationContract|TestReadinessEffectCoverageContract|TestEffectCoverageRequiresRuntimeAuthoritiesContract)$' -count=10` - pass in 157.018s
- `cd src/server && go test ./... -count=1` - pass
- `jq empty config/release/readiness-effect-surface.v1.json` - pass
- `git diff --check` and `git diff --cached --check` - pass

## Files Modified

- `config/release/readiness-effect-surface.v1.json` - Independent strict expected inventory, structural call metadata, and optional/one-of projection selectors.
- `src/server/internal/releasecontract/effect_registry.go` - Exact descriptor specification authority, strict manifest validation, Go AST discovery, selected projection validation, and mandatory authority join.
- `src/server/internal/releasecontract/effect_registry_test.go` - Descriptor/manifest exactness, structural mutation, runtime projection, and authority-negative matrices.

## Decisions Made

- Keep configuration selection declarative and exact: optional cohorts are all-or-none, while web-search provider rows are exactly-one.
- Validate descriptor owner, boundary, EffectID, capability, disposition, and structural metadata before accepting registration or coverage.
- Follow only explicit local call paths declared in manifest metadata; do not infer call relationships from comments, strings, owner names, or arbitrary call-graph guesses.
- Enumerate registrations from source data flow before consulting the specification map; known rows then require exact structure, while unknown rows remain visible to the extra-static gate.
- Qualify method paths by concrete receiver and accept unqualified names only for free functions.
- Preserve descriptor EffectID and helper-call provenance on each enumerated registration, and resolve generated capabilities by lexical AST object plus nearest preceding assignment.
- Preserve the Plan 19 boundary: no producer or real `BuildRuntime` composition file was changed to satisfy this contract.

## Deviations from Plan

**Revision 1 review deviation:** post-completion review found that function-name-only indexing allowed receiver collisions, constructor-wide validation allowed sibling capability evidence borrowing, and specification-driven iteration hid unknown source registrations. The fix remained inside the three Plan 18-owned files, added source-first enumeration and exact receiver/binding proof, and did not cross the Plan 19 producer/composition boundary.

**Revision 2 review deviation:** follow-up review found that known-ID validation discarded the exact enumerated registration and rescanned arbitrary constructor literals, while generated rows still used a generic Resolve heuristic. Revision 2 removed both rescan paths and bound validation to the registration and lexical Resolve data flow without changing producer code.

**Revision 3 review deviation:** final review found that omitted optional cohorts could pass without an explicit disablement, call-name proof could borrow dead or wrong-object calls, descriptor provenance could occur after consumption, and map/input order could change the reported first failure. Revision 3 closed all four gaps within the Plan 18 contract, manifest, and tests without changing producer behavior.

**Revision 4 review deviation:** follow-up review found that resolver/registrar receiver identity and helper reachability were still underconstrained, and that stale earlier scalar/collection definitions could be reported after a later write. Revision 4 bound those paths to AST objects and used the last reaching state before consumption.

**Revision 5 review deviation:** final provenance review found name-based scalar and collection lookup could cross lexical scopes, while unknown-condition writes could be accepted from a single path. Revision 5 moved reaching definitions to exact `ast.Object` identity and merges conditional path states fail closed.

**Revision 6 review deviation:** loop review found Range membership assumed one iteration and `For` ignored its Post statement, allowing zero-iteration and loop-carried state to certify descriptors. Revision 6 merges zero/multiple-iteration paths conservatively and preserves generated production rows only for statically known Range cardinality.

**Revision 7 review deviation:** branch-control review found the loop transfer still processed statements after `break`, `continue`, `goto`, `fallthrough`, and successful return as though they were reached. Revision 7 adds explicit pre-target control ambiguity without introducing a full CFG.

**Revision 8 review deviation:** follow-up review found that the optional-sandbox continue exception recursively whitelisted labels and nested loop/function bodies. Revision 8 binds that exception to the exact current-loop unlabeled AST branch.

**Revision 9 review deviation:** identity review found the narrow continue exception still compared source names rather than the current Range/Resolve objects. Revision 9 carries and exact-matches the effect, error, and capability objects from the same authority assignment.

**Revision 10 review deviation:** reaching-definition review found that any earlier same-object binding could satisfy the Revision 9 identity join after a later Resolve overwrite. Revision 10 makes source position and last-reaching error provenance authoritative.

**Revision 11 review deviation:** follow-up reaching review found that non-Resolve assignments after the selected binding were invisible to the Resolve-only records. Revision 11 adds exact-object write barriers through the condition and descriptor-use positions.

**Revision 12 review deviation:** final sandbox-row review found that the selected Resolve objects could remain stable while the current Range row or its selector changed before descriptor construction. Revision 12 extends the same exact-object write barrier to the row and selector without changing producer code.

**Revision 13 review deviation:** address-escape review found that the current row could still be mutated through `&effect` or `&effect.id` while direct AST writes remained absent. Revision 13 adds a conservative address-taking barrier from Resolve through descriptor construction without changing producer code or adding alias analysis.

**Revision 14 review deviation:** pre-binding alias review found that Revision 13 began address scanning at Resolve and therefore missed aliases established earlier in the current loop/declaration scope. Revision 14 widens only the scan start while retaining the same exact-object conservative barrier.

**Revision 15 review deviation:** implicit-address review found that method dispatch could mutate or expose the current row without a unary `&` in the registration function. Revision 15 rejects current-row-rooted method calls conservatively without type inference or producer changes.

**Revision 16 review deviation:** method-value review found that a selector alias such as `setter := effect.setSandbox` can carry implicit addressability after the selector leaves the direct CallExpr. Revision 16 adds parent-aware exact safe-read proof and rejects every other current-row selector value without changing producers.

**Revision 18 review deviation:** capability provenance review found that arbitrary one-argument calls were recursively unwrapped by `expressionReference` and `sourceExpressionObject`, allowing opaque runtime wrappers to match a real Resolve binding and fall back to authored capability. Revision 18 limits both helpers to direct identifiers and unshadowed predeclared `string(...)` conversions without changing producers.

**Revision 19 review deviation:** package-scope review found that per-file parser objects left a cross-file top-level `string` declaration invisible to `isSafeSourceStringConversion`. Revision 19 aggregates package ASTs and fails closed on package-wide `string` shadows without introducing type analysis or producer changes.

**Revision 20 review deviation:** control-flow review found that lexical call order did not prove denial dominance, generic closure traversal exposed dead evidence, and Range registration accepted rows mutated or escaped before Register. Revision 20 adds checked structural dominance, fail-closed reachability, exact row stability, and a narrowly proven returned-option construction path without changing producer files or the manifest.

**Revision 21 review deviation:** follow-up control-flow review found that `goto` was treated as unconditional denial termination, exact range rows could mutate through captured closure state, and invoked local closure bodies were absent from guard/effect proof. Revision 21 closes those three paths with conservative token rejection, exact-object capture escape, and call-site-bound local FuncLit expansion without changing producer files or the manifest.

**Revision 25 review deviation:** captured-object review found that Revision 24 observed only direct identifier writes in discarded dynamic frames, so Range `=` assignment, address escape, and writes inside an actually invoked nested helper could still change the exact closure binding before use. Revision 25 unifies exact-object write, escape, and invoked-helper provenance while preserving the lexical/dynamic frame boundary and without changing producer files or the manifest.

**Revision 26 review deviation:** callable-provenance review found that discarded dynamic frames no longer carried enough static ancestry to resolve outer writers, direct-only FuncLit lookup could not follow stable aliases, defer/go execution timing was flattened, and opaque callbacks could carry exact-object writers past the provenance walk. Revision 26 closes those paths with per-frame lexical cutoffs, exact-object alias resolution, parent-aware completion timing, and argument-scoped callback inspection without changing producer files or the manifest.

**Revision 27 review deviation:** deferred-effect and carrier review found that source discovery discarded registered defers before dominance analysis and callback inspection stopped at direct FuncLits or stable identifiers. Revision 27 records defer registration at its lexical site, preserves same-frame deferred rebinding timing, and resolves exact local slice, struct, interface, and factory carriers with lexical cutoffs and cycle guards without changing producer files or the manifest.

**Revision 28 review deviation:** named-callable and nested-carrier review found that stable aliases to declared functions lost the target's source identity and one-level carrier selection could neither reach nested/factory values nor distinguish exact map read-only callbacks from ambiguity. Revision 28 adds typed named-target identity and bounded recursive carrier evaluation while preserving ordinary receiver/import/parameter source calls without target flow.

## Issues Encountered

- The former text-based discovery could certify dead markers and the former authority join allowed zero authorities. Both were exposed by the planned RED tests and removed without changing producer code.
- Revision 1 exposed three test blind spots despite the original GREEN suite: receiver identity loss, sibling descriptor evidence borrowing, and unknown registration omission. The expanded mutation matrix now exercises every required producer family.
- Revision 2 exposed a second-order proof gap between source enumeration and known-ID validation. Exact registration metadata is now carried through that boundary rather than recomputed from the constructor.
- Revision 3 exposed selection, control-flow reachability, AST object identity, lexical ordering, and deterministic diagnostics as additional evidence requirements. All are now enforced by focused regressions.
- Revision 4 exposed resolver/registrar ownership, dead registration/helper edges, and stale reaching definitions; Revision 5 closed the remaining lexical-shadow and conditional-ambiguity gaps without changing producer code.
- Revision 6 exposed Range zero-iteration and `For.Post` as the remaining control-flow blind spots; both now fail closed when loop state cannot be proved identical.
- Revision 7 exposed branch/return bypass as a further loop-control blind spot. The first GREEN implementation also exposed and fixed a typed-nil AST block check before the full verification matrix.
- Revision 8 exposed a target-binding gap in the intentionally narrow optional-sandbox exception; exact AST parent checks now prevent cross-loop or labeled evidence borrowing.
- Revision 9 exposed remaining name-based evidence inside the exception. The additional second-Resolve regression ensures two individually authoritative calls cannot mix capability and error provenance.
- Revision 10 exposed stale same-object evidence after sequential Resolve overwrites; latest-before-condition selection now prevents earlier bindings from certifying the continue.
- Revision 11 exposed ordinary and conditional writes that did not create new binding records; exact object-position barriers now fail closed for those writes.
- Revision 12 exposed row and selector writes outside the capability/error binding records; the current Range row now remains stable from authoritative Resolve through descriptor construction.
- Revision 13 exposed pointer-based row mutation outside direct-write tracking; taking the current row or selector address now invalidates the optional-sandbox exception.
- Revision 14 exposed pre-Resolve pointer aliases outside Revision 13's scan window; the barrier now covers the full current row scope through descriptor construction.
- Revision 15 exposed implicit method-based row escapes outside unary-address tracking; current-row-rooted method calls now invalidate the optional-sandbox exception.
- Revision 16 exposed method-value aliases outside direct-call tracking; parent-aware selector context now preserves only the exact live field reads needed by production.
- Revision 18 exposed arbitrary capability-expression wrappers outside descriptor string evaluation; exact conversion-aware reference/object provenance now rejects wrapper-based authored-capability fallback.
- Revision 19 exposed a cross-file parser-scope gap where another file's package-level `string` declaration remained invisible; package-wide AST shadow marking now rejects both descriptor ID and capability conversions.
- Revision 20 initially removed the three production registrations installed inside returned option closures, then exposed Relay batch terminal effects inside switch cases as a live dominance container. Exact option-consumer proof and ordered case-clause dominance restored those live rows without weakening dead-closure or conditional-bypass negatives. Profiling then identified repeated parent-map/call walks, which were cached once per package before final verification.
- Revision 21 exposed the distinction between closure existence and closure execution: range provenance must reject even captured mutable state, while guard/effect reachability may expand only an exact invoked binding. Keeping capture detection at the FuncLit node and call expansion at the exact invocation preserves both fail-closed requirements.
- Revision 25 exposed three mutation shapes outside Revision 24's direct-write predicate: Range assignment, address escape, and nested invoked-helper rebinding. Reusing one exact-object provenance walk now covers all three while retaining the safe shadow, ordering, uninvoked, and read-only controls.
- Revision 26 exposed the difference between dynamic provenance frames and their static lexical ancestry, plus the difference between current-frame defer registration and invoked-helper completion. Its GREEN recovery also showed why loop-control ambiguity must be conditioned on an actual in-scope callable binding candidate rather than applied to global functions.
- The callback follow-up showed that pruning an uninvoked FuncLit is correct only while it remains local and unpassed; once a direct or stably aliased writer closure is handed to an opaque call, its exact-object mutation/escape behavior must be included without rejecting pure read-only callbacks.
- Revision 27 exposed a second defer distinction: a deferred effect is registered at its lexical site for guard dominance even though an unresolved deferred callee in the current frame must not execute early for object provenance. It also showed that callback provenance must follow exact local carrier expressions, while unresolved global/import expressions without target flow remain ordinary.
- Revision 28 exposed the distinction between a stable declared-function target and a local FuncLit, plus the distinction between a real local carrier binding and an ordinary receiver/parameter call with no carrier candidate. Keeping those identities explicit prevents both missed named effects and false production ambiguity while recursive exact selection preserves read-only controls.

## Self-Check: PASSED

- The Plan 18 SUMMARY and both releasecontract implementation files exist.
- Final Revision 28 implementation commit `28352e3` exists and contains only `effect_registry.go` and `effect_registry_test.go`.
- Existing Plan 21 Chat changes and protected `.planning` changes remain unstaged and outside Plan 18 commits.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase tracking remains 18/22 complete; Plan 21 is the next execution target. This Revision 28 closeout does not advance STATE or ROADMAP.
- Plan 18 is repository-local E1 proof only. It does not provide target/live evidence, does not close commercial readiness, and does not complete `RELS-01`.
- `RELS-01` remains Pending; Plans 19-22 and later target-release phases own the remaining runtime gate, audit, tracking-head Stage B, and external proof.

---
*Phase: 31.1-readiness-fail-closed*
*Completed: 2026-07-20*
