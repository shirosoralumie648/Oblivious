---
phase: 31.1-readiness-fail-closed
plan: "19"
type: execute
wave: 2
depends_on: ["31.1-13", "31.1-14", "31.1-15", "31.1-17", "31.1-18"]
files_modified:
  - src/server/cmd/server/main.go
  - src/server/cmd/server/main_test.go
  - src/server/internal/http/server.go
  - src/server/internal/http/runtime_effect_coverage_test.go
  - src/server/internal/chat/relay_gateway.go
  - src/server/internal/chat/relay_gateway_test.go
  - scripts/verify-readiness-contract.sh
  - scripts/verify-readiness-contract-fixtures.sh
autonomous: true
gap_closure: true
requirements: [RELS-01]
must_haves:
  truths:
    - "Production uses a duplicate-rejecting EffectRegistry and exposes a defensive exact descriptor snapshot after strict BuildRuntime construction (D-RDY-11)."
    - "The real monolith composition consumes Plan 18's frozen complete descriptor contract and produces an exact nonzero manifest/static/runtime/profile/authority/configuration join including the selected live Agent/MCP web-search chain, without constructing independent Registry/WebsearchTool objects merely for coverage (D-RDY-11, D-RDY-14, D-RDY-15)."
    - "`scripts/verify-readiness-contract.sh` runs the production exact-join selector in both Stage A and Stage B; missing/extra/duplicate/zero/authority/profile/static/runtime mutations fail (D-RDY-27)."
    - "No constructor registers an identical descriptor twice merely because two consumers share a capability (D-RDY-11)."
  artifacts:
    - path: "src/server/internal/http/server.go"
      provides: "Defensive production descriptor snapshot on Runtime"
      contains: "EffectDescriptors"
    - path: "src/server/internal/http/runtime_effect_coverage_test.go"
      provides: "Real BuildRuntime exact-join and production mutation matrix"
      contains: "TestProductionEffectCoverageContract"
    - path: "scripts/verify-readiness-contract.sh"
      provides: "Mandatory production exact-join invocation before identity-bearing harness work"
      contains: "TestProductionEffectCoverageContract"
  key_links:
    - from: "Plan 18 runtimeDescriptorSpecs/manifest"
      to: "TestProductionEffectCoverageContract"
      via: "Plan 19 treats the contract as read-only and fixes only real producer/composition registration drift"
      pattern: "VerifyEffectCoverage"
    - from: "BuildRuntime"
      to: "releasecontract.EffectRegistry.Snapshot"
      via: "strict constructors finish registration before a defensive runtime snapshot is exposed"
      pattern: "EffectDescriptors"
    - from: "verify-readiness-contract.sh"
      to: "TestProductionEffectCoverageContract"
      via: "both stage modes run the same real composition exact join"
      pattern: "run-go-tests-matched"
---

<objective>
Close the production/gate portions of CR-01 by joining the strict real BuildRuntime descriptor snapshot from the checked-in phase gate.

Purpose: Synthetic unit registries do not prove the descriptors actually constructed by the shipped monolith.
Output: Strict production registry/snapshot consuming Plan 18's complete frozen contract, exact real-composition test, composition mutations, and phase-gate invocation.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-VERIFICATION.md
@.planning/phases/31.1-readiness-fail-closed/31.1-13-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-14-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-18-SUMMARY.md
@src/server/cmd/server/main.go
@src/server/internal/http/server.go
@src/server/internal/releasecontract/effect_registry.go
@scripts/verify-readiness-contract.sh

<constraints>
- Production registry rejects all duplicate IDs, including byte-identical duplicates; fix ownership instead of adding idempotent acceptance.
- `config/release/readiness-effect-surface.v1.json`, `runtimeDescriptorSpecs`, and static discovery are exclusively owned by completed Plan 18. Do not edit them in this plan; a contract defect routes back to Plan 18, while a runtime missing/extra descriptor routes to the producer/composition owner.
- Snapshot is evidence only and cannot authorize runtime effects or replace current Guard checks.
- Gate uses real strict BuildRuntime with test-safe unopened SQL handle/config and no constructor-time external effect.
- Real-composition fixtures may configure supported providers but may not call `agent/tools.NewWebsearchToolWithOptions`, `NewRegistryWithOptions`, or `NewDefaultRegistryWithOptions`; only descriptors naturally returned by BuildRuntime count.
- CR-06/CR-09 remain Phase 34, WR-02 remains Phase 35, and readiness OpenAPI Envelope parity remains Phase 31.2.
- Evidence is repository-local E1/E2 only.
</constraints>

<interfaces>
Use `releasecontract.NewEffectRegistry()` as the production registrar. Add `Runtime.EffectDescriptors() []releasecontract.EffectDescriptor` or an immutable field/accessor populated only after all strict constructors return. The accessor returns a defensive sorted copy. The production coverage test loads the completed Plan 18 manifest/spec contract, authored release contract, monolith profile, selected provider configuration, and RuntimeAuthorities, calls real `BuildRuntime`, then calls `VerifyEffectCoverage` with the runtime snapshot and repository root. It must assert `agent.tool.builtin`, `mcp.websearch.builtin`, and exactly one selected provider descriptor, while `agent.tool.web_search` and ownerless Registry rows remain absent. This plan may change producer registration ownership only in its declared files and may not add or alter expected rows/mappings.
</interfaces>
</context>

## Artifacts this phase produces

- Duplicate-rejecting production descriptor registry.
- Defensive descriptor snapshot from strict BuildRuntime.
- Real production exact-join test and mutation matrix.
- Mandatory phase-gate selector invocation.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose a strict real-composition descriptor snapshot</name>
  <files>src/server/cmd/server/main.go, src/server/cmd/server/main_test.go, src/server/internal/http/server.go, src/server/internal/http/runtime_effect_coverage_test.go</files>
  <read_first>
    - src/server/cmd/server/main.go
    - src/server/cmd/server/main_test.go
    - src/server/internal/http/server.go
    - src/server/internal/releasecontract/effect_registry.go
    - config/release/readiness-effect-surface.v1.json
  </read_first>
  <behavior>
    - Strict BuildRuntime uses `releasecontract.NewEffectRegistry`, and any duplicate registration fails even when byte-identical.
    - Runtime snapshot is sorted, defensive, and unavailable on failed construction.
  </behavior>
  <action>Replace the permissive `runtimeEffectRegistry` with `releasecontract.NewEffectRegistry`; delete byte-identical idempotent acceptance and update the server registry test to require `effect_registry_duplicate`. Capture a defensive sorted registry snapshot only after BuildRuntime finishes and expose it on Runtime for evidence/tests. Add `TestRuntimeEffectRegistrySnapshotContract` with a controlled registry/runtime fixture so this task can prove duplicate rejection, failed-construction absence, sorting, and defensive-copy behavior before Task 2 resolves any real-composition duplicate ownership.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestRuntimeEffectRegistrySnapshotContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./cmd/server '^TestRuntimeEffectRegistryRejectsExactDuplicateContract$'</automated>
  </verify>
  <acceptance_criteria>
    - Controlled strict construction returns a nonzero sorted descriptor snapshot.
    - Snapshot mutation cannot change Runtime/registry state.
    - Duplicate registration fails construction.
  </acceptance_criteria>
  <done>The shipped composition, not a synthetic registry, exposes the descriptor set used for exact coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Match the frozen contract from real strict composition</name>
  <files>src/server/internal/http/server.go, src/server/internal/http/runtime_effect_coverage_test.go, src/server/internal/chat/relay_gateway.go, src/server/internal/chat/relay_gateway_test.go</files>
  <read_first>
    - src/server/internal/http/server.go
    - src/server/internal/http/runtime_effect_coverage_test.go
    - src/server/internal/chat/relay_gateway.go
    - src/server/internal/chat/relay_gateway_test.go
    - src/server/internal/releasecontract/effect_registry.go
    - config/release/readiness-effect-surface.v1.json
    - .planning/phases/31.1-readiness-fail-closed/31.1-18-SUMMARY.md
  </read_first>
  <behavior>
    - Real strict BuildRuntime registers every Plan 18 selected applicable descriptor exactly once, including `agent.tool.builtin`, `mcp.websearch.builtin`, exactly one provider row, Admin, Marketplace/Billing checkout, MCP transport, Chat, Relay, worker, channel, and finance rows.
    - Direct Tavily, fallback-chain, and other single-provider configurations select `mcp.websearch.tavily`, `mcp.websearch.chain`, or `mcp.websearch.provider` respectively; `agent.tool.web_search` and independent Registry rows remain absent.
    - Any real runtime missing, extra, duplicate, unclassified, or excluded descriptor fails without changing the expected manifest/spec map.
    - Shared Chat consumers have one descriptor construction owner; per-call authorization remains at every concrete provider attempt.
  </behavior>
  <action>Run the frozen Plan 18 contract against snapshots produced by real strict BuildRuntime for direct Tavily, a configured fallback chain, and another supported single provider. Assert the shared `agent.tool.builtin`/`mcp.websearch.builtin` rows plus exactly one selected provider descriptor, and assert `agent.tool.web_search` plus independent Registry rows are absent. Add an AST guard that the fixture itself never constructs `agent/tools.WebsearchTool` or Registry objects. Where normal composition exposes identical duplicate registrations, notably shared Chat gateway composition, assign one construction owner in the declared producer/composition files and remove duplicate construction rather than weakening the registry. Keep per-effect current guards in each consumer. Add a producer-family count table to `TestProductionEffectCoverageContract` and mutate the runtime snapshot for exact missing/extra/duplicate/excluded/non-selected-provider cases. Do not edit `effect_registry.go`, its tests, or the manifest; a failure caused by contract incompleteness stops and routes to Plan 18.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestProductionEffectCoverageContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./internal/chat '^TestChatRelayReadinessDispatchContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./cmd/server '^TestRuntimeBuildAndBackgroundLifecycleContract$'</automated>
  </verify>
  <acceptance_criteria>
    - Real strict BuildRuntime returns the complete frozen nonzero descriptor set.
    - Every selected descriptor ID count is exactly one, every live producer family has an asserted row, and every excluded/ownerless independent family has an asserted zero count.
    - Provider configuration selects exactly one provider descriptor and no coverage-only Registry/WebsearchTool constructor is called.
    - Missing, extra, duplicate, excluded, and zero runtime mutations fail stably.
    - Expected contract files remain unchanged from Plan 18.
  </acceptance_criteria>
  <done>The real shipped composition exactly matches the independently completed descriptor contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Invoke the six-input production exact join from the phase gate</name>
  <files>src/server/internal/http/runtime_effect_coverage_test.go, scripts/verify-readiness-contract.sh, scripts/verify-readiness-contract-fixtures.sh</files>
  <read_first>
    - src/server/internal/http/runtime_effect_coverage_test.go
    - scripts/verify-readiness-contract.sh
    - scripts/verify-readiness-contract-fixtures.sh
    - src/server/internal/releasecontract/effect_registry_test.go
  </read_first>
  <behavior>
    - The production test joins Plan 18's checked-in complete manifest/spec map, structural static discovery, real runtime snapshot, committed monolith profile, RuntimeAuthorities, and selected provider configuration.
    - Each missing/extra/duplicate/authority/profile/static/runtime/configuration-selection mutation is rejected.
    - Stage A and Stage B both fail before the deployment harness if the production exact join fails.
  </behavior>
  <action>Complete `TestProductionEffectCoverageContract` with a mutation table covering all six inputs and exact stable failure codes while treating Plan 18 contract files as read-only. Include selection mutations that claim Tavily while the runtime contains chain/provider, omit the selected provider row, or accumulate non-selected alternatives. Add the anchored selector near the start of `verify-readiness-contract.sh` for both stage modes, before Docker/harness work. Extend fixture verification to assert the selector cannot be removed and to run the manifest/spec, structural, authority, configuration-selection, and production mutation selectors. Keep zero-match protection through `run-go-tests-matched.sh`.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestProductionEffectCoverageContract$' &amp;&amp; bash scripts/verify-readiness-contract-fixtures.sh</automated>
  </verify>
  <acceptance_criteria>
    - The checked-in phase gate invokes the anchored production exact-join selector in both stages.
    - All manifest/static/runtime/profile/authority/configuration-selection mutations fail.
    - Zero tests, zero descriptors, or missing Docker-independent prerequisites cannot pass vacuously.
  </acceptance_criteria>
  <done>Production effect completeness is a mandatory repository gate rather than an isolated synthetic test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strict constructors -> production descriptor registry | Real runtime composition declares all effect seams. |
| Repository phase gate -> release evidence | Gate outcome determines whether identity-bearing E2 work may continue. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-19-01 | Tampering | Production descriptor registry | high | mitigate | Reject every duplicate and expose only defensive post-construction snapshots. |
| T-31.1-19-02 | Elevation of Privilege | Missing production seam | high | mitigate | Five-way exact join over real BuildRuntime fails missing/extra/unknown/excluded rows. |
| T-31.1-19-03 | Spoofing | Phase gate evidence | high | mitigate | Anchored non-vacuous selector runs before both Stage A and Stage B harness work. |
| T-31.1-19-04 | Tampering | Expected contract ownership | high | mitigate | Treat Plan 18 manifest/spec map as frozen; contract defects route back instead of being rewritten to match runtime. |
</threat_model>

<verification>
- Run all three focused task selectors plus server, Chat, and releasecontract suites.
- Run `bash scripts/verify-readiness-contract-fixtures.sh`.
- Run `cd src/server &amp;&amp; go test ./internal/http ./internal/chat ./internal/releasecontract ./cmd/server -count=1`.
- Run `git diff --check`.
</verification>

<success_criteria>
- All CR-01 structural, authority, production snapshot, and gate gaps are closed.
- Production registration is exact-once and evidence-only.
</success_criteria>

<commit_policy>
Create one atomic commit: `fix(31.1-19): gate production effect coverage`. Do not push. Plan 20 ends with a HARD EXTERNAL/MANUAL POST-CLOSEOUT GATE handoff; no GSD executor, verifier, tracker, hook, or autonomous orchestrator step owns final Stage B or push.
</commit_policy>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-19-SUMMARY.md` with production descriptor/mutation counts and gate commands.
</output>
