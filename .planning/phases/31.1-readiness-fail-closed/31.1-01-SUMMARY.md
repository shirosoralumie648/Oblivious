---
phase: 31.1-readiness-fail-closed
plan: "01"
subsystem: runtime-readiness-authority
tags: [go, atomic-pointer, fail-closed, catalog-authority, surface-report]
requires:
  - phase: 31-06
    provides: "Authored release contract, trusted build identity, committed profile resolver, typed report registry, and atomic writer patterns"
provides:
  - "Exact authored readiness evaluator and source-authored entrypoint authorization"
  - "Deadline-bounded immutable readiness generation manager with write-only audit export"
  - "Digest-bound catalog and typed effect RuntimeAuthorities carried through RouterOptions"
  - "Side-effect-free online/offline readiness SurfaceReportV1 constructor"
affects: [phase-31.1-plans-02-through-10, phase-31.2, runtime-composition, release-evidence]
tech-stack:
  added: []
  patterns: [single in-memory authorization authority, immutable atomic generations, server-owned current catalog resolution, typed evidence-only inspection]
key-files:
  created:
    - src/server/internal/releasecontract/readiness.go
    - src/server/internal/releasecontract/readiness_test.go
    - src/server/internal/releasecontract/readiness_manager.go
    - src/server/internal/releasecontract/readiness_manager_test.go
    - src/server/internal/releasecontract/catalog.go
    - src/server/internal/releasecontract/catalog_test.go
    - src/server/internal/surfacereport/readiness.go
    - src/server/internal/surfacereport/readiness_test.go
  modified:
    - src/server/internal/buildinfo/identity.go
    - src/server/internal/http/router.go
key-decisions:
  - "ReadinessManager remains the sole runtime authority; audit snapshots are durable output only and cannot seed a manager or guard."
  - "The evaluator accepts only the resolver-confirmed authored monolith profile and its exact 30s/120s/30s timing contract, with UTC epoch-nanosecond boundaries."
  - "RuntimeAuthorities compiles digest-bound catalog keys and the closed typed effect map once; excluded capability mappings remain unavailable to consumers."
  - "BuildIdentityV1 is defined on the release-contract dependency boundary and re-exported by buildinfo to preserve the Phase 31 API without a Go import cycle."
  - "RELS-01 remains pending until all Phase 31.1 runtime consumers, deployment wiring, and repository-local proofs are complete."
patterns-established:
  - "Probe timeout/error becomes typed blocked data, while malformed successful probe payloads are structural failures that preserve the prior generation."
  - "Catalog dispatch resolves (kind, id, runtime, contract digest) before the shared current-generation guard and accepts no caller capability ID."
  - "Readiness report construction validates trusted identity/profile and evaluator-owned snapshot state, but accepts no writer, output path, manager, or guard."
requirements-completed: []
coverage:
  - id: D1
    description: "One evaluator and manager enforce trusted identity, authored timing, exact freshness, monotonic immutable generations, bounded probes, and write-only audit behavior."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/releasecontract/readiness_test.go#TestReadinessEvaluatorContract"
        status: pass
      - kind: unit
        ref: "src/server/internal/releasecontract/readiness_manager_test.go#TestReadinessManagerLifecycleContract"
        status: pass
      - kind: other
        ref: "bash scripts/run-go-tests-matched.sh ./internal/releasecontract '^TestReadinessManagerPublicationRace$' -race"
        status: pass
    human_judgment: false
  - id: D2
    description: "One immutable RuntimeAuthorities value supplies digest-bound catalog resolution and profile-applicable typed effect bindings through RouterOptions."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/releasecontract/catalog_test.go#TestCatalogAuthorityContract"
        status: pass
      - kind: unit
        ref: "src/server/internal/releasecontract/catalog_test.go#TestRuntimeAuthoritiesContract"
        status: pass
    human_judgment: false
  - id: D3
    description: "Readiness reports use a strict registered details schema and trusted online/offline inspection without filesystem or runtime authorization ownership."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/surfacereport/readiness_test.go#TestReadinessSurfaceContract"
        status: pass
      - kind: integration
        ref: "cd src/server && go test ./... -count=1"
        status: pass
    human_judgment: false
duration: 37 min
completed: 2026-07-17
status: complete
---

# Phase 31.1 Plan 01: Readiness Fail-Closed Authority Summary

**Exact 30s/120s/30s readiness evaluation now feeds bounded immutable generations, digest-bound runtime authorities, and evidence-only typed reports from one fail-closed Go contract.**

## Performance

- **Duration:** 37 min
- **Started:** 2026-07-17T15:55:06Z
- **Completed:** 2026-07-17T16:32:02Z
- **Tasks:** 3
- **Files modified:** 10

## Accomplishments

- Added the sole evaluator/manager contract with trusted identity and profile closure, exact UTC nanosecond freshness/future-skew boundaries, monotonic generation publication, defensive copies, per-probe deadlines, strict HTTP probe semantics, and audit output that never becomes authorization input.
- Compiled current catalog subjects and the closed worker/provider/tool/HTTP/finance/Agent effect set into one immutable `RuntimeAuthorities` carrier; unknown, ambiguous, deleted, runtime-mismatched, excluded, or absent mappings fail before dispatch or consumer validation.
- Registered readiness details and built a pure `SurfaceReportV1` constructor for online manager evaluations and offline runtime snapshots, with repository-local claim limits, zero committed skips, trusted resolver identity/profile, and no writer/path/manager/guard input.

## Task Commits

1. **Task 1: Lock the exact evaluator and immutable snapshot contract** - `772a3fa`
2. **Task 2: Publish bounded immutable generations through the sole manager** - `ab52760`
3. **Task 3: Compile catalog authority and emit evidence-only readiness reports** - `a5f02fb`

## Files Created/Modified

- `src/server/internal/releasecontract/readiness.go` - Evaluator, immutable value copies, stable denial codes, guard/effect/entrypoint contracts, and inspection snapshot adapter.
- `src/server/internal/releasecontract/readiness_manager.go` - Bounded probes, strict HTTP probe, atomic generation publication, current-generation authorization, and durable audit writer.
- `src/server/internal/releasecontract/catalog.go` - Digest-bound catalog resolver, catalog authorizer, typed effect map, and immutable runtime authority carrier.
- `src/server/internal/surfacereport/readiness.go` - Strict readiness details registration and pure trusted report constructor.
- `src/server/internal/http/router.go` - Shared readiness, guard, effect registry, and runtime authority injection seam plus completeness validation.
- `src/server/internal/buildinfo/identity.go` - API-preserving alias to the shared release-contract identity shape.
- Four focused test files - Non-vacuous evaluator, manager/race, catalog/runtime-authority, and readiness-report contracts.

## Decisions Made

- Kept every runtime authorization decision in the manager-backed evaluator path; neither audit durability nor offline inspection can mutate or seed authorization.
- Bound catalog keys to the canonical contract digest and omitted mappings whose authored capability is excluded in the selected profile.
- Preserved `buildinfo.BuildIdentityV1` as the public Phase 31 type through an alias while moving its data shape below the existing `buildinfo -> releasecontract` dependency edge.
- Added explicit `RouterOptions.ValidateReadinessAuthorities`; Plan 31.1-02 owns mandatory production composition after it constructs all startup dependencies.
- Kept `RELS-01` pending because this foundation does not yet guard every consumer, wire deployment assets, or produce the final Phase 31.1 repository-local runtime proof.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Removed the Phase 31 identity import cycle without creating a second identity authority**
- **Found during:** Task 1
- **Issue:** The planned `releasecontract` evaluator must consume `buildinfo.BuildIdentityV1`, but `buildinfo` already imports `releasecontract` for contract load/digest verification; a direct import would make the Go module uncompilable.
- **Fix:** Defined the identity data shape at the lower release-contract boundary and re-exported it from `buildinfo` with a type alias. Existing provider, validation, JSON, and caller APIs remain unchanged.
- **Files modified:** `src/server/internal/buildinfo/identity.go`, `src/server/internal/releasecontract/readiness.go`
- **Verification:** Full `buildinfo`, `releasecontract`, and repository-wide Go suites pass.
- **Committed in:** `772a3fa`

**2. [Rule 1 - Bug] Repaired malformed sequential state output from the GSD metric/progress handlers**
- **Found during:** Plan close-out
- **Issue:** `state.record-metric` inserted a four-cell metric row into the six-column Quick Tasks table, while `state.update-progress` reported 25% but left the persisted frontmatter/current-position values at 9% and PLANNED.
- **Fix:** Removed the malformed Quick Tasks row, appended the metric to the Performance Metrics table, and synchronized status, activity, velocity, and the 8/32 progress value.
- **Files modified:** `.planning/STATE.md`, `.planning/ROADMAP.md`
- **Verification:** State now records Plan 1/10, 8/32 completed plans, 25%, IN PROGRESS; ROADMAP records 1/10 and the plan checkbox.
- **Committed in:** plan metadata commit

**Total deviations:** 2 auto-fixed (1 blocking issue, 1 state-generation bug). **Impact:** Both fixes preserve declared APIs and accurate workflow state without expanding product scope.

## Issues Encountered

- The first Task 1 run found an incorrect test-only reflection expectation: Go interface method reflection reports six inputs without a receiver, not seven. The assertion was corrected and every subsequent focused/full run passed.

## Known Stubs

None. Empty maps/slices in the touched code are initialized protocol collections or test fixtures, not runtime placeholders or user-visible fallback data.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 31.1-02 can construct trusted startup inputs once, require `RouterOptions.ValidateReadinessAuthorities`, and enforce listener-before-background lifecycle using these stable contracts.
- Plans 31.1-03 through 31.1-10 can consume the same manager guard, typed effect IDs, catalog authorizer, and report adapter without reloading contract/profile or inventing capability IDs.
- Evidence remains E1 repository-local automated proof. This plan does not establish E2 deployment behavior, E3/E4 evidence, non-monolith parity, target readiness, or commercial release readiness.

## Self-Check: PASSED

- All eight planned readiness/catalog/report files exist; the two required integration files are modified and committed.
- Task commits `772a3fa`, `ab52760`, and `a5f02fb` exist with no tracked-file deletions.
- All six focused top-level contract tests pass, including the manager publication suite under `-race`.
- `cd src/server && go test ./... -count=1`, `git diff --check`, and coverage classification all pass; coverage is 3/3 automated with no unknown result.
- Stub scan found no blocking placeholder; threat surfaces are limited to the plan's bounded dependency HTTP probe and write-only atomic audit boundary.

---
*Phase: 31.1-readiness-fail-closed*
*Completed: 2026-07-17*
