---
phase: 31.1-readiness-fail-closed
plan: "06"
type: execute
wave: 2
depends_on: ["31.1-01"]
files_modified:
  - src/server/internal/mcp/client.go
  - src/server/internal/mcp/client_test.go
  - src/server/internal/agent/tools/registry.go
  - src/server/internal/agent/tools/registry_readiness_test.go
  - src/server/internal/agent/tools/websearch.go
  - src/server/internal/mcp/websearch/websearch.go
  - src/server/internal/mcp/websearch/websearch_test.go
  - src/server/internal/http/mcp_handler.go
  - src/server/internal/http/mcp_handler_test.go
autonomous: true
requirements: [RELS-01]
must_haves:
  truths:
    - "每个 MCP JSON-RPC、独立 agent/tools Registry executor 和 web-search provider attempt 都从当前服务端 catalog subject 重新解析，并在实际 effect 前读取同一 current-generation Guard；先前 ingress、attempt 或 generation 不授权下一次 effect（D-RDY-11, D-RDY-14, D-RDY-16, D-RDY-21）。"
    - "caller-supplied 或旧持久化 capability ID 从不成为 authority；unknown、deleted、ambiguous、runtime-class mismatch 与当前 readiness denial 都稳定拒绝且 downstream call count 为零（D-RDY-20, D-RDY-21, D-RDY-22）。"
    - "MCP create/execute JSON 严格拒绝 capabilityId 和其他未知字段，合法 mutation 在 store write 前使用服务端 MCP runtime subject 解析并授权（D-RDY-20, D-RDY-21）。"
    - "每个无副作用 consumer constructor 从 Plan 31.1-01 bindings resolve 并 Register 稳定 typed descriptor 恰好一次；同一实例的连续 effect 只重新解析 current subject 并调用 shared Guard，绝不重复 Register 或缓存 generation/verdict（D-RDY-11, D-RDY-16）。"
    - "agent/tools Registry 只是独立可复用 API，当前没有 production consumer；其测试不能证明 live agent.ToolExecutor 安全。Plan 10 单独拥有真实 builtin/custom HTTP/local Python/sandbox/MCP dispatch guard，Plan 03 后置 exact join 两组 seam（D-RDY-11, D-RDY-14）。"
  artifacts:
    - path: "src/server/internal/mcp/client.go"
      provides: "Per-request MCP catalog authorization immediately before HTTP transport"
    - path: "src/server/internal/agent/tools/registry.go"
      provides: "Independent registry API with server-derived builtin/custom/MCP subjects and execution-time re-resolution; not the live ToolExecutor proof"
    - path: "src/server/internal/agent/tools/registry_readiness_test.go"
      provides: "Zero-call registry denial contract across all tool runtime classes"
    - path: "src/server/internal/agent/tools/websearch.go"
      provides: "Outer tool selection wired to guarded provider/chain execution"
    - path: "src/server/internal/mcp/websearch/websearch.go"
      provides: "Current catalog/guard check inside every live Chain provider attempt"
    - path: "src/server/internal/http/mcp_handler.go"
      provides: "Strict MCP mutation/execution DTO boundary and server-owned catalog mutation authority"
  key_links:
    - from: "src/server/internal/mcp/client.go"
      to: "src/server/internal/releasecontract/catalog.go"
      via: "CatalogAuthorizer.ResolveAndRequire immediately before httpClient.Do"
      pattern: "ResolveAndRequire"
    - from: "src/server/internal/agent/tools/registry.go"
      to: "src/server/internal/releasecontract/readiness.go"
      via: "Current CatalogSubject and BoundaryOutbound immediately before executor"
      pattern: "BoundaryOutbound"
    - from: "src/server/internal/agent/tools/websearch.go"
      to: "src/server/internal/mcp/websearch"
      via: "Fresh catalog/guard check before every provider.Search attempt"
      pattern: "Search"
    - from: "src/server/internal/http/mcp_handler.go"
      to: "src/server/internal/mcp/client.go"
      via: "Strict request rejection before mutation; client repeats resolution at execution"
      pattern: "DisallowUnknownFields"
---

<objective>
让 MCP transport、独立 Agent tools Registry API、web-search fallback 和 MCP mutation 在每个新 effect 前重新使用服务端 catalog 与 current readiness，拒绝任何 caller/stale capability authority。

Purpose: 关闭 Registry API、JSON-RPC transport、provider fallback 和宽松 DTO 可绕过动态 readiness 的路径，同时明确不把未接入 production 的 Registry 当作 live Agent 安全证明。
Output: guarded MCP client、catalog-aware independent registry、per-attempt web search、strict MCP DTO，以及 transport/registry/store zero-call E1 suites；Plan 10 另行闭合真实 ToolExecutor。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-RESEARCH.md
@.planning/phases/31.1-readiness-fail-closed/31.1-PATTERNS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-VALIDATION.md
@.planning/phases/31.1-readiness-fail-closed/31.1-01-SUMMARY.md
@src/server/internal/mcp/client.go
@src/server/internal/mcp/client_test.go
@src/server/internal/agent/tools/registry.go
@src/server/internal/agent/tools/websearch.go
@src/server/internal/mcp/websearch/websearch.go
@src/server/internal/mcp/websearch/websearch_test.go
@src/server/internal/http/mcp_handler.go
@src/server/internal/http/mcp_handler_test.go

<interfaces>
Phase 31 and Plan 31.1-01 are unexecuted hard dependencies at planning time. Before RED, verify the declared artifacts exist with these exact contracts; if any is absent or differs, stop and complete/correct the dependency rather than creating a local resolver, guard, report type, test helper, effect manifest, timing default, or compatibility authority in this plan:

```go
func Load(ctx context.Context, repoRoot, contractPath, schemaPath string) (AuthoredContractV1, error)
func LoadBytes(ctx context.Context, repoRoot string, contractBytes, schemaBytes []byte) (AuthoredContractV1, error)
type ProfileResolver interface {
    ResolveCommittedProfile(ctx context.Context, repoRoot, contractPath, schemaPath, profileID string) (DeploymentProfile, error)
}
type IdentityProvider interface {
    Resolve(ctx context.Context, repoRoot, contractPath, schemaPath string) (buildinfo.BuildIdentityV1, error)
}
type CatalogSubject struct {
    Kind string
    ID string
    Runtime string
}
func (a CatalogAuthorizer) ResolveAndRequire(ctx context.Context, subject CatalogSubject, boundary Boundary) (CapabilityID, error)
type Guard interface {
    Require(context.Context, string, Boundary) error
}
type EffectDescriptor struct {
    ID string
    CapabilityID string
    Boundary Boundary
    Owner string
}
type EffectRegistrar interface {
    Register(EffectDescriptor) error
}
type EffectID string
type CapabilityBindings struct { /* private immutable lookup */ }
func (b CapabilityBindings) Resolve(effect EffectID) (CapabilityID, error)
type RuntimeAuthorities struct {
    CatalogAuthorizer CatalogAuthorizer
    CapabilityBindings CapabilityBindings
}
func NewRuntimeAuthorities(contract AuthoredContractV1, profile DeploymentProfile, guard Guard) (RuntimeAuthorities, error)
```

`src/server/internal/releasecontract`, `src/server/internal/surfacereport`, and `scripts/run-go-tests-matched.sh` are the only upstream hard dependencies and are not created or altered here. This plan registers typed runtime descriptors and proves them dynamically through package-local registrar spies; it neither reads nor requires `config/release/readiness-effect-surface.v1.json`. Plan 03 runs later, creates that expected inventory, and performs the exact expected/static/runtime join. The shared manager continues to consume only authored `refreshIntervalSeconds=30`, `maxAgeSeconds=120`, and `allowedFutureSkewSeconds=30`; these consumers perform no time/verdict calculation and accept no override.
</interfaces>
</context>

## Artifacts this phase produces

- Modified files: eight existing source/test paths plus new `src/server/internal/agent/tools/registry_readiness_test.go`, exactly nine files in `files_modified`.
- Readiness injection: MCP Client, Registry, WebsearchTool, Chain, and mcpHandler receive the exact startup `RuntimeAuthorities`, shared `Guard`, and `EffectRegistrar`; zero/missing authority fails construction rather than installing local deny-all/global fallback.
- Runtime descriptors: MCP Client, independent Registry, WebsearchTool/Chain provider and mcpHandler constructors each resolve stable server-owned descriptors from validated authored bindings and register them exactly once; effect calls only re-authorize current subjects. Package tests assert construction-time registration dynamically without an expected-manifest file.
- New top-level suites: `TestMCPReadinessDispatchContract`, `TestToolRegistryReadinessExecutionContract`, `TestWebsearchReadinessFallbackContract`, `TestWebsearchChainReadinessPerProviderContract`, and `TestMCPCatalogMutationContract`.
- No new duration, audit reader, catalog cache authority, report type, expected effect inventory, helper script, docs, migration, or E2/E3/E4 evidence artifact.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard every MCP JSON-RPC dispatch from the current catalog</name>
  <read_first>
    - src/server/internal/mcp/client.go
    - src/server/internal/mcp/client_test.go
    - src/server/internal/releasecontract/catalog.go
    - src/server/internal/releasecontract/readiness.go
    - .planning/phases/31.1-readiness-fail-closed/31.1-01-SUMMARY.md
  </read_first>
  <files>src/server/internal/mcp/client.go, src/server/internal/mcp/client_test.go</files>
  <behavior>
    - Test V31.1-R07/T-RDY-03: disabled, blocked, stale, unavailable, identity mismatch, unknown/deleted/ambiguous binding, and MCP runtime-class mismatch make HTTP transport call count zero.
    - Test V31.1-R07/T-RDY-07: Connect initialize, initialized notification, tools/list, and tools/call each construct a fresh server-derived MCP CatalogSubject and call the current authorizer immediately before their own HTTP attempt.
    - Test D-RDY-16: an authorized first request followed by fake-clock expiry/guard denial blocks the next protocol step or tool attempt without retaining a capability ID or generation.
    - Test V31.1-R09/T-RDY-03: `NewClient` resolves and registers its typed transport descriptor exactly once; two authorized requests on the same Client keep registration count at one while guard and HTTP counts reach two, and constructing a duplicate owner against the same strict registry fails before use.
  </behavior>
  <action>Write `TestMCPReadinessDispatchContract` RED first with a counting RoundTripper, current-authority spy, and strict registrar. Extend `NewClient` with the exact startup RuntimeAuthorities/Guard/Effects options; zero/missing carrier fails construction, with no source-compatibility deny-all/global substitute. During `NewClient`, resolve the stable MCP transport `EffectID` once through `Authorities.CapabilityBindings`, build its typed descriptor, and call `Effects.Register` exactly once before returning the instance; registration failure, including duplicate owner construction against the same EffectRegistry, fails construction. Retain only that immutable descriptor as wiring evidence, never a readiness generation or verdict. Derive each runtime `CatalogSubject` from the current persisted server, JSON-RPC method/tool identity and authored MCP runtime; never accept/read/cache a caller capability ID. Pass that subject into `sendRequest`, call `Authorities.CatalogAuthorizer.ResolveAndRequire(ctx,subject,BoundaryOutbound)` as the final authorization operation before `httpClient.Do`, and repeat for initialize, notification, list, call, retry or later attempt. No request path calls `Register`. Prove two authorized requests on one instance produce registrar/guard/HTTP counts 1/2/2, then make the first pass and expire current generation so the second produces counts 1/2/1. Preserve tenant/server lookup and parsing, but denial makes zero transport/state changes. Tests reject local contract/profile reads, hard-coded IDs and global authority.</action>
  <acceptance_criteria>
    - Every actual `httpClient.Do` has exactly one immediately preceding current catalog/guard decision for that attempt.
    - Expiry after one authorized request prevents the next request and leaves its transport counter unchanged.
    - One Client registers once across repeated requests; a second constructor claiming the same descriptor fails as duplicate, while repeated effects on the first instance do not re-register.
    - Unknown/deleted/ambiguous/runtime-mismatched subjects and every readiness denial make zero HTTP calls and return stable sanitized readiness/catalog errors.
    - No Client field, method argument, Server field, request payload, or cache stores an authorizing capability ID or generation.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/mcp '^TestMCPReadinessDispatchContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.1-R07 and T-RDY-03/T-RDY-07 prove all MCP protocol/tool dispatches use current server authority with zero-call denial.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-resolve the independent builtin, custom, and MCP Registry API</name>
  <read_first>
    - src/server/internal/agent/tools/registry.go
    - src/server/internal/mcp/client.go
    - src/server/internal/releasecontract/catalog.go
    - src/server/internal/releasecontract/readiness.go
    - .planning/phases/31.1-readiness-fail-closed/31.1-01-SUMMARY.md
  </read_first>
  <files>src/server/internal/agent/tools/registry.go, src/server/internal/agent/tools/registry_readiness_test.go</files>
  <behavior>
    - Test V31.1-R07: builtin, custom, and MCP entries each retain only their business metadata/server-derived runtime class, then resolve and guard again on every Execute call immediately before executor invocation.
    - Test T-RDY-07: capability-like values in ToolMetadata.Extra, stale response metadata, or persisted entries cannot override current catalog resolution; unknown/deleted/ambiguous/runtime mismatch produces zero executor calls.
    - Test T-RDY-03: a first authorized execution does not authorize a second execution after current generation denial, and missing CatalogAuthorizer/Guard is deny-all.
    - Test V31.1-R07/T-RDY-03: `NewRegistry`/`NewDefaultRegistry` resolve and register the exact typed descriptors for all three Registry categories exactly once without loading an expected manifest; two legal Execute calls keep registrar count unchanged while guard/executor counts increment twice, and duplicate owner construction fails.
  </behavior>
  <action>Create `registry_readiness_test.go` and write `TestToolRegistryReadinessExecutionContract` RED first as a category table with counting executors/current-authority/registrar spies. Add exact RuntimeAuthorities/Guard/Effects options to `NewRegistry` and `NewDefaultRegistry`; zero/missing carrier fails construction. During construction, resolve the stable builtin/custom/MCP Registry `EffectID` values through `Authorities.CapabilityBindings` and register each typed descriptor exactly once; any duplicate/unknown registration fails the constructor. Retain the immutable descriptors as coverage evidence and retain CatalogSubject business metadata in entries, but never retain a generation or verdict. In Execute, copy under lock, reconstruct the current subject, call `Authorities.CatalogAuthorizer.ResolveAndRequire(ctx,subject,BoundaryOutbound)`, then invoke immediately without the lock; Execute never calls `Register`. The suite derives expected descriptor values from Plan 01 typed IDs and injected bindings, not from `readiness-effect-surface.v1.json`, and asserts no file read occurs. For every category, prove two authorized calls on one instance register once and guard/execute twice; after first success, expire the generation and prove the second guard occurs with zero second executor call; constructing a second owner with the same strict EffectRegistry fails duplicate registration. Keep the ownership statement explicit: current production `src/server/internal/agent/executor.go` does not consume this Registry, so these results prove only the independent API; Plan 10 guards that live dispatcher and Plan 03 inventories both. Extra/tags/response/caller data, local contract/profile reads, hard-coded IDs and global authority cannot influence authorization.</action>
  <acceptance_criteria>
    - The category table proves builtin/custom/MCP each resolve and guard once per execution attempt immediately before the executor.
    - Constructor registration is exactly once per stable descriptor; repeated Execute calls never change registrar count, while duplicate construction still fails closed.
    - Capability-like Extra/persisted metadata cannot change the resolver subject or guard capability, and no registry entry stores a resolved verdict/generation.
    - Unknown/deleted/ambiguous/runtime mismatch, missing authority, descriptor failure, and readiness denial all keep executor count at zero.
    - Existing listing/search/OpenAI metadata behavior stays deterministic and cannot itself authorize execution.
    - The suite passes before Plan 03 creates the expected manifest and cannot be cited as live `agent.ToolExecutor` coverage.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/agent/tools '^TestToolRegistryReadinessExecutionContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.1-R07 and T-RDY-03/T-RDY-07 prove current server-derived catalog authority for builtin/custom/MCP registry execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-guard web-search fallback and reject MCP capability injection</name>
  <read_first>
    - src/server/internal/agent/tools/websearch.go
    - src/server/internal/agent/tools/registry_readiness_test.go
    - src/server/internal/mcp/websearch/websearch.go
    - src/server/internal/mcp/websearch/websearch_test.go
    - src/server/internal/http/mcp_handler.go
    - src/server/internal/http/mcp_handler_test.go
    - src/server/internal/mcp/client.go
    - src/server/internal/releasecontract/catalog.go
    - src/server/internal/releasecontract/readiness.go
  </read_first>
  <files>src/server/internal/agent/tools/websearch.go, src/server/internal/agent/tools/registry_readiness_test.go, src/server/internal/mcp/websearch/websearch.go, src/server/internal/mcp/websearch/websearch_test.go, src/server/internal/http/mcp_handler.go, src/server/internal/http/mcp_handler_test.go</files>
  <behavior>
    - Test V31.1-R07/T-RDY-03: outer WebsearchTool and the live `internal/mcp/websearch.Chain.Search` both use the same authority; every Chain provider attempt resolves/guards immediately before `provider.Search`, and expiry after primary failure keeps the internal fallback counter zero.
    - Test V31.1-R07/T-RDY-07: unknown/deleted/ambiguous web_search binding or provider runtime mismatch prevents every provider call; provider/fallback names cannot select a different capability.
    - Test V31.1-R07/T-RDY-07: MCP add/execute bodies containing capabilityId or any unknown member return 400 invalid_request before catalog/store/client/transport calls; valid add resolves the server-owned MCP runtime subject and guards before CreateServer.
    - Test V31.1-R09/T-RDY-03: WebsearchTool, `NewProviderFromConfig`/Chain, and mcpHandler constructors each resolve/register their owned typed descriptors once; repeated calls keep registration counts fixed, re-guard each attempt, and duplicate construction against the same registry fails.
  </behavior>
  <action>Write `TestWebsearchReadinessFallbackContract`, `TestWebsearchChainReadinessPerProviderContract`, and `TestMCPCatalogMutationContract` RED first. Inject the exact same RuntimeAuthorities/Guard/Effects into outer WebsearchTool, `internal/mcp/websearch.NewProviderFromConfig`/Chain, and mcpHandler construction. Each constructor resolves its stable Plan 01 `EffectID` mappings and registers each owned typed descriptor exactly once; unknown or duplicate registration fails construction. A Chain member retains only its immutable wiring descriptor plus server-owned runtime discriminator, never a generation or verdict. `Chain.Search` calls `Authorities.CatalogAuthorizer.ResolveAndRequire(ctx,subject,BoundaryOutbound)` immediately before every provider.Search, including comma-configured fallback, and never calls `Register` or resolves static descriptor mappings during Search. Prove two authorized Search calls on one Chain keep registrar count at the constructor total while current guard/provider counts increment for both; after first success or primary failure, fake-clock denial makes the second/fallback downstream delta zero without changing registrar count. In mcpHandler, use `DisallowUnknownFields`/one JSON object, reject capabilityId, and resolve/guard the current server-owned MCP runtime subject before each AddServer/store effect; request handlers never register. `executeTool` delegates to Task 1 re-resolution. A second constructor claiming any same descriptor against the strict EffectRegistry fails, while repeated calls on the original instance remain valid. No consumer reloads contract/profile, hard-codes IDs, reads global authority or installs fallback. Plan 10 consumes this exact Chain carrier in the committed monolith direct/configured/builtin path.</action>
  <acceptance_criteria>
    - Both the outer tool suite and exact `internal/mcp/websearch` suite prove primary failure followed by current denial makes fallback Search count zero; an authorized fallback performs its own fresh resolver/guard call.
    - Constructor registrar counts remain exactly one per owned descriptor across repeated Search/mutation calls; duplicate owner construction still returns a strict duplicate error.
    - Provider names/configuration never determine capability ID, and every catalog/runtime/readiness denial produces zero Search calls.
    - capabilityId, trailing JSON, and other unknown MCP request members are rejected before store, catalog mutation, client, or transport effects.
    - A valid MCP create resolves/guards once before CreateServer, persists only business/server configuration, and later tool execution resolves/guards independently in Client.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/agent/tools '^TestWebsearchReadinessFallbackContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./internal/mcp/websearch '^TestWebsearchChainReadinessPerProviderContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./internal/http '^TestMCPCatalogMutationContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.1-R07 and T-RDY-03/T-RDY-07 prove per-fallback authorization plus strict mutation/execution capability rejection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| current catalog/readiness -> MCP/tool/provider effect | A previous attempt or stale subject must not authorize a later transport/executor/Search call. |
| MCP/tool metadata and JSON -> server mutation/execution | Caller, UI, Extra, and persisted metadata are untrusted capability inputs. |
| side-effect-free construction -> effect coverage registry | Each stable descriptor registers once; strict duplicate rejection detects repeated construction/owner conflict without affecting repeated effects on one instance. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-RDY-03 | Elevation of Privilege | MCP/registry/web-search attempt sequencing | high | mitigate | Construction-time descriptor registration, current subject resolution plus shared Guard immediately before every effect, same-instance double-call/expiry tests, duplicate-constructor rejection, and zero-call spies. |
| T-RDY-07 | Tampering | capability selection in DTO/metadata/persistence | high | mitigate | Strict unknown-field decoding, server-derived runtime subjects, no stored verdict, mutation and execution re-resolution, and mismatch zero-call tests. |
| T-31.1-06-01 | Information Disclosure | MCP/catalog denial responses | medium | mitigate | Stable sanitized codes and assertions excluding token, URL, probe, resolver, and internal binding detail. |
</threat_model>

<verification>
- Before RED, require the actual Phase 31/31.1-01 releasecontract, surfacereport, exact helper, authored 30s/120s/30s profile fields, and EffectRegistrar contract; do not require or read Plan 03-owned expected effect inventory.
- Run all five focused suites only with `bash scripts/run-go-tests-matched.sh <package-pattern> <anchored-test-regex> [go-test-args...]`; do not use a direct targeted Go selector or the obsolete separator form.
- Each suite proves dynamic behavior with resolver/guard/transport/executor/store spies; descriptor/source token presence alone is not acceptance evidence.
- Constructor registrar counts and per-effect guard counts are asserted separately: registration never occurs in dispatch/Search/Execute/request paths.
- Plan 03 is the sole later owner of `readiness-effect-surface.v1.json` and exact expected/static/runtime joining; Plan 06 remains runnable and green before that file exists.
- Evidence ceiling is E1 repository-local unit/fixture proof. This plan does not claim E2 container behavior, target/live evidence, profile parity, or commercial release readiness.
</verification>

<success_criteria>
- Every MCP, independent-registry, and web-search effect uses a freshly derived current CatalogSubject and shared current-generation Guard immediately before the downstream call.
- Every retry/fallback/new execution rechecks independently; no capability ID or generation is pinned across attempts.
- Every stable descriptor is resolved/registered once by its owner constructor; same-instance repeated effects do not re-register, while duplicate construction remains a hard failure.
- Caller/stale/ambiguous/deleted/runtime-mismatched capability selection and missing authority fail closed with zero downstream calls.
- MCP mutation/execution DTOs reject capabilityId and unknown fields before effects, while valid mutation and later execution perform independent server-side authorization.
- All five named suites are non-vacuous through the Phase 31 helper, and the plan remains within its exact nine-file/E1 boundary.
- Registry results are not substituted for Plan 10's live `ToolExecutor` builtin/custom HTTP/local Python/sandbox/MCP proof.
</success_criteria>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-06-SUMMARY.md` when done.
</output>
