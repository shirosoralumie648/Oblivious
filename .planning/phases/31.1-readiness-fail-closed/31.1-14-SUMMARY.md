---
phase: 31.1-readiness-fail-closed
plan: "14"
subsystem: api
tags: [readiness, agent, web-search, composition, effect-descriptors, tdd]

# Dependency graph
requires:
  - phase: 31.1-readiness-fail-closed
    provides: "RuntimeAuthorities, current-generation Guard, EffectRegistrar, and authority-aware Agent/web-search constructors"
provides:
  - "Strict BuildRuntime provider-before-executor Agent web-search composition"
  - "Exact scoped web-search ID-owner registration proof for Tavily, chain, and non-Tavily provider paths"
  - "Construction, denial, and package-global zero-transport proof with one-call current execution"
affects: [31.1-19 production effect registry, 31.1-20 phase closeout, RELS-01]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Construct authority-aware outbound dependencies before consumers clone or install effect implementations"
    - "Use a test-only recorder scoped to exact web-search ID-owner pairs while leaving global descriptor exactness to the production registry gate"

key-files:
  created:
    - src/server/internal/http/runtime_agent_web_search_test.go
  modified:
    - src/server/internal/http/server.go

key-decisions:
  - "Strict BuildRuntime constructs the authority-aware provider first and passes that exact instance through ToolRuntimeOptions.WebSearchProvider before NewServiceWithRuntimeOptions."
  - "The strict path no longer calls SetWebSearchProvider; the setter remains available only to compatibility and test construction."
  - "Plan 14 records only the exact web-search descriptor subset and ignores non-target descriptors; Plan 19 still owns global duplicate rejection and the complete exact join."

patterns-established:
  - "AST order proof and real BuildRuntime behavior proof jointly protect construction order without introducing production test seams."
  - "Selected provider alternatives are mutually exclusive exact ID-owner pairs; a valid ID with the wrong owner fails comparison."

requirements-completed: []

coverage:
  - id: D1
    description: "Strict BuildRuntime injects the authority-aware instance provider before authorized Agent ToolExecutor construction and never post-sets it."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/http/runtime_agent_web_search_test.go#TestBuildRuntimeAgentWebSearchCompositionContract"
        status: pass
      - kind: integration
        ref: "bash scripts/run-go-tests-matched.sh ./internal/http '^TestBuildRuntimeAgentWebSearchCompositionContract$|^TestWebSearchProviderFactoryAuthorityContract$'"
        status: pass
      - kind: integration
        ref: "go test -race ./internal/http -run '^(TestBuildRuntimeAgentWebSearchCompositionContract|TestWebSearchProviderFactoryAuthorityContract)$' -count=1"
        status: pass
    human_judgment: false
  - id: D2
    description: "Real strict composition registers exactly the selected web-search ID-owner subset and proves zero construction, denial, and global-provider calls with one current provider call."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/http/runtime_agent_web_search_test.go#TestBuildRuntimeAgentWebSearchCompositionContract/direct_Tavily"
        status: pass
      - kind: unit
        ref: "src/server/internal/http/runtime_agent_web_search_test.go#TestBuildRuntimeAgentWebSearchCompositionContract/provider_chain"
        status: pass
      - kind: unit
        ref: "src/server/internal/http/runtime_agent_web_search_test.go#TestBuildRuntimeAgentWebSearchCompositionContract/single_non-Tavily_provider"
        status: pass
      - kind: integration
        ref: "go test ./internal/http ./internal/agent ./internal/mcp -count=1"
        status: pass
    human_judgment: false

# Metrics
duration: 21 min
completed: 2026-07-19
status: complete
---

# Phase 31.1 Plan 14: Agent Web-Search Construction Order Summary

**Strict BuildRuntime now installs one authority-aware instance web-search provider during authorized ToolExecutor construction, with exact scoped descriptor and zero-call evidence across all supported factory shapes.**

## Performance

- **Duration:** 21 min
- **Started:** 2026-07-19T08:50:00Z
- **Completed:** 2026-07-19T09:11:03Z
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments

- Strict `BuildRuntime` constructs `buildAgentWebSearchProviderWithOptions` before the MCP client, Agent gateway, and `agent.NewServiceWithRuntimeOptions`, then passes the same instance in `ToolRuntimeOptions.WebSearchProvider`.
- The strict path no longer calls `SetWebSearchProvider`; compatibility `NewServer` behavior and resource cleanup remain unchanged.
- Real `BuildRuntime` construction for direct Tavily, `brave,duckduckgo` chain, and single Brave records `agent.tool.builtin -> agent.ToolExecutor.builtin`, `mcp.websearch.builtin -> mcp.WebSearchTool`, and exactly one selected provider pair once each.
- Every case records zero `agent.tool.web_search`, zero non-selected provider alternatives, zero construction transport, zero denied transport, zero package-global provider calls, and exactly one authorized provider transport.
- AST proof rejects late injection, a strict setter call, and independent Registry/WebsearchTool construction used only to pad effect coverage.

## Task Commits

The plan-required single implementation commit contains both task outcomes:

1. **Tasks 1-2: Inject and prove strict Agent web search** - `fa8cc8f` (`fix`)

## TDD Evidence

- **RED:** `TestBuildRuntimeAgentWebSearchCompositionContract` failed with `strict composition still calls SetWebSearchProvider at server.go:173`; its order report showed provider construction at `server.go:165`, no `WebSearchProvider` injection, and service construction at `server.go:160`.
- **GREEN:** After moving provider construction ahead of the runtime carrier and service, both exact anchored selectors passed. The composition table then passed for direct Tavily, provider chain, and single non-Tavily provider.
- The RED test was written and observed failing before production code changed. No separate RED commit was created because the plan's `<commit_policy>` requires one atomic `fix(31.1-14)` implementation commit.

## Descriptor and Call Evidence

| Configuration | Required selected provider pair | Counts | Calls |
|---|---|---|---|
| `tavily` | `mcp.websearch.tavily -> mcp.TavilyWebSearchProvider` | builtin `1`, MCP builtin `1`, selected `1`, alternatives `0`, `agent.tool.web_search` `0` | construction `0`, denied `0`, current `1`, global `0` |
| `brave,duckduckgo` | `mcp.websearch.chain -> mcp.websearch.Chain` | builtin `1`, MCP builtin `1`, selected `1`, alternatives `0`, `agent.tool.web_search` `0` | construction `0`, denied `0`, current `1`, global `0` |
| `brave` | `mcp.websearch.provider -> mcp.websearch.Provider` | builtin `1`, MCP builtin `1`, selected `1`, alternatives `0`, `agent.tool.web_search` `0` | construction `0`, denied `0`, current `1`, global `0` |

The recorder returns success for every non-target descriptor. It is test evidence for this subset only and does not claim full ToolExecutor ownership, global duplicate rejection, or complete runtime descriptor exactness.

## Files Created/Modified

- `src/server/internal/http/server.go` - Builds the strict provider before the Agent consumer and includes the exact instance in `ToolRuntimeOptions`.
- `src/server/internal/http/runtime_agent_web_search_test.go` - Real BuildRuntime, AST order, exact pair, wrong-owner mutation, provider-alternative, instance/global separation, and zero-call contracts.

## Decisions Made

- An authorized executor must receive the provider during construction because that is when it clones builtins and replaces legacy `web_search` with `mcp.NewWebSearchTool`.
- The strict path is instance-scoped. Package-global provider mutation remains absent, and the compatibility setter remains outside strict construction.
- Plan 14 deliberately does not create an `agent/tools.Registry` or independent `WebsearchTool`, does not install a global strict duplicate registrar, and does not claim full descriptor exactness. Those responsibilities remain with Plan 19.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The first RED invocation had two unused test imports; they were removed before accepting the RED run. The valid RED then failed only on the intended construction-order behavior.
- The first post-GREEN table used an absolute transport counter across sequential subtests; it was corrected to compare per-subtest deltas. No production behavior changed for this test-harness correction.

## User Setup Required

None - no external service configuration required.

## Known Stubs

None.

## Next Phase Readiness

- Plan 31.1-14 provides repository-local E1 evidence for CR-04; Plan 31.1-15 is next.
- Plan 19 still owns non-web-search duplicate ownership, the production strict registry, and the complete manifest/static/runtime/profile/authority exact join.
- `RELS-01` remains phase-level pending until all gap plans, phase verification, clean-HEAD Stage B, and the hard manual push gate complete.
- No Tavily-live, target/live Provider, payment, Kubernetes, or commercial-readiness claim is made.

## Test Evidence

- Both exact anchored selectors passed and matched exactly two concrete tests.
- `cd src/server && go test ./internal/http ./internal/agent ./internal/mcp -count=1` passed.
- Focused HTTP `-race` passed for both composition/factory selectors.
- `cd src/server && go vet ./internal/http ./internal/agent ./internal/mcp` passed.
- `git diff --check` passed before the implementation commit.

---
*Phase: 31.1-readiness-fail-closed*
*Plan: 14*
*Completed: 2026-07-19*

## Self-Check: PASSED

- SUMMARY exists at the declared output path and `verify-summary` reports `passed: true`.
- Implementation/test commit `fa8cc8f` exists and contains no deletions.
- Both anchored selectors, all three scoped packages, focused race, scoped `go vet`, and diff check pass.
- Coverage classification reports `2/2` deliverables automatically covered.
- Protected user changes remain unstaged; `RELS-01` remains phase-level pending.
