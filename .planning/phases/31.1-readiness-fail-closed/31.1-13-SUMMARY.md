---
phase: 31.1-readiness-fail-closed
plan: "13"
subsystem: api
tags: [readiness, admin, marketplace, financial, model-catalog, composition]

# Dependency graph
requires:
  - phase: 31.1-readiness-fail-closed
    provides: "Startup Guard, RuntimeAuthorities, EffectRegistrar, Admin model catalog readiness, and financial readiness carriers"
provides:
  - "Strict Router-owned Admin construction that rejects caller service injection and requires startup model authority"
  - "Denial-first Admin sync/detect/apply with current returned-model resolution before persistence"
  - "Strict Marketplace checkout construction and zero-effect denial before local order intent or Provider checkout"
affects: [31.1-20 phase closeout, RELS-01]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Strict production composition constructs authority-aware services internally; compatibility injection remains non-strict only"
    - "Separate one generic mutation admission check from current catalog subject checks at later read/write boundaries"
    - "Use one startup FinancialReadiness carrier for local settlement and HTTP Provider checkout owners"

key-files:
  created:
    - src/server/internal/http/router_readiness_composition_test.go
  modified:
    - src/server/internal/http/router.go
    - src/server/internal/http/marketplace_handler.go
    - src/server/internal/admin/service.go
    - src/server/internal/admin/channel_service.go
    - src/server/internal/admin/service_test.go

key-decisions:
  - "NewReadinessRouterWithOptions rejects any caller-supplied AdminService before internal dependency construction; only NewRouterWithOptions retains the compatibility injection seam."
  - "Each public Admin sync/detect/apply call performs the generic model-mutation guard exactly once before GetChannel or TestChannel, while model subjects are still resolved from current authorities after probes and before writes."
  - "Marketplace checkout rechecks the same startup FinancialReadiness before local order intent and again immediately before Provider checkout; strict construction rejects an invalid carrier."

patterns-established:
  - "Production-composition regressions combine dynamic zero-call spies with an AST assertion that production RouterOptions never sets AdminService."
  - "Readiness denial counters cover upstream reads, probes, persistence, local financial intent, Provider dispatch, session update, and failure-state mutation."

requirements-completed: []

coverage:
  - id: D1
    description: "Strict Router composition rejects injected Admin services and internally constructs exactly one readiness-aware Admin model mutation owner."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/http/router_readiness_composition_test.go#TestStrictRouterAdminReadinessCompositionContract"
        status: pass
      - kind: integration
        ref: "bash scripts/run-go-tests-matched.sh ./internal/http '^TestStrictRouterAdminReadinessCompositionContract$'"
        status: pass
      - kind: integration
        ref: "go test -race ./internal/http -run '^(TestStrictRouterAdminReadinessCompositionContract|TestStrictRouterMarketplaceReadinessCompositionContract)$' -count=1"
        status: pass
    human_judgment: false
  - id: D2
    description: "Admin model sync, detect, and apply deny before any channel read or probe and retain current model resolution before persistence."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/admin/service_test.go#TestModelCatalogMutationContract/readiness_denial_precedes_every_model_store_read_and_probe"
        status: pass
      - kind: integration
        ref: "bash scripts/run-go-tests-matched.sh ./internal/admin '^TestModelCatalogMutationContract$'"
        status: pass
      - kind: integration
        ref: "go test -race ./internal/admin -run '^TestModelCatalogMutationContract$' -count=1"
        status: pass
    human_judgment: false
  - id: D3
    description: "Strict Marketplace checkout requires financial readiness and denial leaves local order, Provider, session, and failed-state counters at zero."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/http/router_readiness_composition_test.go#TestStrictRouterMarketplaceReadinessCompositionContract"
        status: pass
      - kind: integration
        ref: "bash scripts/run-go-tests-matched.sh ./internal/http '^TestStrictRouterMarketplaceReadinessCompositionContract$'"
        status: pass
      - kind: integration
        ref: "cd src/server && go test ./internal/http ./internal/admin -count=1"
        status: pass
    human_judgment: false

# Metrics
duration: 25 min
completed: 2026-07-19
status: complete
---

# Phase 31.1 Plan 13: Strict Admin and Marketplace Readiness Composition Summary

**Strict Router composition now owns Admin model and Marketplace checkout authority, denying stale or incomplete readiness before reads, probes, local financial intent, persistence, or Provider effects.**

## Performance

- **Duration:** 25 min
- **Started:** 2026-07-19T07:04:28Z
- **Completed:** 2026-07-19T07:29:30Z
- **Tasks:** 2 completed
- **Files modified:** 6

## Accomplishments

- Strict Router construction rejects caller-supplied `AdminService`, creates Admin only through `admin.NewReadinessService`, and registers `admin.channel.model.mutation` exactly once from the startup carrier.
- Admin sync/detect/apply now perform one generic model-mutation admission check before their first store read or upstream probe, while returned and applied model subjects remain current-authority checked before persistence.
- Strict Marketplace construction installs checkout plus `withMarketplaceFinancialReadiness` together; denial occurs before local order creation, and a second current check remains immediately adjacent to Provider checkout.
- Production-composition tests prove missing carriers fail, non-test sources never set `RouterOptions.AdminService`, `BuildRuntime` reaches the strict Router entrypoint, and denied paths have zero downstream counters.

## Task Commits

The plan-specific commit policy required one atomic implementation commit:

1. **Tasks 1-2: Wire strict Admin and Marketplace readiness** - `b351005` (`fix`)

## TDD Evidence

- **Task 1 RED:** The Admin Router selector failed because a caller-supplied `AdminService` returned a non-nil handler with no error and `admin.channel.model.mutation` had zero registrations. The Admin mutation selector failed because denied sync still called `TestChannel` once, while denied detect/apply each called `GetChannel` and `TestChannel` once.
- **Task 1 GREEN:** Both anchored Admin selectors passed after strict construction and denial-first mutation guards were implemented.
- **Task 2 RED:** The Marketplace selector failed with `http.marketplace.checkout` registrations `0`, no strict constructor, and `createCalls=1` after readiness denial.
- **Task 2 GREEN:** The anchored Marketplace selector passed with one descriptor registration, construction-time invalid-carrier rejection, and zero local order/Provider/session/failure-state effects.
- RED and GREEN were run before the single plan-required implementation commit; no separate RED commit was created because `<commit_policy>` explicitly requires one atomic `fix(31.1-13)` commit.

## Files Created/Modified

- `src/server/internal/http/router_readiness_composition_test.go` - Strict carrier, injection rejection, AST production-path, descriptor, and denial zero-call contracts.
- `src/server/internal/http/router.go` - Rejects strict Admin injection, constructs readiness-aware Admin, shares one financial carrier, and uses the strict Marketplace constructor.
- `src/server/internal/http/marketplace_handler.go` - Adds fail-closed strict checkout construction and guards both local intent and Provider dispatch.
- `src/server/internal/admin/service.go` - Separates generic mutation admission from model-specific current catalog authorization.
- `src/server/internal/admin/channel_service.go` - Runs one generic guard per public mutation before read/probe and uses a checked private detect helper.
- `src/server/internal/admin/service_test.go` - Adds sync/detect/apply denial-first store/probe counters.

## Decisions Made

- Strict Router callers cannot supply an Admin service because a constructed service cannot prove it carries the current startup authority; compatibility tests continue through the non-strict entrypoint.
- Generic model mutation admission is distinct from model catalog authorization. The generic guard runs once at public entry, while every returned/persisted subject is still resolved from the current server catalog before a write.
- The Marketplace HTTP layer and SettlementService consume the same immutable startup carrier but guard their own effects independently; no readiness generation is pinned across local intent and Provider dispatch.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical Correctness] Added the service-owned generic/model guard split omitted from the frontmatter file inventory**

- **Found during:** Task 1 (Make strict Admin construction authority-required)
- **Issue:** The plan frontmatter `files_modified` list omitted `src/server/internal/admin/service.go`, but keeping `requireModels` responsible for both generic mutation admission and model-specific authorization would either run the generic guard more than once in Apply or prevent Sync/Detect/Apply from checking it before their first read/probe.
- **Fix:** Added private `requireMutation` ownership in `service.go` and narrowed `requireModels` to current model-subject resolution. Each public Sync/Detect/Apply path now runs the generic guard exactly once before `GetChannel`/`TestChannel`, while returned and applied models are still re-resolved before persistence.
- **Files modified:** `src/server/internal/admin/service.go`
- **Verification:** The anchored Admin mutation selector, strict Router selector, full scoped packages, and focused race tests pass; denial-first counters remain zero and valid model mutation coverage retains current subject checks.
- **Committed in:** `b351005`

**Total deviations:** 1 auto-fixed (Rule 2 missing critical correctness / plan file-inventory omission).
**Impact on plan:** Directly necessary to implement the specified exactly-once generic guard and write-before-current-model-resolution contract. No overbuild, new API, schema, dependency, or unrelated refactor was introduced.

## Issues Encountered

None. The user-owned `.planning/config.json` modification and untracked `.planning/intel/` were preserved and excluded from implementation commits. No signed-reconciliation behavior, public API, target environment, or push was changed.

## User Setup Required

None - no external service configuration required.

## Known Stubs

None.

## Next Phase Readiness

- Plan 31.1-13 provides repository-local E1 strict-composition and zero-call evidence for CR-02 and CR-03; Plan 31.1-14 is next.
- `RELS-01` remains phase-level pending until all gap plans, phase verification, exact clean-HEAD Stage B, and the manual push gate complete.
- No target/live payment, Provider, Kubernetes, or commercial-readiness claim is made.

## Test Evidence

- All three anchored selectors passed with exactly one concrete match each.
- `cd src/server && go test ./internal/http ./internal/admin -count=1` passed.
- Focused `go test -race` passed for both strict Router contracts and the Admin mutation contract.
- `cd src/server && go vet ./internal/http ./internal/admin` passed.
- `git diff --check` passed before the implementation commit.

---
*Phase: 31.1-readiness-fail-closed*
*Plan: 13*
*Completed: 2026-07-19*

## Self-Check: PASSED

- SUMMARY exists at the declared output path and `verify-summary` reports `passed: true`.
- Implementation commit `b351005` exists and contains six implementation files, including the documented necessary `service.go` deviation, with no deletions.
- All three anchored selectors, both scoped packages, focused race tests, and scoped `go vet` pass.
- Coverage classification reports `3/3` deliverables automatically covered.
- Protected user changes remain unstaged; `RELS-01` remains phase-level pending.
