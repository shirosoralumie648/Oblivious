---
phase: 31.1-readiness-fail-closed
plan: "17"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/internal/config/config.go
  - src/server/internal/config/config_test.go
  - src/server/cmd/server/main.go
  - src/server/cmd/server/main_test.go
  - src/server/cmd/server/readiness_probes.go
  - docker-compose.yml
  - deploy/kubernetes/configmap.yaml
  - scripts/verify-readiness-deployment-harness.sh
autonomous: true
gap_closure: true
requirements: [RELS-01]
must_haves:
  truths:
    - "Config retains a readiness-independent Redis endpoint and typed Kafka broker list even when Redis rate limiting is not selected; normal server startup builds PostgreSQL, Redis, Qdrant, ClickHouse, and Kafka probes from those deployment-owned values (D-RDY-07, D-RDY-09)."
    - "Every probe is deadline-bound, maps transport/config failure to sanitized blocked observations, and never returns DSN, credential, response body, or internal endpoint detail (D-RDY-02, D-RDY-09)."
    - "Canonical Compose starts/configures all authored monolith dependencies and the Kubernetes ConfigMap exposes matching deployment-owned addresses; Plan 22 owns the supported Kubernetes runner that must deploy and wait for Kafka before `/readyz` proof (D-RDY-23, D-RDY-24, D-RDY-25)."
    - "The disposable E2 harness proves healthy startup plus blocked/stale/restored transitions using the real probe wiring; Kubernetes apply/wait mutation proof is intentionally isolated in Plan 22 (D-RDY-26, D-RDY-27)."
  artifacts:
    - path: "src/server/internal/config/config.go"
      provides: "Readiness-capable Redis configuration plus typed Kafka broker configuration independent of optional feature toggles"
      contains: "KafkaBrokers"
    - path: "src/server/cmd/server/readiness_probes.go"
      provides: "Deployment-configured typed probes for all authored monolith dependencies"
      contains: "newDeploymentReadinessProbes"
    - path: "src/server/cmd/server/main_test.go"
      provides: "Config-to-probe success/failure/deadline/sanitization and startup-order tests"
      contains: "TestDeploymentOwnedReadinessProbeContract"
    - path: "docker-compose.yml"
      provides: "Default monolith dependency topology capable of healthy readiness"
      contains: "KAFKA_BROKERS"
    - path: "deploy/kubernetes/configmap.yaml"
      provides: "Kubernetes dependency addresses consumed by canonical app workload"
      contains: "KAFKA_BROKERS"
    - path: "scripts/verify-readiness-deployment-harness.sh"
      provides: "Disposable real-dependency healthy/blocked/stale/restored proof"
      contains: "readiness deployment harness passed"
  key_links:
    - from: "config.Load"
      to: "config.Config.RedisAddr/config.Config.KafkaBrokers"
      via: "REDIS_URL is parsed regardless of RELAY_RATE_LIMIT_BACKEND and KAFKA_BROKERS is trimmed/split/validated once"
      pattern: "REDIS_URL|KAFKA_BROKERS"
    - from: "runServerWithInputs"
      to: "newDeploymentReadinessProbes"
      via: "already-open DB and loaded config are passed into Manager before Bootstrap"
      pattern: "newStartupReadinessManager"
    - from: "docker-compose.yml/deploy/kubernetes/configmap.yaml"
      to: "readiness_probes.go"
      via: "the same real DATABASE/REDIS/QDRANT/CLICKHOUSE/KAFKA configuration drives dependency clients"
      pattern: "REDIS_URL|QDRANT_URL|CLICKHOUSE_DSN|KAFKA_BROKERS"
---

<objective>
Close CR-07 by replacing harness-only blocked/default probe construction with real deployment-owned typed dependency probes in the shipped monolith topology.

Purpose: Canonical Compose and Kubernetes must be capable of producing a healthy manager generation and `/readyz`, while missing or failing dependencies remain fail closed.
Output: Native probe factory, canonical dependency wiring, mutation tests, and updated E2 harness behavior.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-RESEARCH.md
@.planning/phases/31.1-readiness-fail-closed/31.1-PATTERNS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-VERIFICATION.md
@.planning/phases/31.1-readiness-fail-closed/31.1-08-SUMMARY.md
@src/server/cmd/server/main.go
@src/server/internal/config/config.go
@src/server/internal/config/config_test.go
@docker-compose.yml
@deploy/kubernetes/app-deployment.yaml
@deploy/kubernetes/configmap.yaml
@scripts/verify-readiness-deployment-harness.sh

<constraints>
- Probe subjects and endpoints come only from server/deployment configuration, never requests, audit files, report JSON, executable names, or a local fallback.
- Reuse the already-open PostgreSQL handle; other clients must be closed per probe attempt or owned by shutdown, with bounded contexts.
- Kafka is part of the authored monolith dependency set; canonical Compose must start it by default and Kubernetes must provide its broker address.
- Do not edit or claim `scripts/k8s-validate.sh`, `deploy/kubernetes/kafka.yaml`, or Kubernetes apply/wait proof here; Plan 22 owns that continuous gap slice after this plan publishes the address contract.
- Preserve `/livez` startup/liveness, `/readyz` readiness, explicit monolith profile, and output-only audit storage.
- CR-06/CR-09 remain Phase 34, WR-02 remains Phase 35, and OpenAPI Envelope parity remains Phase 31.2.
- Evidence remains repository-local E1/E2; no target Kubernetes or profile-parity claim.
</constraints>

<interfaces>
Add `KafkaBrokers []string` to `config.Config`. When `KAFKA_BROKERS` is present, parse it as a trimmed nonblank comma-separated endpoint list; absence remains typed empty configuration so the applicable probe factory can publish `dependency_unproven` rather than inventing a broker. Parse and retain `REDIS_URL` independently of `RELAY_RATE_LIMIT_BACKEND`; the backend switch decides rate-limiter use, not whether readiness can address Redis. Preserve explicit `REDIS_ADDR`/password/DB overrides and define their precedence in tests. Add a private `newDeploymentReadinessProbes(cfg config.Config, database *sql.DB) ([]releasecontract.Probe, error)` or equivalent typed factory returning exactly one probe for each applicable authored dependency: PostgreSQL `PingContext`, Redis `PING`, Qdrant bounded `/healthz`, ClickHouse `PingContext`, and Kafka broker metadata/dial. `newStartupReadinessManager` receives the loaded config and existing DB handle. Remove normal use of `OBLIVIOUS_READINESS_PROBE_BASE_URL`; the harness may fault a specific real dependency endpoint through deployment configuration, but may not replace the whole authority with a synthetic base service.
</interfaces>
</context>

## Artifacts this phase produces

- Native typed dependency probes and startup integration.
- Typed, deployment-owned Kafka broker and readiness-independent Redis endpoint loading.
- Canonical Compose/Kubernetes dependency addresses and Kafka availability.
- Config-to-probe and real-dependency harness coverage for each dependency row.
- E2 harness proof using shipped probe construction.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Load complete dependency endpoints and build typed probes</name>
  <files>src/server/internal/config/config.go, src/server/internal/config/config_test.go, src/server/cmd/server/main.go, src/server/cmd/server/main_test.go, src/server/cmd/server/readiness_probes.go</files>
  <read_first>
    - src/server/internal/config/config.go
    - src/server/internal/config/config_test.go
    - src/server/cmd/server/main.go
    - src/server/cmd/server/main_test.go
    - src/server/internal/releasecontract/readiness_manager.go
    - config/release/contract.v1.json
    - .planning/phases/31.1-readiness-fail-closed/31.1-REVIEW.md
  </read_first>
  <behavior>
    - `REDIS_URL` populates the typed Redis endpoint when the rate-limit backend is memory/none as well as redis; explicit Redis fields retain documented precedence.
    - `KAFKA_BROKERS` loads into a trimmed nonblank `[]string`, rejects empty members/malformed configuration, and is returned defensively.
    - One typed probe is constructed for postgres, redis, qdrant, clickhouse, and kafka from the loaded deployment config.
    - Each success yields `enabled`; missing/malformed config fails construction or yields typed blocked, never implicit enabled.
    - Timeout, auth, DNS, connection, non-2xx, and malformed responses expose only stable reason codes.
    - Excluded/non-applicable dependencies are never probed.
  </behavior>
  <action>Write config tests first. Add `KafkaBrokers []string`; parse a present `KAFKA_BROKERS` value into trimmed nonblank brokers and reject empty members/malformed lists with a stable configuration error, while an absent value remains an empty typed list. Parse `REDIS_URL` whenever present, regardless of `RELAY_RATE_LIMIT_BACKEND`, then apply explicit `REDIS_ADDR`, `REDIS_PASSWORD`, and `REDIS_DB` overrides in the existing precedence order. Include a memory-backend Redis URL case proving the endpoint survives solely for readiness, Kafka absent/multi-broker cases, invalid cases, and defensive-copy assertions. Then create typed probe implementations with explicit dependency IDs and sanitized observations. Use bounded `context.Context` for every network/database operation and close per-attempt clients/connections. Change manager construction to accept loaded config and the already-pinged database, compile only the authored applicable probes, and delete the startup path that turns absence of the harness base URL into blocked placeholder probes. Put table-driven fake-server/listener/DB probe tests in `main_test.go` so this plan stays within nine files; cover exact IDs, call counts, timeouts, configuration failures, and secret/endpoint absence. Update startup-order dependency injection tests for the new signature without relaxing preflight-before-DB or listener-before-background ordering.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/config '^TestLoadReadinessDependencyConfigContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./cmd/server '^TestDeploymentOwnedReadinessProbeContract$|^TestServerStartupOrderContract$'</automated>
  </verify>
  <acceptance_criteria>
    - The normal startup path has no dependency on `OBLIVIOUS_READINESS_PROBE_BASE_URL`.
    - Memory/none rate limiting does not erase a configured Redis readiness endpoint, and Kafka brokers are available as typed config.
    - All five authored dependency probes are exact, bounded, and sanitized.
    - Missing/failed configuration cannot produce an enabled observation.
    - Startup-order tests remain exact.
  </acceptance_criteria>
  <done>The shipped server can bootstrap readiness from its real dependency configuration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make Compose and deployment configuration capable of healthy readiness</name>
  <files>docker-compose.yml, deploy/kubernetes/configmap.yaml, scripts/verify-readiness-deployment-harness.sh</files>
  <read_first>
    - docker-compose.yml
    - deploy/kubernetes/app-deployment.yaml
    - deploy/kubernetes/configmap.yaml
    - deploy/kubernetes/postgres.yaml
    - deploy/kubernetes/redis.yaml
    - deploy/kubernetes/qdrant.yaml
    - deploy/kubernetes/clickhouse.yaml
    - deploy/kubernetes/kafka.yaml
    - scripts/verify-readiness-deployment-harness.sh
  </read_first>
  <behavior>
    - Default Compose starts all five dependencies, waits for health, and supplies their real configuration to `oblivious-server`.
    - Kubernetes ConfigMap/app workload supplies matching in-cluster addresses while retaining probe/audit semantics.
    - Compose and the Kubernetes ConfigMap name the same real Kafka service/address contract as the existing manifest.
    - The no-synthetic-authority Compose/harness path can reach `/readyz`; fault transitions use a selected real dependency endpoint and still prove blocked/stale/restored behavior.
  </behavior>
  <action>Make Kafka available in the default monolith Compose topology, add its health dependency and `KAFKA_BROKERS`, and verify existing PostgreSQL/Redis/Qdrant/ClickHouse values match the runtime factory. Add the broker value to Kubernetes ConfigMap; `app-deployment.yaml` and `k8s-validate.sh` are read-only because Plan 22 proves the `envFrom` link and actual Kafka apply/wait order. Update the harness to start the real Redis, Qdrant, ClickHouse, and Kafka dependencies alongside PostgreSQL and use the same typed config as canonical Compose for healthy startup. Inject deterministic blocked/structural behavior by redirecting only one selected real dependency endpoint or protocol response through that dependency's config; never install a second all-dependency authority.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./cmd/server '^TestDeploymentOwnedReadinessProbeContract$' &amp;&amp; bash scripts/verify-readiness-deployment-harness.sh</automated>
  </verify>
  <acceptance_criteria>
    - Compose and Kubernetes ConfigMap carry all five real dependency sources with no synthetic probe-base authority.
    - Harness and canonical Compose use shipped typed probes and can reach healthy `/readyz` when dependencies are healthy.
    - Audit remains output-only and no credential appears in evidence.
    - No Kubernetes runner or Kafka manifest file changes in this plan; Plan 22 owns supported apply/wait and mutation proof.
  </acceptance_criteria>
  <done>Canonical monolith assets can become ready without harness-only authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deployment configuration -> dependency clients | Addresses and credentials select real external services. |
| Dependency response -> readiness observation | Untrusted network/service responses influence capability availability. |
| Canonical assets -> orchestrator health | `/readyz` controls whether workloads receive traffic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-17-01 | Spoofing | Synthetic probe authority | high | mitigate | Remove all-dependency base URL from normal startup; use typed real clients and mutation rejection. |
| T-31.1-17-02 | Information Disclosure | Probe errors/audit | high | mitigate | Stable codes only; tests seed credentials/endpoints and assert absence. |
| T-31.1-17-03 | Denial of Service | Dependency probes | high | mitigate | Per-operation contexts, closed clients, and manager probe deadlines bound work. |
| T-31.1-17-04 | Tampering | Canonical deployment wiring | high | mitigate | Parse YAML and fail mutations for each dependency, profile, health split, and audit path. |
</threat_model>

<verification>
- Run the focused server selectors and disposable deployment harness.
- Run `cd src/server &amp;&amp; go test ./internal/config ./cmd/server ./internal/releasecontract -count=1`.
- Run `bash scripts/verify-readiness-contract-fixtures.sh` after the harness update.
- Run `git diff --check`.
</verification>

<success_criteria>
- Canonical deployment/probe gap is closed without audit/synthetic/local authority.
- Healthy, blocked, stale, and restored states remain evaluator-owned and repository-local.
</success_criteria>

<commit_policy>
Create one atomic commit: `fix(31.1-17): wire deployment-owned readiness probes`. Do not push. Plan 20 ends with a HARD EXTERNAL/MANUAL POST-CLOSEOUT GATE handoff; no GSD executor, verifier, tracker, hook, or autonomous orchestrator step owns final Stage B or push.
</commit_policy>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-17-SUMMARY.md` with dependency matrix, mutation counts, commands, and E1/E2 limits.
</output>
