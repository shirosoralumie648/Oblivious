---
phase: 31.1-readiness-fail-closed
plan: "21"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/internal/http/chat_handler.go
  - src/server/internal/http/chat_handler_test.go
autonomous: true
gap_closure: true
requirements: [RELS-01]
must_haves:
  truths:
    - "Strict Chat mutation decoding accepts exactly one JSON object followed only by whitespace and EOF; a second object, scalar, array, or malformed trailing token is rejected (D-RDY-12, D-RDY-20)."
    - "Trailing input is rejected before model/catalog resolution, store mutation, Relay dispatch, usage, or any other business call (D-RDY-12, D-RDY-14, D-RDY-22)."
    - "Unknown-field rejection and the existing sanitized invalid-request envelope remain unchanged while the decoder becomes a strict single-document boundary (D-RDY-17)."
  artifacts:
    - path: "src/server/internal/http/chat_handler.go"
      provides: "EOF-enforcing strict Chat mutation decoder"
      contains: "decodeStrictChatMutation"
    - path: "src/server/internal/http/chat_handler_test.go"
      provides: "Trailing-value matrix and zero-business-call regressions"
      contains: "TestChatMutationStrictDecoderContract"
  key_links:
    - from: "decodeStrictChatMutation"
      to: "Chat mutation handlers"
      via: "a second Decode must return io.EOF before the validated request reaches any business dependency"
      pattern: "io.EOF"
---

<objective>
Close WR-04 in its own bounded slice by making Chat mutation decoding accept exactly one JSON document and proving trailing input cannot reach business effects.

Purpose: Unknown-field rejection is incomplete if a valid first object can be followed by another JSON value that bypasses the validated request boundary.
Output: EOF-enforcing decoder behavior and table-driven zero-call regressions.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-VERIFICATION.md
@.planning/phases/31.1-readiness-fail-closed/31.1-REVIEW.md
@src/server/internal/http/chat_handler.go
@src/server/internal/http/chat_handler_test.go

<constraints>
- Preserve the existing request structs, unknown-field rejection, response envelope, status code, and public route behavior.
- Accept only one valid object plus optional JSON whitespace; no alternate content type, array wrapper, or multi-document mode is introduced.
- CR-06/CR-09 remain Phase 34, WR-02 remains Phase 35, and the two readiness OpenAPI Envelope definitions remain Phase 31.2.
- Evidence is repository-local E1 only.
</constraints>

<interfaces>
Keep `decodeStrictChatMutation(r, target)` and its callers. After `DisallowUnknownFields` and the first target decode, perform a second decode into a throwaway value and accept only `io.EOF`. Any decoded value or non-EOF error is the existing invalid-request result, before the handler invokes model, store, Relay, usage, or mutation dependencies.
</interfaces>
</context>

## Artifacts this phase produces

- Strict single-document Chat mutation decoder.
- Concatenated object/scalar/array/token regression matrix.
- Pre-business zero-call proof for every rejected trailing-input class.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Require EOF after one strict Chat mutation object</name>
  <files>src/server/internal/http/chat_handler.go, src/server/internal/http/chat_handler_test.go</files>
  <read_first>
    - src/server/internal/http/chat_handler.go
    - src/server/internal/http/chat_handler_test.go
    - src/server/internal/http/mcp_handler.go
    - .planning/phases/31.1-readiness-fail-closed/31.1-REVIEW.md
  </read_first>
  <behavior>
    - One valid object plus spaces, tabs, and newlines is accepted.
    - Object plus object, scalar, array, `null`, or malformed trailing token is rejected.
    - Existing unknown-field and malformed-first-object cases retain their exact status and stable error code.
  </behavior>
  <action>Write the trailing-value table first. After the first strict decode succeeds, decode once more into a throwaway value and accept only `io.EOF`; translate a second value or any other result into the existing invalid-request response. Do not use body-length checks or ad hoc string scanning because JSON whitespace and decoder buffering must remain standards-compliant.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestChatMutationStrictDecoderContract$'</automated>
  </verify>
  <acceptance_criteria>
    - Every trailing JSON/value/token variant is rejected.
    - A single valid object with trailing whitespace still succeeds.
    - Unknown-field and first-document error contracts do not change.
  </acceptance_criteria>
  <done>Chat mutation decoding accepts one complete JSON document and no trailing value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove trailing input is denied before every business dependency</name>
  <files>src/server/internal/http/chat_handler_test.go</files>
  <read_first>
    - src/server/internal/http/chat_handler_test.go
    - src/server/internal/http/chat_handler.go
    - src/server/internal/chat/service.go
  </read_first>
  <behavior>
    - Each trailing-input case leaves model resolution, conversation/message store, Relay transport, and usage counters at zero.
    - Rejection returns only the stable sanitized invalid-request envelope and never echoes the trailing payload.
    - The positive single-object case reaches the expected business dependency once, proving the counters are live.
  </behavior>
  <action>Exercise the real Chat mutation handler with counter-bearing service dependencies for every trailing-input class from Task 1. Assert all model/catalog, read/write, Relay, and usage counters remain zero and seed a secret-like trailing value to prove it is not reflected. Include one valid control request whose expected counter increments, so zero-call assertions cannot pass with disconnected spies.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestChatMutationStrictDecoderZeroCallContract$'</automated>
  </verify>
  <acceptance_criteria>
    - All rejected trailing inputs produce exact zero business/effect counters.
    - The positive control proves the counters observe a real handler call.
    - Response text contains no seeded trailing payload or secret-like value.
  </acceptance_criteria>
  <done>Trailing JSON cannot reach Chat catalog, persistence, Relay, or usage effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted HTTP body -> Chat mutation DTO | Multiple JSON values can smuggle data beyond the validated first object. |
| Validated DTO -> Chat business dependencies | Decoder success controls access to catalog, persistence, Relay, and usage effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-21-01 | Tampering | Chat JSON document boundary | high | mitigate | Require second decode to return EOF and reject every second-value class. |
| T-31.1-21-02 | Elevation of Privilege | Chat business dispatch | high | mitigate | Counter-backed handler tests prove trailing input is rejected before catalog/store/Relay/usage calls. |
| T-31.1-21-03 | Information Disclosure | Invalid-request response | high | mitigate | Reuse sanitized envelope and assert seeded trailing secrets are not reflected. |
</threat_model>

<verification>
- Run both focused HTTP selectors and `cd src/server &amp;&amp; go test ./internal/http -count=1`.
- Run `git diff --check` and inspect only the two owned Chat files.
</verification>

<success_criteria>
- WR-04 is closed with strict JSON/EOF and live zero-call evidence.
- No public request/response schema, route, or deferred OpenAPI ownership changes.
</success_criteria>

<commit_policy>
Create one atomic commit: `fix(31.1-21): enforce single chat json document`. Do not push. Plan 20 ends with a HARD EXTERNAL/MANUAL POST-CLOSEOUT GATE handoff; no GSD executor, verifier, tracker, hook, or autonomous orchestrator step owns final Stage B or push.
</commit_policy>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-21-SUMMARY.md` with the trailing-input matrix, zero-call counters, commands, and repository-local claim limit.
</output>
