---
phase: 31.1-readiness-fail-closed
plan: "05"
type: execute
wave: 2
depends_on: ["31.1-01"]
files_modified:
  - src/server/internal/relay/router.go
  - src/server/internal/relay/router_test.go
  - src/server/internal/chat/relay_gateway.go
  - src/server/internal/chat/relay_gateway_test.go
  - src/server/internal/http/chat_handler.go
  - src/server/internal/http/chat_handler_test.go
  - src/web/src/types/api.ts
  - src/server/internal/admin/channel_service.go
  - src/server/internal/admin/service_test.go
autonomous: true
requirements: [RELS-01]
must_haves:
  truths:
    - "Relay 每个 Provider/model retry、fallback 与 batch-independent dispatch 都按 current catalog/current generation 重新授权；先前 ingress/attempt 不授权下一 attempt（D-RDY-14, D-RDY-16, D-RDY-21）。"
    - "Chat Relay HTTP/stream/fallback dispatch 在实际 Do/本地 fallback 前 guard，denial 不产生网络或 fallback call（D-RDY-11, D-RDY-14）。"
    - "model/routing mutation 只接收业务 subject 并明确拒绝 capabilityId；Chat response JSON 与 TypeScript `ModelOption` 精确为 `{id,label,capabilityId}`，其中 capabilityId 只由 handler 的 current server catalog mapping 派生，caller/stale persisted value 不可信（D-RDY-20, D-RDY-21, D-RDY-22）。"
  artifacts:
    - path: "src/server/internal/relay/router.go"
      provides: "Per-attempt model/catalog and Provider transport guards"
    - path: "src/server/internal/chat/relay_gateway.go"
      provides: "Guarded Chat Relay and fallback transport"
    - path: "src/server/internal/http/chat_handler.go"
      provides: "Strict model mutation request and handler-owned server-derived ModelOption response DTO"
    - path: "src/web/src/types/api.ts"
      provides: "Exact read-only ModelOption capability identity consumed by Phase 31.2"
      contains: "capabilityId"
    - path: "src/server/internal/admin/channel_service.go"
      provides: "Catalog-authorized channel model/routing target mutation"
  key_links:
    - from: "src/server/internal/relay/router.go"
      to: "src/server/internal/releasecontract/catalog.go"
      via: "ResolveAndRequire before every model attempt"
      pattern: "ResolveAndRequire"
    - from: "src/server/internal/chat/relay_gateway.go"
      to: "src/server/internal/releasecontract/readiness.go"
      via: "BoundaryOutbound immediately before HTTP client/fallback"
      pattern: "BoundaryOutbound"
---

<objective>
在 Relay/Chat 最终 dispatch 与 model/routing mutation 处持续重解析 authored catalog 并读取 current readiness，证明 retry/fallback 不能沿用旧授权。

Purpose: 关闭最昂贵的 Provider/model dispatch 绕过点与 caller-selected capability escalation。
Output: per-attempt Relay/Chat guards、严格 model DTO、server-derived capability metadata 与 mutation/execution tests。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-01-SUMMARY.md
@src/server/internal/relay/router.go
@src/server/internal/chat/relay_gateway.go
@src/server/internal/http/chat_handler.go
@src/web/src/types/api.ts
@src/server/internal/admin/channel_service.go

<interfaces>
Use the exact startup-built Plan 31.1-01 carrier:

```go
type EffectID string
type CapabilityBindings struct { /* private immutable lookup */ }
func (b CapabilityBindings) Resolve(effect EffectID) (CapabilityID, error)
type RuntimeAuthorities struct {
    CatalogAuthorizer CatalogAuthorizer
    CapabilityBindings CapabilityBindings
}
func NewRuntimeAuthorities(contract AuthoredContractV1, profile DeploymentProfile, guard Guard) (RuntimeAuthorities, error)
```

Dynamic models use `Authorities.CatalogAuthorizer.ResolveAndRequire(ctx, CatalogSubject{Kind,ID,Runtime}, Boundary)`. Provider/Chat transport IDs come only from `Authorities.CapabilityBindings.Resolve(typedEffectID)` and the same Guard. No consumer reloads contract/profile, accepts caller/persisted capability, hard-codes an ID, reads global authority, or installs a local fallback.

The exact downstream response contract is `ModelOption{id:string,label:string,capabilityId:string}` in JSON and TypeScript. Chat create/update mutation DTOs contain business fields such as `modelId` only and reject an input member named `capabilityId`. Implement this as a `chat_handler.go` response mapping over the existing domain model plus current server catalog resolution; do not add or modify `src/server/internal/chat/config.go` to store capability authority.
</interfaces>
</context>

## Artifacts this phase produces

- Modified files: the nine source/test/type paths in frontmatter.
- New constructor options: `relay.Router` and `chat.RelayGateway` receive `Guard`, the exact `RuntimeAuthorities`, and `EffectRegistrar`; catalog and Provider/Chat IDs come only from its two immutable fields.
- Response DTOs gain read-only `capabilityId`; request decoders reject a `capabilityId` member instead of ignoring it.
- New suites: `TestRelayReadinessPerAttemptContract`, `TestChatRelayReadinessDispatchContract`, `TestModelCatalogMutationContract`.

<tasks>

<task type="auto">
  <name>Task 1: Re-authorize every Relay Provider and model attempt</name>
  <read_first>
    - src/server/internal/relay/router.go
    - src/server/internal/relay/router_test.go
    - src/server/internal/relay/types/types.go
    - src/server/internal/releasecontract/catalog.go
    - src/server/internal/releasecontract/readiness.go
  </read_first>
  <files>src/server/internal/relay/router.go, src/server/internal/relay/router_test.go</files>
  <behavior>
    - Test V31.1-R06: disabled/blocked/stale/unknown/identity mismatch before first attempt makes provider/quota/usage/settlement call counts zero.
    - Test V31.1-R06/T-RDY-03: first retryable attempt may run, then fake-clock expiry makes the next model/provider fallback call count zero.
    - Test V31.1-R06/R07: stale persisted capability and catalog runtime mismatch are ignored/rejected; current model subject is resolved for every attempt.
  </behavior>
  <action>Inject manager-backed Guard, the startup RuntimeAuthorities and descriptors into Router constructors; zero/missing authorities or typed Provider binding fail construction. In `Route`, `RouteWithFallback`, and every `RouteWithBilling` loop iteration, construct the current model subject from trusted method input/current route data, call `Authorities.CatalogAuthorizer.ResolveAndRequire`, resolve the typed Provider transport through `Authorities.CapabilityBindings`, then separately guard it immediately before the supplied `fn(ch)`. Perform checks before quota preconsume, pending usage, or state implying dispatch; denial creates only sanitized denial audit. Retry/fallback loops repeat both checks after sleep and cannot retain capability/generation. Preserve safe refund/release only for effects already started. Composition tests prove no Router contract/profile reload, hard-coded ID, global or local fallback. Only after the production changes above are complete, add and run `TestRelayReadinessPerAttemptContract` as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Per-attempt fake-clock test proves a later retry/fallback is blocked with no second provider/quota-preauth dispatch.
    - Initial denial creates no provider, quota, usage-success, settlement, affinity, or rate-limit begin effects.
    - Existing Relay billing/retry behavior remains passing when every current check authorizes.
  </acceptance_criteria>
  <verify><automated>bash scripts/run-go-tests-matched.sh ./internal/relay '^TestRelayReadinessPerAttemptContract$' &amp;&amp; git diff --check</automated></verify>
  <done>V31.1-R06/V31.1-R07 and T-RDY-03/T-RDY-07 close every Relay attempt boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Guard Chat Relay HTTP, streaming and fallback dispatch</name>
  <read_first>
    - src/server/internal/chat/relay_gateway.go
    - src/server/internal/chat/relay_gateway_test.go
    - src/server/internal/relay/router.go
    - src/server/internal/releasecontract/catalog.go
  </read_first>
  <files>src/server/internal/chat/relay_gateway.go, src/server/internal/chat/relay_gateway_test.go</files>
  <behavior>
    - Test V31.1-R06: complete/stream/structured paths resolve current model and guard immediately before HTTP Client.Do.
    - Test V31.1-R06: primary error followed by expiry blocks CompositeGateway fallback with fallback counter zero.
    - Test T-RDY-03/T-RDY-07: unknown model/runtime mismatch/caller capability data produces stable denial and no network call.
  </behavior>
  <action>Add the exact RuntimeAuthorities/Guard/EffectRegistrar options to RelayGateway and CompositeGateway. Resolve `ConversationConfig.ModelID` through `Authorities.CatalogAuthorizer`, resolve typed Chat transport via `Authorities.CapabilityBindings`, and guard immediately before each non-streaming/streaming `httpClient.Do`; structured replies use the same path. Before local fallback invocation, re-resolve and re-check current generation. Return stable errors without Relay URL, headers, credentials, or probe details. Register descriptors from carrier bindings during construction; missing carrier/binding fails rather than using a hard-coded/global/deny-all substitute. Only after the production changes above are complete, add and run `TestChatRelayReadinessDispatchContract` as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - All three transport modes and fallback have independent zero-call spies for every denial class.
    - Expiry between primary failure and fallback blocks fallback; cancellation remains current behavior and cannot authorize another attempt.
    - No direct-provider development fallback bypasses a denied guard.
  </acceptance_criteria>
  <verify><automated>bash scripts/run-go-tests-matched.sh ./internal/chat '^TestChatRelayReadinessDispatchContract$' &amp;&amp; git diff --check</automated></verify>
  <done>V31.1-R06 and T-RDY-03/T-RDY-07 prove Chat transport/fallback current authorization.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce server-derived model and routing mutation authority</name>
  <read_first>
    - src/server/internal/http/chat_handler.go
    - src/server/internal/http/chat_handler_test.go
    - src/server/internal/admin/channel_service.go
    - src/server/internal/admin/service_test.go
    - src/server/internal/releasecontract/catalog.go
  </read_first>
  <files>src/server/internal/http/chat_handler.go, src/server/internal/http/chat_handler_test.go, src/web/src/types/api.ts, src/server/internal/admin/channel_service.go, src/server/internal/admin/service_test.go</files>
  <behavior>
    - Test V31.1-R07: Chat config request containing capabilityId is rejected; valid model mutation resolves/guards before store and response JSON plus TypeScript `ModelOption` expose the exact read-only `{id,label,capabilityId}` contract.
    - Test V31.1-R07: channel model sync/apply rejects unbound/ambiguous models before persistence and enriches safe inventory from current catalog.
    - Test T-RDY-07: old persisted capability metadata or UI selection cannot override current catalog on later Relay execution.
  </behavior>
  <action>Make Chat create/update decoding strict for caller capability members, resolve submitted `modelId` only through injected `Authorities.CatalogAuthorizer` and guard `BoundaryHTTP` before service/store mutation. In `chat_handler.go`, map the existing domain model to a response-only DTO whose serialized fields are exactly `id`, `label`, and server-derived `capabilityId`; update `src/web/src/types/api.ts` so `ModelOption` has the same required camel-case field. Do not add capability state to `chat/config.go`, accept it in a mutation DTO, or derive it from model labels/IDs in TypeScript. In Admin channel model sync/detect/apply, use the same carrier before persistence; reject unknown/ambiguous entries atomically, never save a resolved verdict, and derive response metadata after save. Execution remains Task 1/2 current re-resolution. Tests reject any local Load/ResolveCommittedProfile call, hard-coded ID, global authority or missing-carrier fallback. Only after the production changes above are complete, add and run `TestModelCatalogMutationContract` in both packages as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Caller capability injection, unknown/ambiguous models and runtime mismatch produce zero store writes.
    - Valid response capability IDs equal current server resolution, serialize as `capabilityId`, compile against the exact TypeScript `ModelOption`, and are never accepted on create/update.
    - Mutation and later execution tests demonstrate two independent resolver/guard calls.
  </acceptance_criteria>
  <verify><automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestModelCatalogMutationContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./internal/admin '^TestModelCatalogMutationContract$' &amp;&amp; pnpm --dir src/web exec tsc --noEmit &amp;&amp; git diff --check</automated></verify>
  <done>V31.1-R07 and T-RDY-07 enforce server-derived model/routing authority at mutation and execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| model/routing request -> persistence | Caller-selected subjects must resolve through authored catalog. |
| Relay/Chat attempt -> Provider transport | Every retry/fallback may occur under a newer generation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-RDY-03 | Elevation of Privilege | retry/fallback | high | mitigate | Per-attempt current checks and fake-clock zero-call tests. |
| T-RDY-07 | Tampering | model/capability selection | high | mitigate | Strict requests, current catalog resolution at mutation/execution, no persisted verdict. |
| T-RDY-06 | Information Disclosure | readiness transport errors | medium | mitigate | Stable sanitized codes without URL/header/probe detail. |
</threat_model>

<verification>
- Each behavior suite runs via the Phase 31 helper with an anchored top-level selector.
- Provider/model authorization is dynamic behavior tested with spies and fake time; static scans are not acceptance evidence.
</verification>

<success_criteria>
- Every Relay/Chat attempt re-resolves catalog and current readiness before effects.
- Model/routing mutations reject caller capability authority and unknown bindings before writes.
- Response capability metadata is read-only and cannot authorize later execution.
- Relay, Chat and mutation constructors receive the same startup RuntimeAuthorities and expose no parallel authority path.
</success_criteria>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-05-SUMMARY.md` when done.
</output>
