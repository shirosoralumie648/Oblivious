---
phase: 31.1-readiness-fail-closed
plan: "09"
subsystem: standalone-entrypoints
tags: [go, readiness, preflight, fail-closed, entrypoint]
requires:
  - phase: 31.1-readiness-fail-closed
    provides: "Plan 01 Runtime readiness authority and Plan 02 immutable RunEntrypoint handoff"
provides:
  - "Source-authored preflight wiring for admin, gateway, marketplace, observability, rag, relay, task, and workflow roots"
  - "Dynamic zero-effect and immutable handoff suites for all eight remaining standalone roots"
  - "AST first-call/literal binding proof for every live standalone root"
affects: [31.1-03, 31.1-08, release-readiness]
requirements-completed: []
coverage:
  - id: D1
    description: "Each remaining root submits a literal EntrypointID to the shared runner before configuration, database, provider, listener, worker, or descriptor effects."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/cmd/server/standalone_preflight_test.go#TestStandaloneProfilePreflightGroupTwoContract"
        status: pass
      - kind: unit
        ref: "src/server/cmd/server/standalone_preflight_test.go#TestStandaloneProfilePreflightEffectfulRootsContract"
        status: pass
    human_judgment: false
  - id: D2
    description: "Generic committed monolith profile resolution cannot authorize an excluded standalone root; continuation, dependency, worker and descriptor spies remain zero."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "bash scripts/run-go-tests-matched.sh ./cmd/server '^TestStandaloneProfilePreflightGroupTwoContract$'"
        status: pass
      - kind: unit
        ref: "bash scripts/run-go-tests-matched.sh ./cmd/server '^TestStandaloneProfilePreflightEffectfulRootsContract$'"
        status: pass
    human_judgment: false
  - id: D3
    description: "Authorized fixtures resolve contract, profile and trusted identity once, hand off defensive copies, and reject identity/profile substitution before continuation."
    requirement: RELS-01
    verification:
      - kind: integration
        ref: "cd src/server && go test ./... -count=1"
        status: pass
      - kind: other
        ref: "git diff --check HEAD~2..HEAD"
        status: pass
    human_judgment: false
duration: 20 min
completed: 2026-07-18
status: complete
---

# Phase 31.1 Plan 09: Standalone Entrypoint Preflight

All eight remaining standalone composition roots now use the Plan 02 shared `RunEntrypoint` contract. Their existing configuration, database, listener, gRPC/HTTP, provider, Relay health, batch/schedule worker, and goroutine startup remains inside the authorized continuation and cannot run when the committed monolith profile excludes the source-authored root ID.

## Task Commits

1. **Task 1: Preflight Admin, Gateway, Marketplace and Observability roots** - `db7607a`
2. **Task 2: Preflight RAG, Relay, Task and Workflow effectful roots** - `ea3077c`

## Accomplishments

- Added exact source literals `admin`, `gateway`, `marketplace`, `observability`, `rag`, `relay`, `task`, and `workflow` as the first fallible statement in their live `main` functions.
- Passed the immutable `ResolvedEntrypointInputs` handoff through one continuation without adding a second resolver, environment/argv/executable identity source, audit fallback, profile default, or runtime authority.
- Added `TestStandaloneProfilePreflightGroupTwoContract` and `TestStandaloneProfilePreflightEffectfulRootsContract`. Each suite covers successful one-time contract/profile/identity resolution, defensive-copy handoff, profile/identity denial, committed-monolith exact-ID exclusion, zero continuation/dependency/worker/descriptor effects, and AST literal/first-call binding.

## Verification

- `bash scripts/run-go-tests-matched.sh ./cmd/server '^TestStandaloneProfilePreflightGroupTwoContract$'` passed; matched 1 top-level test.
- `bash scripts/run-go-tests-matched.sh ./cmd/server '^TestStandaloneProfilePreflightEffectfulRootsContract$'` passed; matched 1 top-level test.
- `cd src/server && go test ./... -count=1` passed for the full Go repository.
- `git diff --check` passed.

## Boundary and Residual Risk

- Evidence is E1 repository-local automated proof only. No non-monolith target/live deployment, provider, database, Docker, or commercial-readiness claim is made.
- The authored committed monolith profile intentionally excludes these standalone IDs; an allowed standalone profile fixture is exercised only in-memory by the tests and is not runtime release evidence.
- Plan 03 still owns the expected/static/runtime effect inventory and exact-join classification for excluded roots; this plan registers no runtime descriptors before successful preflight.

## Self-Check: PASSED

- Task commits `db7607a` and `ea3077c` are present.
- Plan-owned files are committed; unrelated `.planning/config.json` remains the sole unstaged user change.
- Focused selectors, full Go regression, and diff checks all passed with zero skips or failures.

---
*Phase: 31.1-readiness-fail-closed*
*Completed: 2026-07-18*
