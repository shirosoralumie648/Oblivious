---
phase: 31.1-readiness-fail-closed
plan: "14"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/internal/http/server.go
  - src/server/internal/http/runtime_agent_web_search_test.go
  - src/server/internal/http/agent_web_search_test.go
autonomous: true
gap_closure: true
requirements: [RELS-01]
must_haves:
  truths:
    - "Strict BuildRuntime constructs the authority-aware instance-scoped web-search provider before `NewServiceWithRuntimeOptions`/`NewAuthorizedToolExecutor` (D-RDY-11, D-RDY-14)."
    - "`ToolRuntimeOptions.WebSearchProvider` is populated at authorized executor construction, so the executor installs `mcp.NewWebSearchTool` rather than retaining the legacy global builtin (D-RDY-20, D-RDY-21)."
    - "The scoped BuildRuntime web-search recorder observes the exact ID-owner pairs `agent.tool.builtin` -> `agent.ToolExecutor.builtin`, `mcp.websearch.builtin` -> `mcp.WebSearchTool`, and exactly one selected provider alternative: `mcp.websearch.tavily` -> `mcp.TavilyWebSearchProvider`, `mcp.websearch.chain` -> `mcp.websearch.Chain`, or `mcp.websearch.provider` -> `mcp.websearch.Provider`; wrong owners and `agent.tool.web_search` are rejected (D-RDY-11, D-RDY-14, D-RDY-22)."
    - "The web-search construction path leaves non-web-search ToolExecutor behavior untouched; global descriptor ownership and complete exactness are verified later by Plan 19 (D-RDY-11)."
    - "Wave 1 web-search composition checks do not enforce global duplicate ownership or the complete exact join; those remain the strict production registry and gate responsibility of Plan 19 (D-RDY-11)."
  artifacts:
    - path: "src/server/internal/http/server.go"
      provides: "Provider-before-executor BuildRuntime composition"
      contains: "WebSearchProvider"
    - path: "src/server/internal/http/runtime_agent_web_search_test.go"
      provides: "Real BuildRuntime registration/order/zero-transport proof"
      contains: "TestBuildRuntimeAgentWebSearchCompositionContract"
  key_links:
    - from: "buildAgentWebSearchProviderWithOptions"
      to: "agent.NewServiceWithRuntimeOptions"
      via: "provider is included in ToolRuntimeOptions before authorized executor construction"
      pattern: "WebSearchProvider"
    - from: "NewAuthorizedToolExecutor"
      to: "mcp.NewWebSearchTool"
      via: "non-nil instance provider installs the guarded builtin at construction"
      pattern: "NewWebSearchTool"
---

<objective>
Close CR-04 by correcting strict BuildRuntime construction order for Agent web search.

Purpose: Setting a provider after the authorized executor clones builtins cannot install the guarded instance-scoped web-search tool.
Output: Provider-before-executor wiring and production composition evidence.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/31.1-readiness-fail-closed/31.1-CONTEXT.md
@.planning/phases/31.1-readiness-fail-closed/31.1-VERIFICATION.md
@.planning/phases/31.1-readiness-fail-closed/31.1-10-SUMMARY.md
@src/server/internal/http/server.go
@src/server/internal/http/agent_web_search.go
@src/server/internal/http/agent_web_search_test.go
@src/server/internal/agent/executor.go
@src/server/internal/agent/service.go

<constraints>
- Do not restore package-global provider swapping in strict production composition.
- BuildRuntime remains side-effect-free: no provider HTTP request, worker, or health loop during construction.
- Do not construct `agent/tools.WebsearchTool`, `agent/tools.Registry`, or any other independent consumer merely to add `agent.tool.web_search` or Registry descriptors to the production snapshot; no production owner exists on the inspected BuildRuntime path.
- Do not wrap the complete BuildRuntime descriptor stream in a strict duplicate-rejecting registrar in this plan: use a recorder scoped to the web-search subset so existing non-web-search Chat duplicate ownership can be repaired by Plan 19. Plan 19 remains responsible for global duplicate rejection and the complete manifest/static/runtime/profile/authority exact join.
- CR-06/CR-09 remain Phase 34, WR-02 remains Phase 35, and readiness OpenAPI Envelope parity remains Phase 31.2.
- Evidence is repository-local E1; no Tavily/live-provider claim.
</constraints>

<interfaces>
Use the existing `mcp.WebSearchRuntimeOptions` and `agent.ToolRuntimeOptions.WebSearchProvider`. The strict path must build the provider with the same Guard/Authorities/Effects, then pass that exact instance into `agent.NewServiceWithRuntimeOptions`. `SetWebSearchProvider` may remain only for compatibility/test paths and cannot be the strict composition mechanism.

Descriptor expectations must be derived from the selected production factory path as exact `(ID, Owner)` pairs, not a synthetic union or an ID-only set. The live subset asserted for web search is:

- `agent.tool.builtin` -> `agent.ToolExecutor.builtin`
- `mcp.websearch.builtin` -> `mcp.WebSearchTool`
- exactly one provider alternative: `mcp.websearch.tavily` -> `mcp.TavilyWebSearchProvider`, `mcp.websearch.chain` -> `mcp.websearch.Chain`, or `mcp.websearch.provider` -> `mcp.websearch.Provider`

This is a subset of the complete runtime snapshot, not the complete ToolExecutor descriptor set. The existing non-web-search pairs `agent.tool.custom_api_http` -> `agent.ToolExecutor.custom_api_http`, `agent.tool.local_python` -> `agent.ToolExecutor.local_python`, `agent.tool.python_sandbox` -> `agent.ToolExecutor.python_sandbox`, and `agent.tool.mcp` -> `agent.ToolExecutor.mcp` remain registered under a complete strict authority configuration; Plan14 leaves that path untouched and Plan19 owns its exact count/ownership assertion. `agent.tool.web_search` belongs to the separate `agent/tools.WebsearchTool` constructor and is not expected from BuildRuntime.
</interfaces>
</context>

## Artifacts this phase produces

- Correct strict Agent web-search construction order.
- Exact web-search descriptor subset registration proof from real BuildRuntime.
- No-transport construction and denied-provider zero-call evidence.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Construct the provider before the authorized executor</name>
  <files>src/server/internal/http/server.go, src/server/internal/http/runtime_agent_web_search_test.go</files>
  <read_first>
    - src/server/internal/http/server.go
    - src/server/internal/http/agent_web_search.go
    - src/server/internal/agent/service.go
    - src/server/internal/agent/executor.go
    - .planning/phases/31.1-readiness-fail-closed/31.1-REVIEW.md
  </read_first>
  <behavior>
    - A strict configured provider is built before Agent service construction.
    - ToolRuntimeOptions contains that exact provider instance.
    - The post-construction setter is not used by the strict path.
  </behavior>
  <action>Reorder the strict block in `buildRuntime`: create the authority-aware provider first, assign it to `runtimeCarrier.WebSearchProvider`, then construct MCP Client, Agent gateway, and `agent.NewServiceWithRuntimeOptions`. Remove the strict-path `SetWebSearchProvider` call. Keep compatibility behavior unchanged. Add an AST/order assertion plus dynamic strict BuildRuntime test so later refactors cannot move provider injection after executor construction.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestBuildRuntimeAgentWebSearchCompositionContract$'</automated>
  </verify>
  <acceptance_criteria>
    - Strict source/data flow assigns `WebSearchProvider` before `NewServiceWithRuntimeOptions`.
    - Strict construction succeeds for a complete provider config and fails closed for invalid authority/config.
    - Construction performs zero provider HTTP calls.
  </acceptance_criteria>
  <done>The production Agent service receives its web-search provider during authorized executor construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove instance-scoped registration and current denial</name>
  <files>src/server/internal/http/runtime_agent_web_search_test.go, src/server/internal/http/agent_web_search_test.go</files>
  <read_first>
    - src/server/internal/http/runtime_agent_web_search_test.go
    - src/server/internal/http/agent_web_search_test.go
    - src/server/internal/agent/executor_test.go
    - src/server/internal/mcp/builtin.go
  </read_first>
  <behavior>
    - Tavily BuildRuntime registers the exact pairs `agent.tool.builtin` -> `agent.ToolExecutor.builtin`, `mcp.websearch.builtin` -> `mcp.WebSearchTool`, and `mcp.websearch.tavily` -> `mcp.TavilyWebSearchProvider` exactly once, with no `agent.tool.web_search` row.
    - Chain and non-Tavily single-provider cases substitute exactly `mcp.websearch.chain` -> `mcp.websearch.Chain` or `mcp.websearch.provider` -> `mcp.websearch.Provider` for the selected provider pair; they never accumulate provider alternatives or accept a correct ID with a wrong owner.
    - The recorder ignores non-target descriptors without changing their construction path; it does not claim complete runtime descriptor coverage.
    - BuildRuntime uses no global web-search provider and makes no network call.
    - A stale/blocked guard before Search produces zero upstream calls; a current guard calls the configured provider once.
  </behavior>
  <action>Use a recorder scoped to the web-search subset that records both `EffectDescriptor.ID` and `EffectDescriptor.Owner`, counts every target pair, and returns registration success for non-target descriptors so existing non-web-search Chat duplicate ownership does not block Wave 1. Build the real runtime with an unopened SQL handle as existing lifecycle tests do. Run a configuration table for direct Tavily, comma-separated chain, and another supported single provider. Compare only the exact target pairs: the builtin pair, the MCP web-search pair, and only the configured provider pair from the mapping in `<interfaces>`. Assert every non-selected provider ID-owner pair is absent, each selected target pair occurs exactly once, and a mutation retaining a valid ID but replacing its owner fails pair-set equality. Add an AST assertion that the production BuildRuntime path does not call `agent/tools.NewWebsearchToolWithOptions`, `NewRegistryWithOptions`, or `NewDefaultRegistryWithOptions` to pad coverage. Reuse the built factory path for denied/current Search cases, checking current catalog/guard traces and provider counts. This recorder is test evidence only; do not use it to claim global duplicate rejection, non-web-search descriptor completeness, or complete exact-join coverage, which Plan 19 must enforce with the production registry and phase gate. Do not construct an independent Registry/WebsearchTool to make the test pass.</action>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestBuildRuntimeAgentWebSearchCompositionContract$|^TestWebSearchProviderFactoryAuthorityContract$'</automated>
  </verify>
  <acceptance_criteria>
    - Every selected live ID-owner pair is present exactly once in the scoped recorder; correct IDs with wrong owners fail, and non-selected provider alternatives are absent.
    - `agent.tool.web_search` has an asserted zero count and the production AST contains no independent Registry/WebsearchTool construction used only for coverage.
    - Non-web-search descriptors are not re-registered or synthesized by this test; their global ownership/count contract remains a Plan 19 assertion.
    - Construction transport count is zero.
    - Denied execution transport count is zero; authorized execution count is one.
    - Global duplicate rejection, descriptor ownership across all effect classes, and the six-input exact join remain explicitly owned by Plan 19; this Wave 1 plan does not weaken those later gates.
  </acceptance_criteria>
  <done>Real strict composition installs one instance-scoped guarded web-search chain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime configuration -> provider instance | Deployment-owned provider settings select an outbound implementation. |
| Agent executor -> web-search transport | A persisted tool subject can initiate an external network effect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31.1-14-01 | Elevation of Privilege | Late provider injection | high | mitigate | Provider is present during authorized executor construction and guarded descriptor installation. |
| T-31.1-14-02 | Tampering | Package-global provider | high | mitigate | Strict BuildRuntime uses only an instance-scoped provider; test rejects global substitution. |
| T-31.1-14-03 | Denial of Service | Constructor network effect | medium | mitigate | BuildRuntime remains side-effect-free and the test asserts zero construction transport. |
</threat_model>

<verification>
- Run both focused HTTP selectors and `cd src/server &amp;&amp; go test ./internal/http ./internal/agent ./internal/mcp -count=1`.
- Run `git diff --check`.
</verification>

<success_criteria>
- Canonical Agent web-search production composition gap is closed.
- Web-search target-pair uniqueness and current-generation denial are proven without global state or live-provider claims; global descriptor exactness remains Plan 19-owned.
</success_criteria>

<commit_policy>
Create one atomic commit: `fix(31.1-14): inject agent web search before executor`. Do not push. Plan 20 ends with a HARD EXTERNAL/MANUAL POST-CLOSEOUT GATE handoff; no GSD executor, verifier, tracker, hook, or autonomous orchestrator step owns final Stage B or push.
</commit_policy>

<output>
Create `.planning/phases/31.1-readiness-fail-closed/31.1-14-SUMMARY.md` with composition order, descriptor counts, and zero-call results.
</output>
