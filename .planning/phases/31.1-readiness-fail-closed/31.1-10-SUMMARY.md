---
phase: 31.1-readiness-fail-closed
plan: "10"
subsystem: agent-tools
tags: [readiness, web-search, agent, mcp, capability-authority]
requires:
  - phase: 31.1-readiness-fail-closed
    provides: "RuntimeAuthorities, CatalogAuthorizer, typed EffectID bindings, Guard, and EffectRegistrar"
  - phase: 31.1-readiness-fail-closed
    provides: "MCP/web-search per-attempt guard patterns and strict mutation decoding"
provides:
  - "Authority-required direct Tavily and configured web-search factory construction"
  - "Live Agent ToolExecutor guards for builtin, custom HTTP, local Python, Python sandbox, and MCP effects"
  - "Instance-scoped WebSearchTool, shared Service executor wiring, and server-derived frontend capability identity"
affects: [agent, mcp, web-search, frontend, phase-31.1-plan-02]
requirements-completed: []
coverage:
  - id: D1
    description: "Direct Tavily and HTTP factory constructors require startup authority, register stable descriptors once, and deny current-expired searches before transport."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/mcp/web_search_provider_test.go#TestTavilyWebSearchReadinessDispatchContract"
        status: pass
      - kind: unit
        ref: "src/server/internal/http/agent_web_search_test.go#TestWebSearchProviderFactoryAuthorityContract"
        status: pass
  - id: D2
    description: "The production-capable ToolExecutor resolves typed descriptors at construction and re-resolves current server-owned subjects immediately before concrete effects."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/server/internal/agent/executor_test.go#TestLiveAgentToolExecutorReadinessContract"
        status: pass
      - kind: integration
        ref: "cd src/server && go test ./... -count=1"
        status: pass
  - id: D3
    description: "Tool response capabilityId is preserved in frontend normalization, omitted from mutation payloads/provider conversion, and caller capabilityId injection is rejected by Tool JSON decoding."
    requirement: RELS-01
    verification:
      - kind: unit
        ref: "src/web/src/features/agents/agentsApi.test.ts"
        status: pass
      - kind: typecheck
        ref: "pnpm --dir src/web exec tsc --noEmit"
        status: pass
duration: 70 min
completed: 2026-07-18
status: complete
---

# Phase 31.1 Plan 10: Web Search and Live Agent Tool Dispatch

Authority-bound web-search constructors and the real Agent `ToolExecutor` now fail closed at every new effect while preserving server-owned capability identity at the API boundary.

## Task Commits

1. **Task 1: Guard web-search provider construction and dispatch** - `9c9e8b4`
2. **Task 2: Guard live Agent tool effects and frontend identity** - `439bcd2`

## Accomplishments

- Added `mcp.WebSearchRuntimeOptions`, strict Tavily construction, and an authority-aware HTTP factory without changing `server.go` ownership. Configured provider attempts re-resolve `web_search`/`network` immediately before transport; duplicate owner registration fails.
- Added `NewWebSearchTool` with instance-scoped provider/readiness state. The live executor no longer swaps the production global web-search provider.
- Added `ToolRuntimeOptions` and `NewAuthorizedToolExecutor`. The constructor resolves/registers the Agent builtin, custom HTTP, local Python, Python sandbox, and MCP descriptors before returning; each execution re-resolves the persisted server-owned runtime subject and guards before the concrete builtin/HTTP/process/sandbox/MCP effect.
- Refactored Service runner, plan-step, and direct `ExecuteTool` paths to share one executor when runtime options are supplied. Added HTTP client, Python process, sandbox, builtin/provider seams for independent counters.
- Added server-derived `ToolDefinition.capabilityId`, explicit OpenAI conversion that omits it, strict Tool JSON mutation rejection, and frontend response normalization/mutation serialization that preserves identity only in responses.

## Verification

- `bash scripts/run-go-tests-matched.sh ./internal/mcp '^TestTavilyWebSearchReadinessDispatchContract$'` passed.
- `bash scripts/run-go-tests-matched.sh ./internal/mcp/websearch '^TestWebsearchChainReadinessPerProviderContract$'` passed.
- `bash scripts/run-go-tests-matched.sh ./internal/http '^TestWebSearchProviderFactoryAuthorityContract$'` passed.
- `bash scripts/run-go-tests-matched.sh ./internal/agent '^TestLiveAgentToolExecutorReadinessContract$'` passed with named builtin, web-search, custom API HTTP, local Python, Python sandbox, and MCP subtests.
- `pnpm --dir src/web exec vitest run src/features/agents/agentsApi.test.ts --reporter=verbose` passed (9 tests).
- `pnpm --dir src/web exec tsc --noEmit` passed.
- `cd src/server && go test ./... -count=1` passed.
- `git diff --check` passed.

## Boundary and Residual Risk

- `server.go` and `cmd/agent/main.go` still use the legacy single-result web-search wrapper because Plan 02 owns their startup composition. The strict factory is available for that handoff; this plan does not claim the complete BuildRuntime wiring.
- The authored monolith profile currently excludes `sandbox.code_execution`; the strict executor therefore does not register that excluded descriptor and rejects sandbox dispatch at the catalog boundary. The live suite enables the binding in an isolated test fixture to prove the zero-call guard seam.
- Existing one-argument compatibility constructors remain for unrelated legacy tests. They are not used by `NewServiceWithRuntimeOptions` or `NewAuthorizedToolExecutor`; target production composition remains Plan 02 scope.
- Evidence class is E1 repository-local automated behavior proof only. No provider-live, target/live, deployment parity, or commercial-readiness claim is made.

## Self-Check: PASSED

- Task commits `9c9e8b4` and `439bcd2` are present.
- Plan-owned tracked files are committed; unrelated `.planning/config.json` remains unstaged and untouched.
- All required focused and full regression commands passed with zero test failures.

---
*Phase: 31.1-readiness-fail-closed*
*Completed: 2026-07-18*
