---
phase: 31.2-contract-surface-parity-gate
plan: "14"
type: execute
wave: 4
depends_on: ["31.2-02", "31.2-13"]
files_modified:
  - src/server/internal/http/router.go
  - src/server/internal/http/routes_chat.go
  - src/server/internal/http/routes_channel.go
  - src/server/internal/http/routes_console.go
  - src/server/internal/http/routes_knowledge.go
  - src/server/internal/http/routes_knowledge_alias.go
  - src/server/internal/http/routes_workflow.go
  - src/server/internal/http/routes_observability_alert.go
  - src/server/internal/http/routes_release_evidence.go
  - src/server/internal/http/route_surface_group_b_test.go
autonomous: true
requirements: [RELS-02]
must_haves:
  truths:
    - "Group B route helpers and their remaining router.go call sites move together to the shared RouteSurfaceRegistrar, leaving no helper-signature mismatch and retaining chat, channel, knowledge, workflow, observability and release-evidence behavior."
    - "Each Group B registration carries exact operation, security/CSRF, guard, capability and request/success schema identity; no manifest JSON constructs a runtime mount."
    - "A direct mux mount, missing descriptor, duplicate method/path, excluded operation or zero Group B discovery fails the focused suite."
  artifacts:
    - path: "src/server/internal/http/routes_chat.go"
      provides: "Chat route declarations using the shared runtime registrar"
      contains: "RouteSurfaceRegistration"
    - path: "src/server/internal/http/route_surface_group_b_test.go"
      provides: "Group B runtime registration and dispatch coverage"
      contains: "TestRouteSurfaceGroupBContract"
  key_links:
    - from: "src/server/internal/http/routes_chat.go"
      to: "src/server/internal/http/route_surface.go"
      via: "registrar.Register"
      pattern: "Register"
    - from: "src/server/internal/http/route_surface_group_b_test.go"
      to: "src/server/internal/http/routes_release_evidence.go"
      via: "group-wide registration snapshot"
      pattern: "route_surface_group_b"
---

<objective>
迁移第二组真实 Go route helpers 到同源 RouteSurfaceRegistrar，关闭剩余 8 个 route source 文件的 direct mux ownership。

Purpose: 在完整 http-runtime producer 前关闭聊天、知识、工作流和运维路由的同源注册缺口。
Output: 八个 route helper 的 registrar 接入、Group B 动态 dispatch/descriptor 测试和非空发现证明。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-PATTERNS.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-02-PLAN.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-13-PLAN.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-02-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-13-SUMMARY.md
@src/server/internal/http/routes_chat.go
@src/server/internal/http/routes_workflow.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Group B route helpers to same-source registration</name>
  <read_first>
    - src/server/internal/http/route_surface.go
    - src/server/internal/http/router.go
    - src/server/internal/http/routes_chat.go
    - src/server/internal/http/routes_channel.go
    - src/server/internal/http/routes_console.go
    - src/server/internal/http/routes_knowledge.go
    - src/server/internal/http/routes_knowledge_alias.go
    - src/server/internal/http/routes_workflow.go
    - src/server/internal/http/routes_observability_alert.go
    - src/server/internal/http/routes_release_evidence.go
  </read_first>
  <files>src/server/internal/http/router.go, src/server/internal/http/routes_chat.go, src/server/internal/http/routes_channel.go, src/server/internal/http/routes_console.go, src/server/internal/http/routes_knowledge.go, src/server/internal/http/routes_knowledge_alias.go, src/server/internal/http/routes_workflow.go, src/server/internal/http/routes_observability_alert.go, src/server/internal/http/routes_release_evidence.go, src/server/internal/http/route_surface_group_b_test.go</files>
  <behavior>
    - Every direct mux.Handle/HandleFunc in these helpers is replaced by an exact RouteSurfaceRegistration passed to the shared registrar.
    - Existing streaming, multipart, auth, CSRF, middleware, handler and response behavior remains unchanged.
    - Method/path, operationId, capability, media/schema, guard and descriptor identity are explicit and non-empty for every included route.
    - Existing behavior tests may keep calling a thin `registerXRoutes(*ServeMux, ...)` adapter, but production `router.go` calls only the new explicit registrar entrypoint; the adapter contains no metadata, direct mount, global lookup or retained snapshot.
  </behavior>
  <action>Refactor the eight owned helpers and update their remaining `router.go` call sites in the same task to call new explicit registrar entrypoints with the shared registrar established by Plan 02. Preserve each old `registerXRoutes(*ServeMux, ...)` symbol only as a thin behavior-test adapter that creates a local registrar and delegates to the same explicit entrypoint; it must contain no route metadata, direct Handle/HandleFunc, global state, mux-to-registrar lookup or production caller. Consume the already-compiled Group A wiring from Plan 13; the adapter is not a compatibility authority because it cannot author or retain descriptor state used by production. Make the registrar the sole mount path for Group B, keep route-specific media behavior for JSON, markdown, SSE and multipart in the registration value, and preserve existing handler semantics. After the production migration compiles, create `route_surface_group_b_test.go` with the runnable base `TestRouteSurfaceGroupBContract`, positive owner/registration/descriptor/media counts, representative dispatch assertions, and AST proof that migrated production call sites use only the explicit registrar entrypoints, then run it as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - All eight helpers and their updated `router.go` call sites compile with the Plan 02 registrar contract and have no direct mux mount bypass.
    - Existing chat/channel/knowledge/workflow/observability route tests remain compatible.
    - Legacy test adapters delegate only and are absent from production router call sites; they cannot supply or retain an independent descriptor authority.
    - Missing operation metadata or typed binding fails construction rather than installing a permissive fallback.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestRouteSurfaceGroupBContract$' &amp;&amp; (cd src/server &amp;&amp; go test ./internal/http -count=1) &amp;&amp; git diff --check</automated>
  </verify>
  <done>Group B route helpers use same-source typed registrations and preserve their existing behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Prove Group B registration, media and dispatch closure</name>
  <read_first>
    - src/server/internal/http/route_surface_group_b_test.go
    - src/server/internal/http/route_surface.go
    - docs/api/route-surface-manifest.json
  </read_first>
  <files>src/server/internal/http/route_surface_group_b_test.go</files>
  <behavior>
    - The suite discovers all eight source owners and a nonzero set of registrations, descriptors and exact dispatch probes including JSON, markdown, SSE and multipart branches.
    - One-field mutations of method/path/auth/CSRF/guard/capability/request/response identity fail in both registration and comparison directions.
    - Direct mount bypass, duplicate registration, excluded route and zero inventory fail without a passing report.
  </behavior>
  <action>After Task 1 production migration and base test are complete, extend the group-level test harness with full static discovery, immutable-snapshot comparison, media probes and one-field mutation families. Enumerate the eight owned source files, compare the registrar snapshot to expected operation rows, and exercise additional chat, channel, knowledge, workflow and release-evidence requests with injected handler counters. Assert positive counts and stable failure codes for each mutation family as implementation-complete verification.</action>
  <acceptance_criteria>
    - `TestRouteSurfaceGroupBContract` has positive owner, registration, descriptor, media and dispatch counts.
    - No source-token-only assertion can make the suite pass when a direct mount or missing descriptor is present.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestRouteSurfaceGroupBContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>Group B has executable same-source registration, media, dispatch and mutation evidence.</done>
</task>

</tasks>

<verification>
- Run the Group B matched suite, the complete `internal/http` package regression, and `git diff --check`.
- Evidence is repository-local E1/E2 only; the full http-runtime report remains owned by Plan 15.
</verification>

<success_criteria>
- Eight named route helper files have no direct mux mount bypass.
- Group B registrations and descriptors are exact, non-empty and mutation-tested.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-14-SUMMARY.md` when done.
</output>
