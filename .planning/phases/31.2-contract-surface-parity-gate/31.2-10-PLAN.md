---
phase: 31.2-contract-surface-parity-gate
plan: "10"
type: execute
wave: 4
depends_on: ["31.2-01", "31.2-04", "31.2-11", "31.2-12"]
files_modified:
  - src/web/src/features/releaseProjection/releaseProjection.tsx
  - src/web/src/features/releaseProjection/releaseProjection.test.tsx
  - src/web/src/app/router.tsx
  - src/web/src/features/layouts/WorkspaceLayout.tsx
  - src/web/src/features/layouts/ConsoleLayout.tsx
  - src/web/src/features/layouts/AdminSidebar.tsx
  - src/web/src/routes/marketing/HomePage.tsx
  - src/web/src/routes/workspace/ChatPage.tsx
  - src/web/src/routes/workspace/AgentsPage.tsx
autonomous: true
requirements: [RELS-02]
must_haves:
  truths:
    - "The app-safe projection has one typed frontend API/state/provider that fetches the authenticated identity-bound runtime response; callers cannot supply projection JSON, identity, availability or fallback data (D-SUR-08, D-SUR-19)."
    - "The release-projection API imports and passes Plan 01's exact generated operation symbol into the Plan 04 shared HttpClient contract; method/path/core lookup and TypeScript generic synthesis are forbidden (D-SUR-02, D-SUR-06)."
    - "Router plus workspace/console/admin navigation and marketing links consume the generated release projection; excluded is absent and conditional authenticated routes/nav require current enabled runtime availability (D-SUR-07, D-SUR-19)."
    - "Chat consumes exact `ModelOption.capabilityId` and Agents consumes exact `AgentToolDefinition.capabilityId`; selectors join current app projection and mutations explicitly omit capabilityId (D-SUR-08, D-SUR-19)."
    - "Loading, unauthenticated, unavailable, identity-mismatched, disabled and blocked states fail closed without treating UI visibility as server authorization (D-SUR-08, D-SUR-20)."
  artifacts:
    - path: "src/web/src/features/releaseProjection/releaseProjection.tsx"
      provides: "Typed authenticated API, identity validation, state/provider and projection hooks"
      exports: ["AppCapabilityProjectionResponse", "createReleaseProjectionApi", "ReleaseProjectionProvider", "useReleaseProjection"]
    - path: "src/web/src/features/releaseProjection/releaseProjection.test.tsx"
      provides: "Runtime response, nav/marketing and model/tool selector behavior proof"
      contains: "capabilityId"
    - path: "src/web/src/app/router.tsx"
      provides: "Actual release-projection route/provider wiring"
      contains: "ReleaseProjectionProvider"
  key_links:
    - from: "src/web/src/features/releaseProjection/releaseProjection.tsx"
      to: "/api/v1/app/readiness/capabilities"
      via: "authenticated shared HttpClient GET with exact identity-bound response"
      pattern: "createReleaseProjectionApi"
    - from: "src/web/src/routes/workspace/ChatPage.tsx"
      to: "src/web/src/types/api.ts"
      via: "exact Phase 31.1 ModelOption.capabilityId join"
      pattern: "capabilityId"
    - from: "src/web/src/routes/workspace/AgentsPage.tsx"
      to: "src/web/src/features/agents/agentsApi.ts"
      via: "exact Phase 31.1 AgentToolDefinition.capabilityId join and mutation omission"
      pattern: "capabilityId"
---

<objective>
把 release/app-safe projection 接到真实 frontend API/state/provider、router/nav/marketing 与 Chat/Agents selectors，形成可由唯一 sidecar 验证的实际产品呈现。

Purpose: 关闭“有 projection/verifier 但产品没有消费”的断点，并阻止 caller JSON、Admin inventory 或 TypeScript capability map成为前端权威。
Output: typed authenticated provider、实际 route/nav/marketing visibility、server-derived model/tool selector wiring与行为测试。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VALIDATION.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-01-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-04-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-11-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-12-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-03-PLAN.md
@.planning/phases/31.1-readiness-fail-closed/31.1-05-PLAN.md
@.planning/phases/31.1-readiness-fail-closed/31.1-10-PLAN.md
@src/web/src/app/router.tsx
@src/web/src/types/api.ts
@src/web/src/features/agents/agentsApi.ts

<interfaces>
Phase 31.1 is unexecuted at planning time. Require and consume these exact upstream response contracts; correct their owning plans if implementation differs rather than guessing a future API:

`GET /api/v1/app/readiness/capabilities` is authenticated and returns `AppCapabilityProjectionResponse{releaseIdentity:{sourceTree,contractDigest,deploymentProfile},generation,projectionDigest,capabilities:[{capabilityId,disposition,availability,enabled}]}` with excluded capabilities omitted. `createReleaseProjectionApi` explicitly imports `operationContracts.getAppReadinessCapabilities` (use the generated operationId member if its exact name differs) and passes that symbol into the shared HttpClient; it never selects metadata by URL.

Chat catalog response is exact JSON/TypeScript `ModelOption{id,label,capabilityId}` from 31.1-05. Agent catalog response is live `agent.ToolDefinition`/frontend `AgentToolDefinition` with required `capabilityId` from 31.1-10. Both create/update mutation DTOs reject capabilityId; frontend mutation serializers explicitly select business fields.

Plan 01 provides readonly `releaseSurfaceProjection` from `@/generated/release-projection.generated`; it contains surface/capability/disposition/navigation metadata and digest but no runtime availability. The provider exact-matches the authenticated response identity/projection digest to that generated projection. Provider props accept children only; there is no `projection`, `initialProjection`, identity or availability prop and no localStorage/env fallback.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the authenticated identity-bound projection API and provider</name>
  <read_first>
    - src/web/src/services/http/client.ts
    - src/web/src/app/router.tsx
    - src/web/src/generated/release-projection.generated.ts
    - src/server/internal/http/readiness_handler.go
  </read_first>
  <files>src/web/src/features/releaseProjection/releaseProjection.tsx, src/web/src/features/releaseProjection/releaseProjection.test.tsx</files>
  <behavior>
    - V31.2-S10/T-31.2-03: authenticated API passes its imported exact operation symbol to HttpClient and strict-validates the response, generated projection digest and release identity before publishing state.
    - Provider props cannot carry projection/identity/availability JSON; loading/error/401/identity mismatch/unknown availability yields fail-closed state and no enabled conditional capability.
    - Duplicate/unknown/excluded capability, stale generation regression, wrong contract/profile/source tree or Admin inventory fields fail without local fallback.
  </behavior>
  <action>Implement one typed `createReleaseProjectionApi(client)` over the shared authenticated HttpClient: import the exact generated operation metadata member and pass it through `OperationTransportContract`; do not reconstruct it or use endpoint lookup/generics. Strict-validate response fields, generation, digests, unique capability IDs and enums. Implement `ReleaseProjectionProvider({children})` and `useReleaseProjection`; exact-match runtime identity/projection digest to `releaseSurfaceProjection`, expose immutable lookups and fail closed. Do not accept caller JSON/initial state/identity/availability/Admin inventory/localStorage/env/maps. Run Plan 01's compiler-symbol preflight with `releaseProjection.tsx` as a known owner and require positive exact-symbol use. UI filtering remains presentation only. After the production API/provider implementation is complete, create `releaseProjection.test.tsx`, add the named response/identity/generation/injection mutation cases, and run the Vitest, TypeScript and compiler-symbol checks as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Only the authenticated server response plus generated release projection can publish enabled conditional availability.
    - Every response/identity/generation/injection mutation is a named nonzero test case.
    - Provider has no caller-supplied projection data path or permissive fallback.
  </acceptance_criteria>
  <verify><automated>pnpm --dir src/web exec vitest run src/features/releaseProjection/releaseProjection.test.tsx --reporter=verbose &amp;&amp; pnpm --dir src/web exec tsc --noEmit &amp;&amp; node scripts/verify_frontend_operation_contract_usage.mjs --tsconfig src/web/tsconfig.json --expect-owner src/web/src/features/releaseProjection/releaseProjection.tsx --require-all &amp;&amp; git diff --check</automated></verify>
  <done>V31.2-S10 has one typed authenticated app-safe projection authority in frontend state.</done>
</task>

<task type="auto">
  <name>Task 2: Wire router, workspace/console/admin navigation and marketing exposure</name>
  <read_first>
    - src/web/src/app/router.tsx
    - src/web/src/features/layouts/WorkspaceLayout.tsx
    - src/web/src/features/layouts/ConsoleLayout.tsx
    - src/web/src/features/layouts/AdminSidebar.tsx
    - src/web/src/routes/marketing/HomePage.tsx
    - src/web/src/features/releaseProjection/releaseProjection.tsx
  </read_first>
  <files>src/web/src/app/router.tsx, src/web/src/features/layouts/WorkspaceLayout.tsx, src/web/src/features/layouts/ConsoleLayout.tsx, src/web/src/features/layouts/AdminSidebar.tsx, src/web/src/routes/marketing/HomePage.tsx, src/web/src/features/releaseProjection/releaseProjection.test.tsx</files>
  <behavior>
    - V31.2-S10/T-31.2-03: excluded route/nav/marketing rows are absent; committed public marketing links follow generated projection; conditional authenticated rows render only when current provider availability is enabled.
    - Workspace, console and admin navigation plus direct route guards use the same provider lookup and projection identity; Admin full inventory cannot satisfy app exposure.
    - Loading/error/blocked/disabled/unknown/mismatch hides conditional nav and rejects direct route entry with the existing recovery surface rather than mounting content.
  </behavior>
  <action>Mount the provider at the authenticated router boundary and drive route guards/navigation entries from stable release projection IDs plus provider lookups. Update workspace, console and admin layouts without duplicating maps; excluded rows are not rendered, conditional rows require enabled current availability, and committed rows retain existing auth/role checks. HomePage consumes only generated committed public marketing rows and never treats unauthenticated absence as permission for conditional links. Add all behavior to the single Plan 10 test file using real router/layout components and mocked authenticated HttpClient responses, not caller projection props. Keep text/layout changes minimal and preserve existing accessibility/navigation behavior.</action>
  <acceptance_criteria>
    - Router and all four exposure families share projection IDs/provider behavior with no local capability map.
    - Direct URLs and navigation fail closed identically for unavailable conditional surfaces.
    - Marketing exposes only generated committed public rows; Admin inventory is never imported.
  </acceptance_criteria>
  <verify><automated>pnpm --dir src/web exec vitest run src/features/releaseProjection/releaseProjection.test.tsx --reporter=verbose &amp;&amp; pnpm --dir src/web exec tsc --noEmit &amp;&amp; git diff --check</automated></verify>
  <done>Router, nav and marketing are actual consumers of release/runtime projection rather than verifier-only records.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Chat and Agents selectors to exact server-derived capability DTOs</name>
  <read_first>
    - src/web/src/routes/workspace/ChatPage.tsx
    - src/web/src/routes/workspace/AgentsPage.tsx
    - src/web/src/types/api.ts
    - src/web/src/features/agents/agentsApi.ts
    - src/web/src/features/releaseProjection/releaseProjection.tsx
  </read_first>
  <files>src/web/src/routes/workspace/ChatPage.tsx, src/web/src/routes/workspace/AgentsPage.tsx, src/web/src/features/releaseProjection/releaseProjection.test.tsx</files>
  <behavior>
    - V31.2-S10/T-31.2-03: Chat preserves complete `ModelOption` and joins its exact `capabilityId`; Agents preserves normalized `AgentToolDefinition.capabilityId`; missing catalog entries are not synthesized.
    - Disabled/blocked/excluded/mismatched model/tool subjects are unavailable, while enabled returned subjects render; UI state never authorizes server execution.
    - Actual model/tool mutation objects contain business fields only and omit capabilityId even when based on a selected response object.
  </behavior>
  <action>In ChatPage retain full `ModelOption` response values and intersect exact capabilityId with current provider availability; do not reduce to id/label or synthesize historical values. In AgentsPage retain exact normalized tool definition capabilityId for display/selection, then construct create/update payloads by explicit business-field selection through the Phase 31.1 API serializer; never spread response objects. Extend the Plan 10 test to render both pages against exact upstream DTO fixtures, inspect submitted payloads, reject missing/unknown identities and prove no client-authored model/tool mapping or caller projection object. Preserve server-side guard as final authorization.</action>
  <acceptance_criteria>
    - Both selectors use exact Phase 31.1 fields and current authenticated projection.
    - Every mutation assertion proves capabilityId absent; absent/blocked catalog items cannot become selectable.
    - Plan 03 sidecar can resolve the actual selector reads and Plan 05 consumes them without a second scan.
  </acceptance_criteria>
  <verify><automated>pnpm --dir src/web exec vitest run src/features/releaseProjection/releaseProjection.test.tsx src/features/agents/agentsApi.test.ts --reporter=verbose &amp;&amp; pnpm --dir src/web exec tsc --noEmit &amp;&amp; git diff --check</automated></verify>
  <done>Chat/Agents actual product selectors consume exact server capability identity and never submit or author it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated projection response -> frontend state | Runtime availability and release identity control product visibility. |
| server catalog DTO -> selectors/mutations | Read-only capability identity must survive display but never enter mutation authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-03 | Elevation of Privilege | caller/client projection | high | mitigate | No projection props/fallbacks, authenticated identity/digest join, closed response validation and fail-closed states. |
| T-31.2-08 | Information Disclosure | app projection | medium | mitigate | App-safe fields only, no Admin inventory/dependency details, stable IDs/digests and shared redaction boundary. |
</threat_model>

<verification>
- Run the single behavior suite plus exact agents API test, TypeScript no-emit, and `git diff --check`.
- Plan 05 subsequently consumes the same sidecar and verifies these actual exposures against authoritative projection/catalog artifacts.
- Evidence remains E1 repository-local and UI visibility is never treated as runtime authorization or E3/E4 proof.
</verification>

<success_criteria>
- Typed authenticated projection state is wired into actual product routes/navigation/marketing/selectors.
- Excluded and unavailable conditional surfaces fail closed without caller JSON or client maps.
- Exact model/tool capability identity is response-only and mutation-safe.
- All nine owned files fit one focused three-task plan and expose AST-resolvable symbols for Plan 05.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-10-SUMMARY.md` when done.
</output>
