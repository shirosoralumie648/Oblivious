---
phase: 31.2-contract-surface-parity-gate
plan: "09"
type: execute
wave: 8
depends_on: ["31.2-02", "31.2-05", "31.2-06", "31.2-08", "31.2-13", "31.2-14", "31.2-15"]
files_modified:
  - src/server/cmd/release-contract/main.go
  - src/server/cmd/release-contract/main_test.go
  - scripts/verify-release-contract.sh
  - scripts/verify_release_contract.py
  - scripts/verify-quality-gates.sh
  - scripts/check.sh
  - .github/workflows/ci.yml
  - docs/release/commercial-gates.md
  - docs/release/rc-checklist.md
autonomous: true
requirements: [RELS-02]
must_haves:
  truths:
    - "Aggregate requires exactly ten nested surfaces once each: build-identity, readiness, deployment, http-runtime, frontend-transport, frontend-exposure, protobuf, migration-static, migration-ledger, and migration-replay (D-SUR-15, D-SUR-16)."
    - "Every input passes strict typed details validation, exact trusted release identity, unique surface key, pass/no-drift/no-skip outcome, and redaction; missing, extra, duplicate, folded, flat, or spliced reports fail (D-SUR-16, D-SUR-20)."
    - "The only direct parent is verify-quality-gates.sh -> verify-release-contract.sh -> producers; check/commercial/CI reach it only through quality, aggregate never calls ancestors, and each producer executes once (D-SUR-17)."
    - "One release session builds the Phase 31 immutable artifact bundle/image tag/digest exactly once; build-identity and the Phase 31.1 readiness/deployment aggregate-consume harness inspect/use that same bundle and any rebuild/mismatch fails (D-SUR-16, D-SUR-17)."
    - "Identity-bearing aggregate runs only on a clean commit/CI; dirty development behavior uses a temporary clean fixture and cannot form a pass claim (D-SUR-18)."
    - "CI bootstraps/checks protobuf tools by manifest digest before the gate, while operator docs state E1/E2 evidence ceiling and redact sensitive data (D-SUR-11, D-SUR-20)."
  artifacts:
    - path: "scripts/verify_release_contract.py"
      provides: "Exact report-set, identity, unique-key, outcome, details and redaction aggregate validator"
      contains: "release-contract-aggregate/v1"
    - path: "scripts/verify-release-contract.sh"
      provides: "Single clean-head producer registry and aggregate orchestration"
      contains: "migration-replay"
    - path: "scripts/verify-quality-gates.sh"
      provides: "Sole direct aggregate parent"
      contains: "verify-release-contract.sh"
    - path: "docs/release/commercial-gates.md"
      provides: "Operator evidence-class, command, failure, migration and residual-risk contract"
      contains: "E1/E2"
  key_links:
    - from: "scripts/verify-quality-gates.sh"
      to: "scripts/verify-release-contract.sh"
      via: "one and only direct aggregate invocation"
      pattern: "verify-release-contract\.sh"
    - from: "scripts/verify-release-contract.sh"
      to: "scripts/verify_release_contract.py"
      via: "new output directory, exact producer registry, then aggregate validation"
      pattern: "verify_release_contract\.py"
    - from: ".github/workflows/ci.yml"
      to: "scripts/check.sh"
      via: "release-gates reaches aggregate transitively through docs -> quality"
      pattern: "check\.sh docs"
---

<objective>
建立 exact ten-report aggregate、唯一 parent DAG、clean-identity gate、CI protobuf cache/bootstrap 和 operator redaction/claim contract。

Purpose: 让任何 surface drift、identity splice、duplicate/fold/skip、producer failure、递归/重复执行或敏感输出都阻断 repository release gate。
Output: typed aggregate validator、single producer registry、quality/check/CI wiring、DAG/identity/redaction fixtures与 operator docs。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-PATTERNS.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VALIDATION.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-02-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-05-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-06-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-08-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-13-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-14-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-15-SUMMARY.md
@.planning/phases/31-release-contract-current-baseline/31-06-PLAN.md
@.planning/phases/31.1-readiness-fail-closed/31.1-03-PLAN.md
@.planning/phases/31.1-readiness-fail-closed/31.1-08-PLAN.md
@scripts/check.sh
@scripts/verify-quality-gates.sh
@scripts/verify-commercial-completion.sh
@.github/workflows/ci.yml

<interfaces>
Preserve Phase 31/31.1 producer APIs and register all ten typed details schemas in the existing `cmd/release-contract verify-report` registry. Readiness must be generated only by Phase 31.1 Go inspector; Python receives its finished nested report and never evaluates timestamps/freshness.

The aggregate shell has an explicit ordered producer registry. It creates one transient `release-artifact-bundle/v1` by invoking Phase 31 `build-release-image.sh` exactly once; the bundle records trusted release identity, immutable image tag, resolved image digest, inspection paths and bundle digest. `report-build-identity` consumes its inspection, and Phase 31.1 harness runs `aggregate-consume` with the same bundle/tag/digest and builder count zero. Missing/mismatched bundle or rebuild fails.

A grouped producer may yield independent reports only at approved boundaries: one frontend sidecar execution yields transport/exposure; one readiness deployment harness yields readiness/deployment; one `verify-migration-replay.sh session` invocation yields static/ledger/replay. Each report constructor executes once. Aggregate must not invoke a standalone migration-static producer before/after that session.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate the exact nested report set and trusted identity</name>
  <read_first>
    - src/server/cmd/release-contract/main.go
    - src/server/cmd/release-contract/main_test.go
    - src/server/internal/surfacereport/report.go
    - src/server/internal/surfacereport/registry.go
    - src/server/internal/surfacereport/build.go
    - src/server/internal/surfacereport/readiness.go
    - src/server/internal/surfacereport/deployment.go
    - src/server/internal/surfacereport/frontend.go
    - src/server/internal/surfacereport/migration.go
    - src/server/internal/surfacereport/protobuf.go
  </read_first>
  <files>src/server/cmd/release-contract/main.go, src/server/cmd/release-contract/main_test.go, scripts/verify_release_contract.py</files>
  <behavior>
    - V31.2-S09/T-31.2-06: exact ten surface IDs are required once; missing, extra, duplicate, stale directory, empty glob, folded report, or flat legacy shape fails.
    - Every report strict-decodes through its registered typed details schema and has one unique `(surface,canonicalSource,consumer,version)` key.
    - releaseCommit/sourceTree/contractDigest/deploymentProfile/dirty/evidenceClass match exactly across inputs and the trusted clean-head identity; any splice fails.
    - Every outcome is pass with empty missing/extra/incompatible, errorCodes, and skippedChecks; producer nonzero status remains failure even if a report file exists.
    - Readiness report is accepted only after Go `verify-report` typed validation; Python performs no freshness/timestamp verdict.
  </behavior>
  <action>Extend the existing release-contract CLI registry to register the Phase 31 build, Phase 31.1 readiness/deployment, and all seven Phase 31.2 details types, preserving every producer subcommand. `verify-report` strict-decodes the complete nested envelope and typed details, rejects flat/unknown fields, and exposes a machine status without rewriting reports. Implement `verify_release_contract.py` as the aggregate validator over an explicit list of report paths plus trusted identity JSON. Require schema `release-contract-aggregate/v1`, exact required set constant, one strict CLI validation per report, exact identity tuple, unique surface identity key, valid digests, pass/no-drift/no-errors/no-skips, and producer status map exactly one execution per surface. Embed validated reports without flattening them. Python may compare readiness identity/outcome only and must not parse checkedAt/validUntil for authorization. After the production CLI and aggregate validator are complete, add and run `TestRunVerifyAllSurfaceReportTypesContract` and Python `--fixtures` as implementation-complete automated verification; fixture mode builds a valid ten-report set from typed Go fixture output, applies every one-field missing/extra/fold/duplicate/identity/details/outcome/skip/flat/stale mutation, asserts nonzero cases, and rejects zero reports.</action>
  <acceptance_criteria>
    - All ten report details types strict-validate through the shared Go registry.
    - Exact set/identity/key/outcome/producer status rules are enforced with nonzero fixture counts.
    - Aggregate output remains nested and readiness freshness has one Go owner.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./cmd/release-contract '^TestRunVerifyAllSurfaceReportTypesContract$' &amp;&amp; python3 -m py_compile scripts/verify_release_contract.py &amp;&amp; python3 scripts/verify_release_contract.py --fixtures &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.2-S09 exact report-set, typed-details, identity, unique-key, no-drift/no-skip and producer-status rules are executable.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce the sole-owner DAG and one execution per producer</name>
  <read_first>
    - scripts/verify-release-contract.sh
    - scripts/verify-quality-gates.sh
    - scripts/check.sh
    - scripts/verify-commercial-completion.sh
    - scripts/verify-http-runtime-contract.sh
    - scripts/verify-frontend-surface-sidecar.sh
    - scripts/verify-protobuf-contract.sh
    - scripts/verify-migration-replay.sh
    - .github/workflows/ci.yml
  </read_first>
  <files>scripts/verify-release-contract.sh, scripts/verify_release_contract.py, scripts/verify-quality-gates.sh, scripts/check.sh, .github/workflows/ci.yml</files>
  <behavior>
    - V31.2-S09/T-31.2-07: quality is the only script with a direct executable aggregate call; check docs/all, commercial, and CI reach it transitively and contain no direct call.
    - Aggregate never calls check.sh, test.sh, quality, or commercial and runs each producer/surface exactly once; frontend/readiness-deployment/migration grouped harnesses do not duplicate scan/build/session work.
    - A producer nonzero exit, absent report, duplicate invocation, stale output, unwritable output, or cleanup failure remains nonzero.
    - `check.sh docs` no longer directly invokes OpenAPI/migration/protobuf/frontend producers outside quality.
    - CI bootstraps protobuf tools before release gate, verifies exact versions on cache hit, and keys cache with canonical tool manifest digest.
  </behavior>
  <action>In clean mode require monolith, empty porcelain, trusted HEAD identity and a new output directory. Start one release session: invoke Phase 31 `build-release-image.sh` exactly once, resolve the immutable image digest, create bounded transient `release-artifact-bundle/v1`, call `report-build-identity` once with its inspection, then invoke Phase 31.1 readiness/deployment harness once in `aggregate-consume` with that exact bundle/tag/digest and assert build count zero. Run http runtime once; frontend extractor once then transport/exposure constructors once; protobuf once; invoke `verify-migration-replay.sh session` exactly once to yield static/ledger/replay and never run migration static separately. Record exact surface and expensive-operation counts/status, then invoke the Python validator once. Add exactly one direct aggregate call to quality; check/commercial/CI only wire to quality and other parents use disposable wiring fixtures. Assert the ancestor ban. CI keys protobuf cache by manifest digest, revalidates versions, then reaches aggregate through `check.sh docs` once. After the production DAG and parent wiring are complete, add and run the runtime call-graph fixture inside `verify-release-contract.sh --fixtures` as implementation-complete automated verification using an instrumented temporary PATH and producer shims recording invocation IDs/status/report paths; assertions use observed calls/outputs, and Stage-A exercises identical topology in a disposable clean Git fixture.</action>
  <acceptance_criteria>
    - Observed call graph equals the fixed DAG; image build, frontend scan, readiness/deployment harness and migration session each count one, every report constructor counts one, and readiness aggregate-mode rebuild/static pre-run counts zero.
    - Aggregate/quality/check/CI cannot recurse or duplicate expensive producers.
    - CI cache key and cache-hit path both verify the approved protobuf manifest/tool versions.
    - Any producer/output/cleanup/status fault makes quality/check/CI path nonzero.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/verify-release-contract.sh scripts/verify-quality-gates.sh scripts/check.sh &amp;&amp; bash scripts/verify-release-contract.sh --fixtures &amp;&amp; python3 scripts/verify_release_contract.py --fixtures --call-graph &amp;&amp; git diff --check</automated>
  </verify>
  <done>The release graph has one direct aggregate parent, no ancestor recursion, and one execution per producer/surface.</done>
</task>

<task type="auto">
  <name>Task 3: Close clean-identity, redaction, operator-doc, and E1/E2 claim gates</name>
  <read_first>
    - scripts/verify-release-contract.sh
    - scripts/verify_release_contract.py
    - docs/release/commercial-gates.md
    - docs/release/rc-checklist.md
    - docs/product/operator-guide.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>scripts/verify-release-contract.sh, scripts/verify_release_contract.py, docs/release/commercial-gates.md, docs/release/rc-checklist.md</files>
  <behavior>
    - Dirty real worktree cannot produce identity-bearing aggregate pass; development mutations run only in a temporary clean committed fixture.
    - Public diagnostics/reports/docs allow repository-relative paths, stable IDs, digests, counts, versions, error classes and remediation refs only.
    - Authorization/Cookie, secret/key/token values, credential-bearing/internal URLs, raw response bodies, DSNs, external evidence paths, and exact-current-commit pass claims fail/redact.
    - Docs record evidence class, environment, command, migration state, pass/fail, skips, residual risk, and remediation while stating only E1/E2 RELS-02 contribution.
  </behavior>
  <action>Implement recursive structured redaction before aggregate output. Keep machine output only in ignored/CI paths and never track an exact-current-commit pass statement. Update operator docs with focused commands, sole quality-parent protocol, ten surfaces, one immutable bundle build/shared consumption, protobuf cache, exact-once migration session/unavailable failure, evidence metadata, remediation and E1/E2 ceiling. After the production redaction/output/docs implementation is complete, extend aggregate fixtures with clean/dirty/staged/untracked source, identity substitution, secret-like values, credential URL, raw body, DSN, external path, claim inflation, bundle/tag/digest mismatch, rebuild attempt and duplicate migration-session/static invocation. During tasks run only disposable Stage-A callgraph/identity/redaction fixtures; do not call dirty-worktree `check.sh docs`, real aggregate, or any parent already wired to aggregate. After the atomic implementation commit, the single real command is clean porcelain plus `bash scripts/verify-quality-gates.sh`; quality is the one direct aggregate invocation. Any repair requires a new commit and rerun.</action>
  <acceptance_criteria>
    - Every secret/path/body/URL/claim mutation is nonzero or deterministically redacted without leaking its value.
    - Tracked docs contain no current exact commit/digest pass assertion and do not call E1/E2 target/commercial proof.
    - Clean-head and quality commands, migration failure semantics, exact surfaces, and residual risks are operator-actionable.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/verify_release_contract.py --fixtures --redaction &amp;&amp; bash scripts/verify-release-contract.sh --fixtures --identity --stage-a &amp;&amp; git diff --check</automated>
  </verify>
  <done>Identity-bearing aggregation and public operator output are clean-source-only, redacted, and bounded to E1/E2 claims.</done>
</task>

</tasks>

## Post-commit clean HEAD gate

After the executor creates the atomic Phase 31.2 implementation commit, and before push, run from that exact clean commit:

`test -z "$(git status --porcelain=v1 --untracked-files=all)" && bash scripts/verify-quality-gates.sh`

Record the exact HEAD/tree/contract digest, environment class, commands, migration state, ten surface IDs, pass/fail, empty skip list, and residual risks in `31.2-09-SUMMARY.md`. Any tracked repair requires a new commit and complete rerun; a failed/dirty tuple is never amended or reported as passing.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| ten producer outputs -> aggregate | Independent nested evidence may be stale, duplicated, folded, or identity-spliced. |
| parent scripts/CI -> aggregate | Call ownership determines recursion, duplicate expense, and false-passing paths. |
| reports/diagnostics -> operators/artifacts | Public evidence may leak sensitive runtime material or overstate readiness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-06 | Spoofing | report identity/set | high | mitigate | Exact ten-set, typed details, trusted identity equality, unique keys, producer status/counts, and splice fixtures. |
| T-31.2-07 | Tampering/Denial of Service | gate DAG | high | mitigate | Observed call graph, sole parent, ancestor ban, new output dir, one producer execution, and status preservation. |
| T-31.2-08 | Information Disclosure | reports/docs | high | mitigate | Recursive structured redaction, external-path/body/credential rejection, ignored artifacts, and E1/E2 wording tests. |
| T-31.2-04 | Tampering | CI protobuf cache | high | mitigate | Manifest-digest cache key plus exact bootstrap/version verification before gate even on cache hit. |
</threat_model>

<verification>
- During implementation run typed Go/Python fixtures, observed DAG fixtures, redaction/identity fixtures and `git diff --check` only in disposable Stage-A fixtures; do not invoke a newly wired aggregate parent from the dirty product worktree.
- After atomic commit run only the exact quality command above; it directly invokes clean-head aggregate once. No Docker/DB/protobuf prerequisite may be recorded as a skip.
- This completes the repository-local E1/E2 RELS-02 contribution only; Phase 39 still owns E3/E4 same-commit commercial proof.
</verification>

<success_criteria>
- Exact ten typed reports share one trusted identity and appear once with pass/no-drift/no-skip outcomes.
- Quality is the sole direct parent and every producer executes once without recursion.
- CI protobuf bootstrap/cache identity and all environment-backed gates fail closed.
- Reports/docs are redacted and state only E1/E2 progress.
- RELS-02 aggregates the exact ten report surfaces after all fifteen plans without claiming target/commercial completion.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-09-SUMMARY.md` when done.
</output>
