---
phase: 31.2-contract-surface-parity-gate
plan: "22"
type: execute
wave: 10
depends_on: ["31.2-16", "31.2-18", "31.2-19", "31.2-20", "31.2-21"]
files_modified: []
autonomous: true
gap_closure: true
requirements: [RELS-02]
must_haves:
  truths:
    - "Task 1 freezes one expected commit/tree before cloning, persists it in ignored `.tmp/phase-31.2-plan22-expected-tuple`, and retains that handoff only after the complete focused Plan 19 persistence/idempotency/recovery/crash-window/worker-ownership matrix succeeds."
    - "Task 2 activates tuple cleanup immediately after assigning the handoff path, so malformed, symlinked, drifted, or failed handoffs are removed on every exit; it proves main and fresh-clone commit/tree equality, installs frozen dependencies, and runs the complete Phase 31.2 matrix."
    - "Plan 22 ends after its repository-local summary is committed; Plan 23 alone owns the later summary-containing closure tuple and canonical clean-head aggregate (D-SUR-17, D-SUR-18)."
  artifacts:
    - path: ".tmp/phase-31.2-plan22-expected-tuple"
      provides: "Ignored ephemeral commit/tree handoff from focused fixtures to the full matrix"
      contains: "expected commit and tree"
    - path: ".planning/phases/31.2-contract-surface-parity-gate/31.2-22-SUMMARY.md"
      provides: "Verification-only record of the shared expected tuple and final closure tuple without target/live claim"
      contains: "repository-local"
    - path: ".planning/phases/31.2-contract-surface-parity-gate/31.2-VERIFICATION.md"
      provides: "Canonical re-verification destination owned by the verifier, not this plan"
      contains: "RELS-02"
  key_links:
    - from: "focused gap fixtures"
      to: "complete Phase 31.2 fixture matrix"
      via: "the ignored tuple handoff binds both clean clones to one expected commit/tree"
      pattern: "phase-31.2-plan22-expected-tuple"
---

<objective>
Close the gap-planning execution chain with focused adversarial checks and the complete Phase 31.2 matrix bound to one persisted expected tuple, then hand the committed Plan 22 summary boundary to Plan 23's separate canonical clean-head gate.

Purpose: Re-establish the focused and full-matrix repository-local evidence without allowing dirty state, skips, or a pre-summary tuple to become the readiness claim.
Output: ignored expected-tuple handoff, verification-only summary, and clean-checkout command evidence for Plan 23's final closure gate.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VERIFICATION.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-REVIEW.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-16-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-17-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-18-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-19-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-20-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-21-SUMMARY.md

<interfaces>
The main checkout contains protected user changes that are outside Phase 31.2 evidence. Verification therefore runs in temporary no-hardlinks clones and never stages, copies, or commits `.planning/config.json`, `src/server/internal/releasecontract/effect_registry.go`, `src/server/internal/releasecontract/effect_registry_test.go`, or `.planning/intel/`. Task 1 freezes `expected_commit` and `expected_tree` before clone and writes exactly that pair to gitignored `.tmp/phase-31.2-plan22-expected-tuple`; Task 2 must consume the same file, reject main HEAD/tree drift, and delete it after use.

`scripts/verify-quality-gates.sh` is the canonical sole direct parent for the exact ten-report aggregate. Plan 23, not this plan, owns all work after `31.2-22-SUMMARY.md` is committed: it freezes the closure commit/tree, proves the committed summary blob, uses a tuple-bound clean clone, invokes the aggregate, and records the sanitized repository-local evidence. This plan must not run or claim that final gate.

Every Task 1, Task 2, and post-summary clean clone must first pass its exact commit/tree assertions and empty-porcelain check, then run `pnpm install --frozen-lockfile` before any frontend or aggregate verifier in that clone. Task 1 must execute every focused Plan 19 selector, including persistence, token-only identity isolation, effect idempotency, restart recovery, crash-window replay, and monolith/standalone worker ownership; the legacy Relay readiness selector alone is insufficient.
</interfaces>
</context>

## Artifacts this phase produces

- Terminal/CI command evidence from clean temporary clones; raw outputs remain outside Git.
- Ephemeral ignored `.tmp/phase-31.2-plan22-expected-tuple`, created before Task 1 clone and removed after Task 2.
- A verification-only `31.2-22-SUMMARY.md` capped at repository-local E1/E2.
- A committed summary boundary consumed by Plan 23; this plan creates no closure tuple and does not invoke the canonical aggregate.

<tasks>

<task type="auto">
  <name>Task 1: Run every focused gap negative fixture from one clean tuple</name>
  <read_first>
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-16-SUMMARY.md
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-18-SUMMARY.md
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-19-SUMMARY.md
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-20-SUMMARY.md
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-21-SUMMARY.md
    - scripts/verify-frontend-surface-sidecar-fixtures.sh
    - scripts/verify-migration-replay-fixtures.sh
    - src/server/internal/relay/quota_compensation_test.go
    - src/server/internal/relay/router_test.go
    - src/server/internal/relay/api_token_test.go
    - src/server/internal/quota/service_test.go
    - src/server/internal/http/server_test.go
    - src/server/cmd/relay/main_test.go
    - src/web/src/services/http/stream.test.ts
    - scripts/verify_release_contract.py
  </read_first>
  <files>None (verification-only; do not modify tracked files)</files>
  <action>From the main repository root, capture the main status before creating the handoff. Immediately after assigning `tuple_file`, initialize an empty `verification_root` and `tuple_handoff_retained=0`, then install an EXIT cleanup handler before any path, symlink, ignore-rule, tuple-content, or clone check. The handler removes a nonempty clone root and removes the tuple unless the retain flag is set. Verify `.tmp/phase-31.2-plan22-expected-tuple` is covered by ignore rules and is not a symlink. Before any clone, freeze `expected_commit=$(git rev-parse HEAD)` and `expected_tree=$(git rev-parse 'HEAD^{tree}')`, then write exactly those two newline-delimited values to the ignored handoff under `umask 077`. Create a temporary no-hardlinks clone, compute `clone_commit` and `clone_tree`, explicitly `test` both equalities, and require empty porcelain. Only after those assertions, run `pnpm install --frozen-lockfile` before any frontend or aggregate command. In that clone run the sidecar alias/global/wrapper/dynamic fixture family, migration contaminated-database/owned-teardown fixture family, CRLF stream Vitest, aggregate symlink/race fixtures, and the complete Plan 19 focused matrix: compensation persistence/lease/mismatch plus token-only identity isolation, migration shape, organization/API-token idempotency, late-readiness and per-attempt routing, empty/reused caller-idempotency collision/cross-refund negatives, restart recovery, effect-before-mark crash replay, and monolith plus standalone worker ownership. Require all selectors/mutation counters to be nonzero and expected coded failures to be observed; Docker/PostgreSQL/tool absence is a blocker, not a skip. After every focused check and the main-status comparison passes, set `tuple_handoff_retained=1` immediately before exit so exactly this successful Task 1 handoff remains for Task 2. Leave all protected paths untouched, and capture only the tuple, commands, pass/fail, skip count, environment class, and residual risks for the summary.</action>
  <acceptance_criteria>
    - The tuple is frozen and persisted before clone, and explicit clone commit/tree comparisons pass before any fixture starts.
    - Frozen dependencies install only after tuple/porcelain assertions and before every frontend or aggregate command in the clone.
    - The complete Plan 19 persistence, idempotency, token-only identity-isolation, recovery, crash-window, and two-runtime worker-ownership matrix passes; no legacy single-selector substitute is accepted.
    - Canonical CR-01 and CR-02 probes plus CR-03, WR-01, and WR-02 fixtures all pass fail-closed expectations at that persisted commit/tree.
    - Every matched selector and fixture family reports positive discovery/mutation counts.
    - The only main-checkout handoff is the ignored two-line tuple file; no protected or tracked path changes, and no unavailable prerequisite is counted as pass/skip.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; repo_root=$(git rev-parse --show-toplevel); cd "$repo_root"; main_status_before=$(git status --porcelain=v1 --untracked-files=all); tuple_file="$repo_root/.tmp/phase-31.2-plan22-expected-tuple"; verification_root=""; tuple_handoff_retained=0; cleanup(){ if test -n "$verification_root"; then rm -rf -- "$verification_root"; fi; if test "$tuple_handoff_retained" -ne 1; then rm -f -- "$tuple_file"; fi; }; trap cleanup EXIT; mkdir -p "$repo_root/.tmp"; test ! -L "$tuple_file"; git check-ignore -q -- .tmp/phase-31.2-plan22-expected-tuple; expected_commit=$(git rev-parse HEAD); expected_tree=$(git rev-parse "HEAD^{tree}"); umask 077; printf "%s\n%s\n" "$expected_commit" "$expected_tree" &gt; "$tuple_file"; verification_root=$(mktemp -d); git clone --quiet --no-hardlinks "$repo_root" "$verification_root/repo"; clone_commit=$(git -C "$verification_root/repo" rev-parse HEAD); clone_tree=$(git -C "$verification_root/repo" rev-parse "HEAD^{tree}"); test "$clone_commit" = "$expected_commit"; test "$clone_tree" = "$expected_tree"; cd "$verification_root/repo"; test -z "$(git status --porcelain=v1 --untracked-files=all)"; pnpm install --frozen-lockfile; node --test scripts/frontend_surface_sidecar.test.mjs; bash scripts/verify-frontend-surface-sidecar-fixtures.sh; bash scripts/verify-migration-replay-fixtures.sh; bash scripts/run-go-tests-matched.sh ./internal/relay "^(TestQuotaCompensationStorePersistsDeterministicSanitizedJobs|TestQuotaCompensationStoreClaimsLeasedJobs|TestQuotaCompensationStoreRejectsReceiptKeyMismatch|TestQuotaCompensationTokenOnlyReservationIdentityIsolation)$"; bash scripts/verify-migration-contract.sh --fixtures; bash scripts/run-go-tests-matched.sh ./internal/quota "^TestSQLStoreRefundBillingSessionIsIdempotent$"; bash scripts/run-go-tests-matched.sh ./internal/relay "^(TestRelayAPITokenQuotaRefundOnceContract|TestRelayLateReadinessQuotaCompensationContract|TestRelaySemanticCacheProviderRecheckContract|TestRelayReadinessPerAttemptContract|TestRelayTokenOnlyReservationIdentityIsolation|TestRelayRouteAttemptIdentityRequiredBeforePreauthorization)$"; bash scripts/run-go-tests-matched.sh ./internal/relay "^(TestQuotaCompensationWorkerRecoversPersistedFailures|TestQuotaCompensationWorkerReplaysCrashWindowExactlyOnce)$"; bash scripts/run-go-tests-matched.sh ./internal/http "^TestBuildRuntimeStartsRelayQuotaCompensationWorker$"; bash scripts/run-go-tests-matched.sh ./cmd/relay "^TestStartStandaloneRelayQuotaCompensationWorker$"; pnpm --dir src/web exec vitest run src/services/http/stream.test.ts --reporter=verbose; python3 scripts/verify_release_contract.py --fixtures --call-graph --redaction; git diff --check; cd "$repo_root"; test "$(git status --porcelain=v1 --untracked-files=all)" = "$main_status_before"; tuple_handoff_retained=1'</automated>
  </verify>
  <done>All five gap domains pass focused adversarial verification at one clean committed tuple with zero accepted skips.</done>
</task>

<task type="auto">
  <name>Task 2: Run the complete Phase 31.2 fixture matrix</name>
  <read_first>
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-01-SUMMARY.md
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-03-SUMMARY.md
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-05-SUMMARY.md
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-06-SUMMARY.md
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-07-SUMMARY.md
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-08-SUMMARY.md
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-09-SUMMARY.md
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-15-SUMMARY.md
    - scripts/verify-quality-gates.sh
  </read_first>
  <files>None (verification-only; do not modify tracked files)</files>
  <action>Immediately after assigning `tuple_file`, initialize `verification_root` and `tuple_handoff_retained=0`, then install the EXIT cleanup handler before `test -f`, symlink inspection, `mapfile`, tuple line-count validation, main HEAD/tree drift checks, or clone creation. The handler removes a nonempty clone root and removes the tuple while the retain flag remains zero; Task 2 never sets the flag because it consumes, rather than creates, the Task 1 handoff. Read exactly two nonempty values from the existing ignored tuple handoff as `expected_commit` and `expected_tree`; reject a missing, symlinked, malformed, or extra-line file. Before cloning, explicitly assert the current main `HEAD` and `HEAD^{tree}` still equal those values. Create a fresh temporary no-hardlinks clone, compute its `clone_commit` and `clone_tree`, explicitly compare both to the same persisted values, and require empty porcelain. Only after those assertions, run `pnpm install --frozen-lockfile` before any frontend or aggregate command, then run the complete Phase 31.2 families in dependency order: OpenAPI projector/determinism, HTTP runtime/owner closure, generated frontend operation ownership, frontend sidecar, frontend transport/exposure pair, protobuf manifest/regeneration, migration static inventory, migration replay, aggregate exact-set/call-graph/redaction, and release call-graph identity Stage A. Treat any missing tool, Docker/DB unavailable result, zero fixture count, committed skip, report mismatch, tuple drift, or dirty clone as failure. Do not run the identity-bearing quality aggregate in Plan 22. After Task 2 passes, create and atomically commit the repository-local Plan 22 summary; Plan 23 exclusively performs the later closure-tuple aggregate.</action>
  <acceptance_criteria>
    - Main HEAD/tree and fresh-clone HEAD/tree explicitly equal the exact two values written by Task 1 before any broad fixture begins.
    - Frozen dependencies install after tuple/porcelain assertions and before the first frontend or aggregate command.
    - Every Phase 31.2 fixture family passes from that same clean source tuple.
    - Protobuf and migration environment-backed families execute with no skip/unavailable acceptance.
    - Exact report set, producer counts, identity, redaction, owner closure, and sidecar provenance remain intact after all repairs.
    - The ignored tuple handoff is removed after Task 2 on both success and failure.
    - The cleanup handler is already active when tuple validation begins, so malformed, symlinked, or drifted handoffs cannot remain after an early exit.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; repo_root=$(git rev-parse --show-toplevel); cd "$repo_root"; tuple_file="$repo_root/.tmp/phase-31.2-plan22-expected-tuple"; verification_root=""; tuple_handoff_retained=0; cleanup(){ if test -n "$verification_root"; then rm -rf -- "$verification_root"; fi; if test "$tuple_handoff_retained" -ne 1; then rm -f -- "$tuple_file"; fi; }; trap cleanup EXIT; test -f "$tuple_file"; test ! -L "$tuple_file"; mapfile -t expected_tuple &lt; "$tuple_file"; test "${#expected_tuple[@]}" -eq 2; expected_commit=${expected_tuple[0]}; expected_tree=${expected_tuple[1]}; test -n "$expected_commit"; test -n "$expected_tree"; test "$(git rev-parse HEAD)" = "$expected_commit"; test "$(git rev-parse "HEAD^{tree}")" = "$expected_tree"; verification_root=$(mktemp -d); git clone --quiet --no-hardlinks "$repo_root" "$verification_root/repo"; clone_commit=$(git -C "$verification_root/repo" rev-parse HEAD); clone_tree=$(git -C "$verification_root/repo" rev-parse "HEAD^{tree}"); test "$clone_commit" = "$expected_commit"; test "$clone_tree" = "$expected_tree"; cd "$verification_root/repo"; test -z "$(git status --porcelain=v1 --untracked-files=all)"; pnpm install --frozen-lockfile; bash scripts/verify-openapi-contract-fixtures.sh; bash scripts/verify-http-runtime-contract-fixtures.sh; node scripts/verify_frontend_operation_contract_usage.mjs --fixtures --require-all; bash scripts/verify-frontend-surface-sidecar-fixtures.sh; bash scripts/verify-frontend-surface-fixtures.sh; bash scripts/verify-protobuf-contract.sh --fixtures; bash scripts/verify-migration-contract.sh --fixtures; bash scripts/verify-migration-replay-fixtures.sh; python3 scripts/verify_release_contract.py --fixtures --call-graph --redaction; bash scripts/verify-release-contract.sh --fixtures --identity --stage-a'</automated>
  </verify>
  <done>The full Phase 31.2 fixture suite passes without skips at one clean committed tuple and the committed Plan 22 summary is ready for Plan 23's separate clean-head aggregation.</done>
</task>

</tasks>

<scope_fence>
Plan 22 stops at its committed summary boundary. It must not freeze a closure tuple, prove the summary blob, invoke `bash scripts/verify-quality-gates.sh`, amend a commit, or write `31.2-VERIFICATION.md`. Plan 23 is the sole owner of those final repository-local clean-head actions; neither plan may claim target/live, E3/E4, deployment-profile parity, or commercial-release proof.
</scope_fence>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| dirty main checkout -> verification | Protected user changes must not contaminate release evidence. |
| focused fixtures -> complete matrix | Both suites must be bound to the same persisted expected commit/tree despite time between tasks. |
| summary commit -> clean-head aggregate | Positive pre-summary tests cannot substitute for the exact summary-containing closure tuple. |
| repository-local outputs -> readiness claim | E1/E2 evidence must not be inflated into target/live or commercial proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-22-01 | Spoofing | expected source tuple | high | mitigate | Freeze before Task 1 clone, persist an ignored two-value handoff, and compare main plus both clean clones explicitly to it before tests. |
| T-31.2-22-02 | Tampering | protected user changes | high | mitigate | Verify committed clones only; the sole main-checkout runtime artifact is an ignored tuple file removed after Task 2. |
| T-31.2-22-04 | Repudiation | skipped/unavailable fixtures | high | mitigate | Positive counters, no accepted skip, environment blockers nonzero, exact tuple comparisons, and exact commands recorded. |
| T-31.2-22-05 | Information Disclosure | raw verification output | medium | mitigate | Keep raw logs outside Git; summary records only stable IDs, digests, counts, classes, and residual risks. |
| T-31.2-22-06 | Tampering | clean-clone dependency graph | high | mitigate | After exact tuple and porcelain assertions, install only the committed frozen lockfile before frontend or aggregate verification in every clone. |
</threat_model>

<verification>
- Focused gap fixtures and the complete Phase 31.2 matrix are bound to the same expected commit/tree through the ignored handoff, which Task 2 removes.
- Every clean clone installs the committed frozen dependency graph only after exact tuple and empty-porcelain assertions; Task 1 runs the complete Plan 19 focused selector matrix.
- Plan 23 owns the only final identity-bearing closure tuple, committed-summary blob proof, tuple-bound clean clone, and canonical aggregate invocation after this plan's summary commit.
- Independent `$gsd-verify-work`/canonical verifier owns the new verification verdict; this plan must not edit `31.2-VERIFICATION.md`, ROADMAP, STATE, or REQUIREMENTS.
</verification>

<success_criteria>
- All five gap domains and the complete Plan 19 persistence/idempotency/recovery/crash-window/worker-ownership matrix fail closed under focused adversarial fixtures at the persisted expected commit/tree.
- The complete Phase 31.2 fixture matrix passes without skip/unavailable acceptance at that same persisted tuple, and the handoff is removed.
- The committed Plan 22 summary creates the only handoff boundary Plan 23 may consume for the summary-containing closure tuple and canonical aggregate.
- Evidence remains explicitly repository-local E1/E2 and is handed to Plan 23 and then independent re-verification.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-22-SUMMARY.md` when done.
</output>
