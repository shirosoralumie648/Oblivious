---
phase: 31.2-contract-surface-parity-gate
plan: "23"
type: execute
wave: 11
depends_on: ["31.2-22"]
files_modified: []
autonomous: true
gap_closure: true
requirements: [RELS-02]
must_haves:
  truths:
    - "After the committed Plan 22 summary boundary, one new closure commit/tree tuple proves `31.2-22-SUMMARY.md` is a blob in that exact commit rather than a main-worktree file (D-SUR-17, D-SUR-18)."
    - "A fresh no-hardlinks clone exactly matches the frozen closure commit/tree and has empty porcelain before frozen dependencies install; `bash scripts/verify-quality-gates.sh` runs only in that tuple-bound clone (D-SUR-17, D-SUR-18)."
    - "The Plan 23 summary records only sanitized repository-local E1/E2 command evidence, while cleanup removes tuple and clone paths on every exit and no target/live claim or secret input is used (D-SUR-20)."
  artifacts:
    - path: ".planning/phases/31.2-contract-surface-parity-gate/31.2-23-SUMMARY.md"
      provides: "Sanitized repository-local record of the committed-summary closure tuple and canonical aggregate result"
      contains: "repository-local"
    - path: "scripts/verify-quality-gates.sh"
      provides: "Canonical sole direct aggregate owner invoked only in the tuple-bound clean clone"
      contains: "verify-release-contract.sh"
  key_links:
    - from: "31.2-22-SUMMARY.md"
      to: "frozen closure commit/tree"
      via: "git rev-parse and git cat-file resolve the committed tree entry to an object whose type is blob"
      pattern: "git cat-file -t"
    - from: "frozen closure commit/tree"
      to: "scripts/verify-quality-gates.sh"
      via: "a no-hardlinks clone proves commit/tree equality and empty porcelain before frozen installation and aggregate execution"
      pattern: "--no-hardlinks"
---

<objective>
Execute the final repository-local clean-head quality gate from a newly frozen closure tuple that already contains the committed Plan 22 summary.

Purpose: Ensure the only canonical aggregate result is bound to committed, clean source rather than a mutable worktree or a pre-summary tuple.
Output: sanitized Plan 23 command evidence, a repository-local summary, and cleanup of the ephemeral closure tuple and clean clone.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VERIFICATION.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-22-PLAN.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-22-SUMMARY.md
@scripts/verify-quality-gates.sh

<interfaces>
Plan 22 has already run its focused and complete matrix, written `31.2-22-SUMMARY.md`, and committed that summary. This plan begins only after that commit exists. `scripts/verify-quality-gates.sh` remains the sole direct aggregate parent; it must run once only from a fresh no-hardlinks clone whose commit and tree exactly equal the tuple frozen by this task.

The main checkout can contain protected user changes outside Phase 31.2. It supplies Git objects for the clone but is never the quality-gate working directory. The task may use ignored `.tmp/phase-31.2-plan23-closure-tuple` and `.tmp/phase-31.2-plan23-clean-head-evidence.json`; neither may be a symlink. The tuple and clone paths are always removed by traps. The sanitized evidence handoff exists only until the executor writes `31.2-23-SUMMARY.md`, then must be removed before plan completion.
</interfaces>
</context>

## Artifacts this phase produces

- One committed-summary closure commit/tree tuple, held only in an ignored temporary handoff during verification.
- One no-hardlinks clean clone whose identity and clean porcelain are explicitly checked before dependency installation.
- A sanitized repository-local Plan 23 summary; raw command output remains only in the temporary clone and is removed on exit.

<scope_fence>
This is a verification-only repository-local task. Do not modify product source, tests, configuration, lockfiles, `31.2-VERIFICATION.md`, ROADMAP, STATE, or REQUIREMENTS. Do not read real `.env` files, secrets, target manifests, target evidence, or external credentials. Do not use a dirty checkout as a fallback, do not rerun the Plan 22 focused/full matrix, and do not claim E3/E4, target/live, deployment-profile, or commercial-release proof. Plan 23's summary is audit metadata outside the frozen closure tuple; never amend the tuple after the gate.
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Verify the committed-summary closure tuple in a fresh clean clone</name>
  <read_first>
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-22-SUMMARY.md
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-22-PLAN.md
    - scripts/verify-quality-gates.sh
    - package.json
    - pnpm-lock.yaml
    - .gitignore
  </read_first>
  <files>None (verification-only; do not modify tracked product, test, configuration, or verification files)</files>
  <action>From the main repository root, record `main_status_before` only to prove this task leaves protected changes untouched. Immediately assign `closure_tuple_file`, `evidence_file`, and an empty `verification_root`; install EXIT, HUP, INT, and TERM cleanup before checking ignore rules, symlinks, tuple contents, or cloning. Cleanup must always remove the exact tuple file and the `mktemp -d` clone root; it removes the evidence handoff unless all checks pass and the executor needs it to write the Plan 23 summary. Require both ignored handoff paths and `.tmp` itself to be non-symlinks, remove stale regular handoffs, and set `umask 077`. Freeze a new `closure_commit=$(git rev-parse HEAD)` and `closure_tree=$(git rev-parse "HEAD^{tree}")`, write exactly those two values to the tuple handoff, then resolve `${closure_commit}:.planning/phases/31.2-contract-surface-parity-gate/31.2-22-SUMMARY.md` with `git rev-parse` and require `git cat-file -t` to return `blob`; never use a worktree existence check as this proof. Create a temporary no-hardlinks clone, explicitly compare its HEAD and tree to the frozen tuple, and require empty porcelain before installing dependencies. Inside that clone only, run `pnpm install --frozen-lockfile`, recheck porcelain, then run `bash scripts/verify-quality-gates.sh` exactly once; missing dependencies, nonzero exit, committed skip, tuple mismatch, dirty clone, or unavailable prerequisite must fail nonzero with no fallback to the main checkout. Recheck clone porcelain, return to the main root, and prove its porcelain still equals `main_status_before`. On success write only the tuple, summary blob object ID, clone equality/clean status, fixed command names, pass result, repository-local evidence class, and residual-risk ceiling to the ignored JSON handoff. The executor must consume that handoff into `31.2-23-SUMMARY.md` using repository-local wording, then delete the handoff before plan completion; preserve no raw output, paths outside the repository, credentials, environment values, or target/live assertions.</action>
  <acceptance_criteria>
    - A new frozen commit/tree tuple contains the Plan 22 summary as a committed blob, independent of any main-worktree file state.
    - The quality gate runs once only in a no-hardlinks clone that exactly matches both frozen Git values and has clean porcelain before and after frozen dependency installation.
    - Tuple/clone cleanup occurs for success, command failure, malformed handoff, interrupt, and termination paths; no dirty-checkout fallback exists.
    - The Plan 23 summary receives only sanitized E1/E2 evidence and the temporary evidence handoff is removed after summary creation.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; repo_root=$(git rev-parse --show-toplevel); cd "$repo_root"; main_status_before=$(git status --porcelain=v1 --untracked-files=all); closure_tuple_file="$repo_root/.tmp/phase-31.2-plan23-closure-tuple"; evidence_file="$repo_root/.tmp/phase-31.2-plan23-clean-head-evidence.json"; verification_root=""; evidence_retained=0; cleanup(){ if test -n "$verification_root"; then rm -rf -- "$verification_root"; fi; rm -f -- "$closure_tuple_file"; if test "$evidence_retained" -ne 1; then rm -f -- "$evidence_file"; fi; }; trap cleanup EXIT; trap "cleanup; exit 1" HUP INT TERM; mkdir -p "$repo_root/.tmp"; test -d "$repo_root/.tmp"; test ! -L "$repo_root/.tmp"; test ! -L "$closure_tuple_file"; test ! -L "$evidence_file"; git check-ignore -q -- .tmp/phase-31.2-plan23-closure-tuple; git check-ignore -q -- .tmp/phase-31.2-plan23-clean-head-evidence.json; rm -f -- "$closure_tuple_file" "$evidence_file"; umask 077; closure_commit=$(git rev-parse HEAD); closure_tree=$(git rev-parse "HEAD^{tree}"); printf "%s\n%s\n" "$closure_commit" "$closure_tree" &gt; "$closure_tuple_file"; summary_path=.planning/phases/31.2-contract-surface-parity-gate/31.2-22-SUMMARY.md; summary_blob=$(git rev-parse "${closure_commit}:${summary_path}"); test "$(git cat-file -t "$summary_blob")" = blob; verification_root=$(mktemp -d "${TMPDIR:-/tmp}/phase-31.2-plan23.XXXXXX"); git clone --quiet --no-hardlinks "$repo_root" "$verification_root/repo"; clone_commit=$(git -C "$verification_root/repo" rev-parse HEAD); clone_tree=$(git -C "$verification_root/repo" rev-parse "HEAD^{tree}"); test "$clone_commit" = "$closure_commit"; test "$clone_tree" = "$closure_tree"; cd "$verification_root/repo"; test -z "$(git status --porcelain=v1 --untracked-files=all)"; pnpm install --frozen-lockfile; test -z "$(git status --porcelain=v1 --untracked-files=all)"; bash scripts/verify-quality-gates.sh; test -z "$(git status --porcelain=v1 --untracked-files=all)"; cd "$repo_root"; test "$(git status --porcelain=v1 --untracked-files=all)" = "$main_status_before"; printf "{\"schema\":\"phase-31.2-clean-head-evidence/v1\",\"evidenceClass\":\"repository-local\",\"closureCommit\":\"%s\",\"closureTree\":\"%s\",\"summaryBlob\":\"%s\",\"cloneCommit\":\"%s\",\"cloneTree\":\"%s\",\"clonePorcelain\":\"empty\",\"dependencyCommand\":\"pnpm install --frozen-lockfile\",\"qualityCommand\":\"bash scripts/verify-quality-gates.sh\",\"qualityResult\":\"pass\",\"targetLiveClaim\":false,\"residualRisk\":\"E1/E2 repository-local evidence only\"}\n" "$closure_commit" "$closure_tree" "$summary_blob" "$clone_commit" "$clone_tree" &gt; "$evidence_file"; evidence_retained=1'</automated>
  </verify>
  <done>The committed Plan 22 summary is proven as a blob in a new frozen tuple, and the canonical quality gate passes exactly once inside the matching clean clone with sanitized repository-local evidence ready for the Plan 23 summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Plan 22 summary commit -> closure tuple | A worktree-only summary or changed HEAD could be mistaken for committed closure evidence. |
| main checkout -> clean clone | Protected user changes must not enter the aggregate evidence path. |
| clean clone -> quality aggregate | Frozen dependencies and the canonical parent must be bound to the exact tuple with no dirty fallback. |
| command evidence -> Plan 23 summary | Raw output or an overstated readiness claim could expose data or inflate repository-local proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-23-01 | Spoofing | closure tuple / Plan 22 summary | high | mitigate | Freeze HEAD/tree once, resolve the summary through that commit tree, and require object type `blob` before cloning. |
| T-31.2-23-02 | Tampering | clone source and aggregate input | high | mitigate | No-hardlinks clone, exact commit/tree comparisons, clean porcelain before and after install/gate, and no main-checkout fallback. |
| T-31.2-23-03 | Repudiation | quality result and skip behavior | high | mitigate | One canonical command, fail-on-nonzero behavior, sanitized command/result evidence, and no accepted unavailable/skip branch. |
| T-31.2-23-04 | Information Disclosure | evidence handoff / summary | medium | mitigate | Persist only Git object IDs, command names, result, evidence class, and residual-risk ceiling; never read `.env` or write raw logs. |
| T-31.2-23-05 | Denial of Service | temporary tuple and clone resources | medium | mitigate | Register cleanup before validation, use an explicit `mktemp -d` root, and remove exact temporary paths on normal and signal exits. |
</threat_model>

<verification>
- Assert the committed-summary blob through the frozen commit tree, not the worktree.
- Assert clone commit/tree equality and empty porcelain before dependency installation, after installation, and after the canonical aggregate.
- Run only `pnpm install --frozen-lockfile` and `bash scripts/verify-quality-gates.sh` inside the tuple-bound clean clone.
- Record only sanitized repository-local E1/E2 evidence in the Plan 23 summary, delete the ignored handoff afterward, and hand the result to the independent canonical verifier.
</verification>

<success_criteria>
- Plan 23 is the sole executable owner of the post-Plan-22 summary closure tuple and clean-head aggregate.
- The canonical aggregate cannot pass from a dirty checkout, mismatched clone, worktree-only summary, missing frozen dependency install, unavailable prerequisite, or accepted skip.
- Temporary tuple/clone resources are removed on every exit, and the final summary neither exposes sensitive data nor claims target/live release evidence.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-23-SUMMARY.md` when done. Consume the ignored sanitized evidence handoff, state the exact tuple and command result as repository-local E1/E2 only, then delete the handoff without changing the frozen closure tuple.
</output>
