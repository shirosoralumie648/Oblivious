---
phase: 31.2-contract-surface-parity-gate
plan: "01"
subsystem: api-contracts
tags: [openapi, fingerprint, typescript, manifest, release-evidence]

# Dependency graph
requires:
  - phase: 31-release-contract-current-baseline
    provides: "Authored release capability and surface IDs in config/release/contract.v1.json"
  - phase: 31.1-readiness-fail-closed
    provides: "Repository-local readiness contracts and the two deferred raw control-plane response shapes"
provides:
  - "One closed public-operation scope with deterministic full server-core fingerprints for all 374 dispositions"
  - "Derived route manifest v2 plus typed per-operation and release-surface projections"
  - "Bidirectional parser-based OpenAPI parity and narrow TypeScript symbol-usage preflight"
affects: [31.2-02, 31.2-03, 31.2-04, 31.2-09, 31.2-10, RELS-02]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Project authored OpenAPI and release-contract references into deterministic compare-only consumers"
    - "Represent refs exactly and inline schemas by canonical SHA-256 identity"
    - "Require exact generated TypeScript symbols at explicit transport-owner boundaries"

key-files:
  created:
    - scripts/openapi_surface_fingerprint.py
    - docs/api/route-surface-manifest.schema.json
    - scripts/verify-openapi-contract-fixtures.sh
    - scripts/verify_frontend_operation_contract_usage.mjs
    - src/web/src/generated/operation-contracts.generated.ts
    - src/web/src/generated/release-projection.generated.ts
  modified:
    - docs/api/openapi.yaml
    - docs/api/route-surface-manifest.json
    - scripts/verify_openapi_contract.py

key-decisions:
  - "Keep OpenAPI as the only HTTP schema authority; route-surface-manifest.json and TypeScript files are deterministic derived compare inputs."
  - "Use explicit operationId overrides for legacy Knowledge ref aliases instead of inferring identities from path, tags, or handler names."
  - "Represent WebSocket 101 and legacy top-up 402 through explicit terminal-success annotations while keeping the canonical success tuple closed."
  - "Keep the existing readiness endpoints as narrowly named raw control-plane exceptions to the older Envelope rule because their committed runtime handlers return raw objects."

patterns-established:
  - "Every /api/* and /v1/* operation is included; every other public endpoint has one explicit included/excluded disposition."
  - "Parity diagnostics expose stable operation IDs, field names, counts, and digests without request bodies, target URLs, or secrets."

requirements-completed: [RELS-02]

coverage:
  - id: D1
    description: "The canonical projector and closed schema produce one complete server-core fingerprint per included public operation and reject every core mutation family."
    requirement: RELS-02
    verification:
      - kind: integration
        ref: "bash scripts/verify-openapi-contract-fixtures.sh --projector-only"
        status: pass
      - kind: other
        ref: "python3 -m py_compile scripts/openapi_surface_fingerprint.py"
        status: pass
    human_judgment: false
  - id: D2
    description: "OpenAPI projects deterministically to manifest v2 and both typed consumers with stable non-empty counts and byte-for-byte stale-artifact checks."
    requirement: RELS-02
    verification:
      - kind: integration
        ref: "bash scripts/verify-openapi-contract-fixtures.sh --determinism"
        status: pass
      - kind: integration
        ref: "python3 scripts/openapi_surface_fingerprint.py --openapi docs/api/openapi.yaml --contract config/release/contract.v1.json --manifest docs/api/route-surface-manifest.json --check"
        status: pass
    human_judgment: false
  - id: D3
    description: "The production OpenAPI gate compares full fingerprints bidirectionally and the TypeScript AST preflight rejects missing, reconstructed, generic-only, lookup, duplicate, and unresolved metadata use."
    requirement: RELS-02
    verification:
      - kind: integration
        ref: "bash scripts/verify-openapi-contract-fixtures.sh"
        status: pass
      - kind: integration
        ref: "bash scripts/verify-openapi-contract.sh"
        status: pass
      - kind: integration
        ref: "node scripts/verify_frontend_operation_contract_usage.mjs --fixtures"
        status: pass
      - kind: other
        ref: "pnpm --dir src/web exec tsc --noEmit"
        status: pass
    human_judgment: false

# Metrics
duration: 27 min
completed: 2026-07-21
status: complete
---

# Phase 31.2 Plan 01: OpenAPI Server-Core Fingerprint Summary

**A closed 374-disposition OpenAPI scope now emits 372 exact operation fingerprints and deterministic compare-only consumers, with bidirectional drift and typed-symbol gates.**

## Performance

- **Duration:** 27 min
- **Started:** 2026-07-21T18:11:50+08:00
- **Completed:** 2026-07-21T18:38:37+08:00
- **Tasks:** 3 completed
- **Files modified:** 9 production and verification files plus this SUMMARY

## Accomplishments

- Defined `PublicOperationScopeV1`, `SchemaIdentityV1`, and `OperationContractMetadataV1` projection with closed schema validation, canonical inline hashes, atomic generation, and structured diagnostics.
- Annotated all public OpenAPI operations and generated manifest v2 plus readonly operation/release TypeScript projections from the same source.
- Replaced route-key-only comparison with complete bidirectional fingerprint parity and added a TypeScript Program/TypeChecker preflight for explicit generated metadata symbols.
- Verified 374 dispositions, 372 included operations, two exclusions, 372 unique operation IDs, all core mutation families, and deterministic regeneration.

## Task Commits

Each task was committed atomically:

1. **Task 1: Define and mutation-test the canonical operation projector** - `a347bb3` (`feat`)
2. **Task 2: Annotate OpenAPI and generate both derived consumers deterministically** - `3832fa6` (`feat`)
3. **Task 3: Replace route-key parity with bidirectional fingerprint parity** - `d5117ca` (`feat`)

This SUMMARY is committed separately as the execute-plan metadata commit.

## Files Created/Modified

- `scripts/openapi_surface_fingerprint.py` - Canonical scope/project/validate/digest/write implementation and structured fixtures.
- `docs/api/route-surface-manifest.schema.json` - Closed draft-2020-12 schema for the complete operation fingerprint.
- `scripts/verify-openapi-contract-fixtures.sh` - Projector, determinism, and full parity fixture entrypoint.
- `docs/api/openapi.yaml` - Explicit public-operation dispositions, capabilities, identity overrides, and terminal-success annotations.
- `docs/api/route-surface-manifest.json` - Deterministic route-surface-manifest/v2 compare input.
- `src/web/src/generated/operation-contracts.generated.ts` - Readonly operation metadata plus one exact symbol per operation.
- `src/web/src/generated/release-projection.generated.ts` - Readonly release capability/surface/disposition projection.
- `scripts/verify_openapi_contract.py` - Parser-based bidirectional parity and generated-byte verification.
- `scripts/verify_frontend_operation_contract_usage.mjs` - Explicit-owner TypeScript symbol preflight and negative fixtures.

## Decisions Made

- The authored OpenAPI remains the sole HTTP schema authority; generated JSON and TypeScript artifacts cannot become independent schema sources.
- Runtime Go descriptors are intentionally not joined in this plan. Plan 02 owns loading this compare input after runtime route registration.
- The AST checker remains a narrow expected-owner prerequisite. Plan 03 owns production caller discovery and unknown-caller closure.
- The generated release projection carries commitments and dispositions only; it does not encode runtime availability.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Legacy Knowledge path-item refs reused operation IDs. Explicit `x-oblivious-operation-id-overrides` preserve stable unique identities without path or tag inference.
- WebSocket `101` and legacy top-up `402` are terminal non-2xx outcomes. Explicit terminal-success annotations keep them representable without broadening success heuristics.
- The existing readiness endpoints return raw control-plane objects in committed runtime handlers. They remain two named exceptions to the older Envelope-only check; the new generic fingerprint parity still covers their full metadata.

## Verification

- `python3 -m py_compile scripts/openapi_surface_fingerprint.py` - PASS.
- `bash -n scripts/verify-openapi-contract-fixtures.sh` - PASS.
- `bash scripts/verify-openapi-contract-fixtures.sh --projector-only` - PASS; 372 metadata rows, two exclusions, and all 11 core mutation families positive.
- `python3 scripts/openapi_surface_fingerprint.py ... --check` - PASS; 374 dispositions, 372 operations, digest `sha256:a877f8ebf7a71dd3e3f91219b099bd69c94ae487f11e151b3e8aba9756fd0091`.
- `bash scripts/verify-openapi-contract-fixtures.sh --determinism` - PASS; two byte-identical generations, 29 release capabilities, and eight release surfaces.
- `bash scripts/verify-openapi-contract.sh` - PASS.
- `node scripts/verify_frontend_operation_contract_usage.mjs --fixtures` - PASS; owners/imports/uses/transport calls all positive and eight fixture families exercised.
- `pnpm --dir src/web exec tsc --noEmit` - PASS.
- `git diff --check` - PASS before SUMMARY creation.

## User Setup Required

None - no external service configuration is required for this repository-local contract slice.

## Evidence Boundary

This plan contributes repository-local E1 contract evidence to RELS-02. It does not prove runtime route parity, complete frontend caller closure, target/live behavior, deployment readiness, or commercial release readiness.

## Next Phase Readiness

Plan 02 can consume the deterministic manifest after runtime route registration and compare Go route descriptors against the same fingerprint. The remaining 14 Phase 31.2 plans are still pending, so RELS-02 and Phase 31.2 are not complete.

---
*Phase: 31.2-contract-surface-parity-gate*
*Completed: 2026-07-21*

## Self-Check: PASSED

- All nine declared production and verification artifacts plus this SUMMARY exist.
- Task commits `a347bb3`, `3832fa6`, and `d5117ca` resolve as commits.
- Coverage classification reports 3/3 deliverables automatically covered by passing evidence.
- Projector, determinism, OpenAPI, AST, TypeScript, and patch checks all passed after the final task commit.
