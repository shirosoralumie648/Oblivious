---
phase: 31.2-contract-surface-parity-gate
plan: "20"
type: execute
wave: 3
depends_on: ["31.2-04"]
files_modified:
  - src/web/src/services/http/stream.ts
  - src/web/src/services/http/stream.test.ts
autonomous: true
gap_closure: true
requirements: [RELS-02]
must_haves:
  truths:
    - "The shared SSE decoder recognizes both LF and CRLF blank-line event boundaries without merging adjacent events."
    - "CRLF boundaries split across byte chunks remain buffered correctly, `[DONE]` terminates emission, and `event: error` still rejects without emitting data."
    - "Existing exact operation metadata, media validation, LF framing, and stream behavior remain unchanged."
  artifacts:
    - path: "src/web/src/services/http/stream.ts"
      provides: "CRLF-aware buffered SSE frame boundary parsing"
      contains: "emitBufferedChunks"
    - path: "src/web/src/services/http/stream.test.ts"
      provides: "CRLF multi-frame, split-boundary, done, and error regression coverage"
      contains: "streamText"
  key_links:
    - from: "src/web/src/services/http/stream.ts"
      to: "src/web/src/services/http/client.ts"
      via: "validated raw response is decoded into protocol-correct SSE frames"
      pattern: "createHttpClient"
    - from: "src/web/src/services/http/stream.test.ts"
      to: "src/web/src/services/http/stream.ts"
      via: "real ReadableStream chunks exercise boundaries across decoder reads"
      pattern: "streamFromChunks"
---

<objective>
Make the shared SSE parser protocol-correct for CRLF as well as LF event framing.

Purpose: Prevent valid CRLF streams from merging events, losing `[DONE]`, or suppressing error frames.
Output: CRLF-focused tests and a delimiter-length-aware buffered parser.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VERIFICATION.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-REVIEW.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-04-SUMMARY.md
@src/web/src/services/http/client.ts

<interfaces>
`streamText` appends decoded bytes to `buffer` and calls `emitBufferedChunks`. `emitFrame` already parses individual lines with `/\r?\n/`; only `emitBufferedChunks` assumes the two-byte LF delimiter. Preserve the current `onChunk`, `[DONE]`, `event: error`, flush, and operation-contract interfaces.
</interfaces>
</context>

## Artifacts this phase produces

- Focused protocol regression tests over a real `ReadableStream`.
- A boundary parser that consumes the exact matched LF or CRLF delimiter length.
- Repository-local frontend evidence only.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce CRLF multi-frame and split-boundary failures</name>
  <read_first>
    - src/web/src/services/http/stream.test.ts
    - src/web/src/services/http/stream.ts
    - src/web/src/services/http/client.test.ts
  </read_first>
  <files>src/web/src/services/http/stream.test.ts</files>
  <behavior>
    - RED: two CRLF-delimited data frames emit two ordered chunks instead of one merged frame.
    - RED: `\r\n\r\n` split across separate `ReadableStream` chunks is recognized after buffering.
    - RED: CRLF `[DONE]` produces no user chunk and does not consume the preceding event incorrectly.
    - RED: CRLF `event: error` throws its data and calls `onChunk` zero times.
  </behavior>
  <action>Add focused cases to the existing `streamText` suite before production changes. Build real `Response` objects with `text/event-stream` and use `streamFromChunks` to cover full CRLF delimiters, every meaningful split position across the delimiter, multiple frames, `[DONE]`, and error frames. Keep an LF regression assertion so the implementation cannot fix CRLF by changing established LF behavior. Require exact ordered chunks and zero emission on done/error.</action>
  <acceptance_criteria>
    - Current code fails the new CRLF cases for the intended framing reason.
    - Tests cover multiple events, delimiter splits, done, error, and LF compatibility.
    - All tests use the real shared parser path rather than a mocked helper.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; output=$(pnpm --dir src/web exec vitest run src/services/http/stream.test.ts --reporter=verbose 2&gt;&amp;1); status=$?; set -e; test "$status" -ne 0; printf "%s\n" "$output" | rg "CRLF|split|DONE|error"'</automated>
  </verify>
  <done>The test suite deterministically reproduces CRLF frame merging and split-delimiter handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consume LF and CRLF blank-line delimiters exactly</name>
  <read_first>
    - src/web/src/services/http/stream.ts
    - src/web/src/services/http/stream.test.ts
  </read_first>
  <files>src/web/src/services/http/stream.ts, src/web/src/services/http/stream.test.ts</files>
  <behavior>
    - GREEN: boundary detection finds `\n\n` and `\r\n\r\n`, removes the exact matched delimiter length, and loops over all complete frames.
    - GREEN: incomplete delimiter prefixes remain buffered across reads.
    - GREEN: `emitFrame` receives no trailing carriage return from a delimiter and retains existing multiline data normalization.
    - GREEN: final flush behavior remains bounded and does not manufacture an event from whitespace.
  </behavior>
  <action>Replace the fixed `indexOf`/two-character slice in `emitBufferedChunks` with a CRLF-aware blank-line match that returns both start offset and exact matched length. Emit the prefix frame, remove the whole delimiter, and continue until no complete boundary remains; leave incomplete suffix bytes untouched for the next decoder read. Keep `emitFrame` and public APIs unchanged unless a small private helper makes delimiter matching clearer. Make every RED case green, then run the client/stream focused suite and TypeScript compilation.</action>
  <acceptance_criteria>
    - LF and CRLF frames are parsed identically at every chunk split.
    - Multiple events do not merge; done and error semantics remain exact.
    - Existing operation/media validation and TypeScript types remain passing.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir src/web exec vitest run src/services/http/stream.test.ts src/services/http/client.test.ts --reporter=verbose &amp;&amp; pnpm --dir src/web exec tsc --noEmit &amp;&amp; git diff --check</automated>
  </verify>
  <done>The shared stream transport parses LF/CRLF SSE boundaries correctly without changing its public contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| provider/Relay byte stream -> browser parser | Framing bytes are untrusted and may be split arbitrarily by the transport. |
| parsed SSE event -> UI callbacks | Merged or suppressed frames can hide error/done state or corrupt displayed output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-20-01 | Tampering | SSE frame boundaries | medium | mitigate | Protocol-aware LF/CRLF delimiter parsing and split-boundary tests. |
| T-31.2-20-02 | Denial of Service | buffered stream suffix | medium | mitigate | Consume every complete frame and retain only incomplete suffix bytes. |
| T-31.2-20-03 | Spoofing | done/error event semantics | medium | mitigate | Exact CRLF `[DONE]` and `event: error` zero-emission tests. |
</threat_model>

<verification>
- Use real `Response`/`ReadableStream` data through `streamText`; do not mock `emitBufferedChunks`.
- Run focused Vitest, TypeScript compilation, and `git diff --check`.
- This does not claim live Provider stream evidence.
</verification>

<success_criteria>
- WR-01 CRLF frames pass for complete and split delimiters.
- `[DONE]`, error, multiline data, and LF behavior remain correct.
- No public transport or operation-contract interface changes.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-20-SUMMARY.md` when done.
</output>
