---
phase: 31.2-contract-surface-parity-gate
plan: "17"
type: execute
wave: 3
depends_on: ["31.2-07", "31.2-08"]
files_modified:
  - src/server/internal/surfacereport/migration.go
  - src/server/internal/surfacereport/migration_test.go
  - src/server/cmd/release-migration-surface/main.go
  - src/server/cmd/release-migration-surface/main_test.go
autonomous: true
gap_closure: true
requirements: [RELS-02]
must_haves:
  truths:
    - "Migration replay evidence states whether the migrated resource is owned and disposable; caller-owned resources can never produce a passing cleanup claim (D-SUR-12, D-SUR-14)."
    - "A pass requires `resourceOwnership=owned-disposable` and cleanup verified as succeeded after effects; unavailable/failure evidence stays typed, nonzero, no-skip, and sanitized."
    - "The closed observation/details schemas reject missing, unknown, forged, or contradictory ownership and cleanup values before a report can pass (D-SUR-15, D-SUR-20)."
  artifacts:
    - path: "src/server/internal/surfacereport/migration.go"
      provides: "Closed migration replay ownership and cleanup invariants"
      contains: "MigrationReplayDetails"
    - path: "src/server/cmd/release-migration-surface/main.go"
      provides: "Strict replay observation decoder carrying trusted resource ownership"
      contains: "migration-replay-observation/v1"
    - path: "src/server/internal/surfacereport/migration_test.go"
      provides: "Pass/failure contradiction matrix for ownership and cleanup"
      contains: "TestMigrationReplaySurfaceContract"
  key_links:
    - from: "src/server/cmd/release-migration-surface/main.go"
      to: "src/server/internal/surfacereport/migration.go"
      via: "closed observation derives typed details and outcome without caller identity overrides"
      pattern: "NewMigrationReplayReport"
    - from: "src/server/internal/surfacereport/migration.go"
      to: "scripts/verify-migration-replay.sh"
      via: "Plan 18 supplies ownership and verified teardown in the typed observation"
      pattern: "CleanupResult"
---

<objective>
Make migration replay resource ownership an explicit typed fact so a caller-owned database cannot be mislabeled as successfully cleaned up.

Purpose: Close the evidence-schema half of CR-01 before changing the external replay harness.
Output: closed `resourceOwnership` contract, pass/failure invariants, strict CLI observation decoding, and focused Go tests.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VERIFICATION.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-REVIEW.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-07-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-08-SUMMARY.md

<interfaces>
`MigrationReplayDetails` currently closes `databaseKind`, `replayMode`, ledger/apply counts, digests, and `cleanupResult`. `migrationReplayPassingDetails` requires `cleanupResult == succeeded`; `migrationReplayObservation` is the only shell-to-Go replay input. Extend these existing contracts rather than creating another report or checksum authority.

Use exact ownership values `owned-disposable` and `caller-owned`. Only `owned-disposable` may satisfy a pass. `caller-owned` is permitted only in a typed `migration_replay_unavailable` failure observation so the failure report truthfully records why no cleanup/pass was possible.
</interfaces>
</context>

## Artifacts this phase produces

- A closed resource-ownership field in migration replay observation and report details.
- Tests proving ownership, cleanup, replay mode, counts, and outcome cannot contradict one another.
- No migration execution or target/live evidence; Plan 18 owns the harness behavior.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define fail-closed migration replay ownership invariants</name>
  <read_first>
    - src/server/internal/surfacereport/migration.go
    - src/server/internal/surfacereport/migration_test.go
    - src/server/internal/surfacereport/registry.go
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-VERIFICATION.md
  </read_first>
  <files>src/server/internal/surfacereport/migration.go, src/server/internal/surfacereport/migration_test.go</files>
  <behavior>
    - RED/GREEN: passing replay details require `ResourceOwnership == "owned-disposable"` and `CleanupResult == "succeeded"`.
    - RED/GREEN: `caller-owned`, unknown, empty, or contradictory ownership never validates with a passing outcome.
    - RED/GREEN: unavailable details may record `caller-owned` or `owned-disposable`, but remain `result=fail`, exact error `migration_replay_unavailable`, empty skips, and cannot satisfy passing counts.
    - Existing static/ledger digests, external/docker replay modes, and typed details exact-key validation remain unchanged.
  </behavior>
  <action>Add `ResourceOwnership string` with JSON key `resourceOwnership` to `MigrationReplayDetails` and keep the details registry closed. Define package-owned constants for `owned-disposable` and `caller-owned`; validate the enum in `validateMigrationReplayDetails`. Tighten `migrationReplayPassingDetails` so only owned disposable resources with verified successful cleanup pass. Keep unavailable evidence distinguishable and no-skip without allowing caller-owned state to become a success path. Write the ownership/cleanup contradiction table first in `TestMigrationReplaySurfaceContract`, including missing, unknown, forged caller-owned pass, cleanup failure, and valid unavailable cases, then implement the production invariant.</action>
  <acceptance_criteria>
    - The report schema truthfully distinguishes ownership from replay mode.
    - No caller-owned or unverified-cleanup details can validate with `result=pass`.
    - Typed unavailable reports remain constructible, sanitized, and nonzero/no-skip.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/surfacereport '^TestMigrationReplaySurfaceContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>The shared migration report contract makes owned teardown a necessary passing fact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry ownership through the strict replay observation CLI</name>
  <read_first>
    - src/server/cmd/release-migration-surface/main.go
    - src/server/cmd/release-migration-surface/main_test.go
    - src/server/internal/surfacereport/migration.go
    - src/server/internal/surfacereport/writer.go
  </read_first>
  <files>src/server/cmd/release-migration-surface/main.go, src/server/cmd/release-migration-surface/main_test.go</files>
  <behavior>
    - RED/GREEN: `migration-replay-observation/v1` requires exactly one valid `resourceOwnership` and rejects unknown fields or values.
    - RED/GREEN: pass observations forward ownership into `MigrationReplayDetails`; caller-owned pass and cleanup contradictions fail before report write.
    - RED/GREEN: unavailable observations preserve ownership and cleanup truth while returning nonzero with stable sanitized output.
    - No CLI flag or environment value can override release identity, outcome, ownership, or cleanup independently of the bounded observation.
  </behavior>
  <action>Extend the existing strict `migrationReplayObservation` with `resourceOwnership` and pass it into both derived passing details and `unavailableReplayDetails`. Keep the bounded JSON decoder's exact-shape behavior; unknown/missing fields must fail with the existing typed schema error rather than receiving a default. Add command tests first for owned pass, caller-owned pass rejection, missing/unknown ownership, forged cleanup, and typed unavailable propagation. Preserve atomic writer behavior, named database URL indirection, sanitized CLI errors, and the existing three-report identity boundary.</action>
  <acceptance_criteria>
    - The CLI cannot omit, default, or independently forge migration resource ownership.
    - A caller-owned pass is rejected before an output report can claim success.
    - Failure reports preserve stable codes without DSN, SQL, raw driver error, or external path leakage.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./cmd/release-migration-surface '^TestReleaseMigrationReplayCommandContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>The replay producer accepts only a closed ownership-aware observation and emits ownership-consistent typed evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| replay shell observation -> Go producer | Caller-supplied JSON could forge ownership, cleanup, or outcome. |
| migration report -> aggregate/operator | A false cleanup claim could conceal mutation of caller data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-17-01 | Spoofing | replay resource ownership | critical | mitigate | Closed enum, exact observation/details schemas, and caller-owned pass rejection. |
| T-31.2-17-02 | Tampering | cleanup/outcome combination | high | mitigate | Passing invariant exact-joins owned-disposable, succeeded cleanup, counts, digests, and pass/no-skip outcome. |
| T-31.2-17-03 | Information Disclosure | unavailable report | medium | mitigate | Stable codes and bounded typed fields only; no DSN, SQL, driver error, or external path. |
</threat_model>

<verification>
- Run focused matched Go tests for details and CLI contracts; selectors must match concrete tests.
- Do not run the real migration harness in this plan; Plan 18 consumes the new interface.
- Evidence remains repository-local E1 until Plan 18 and the final clean-head gate pass.
</verification>

<success_criteria>
- Migration replay ownership is explicit, closed, and mandatory.
- Passing evidence requires an owned disposable resource plus verified teardown.
- Caller-owned resources can only yield typed failure, never cleanup success.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-17-SUMMARY.md` when done.
</output>
