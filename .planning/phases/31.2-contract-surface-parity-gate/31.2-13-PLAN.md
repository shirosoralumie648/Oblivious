---
phase: 31.2-contract-surface-parity-gate
plan: "13"
type: execute
wave: 3
depends_on: ["31.2-02"]
files_modified:
  - src/server/internal/http/router.go
  - src/server/internal/http/routes_auth.go
  - src/server/internal/http/routes_agent_memories.go
  - src/server/internal/http/routes_agent_runs.go
  - src/server/internal/http/routes_conversation_alias.go
  - src/server/internal/http/routes_gateway.go
  - src/server/internal/http/routes_preferences.go
  - src/server/internal/http/routes_task.go
  - src/server/internal/http/routes_schedule.go
  - src/server/internal/http/route_surface_group_a_test.go
autonomous: true
requirements: [RELS-02]
must_haves:
  truths:
    - "Group A route helpers and their router.go call sites move together to the shared RouteSurfaceRegistrar, so the repository compiles after this wave while retaining existing handler behavior."
    - "Each Group A registration carries exact operation, security/CSRF, guard, capability and request/success schema identity; no manifest JSON constructs a runtime mount."
    - "A direct mux mount, missing descriptor, duplicate method/path, excluded operation or zero Group A discovery fails the focused suite."
  artifacts:
    - path: "src/server/internal/http/routes_auth.go"
      provides: "Auth route declarations using the shared runtime registrar"
      contains: "RouteSurfaceRegistration"
    - path: "src/server/internal/http/route_surface_group_a_test.go"
      provides: "Group A runtime registration and dispatch coverage"
      contains: "TestRouteSurfaceGroupAContract"
  key_links:
    - from: "src/server/internal/http/routes_auth.go"
      to: "src/server/internal/http/route_surface.go"
      via: "registrar.Register"
      pattern: "Register"
    - from: "src/server/internal/http/route_surface_group_a_test.go"
      to: "src/server/internal/http/routes_schedule.go"
      via: "group-wide registration snapshot"
      pattern: "route_surface_group_a"
---

<objective>
迁移第一组真实 Go route helpers 到同源 RouteSurfaceRegistrar，关闭 8 个 route source 文件的 direct mux ownership。

Purpose: 让 runtime route descriptor 与真实 handler mount 使用同一注册值，并为后续完整 http-runtime producer提供可验证的分组交付边界。
Output: 八个 route helper 的 registrar 接入、Group A 动态 dispatch/descriptor 测试和非空发现证明。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-PATTERNS.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-02-PLAN.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-02-SUMMARY.md
@src/server/internal/http/routes_auth.go
@src/server/internal/http/routes_schedule.go
@src/server/internal/http/route_surface.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Group A route helpers to same-source registration</name>
  <read_first>
    - src/server/internal/http/route_surface.go
    - src/server/internal/http/router.go
    - src/server/internal/http/routes_auth.go
    - src/server/internal/http/routes_agent_memories.go
    - src/server/internal/http/routes_agent_runs.go
    - src/server/internal/http/routes_conversation_alias.go
    - src/server/internal/http/routes_gateway.go
    - src/server/internal/http/routes_preferences.go
    - src/server/internal/http/routes_task.go
    - src/server/internal/http/routes_schedule.go
  </read_first>
  <files>src/server/internal/http/router.go, src/server/internal/http/routes_auth.go, src/server/internal/http/routes_agent_memories.go, src/server/internal/http/routes_agent_runs.go, src/server/internal/http/routes_conversation_alias.go, src/server/internal/http/routes_gateway.go, src/server/internal/http/routes_preferences.go, src/server/internal/http/routes_task.go, src/server/internal/http/routes_schedule.go, src/server/internal/http/route_surface_group_a_test.go</files>
  <behavior>
    - Every direct mux.Handle/HandleFunc in these helpers is replaced by an exact RouteSurfaceRegistration passed to the shared registrar.
    - Existing auth, tenant, CSRF, middleware, handler and response behavior remains unchanged.
    - Method/path, operationId, capability, media/schema, guard and descriptor identity are explicit and non-empty for every included route.
    - Existing behavior tests may keep calling a thin `registerXRoutes(*ServeMux, ...)` adapter, but production `router.go` calls only the new explicit registrar entrypoint; the adapter contains no metadata, direct mount, global lookup or retained snapshot.
  </behavior>
  <action>Refactor the eight owned helpers and update their exact `router.go` call sites in the same task to call new explicit registrar entrypoints with the shared registrar established by Plan 02. Preserve each old `registerXRoutes(*ServeMux, ...)` symbol only as a thin behavior-test adapter that creates a local registrar and delegates to the same explicit entrypoint; it must contain no route metadata, direct Handle/HandleFunc, global state, mux-to-registrar lookup or production caller. The untouched Group B helpers continue using their existing mux call sites until Plan 14, so this intermediate state must compile. Make the registrar the sole mount path for Group A. Use canonical operation metadata only as a compare input; do not decode it to create handlers or derive capability IDs from paths. Keep route-specific middleware/auth/CSRF and response behavior in the registration value. After the production migration compiles, create `route_surface_group_a_test.go` with the runnable base `TestRouteSurfaceGroupAContract`, positive owner/registration/descriptor counts, representative dispatch assertions, and AST proof that migrated production call sites use only the explicit registrar entrypoints, then run it as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - All eight helpers and their updated `router.go` call sites compile with the Plan 02 registrar contract and have no direct mux mount bypass.
    - Existing handler tests and route dispatch behavior remain compatible.
    - Legacy test adapters delegate only and are absent from production router call sites; they cannot supply or retain an independent descriptor authority.
    - Missing operation metadata or typed binding fails construction rather than installing a permissive fallback.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestRouteSurfaceGroupAContract$' &amp;&amp; (cd src/server &amp;&amp; go test ./internal/http -count=1) &amp;&amp; git diff --check</automated>
  </verify>
  <done>Group A route helpers use same-source typed registrations and preserve their existing behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Prove Group A registration and dispatch closure</name>
  <read_first>
    - src/server/internal/http/route_surface_group_a_test.go
    - src/server/internal/http/route_surface.go
    - docs/api/route-surface-manifest.json
  </read_first>
  <files>src/server/internal/http/route_surface_group_a_test.go</files>
  <behavior>
    - The suite discovers all eight source owners and a nonzero set of registrations, descriptors and exact dispatch probes.
    - One-field mutations of method/path/auth/CSRF/guard/capability/request/response identity fail in both registration and comparison directions.
    - Direct mount bypass, duplicate registration, excluded route and zero inventory fail without a passing report.
  </behavior>
  <action>After Task 1 production migration and base test are complete, extend the group-level test harness with the full static discovery, immutable-snapshot comparison and one-field mutation families. Enumerate the eight owned source files, compare the registrar snapshot to the expected operation rows, and exercise additional auth, gateway, alias, task and schedule requests with injected handler counters. Assert positive counts and stable failure codes for each mutation family. Keep the test as implementation-complete verification only; it must not become a second runtime authority.</action>
  <acceptance_criteria>
    - `TestRouteSurfaceGroupAContract` has positive owner, registration, descriptor and dispatch counts.
    - No source-token-only assertion can make the suite pass when a direct mount or missing descriptor is present.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestRouteSurfaceGroupAContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>Group A has executable same-source registration, dispatch and mutation evidence.</done>
</task>

</tasks>

<verification>
- Run the Group A matched suite, the complete `internal/http` package regression, and `git diff --check`.
- Evidence is repository-local E1/E2 only; the full http-runtime report remains owned by Plan 15.
</verification>

<success_criteria>
- Eight named route helper files have no direct mux mount bypass.
- Group A registrations and descriptors are exact, non-empty and mutation-tested.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-13-SUMMARY.md` when done.
</output>
