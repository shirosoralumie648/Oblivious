---
phase: 31.2-contract-surface-parity-gate
plan: "11"
type: execute
wave: 3
depends_on: ["31.2-01", "31.2-04"]
files_modified:
  - src/web/src/app/appContext.tsx
  - src/web/src/features/admin/api.ts
  - src/web/src/features/agents/agentsApi.ts
  - src/web/src/features/agents/memoriesApi.ts
  - src/web/src/features/agents/planStepsApi.ts
  - src/web/src/features/auth/api.ts
  - src/web/src/features/auth/api.test.ts
  - src/web/src/features/console/api.ts
  - src/web/src/features/knowledge/api.ts
autonomous: true
requirements: [RELS-02]
must_haves:
  truths:
    - "All nine Group A expected owners are either concrete callers that import/use Plan 01 generated exact operation metadata through Plan 04 transports or compiler-proven non-callers with an explicit disposition; no caller derives core identity from path lookup or TypeScript generics (D-SUR-02, D-SUR-05, D-SUR-06)."
    - "Request encoder and selected success status/media/schema identity come from each imported operation symbol; local metadata objects, inferred schemas and generic-only identity fail (D-SUR-02)."
    - "Feature API external interfaces and existing behavior remain compatible; every listed file has a nonzero compiler-AST caller/owner disposition and no owner is silently removed (D-SUR-05, D-SUR-07)."
    - "Agent catalog normalization preserves exact Phase 31.1 read-only capabilityId while mutations omit it (D-SUR-08)."
  artifacts:
    - path: "src/web/src/features/admin/api.ts"
      provides: "Explicit generated operation contracts for Admin calls"
      contains: "operationContracts"
    - path: "src/web/src/features/agents/agentsApi.ts"
      provides: "Explicit Agent operation contracts plus capabilityId-safe serialization"
      contains: "capabilityId"
    - path: "src/web/src/features/knowledge/api.ts"
      provides: "Explicit JSON/multipart Knowledge operation contracts"
      contains: "operationContracts"
  key_links:
    - from: "src/web/src/generated/operation-contracts.generated.ts"
      to: "src/web/src/features/agents/agentsApi.ts"
      via: "concrete generated members passed into HttpClient OperationTransportContract"
      pattern: "operationContracts"
    - from: "scripts/verify_frontend_operation_contract_usage.mjs"
      to: "src/web/src/features/knowledge/api.ts"
      via: "TypeScript symbol resolution and nonzero owner/import/use proof"
      pattern: "operation-contract-usage/v1"
---

<objective>
迁移 Group A 的九个 expected owners：具体 transport callers 显式携带 Plan 01 generated operation/schema identity，真正的 non-caller 由编译器证明并显式 disposition，同时保持现有 feature API 外部接口。

Purpose: 消除 Plan 03 必须从 path 或 generic 事后猜 operation contract 的断点。
Output: app/provider、Admin、Agent、Auth、Console、Knowledge caller migration 和 nonzero AST proof。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VALIDATION.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-01-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-04-SUMMARY.md
@src/web/src/generated/operation-contracts.generated.ts
@scripts/verify_frontend_operation_contract_usage.mjs

<interfaces>
Every concrete callsite imports the generated `operationContracts.<operationId>` member and passes that exact symbol into Plan 04 `OperationTransportContract`, HttpClient, stream, upload or SWR. The generated member supplies method/path, request media/schema and every success status/media/schema; caller selects a declared success response and codec but cannot rewrite core fields. Generic type arguments are diagnostics only.

The nine expected-owner paths are a fixed preflight set. `src/web/src/app/providers.tsx` is a read-only compiler-proven non-caller/disposition input rather than a write owner. Existing tests are read-only verification inputs, except the absent `src/web/src/features/auth/api.test.ts`, which this plan creates only after the Auth production migration is complete.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate app/provider, Admin, Auth and Console callers</name>
  <read_first>
    - src/web/src/app/appContext.tsx
    - src/web/src/app/providers.tsx
    - src/web/src/features/admin/api.ts
    - src/web/src/features/auth/api.ts
    - src/web/src/features/console/api.ts
    - src/web/src/services/http/client.ts
  </read_first>
  <files>src/web/src/app/appContext.tsx, src/web/src/features/admin/api.ts, src/web/src/features/auth/api.ts, src/web/src/features/auth/api.test.ts, src/web/src/features/console/api.ts</files>
  <behavior>
    - Every concrete HttpClient/SWR call imports and passes its exact generated operation symbol; selected status/codec matches that symbol.
    - App context forwarding retains the symbol unchanged; `providers.tsx` is compiler-proven to make zero transport calls and receives an explicit non-caller disposition.
    - Existing app/Admin/Console tests and the new post-implementation Auth API regression preserve external return/request APIs; missing/wrong symbol fails type/runtime tests.
  </behavior>
  <action>Migrate each concrete App context, Admin, Auth and Console call first. Import exact generated members, build typed encoder/decoder selection from their request/success identities, and pass the symbol to the shared transport. Keep `providers.tsx` unchanged and require the compiler preflight to prove its zero-call non-caller disposition; do not introduce a dynamic lookup. Preserve exported feature API signatures, auth/error semantics and cache keys. After the Auth production migration, create `src/web/src/features/auth/api.test.ts` and run it with the existing App/Admin/Console tests as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Four concrete caller owners compile with exact generated symbols and no core reconstruction; `providers.tsx` has one explicit compiler-proven non-caller disposition.
    - Existing feature behavior remains passing.
    - AST preflight reports each owner exactly once with positive applicable counts.
  </acceptance_criteria>
  <verify><automated>pnpm --dir src/web exec vitest run src/app/appContext.test.tsx src/features/admin/api.test.ts src/features/auth/api.test.ts src/features/console/api.test.ts --reporter=verbose &amp;&amp; pnpm --dir src/web exec tsc --noEmit &amp;&amp; node scripts/verify_frontend_operation_contract_usage.mjs --tsconfig src/web/tsconfig.json --expect-owner src/web/src/app/appContext.tsx --expect-owner src/web/src/app/providers.tsx --expect-owner src/web/src/features/admin/api.ts --expect-owner src/web/src/features/auth/api.ts --expect-owner src/web/src/features/console/api.ts --require-all &amp;&amp; git diff --check</automated></verify>
  <done>App/provider/Admin/Auth/Console owners carry exact generated operation identity.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all Agent feature callers without leaking capability authority</name>
  <read_first>
    - src/web/src/features/agents/agentsApi.ts
    - src/web/src/features/agents/memoriesApi.ts
    - src/web/src/features/agents/planStepsApi.ts
    - src/web/src/features/agents/agentsApi.test.ts
    - src/web/src/features/agents/memoriesApi.test.ts
    - src/web/src/features/agents/planStepsApi.test.ts
  </read_first>
  <files>src/web/src/features/agents/agentsApi.ts, src/web/src/features/agents/memoriesApi.ts, src/web/src/features/agents/planStepsApi.ts</files>
  <behavior>
    - Agent/tool/memory/plan-step calls pass their own exact generated operation symbol and declared request/success identity.
    - `AgentToolDefinition.capabilityId` remains response-only; create/update serializers explicitly omit it.
    - Existing Agent API tests remain passing and wrong/missing operation symbol or capability mutation fails.
  </behavior>
  <action>Migrate every transport call in the three files to concrete generated metadata members and Plan 04 typed codecs. Keep current public functions/types. Preserve response normalization of exact capabilityId, and serialize mutations by business-field selection. Do not spread response objects, map tool IDs to capabilities, select contract metadata by URL, or treat a generic as schema identity.</action>
  <acceptance_criteria>
    - Three Agent owners have complete exact-symbol coverage and positive AST counts.
    - Capability identity remains server-derived and mutation-safe.
    - Existing Agent tests and TypeScript compilation pass.
  </acceptance_criteria>
  <verify><automated>pnpm --dir src/web exec vitest run src/features/agents/agentsApi.test.ts src/features/agents/memoriesApi.test.ts src/features/agents/planStepsApi.test.ts --reporter=verbose &amp;&amp; pnpm --dir src/web exec tsc --noEmit &amp;&amp; node scripts/verify_frontend_operation_contract_usage.mjs --tsconfig src/web/tsconfig.json --expect-owner src/web/src/features/agents/agentsApi.ts --expect-owner src/web/src/features/agents/memoriesApi.ts --expect-owner src/web/src/features/agents/planStepsApi.ts --require-all &amp;&amp; git diff --check</automated></verify>
  <done>All Agent production callers use generated contracts without capability escalation.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate Knowledge and close the nine-owner Group A set</name>
  <read_first>
    - src/web/src/features/knowledge/api.ts
    - src/web/src/features/knowledge/api.test.ts
    - src/web/src/services/http/upload.ts
    - scripts/verify_frontend_operation_contract_usage.mjs
  </read_first>
  <files>src/web/src/features/knowledge/api.ts</files>
  <behavior>
    - Knowledge JSON and multipart calls pass exact generated symbols with correct request encoder and selected success identity.
    - All nine Group A files appear exactly once in the AST result as resolved callers/transport owners or explicit compiler-proven non-callers.
    - Zero owner/import/use counts, silent owner deletion, local metadata, path lookup or generic synthesis fails.
  </behavior>
  <action>Migrate Knowledge calls, including multipart upload, to exact generated operation members and typed Plan 04 transport contracts while preserving current exported functions/progress/error behavior. Then run one Group A preflight with all nine expected owners. If any listed file has no transport call, retain it with the helper's explicit compiler-proven disposition/reason and named test; do not delete it from ownership. Require set equality and positive counts.</action>
  <acceptance_criteria>
    - Knowledge media/schema identity comes only from generated metadata.
    - Group A has exact 9/9 owner dispositions and no unowned local caller within these files.
    - Existing Knowledge tests, all Group A tests and tsc remain passing.
  </acceptance_criteria>
  <verify><automated>pnpm --dir src/web exec vitest run src/features/knowledge/api.test.ts --reporter=verbose &amp;&amp; pnpm --dir src/web exec tsc --noEmit &amp;&amp; node scripts/verify_frontend_operation_contract_usage.mjs --tsconfig src/web/tsconfig.json --expect-owner src/web/src/app/appContext.tsx --expect-owner src/web/src/app/providers.tsx --expect-owner src/web/src/features/admin/api.ts --expect-owner src/web/src/features/agents/agentsApi.ts --expect-owner src/web/src/features/agents/memoriesApi.ts --expect-owner src/web/src/features/agents/planStepsApi.ts --expect-owner src/web/src/features/auth/api.ts --expect-owner src/web/src/features/console/api.ts --expect-owner src/web/src/features/knowledge/api.ts --require-all &amp;&amp; git diff --check</automated></verify>
  <done>Group A is a closed nine-owner exact-operation-symbol migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated operation metadata -> feature callers | Canonical request/response identity enters production transport selection. |
| server catalog response -> Agent mutations | Read-only capability identity must not become caller authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-01 | Tampering | caller contract identity | high | mitigate | Exact generated symbols, typed codecs, existing behavior tests and compiler-symbol preflight. |
| T-31.2-02 | Tampering | caller inventory | high | mitigate | Fixed nine-owner set, explicit dispositions and nonzero AST counts. |
| T-31.2-03 | Elevation of Privilege | Agent capability mutation | high | mitigate | Response-only normalization, explicit mutation serialization and exact tests. |
</threat_model>

<verification>
- Run named existing Vitest files plus the plan-owned post-implementation Auth API test, TypeScript no-emit, Group A compiler-symbol preflight and `git diff --check`.
- Plan 03 later exact-joins Group A with the remaining 16 known owners and rejects newly discovered callers.
- Evidence is E1 repository-local and does not claim browser/target/commercial readiness.
</verification>

<success_criteria>
- Nine fixed Group A files have explicit, exact generated operation metadata ownership.
- Request/response schema identity is symbol-derived, never path/generic-derived.
- Existing feature APIs/tests remain compatible.
- Agent capability identity remains response-only and mutation-safe.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-11-SUMMARY.md` when done.
</output>
