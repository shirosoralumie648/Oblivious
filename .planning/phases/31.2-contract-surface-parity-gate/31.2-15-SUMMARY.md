---
phase: 31.2-contract-surface-parity-gate
plan: "15"
subsystem: contract-surface-gates
tags: [go, react, typescript, http-runtime, operation-contracts, release-evidence]

requires:
  - phase: 31.2-contract-surface-parity-gate
    provides: "Plans 01/02/04/10-14 generated contracts, operation-aware transports, product projection, and all 17 same-source route owners"
provides:
  - "Trusted complete http-runtime producer over the real 197-operation registrar snapshot"
  - "Exact generated operation contracts for public Login and Register production callers"
  - "Single explicit 25-disposition frontend owner inventory with exact compiler-symbol and call-count handoff"
  - "Executable direct-mount, descriptor, browser-field, identity, skip, output, and owner-closure fixtures"
affects: [31.2-03, 31.2-05, 31.2-09, http-runtime, frontend-sidecar, RELS-02]

tech-stack:
  added: []
  patterns:
    - "Construct the real runtime registrar snapshot before loading any compare-only manifest data"
    - "Derive repository identity and deployment profile only through trusted resolvers, then persist one typed report through AtomicWriter"
    - "Pass named generated operation symbols through request encoder, response decoder, and HttpClient transport contracts"
    - "Expose one reviewable owner/disposition list that both shell fixtures and command-level tests consume"

key-files:
  created:
    - src/server/cmd/release-http-surface/main.go
    - src/server/cmd/release-http-surface/main_test.go
    - scripts/verify-http-runtime-contract.sh
    - scripts/verify-http-runtime-contract-fixtures.sh
  modified:
    - src/server/internal/http/route_surface.go
    - src/server/internal/http/route_surface_test.go
    - src/web/src/routes/marketing/LoginPage.tsx
    - src/web/src/routes/marketing/RegisterPage.tsx

key-decisions:
  - "Build and validate the full 197-descriptor runtime snapshot before resolving the compare-only manifest, trusted identity, profile, or report destination."
  - "Keep producer authority closed to repo, contract, schema, profile, manifest, and output flags; release identity, evidence class, skips, and outcome have no caller override."
  - "Use one explicit 25-disposition inventory for the Plan 03 handoff: 24 exact transport owners plus providers.tsx as the sole compiler-proven non-caller."
  - "Bind Login and Register to loginOperationContract and registerOperationContract through JSON request/envelope response transports without changing journey behavior."

patterns-established:
  - "Complete HTTP runtime evidence requires exactly 17 route owners, 197 operations/descriptors/mounts, positive media probes, equal digests, empty drift, and no skips."
  - "Frontend closure records exact owner, import, use, disposition, transport-call, and shared-transport counts; zero or changed counts fail before producer execution."

requirements-completed: [RELS-02]

coverage:
  - id: D1
    description: "The trusted http-runtime command constructs the complete real registrar snapshot, compares 197 operations, resolves repository identity/profile, and atomically writes one schema-valid no-skip report."
    requirement: RELS-02
    verification:
      - kind: integration
        ref: "src/server/cmd/release-http-surface/main_test.go#TestReleaseHTTPRuntimeSurfaceCommandContract"
        status: pass
      - kind: integration
        ref: "bash scripts/verify-http-runtime-contract-fixtures.sh"
        status: pass
    human_judgment: false
  - id: D2
    description: "Public Login and Register callers carry exact generated auth operation identity through shared JSON transport contracts while preserving bootstrap and navigation behavior."
    requirement: RELS-02
    verification:
      - kind: unit
        ref: "src/web/src/routes/marketing/LoginPage.test.tsx and RegisterPage.test.tsx"
        status: pass
      - kind: integration
        ref: "node scripts/verify_frontend_operation_contract_usage.mjs --expect-owner LoginPage.tsx --expect-owner RegisterPage.tsx --require-all"
        status: pass
    human_judgment: false
  - id: D3
    description: "The Plan 03 handoff enforces one exact 25-disposition frontend inventory, 267 exact-symbol transport calls, all 17 Go route owners, and all negative compiler-symbol fixture families."
    requirement: RELS-02
    verification:
      - kind: integration
        ref: "bash scripts/verify-http-runtime-contract-fixtures.sh --owner-closure"
        status: pass
      - kind: unit
        ref: "node scripts/verify_frontend_operation_contract_usage.mjs --fixtures --require-all"
        status: pass
    human_judgment: false

duration: 26 min
completed: 2026-07-22
status: complete
---

# Phase 31.2 Plan 15: Complete HTTP Runtime Producer and Marketing Auth Contracts Summary

**A trusted 197-operation HTTP runtime report producer now closes all 17 Go route owners, while Login/Register and the exact 25-disposition frontend inventory are ready for the one-Program sidecar.**

## Performance

- **Duration:** 26 min
- **Started:** 2026-07-22T07:26:00Z
- **Completed:** 2026-07-22T07:52:07Z
- **Tasks:** 3 completed
- **Files modified:** 8 production/test files plus this SUMMARY

## Accomplishments

- Added an independently runnable `release-http-surface` command that constructs the real router first, captures exactly 197 descriptors, loads manifest metadata only for comparison, and emits one trusted schema-valid `http-runtime` report through `AtomicWriter`.
- Added Stage-A/clean-head and mutation fixtures covering all 17 route owners, direct mount bypass, missing/duplicate descriptors, browser-only fields, unavailable/spliced identity, committed skips, output failure, report validation, and worktree preservation.
- Migrated public Login and Register calls to exact generated operation symbols with request/response codec identity while preserving form, bootstrap, error, and navigation behavior.
- Closed the handoff at 25 explicit frontend dispositions, 264 imports, 267 exact uses/calls, and four shared transport owners; all compiler-symbol negative fixture families remain executable.

## Task Commits

Each task was committed atomically:

1. **Task 1: Build the complete http-runtime producer and fixtures** - `1192bfd` (`feat`)
2. **Task 2: Migrate public marketing Login and Register callers** - `c68774c` (`feat`)
3. **Task 3: Close the 25-owner preflight handoff** - `de35de6` (`test`)

This SUMMARY is committed separately as the execute-plan metadata commit.

## Files Created/Modified

- `src/server/cmd/release-http-surface/main.go` and `main_test.go` - Trusted producer CLI, real-router snapshot construction, strict manifest projection, report persistence, authority rejection, and command-level owner closure.
- `scripts/verify-http-runtime-contract.sh` - Stage-A/clean-head producer build, trusted run, report read-back, count/digest/drift/outcome validation, and one-invocation proof.
- `scripts/verify-http-runtime-contract-fixtures.sh` - Exact 17-route/25-frontend closure plus descriptor, identity, direct-mount, browser-field, skip, output, and clean-head failure fixtures.
- `src/server/internal/http/route_surface.go` and `route_surface_test.go` - Production-owned route contract error-code helper required by non-test builds.
- `src/web/src/routes/marketing/LoginPage.tsx` and `RegisterPage.tsx` - Exact generated auth operation transports over the shared HttpClient.

## Decisions Made

- Runtime registration is constructed and validated before the compare-only manifest is opened, so JSON metadata cannot create handlers, middleware, mounts, or runtime authority.
- The producer accepts only explicit source/config/output paths and profile ID. Git identity, contract digest, evidence class, result, drift, and skips remain trusted resolver/report concerns.
- The frontend handoff list lives once in the executable fixture and is consumed by the Go command test through a read-only list mode. It includes one explicit composition-only non-caller rather than fabricating a transport.
- Marketing auth pages use the canonical `@/generated/operation-contracts.generated` module so compiler-symbol proof can reject relative/spoofed metadata sources.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Restored production build ownership for route contract error decoding**
- **Found during:** Task 1 focused RED verification
- **Issue:** Plan 14 production code called `routeSurfaceErrorCode`, but the helper existed only in `route_surface_test.go`; package tests compiled while a real `go build` of the producer failed.
- **Fix:** Moved the helper into `route_surface.go` and removed the test-only duplicate without changing error behavior.
- **Files modified:** `src/server/internal/http/route_surface.go`, `src/server/internal/http/route_surface_test.go`
- **Verification:** Focused command test, producer builds, full 13-case fixture gate, and final Plan 15 regression all passed.
- **Committed in:** `1192bfd`

---

**Total deviations:** 1 auto-fixed blocking production-build defect.
**Impact on plan:** The fix was required for the planned real binary and retained the existing typed error contract; no feature scope expanded.

## Issues Encountered

- The first marketing compiler-symbol run rejected relative generated imports as `metadata_argument_missing`. Switching both imports to the canonical `@/generated/operation-contracts.generated` authority resolved the symbol identity without changing runtime behavior.
- The mounted filesystem ignored ordinary executable-bit changes through `core.fileMode`; the two shell entries were explicitly recorded as `100755` in the Git index.

## Verification

- Matched command contract - PASS; one concrete `TestReleaseHTTPRuntimeSurfaceCommandContract` matched, including trusted report and exact frontend handoff assertions.
- Complete producer fixtures - PASS; `routeOwners=17`, `frontendDispositions=25`, `frontendCalls=267`, `operations=197`, `cases=13`.
- Marketing behavior - PASS; 2 Vitest files and 2 tests.
- `pnpm --dir src/web exec tsc --noEmit` - PASS.
- Marketing exact-symbol preflight - PASS; `owners=2`, `imports=2`, `uses=2`, `dispositions=2`, `transportCalls=2`.
- Exact owner closure - PASS; 17 Go owners, 25 frontend dispositions, 267 frontend calls, 197 runtime operations.
- Compiler-symbol fixtures - PASS; nine fixture families including lookup, local metadata, generic-only, duplicate, missing, zero, and duplicate-owner failures.
- `bash -n` and `git diff --check` - PASS.

## User Setup Required

None - no database, provider, secret, external service, target environment, or deployment action is required.

## Evidence Boundary

This plan contributes repository-local E1/E2 source, runtime-registration, typed-report, caller-symbol, and mutation evidence to RELS-02. It does not provide the Plan 03 production-wide sidecar, Plan 05 transport/exposure reports, Plan 09 aggregate session, target/live proof, deployment readiness, or commercial release readiness.

## Next Phase Readiness

- Plan 03 can consume the stable 25-disposition list and the completed operation-aware callers to build the one-Program production sidecar and distinct frontend report registrations.
- Plans 05 and 09 remain blocked on Plan 03 and later surface aggregation; Phase 31.2 remains in progress at 12/15.
- Protected user changes in `.planning/config.json`, `effect_registry.go`, `effect_registry_test.go`, and `.planning/intel/` remain unstaged and untouched.

## Self-Check: PASSED

- Confirmed task commits `1192bfd`, `c68774c`, and `de35de6` exist on the current branch.
- Confirmed every Task 1-3 verification and the complete plan-level regression passed after the final task commit.
- Confirmed all created key files exist and the exact 17-route/25-frontend inventories are nonzero and closed.

---
*Phase: 31.2-contract-surface-parity-gate*
*Completed: 2026-07-22*
