---
phase: 31.2-contract-surface-parity-gate
plan: "06"
type: execute
wave: 1
depends_on: ["31.1-08"]
files_modified:
  - config/release/protobuf-toolchain.v1.json
  - scripts/bootstrap-protobuf-tools.sh
  - scripts/verify-protobuf-contract.sh
  - scripts/verify_protobuf_contract.py
  - src/server/Makefile
  - src/server/internal/surfacereport/protobuf.go
  - src/server/internal/surfacereport/protobuf_test.go
  - src/server/cmd/release-contract/main.go
  - src/server/cmd/release-contract/main_test.go
autonomous: true
requirements: [RELS-02]
must_haves:
  truths:
    - "Protobuf tooling is pinned exactly to protoc release 25.1 (tracked generated header `protoc v4.25.1`), protoc-gen-go 1.36.11, and protoc-gen-go-grpc 1.6.2 with verified checksums and manifest digest (D-SUR-09)."
    - "Bootstrap writes only `.tmp/protobuf-tools/bin`, generation explicitly uses that directory, and host PATH/global tools cannot satisfy the gate (D-SUR-09, D-SUR-11)."
    - "Every tracked proto source and generated Go output outside reference has exactly one deterministic disposition; source-only rows name a concrete consumer/reason, and empty/duplicate/unmanaged/stale sets fail without modifying source/output files (D-SUR-10)."
    - "A fresh checkout regenerates managed outputs only into a new temporary tree and byte-compares all declared files; any byte diff blocks rather than repairing tracked outputs (D-SUR-10, D-SUR-15)."
  artifacts:
    - path: "config/release/protobuf-toolchain.v1.json"
      provides: "Pinned tools/checksums plus total source/output disposition manifest"
      contains: '"generatedHeaderVersion": "v4.25.1"'
    - path: "scripts/bootstrap-protobuf-tools.sh"
      provides: "Checksum-verified repository-local tool bootstrap"
      contains: ".tmp/protobuf-tools/bin"
    - path: "scripts/verify_protobuf_contract.py"
      provides: "Git-discovered total disposition and deterministic regeneration verifier"
      contains: "protobuf-toolchain/v1"
    - path: "src/server/internal/surfacereport/protobuf.go"
      provides: "Typed protobuf details registration/report constructor"
      exports: ["ProtobufSurfaceID", "ProtobufDetails", "NewProtobufReport"]
  key_links:
    - from: "config/release/protobuf-toolchain.v1.json"
      to: "scripts/bootstrap-protobuf-tools.sh"
      via: "exact artifact/module integrity and manifest digest"
      pattern: '25\.1|1\.36\.11|1\.6\.2'
    - from: "src/server/Makefile"
      to: "config/release/protobuf-toolchain.v1.json"
      via: "manifest-driven source/output generation loop"
      pattern: "protobuf-toolchain"
    - from: "src/server/cmd/release-contract/main.go"
      to: "src/server/internal/surfacereport/protobuf.go"
      via: "typed report construction and AtomicWriter"
      pattern: "NewProtobufReport"
---

<objective>
固定 protobuf 工具供应链、建立 tracked source/output total disposition，并证明 fresh checkout byte regeneration。

Purpose: 消除 host tool 偶然匹配、Makefile 部分覆盖、orphan output 与 stale generated consumer 的假绿。
Output: single tool/disposition manifest、isolated bootstrap、manifest-driven generation、fresh gate、typed `protobuf` report producer。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-RESEARCH.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-PATTERNS.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VALIDATION.md
@.planning/phases/31-release-contract-current-baseline/31-04-PLAN.md
@api/proto/agent.proto
@api/proto/events.proto
@src/server/api/proto/events.proto
@src/server/api/proto/observability/v1/observability.proto
@src/server/pkg/relay/proto/relay.proto
@src/server/Makefile

<interfaces>
The single manifest contains exact tool records and `dispositions[]`. A managed entry declares one source, includeRoot, exact outputs, plugins/options, consumers, and expected generated headers (`protoc v4.25.1`, `protoc-gen-go v1.36.11`, `protoc-gen-go-grpc 1.6.2` where applicable). A source-only entry declares a concrete existing consumer/reason and zero outputs. Git discovery excludes only `reference/` and requires nonzero source/output/disposition/fixture-case sets. All current tracked sources and outputs receive a total disposition in the manifest; this plan never modifies `.proto`, `*.pb.go`, or `*_grpc.pb.go` bytes.

Before report work, require Phase 31 trusted resolvers, typed registry/writer, and the existing release-contract CLI. Preserve all upstream subcommands while adding protobuf reporting.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit exact tool integrity and total non-mutating dispositions</name>
  <read_first>
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-RESEARCH.md
    - api/proto/agent.proto
    - api/proto/events.proto
    - src/server/api/proto/events.proto
    - src/server/api/proto/observability/v1/observability.proto
    - src/server/pkg/relay/proto/relay.proto
    - src/server/Makefile
  </read_first>
  <files>config/release/protobuf-toolchain.v1.json, scripts/verify_protobuf_contract.py, scripts/verify-protobuf-contract.sh</files>
  <behavior>
    - V31.2-S06/T-31.2-04: exact versions, artifact/module checksums, architecture, manifest digest, and every source/output disposition validate.
    - `git ls-files` discovered proto sources and generated outputs outside reference equal manifest sets exactly and are both nonzero.
    - Duplicate source/output, undispositioned output/source, source-only without concrete existing consumer/reason, empty output declaration, empty glob, unsafe path, or generated header/version mismatch fails.
    - Fixture/TAP-style case summary reports positive source, output, disposition and mutation counts; zero discovered/case totals fail.
  </behavior>
  <action>Author `protobuf-toolchain/v1` with exact approved versions, generated header expectations, official protoc archive SHA-256 values for supported CI platforms, Go module sums and canonical manifest digest. Enumerate every currently tracked source and output exactly once. Each current source is either managed with exact outputs or source-only with a concrete existing consumer/reason and zero outputs; do not delete, rewrite, regenerate in place, or otherwise repair any `.proto`/generated Go file in this task. Implement Python validation with JSON and Git subprocess arrays, not regex authority. Check repository-relative paths, include roots, exact header/tool versions already present, existing consumers, closed plugins/options and canonical digest. Any total-disposition gap or byte/header drift is blocking evidence. `--manifest-only` preserves nonzero status. After the production manifest and validator are complete, add and run structured mutation fixtures inside `--fixtures` as implementation-complete automated verification, emitting a TAP-compatible/structured summary whose source, output, disposition, total-case and each mutation-family counts are positive.</action>
  <acceptance_criteria>
    - Tracked source and output sets equal dispositions exactly with no ignored remainder.
    - Tool/version/integrity values and manifest digest are deterministic and machine-readable.
    - Every malformed/empty/duplicate/undispositioned/source-only family is executed with positive counts and fails with a stable code.
    - No task file list or action authorizes edits to proto/generated output bytes.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m py_compile scripts/verify_protobuf_contract.py &amp;&amp; bash -n scripts/verify-protobuf-contract.sh &amp;&amp; python3 scripts/verify_protobuf_contract.py --manifest config/release/protobuf-toolchain.v1.json --fixtures &amp;&amp; bash scripts/verify-protobuf-contract.sh --manifest-only &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.2-S06 has a total unique tracked-file disposition and exact tool integrity contract.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap only pinned tools and byte-regenerate every managed output</name>
  <read_first>
    - config/release/protobuf-toolchain.v1.json
    - src/server/Makefile
    - src/server/go.mod
    - scripts/verify_protobuf_contract.py
    - .github/workflows/ci.yml
  </read_first>
  <files>scripts/bootstrap-protobuf-tools.sh, scripts/verify-protobuf-contract.sh, scripts/verify_protobuf_contract.py, src/server/Makefile</files>
  <behavior>
    - Bootstrap installs/verifies exact tools only under `.tmp/protobuf-tools/bin`; wrong checksum/version, absent download/module, unwritable directory, or a host-only tool fails.
    - Generation uses explicit absolute plugin paths, declared include roots/options, stable locale, and a fresh output tree.
    - Every declared output is generated once in the temp tree and byte-equal; missing/extra/generated drift, zero generated files, or access to host protoc/plugin paths fails without copying results into the repository.
    - A clean archive with global tool locations removed from PATH succeeds after only declared bootstrap.
  </behavior>
  <action>Build `bootstrap-protobuf-tools.sh` with `set -euo pipefail`, closed platform lookup, bounded installation, integrity verification, exact version checks, atomic tool-directory replacement and no fallback search. Refactor the Makefile protobuf verification target to invoke the manifest-driven temp generator with explicit `.tmp/protobuf-tools/bin` paths rather than partial commands; its release-gate mode never writes generated output into tracked locations. Extend Python to regenerate each managed disposition into a newly empty temp tree, assert source/output/generation counts positive and exact, byte-compare every declared tracked output, and reject undeclared temp files. The full gate uses a clean archive, strips global tools, bootstraps, regenerates only to temp, verifies header/tool versions/digest, and asserts the checkout remains byte-clean. Any byte difference is a blocking failure with stable IDs; it must not trigger an in-repo regeneration or repair. Cache hits still recheck versions; Plan 09 wires CI ownership.</action>
  <acceptance_criteria>
    - Host PATH cannot satisfy bootstrap or generation.
    - Fresh checkout regeneration covers every managed output and leaves no diff.
    - Wrong tool/checksum, missing tool, output drift, and empty generation are hard failures.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/bootstrap-protobuf-tools.sh scripts/verify-protobuf-contract.sh &amp;&amp; bash scripts/verify-protobuf-contract.sh --fresh-checkout &amp;&amp; git diff --check</automated>
  </verify>
  <done>Pinned protobuf tooling and total managed outputs are reproducible without global tools.</done>
</task>

<task type="auto">
  <name>Task 3: Register and emit the protobuf surface report</name>
  <read_first>
    - src/server/internal/surfacereport/report.go
    - src/server/internal/surfacereport/registry.go
    - src/server/internal/surfacereport/writer.go
    - src/server/cmd/release-contract/main.go
    - config/release/protobuf-toolchain.v1.json
    - scripts/verify_protobuf_contract.py
  </read_first>
  <files>src/server/internal/surfacereport/protobuf.go, src/server/internal/surfacereport/protobuf_test.go, src/server/cmd/release-contract/main.go, src/server/cmd/release-contract/main_test.go, scripts/verify-protobuf-contract.sh</files>
  <behavior>
    - `protobuf` details include exact tool versions, manifest digest, nonzero source/output/managed/source-only counts, and regeneration digest/result.
    - Trusted identity/profile is resolved internally; observations cannot supply commit/tree/contract/profile/evidence class.
    - Tool mismatch, disposition drift, generated drift, unknown details, identity splice, non-empty skip, failed regeneration, or E3/E4 claim fails report construction/verification.
  </behavior>
  <action>Register `ProtobufSurfaceID = "protobuf"` with a closed `ProtobufDetails` schema and implement side-effect-free `NewProtobufReport` using Phase 31 resolvers. Use canonical source `config/release/protobuf-toolchain.v1.json`, consumer `tracked-protobuf-generated-consumers`, version `v1`. Extend the existing release-contract CLI with `report-protobuf --repo --contract --schema --profile --observation --output`; preserve all Phase 31/31.1 commands, reject identity/skip overrides, and persist with AtomicWriter. Make the shell gate collect structured observation from the real validator, then invoke the CLI exactly once in clean-head mode. Diagnostics expose versions/digests/counts/stable codes only. After the production report/CLI/gate implementation is complete, add and run `TestProtobufSurfaceContract` and `TestRunProtobufSurfaceReportContract` as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - A fresh clean regeneration writes one valid trusted `protobuf` report with zero drift/skips.
    - Every tool/disposition/output/identity/report negative case remains nonzero.
    - The focused producer is independently runnable and does not call aggregate ancestors.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/surfacereport '^TestProtobufSurfaceContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./cmd/release-contract '^TestRunProtobufSurfaceReportContract$' &amp;&amp; bash scripts/verify-protobuf-contract.sh --stage-a &amp;&amp; git diff --check</automated>
  </verify>
  <done>The phase owns one trusted typed `protobuf` report backed by exact fresh regeneration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| remote tool artifacts/modules -> local executable | Downloaded compiler/plugin bytes enter a release gate. |
| proto source -> generated Go consumers | Canonical contracts generate checked-in executable code. |
| regeneration observations -> report | Tool/file results enter trusted nested evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-04 | Tampering | protobuf toolchain/generated output | high | mitigate | Exact checksums/versions, isolated bin, total dispositions, fresh temp regeneration, and byte comparison. |
| T-31.2-06 | Spoofing | protobuf report identity | high | mitigate | Trusted resolvers, typed details, atomic writer, and splice/skip rejection. |
</threat_model>

<verification>
- Run manifest fixtures, isolated bootstrap, fresh-checkout regeneration, matched Go report/CLI suites, focused producer Stage A, and `git diff --check`.
- Plan 09 adds manifest-digest CI cache wiring and reruns exact version checks on cache hit.
- Evidence is E1 repository-local RELS-02 contribution only.
</verification>

<success_criteria>
- Approved protobuf versions and integrity are exact and host-independent.
- Every tracked source/output has one deterministic disposition and temp-only regeneration proof; tracked proto/generated bytes remain outside task ownership.
- `protobuf` report is distinct, trusted, drift-free, and skip-free.
- No E3/E4 or commercial release proof is claimed.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-06-SUMMARY.md` when done.
</output>
