---
phase: 31.2-contract-surface-parity-gate
plan: "02"
type: execute
wave: 2
depends_on: ["31.2-01"]
files_modified:
  - src/server/internal/http/route_surface.go
  - src/server/internal/http/route_surface_test.go
  - src/server/internal/http/router.go
  - src/server/internal/surfacereport/http.go
  - src/server/internal/surfacereport/http_test.go
autonomous: true
requirements: [RELS-02]
must_haves:
  truths:
    - "One runtime `RouteSurfaceRegistrar` declaration owns method/path, middleware/auth/CSRF/guard, handler dispatch and descriptor metadata together; no OpenAPI-derived JSON constructs runtime descriptors or mounts (D-SUR-02, D-SUR-03)."
    - "The router foundation exposes one typed runtime descriptor per registered operation and establishes the exact registrar handoff used by the later route-helper migration plans (D-SUR-02, D-SUR-03)."
    - "Runtime descriptors own method/path/operation/security/CSRF/capability/request/response refs only; browser encoder, decoder, and event fields are structurally impossible (D-SUR-02)."
    - "Excluded operations have neither mount nor descriptor, and a mount without a descriptor or descriptor without a mount fails (D-SUR-03)."
    - "The `http-runtime` details constructor uses Phase 31 trusted identity and typed details registration while remaining E1/E2 repository-local; the producer CLI is owned after all route helpers migrate (D-SUR-15, D-SUR-20)."
  artifacts:
    - path: "src/server/internal/http/route_surface.go"
      provides: "Same-source runtime registrations, real method/subroute dispatch and immutable descriptor registry"
      exports: ["RouteSurfaceRegistration", "RouteSurfaceDescriptor", "RouteSurfaceRegistrar", "NewRouteSurfaceRegistrar"]
    - path: "src/server/internal/surfacereport/http.go"
      provides: "Typed http-runtime details registration and report constructor"
      exports: ["HTTPRuntimeSurfaceID", "HTTPRuntimeDetails", "NewHTTPRuntimeReport"]
  key_links:
    - from: "src/server/internal/http/router.go"
      to: "src/server/internal/http/route_surface.go"
      via: "router-owned method/subroute mounts use RouteSurfaceRegistrar.Register; route-helper migrations are owned by Plans 13 and 14"
      pattern: "RouteSurfaceRegistrar|RouteSurfaceRegistration"
    - from: "src/server/internal/http/route_surface.go"
      to: "docs/api/route-surface-manifest.json"
      via: "runtime snapshot joins a separately loaded compare-only projection after registration"
      pattern: "CompareRouteSurfaceSnapshot"
    - from: "src/server/cmd/release-http-surface/main.go"
      to: "src/server/internal/surfacereport/http.go"
      via: "typed report constructor; producer CLI is owned by Plan 15"
      pattern: "NewHTTPRuntimeReport"
---

<objective>
把 method/path/middleware/auth/CSRF/guard/handler/descriptor 收拢到真实 Go runtime registrar，并先让 router-owned migrated subset 与 Plan 01 compare-only projection 双向 join；Plans 13/14 完成 route-helper migration 后，Plan 15 才对完整 runtime inventory 运行同一 comparator 并产出 `http-runtime` report。

Purpose: 证明 registrar/comparator 对已迁移 router-owned subset 具有真实双向行为约束，而不是由测试从 Go source 猜测 handler 行为；本计划不提前声明完整 route inventory parity。
Output: typed descriptor registry、router-owned subset wiring、subset bidirectional behavior tests、registered report adapter 和供 Plan 15 完整 producer 使用的 snapshot/mutation seam。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-PATTERNS.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VALIDATION.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-01-SUMMARY.md
@.planning/phases/31-release-contract-current-baseline/31-04-PLAN.md
@.planning/phases/31.1-readiness-fail-closed/31.1-03-PLAN.md
@src/server/internal/http/router.go
@src/server/internal/http/route_surface_test.go
@src/server/internal/http/chat_actions_handler.go

<interfaces>
Before implementation, require the executed upstream artifacts exactly as declared: Phase 31 `IdentityProvider`, `ProfileResolver`, `RegisterDetails`, `SurfaceReportV1`, `AtomicWriter`; Phase 31.1 `RouterOptions{Readiness,Guard,Effects,Authorities}`; and Plan 01 `PublicOperationScopeV1`/`OperationContractMetadataV1`. Correct the owning plan if an interface differs instead of adding a compatibility authority.

This plan adds this exact runtime authority, which is populated by Go route declarations only:

`RouteSurfaceRegistration{Method,Path,OperationID,MiddlewareIDs,Auth,CSRF,GuardEffectID,CapabilityID,Request,SuccessResponses,Handler}`

`func (r *RouteSurfaceRegistrar) Register(reg RouteSurfaceRegistration) error`

`func (r *RouteSurfaceRegistrar) Snapshot() []RouteSurfaceDescriptor`

`func CompareRouteSurfaceSnapshot(scope PublicOperationScopeV1, expected []OperationContractMetadataV1, actual []RouteSurfaceDescriptor) (HTTPRuntimeObservation, error)`

`Register` derives the immutable descriptor from the same registration value, composes the declared middleware/auth/CSRF/current guard, and mounts the exact method plus subroute handler. `Request` and `SuccessResponses` use Plan 01 `SchemaIdentityV1`. The derived manifest is loaded only by the comparison command/tests after runtime registrations exist; it is never decoded to populate `RouteSurfaceRegistration`, `Handler`, middleware, guard, or descriptor state. No Go type contains browser encoder, response decoder, or event identity.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the same-source registrar and migrate router-owned mounts</name>
  <read_first>
    - src/server/internal/http/router.go
    - src/server/internal/http/route_surface_test.go
    - docs/api/route-surface-manifest.json
    - src/server/internal/http/routes_chat.go
    - src/server/internal/http/chat_actions_handler.go
  </read_first>
  <files>src/server/internal/http/route_surface.go, src/server/internal/http/route_surface_test.go, src/server/internal/http/router.go</files>
  <behavior>
    - V31.2-S02/T-31.2-01: each registration value drives exact method/path/subroute dispatch, middleware/auth/CSRF/current guard, handler and derived descriptor; mutating any one member changes behavior/parity or fails construction.
    - For the nonempty router-owned migrated subset, method/path/operation/security/CSRF/capability/request media/schema and all success response status/media/schema match the corresponding Plan 01 included-scope rows in both directions; complete inventory closure is deferred to Plan 15 after Plans 13/14.
    - A runtime mount lacking a descriptor, a descriptor lacking an exact mounted pattern, duplicate descriptors, zero descriptors, or an excluded descriptor/mount fails construction or the focused suite.
    - Markdown runtime `text/markdown; charset=utf-8` is compatible with OpenAPI base media `text/markdown`; wrong base media or copied browser fields fail.
  </behavior>
  <action>Implement the exact closed `RouteSurfaceRegistration`, `RouteSurfaceDescriptor`, and deterministic `RouteSurfaceRegistrar` APIs above. Migrate router-owned direct mounts through `Register(reg)` and establish the injected registrar seam; route-helper migrations are explicitly owned by Plans 13 and 14 and must complete before the producer command in Plan 15. Refactor `route_surface_test.go` away from source strings that pin legacy helper call signatures and establish the staged AST rule: a migrated production router must call the explicit registrar entrypoint, while an unmigrated helper may remain only until its owning wave. The later legacy `registerXRoutes(*ServeMux, ...)` test adapters may only construct a local registrar and delegate; they cannot contain metadata, direct Handle calls, globals, mux lookups, or production call sites. Do not decode/embed Plan 01 JSON to create any registration or descriptor. After registration, load Plan 01 metadata as a separate compare input and exact-join the immutable snapshot; use Go AST discovery only as an independent completeness guard. Reject prefix-only coverage, direct ServeMux bypass, duplicate method/path, absent exact method/subroute dispatch, descriptor-only/handler-only declarations, excluded runtime registration, unknown capability and empty inventory. Add real-handler media probes for markdown, SSE, JSON, multipart request and 204 using `mime.ParseMediaType`. Store only canonical schema identities, never bodies/frontend fields.</action>
  <acceptance_criteria>
    - Router-owned operations have exactly one same-source registration/descriptor/handler dispatch; route-helper operations are closed by Plans 13/14 before producer parity.
    - Both parity directions compare the nonempty router-owned migrated subset, with nonzero descriptor/mount counts; Plan 15 is the only plan that may claim complete server-core parity after all route helpers migrate.
    - Go compile-time/runtime tests prove browser extension fields cannot enter the descriptor.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestRouteSurfaceRuntimeDescriptorParityContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.2-S02 provides the non-vacuous registrar/comparator foundation and router-owned subset bidirectional parity without browser ownership; full-core parity remains Plan 15-owned.</done>
</task>

<task type="auto">
  <name>Task 2: Register and construct the typed http-runtime report</name>
  <read_first>
    - src/server/internal/http/route_surface.go
    - src/server/internal/surfacereport/report.go
    - src/server/internal/surfacereport/registry.go
    - src/server/internal/surfacereport/writer.go
    - src/server/internal/buildinfo/provider.go
    - src/server/internal/releasecontract/load.go
  </read_first>
  <files>src/server/internal/surfacereport/http.go, src/server/internal/surfacereport/http_test.go</files>
  <behavior>
    - `HTTPRuntimeSurfaceID` is exactly `http-runtime` and has one typed details registration with nonzero operation counts and source/consumer digests.
    - Release identity and committed profile come only from Phase 31 resolvers; observations cannot supply commit/tree/contract/profile/evidence class.
    - Non-empty drift, skipped checks, failed mounted/core join, unknown details, or E3/E4 claims cannot produce a passing report.
  </behavior>
  <action>Define exact `http-runtime` details for operation count, mounted count, descriptor count, core digest, runtime digest, media-probe count, and parity result; register only under `http-runtime`. Implement side-effect-free `NewHTTPRuntimeReport` using the same explicit repoRoot/contract/schema/profile parameters and Phase 31 trusted identity/profile resolvers as upstream producers. Set canonical source `docs/api/openapi.yaml`, consumer `runtime-route-registry`, version `v1`; carry mismatch IDs only through `drift` and stable error codes. Validate nonzero counts, digest shape, count equality, empty pass drift/skips, and repository-local evidence. Do not accept a writer or output path in the constructor. After the production implementation is complete, add and run `TestHTTPRuntimeSurfaceContract` as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Valid parity observations create one schema-valid nested report.
    - Identity/profile substitution, arbitrary details, zero counts, drift, skip, and claim inflation fail deterministically.
    - The registry remains shared with Phase 31 rather than replaced.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/surfacereport '^TestHTTPRuntimeSurfaceContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>The phase owns exactly one typed `http-runtime` registration/report and no caller-owned identity path.</done>
</task>

<task type="auto">
  <name>Task 3: Expose the registrar snapshot and runtime mutation suite</name>
  <read_first>
    - src/server/internal/http/route_surface.go
    - src/server/internal/http/route_surface_test.go
    - src/server/internal/surfacereport/http.go
    - src/server/internal/surfacereport/writer.go
    - scripts/run-go-tests-matched.sh
  </read_first>
  <files>src/server/internal/http/route_surface.go, src/server/internal/http/route_surface_test.go</files>
  <behavior>
    - The registrar snapshot is built from runtime declarations without contract-derived registration; manifest comparison remains downstream of registration.
    - Focused fixtures mutate one descriptor/core/media field and assert stable nonzero codes; selectors and descriptor counts must be nonzero.
    - Empty or split registration/descriptor state remains nonzero and cannot produce a passing snapshot.
  </behavior>
  <action>Expose the registrar snapshot and mutation fixture seam only. Plan 15 owns the release-http-surface command, shell producer and clean-head wiring after all route-helper registrations are complete. Fixtures cover direct mount bypass, omitted/extra/duplicate registration, descriptor/handler split, wrong middleware/auth/CSRF/guard/capability/request/response identity, markdown base-media, browser field injection, excluded registration and zero inventory. Every Go selector uses `run-go-tests-matched.sh`; fixtures assert nonzero case, registration, handler-dispatch and descriptor counts.</action>
  <acceptance_criteria>
    - Registrar snapshot can run independently and emits exact nonzero registration/descriptor evidence.
    - Every runtime/core/media mutation and empty selector is a hard failure.
    - Output contains repository-relative IDs/digests/counts only and no request body or environment path.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/http '^TestRouteSurfaceSnapshotContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>The independently runnable snapshot verifier preserves nonzero-discovery and runtime-parity failure semantics for the Plan 15 producer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| separately loaded compare-only OpenAPI projection -> Go registry | Derived contract metadata is bound to actual mounted handlers only after same-source registration. |
| runtime observations -> nested report | Router behavior enters release evidence without authoring identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-01 | Tampering | runtime descriptor parity | high | mitigate | Exact mount/descriptor/core joins, complete field/media mutations, typed descriptors, and nonzero counts. |
| T-31.2-06 | Spoofing | http-runtime report identity | high | mitigate | Phase 31 trusted resolvers, typed details registry, atomic writer, and caller identity rejection. |
</threat_model>

<verification>
- Run the three named matched Go suites, runtime snapshot mutation fixtures, and `git diff --check`.
- Focused producer creation is owned by Plan 15 and final report execution by Plan 09's clean aggregate; registrar, comparison and report construction remain independently runnable here.
- Evidence remains repository-local E1/E2 and is a RELS-02 contribution only.
</verification>

<success_criteria>
- Runtime same-source registrations and descriptors exact-join the nonempty router-owned migrated subset in both directions; derived JSON is compare-only, and Plan 15 owns the complete included public server-core join after Plans 13/14.
- Browser extension ownership is absent from Go.
- `http-runtime` details/report construction is registered with trusted identity and no skips; Plan 15 owns its single producer emission.
- No target/commercial readiness claim is made.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-02-SUMMARY.md` when done.
</output>
