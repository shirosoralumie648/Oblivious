---
phase: 31.2-contract-surface-parity-gate
plan: "20"
subsystem: frontend-http-stream
tags: [typescript, sse, crlf, tdd, stream]

# Dependency graph
requires:
  - phase: 31.2-contract-surface-parity-gate
    plan: "04"
    provides: "Caller-supplied validated streaming transport contracts and rawResponseDecoder"
provides:
  - "CRLF-aware buffered SSE frame boundary parsing in the shared stream transport"
  - "Protocol-correct emitBufferedChunks handling LF (\\n\\n) and CRLF (\\r\\n\\r\\n) delimiters"
affects: [RELS-02]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Dual blank-line boundary search: find both \\r\\n\\r\\n and \\n\\n independently and consume the exact matched delimiter length"
    - "Incomplete delimiter suffix bytes remain buffered across decoder reads"

key-files:
  created: []
  modified:
    - src/web/src/services/http/stream.ts
    - src/web/src/services/http/stream.test.ts

key-decisions:
  - "Dual-search for \\r\\n\\r\\n and \\n\\n independently; whichever starts earlier is consumed with its exact length (4 or 2 bytes)"
  - "emitFrame and all public APIs remain unchanged; only emitBufferedChunks boundary detection is updated"

# Metrics
duration: 4 min
completed: 2026-07-26
status: complete
---

# Phase 31.2 Plan 20: CRLF-Aware SSE Frame Boundaries Summary

**CRLF-aware blank-line boundary parsing added to the shared SSE stream transport; LF and CRLF frames now emit independently without merging, split delimiters buffer correctly, and [DONE]/error semantics remain exact.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-07-26T03:47:32Z
- **Completed:** 2026-07-26T03:51:00Z
- **Tasks:** 2 completed (TDD RED + GREEN)
- **Files modified:** 2

## Accomplishments

- Added four focused CRLF regression tests (multi-frame, split-boundary, [DONE], error) plus one LF compatibility regression assertion to the `streamText` Vitest suite.
- Replaced the fixed `indexOf('\n\n') + 2-char slice` in `emitBufferedChunks` with a dual-search that locates both `\r\n\r\n` (delimLen=4) and `\n\n` (delimLen=2) independently, consumes the exact matched length, and loops until no complete boundary remains.
- All 20 tests in `stream.test.ts` and `client.test.ts` pass; TypeScript compilation is clean; `git diff --check` is clean.

## Task Commits

1. **Task 1 RED: Failing CRLF tests** — `540652f` (`test`)
2. **Task 2 GREEN: CRLF-aware emitBufferedChunks** — `a03ec52` (`feat`)

## Files Created/Modified

- `src/web/src/services/http/stream.ts` — `emitBufferedChunks` now searches for `\r\n\r\n` and `\n\n` separately, uses the earlier match, consumes delimLen bytes, and continues the loop.
- `src/web/src/services/http/stream.test.ts` — New `CRLF frame boundaries` describe block with 5 cases; existing LF and contract-identity tests unchanged.

## TDD Gate Compliance

- RED gate commit `540652f`: 4 CRLF cases fail, 1 LF regression passes. Exit code non-zero confirmed.
- GREEN gate commit `a03ec52`: 20/20 tests pass, TypeScript clean.
- No REFACTOR pass required; implementation is already minimal and idiomatic.

## Decisions Made

- Dual-search (`indexOf('\r\n\r\n')` and `indexOf('\n\n')`) rather than regex so neither sequence is a substring of the other and no ambiguous match is possible.
- `emitFrame` receives no carriage-return residue because the exact 4-byte delimiter is consumed before slicing the frame prefix.
- `emitFrame`, `streamText`, `onChunk`, `[DONE]`, and `event: error` semantics are unchanged.

## Deviations from Plan

None — plan executed exactly as written.

## Verification

- `pnpm --dir src/web exec vitest run src/services/http/stream.test.ts src/services/http/client.test.ts --reporter=verbose` — PASS; 2 files, 20/20 tests.
- `pnpm --dir src/web exec tsc --noEmit` — PASS.
- `git diff --check` — PASS.

## Known Stubs

None.

## Threat Flags

No new network endpoints, auth paths, file access patterns, or schema changes were introduced.

## User Setup Required

None.

## Evidence Boundary

This plan contributes repository-local E1/E2 evidence to RELS-02. It does not prove live Provider stream evidence, target/live behavior, E3/E4 evidence, or Phase 31.2 completion.

## Self-Check: PASSED

- `src/web/src/services/http/stream.ts` — exists and contains `emitBufferedChunks`.
- `src/web/src/services/http/stream.test.ts` — exists and contains `streamText`.
- Commit `540652f` (RED) and `a03ec52` (GREEN) both resolve on branch `commercial-target-release-runner`.
