---
phase: 31.2-contract-surface-parity-gate
plan: "12"
type: execute
wave: 3
depends_on: ["31.2-01", "31.2-04"]
files_modified:
  - src/web/src/features/marketplace/api.ts
  - src/web/src/features/mcp/mcpServersApi.ts
  - src/web/src/features/notifications/notificationsApi.ts
  - src/web/src/features/publishingChannels/publishingChannelsApi.ts
  - src/web/src/features/scheduledTasks/scheduledTasksApi.ts
  - src/web/src/features/tasks/api.ts
  - src/web/src/features/tasks/api.test.ts
  - src/web/src/features/workflows/workflowsApi.ts
  - src/web/src/routes/admin/AdminHomePage.tsx
autonomous: true
requirements: [RELS-02]
must_haves:
  truths:
    - "All eight Group B production owners explicitly import/use Plan 01 generated operation metadata and pass exact symbols into Plan 04 transports; path lookup, local core objects and generic-only identity are forbidden (D-SUR-02, D-SUR-05, D-SUR-06)."
    - "Request encoder and selected success status/media/schema identity come from generated symbols across Marketplace, MCP, notifications, channels, schedules, tasks, workflows and Admin SWR (D-SUR-02)."
    - "Existing feature APIs/cache keys/tests remain compatible; each listed file has a nonzero compiler-AST caller/owner disposition and cannot be silently removed (D-SUR-05, D-SUR-07)."
    - "MCP/catalog mutation behavior preserves Phase 31.1 strict capability rejection while adopting exact operation metadata (D-SUR-08)."
  artifacts:
    - path: "src/web/src/features/mcp/mcpServersApi.ts"
      provides: "Exact MCP generated operation metadata with strict mutation shape"
      contains: "operationContracts"
    - path: "src/web/src/features/workflows/workflowsApi.ts"
      provides: "Exact workflow operation contracts across JSON/streaming actions"
      contains: "operationContracts"
    - path: "src/web/src/routes/admin/AdminHomePage.tsx"
      provides: "Exact generated operation metadata forwarded through the SWR owner"
      contains: "operationContracts"
  key_links:
    - from: "src/web/src/generated/operation-contracts.generated.ts"
      to: "src/web/src/features/workflows/workflowsApi.ts"
      via: "concrete operation members passed to shared transport contracts"
      pattern: "operationContracts"
    - from: "scripts/verify_frontend_operation_contract_usage.mjs"
      to: "src/web/src/routes/admin/AdminHomePage.tsx"
      via: "compiler-symbol resolution and nonzero owner/import/use disposition"
      pattern: "operation-contract-usage/v1"
---

<objective>
迁移 Group B 的八个 production caller owners，让所有 transport 调用显式携带 generated operation/schema identity，并保持既有 feature API 与 SWR 行为。

Purpose: 完成剩余 caller ownership，使后置 Plan 03 可以做 exact 25-owner production closure，而不是事后猜 contract。
Output: Marketplace/MCP/notification/channel/schedule/task/workflow/Admin caller migration 与 nonzero AST proof。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VALIDATION.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-01-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-04-SUMMARY.md
@src/web/src/generated/operation-contracts.generated.ts
@scripts/verify_frontend_operation_contract_usage.mjs

<interfaces>
Every concrete caller imports `operationContracts.<operationId>` and passes that exact generated symbol to `OperationTransportContract`, HttpClient, stream, upload or SWR. Method/path, request identity and success alternatives are read from the symbol; caller selects only a declared codec/status. No runtime path/core lookup or TypeScript generic synthesis is allowed.

The eight production owner files are fixed expected owners. A compiler-proven non-caller remains explicitly dispositioned with a reason/test; it is not deleted from ownership. Existing tests are read-only verification inputs, except the absent `src/web/src/features/tasks/api.test.ts`, which this plan creates only after the Tasks production migration is complete.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Marketplace, MCP and publishing-channel callers</name>
  <read_first>
    - src/web/src/features/marketplace/api.ts
    - src/web/src/features/mcp/mcpServersApi.ts
    - src/web/src/features/publishingChannels/publishingChannelsApi.ts
    - src/web/src/services/http/client.ts
    - src/web/src/services/http/stream.ts
  </read_first>
  <files>src/web/src/features/marketplace/api.ts, src/web/src/features/mcp/mcpServersApi.ts, src/web/src/features/publishingChannels/publishingChannelsApi.ts</files>
  <behavior>
    - Every Marketplace/MCP/channel call imports and passes its exact generated operation member and declared codec/status/schema identity.
    - MCP create/execute mutations still omit/reject capabilityId and unknown authority fields.
    - Existing feature tests/interfaces remain passing; missing/wrong symbol and local/path/generic reconstruction fail.
  </behavior>
  <action>After the Plan 04 transport contract is available, migrate each production callsite to exact generated members first. Preserve exported API shapes, structured errors and streaming behavior. Keep MCP capability identity server-owned and mutation-safe. Do not construct core metadata locally, select by URL, or infer from generic response names. After implementation, run the existing named tests and compiler-symbol preflight over all three owners as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Three owners have exact symbol coverage with positive AST counts.
    - MCP mutation authority remains strict.
    - Existing Marketplace/MCP/channel behavior remains compatible.
  </acceptance_criteria>
  <verify><automated>pnpm --dir src/web exec vitest run src/features/marketplace/api.test.ts src/features/mcp/mcpServersApi.test.ts src/features/publishingChannels/publishingChannelsApi.test.ts --reporter=verbose &amp;&amp; pnpm --dir src/web exec tsc --noEmit &amp;&amp; node scripts/verify_frontend_operation_contract_usage.mjs --tsconfig src/web/tsconfig.json --expect-owner src/web/src/features/marketplace/api.ts --expect-owner src/web/src/features/mcp/mcpServersApi.ts --expect-owner src/web/src/features/publishingChannels/publishingChannelsApi.ts --require-all &amp;&amp; git diff --check</automated></verify>
  <done>Marketplace/MCP/channel callers carry exact generated contracts without capability escalation.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate notification, scheduled-task and task callers</name>
  <read_first>
    - src/web/src/features/notifications/notificationsApi.ts
    - src/web/src/features/scheduledTasks/scheduledTasksApi.ts
    - src/web/src/features/tasks/api.ts
    - src/web/src/services/http/client.ts
  </read_first>
  <files>src/web/src/features/notifications/notificationsApi.ts, src/web/src/features/scheduledTasks/scheduledTasksApi.ts, src/web/src/features/tasks/api.ts, src/web/src/features/tasks/api.test.ts</files>
  <behavior>
    - Notification, schedule and task operations pass concrete generated symbols through shared HttpClient contracts.
    - Request/selected-success identities and 204/JSON decoding come only from those symbols.
    - Existing cache/update/error behavior and feature tests remain passing; zero/missing/ambiguous symbol uses fail.
  </behavior>
  <action>Migrate all calls in the three production files to generated operation members and Plan 04 encoders/decoders first. Preserve external APIs, polling/cache keys and mutation semantics. Do not use a path lookup, local metadata object or generic type as identity. After the Tasks production migration, create `src/web/src/features/tasks/api.test.ts` and run it with the existing notification/scheduled-task tests plus the exact-owner preflight as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Three owners have complete exact-symbol coverage and positive AST counts.
    - JSON/none decoder selection matches generated success metadata.
    - Existing behavior remains compatible.
  </acceptance_criteria>
  <verify><automated>pnpm --dir src/web exec vitest run src/features/notifications/notificationsApi.test.ts src/features/scheduledTasks/scheduledTasksApi.test.ts src/features/tasks/api.test.ts --reporter=verbose &amp;&amp; pnpm --dir src/web exec tsc --noEmit &amp;&amp; node scripts/verify_frontend_operation_contract_usage.mjs --tsconfig src/web/tsconfig.json --expect-owner src/web/src/features/notifications/notificationsApi.ts --expect-owner src/web/src/features/scheduledTasks/scheduledTasksApi.ts --expect-owner src/web/src/features/tasks/api.ts --require-all &amp;&amp; git diff --check</automated></verify>
  <done>Notification/schedule/task callers use exact generated operation/schema identity.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate Workflow and Admin SWR and close Group B</name>
  <read_first>
    - src/web/src/features/workflows/workflowsApi.ts
    - src/web/src/routes/admin/AdminHomePage.tsx
    - src/web/src/features/workflows/workflowsApi.test.ts
    - src/web/src/lib/swr.ts
    - scripts/verify_frontend_operation_contract_usage.mjs
  </read_first>
  <files>src/web/src/features/workflows/workflowsApi.ts, src/web/src/routes/admin/AdminHomePage.tsx</files>
  <behavior>
    - Workflow calls and Admin SWR fetcher pass exact generated operation symbols with declared encoders/decoders/status/schema.
    - All eight Group B files appear exactly once as resolved callers/owners or explicit compiler-proven non-callers.
    - A silent owner deletion, new local lookup, generic-only identity, zero count or duplicate disposition fails.
  </behavior>
  <action>Migrate Workflow operations and AdminHomePage SWR usage to exact generated metadata while preserving feature/page interfaces, cache keys and rendering behavior. Run one Group B preflight across all eight expected owners. If compiler AST proves a listed file is not a transport caller, retain it with explicit tested disposition/reason; never remove it. Require exact set equality and positive counts.</action>
  <acceptance_criteria>
    - Workflow/Admin metadata is generated-symbol-derived and existing tests remain passing.
    - Group B has exact 8/8 owner dispositions with nonzero applicable counts.
    - No path/core lookup or generic synthesis remains.
  </acceptance_criteria>
  <verify><automated>pnpm --dir src/web exec vitest run src/features/workflows/workflowsApi.test.ts src/routes/admin/AdminHomePage.test.tsx --reporter=verbose &amp;&amp; pnpm --dir src/web exec tsc --noEmit &amp;&amp; node scripts/verify_frontend_operation_contract_usage.mjs --tsconfig src/web/tsconfig.json --expect-owner src/web/src/features/marketplace/api.ts --expect-owner src/web/src/features/mcp/mcpServersApi.ts --expect-owner src/web/src/features/notifications/notificationsApi.ts --expect-owner src/web/src/features/publishingChannels/publishingChannelsApi.ts --expect-owner src/web/src/features/scheduledTasks/scheduledTasksApi.ts --expect-owner src/web/src/features/tasks/api.ts --expect-owner src/web/src/features/workflows/workflowsApi.ts --expect-owner src/web/src/routes/admin/AdminHomePage.tsx --require-all &amp;&amp; git diff --check</automated></verify>
  <done>Group B is a closed eight-owner exact-operation-symbol migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated operation metadata -> Group B callers | Canonical request/response identity controls production transport behavior. |
| MCP/catalog response -> mutation | Read-only capability identity cannot become client authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-01 | Tampering | caller contract identity | high | mitigate | Exact generated symbols, typed codec selection, named tests and compiler preflight. |
| T-31.2-02 | Tampering | hidden/new caller | high | mitigate | Fixed eight-owner set, explicit dispositions and nonzero counts. |
| T-31.2-03 | Elevation of Privilege | MCP capability mutation | high | mitigate | Strict existing mutation DTO, no capability mapping, exact response-only tests. |
</threat_model>

<verification>
- Run named existing Vitest files plus the plan-owned post-implementation Tasks API test, TypeScript no-emit, Group B compiler-symbol preflight and `git diff --check`.
- Plan 03 later joins Group B with Plan 04/10/11 owners and rejects any new production caller.
- Evidence remains E1 repository-local only.
</verification>

<success_criteria>
- Eight fixed Group B files have explicit exact generated operation metadata ownership.
- Request/response schema identity is symbol-derived, never path/generic-derived.
- Existing feature/page behavior stays compatible.
- MCP capability identity remains response-only and mutation-safe.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-12-SUMMARY.md` when done.
</output>
