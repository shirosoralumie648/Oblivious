---
phase: 31.2-contract-surface-parity-gate
plan: "09"
subsystem: release-contract-aggregation
tags: [release-contract, clean-head, docker, protobuf, migration, redaction, ci]

requires:
  - phase: 31-release-contract-current-baseline
    provides: "Trusted clean-Git build identity, immutable release image inspection, and the shared surface-report envelope"
  - phase: 31.1-readiness-fail-closed
    provides: "Repository-local readiness/deployment harness and aggregate-consume artifact-bundle contract"
  - phase: 31.2-contract-surface-parity-gate
    provides: "Plans 01-08 and 10-15 typed HTTP, frontend, protobuf, migration, readiness, deployment, and build report producers"
provides:
  - "Exact ten-surface aggregate with strict typed details, one trusted identity, pass/no-drift/no-skip outcomes, and recursive redaction"
  - "Sole quality-parent producer DAG with one immutable image build, one grouped frontend scan, one readiness/deployment consume, and one migration replay session"
  - "Clean-head repository-local Stage B proof for the complete Phase 31.2 contract surface"
affects: [phase-31.2-verification, phase-32, release-quality-gates, RELS-02]

tech-stack:
  added: []
  patterns:
    - "Build one immutable release artifact bundle and pass its exact tag and digest to all identity-bearing consumers"
    - "Aggregate only an exact nested report set whose identities, typed details, outcomes, producer statuses, and surface execution counts all fail closed"
    - "Require consecutive dependency health successes so an initialization server cannot satisfy a stable readiness boundary"

key-files:
  created: []
  modified:
    - src/server/cmd/release-contract/main_test.go
    - scripts/verify_release_contract.py
    - scripts/verify-release-contract.sh
    - scripts/verify-quality-gates.sh
    - scripts/verify-readiness-deployment-harness.sh
    - scripts/check.sh
    - .github/workflows/ci.yml
    - docs/release/commercial-gates.md
    - docs/release/rc-checklist.md

key-decisions:
  - "Quality is the only direct aggregate parent; check, commercial verification, and CI reach it transitively and aggregate producers never call an ancestor."
  - "The release session builds one image bundle, records zero readiness rebuilds, and invokes the migration session once with no independent migration-static pre-run."
  - "A ClickHouse readiness observation must succeed in consecutive polling intervals because the official entrypoint exposes a transient initialization server before its final server restart."
  - "The passing result contributes repository-local E1/E2 evidence to RELS-02 only; target Kubernetes, commercial rails, and external target inspection remain deferred."

patterns-established:
  - "Every surface report is strict-validated by the Go registry before the Python aggregate checks exact set, identity, unique key, drift, outcome, skips, and producer execution state."
  - "Clean-head evidence is generated only after an empty Git porcelain check; dirty, staged, untracked, identity-spliced, duplicate, folded, and sensitive-value cases execute in disposable fixtures."

requirements-completed: [RELS-02]

coverage:
  - id: D1
    description: "Exactly ten nested surface reports share one trusted clean-head identity and pass strict typed-details, unique-key, no-drift, no-error, and no-skip validation."
    requirement: RELS-02
    verification:
      - kind: unit
        ref: "src/server/cmd/release-contract/main_test.go#TestRunVerifyAllSurfaceReportTypesContract"
        status: pass
      - kind: integration
        ref: "python3 scripts/verify_release_contract.py --fixtures"
        status: pass
      - kind: integration
        ref: "clean clone: bash scripts/verify-quality-gates.sh"
        status: pass
    human_judgment: false
  - id: D2
    description: "The fixed release DAG executes every session producer and report constructor once, builds one immutable image, performs no readiness rebuild, and runs one migration session without a static pre-run."
    requirement: RELS-02
    verification:
      - kind: integration
        ref: "bash scripts/verify-release-contract.sh --fixtures --identity --stage-a"
        status: pass
      - kind: integration
        ref: ".tmp/release-contract.UOrCDc/producer-status.json#all executions one, rebuildCount zero, migrationStaticPreRunCount zero"
        status: pass
    human_judgment: false
  - id: D3
    description: "Clean-source identity, structured redaction, CI protobuf bootstrap, and operator claim language fail closed while retaining an E1/E2 evidence ceiling."
    requirement: RELS-02
    verification:
      - kind: integration
        ref: "python3 scripts/verify_release_contract.py --fixtures --redaction"
        status: pass
      - kind: integration
        ref: "clean clone aggregate#redactedValueCount 4 and skippedChecks empty"
        status: pass
    human_judgment: false
  - id: D4
    description: "Readiness waits through the ClickHouse entrypoint initialization restart instead of accepting one transient successful query."
    requirement: RELS-02
    verification:
      - kind: integration
        ref: "scripts/verify-readiness-deployment-harness.sh#standalone-build full disposable Docker harness"
        status: pass
      - kind: integration
        ref: "bash scripts/verify-readiness-contract-fixtures.sh"
        status: pass
    human_judgment: false

duration: 1h 59m
completed: 2026-07-22
status: complete
---

# Phase 31.2 Plan 09: Exact Release Contract Aggregate Summary

**One clean-head release session now builds one immutable image and aggregates ten independently typed contract surfaces exactly once, with strict identity, drift, skip, producer-status, and redaction enforcement.**

## Performance

- **Duration:** 1h 59m
- **Started:** 2026-07-22T19:09:15+08:00
- **Completed:** 2026-07-22T21:08:07+08:00
- **Tasks:** 3 completed
- **Files modified:** 9 implementation, workflow, test, and operator-documentation files plus this SUMMARY

## Accomplishments

- Added an exact ten-report aggregate for `build-identity`, `readiness`, `deployment`, `http-runtime`, `frontend-transport`, `frontend-exposure`, `protobuf`, `migration-static`, `migration-ledger`, and `migration-replay`; all reports retain their nested typed envelope and one trusted release identity.
- Established `verify-quality-gates.sh` as the only direct aggregate parent. The observed producer status records one execution for every session producer, surface, and report constructor, with `rebuildCount: 0` and `migrationStaticPreRunCount: 0`.
- Enforced clean-commit identity, recursive public-output redaction, exact CI protobuf toolchain bootstrap/cache checks, and operator documentation that caps this proof at repository-local E1/E2.
- Hardened the disposable readiness harness against ClickHouse's transient initialization server by requiring stable consecutive health observations before migration and server startup.

## Task Commits

1. **Task 1: Validate the exact nested report set and trusted identity** - `82f9820` (`feat`)
2. **Task 2: Enforce the sole-owner DAG and one execution per producer** - `1e82f38` (`feat`)
3. **Task 3: Close clean-identity, redaction, operator-doc, and E1/E2 claim gates** - `ad514cb` (`feat`)

Post-task clean-gate repairs were committed independently:

- `570329c` - aligned quality route assertions with the new sole-parent topology.
- `2aed196` - aligned the migration quality assertion with the grouped session owner.
- `265b854` - aligned replay quality assertions with the exact session contract.
- `1b56635` - required stable consecutive ClickHouse readiness after reproducing the entrypoint restart race.

This SUMMARY and the phase trackers are committed separately as plan metadata.

## Decisions Made

- Python accepts only reports that first pass the shared Go typed-details registry; it compares readiness identity and outcome but does not become a second freshness authority.
- Grouped producers remain explicit: one frontend sidecar yields two distinct reports, one readiness/deployment harness yields two reports, and one migration session yields static, ledger, and replay reports.
- Clean-head aggregation writes only to a new ignored output directory and rejects dirty, staged, untracked, stale-output, duplicate, folded, spliced, failed, partial, or credential-bearing evidence.
- Frontend workspace dependencies are a declared verification prerequisite. The successful fresh-clone run used `pnpm install --frozen-lockfile` before the exact quality command; installation left Git porcelain empty.

## Deviations from Plan

### Auto-fixed Issues

**1. Quality assertions still described the superseded direct-owner topology**
- **Found during:** Post-Task 3 clean-head quality gate
- **Issue:** Three source assertions expected older route, migration, and replay ownership even though the runtime fixtures proved the new sole-parent DAG.
- **Fix:** Updated only the stale assertions to match the executable topology.
- **Files modified:** `scripts/verify-quality-gates.sh`
- **Verification:** Each repair was committed separately and the complete clean-head gate was restarted.
- **Committed in:** `570329c`, `2aed196`, and `265b854`

**2. ClickHouse initialization exposed a transient successful server before restart**
- **Found during:** Clean-clone aggregate readiness/deployment producer
- **Issue:** Docker events reproducibly showed ClickHouse client exits `210 -> 0 -> 32`; the original loop accepted the temporary initialization server and the immediate final check collided with the final-server restart.
- **Fix:** Require two consecutive successful health observations and reset the count after any failure.
- **Files modified:** `scripts/verify-readiness-deployment-harness.sh`
- **Verification:** The pre-fix full standalone harness failed twice at `clickhouse_unavailable`; the byte-identical fix passed the complete standalone harness and the final clean-head quality gate.
- **Committed in:** `1b56635`

---

**Total deviations:** 2 auto-fixed correctness issues across 4 repair commits.
**Impact on plan:** Both changes preserve the planned fail-closed topology and no-skip evidence boundary; neither adds target/live scope.

## Validation Evidence

- **Command:** `pnpm install --frozen-lockfile`, then `test -z "$(git status --porcelain=v1 --untracked-files=all)" && bash scripts/verify-quality-gates.sh`
- **Environment:** fresh local Git clone, `clean-head`, repository-local E1/E2, disposable Docker dependencies, monolith profile
- **Result:** PASS (`exit 0`), Stage B `release-contract-verification/v1`
- **Release commit:** `1b56635c3fd1e954df39650f43fc3aae94d228b8`
- **Source tree:** `678a3bf589c264c3717d847f2089232b237926c4`
- **Contract digest:** `sha256:d7a048fd9fa1ad4eac5b3e81d24632abcb57c0802e566175c5c1254d6bb0b7b0`
- **Image digest:** `sha256:aa9aadeec6efcce906b08c3ae6911a3e1f18c261ab311c67d9b1c34f2ff0ce6d`
- **Output:** `.tmp/release-contract.UOrCDc`
- **Migration state:** `fresh-first-apply-second-no-op`; 0 initial rows, 112 first-apply rows, 0/112 second-apply applied/skipped, 112 final rows, cleanup succeeded
- **Surfaces:** all ten named surfaces passed once with empty missing, extra, incompatible, error, and skip arrays
- **Producer graph:** every session producer and report constructor passed once; no rebuild and no independent migration-static pre-run
- **Redaction:** policy `v1`, 4 public values redacted
- **Skipped checks:** none
- **Residual risks:** external target not inspected; target Kubernetes and commercial rails deferred; supply-chain attestations remain deferred

## User Setup Required

None for repository-local execution beyond the documented Go, Node/pnpm, Docker, and frontend dependency prerequisites. No Provider secret, payment credential, target evidence directory, or external environment was used.

## Evidence Boundary

This plan completes the repository-local E1/E2 contract-surface contribution to RELS-02. It does not provide E3/E4 evidence, apply the target Kubernetes workload, inspect an external target, validate live commercial rails, or approve a commercial release. Those claims remain blocked on their later target/live phases.

## Next Phase Readiness

- All 15 Phase 31.2 plans now have implementation summaries and the phase is ready for canonical phase verification.
- Phase 32 must consume the verified contract surfaces without treating the compare-only manifests, client projections, or repository-local aggregate as independent runtime authority.
- Protected user changes in `.planning/config.json`, `src/server/internal/releasecontract/effect_registry.go`, `src/server/internal/releasecontract/effect_registry_test.go`, and `.planning/intel/` remain unstaged and untouched.

## Self-Check: PASSED

- Confirmed task commits `82f9820`, `1e82f38`, and `ad514cb` plus repair commits `570329c`, `2aed196`, `265b854`, and `1b56635` exist on `commercial-target-release-runner`.
- Confirmed the final fresh clone was clean at the exact recorded commit before `verify-quality-gates.sh` and the command returned `0`.
- Confirmed the aggregate contains exactly ten passing reports, all producer/surface counts equal one, skips and drift arrays are empty, and the claim remains repository-local E1/E2 only.
- Confirmed the four protected user change paths remain outside every Plan 09 commit and outside this metadata change.

---
*Phase: 31.2-contract-surface-parity-gate*
*Completed: 2026-07-22*
