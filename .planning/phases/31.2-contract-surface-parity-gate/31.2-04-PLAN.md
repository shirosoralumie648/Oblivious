---
phase: 31.2-contract-surface-parity-gate
plan: "04"
type: execute
wave: 2
depends_on: ["31.2-01"]
files_modified:
  - src/web/src/services/http/client.ts
  - src/web/src/services/http/client.test.ts
  - src/web/src/features/chat/api.ts
  - src/web/src/features/chat/api.test.ts
  - src/web/src/services/http/stream.ts
  - src/web/src/services/http/stream.test.ts
  - src/web/src/services/http/upload.ts
  - src/web/src/services/http/upload.test.ts
  - src/web/src/lib/swr.ts
autonomous: true
requirements: [RELS-02]
must_haves:
  truths:
    - "The shared HttpClient selects request encoding and success decoding through typed contracts that carry/reference Plan 01 `OperationContractMetadataV1` and canonical `SchemaIdentityV1`, while preserving one structured error path (D-SUR-02, D-SUR-04)."
    - "Markdown export sends Accept text/markdown, accepts charset parameters, calls Response.text(), and never enters JSON envelope decoding (D-SUR-04)."
    - "JSON envelope, non-OK structured errors/redaction, 204 none, media mismatch, and contradictory Accept behavior remain explicit and tested (D-SUR-02, D-SUR-04)."
    - "Tests exercise the real shared client with real Fetch Response objects rather than mocking HttpClient.get (D-SUR-04)."
    - "Shared stream/upload/SWR transports accept exact generated operation metadata from callers; wrappers cannot recover core identity from path strings or TypeScript generics (D-SUR-02, D-SUR-05)."
  artifacts:
    - path: "src/web/src/services/http/client.ts"
      provides: "Typed operation-aware RequestEncoder/ResponseDecoder contracts and JSON/text/none/raw codecs"
      exports: ["RequestEncoder", "ResponseDecoder", "OperationTransportContract", "jsonRequestEncoder", "jsonEnvelopeDecoder", "textResponseDecoder", "noneResponseDecoder", "rawResponseDecoder"]
    - path: "src/web/src/features/chat/api.test.ts"
      provides: "Real shared-client markdown regression proof"
      contains: "text/markdown; charset=utf-8"
  key_links:
    - from: "src/web/src/features/chat/api.ts"
      to: "src/web/src/services/http/client.ts"
      via: "exportConversationMarkdown passes textResponseDecoder"
      pattern: "textResponseDecoder"
    - from: "src/web/src/services/http/client.ts"
      to: "src/web/src/features/chat/api.test.ts"
      via: "one named Markdown regression constructs the real shared client and returns a real Fetch Response"
      pattern: "downloads markdown through real shared client Response[\s\S]{0,2000}new Response[\s\S]{0,2000}createHttpClient\(\{"
---

<objective>
为 shared HttpClient 增加 typed success decoder，并修复真实 Markdown export 浏览器路径。

Purpose: 让 response media/decoder 成为可由 AST sidecar 和 runtime tests共同验证的显式契约，而不是固定 JSON 假设。
Output: typed decoder API、完整 media/error/204 tests、经过真实 shared client 的 Markdown export。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-PATTERNS.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VALIDATION.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-01-SUMMARY.md
@src/web/src/services/http/client.ts
@src/web/src/services/http/client.test.ts
@src/web/src/features/chat/api.ts
@src/web/src/features/chat/api.test.ts
@src/web/src/services/http/stream.ts
@src/web/src/services/http/upload.ts
@src/web/src/lib/swr.ts
@src/server/internal/http/chat_actions_handler.go
@docs/api/openapi.yaml
@src/web/src/generated/operation-contracts.generated.ts

<interfaces>
Consume Plan 01 exact generated types and preserve method ordering with an optional transport contract after `RequestInit`:

`SchemaIdentityV1` and `OperationContractMetadataV1` are imported from `@/generated/operation-contracts.generated` and are never recreated locally.

`RequestEncoder = {id:"none"|"json"|"form-data"|"raw",mediaType:string|null,schemaIdentity:SchemaIdentityV1}`

`ResponseDecoder<T> = {id:"json-envelope"|"text"|"raw-response"|"none",status:number,mediaType:string|null,schemaIdentity:SchemaIdentityV1,decode(response):Promise<T>}`

`OperationTransportContract<T> = {operation:OperationContractMetadataV1,requestEncoder:RequestEncoder,responseDecoder:ResponseDecoder<T>}`

`request/get/post/put/delete(..., contract?: OperationTransportContract<T>): Promise<T>`

The default remains JSON envelope for source compatibility, but its generic type is diagnostic only. Every production caller migration in Plans 04/10/11/12 must import a concrete `operationContracts.<operationId>` symbol and pass it into HttpClient/stream/upload/SWR. Shared wrappers import the generated metadata type and require the supplied symbol; they never derive identity from method/path/core lookup. Plan 03 later records those symbols and rejects generic-only parity.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add operation-aware request encoders and response decoders</name>
  <read_first>
    - src/web/src/services/http/client.ts
    - src/web/src/services/http/client.test.ts
    - src/web/src/services/http/errors.ts
    - src/web/src/services/http/envelope.ts
    - src/web/src/generated/operation-contracts.generated.ts
  </read_first>
  <files>src/web/src/services/http/client.ts, src/web/src/services/http/client.test.ts</files>
  <behavior>
    - V31.2-S04: default JSON sends `Accept: application/json`, validates JSON-compatible base media, calls `Response.json`, and unwraps the envelope.
    - Text decoder sends its declared Accept, accepts case-insensitive base media plus allowed charset, calls `Response.text`, and returns text directly.
    - 204 uses none and invokes no body decoder; raw response returns Response only for its declared media policy.
    - Contradictory caller Accept, missing/wrong success content type, malformed decoder, or body decoder on 204 fails before returning data.
    - Non-2xx JSON errors preserve stable code/data; non-JSON/malformed bodies never enter public error messages or details.
    - Encoder/decoder metadata must equal the selected generated operation request/success `SchemaIdentityV1`; wrong status/media/ref/inline digest, local duplicate metadata, or generic-only identity fails before transport.
  </behavior>
  <action>Import the exact generated `SchemaIdentityV1`/`OperationContractMetadataV1`; define readonly `RequestEncoder`, `ResponseDecoder<T>`, and `OperationTransportContract<T>` exactly as above. Factories receive an operation metadata export and selected success status, copy its canonical schema identity, and reject local refs/digests or a status/media not declared by that operation. Implement shared base-media parsing with case-insensitive parameters. Make `request` choose the explicit operation contract or the source-compatible JSON default, validate request encoder against `operation.request`, synthesize decoder Accept, and reject contradictory caller headers before `fetchFn`. Keep the existing structured error parser as the sole non-OK path; redact non-JSON raw bodies. Route 204 through `noneResponseDecoder`; export stable codec/metadata symbols so Plan 03 resolves them, and ensure TypeScript generic names are diagnostic only. After the production client/codec implementation is complete, add and run encoder/decoder matrix tests with real `Response` objects and spies on JSON/text/body access as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - JSON/text/none/raw success behavior and every media/error negative branch use real Response values.
    - Existing caller signatures compile and default behavior remains JSON envelope.
    - Operation, encoder and decoder symbols expose exact canonical request/success schema identity to Plan 03; generics cannot substitute.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir src/web exec vitest run src/services/http/client.test.ts --reporter=verbose &amp;&amp; pnpm --dir src/web exec tsc --noEmit &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.2-S04 owns one typed shared success-decoder contract while preserving the current structured error authority.</done>
</task>

<task type="auto">
  <name>Task 2: Route Markdown export through the real text decoder</name>
  <read_first>
    - src/web/src/features/chat/api.ts
    - src/web/src/features/chat/api.test.ts
    - src/web/src/services/http/client.ts
    - src/server/internal/http/chat_actions_handler.go
    - docs/api/openapi.yaml
    - src/web/src/generated/operation-contracts.generated.ts
  </read_first>
  <files>src/web/src/features/chat/api.ts, src/web/src/features/chat/api.test.ts</files>
  <behavior>
    - V31.2-S04/T-31.2-01: `createChatApi(realClient).exportConversationMarkdown` references `operationContracts.exportConversationMarkdown`, exact success schema identity and typed text decoder, sends `Accept: text/markdown`, accepts charset, and returns exact raw text.
    - JSON parsing and envelope unwrap spies remain zero for the markdown response.
    - Wrong/missing markdown media, conflicting Accept, non-OK JSON envelope, and non-JSON failure body produce stable redacted errors.
  </behavior>
  <action>Build the production Markdown contract from `operationContracts.exportConversationMarkdown`, the `none` request encoder and `textResponseDecoder` selected success metadata first; do not spell a schema ref/digest locally. Preserve URL encoding and Promise string, and do not introduce raw fetch or change the Go/OpenAPI endpoint. After implementation, replace the mocked `get` regression with an implementation-complete automated case named exactly `downloads markdown through real shared client Response`; it creates the real client using `createHttpClient({ ... })`, injects fetch returning a real `new Response(...)`, and calls `createChatApi(client).exportConversationMarkdown`. Assert exact generated operation/schema identity reaches the client/AST-visible symbol, exact headers/bytes, zero JSON/envelope calls, and wrong/missing schema identity, status, media and error behavior through the same API call.</action>
  <acceptance_criteria>
    - The production Chat API uses the shared typed decoder and no per-feature transport bypass.
    - The regression proves charset compatibility and `Response.text()` through the real client.
    - Errors remain structured/redacted and no JSON success decoder touches markdown.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir src/web exec vitest run src/features/chat/api.test.ts src/services/http/client.test.ts --reporter=verbose &amp;&amp; pnpm --dir src/web exec tsc --noEmit &amp;&amp; git diff --check</automated>
  </verify>
  <done>The live Markdown export path matches OpenAPI/Go media behavior and is visible to the shared AST taxonomy.</done>
</task>

<task type="auto">
  <name>Task 3: Require exact operation metadata through stream, upload and SWR transports</name>
  <read_first>
    - src/web/src/services/http/stream.ts
    - src/web/src/services/http/stream.test.ts
    - src/web/src/services/http/upload.ts
    - src/web/src/lib/swr.ts
    - scripts/verify_frontend_operation_contract_usage.mjs
  </read_first>
  <files>src/web/src/services/http/stream.ts, src/web/src/services/http/stream.test.ts, src/web/src/services/http/upload.ts, src/web/src/services/http/upload.test.ts, src/web/src/lib/swr.ts</files>
  <behavior>
    - stream, upload and SWR wrapper entrypoints require an exact `OperationContractMetadataV1` argument and validate encoder/decoder/status/media/schema identity before dispatch.
    - Callers pass imported generated operation symbols; wrapper code cannot select metadata by method/path lookup, construct a local core object, or use TypeScript generic names as identity.
    - Existing stream/upload/SWR public behavior and tests remain compatible; missing/wrong symbol and zero AST owner/import/use counts fail.
  </behavior>
  <action>Make `streamText`, upload helpers and the shared SWR fetcher accept/forward `OperationTransportContract` or exact generated operation metadata first while keeping feature-facing return types and behavior stable. Wrapper files import generated metadata types and validate the caller-supplied exact symbol; concrete callsites import the generated member. Do not add a runtime method/path-to-operation lookup or synthesize schema identity from generics. Run the Plan 01 compiler-symbol preflight over the five Plan 04 production owners: classify `client.ts`, `stream.ts`, `upload.ts` and `lib/swr.ts` explicitly as shared transport owners, and require `features/chat/api.ts` concrete operation-symbol uses. Any compiler-proven non-caller remains in the expected set with an explicit tested disposition/reason. After the production wrapper implementation is complete, create `src/web/src/services/http/upload.test.ts`, extend `stream.test.ts`, and rerun the existing client/Chat tests read-only as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - All shared transports carry exact metadata supplied by callers and preserve existing observable APIs.
    - Five expected Plan 04 owners have explicit AST dispositions with positive import/use counts where applicable.
    - No path lookup, local core object or generic-only identity can dispatch.
  </acceptance_criteria>
  <verify><automated>pnpm --dir src/web exec vitest run src/services/http/client.test.ts src/services/http/stream.test.ts src/services/http/upload.test.ts src/features/chat/api.test.ts --reporter=verbose &amp;&amp; pnpm --dir src/web exec tsc --noEmit &amp;&amp; node scripts/verify_frontend_operation_contract_usage.mjs --tsconfig src/web/tsconfig.json --expect-owner src/web/src/services/http/client.ts --expect-owner src/web/src/services/http/stream.ts --expect-owner src/web/src/services/http/upload.ts --expect-owner src/web/src/lib/swr.ts --expect-owner src/web/src/features/chat/api.ts --require-all &amp;&amp; git diff --check</automated></verify>
  <done>Plan 04 shared transports and Chat caller expose exact generated operation/schema identities before the full production sidecar runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| HTTP response headers/body -> typed client value | Untrusted server/provider bytes cross a decoder selected by product code. |
| caller headers -> transport | Caller-supplied Accept could contradict the declared decoder. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-01 | Tampering | media/decoder selection | high | mitigate | Closed typed decoders, parsed base-media compatibility, contradictory-header rejection, and real Response tests. |
| T-31.2-08 | Information Disclosure | error bodies | medium | mitigate | One structured error parser and raw non-JSON body redaction. |
</threat_model>

<verification>
- Run both exact Vitest files, TypeScript no-emit, and `git diff --check`; file-level Vitest selection fails when files/tests are absent.
- Plan 05 re-runs the sidecar and proves the decoder fingerprint joins the OpenAPI core.
- This is E1 repository-local RELS-02 contribution evidence only.
</verification>

<success_criteria>
- Shared HttpClient has explicit JSON/text/raw/none success semantics.
- Shared request/response codecs carry the same canonical schema identity consumed by Plans 01/03/05; TypeScript generic names remain diagnostics only.
- Markdown export uses Accept/base-media/text decoding end to end through the shared client.
- JSON/error/204 behavior remains covered and no feature-level bypass exists.
- No target or commercial-readiness claim is made.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-04-SUMMARY.md` when done.
</output>
