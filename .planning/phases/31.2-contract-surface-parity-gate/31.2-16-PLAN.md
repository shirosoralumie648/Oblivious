---
phase: 31.2-contract-surface-parity-gate
plan: "16"
type: execute
wave: 8
depends_on: ["31.2-03", "31.2-05"]
files_modified:
  - scripts/frontend_surface_sidecar.mjs
  - scripts/frontend_surface_sidecar.test.mjs
  - scripts/testdata/frontend-surface/production/transports.ts
  - scripts/verify-frontend-surface-sidecar-fixtures.sh
autonomous: true
gap_closure: true
requirements: [RELS-02]
must_haves:
  truths:
    - "Every production transport call is discovered through TypeScript symbol/declaration resolution, including aliases, globalThis.fetch, injected/imported functions, and approved object-method wrappers (D-SUR-05, D-SUR-06)."
    - "Static literals, const aliases, and static template expressions resolve to one method/path shape; dynamic or ambiguous expressions produce a coded unresolved record and fail the sidecar instead of passing with unresolved=[] (D-SUR-02, D-SUR-06)."
    - "Adversarial alias, wrapper, global fetch, wrong static path, and dynamic path fixtures all fail for the expected non-empty code while the complete production owner closure still passes."
  artifacts:
    - path: "scripts/frontend_surface_sidecar.mjs"
      provides: "Bounded compiler-symbol transport call graph and fail-closed static method/path evaluator"
      contains: "frontend-surface-sidecar/v1"
    - path: "scripts/frontend_surface_sidecar.test.mjs"
      provides: "Focused alias, wrapper, template, and dynamic-expression regression matrix"
      contains: "frontend surface sidecar"
    - path: "scripts/verify-frontend-surface-sidecar-fixtures.sh"
      provides: "Fresh-checkout production mutations proving hidden transports cannot stay green"
      contains: "expect_stage_failure"
  key_links:
    - from: "scripts/frontend_surface_sidecar.mjs"
      to: "src/web/src/services/http/client.ts"
      via: "TypeChecker symbol/declaration traversal from approved shared transport roots to production call sites"
      pattern: "getTypeChecker"
    - from: "scripts/verify-frontend-surface-sidecar-fixtures.sh"
      to: "scripts/frontend_surface_sidecar.mjs"
      via: "clean-checkout adversarial mutations requiring stable unresolved codes and nonzero exit"
      pattern: "verify-frontend-surface-sidecar.sh"
---

<objective>
Close the canonical frontend inventory false-green by replacing syntax-name transport discovery with bounded TypeScript symbol resolution and fail-closed method/path evaluation.

Purpose: A production network call must not escape RELS-02 merely because the callee or URL was aliased.
Output: compiler-symbol call graph, static expression evaluator, coded unresolved failures, and adversarial Node/fresh-checkout fixtures.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VERIFICATION.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-REVIEW.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-03-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-05-SUMMARY.md

<interfaces>
`extractSidecar()` creates one `typescript.Program` and `TypeChecker`, then emits `operations` and `unresolved`. Current weak points are `transportKind(node, checker)`, `literalValue(node)`, `invocationMethod(...)`, and `invocationPathMatches(...)`. Generated operation metadata remains the sole transport identity authority; method/path resolution validates that identity but never selects it.

Approved production roots are the existing shared `HttpClient`, `streamText`, upload, SWR, raw `fetch`/injected `fetchFn`, `EventSource`, and `WebSocket` boundaries. Traversal must be bounded by a visited-symbol set and explicit maximum depth; cycles or ambiguous declarations are unresolved, never guessed.
</interfaces>
</context>

## Artifacts this phase produces

- A deterministic symbol/declaration transport resolver over the existing one-Program sidecar.
- Unit and clean-checkout negative fixtures for every reproduced bypass.
- Repository-local E1/E2 evidence only; no target/live or commercial-release claim.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify alias, wrapper, global fetch, and static-expression failures first</name>
  <read_first>
    - scripts/frontend_surface_sidecar.test.mjs
    - scripts/testdata/frontend-surface/production/transports.ts
    - scripts/verify-frontend-surface-sidecar-fixtures.sh
    - scripts/frontend_surface_sidecar.mjs
  </read_first>
  <files>scripts/frontend_surface_sidecar.test.mjs, scripts/testdata/frontend-surface/production/transports.ts, scripts/verify-frontend-surface-sidecar-fixtures.sh</files>
  <behavior>
    - RED: `const hiddenFetch = fetch`, `globalThis.fetch`, imported/local aliases, injected `fetchFn`, and approved object-method aliases are discovered rather than silently skipped.
    - RED: const string paths and no-substitution/static templates resolve exactly; a wrong const/static template yields `transport_path_mismatch`.
    - RED: runtime interpolation, mutable variables, unresolved method values, ambiguous declarations, and wrapper cycles yield non-empty stable unresolved codes.
    - Every mutation asserts a positive baseline operation count and the expected code; a generic failure or `unresolved=[]` is not accepted.
  </behavior>
  <action>Before production changes, extend the isolated `transports.ts` fixture and `withMutatedFixture` cases with the exact canonical verification probes: direct alias, `globalThis.fetch`, wrapper/object-method alias, imported or parameter-injected fetch alias, const path, static template, wrong variable path, dynamic interpolation, dynamic method, and a bounded-cycle case. Extend the fresh-checkout shell family so at least alias/global/wrapper/wrong-path/dynamic-path mutations are injected into a known production owner while retaining one valid call, then require a specific coded failure from `verify-frontend-surface-sidecar.sh --stage-a`. Record nonzero mutation/discovery counters and reject an empty unresolved array. Do not add fixture calls to `src/web/src` or change the fixed production owner inventory.</action>
  <acceptance_criteria>
    - Every reproduced false-green has a failing regression before implementation.
    - Static valid aliases/templates have positive expectations, while ambiguous/dynamic forms require coded failure.
    - Fixture roots remain isolated from the production scan and all counters are nonzero.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; output=$(node --test scripts/frontend_surface_sidecar.test.mjs 2&gt;&amp;1); status=$?; set -e; test "$status" -ne 0; printf "%s\n" "$output" | rg "alias|global|wrapper|dynamic"'</automated>
  </verify>
  <done>The negative matrix reproduces alias/global/wrapper/path bypasses and cannot pass with zero discovery or an empty unresolved set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve transports through a bounded TypeScript symbol call graph</name>
  <read_first>
    - scripts/frontend_surface_sidecar.mjs
    - scripts/frontend_surface_sidecar.test.mjs
    - scripts/testdata/frontend-surface/production/transports.ts
    - src/web/src/services/http/client.ts
    - src/web/src/services/http/stream.ts
    - src/web/src/services/http/upload.ts
    - src/web/src/lib/swr.ts
  </read_first>
  <files>scripts/frontend_surface_sidecar.mjs, scripts/frontend_surface_sidecar.test.mjs, scripts/testdata/frontend-surface/production/transports.ts, scripts/verify-frontend-surface-sidecar-fixtures.sh</files>
  <behavior>
    - GREEN: alias symbols are unwrapped with `checker.getAliasedSymbol` and declaration traversal; built-in/global fetch, imported/injected aliases, destructured/bound object methods, and approved wrappers reach one canonical transport kind.
    - GREEN: a visited set and fixed depth bound terminate cycles; multiple incompatible declarations or unknown network-capable wrappers produce `transport_callee_unresolved`.
    - GREEN: an evaluator resolves immutable identifiers, string concatenation, and fully static templates to method/path values; dynamic or mutable expressions produce `transport_method_unresolved` or `transport_path_unresolved`.
    - Existing generated-symbol operation identity, exact 25-owner closure, exclusion rules, deterministic ordering, and one-Program digest remain unchanged.
  </behavior>
  <action>Replace the syntax-only `transportKind` path with a resolver that starts from each `CallExpression`/`NewExpression`, follows the callee symbol through TypeScript aliases, variable initializers, imports, property/access declarations, approved wrapper function bodies, and parameter bindings into the existing transport roots. Carry the resolved method identity for aliased `HttpClient` methods. Bound traversal with a maximum depth plus visited declaration/symbol keys and fail closed on cycles, unions, multiple targets, or network-capable unknown wrappers. Extend `literalValue` into a checker-aware immutable expression evaluator for const declarations, property initializers, string concatenation, and static templates; return an explicit unresolved result instead of treating every nonliteral path as a match. `validateTransportInvocation` must distinguish unresolved method/path from an exact mismatch. Preserve generated metadata as the only operation selector and keep all source locations repository-relative. Make the RED fixtures pass, then run the full isolated and production Stage-A families.</action>
  <acceptance_criteria>
    - No aliased/global/injected/approved-wrapper transport is silently ignored.
    - No dynamic or ambiguous method/path can satisfy `invocationPathMatches` or leave `unresolved=[]`.
    - Baseline taxonomy, exact owner closure, production scan, and deterministic bytes remain passing.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/frontend_surface_sidecar.test.mjs &amp;&amp; bash scripts/verify-frontend-surface-sidecar-fixtures.sh &amp;&amp; bash scripts/verify-frontend-surface-sidecar.sh --stage-a --root src/web/src &amp;&amp; git diff --check</automated>
  </verify>
  <done>The one-Program sidecar classifies transport aliases through symbols and fails closed on every unresolved callee, method, or path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| production TypeScript -> sidecar evidence | Caller syntax is untrusted evidence input and may hide a real network effect behind aliases or wrappers. |
| sidecar -> transport/exposure reports | A false-empty unresolved set can authorize a drifted release surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-16-01 | Tampering | `transportKind`/callee resolution | high | mitigate | TypeChecker alias/declaration graph, bounded traversal, stable unresolved codes, and adversarial alias/global/wrapper fixtures. |
| T-31.2-16-02 | Spoofing | method/path expression identity | high | mitigate | Immutable static evaluator; dynamic, ambiguous, mutable, and mismatched expressions fail closed. |
| T-31.2-16-03 | Denial of Service | wrapper graph traversal | medium | mitigate | Fixed depth, visited declarations/symbols, deterministic cycle failure, and bounded fixture. |
</threat_model>

<verification>
- Run Node unit mutations before implementation and the full sidecar fixture/production Stage-A suites after implementation.
- Require nonzero taxonomy/mutation/owner counts and stable failure codes; generic nonzero output is insufficient.
- Do not treat the repository-local sidecar result as E3/E4 or target/live evidence.
</verification>

<success_criteria>
- Canonical CR-02 alias/global-fetch and wrong-variable-path probes fail closed.
- Approved static aliases/templates resolve exactly; dynamic/ambiguous expressions remain unresolved.
- Existing 25-owner production closure and downstream shared sidecar contract remain intact.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-16-SUMMARY.md` when done.
</output>
