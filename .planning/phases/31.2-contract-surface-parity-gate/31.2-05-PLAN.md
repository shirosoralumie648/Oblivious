---
phase: 31.2-contract-surface-parity-gate
plan: "05"
type: execute
wave: 7
depends_on: ["31.2-01", "31.2-02", "31.2-03", "31.2-04", "31.2-06", "31.2-10", "31.2-11", "31.2-12", "31.2-15"]
files_modified:
  - src/server/cmd/release-contract/main.go
  - src/server/cmd/release-contract/main_test.go
  - scripts/verify_frontend_surface.py
  - scripts/verify-frontend-transport-contract.sh
  - scripts/verify-frontend-exposure-contract.sh
  - scripts/verify-frontend-surface-fixtures.sh
autonomous: true
requirements: [RELS-02]
must_haves:
  truths:
    - "Transport and exposure verifiers consume the exact same sidecar bytes/source digest and neither reparses TypeScript (D-SUR-07)."
    - "Every frontend operation joins one OpenAPI server core and proves compatible transport kind, request encoder, response decoder, and SSE/WebSocket event identity (D-SUR-02, D-SUR-06)."
    - "Router/navigation/public/generated/catalog exposure from Plan 10 is checked against the authenticated identity-bound app projection, release projection and server catalogs; excluded is absent and conditional is exposed only when enabled (D-SUR-08, D-SUR-19)."
    - "Verifier consumes exact Phase 31.1 `ModelOption.capabilityId` and `AgentToolDefinition.capabilityId` plus Plan 10 omission evidence; it never guesses future type names or accepts caller-supplied projection JSON (D-SUR-08)."
    - "Exactly one `frontend-transport` and one `frontend-exposure` report are written through Phase 31 adapters with E1/E2 claim bounds (D-SUR-15, D-SUR-20)."
  artifacts:
    - path: "scripts/verify_frontend_surface.py"
      provides: "Structured sidecar-to-core transport join and product-exposure projection verifier"
      contains: "frontend-surface-sidecar/v1"
    - path: "scripts/verify-frontend-exposure-contract.sh"
      provides: "Structured Plan 10 exposure/projection/catalog join over the shared sidecar"
      contains: "frontend-exposure"
    - path: "scripts/verify-frontend-surface-fixtures.sh"
      provides: "Sidecar splice, transport, exposure, catalog and generated-consumer mutation suite"
      contains: "frontend-exposure"
  key_links:
    - from: "scripts/verify-frontend-transport-contract.sh"
      to: "scripts/verify-frontend-exposure-contract.sh"
      via: "one pre-generated sidecar path plus exact digest handoff"
      pattern: "SIDECAR"
    - from: "scripts/verify_frontend_surface.py"
      to: "src/web/src/features/releaseProjection/releaseProjection.tsx"
      via: "shared sidecar exposure rows join Plan 10 authenticated projection/provider and exact catalog DTO reads"
      pattern: "capabilityId|releaseIdentity"
    - from: "src/server/cmd/release-contract/main.go"
      to: "src/server/internal/surfacereport/frontend.go"
      via: "frontend report constructors plus AtomicWriter"
      pattern: "NewFrontendTransportReport|NewFrontendExposureReport"
---

<objective>
用同一 sidecar 完成 frontend transport/core join 与 Plan 10 产品 exposure/catalog join，并产出两个独立 reports；页面/API/state/provider wiring 由 Plan 10 单独拥有。

Purpose: 防止两个 frontend verifier 分叉扫描、客户端重写 capability 映射、或 excluded/blocked product surface 继续公开。
Output: structured verifiers、selector consumption tests、two report CLI paths、focused gates 和 one-field fixture family。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-PATTERNS.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VALIDATION.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-01-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-02-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-03-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-04-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-06-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-10-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-11-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-12-SUMMARY.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-15-SUMMARY.md
@.planning/phases/31.1-readiness-fail-closed/31.1-03-PLAN.md
@.planning/phases/31.1-readiness-fail-closed/31.1-05-PLAN.md
@.planning/phases/31.1-readiness-fail-closed/31.1-06-PLAN.md
@src/web/src/features/releaseProjection/releaseProjection.tsx

<interfaces>
Phase 31.1 is unexecuted at planning time. Before implementation, require its exact implemented contracts: authenticated identity-bound `AppCapabilityProjectionResponse`; response JSON/TypeScript `ModelOption{id,label,capabilityId}` from Plan 31.1-05; live `agent.ToolDefinition` response JSON and frontend `AgentToolDefinition.capabilityId` from Plan 31.1-10; strict mutation DTO rejection/omission. If implementation differs, correct those upstream owners rather than guessing another type name, adding a TypeScript map, or accepting capabilityId in mutation bodies.

Plan 01 provides exact `OperationContractMetadataV1`/`SchemaIdentityV1`; Plans 04/10/11/12/15 migrate all 25 known production owners to explicit generated symbols; Plan 03 sidecar records those identities and provides side-effect-free `NewFrontendTransportReport`/`NewFrontendExposureReport`; Plan 10 owns actual product exposure wiring. Plan 06 may already have extended `cmd/release-contract`, so preserve its subcommands while adding frontend report commands.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exact-join transport extensions to the OpenAPI server core</name>
  <read_first>
    - scripts/frontend_surface_sidecar.mjs
    - scripts/openapi_surface_fingerprint.py
    - docs/api/route-surface-manifest.json
    - src/web/src/services/http/client.ts
    - src/web/src/services/http/stream.ts
    - src/web/src/services/http/upload.ts
  </read_first>
  <files>scripts/verify_frontend_surface.py, scripts/verify-frontend-transport-contract.sh, scripts/verify-frontend-surface-fixtures.sh</files>
  <behavior>
    - V31.2-S05/T-31.2-01: every sidecar operation joins exactly one core by operationId plus normalized method/path/capability; missing, extra, ambiguous, or extension attempts to redefine core fields fail.
    - JSON/form-data/raw encoders, JSON/text/raw/none decoders, success alternatives, SSE events, EventSource events, and WebSocket inbound/outbound identities are compatible with exact `SchemaIdentityV1`; TypeScript generic names never satisfy equality.
    - Wrong encoder/decoder/event/media/charset/schema, unresolved operation, unknown taxonomy, generated call counted as production, zero operations, or source/core digest mismatch fails.
  </behavior>
  <action>Implement `verify_frontend_surface.py` using JSON/YAML parsers and canonical structured comparisons, then add implementation-complete one-field JSON fixtures. Accept explicit sidecar, core manifest, release contract/projection, authenticated app-projection observation, server-catalog observation, and output-detail paths. Validate schema versions/digests and require nonzero sidecar files/operations with unresolved empty. Join operationId/method/path/capability to Plan 01 and exact request/success `SchemaIdentityV1` to the Plan 03 operation/typed encoder/ResponseDecoder symbols; prevent the extension from redefining core media/schema and ignore TypeScript generics except diagnostics. Apply compatibility rules for JSON, form-data, raw, text charset, 204 none, success alternatives and events. Emit typed counts/digests/drift IDs only. The wrapper receives an already-generated sidecar and must not invoke the extractor; it records exactly one digest.</action>
  <acceptance_criteria>
    - Every production operation is joined and classified exactly once with nonzero counts.
    - All encoder/decoder/event/core mismatch families have a named nonzero fixture.
    - The verifier has no regex source scanner or TypeScript parser path.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m py_compile scripts/verify_frontend_surface.py &amp;&amp; bash -n scripts/verify-frontend-transport-contract.sh scripts/verify-frontend-surface-fixtures.sh &amp;&amp; bash scripts/verify-frontend-surface-fixtures.sh --transport &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.2-S05 transport projection exact-joins the complete server core and browser extension without rescanning source.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Plan 10 product exposure against authenticated projection and exact DTOs</name>
  <read_first>
    - .planning/phases/31.2-contract-surface-parity-gate/31.2-10-SUMMARY.md
    - scripts/frontend_surface_sidecar.mjs
    - scripts/verify_frontend_surface.py
    - src/web/src/features/releaseProjection/releaseProjection.tsx
    - src/web/src/types/api.ts
    - src/web/src/features/agents/agentsApi.ts
  </read_first>
  <files>scripts/verify_frontend_surface.py, scripts/verify-frontend-exposure-contract.sh, scripts/verify-frontend-surface-fixtures.sh</files>
  <behavior>
    - V31.2-S05/T-31.2-03: sidecar proves Chat reads exact `ModelOption.capabilityId`, Agents reads exact `AgentToolDefinition.capabilityId`, and absent server catalog entries are never synthesized.
    - Current app-safe projection hides excluded subjects and exposes a conditional subject only when availability is enabled; Admin full inventory is never treated as app exposure.
    - Plan 10 mutation observations submit business fields only; `capabilityId` is absent even when selected from a response object, and caller-supplied projection JSON/provider props are impossible.
    - A literal model/tool ID-to-capability map or fallback capability string makes the test/verifier fail.
  </behavior>
  <action>Complete the structured exposure verifier, then extend its implementation-complete fixture coverage using exact Phase 31.1 response shapes and Plan 10 sidecar output. Require the app projection observation to originate from the authenticated API/provider path and exact-match release identity/profile/contract/source digest; reject a caller-provided projection object or provider prop path. Verify router, workspace/console/admin nav, marketing, generated exports and Chat/Agents selector reads against release projection plus current availability. Require exact DTO field symbols `ModelOption.capabilityId` and `AgentToolDefinition.capabilityId`, no local model/tool capability map, and mutation serialization/observation without capabilityId. Excluded is absent, conditional disabled unavailable, Admin full inventory not imported, and runtime server remains authorization authority. This task modifies only structured verifier/wrappers; all product wiring/tests stay in Plan 10.</action>
  <acceptance_criteria>
    - Verifier proves both selectors consume exact server DTO capability identity and authenticated current app-safe availability.
    - Excluded/blocked subjects do not become selectable and no UI-only decision is treated as authorization.
    - Mutation payload assertions prove capabilityId is never sent.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/verify-frontend-surface-fixtures.sh --exposure &amp;&amp; bash scripts/verify-frontend-exposure-contract.sh --sidecar .tmp/frontend-surface-sidecar.json --stage-a &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.2-S05 structurally proves Plan 10 consumes, but never authors/submits, exact Phase 31.1 capability identity and projection.</done>
</task>

<task type="auto">
  <name>Task 3: Emit transport and exposure reports from one sidecar execution</name>
  <read_first>
    - src/server/cmd/release-contract/main.go
    - src/server/cmd/release-contract/main_test.go
    - src/server/internal/surfacereport/frontend.go
    - src/server/internal/surfacereport/writer.go
    - scripts/verify_frontend_surface.py
    - scripts/verify-frontend-surface-sidecar.sh
    - src/web/src/app/router.tsx
    - src/web/src/features/layouts/WorkspaceLayout.tsx
    - src/web/src/features/layouts/ConsoleLayout.tsx
    - src/web/src/features/layouts/AdminSidebar.tsx
  </read_first>
  <files>src/server/cmd/release-contract/main.go, src/server/cmd/release-contract/main_test.go, scripts/verify_frontend_surface.py, scripts/verify-frontend-transport-contract.sh, scripts/verify-frontend-exposure-contract.sh, scripts/verify-frontend-surface-fixtures.sh</files>
  <behavior>
    - One producer orchestration generates the sidecar once, then passes the same file bytes/digest to transport and exposure projections and writes two distinct nested reports.
    - Exposure covers router entries, workspace/console/admin navigation, public links, model/tool selectors, and generated client exports; missing/extra/disabled/excluded/generated-stale entries fail.
    - Sidecar digest splice, second scan attempt, client capability map, mutation capabilityId, Admin inventory exposure, report fold, identity splice, drift, skip, and output failure are nonzero.
  </behavior>
  <action>Extend the existing Phase 31/31.1/Plan 06 release-contract CLI without removing upstream subcommands. Add `report-frontend-transport` and `report-frontend-exposure`, each requiring explicit repo/contract/schema/profile, bounded observation JSON, the exact sidecar digest, and output; neither accepts identity/evidence/skip fields. Construct through Plan 03 report APIs and persist with `AtomicWriter`. Implement exposure projection in the structured verifier over the sidecar exposure array, Phase 31 release projection, Phase 31.1 app-safe projection/server catalog, and generated consumer records. The two shell wrappers accept an explicit existing sidecar only. The combined fixture wrapper generates once, records extractor invocation count one, invokes each projection/CLI once, asserts equal sidecar digest and different surface IDs, and runs every product/identity/redaction mutation. Focused wrappers remain independently runnable for developers.</action>
  <acceptance_criteria>
    - A passing run writes exactly one report for each frontend surface from identical sidecar bytes.
    - All transport/exposure/catalog/generated/identity/skip mutation families execute and fail.
    - Public diagnostics contain repository-relative locations, stable IDs, digests and counts only.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./cmd/release-contract '^TestRunFrontendSurfaceReportsContract$' &amp;&amp; bash -n scripts/verify-frontend-exposure-contract.sh &amp;&amp; bash scripts/verify-frontend-surface-fixtures.sh &amp;&amp; bash scripts/verify-frontend-surface-sidecar.sh --stage-a &amp;&amp; bash scripts/verify-frontend-transport-contract.sh --sidecar .tmp/frontend-surface-sidecar.json --stage-a &amp;&amp; bash scripts/verify-frontend-exposure-contract.sh --sidecar .tmp/frontend-surface-sidecar.json --stage-a &amp;&amp; git diff --check</automated>
  </verify>
  <done>One AST execution produces two distinct trusted frontend reports with exact transport and product-exposure joins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| sidecar -> OpenAPI core | Browser-owned extensions join the canonical server contract. |
| server catalog/app projection -> selectors | Server-derived capability identity controls presentation but not final authorization. |
| frontend observations -> reports | Two projections share inventory without sharing identity ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-01 | Tampering | transport/core join | high | mitigate | Structured full-field compatibility, one sidecar digest, one-field fixtures, and no source regex. |
| T-31.2-03 | Elevation of Privilege | client catalog capability | high | mitigate | Server DTO/projection consumption, no client map, mutation omission, and server remains final guard. |
| T-31.2-06 | Spoofing | frontend report pair | high | mitigate | Trusted resolvers, identical sidecar digest, distinct registrations, identity-splice and fold rejection. |
</threat_model>

<verification>
- Run combined selector Vitest, TypeScript no-emit, both structured verifier fixtures, the matched CLI suite, one-scan report orchestration, and `git diff --check`.
- Focused producer scripts remain runnable outside the aggregate.
- Results are E1/E2 repository-local RELS-02 contributions, not target/browser commercial proof.
</verification>

<success_criteria>
- Transport and exposure consume one sidecar and cannot diverge exclusions/digests.
- Every frontend operation and product exposure exact-joins its authoritative server source.
- Model/tool capability remains server-derived and mutation-safe.
- Two distinct frontend reports pass with no drift/skips and no E3/E4 claim.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-05-SUMMARY.md` when done.
</output>
