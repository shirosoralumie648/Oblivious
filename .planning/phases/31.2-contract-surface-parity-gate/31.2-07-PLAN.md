---
phase: 31.2-contract-surface-parity-gate
plan: "07"
type: execute
wave: 1
depends_on: ["31.1-08"]
files_modified:
  - config/release/migration-disposition.v1.json
  - src/server/internal/migrations/inventory.go
  - src/server/internal/migrations/inventory_test.go
  - src/server/internal/surfacereport/migration.go
  - src/server/internal/surfacereport/migration_test.go
  - src/server/cmd/release-migration-surface/main.go
  - src/server/cmd/release-migration-surface/main_test.go
  - scripts/verify-migration-contract.sh
autonomous: true
requirements: [RELS-02]
must_haves:
  truths:
    - "Migration static inventory and runtime ledger share exactly `MigrationIdentity{Version,Checksum}` canonical rows/digest; filenames/dispositions have a separate static metadata digest and never enter ledger equality (D-SUR-12, D-SUR-13)."
    - "Static construction only calls the already-live `migrations.Checksum` in `migrations.go`; `inventory.go` neither owns nor refactors checksum semantics and preserves `schema_migrations(version,checksum)` identity (D-SUR-13)."
    - "Root monolith SQL, microservices SQL, and ClickHouse SQL have explicit total dispositions; non-monolith files are not implied proven by monolith evidence (D-SUR-13)."
    - "`migration-static` and `migration-ledger` reports are distinct Phase 31 adapters with trusted identity and no skip/claim inflation (D-SUR-15, D-SUR-20)."
  artifacts:
    - path: "config/release/migration-disposition.v1.json"
      provides: "Explicit monolith, microservices, and ClickHouse migration surface disposition"
      contains: '"schemaVersion": "migration-disposition/v1"'
    - path: "src/server/internal/migrations/inventory.go"
      provides: "Shared MigrationIdentity canonical rows/digest plus separate static filename/disposition metadata"
      exports: ["MigrationIdentity", "StaticMigrationEntry", "BuildStaticInventory", "ReadLedger", "IdentityDigest", "StaticMetadataDigest"]
    - path: "src/server/internal/surfacereport/migration.go"
      provides: "Distinct static and ledger report registrations/constructors"
      exports: ["MigrationStaticSurfaceID", "MigrationLedgerSurfaceID", "NewMigrationStaticReport", "NewMigrationLedgerReport"]
    - path: "src/server/cmd/release-migration-surface/main.go"
      provides: "Focused static and ledger producer command"
      contains: "migration-ledger"
  key_links:
    - from: "src/server/internal/migrations/inventory.go"
      to: "src/server/internal/migrations/migrations.go"
      via: "BuildStaticInventory consumes the existing live Checksum implementation over exact bytes"
      pattern: "Checksum"
    - from: "src/server/cmd/release-migration-surface/main.go"
      to: "src/server/internal/surfacereport/migration.go"
      via: "independent static/ledger report construction and AtomicWriter"
      pattern: "NewMigrationStaticReport|NewMigrationLedgerReport"
---

<objective>
建立 migration static inventory 与 runtime ledger 两个独立事实，并显式 disposition monolith/non-monolith SQL surfaces。

Purpose: 阻止 filename/row-count 假绿、第二套 checksum identity，以及把 microservices/ClickHouse 资产误报为 monolith replay proof。
Output: total disposition、shared ordered digest APIs、two typed reports、focused static/ledger CLI 与 mutation gate。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-RESEARCH.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-PATTERNS.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VALIDATION.md
@.planning/phases/31-release-contract-current-baseline/31-04-PLAN.md
@src/server/internal/migrations/migrations.go
@src/server/migrations
@scripts/verify-migration-contract.sh
@scripts/verify-migration-replay.sh

<interfaces>
Preserve the current ledger table, apply behavior, and already-exported live `migrations.Checksum(statement []byte) string` in `src/server/internal/migrations/migrations.go`. This plan does not own or modify `migrations.go`; `inventory.go` only calls that function on exact SQL bytes.

The exact shared equality contract is `MigrationIdentity{Version string, Checksum string}` with canonical sorting by parsed numeric version and digest over only the canonical `(version,checksum)` sequence. Static rows are `StaticMigrationEntry{Identity MigrationIdentity, Filename string, Disposition string}`. `StaticMetadataDigest` covers filename/disposition separately. `ReadLedger` returns only `[]MigrationIdentity`. Static-to-ledger equality compares identity rows/count/digest exactly and never filename metadata.

The focused command has `static` and `ledger` subcommands. Ledger accepts a DB URL only through a named environment-variable reference and never prints its value. Both constructors reuse Phase 31 trusted identity/profile/report APIs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build total SQL disposition and one ordered checksum identity</name>
  <read_first>
    - src/server/internal/migrations/migrations.go
    - scripts/verify-migration-contract.sh
    - src/server/migrations
    - src/server/migrations/microservices
    - src/server/migrations/clickhouse
  </read_first>
  <files>config/release/migration-disposition.v1.json, src/server/internal/migrations/inventory.go, src/server/internal/migrations/inventory_test.go</files>
  <behavior>
    - V31.2-S07/T-31.2-05: all tracked SQL under the three migration surfaces appears exactly once with monolith-managed or explicit non-monolith disposition.
    - Static monolith inventory parses filenames, reads exact bytes, calls live `migrations.Checksum`, creates `MigrationIdentity{Version,Checksum}`, and computes identity digest separate from filename/disposition metadata digest.
    - Historical SQL mutation, duplicate version/file, missing/extra/unmanaged path, unsafe path, empty matched surface, or alternate checksum changes digest/fails with a stable code.
    - Microservices and ClickHouse entries remain visible but cannot enter monolith file count/digest.
  </behavior>
  <action>Consume the already-live `migrations.Checksum` from `migrations.go` without adding that file to ownership or changing its signature/output/callers. Implement `migration-disposition/v1` with closed nonempty entries for monolith, microservices and ClickHouse; every tracked file matches once and every pattern matches nonzero files. Define exact `MigrationIdentity` and `StaticMigrationEntry` contracts above. `BuildStaticInventory` validates numbered monolith filenames, reads exact bytes, calls live Checksum, sorts canonical identities by numeric version, returns identity rows plus filename/disposition metadata, computes `IdentityDigest` over only `(version,checksum)`, and computes `StaticMetadataDigest` separately over filename/disposition. `ReadLedger` produces the same canonical identities. Reject duplicates/order/shape mismatch. Do not rewrite SQL, normalize bytes, claim inventory owns Checksum, or treat non-monolith rows as applied. After the production inventory implementation is complete, add and run `TestMigrationInventoryContract` with temp copies and one-field disposition/SQL mutations as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Git-discovered SQL set equals the disposition set exactly and every surface has nonzero discovery.
    - Static inventory consumes the live Checksum implementation used by Apply; `migrations.go` is neither owned nor refactored.
    - Static/ledger equality uses only identical `MigrationIdentity` rows/digest, while filename/disposition changes affect only static metadata digest.
    - Every mutation/empty/duplicate/non-monolith contamination case is nonzero.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/migrations '^TestMigrationInventoryContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.2-S07 owns one total SQL disposition and one exact-byte ordered checksum identity.</done>
</task>

<task type="auto">
  <name>Task 2: Register separate static and ledger report contracts</name>
  <read_first>
    - src/server/internal/migrations/inventory.go
    - src/server/internal/migrations/migrations.go
    - src/server/internal/surfacereport/report.go
    - src/server/internal/surfacereport/registry.go
    - src/server/internal/buildinfo/provider.go
    - src/server/internal/releasecontract/load.go
  </read_first>
  <files>src/server/internal/surfacereport/migration.go, src/server/internal/surfacereport/migration_test.go</files>
  <behavior>
    - `migration-static` details contain database kind, nonzero identity/file counts, `identityDigest`, and separate `staticMetadataDigest` plus non-monolith disposition counts.
    - `migration-ledger` details contain database kind, nonzero row count, the same `identityDigest`, and exact `MigrationIdentity` static comparison result without filename fields.
    - Reports cannot fold details, substitute surfaces, accept caller identity/profile, pass with mismatch/skip/zero rows, or claim target/commercial readiness.
  </behavior>
  <action>Register exact closed static and ledger details under distinct surface constants. Implement side-effect-free constructors with trusted resolver inputs; use canonical sources `src/server/migrations` and `schema_migrations(version,checksum)`, consumers `monolith-migration-static-inventory` and `monolith-runtime-ledger`, version `v1`. Static validation requires total disposition plus separate `identityDigest` and `staticMetadataDigest`; ledger validation accepts canonical `MigrationIdentity` rows only and requires exact identity count/rows/digest equality to trusted static observation. A matching filename digest is neither requested nor possible from ledger. Reject connection data, filenames in ledger details, SQL bodies, arbitrary details, drift/skips and E3/E4 claims. After both production report constructors are complete, add and run `TestMigrationStaticAndLedgerSurfaceContracts` as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Static and ledger reports validate independently and cannot be merged/substituted.
    - Missing/extra/checksum mismatch becomes drift/failure rather than a count-only pass.
    - Trusted identity/profile is resolver-derived for both reports.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/surfacereport '^TestMigrationStaticAndLedgerSurfaceContracts$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>The phase owns two distinct typed migration report surfaces sharing one checksum identity.</done>
</task>

<task type="auto">
  <name>Task 3: Expose independently runnable static and ledger producers</name>
  <read_first>
    - src/server/internal/migrations/inventory.go
    - src/server/internal/surfacereport/migration.go
    - src/server/internal/surfacereport/writer.go
    - src/server/cmd/migrate/main.go
    - scripts/verify-migration-contract.sh
  </read_first>
  <files>src/server/cmd/release-migration-surface/main.go, src/server/cmd/release-migration-surface/main_test.go, scripts/verify-migration-contract.sh, config/release/migration-disposition.v1.json</files>
  <behavior>
    - `static` discovers/validates inventory and writes exactly one `migration-static` report without DB access.
    - `ledger` queries ordered version/checksum rows from a reachable DB, compares the exact static inventory, and writes exactly one `migration-ledger` report; absent/unreadable DB is nonzero.
    - Both commands require explicit repo/contract/schema/profile/output, use AtomicWriter, and reject identity/skip/evidence overrides.
    - Fixture mode covers historical mutation, disposition missing/extra, ledger missing/extra/checksum mismatch/order/duplicate/zero rows, DB unavailable, identity splice, and output failure with nonzero case counts.
  </behavior>
  <action>Implement the dedicated command with `static` and `ledger`; both derive trusted identity/profile, use the inventory APIs and report constructors, and atomically write separate paths. Ledger accepts `--database-url-env TEST_DATABASE_URL`-style indirection only, opens/pings through the existing driver, queries exact rows, never applies migrations, and sanitizes all errors to environment class/stable codes. Expand `verify-migration-contract.sh` into the static focused producer plus `--fixtures`; preserve `set -euo pipefail`, assert nonzero file/case counts, and use temporary DB fakes/recording query seams for ledger negative unit coverage. Plan 08 owns real apply/replay and invokes ledger against its isolated DB. After the production commands and shell producer are complete, add and run `TestReleaseMigrationStaticAndLedgerCommandsContract` plus the named fixture modes as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Static and ledger can be run separately and each emits one trusted surface report.
    - Ledger cannot pass without readable exact rows matching static identity.
    - No SQL/DSN/body/external path enters output and all selectors/discoveries are nonzero.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./cmd/release-migration-surface '^TestReleaseMigrationStaticAndLedgerCommandsContract$' &amp;&amp; bash -n scripts/verify-migration-contract.sh &amp;&amp; bash scripts/verify-migration-contract.sh --fixtures &amp;&amp; bash scripts/verify-migration-contract.sh --stage-a &amp;&amp; git diff --check</automated>
  </verify>
  <done>Independent static and ledger focused producers preserve exact digest, identity, and fail-closed behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| tracked SQL bytes -> static identity | Historical migration bytes become release contract evidence. |
| database ledger -> runtime identity | Mutable DB rows are compared to immutable static inventory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-05 | Tampering | migration checksum/inventory/ledger | high | mitigate | Shared exact-byte Checksum, total dispositions, ordered digests, exact row joins, and mutation tests. |
| T-31.2-06 | Spoofing | migration reports | high | mitigate | Trusted resolvers, distinct typed registrations, atomic outputs, and identity/fold/skip rejection. |
</threat_model>

<verification>
- Run the three matched Go suites, disposition/static fixtures, focused static Stage A, and `git diff --check`.
- Real ledger/replay automation is mandatory in Plan 08; no environment absence is counted as a pass here.
- Evidence is E1 repository-local RELS-02 contribution only.
</verification>

<success_criteria>
- Static and ledger use one exact `MigrationIdentity{Version,Checksum}` sequence/digest; filename/disposition metadata remains separately static.
- Monolith/non-monolith SQL surfaces have total explicit dispositions.
- Two distinct reports fail on any mismatch or skip.
- No replay, target, or commercial proof is claimed.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-07-SUMMARY.md` when done.
</output>
