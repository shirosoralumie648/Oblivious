---
phase: 31.2-contract-surface-parity-gate
plan: "05"
subsystem: contract-surface-gates
tags: [typescript, openapi, frontend, exposure, release-evidence, provenance]

requires:
  - phase: 31.2-contract-surface-parity-gate
    provides: "Plans 01/03/10/11/12/15 canonical OpenAPI projection, one-Program frontend sidecar, authenticated app projection, server catalog, and exact owner closure"
provides:
  - "Exact frontend transport joins across 264 compiler-owned operations and canonical OpenAPI request/response contracts"
  - "Fail-closed product exposure joins across authenticated projection, server catalog DTOs, selectors, navigation, generated consumers, and mutation omissions"
  - "Two distinct trusted repository-local reports emitted from one shared sidecar byte stream and digest"
affects: [31.2-09, frontend-transport, frontend-exposure, release-contract-aggregate, RELS-02]

tech-stack:
  added: []
  patterns:
    - "Generate the compiler sidecar once, then pass its exact existing bytes and digest to independent transport and exposure projections"
    - "Strict-decode observations and recheck the sidecar digest at both projection and report-construction boundaries"
    - "Bind exposure projection to generated release projection, authenticated app projection, server catalog subjects, selector subjects, and mutation omission contracts"

key-files:
  created:
    - scripts/verify_frontend_surface.py
    - scripts/verify-frontend-transport-contract.sh
    - scripts/verify-frontend-exposure-contract.sh
    - scripts/verify-frontend-surface-fixtures.sh
  modified:
    - scripts/frontend_surface_sidecar.mjs
    - scripts/frontend_surface_sidecar.test.mjs
    - scripts/verify-frontend-surface-sidecar.sh
    - scripts/openapi_surface_fingerprint.py
    - docs/api/route-surface-manifest.json
    - docs/api/route-surface-manifest.schema.json
    - src/web/src/generated/operation-contracts.generated.ts
    - src/server/cmd/release-contract/main.go
    - src/server/cmd/release-contract/main_test.go

key-decisions:
  - "Transport and exposure consume the same sidecar bytes and sha256 digest; neither producer may rescan TypeScript or substitute a caller-provided inventory."
  - "Authenticated projection identity, catalog DTO capability subjects, generated consumers, selector bindings, and mutation capabilityId omission all fail closed."
  - "frontend-transport and frontend-exposure retain independent closed schemas, surface identities, projection digests, and diagnostics while sharing only trusted provenance."

patterns-established:
  - "Stage A creates a clean local clone at current HEAD and lets trusted Git/profile resolvers write repository-local identity; CLI flags cannot inject identity, evidence class, skips, or drift."
  - "The combined fixture invokes the extractor once, reuses one sidecar for both projections, and rejects transport, exposure, report-fold, digest-splice, identity, skip, drift, and output mutations."

requirements-completed: [RELS-02]

coverage:
  - id: D1
    description: "All compiler-owned frontend operations exact-join canonical OpenAPI request, response, media, decoder, streaming, and status contracts from one sidecar."
    requirement: RELS-02
    verification:
      - kind: integration
        ref: "bash scripts/verify-frontend-transport-contract.sh --sidecar .tmp/frontend-surface-sidecar.json --stage-a"
        status: pass
      - kind: mutation
        ref: "scripts/verify-frontend-surface-fixtures.sh#transport baseline and 20 rejected mutations"
        status: pass
    human_judgment: false
  - id: D2
    description: "Product exposure exact-joins authenticated navigation, capability selectors, server catalog DTOs, generated projection consumers, and mutation omissions."
    requirement: RELS-02
    verification:
      - kind: integration
        ref: "bash scripts/verify-frontend-exposure-contract.sh --sidecar .tmp/frontend-surface-sidecar.json --stage-a"
        status: pass
      - kind: mutation
        ref: "scripts/verify-frontend-surface-fixtures.sh#exposure baseline and 20 rejected mutations"
        status: pass
    human_judgment: false
  - id: D3
    description: "The release-contract CLI emits two distinct trusted reports from the identical sidecar digest and rejects caller identity, digest splice, folded surfaces, drift, skips, and output failure."
    requirement: RELS-02
    verification:
      - kind: unit
        ref: "src/server/cmd/release-contract/main_test.go#TestRunFrontendSurfaceReportsContract"
        status: pass
      - kind: integration
        ref: "scripts/verify-frontend-surface-fixtures.sh#combined one-sidecar report pair"
        status: pass
    human_judgment: false

duration: 1h 40m
completed: 2026-07-22
status: complete
---

# Phase 31.2 Plan 05: Frontend Transport and Exposure Exact-Join Summary

**One 127-file compiler scan now feeds independent transport and product-exposure projections, yielding two trusted repository-local reports over the same sidecar digest with no drift or skips.**

## Performance

- **Duration:** 1h 40m
- **Started:** 2026-07-22T09:11:00Z
- **Completed:** 2026-07-22T10:51:00Z
- **Tasks:** 3 completed
- **Files modified:** 19 production, fixture, contract, and test files plus this SUMMARY

## Accomplishments

- Exact-joined all 264 frontend operations against the canonical OpenAPI projection, including request/response schemas, status, media, decoder, upload, stream, SSE, WebSocket, raw, text, and no-content behavior.
- Bound 80 product exposures to the authenticated app projection, release projection, server catalog capability subjects, navigation, selectors, generated consumers, provider wiring, and mutation omission rules.
- Added `report-frontend-transport` and `report-frontend-exposure` to the release-contract CLI. Both strict-decode a closed observation, recheck the exact sidecar digest, resolve trusted Git/profile identity, and persist atomically.
- Made the combined fixture generate the sidecar exactly once, execute both projections over the same bytes, and reject 20 transport mutations, 20 exposure mutations, plus report provenance/outcome/output mutations.

## Task Commits

1. **Task 1: Exact-join compiler transport operations with canonical OpenAPI contracts** - `3407684` (`feat`)
2. **Task 2: Verify authenticated product exposure and mutation authority** - `558f819` (`feat`)
3. **Task 3: Emit the paired trusted frontend surface reports** - `e7ab59d` (`feat`)

This SUMMARY and the phase trackers are committed separately as plan metadata.

## Decisions Made

- The transport and exposure producers receive an explicit existing sidecar; only the aggregate fixture/Stage A wrapper may create it, and it does so once.
- Sidecar provenance is shared, but transport taxonomy and exposure projection remain separate typed schemas, consumer digests, surface IDs, and report details.
- Product presentation cannot authorize itself: authenticated projection identity, server catalog capability bindings, selector subjects, provider wiring, generated consumers, and capabilityId omission must all exact-join or fail.
- CLI callers cannot set release identity, source tree, contract digest, deployment profile, evidence class, drift, or skipped checks.

## Deviations from Plan

None. The implementation preserved the planned one-scan/two-report boundary and did not add target or live evidence scope.

## Validation Evidence

- Frontend Vitest - PASS; 5 files, 127 tests.
- Frontend type check - PASS; `pnpm --dir src/web exec tsc --noEmit`.
- Sidecar Node tests - PASS; 6 tests.
- Matched Go CLI contract - PASS; `TestRunFrontendSurfaceReportsContract`.
- Combined fixture - PASS; one extractor execution, exposure baseline plus 20 rejected mutations, transport baseline plus 20 rejected mutations, and paired report mutations.
- Production Stage A - PASS; 127 files, 264 operations, 80 projected exposures, zero unresolved records.
- Paired reports - PASS; both carry `sha256:e307aaac14c6d6bf4931057e9ad13fa442449a344ff2acaf135651c2c3ff5eb6` while retaining distinct surface and consumer identities.
- `git diff --check` - PASS after implementation.

## User Setup Required

None. The repository-local checks require no database, Provider secret, deployment, target environment, or external evidence directory.

## Evidence Boundary

This plan contributes repository-local E1/E2 compiler, source, fixture, strict-decoder, and typed-report evidence to RELS-02. It does not create E3/E4 evidence, prove target/browser behavior, establish commercial release readiness, or complete Phase 31.2 before Plan 09 aggregation and phase verification pass.

## Next Phase Readiness

- Plan 09 can consume the complete ten-surface set, including the paired frontend reports, without rescanning TypeScript or folding surface identities.
- Phase 31.2 remains in progress at 14/15 until Plan 09 implements the sole-parent aggregate and passes its clean-HEAD quality gate.
- Protected user changes in `.planning/config.json`, `effect_registry.go`, `effect_registry_test.go`, and `.planning/intel/` remain unstaged and untouched.

## Self-Check: PASSED

- Confirmed task commits `3407684`, `558f819`, and `e7ab59d` exist on `commercial-target-release-runner`.
- Confirmed the two ignored Stage A reports share the recorded sidecar digest, contain distinct surface identities, and report pass with empty drift/error/skip arrays.
- Confirmed all four protected user change paths remain outside this plan's commits.

---
*Phase: 31.2-contract-surface-parity-gate*
*Completed: 2026-07-22*
