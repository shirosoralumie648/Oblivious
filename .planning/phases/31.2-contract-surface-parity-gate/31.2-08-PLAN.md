---
phase: 31.2-contract-surface-parity-gate
plan: "08"
type: execute
wave: 2
depends_on: ["31.2-07"]
files_modified:
  - src/server/internal/surfacereport/migration.go
  - src/server/internal/surfacereport/migration_test.go
  - src/server/cmd/release-migration-surface/main.go
  - src/server/cmd/release-migration-surface/main_test.go
  - scripts/verify-migration-replay.sh
  - scripts/verify-migration-replay-fixtures.sh
autonomous: true
requirements: [RELS-02]
must_haves:
  truths:
    - "Replay uses only a provably fresh external database or unique ephemeral PostgreSQL/pgvector Docker database and starts with an empty ledger (D-SUR-14)."
    - "First apply is complete, second apply has applied=0 and all rows skipped, and final ordered ledger digest/count equal static inventory (D-SUR-12, D-SUR-14)."
    - "Missing DB/Docker/psql, non-fresh DB, partial apply, non-noop second apply, or unreadable ledger emits `migration_replay_unavailable`/failure report and exits nonzero; it is never an accepted skip (D-SUR-14)."
    - "`migration-replay` remains distinct from `migration-static` and `migration-ledger`; the isolated session invokes each needed report producer exactly once (D-SUR-12, D-SUR-15)."
    - "Applied/skipped counts are derived only from typed exact ledger snapshots before apply, after first apply and after second apply; live `cmd/migrate` human text is ignored as evidence (D-SUR-12, D-SUR-14)."
  artifacts:
    - path: "src/server/internal/surfacereport/migration.go"
      provides: "Typed migration-replay details registration/report constructor"
      exports: ["MigrationReplaySurfaceID", "MigrationReplayDetails", "NewMigrationReplayReport"]
    - path: "scripts/verify-migration-replay.sh"
      provides: "Fresh external/ephemeral Docker first-apply, no-op replay, ledger/report orchestration"
      contains: "migration_replay_unavailable"
    - path: "scripts/verify-migration-replay-fixtures.sh"
      provides: "Freshness, partial/noop/digest/environment/cleanup mutation suite"
      contains: "initialLedgerRows"
  key_links:
    - from: "scripts/verify-migration-replay.sh"
      to: "src/server/cmd/release-migration-surface/main.go"
      via: "one static, one final ledger, and one replay report invocation in an isolated session"
      pattern: "migration-static|migration-ledger|migration-replay"
    - from: "src/server/cmd/release-migration-surface/main.go"
      to: "src/server/internal/surfacereport/migration.go"
      via: "replay report constructor plus AtomicWriter"
      pattern: "NewMigrationReplayReport"
---

<objective>
在 fresh monolith PostgreSQL/pgvector 上证明 first apply、second no-op 与最终 ledger identity，并生成独立 nested replay evidence。

Purpose: 把 environment-backed replay 从 human text/count check 提升为 fail-closed typed evidence，同时保持 static/ledger/replay 三个事实互不折叠。
Output: replay report schema/CLI、fresh external/Docker harness、unavailable failure report、one-field mutation与 cleanup proof。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-PATTERNS.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VALIDATION.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-07-SUMMARY.md
@src/server/internal/migrations/migrations.go
@src/server/cmd/migrate/main.go
@scripts/verify-migration-replay.sh

<interfaces>
Extend Plan 07's existing migration report/command files rather than creating a second checksum, ledger reader, or CLI. `migration-replay` details are exact: databaseKind, replayMode, initialLedgerRows, firstApply{applied,skipped}, secondApply{applied,skipped}, finalLedgerRows, staticDigest, ledgerDigest, cleanupResult.

The shell accepts either an isolated external URL by environment-variable reference with explicit freshness proof, or starts its own unique Docker database. It never prints/stores a connection URL in reports.

The exact owned adapter contract is `MigrationLedgerSnapshot{Identities []migrations.MigrationIdentity, IdentityDigest string}` and `DeriveReplayObservation(static []MigrationIdentity, before, afterFirst, afterSecond MigrationLedgerSnapshot) (MigrationReplayDetails, error)`. It derives both applied/skipped pairs from exact identity-set transitions only. `cmd/migrate` output may be retained as redacted diagnostics but is never parsed for counts or verdicts.

`verify-migration-replay.sh session` is the sole migration producer boundary: one invocation creates a new output directory and invokes static, ledger and replay report constructors exactly once each, yielding three independent files. Plan 09 must not run static separately before this session.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register replay details and extend the migration producer command</name>
  <read_first>
    - src/server/internal/surfacereport/migration.go
    - src/server/internal/surfacereport/migration_test.go
    - src/server/cmd/release-migration-surface/main.go
    - src/server/cmd/release-migration-surface/main_test.go
    - src/server/internal/migrations/inventory.go
  </read_first>
  <files>src/server/internal/surfacereport/migration.go, src/server/internal/surfacereport/migration_test.go, src/server/cmd/release-migration-surface/main.go, src/server/cmd/release-migration-surface/main_test.go</files>
  <behavior>
    - V31.2-S08/T-31.2-05: replay pass requires initialLedgerRows=0, typed before/after-first/after-second identity snapshots, first apply count equals static count, second applied=0/skipped=static count, and final count/digest equality.
    - Replay mode is exactly `external-isolated` or `docker-ephemeral`; database kind is PostgreSQL/pgvector and connection material is structurally absent.
    - Partial apply, reused DB, non-noop replay, count/digest mismatch, missing cleanup result, skip, fold, identity splice, or E3/E4 claim fails.
    - A failure observation can create a trusted `migration-replay` failure report while command exit remains nonzero.
  </behavior>
  <action>Add a third closed registration/constructor under exact surface `migration-replay`, reusing Plan 07 trusted identity/profile and `MigrationIdentity` digest. Implement `MigrationLedgerSnapshot` and `DeriveReplayObservation` exactly as declared: compare canonical identities across before/after-first/after-second snapshots and derive applied/skipped without reading migrate text. Extend the command with `replay-report`, accepting only bounded structured snapshots/observation plus explicit repo/contract/schema/profile/output; reject identity, DSN, skip, evidence-class, raw error and human migrate-output inputs. Enforce every count/digest invariant and cleanup, persist with AtomicWriter, preserve producer failure, and keep all three schemas/commands independent. After the production replay report and command are complete, add and run `TestMigrationReplaySurfaceContract` and `TestReleaseMigrationReplayCommandContract` as implementation-complete automated verification.</action>
  <acceptance_criteria>
    - Exact replay invariants and every mismatch/fold/splice/skip case are covered by matched tests.
    - Failure report existence cannot convert nonzero producer status to success.
    - Report contains environment class/mode/digests/counts only, never connection data.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/run-go-tests-matched.sh ./internal/surfacereport '^TestMigrationReplaySurfaceContract$' &amp;&amp; bash scripts/run-go-tests-matched.sh ./cmd/release-migration-surface '^TestReleaseMigrationReplayCommandContract$' &amp;&amp; git diff --check</automated>
  </verify>
  <done>V31.2-S08 owns a distinct trusted replay report whose failure report preserves nonzero execution status.</done>
</task>

<task type="auto">
  <name>Task 2: Prove fresh apply/no-op/final digest and fail closed on unavailable environments</name>
  <read_first>
    - scripts/verify-migration-replay.sh
    - src/server/cmd/migrate/main.go
    - src/server/cmd/release-migration-surface/main.go
    - src/server/internal/migrations/inventory.go
    - docker-compose.yml
    - Dockerfile.postgres-pgvector
  </read_first>
  <files>scripts/verify-migration-replay.sh, scripts/verify-migration-replay-fixtures.sh</files>
  <behavior>
    - External mode proves zero initial ledger rows before apply; Docker mode creates a unique container/database/network identity and also proves zero initial rows.
    - The real migrate command runs twice, while exact ledger queries before/after each invocation feed the typed adapter; no assertion parses its human output.
    - No DB URL plus unavailable Docker, missing `psql`, failed ping, nonempty initial ledger, partial first apply, nonzero second apply, unreadable ledger, digest mismatch, cleanup failure, or missing output writes a failure report and exits nonzero.
    - Success/failure/INT/TERM cleanup removes all harness resources; fixture case/discovery/producer invocation counts are asserted nonzero/exact.
  </behavior>
  <action>Rewrite the replay shell around one bounded `session` with `set -euo pipefail` and EXIT/INT/TERM cleanup. Require `psql`. External mode accepts only named URL env indirection and proves empty ledger; otherwise create a unique pgvector container/database. Create a new output directory, invoke Plan 07 static producer exactly once, query ordered `(version,checksum)` identities before apply, run real migrate without parsing its text, query after first apply, run migrate again, query after second apply, invoke Plan 07 ledger producer exactly once on the final snapshot, then invoke replay-report exactly once with all three typed snapshots. Compare canonical rows/digests through `DeriveReplayObservation`. Produce exactly three independent static/ledger/replay files; fixtures assert session count one and each producer count one. On failure, best-effort write trusted replay failure while preserving nonzero; never accepted skip. Cover snapshot transition, unavailable, cleanup, duplicate invocation and human-output spoof mutations with deterministic shims plus one real Docker/DB run.</action>
  <acceptance_criteria>
    - A mandatory session invocation produces three distinct static/ledger/replay reports with matching identity and digest invariants, each constructor called once.
    - Applied/skipped evidence comes only from exact ledger snapshots; changing migrate human text cannot change verdict.
    - Missing prerequisites are explicit nonzero failures with `migration_replay_unavailable`, not skips.
    - Every resource is cleaned and no URL/credential/raw SQL appears in reports/log summaries.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/verify-migration-replay.sh scripts/verify-migration-replay-fixtures.sh &amp;&amp; bash scripts/verify-migration-replay-fixtures.sh &amp;&amp; bash scripts/verify-migration-replay.sh --stage-a --output-dir .tmp/phase-31.2-migration-replay &amp;&amp; git diff --check</automated>
  </verify>
  <done>Fresh monolith first-apply/no-op/final-ledger evidence is mandatory, typed, isolated, and fail closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| external/Docker database -> replay harness | Mutable infrastructure must be proven isolated before migration effects. |
| migrate output/ledger -> reports | Environment-backed execution becomes nested repository evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-05 | Tampering | migration replay/ledger | high | mitigate | Initial-empty proof, real two-pass apply, ordered digest equality, unavailable failure, and cleanup fixtures. |
| T-31.2-06 | Spoofing | replay report identity/status | high | mitigate | Trusted resolvers, distinct typed details, producer-exit preservation, and identity/fold/skip rejection. |
</threat_model>

<verification>
- Run both matched Go suites, shell mutation fixtures, mandatory isolated replay Stage A, and `git diff --check`.
- Docker/DB/psql absence is a failed close proof and cannot be waived by narrower tests.
- Evidence is E2 repository-local database replay, not target or commercial proof.
</verification>

<success_criteria>
- Fresh DB starts empty, first apply is complete, second apply is a total no-op, and final digest equals static.
- Static, ledger, and replay remain separate trusted reports.
- Every unavailable/nonfresh/partial/mismatch/cleanup path is nonzero with no accepted skip.
- No E3/E4 claim is made.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-08-SUMMARY.md` when done.
</output>
