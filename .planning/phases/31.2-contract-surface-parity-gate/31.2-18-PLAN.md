---
phase: 31.2-contract-surface-parity-gate
plan: "18"
type: execute
wave: 4
depends_on: ["31.2-17"]
files_modified:
  - scripts/verify-migration-replay.sh
  - scripts/verify-migration-replay-fixtures.sh
autonomous: true
gap_closure: true
requirements: [RELS-02]
must_haves:
  truths:
    - "External replay rejects a caller database before `cmd/migrate` when any non-system application schema, relation, sequence, table, or row exists, even when `schema_migrations` is absent or empty (D-SUR-14)."
    - "A passing external replay migrates only a uniquely named database owned by the session, drops it, and verifies absence before `cleanupResult=succeeded`; the caller database is never the migration target."
    - "The empty-ledger plus pre-existing-table/row fixture proves the migrate binary invocation count is zero and emits typed `migration_replay_unavailable` without leaking connection material."
  artifacts:
    - path: "scripts/verify-migration-replay.sh"
      provides: "Whole-database freshness preflight, owned disposable external database lifecycle, and verified teardown"
      contains: "prepare_external_database"
    - path: "scripts/verify-migration-replay-fixtures.sh"
      provides: "Real PostgreSQL contaminated-database and owned-teardown negative fixtures"
      contains: "run_external_case"
  key_links:
    - from: "scripts/verify-migration-replay.sh"
      to: "src/server/cmd/migrate/main.go"
      via: "freshness and ownership checks complete before the first `run_migrate` call"
      pattern: "run_migrate"
    - from: "scripts/verify-migration-replay.sh"
      to: "src/server/cmd/release-migration-surface/main.go"
      via: "ownership-aware pass/failure observation after verified teardown"
      pattern: "replay-report"
---

<objective>
Make external migration replay effects occur only in an owned disposable database after a whole-database freshness preflight, with teardown verified before success.

Purpose: Prevent CR-01 from mutating caller data or falsely reporting cleanup.
Output: fail-before-migrate external preflight, owned database lifecycle, teardown proof, and real PostgreSQL regression fixtures.
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-CONTEXT.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-VERIFICATION.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-REVIEW.md
@.planning/phases/31.2-contract-surface-parity-gate/31.2-17-SUMMARY.md
@scripts/verify-migration-replay.sh
@scripts/verify-migration-replay-fixtures.sh

<interfaces>
The harness accepts only `--database-url-env NAME`. Treat that URL as a caller-owned candidate/maintenance connection, never as the migration target. `prepare_external_database` must prove the candidate has no application namespace/object before creating a uniquely named owned database. `run_migrate`, ledger snapshots, and ledger report use only the owned database URL.

Plan 17 adds `resourceOwnership` to `migration-replay-observation/v1`: successful Docker and external sessions use `owned-disposable`; a rejected caller candidate uses `caller-owned` in its typed failure observation. `cleanup_resources` is the sole owner of resource removal and may set succeeded only after a post-drop/nonexistence probe.
</interfaces>
</context>

## Artifacts this phase produces

- A fail-before-effect external database freshness and ownership boundary.
- Verified creation/removal of an external session-owned disposable database.
- Real PostgreSQL negative fixtures proving contaminated empty-ledger databases do not invoke migration.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce contaminated-database mutation and false cleanup first</name>
  <read_first>
    - scripts/verify-migration-replay-fixtures.sh
    - scripts/verify-migration-replay.sh
    - src/server/migrations/0027_tenant_scope_core_domains.sql
    - src/server/cmd/release-migration-surface/main_test.go
  </read_first>
  <files>scripts/verify-migration-replay-fixtures.sh</files>
  <behavior>
    - RED: a real PostgreSQL database with no `schema_migrations` and one `public.customer_data` row must fail with `migration_replay_unavailable` before migrate invocation count changes from zero.
    - RED: empty ledger plus a sequence, a relation without rows, or a non-system schema must also fail before migrate.
    - RED: a clean candidate must produce a separate owned database; the caller candidate remains unchanged and the owned database is absent after pass.
    - RED: drop/probe failure must yield cleanup failed and nonzero; no report may claim success before verified absence.
  </behavior>
  <action>Before production changes, instrument the fixture-generated migrate wrapper with a dedicated invocation log written before any behavior branch. Add real pgvector cases for absent-ledger plus table/row, empty-ledger plus row, sequence-only, empty relation, and extra schema; require each to reject for freshness, retain its pre-existing object/data, and show migrate count zero. Add the expected clean-candidate owned-child/removal assertions and drop/probe failure mutations. Keep all discovery/session/real-database counters positive and retain DSN/password redaction scans. The current harness must fail these new expectations for the canonical false-green reason.</action>
  <acceptance_criteria>
    - The canonical empty-ledger/pre-existing-row probe is executable and observes current false-green behavior.
    - Every contaminated case has a zero-migrate assertion, not an inference from migrate stdout.
    - Owned-child and cleanup expectations are explicit before the harness implementation.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; output=$(bash scripts/verify-migration-replay-fixtures.sh 2&gt;&amp;1); status=$?; set -e; test "$status" -ne 0; printf "%s\n" "$output" | rg "pre-existing|contaminated|migrate|cleanup|owned"'</automated>
  </verify>
  <done>The real PostgreSQL fixture suite fails until contaminated candidates are rejected before migrate and owned teardown is proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce whole-database freshness and owned disposable replay</name>
  <read_first>
    - scripts/verify-migration-replay.sh
    - scripts/verify-migration-replay-fixtures.sh
    - src/server/cmd/migrate/main.go
    - src/server/internal/migrations/migrations.go
    - src/server/cmd/release-migration-surface/main.go
    - src/server/internal/surfacereport/migration.go
  </read_first>
  <files>scripts/verify-migration-replay.sh, scripts/verify-migration-replay-fixtures.sh</files>
  <behavior>
    - GREEN: external preflight queries `pg_namespace`, `pg_class`, and relevant catalog ownership before effects; any application schema/relation/sequence/object rejects the candidate.
    - GREEN: absence or emptiness of `schema_migrations` is necessary but never sufficient for freshness.
    - GREEN: a clean candidate is used only as a maintenance connection to create a collision-resistant owned database; all ledger/migrate/report DB calls target the owned database.
    - GREEN: cleanup drops the owned database, probes `pg_database` for absence, and only then records `resourceOwnership=owned-disposable` plus `cleanupResult=succeeded`.
  </behavior>
  <action>Refactor `prepare_external_database`, `psql_scalar`/`psql_rows`, lifecycle state, `cleanup_resources`, `write_failure_observation`, and `write_pass_observation` around an explicit maintenance URL, owned database name/URL, and `resource_ownership`. Before creating or invoking anything effectful, query PostgreSQL catalogs with fixed SQL to reject non-system schemas and all user relations including tables, partitions, views, materialized views, sequences, and foreign tables; a table implies possible data and fails without row sampling. Also require absent/empty ledger. If clean and the role can create databases, generate a restricted identifier from PID/random state, create exactly that database, verify it exists and is initially empty, set the migration URL to it, and mark it owned. If create/drop or safe URL derivation is unavailable, emit typed unavailable and never migrate the caller database. On every return/signal, terminate owned DB connections if needed, drop only the exact owned name, verify absence, and set cleanup success only after verification. Update the fixture psql/createdb/dropdb shims for these fixed catalog queries and lifecycle operations. Preserve named environment indirection, sanitized output, Docker ownership behavior, exact three reports, and no accepted skip; then make every RED case green.</action>
  <acceptance_criteria>
    - A caller-owned database is never passed to `run_migrate`.
    - Any application namespace/object rejects with migrate count zero.
    - Every pass has an owned disposable target and post-drop absence proof.
    - The full replay fixture family passes with no leaked resource or connection material.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/verify-migration-replay.sh scripts/verify-migration-replay-fixtures.sh &amp;&amp; bash scripts/verify-migration-replay-fixtures.sh &amp;&amp; git diff --check</automated>
  </verify>
  <done>The harness rejects contaminated callers before effects, migrates only an owned database, and verifies teardown before success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| named external URL -> migration harness | Caller credentials may point at a populated, valuable database. |
| harness -> `cmd/migrate` | The first migration effect can update/delete pre-existing rows. |
| cleanup state -> typed report | Cleanup intent must not be represented as verified resource removal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-31.2-18-01 | Tampering | caller-owned PostgreSQL data | critical | mitigate | Whole-database catalog preflight and absolute ban on using caller DB as migration target. |
| T-31.2-18-02 | Elevation of Privilege | owned DB creation/drop | high | mitigate | Restricted generated identifier, exact-name ownership state, create/drop capability checks, and no arbitrary identifier input. |
| T-31.2-18-03 | Repudiation | cleanup evidence | high | mitigate | Post-drop `pg_database` absence probe and typed ownership/cleanup exact join before pass. |
| T-31.2-18-04 | Information Disclosure | DB errors/logs | high | mitigate | Named env indirection, stable error codes, fixed queries, and fixture scans for DSN/password/SQL leakage. |
</threat_model>

<verification>
- Run the real PostgreSQL fixture suite; absence of Docker/PostgreSQL is a nonzero blocker, never a skip.
- Require explicit migrate invocation counts and post-teardown database catalog checks.
- Do not modify historical SQL or claim non-monolith/target parity.
</verification>

<success_criteria>
- CR-01's populated empty-ledger probe fails before `cmd/migrate`.
- External passing replay always owns and removes its disposable target.
- Cleanup reporting reflects verified state, not intent, across pass/failure/signal paths.
</success_criteria>

<output>
Create `.planning/phases/31.2-contract-surface-parity-gate/31.2-18-SUMMARY.md` when done.
</output>
