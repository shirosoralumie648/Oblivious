---
phase: 31.2-contract-surface-parity-gate
plan: "06"
subsystem: api-contracts
tags: [protobuf, protoc, codegen, supply-chain, release-evidence]

# Dependency graph
requires:
  - phase: 31-release-contract-current-baseline
    provides: "Trusted Git identity/profile resolvers, typed surface registry, and atomic report writer"
  - phase: 31.1-readiness-fail-closed
    provides: "Committed monolith profile and repository-local no-skip reporting boundary"
provides:
  - "Exact pinned protobuf compiler/plugin integrity manifest and total tracked source/output disposition"
  - "Repository-local bootstrap plus fresh temp-only byte regeneration for all 21 generated outputs"
  - "Trusted typed protobuf surface report backed by real validator observation"
affects: [31.2-09, RELS-02, protobuf, release-evidence]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Bootstrap code generators only into a manifest-bound repository-local tool directory"
    - "Regenerate into a fresh temporary tree and compare bytes without repairing tracked outputs"
    - "Convert validator observations into identity-bound typed reports through trusted resolvers"

key-files:
  created:
    - config/release/protobuf-toolchain.v1.json
    - scripts/bootstrap-protobuf-tools.sh
    - scripts/verify_protobuf_contract.py
    - src/server/internal/surfacereport/protobuf.go
    - src/server/internal/surfacereport/protobuf_test.go
  modified:
    - scripts/verify-protobuf-contract.sh
    - src/server/Makefile
    - src/server/internal/surfacereport/build.go
    - src/server/cmd/release-contract/main.go
    - src/server/cmd/release-contract/main_test.go

key-decisions:
  - "The protobuf manifest is the only tool/disposition authority and pins exact versions, integrity values, inventories, and the canonical manifest digest."
  - "Generation is verification-only: every managed output is produced in a fresh temporary tree and byte-compared, never copied into the worktree."
  - "The protobuf report accepts only the validator's closed observation; release identity, committed profile, evidence class, and report outcome remain resolver/producer-owned."

patterns-established:
  - "Every tracked proto source and generated Go output outside reference has exactly one managed or source-only disposition."
  - "Clean Stage A evidence is built from the Git index in a disposable repository so unrelated working-tree edits cannot enter identity-bearing proof."

requirements-completed: [RELS-02]

coverage:
  - id: D1
    description: "Exact protobuf tool integrity and total source/output dispositions reject version, checksum, duplicate, empty, unsafe, stale-header, and undispositioned mutations."
    requirement: RELS-02
    verification:
      - kind: integration
        ref: "python3 scripts/verify_protobuf_contract.py --manifest config/release/protobuf-toolchain.v1.json --fixtures"
        status: pass
      - kind: other
        ref: "bash scripts/verify-protobuf-contract.sh --manifest-only"
        status: pass
    human_judgment: false
  - id: D2
    description: "Pinned repository-local tools regenerate all 21 managed outputs in a fresh tree with exact set and byte equality while host tools and repair writes remain excluded."
    requirement: RELS-02
    verification:
      - kind: integration
        ref: "bash scripts/verify-protobuf-contract.sh --fresh-checkout"
        status: pass
    human_judgment: false
  - id: D3
    description: "A real validator observation produces one trusted no-skip protobuf surface report whose schema, identity boundary, versions, counts, and digests are fail-closed."
    requirement: RELS-02
    verification:
      - kind: unit
        ref: "src/server/internal/surfacereport/protobuf_test.go#TestProtobufSurfaceContract"
        status: pass
      - kind: unit
        ref: "src/server/cmd/release-contract/main_test.go#TestRunProtobufSurfaceReportContract"
        status: pass
      - kind: integration
        ref: "bash scripts/verify-protobuf-contract.sh --stage-a"
        status: pass
    human_judgment: false

# Metrics
duration: 42 min
completed: 2026-07-21
status: complete
---

# Phase 31.2 Plan 06: Protobuf Toolchain And Surface Report Summary

**Pinned protobuf tooling, total tracked-file dispositions, byte-clean fresh regeneration, and one trusted typed protobuf surface report now fail closed on drift or skips.**

## Performance

- **Duration:** 42 min
- **Started:** 2026-07-21T10:50:29Z
- **Completed:** 2026-07-21T11:32:05Z
- **Tasks:** 3 completed
- **Files modified:** 10 production and verification files plus this SUMMARY

## Accomplishments

- Pinned `protoc 25.1`, generated header `v4.25.1`, `protoc-gen-go 1.36.11`, and `protoc-gen-go-grpc 1.6.2` with artifact/module integrity and manifest digest `sha256:3b225e40c4a7659d07c2638f128ac2087483d2ebab737f4533415793dcad54eb`.
- Dispositioned all 10 tracked proto sources and 21 tracked generated outputs into nine managed sources plus one source-only compatibility schema, with 15 positive mutation fixtures.
- Bootstrapped tools only under `.tmp/protobuf-tools/bin`, generated all declared outputs into a fresh temporary tree, and proved byte equality with regeneration digest `sha256:c74af7cc805309b00807b9ecfbbbce31ec2737c4fb726130a70ecbbc168da96a`.
- Registered the `protobuf` report surface and added `report-protobuf`, which resolves identity/profile internally, rejects observation authority injection, writes atomically, and remains E1 repository-local evidence only.

## Task Commits

Each task was committed atomically:

1. **Task 1: Commit exact tool integrity and total non-mutating dispositions** - `1c55e84` (`feat`)
2. **Task 2: Bootstrap only pinned tools and byte-regenerate every managed output** - `f4ba1a6` (`feat`)
3. **Task 3: Register and emit the protobuf surface report** - `8824b31` (`feat`)

This SUMMARY is committed separately as the execute-plan metadata commit.

## Files Created/Modified

- `config/release/protobuf-toolchain.v1.json` - Exact tools, checksums, headers, dispositions, generation invocations, and manifest digest.
- `scripts/bootstrap-protobuf-tools.sh` - Isolated checksum/version-verified bootstrap with bounded downloads and atomic directory replacement.
- `scripts/verify_protobuf_contract.py` - Git inventory, manifest validation, mutation fixtures, temp regeneration, byte comparison, and structured observation.
- `scripts/verify-protobuf-contract.sh` - Manifest/fresh-checkout/Stage A entrypoints and clean indexed report fixture.
- `src/server/Makefile` - Manifest-driven verification targets with no in-place generation.
- `src/server/internal/surfacereport/protobuf.go` - Closed protobuf details schema, report constructor, and decoded report consistency checks.
- `src/server/internal/surfacereport/protobuf_test.go` - Tool/disposition/regeneration/profile/claim/report-splice contract coverage.
- `src/server/internal/surfacereport/build.go` - Central details registration and protobuf report validation dispatch.
- `src/server/cmd/release-contract/main.go` - Strict `report-protobuf` CLI producer.
- `src/server/cmd/release-contract/main_test.go` - Atomic writer, resolver ownership, override, unknown-field, and failure preservation coverage.

## Decisions Made

- The current exact inventory and regeneration digest are part of the typed report contract. An intentional proto/output change must update the manifest and report contract together.
- `api/proto/events.proto` remains source-only with `src/server/api/proto/events.proto` as the concrete compatibility consumer and tracked Go binding owner.
- Stage A snapshots the Git index rather than the dirty working tree, so unrelated user edits cannot become part of the identity-bearing repository fixture.
- Evidence remains E1 repository-local. This plan does not claim target runtime, same-commit release, E3, E4, or completion of Phase 31.2 aggregate parity.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Clean read-only Go module cache paths before atomic bootstrap replacement**
- **Found during:** Task 2 (isolated bootstrap verification)
- **Issue:** Downloaded Go module cache files can be read-only, causing ordinary directory cleanup to fail even after all integrity checks passed.
- **Fix:** Made the temporary isolated cache writable before removing it; no tracked or global cache is changed.
- **Files modified:** `scripts/bootstrap-protobuf-tools.sh`
- **Verification:** `bash scripts/verify-protobuf-contract.sh --fresh-checkout`
- **Committed in:** `f4ba1a6`

**2. [Rule 3 - Blocking] Add bounded retries for approved pinned Go plugin downloads**
- **Found during:** Task 2 (isolated fresh bootstrap)
- **Issue:** Transient module transport failures could abort an otherwise deterministic bootstrap before checksum verification.
- **Fix:** Added a bounded retry loop around the exact approved module downloads; every successful result still requires the pinned module sums and exact binary versions.
- **Files modified:** `scripts/bootstrap-protobuf-tools.sh`
- **Verification:** Fresh Stage A encountered one TLS EOF, retried once, then passed exact checksum/version verification.
- **Committed in:** `f4ba1a6`

---

**Total deviations:** 2 auto-fixed (1 bug, 1 blocking issue)
**Impact on plan:** Both fixes preserve the closed tool authority and improve deterministic execution without broadening versions, sources, output ownership, or evidence claims.

## Issues Encountered

- One final Stage A download attempt returned `curl: (35) ... unexpected eof while reading`; the bounded retry completed successfully and all pinned integrity checks still passed.

## Verification

- `python3 -m py_compile scripts/verify_protobuf_contract.py` - PASS.
- `python3 scripts/verify_protobuf_contract.py --manifest config/release/protobuf-toolchain.v1.json --fixtures` - PASS; 10 sources, 21 outputs, 10 dispositions, 11 generation invocations, and 15/15 mutation families.
- `bash scripts/verify-protobuf-contract.sh --manifest-only` - PASS with zero errors/skips.
- `bash scripts/verify-protobuf-contract.sh --fresh-checkout` - PASS; isolated tools, 21 byte-equal outputs, and 4/4 regeneration negative fixtures.
- `bash scripts/run-go-tests-matched.sh ./internal/surfacereport '^TestProtobufSurfaceContract$'` - PASS; exactly one concrete test matched.
- `bash scripts/run-go-tests-matched.sh ./cmd/release-contract '^TestRunProtobufSurfaceReportContract$'` - PASS; exactly one concrete test matched.
- `bash scripts/verify-protobuf-contract.sh --stage-a` - PASS; one typed protobuf report produced and reverified as E1 `repository-local`.
- `git diff --check` - PASS before SUMMARY creation.
- `git diff --exit-code -- '*.proto' '*.pb.go' '*_grpc.pb.go'` - PASS; no tracked source/generated bytes changed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The protobuf foundation and report producer are ready for Phase 31.2 Plan 09 aggregate ownership.
- Phase 31.2 still requires runtime, frontend, migration, exposure, and aggregate plans; RELS-02 is not target/live or commercial-release proof.
- No new blocker was introduced.

## Self-Check: PASSED

- Confirmed task commits `1c55e84`, `f4ba1a6`, and `8824b31` exist on the current branch.
- Confirmed all created/modified implementation files exist and no proto/generated output is changed.
- Confirmed user-owned `.planning/config.json`, effect registry edits, and `.planning/intel/` remain unstaged and untouched.

---
*Phase: 31.2-contract-surface-parity-gate*
*Completed: 2026-07-21*
