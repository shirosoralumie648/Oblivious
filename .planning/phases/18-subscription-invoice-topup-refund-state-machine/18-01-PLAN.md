---
phase: 18-subscription-invoice-topup-refund-state-machine
plan: 01
type: execute
wave: 1
depends_on:
  - 17-stripe-payment-authority-and-webhook-ledger
files_modified:
  - src/server/migrations/0029_billing_lifecycle.sql
  - src/server/internal/stripe/lifecycle.go
  - src/server/internal/stripe/lifecycle_test.go
  - src/server/internal/stripe/payment.go
  - src/server/internal/stripe/webhook.go
  - src/server/internal/stripe/webhook_test.go
  - src/server/internal/http/billing_handler.go
  - src/server/internal/http/quota_handler.go
  - src/server/internal/http/router.go
  - src/server/internal/http/server_test.go
  - src/server/internal/http/stripe_handler_test.go
  - src/server/internal/quota/service.go
  - docs/release/commercial-gates.md
  - .planning/phases/18-subscription-invoice-topup-refund-state-machine/18-01-SUMMARY.md
autonomous: true
requirements: [PAY-03]
user_setup:
  - "No live Stripe keys are required. Tests use fake checkout creators and Stripe signed webhook fixtures."
  - "Use TEST_DATABASE_URL with OBLIVIOUS_REQUIRE_TEST_DATABASE=true for DB-backed HTTP tests."
  - "Use restricted-network Go proxy overrides if module downloads fail: GOPROXY=https://mirrors.aliyun.com/goproxy/,direct and GOSUMDB=sum.golang.google.cn."
must_haves:
  truths:
    - "T-01: Provider webhook signatures must still be verified before any lifecycle state is applied."
    - "T-02: Duplicate Stripe event IDs must not reapply subscription, top-up, invoice, or refund effects."
    - "T-03: Top-up quota is credited only after a verified payment event and exactly once."
    - "T-04: Subscription checkout creates or updates organization-scoped subscription state and user plan assignment."
    - "T-05: Invoice success and failure update invoice and subscription payment state through auditable transitions."
    - "T-06: Refund events create refund records and reverse paid top-up effects through audited state transitions."
    - "T-07: Marketplace settlement, platform fees, payout state, and refund impact remain Phase 19 and must not be silently enabled."
  artifacts:
    - path: "src/server/migrations/0029_billing_lifecycle.sql"
      provides: "Billing lifecycle persistence for invoices, refunds, provider IDs, and append-only transitions"
      contains:
        - "billing_lifecycle_events"
        - "billing_invoices"
        - "billing_refunds"
    - path: "src/server/internal/stripe/lifecycle.go"
      provides: "PAY-03 event application service and SQL store"
      contains:
        - "type LifecycleService"
        - "ApplyStripeEvent"
        - "RecordTransition"
    - path: "src/server/internal/stripe/lifecycle_test.go"
      provides: "Focused lifecycle idempotency and state-transition tests"
      contains:
        - "TestLifecycleApplyCheckoutSessionCompletedCreatesSubscriptionOnce"
        - "TestLifecycleApplyCheckoutSessionCompletedFulfillsTopupOnce"
        - "TestLifecycleApplyInvoicePaidAndPaymentFailedTransitions"
        - "TestLifecycleApplyRefundRecordsRefundAndAdjustsTopup"
    - path: "src/server/internal/http/stripe_handler_test.go"
      provides: "DB-backed route tests proving signed webhooks apply local lifecycle effects once"
      contains:
        - "TestStripeWebhookRouteAppliesCheckoutCompletedSubscriptionOnce"
        - "TestBillingCheckoutTopupDoesNotCreditQuotaBeforeWebhook"
  key_links:
    - from: "src/server/internal/stripe/webhook.go"
      to: "src/server/internal/stripe/lifecycle.go"
      via: "WebhookHandler records a provider event, then applies lifecycle effects only for newly inserted events"
    - from: "src/server/internal/stripe/lifecycle.go"
      to: "src/server/internal/quota/service.go"
      via: "Top-up checkout completion credits tenant quota through the existing quota service"
    - from: "billing_lifecycle_events"
      to: "docs/release/commercial-gates.md"
      via: "PAY-03 evidence must show auditable money-movement state transitions"
---

<objective>
Implement Phase 18 by applying Stripe payment events to subscription, invoice, top-up, failed-payment, plan-change, and refund state through an auditable, idempotent lifecycle service.

Purpose: close `PAY-03` without enabling Phase 19 Marketplace settlement or Phase 20 admin billing UI prematurely.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
<read_first>
- .planning/phases/18-subscription-invoice-topup-refund-state-machine/18-CONTEXT.md
- .planning/phases/17-stripe-payment-authority-and-webhook-ledger/17-01-SUMMARY.md
- .planning/PROJECT.md
- .planning/REQUIREMENTS.md
- .planning/ROADMAP.md
- .planning/STATE.md
- docs/superpowers/specs/2026-05-27-commercial-complete-program-design.md
- docs/release/commercial-gates.md
- src/server/migrations/0017_quotas.sql
- src/server/migrations/0021_plan_extensions.sql
- src/server/migrations/0028_payment_authority.sql
- src/server/internal/stripe/payment.go
- src/server/internal/stripe/ledger.go
- src/server/internal/stripe/webhook.go
- src/server/internal/http/billing_handler.go
- src/server/internal/http/quota_handler.go
- src/server/internal/quota/service.go
</read_first>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add billing lifecycle schema and test database setup</name>
  <files>src/server/migrations/0029_billing_lifecycle.sql, src/server/internal/http/server_test.go</files>
  <action>
  Add migration/test schema for `billing_lifecycle_events`, `billing_invoices`, `billing_refunds`, payment intent provider reconciliation columns, top-up reconciliation/refund columns, and subscription Stripe lifecycle columns.
  </action>
  <verify_red>
  <automated>cd src/server && go test ./internal/stripe -run 'Lifecycle' -count=1</automated>
  <expected>Initially no lifecycle tests exist. Add schema first, then continue to RED tests.</expected>
  </verify_red>
</task>

<task type="tdd">
  <name>Task 2: Add lifecycle service RED tests for checkout completion</name>
  <files>src/server/internal/stripe/lifecycle_test.go</files>
  <action>
  Add failing tests:
  - `TestLifecycleApplyCheckoutSessionCompletedCreatesSubscriptionOnce`
  - `TestLifecycleApplyCheckoutSessionCompletedFulfillsTopupOnce`
  </action>
  <verify_red>
  <automated>cd src/server && go test ./internal/stripe -run 'LifecycleApplyCheckoutSessionCompleted' -count=1</automated>
  <expected>FAIL because `LifecycleService`, lifecycle schema helpers, and event application do not exist.</expected>
  </verify_red>
</task>

<task type="tdd">
  <name>Task 3: Implement lifecycle store and checkout completion application</name>
  <files>src/server/internal/stripe/lifecycle.go, src/server/internal/stripe/payment.go, src/server/internal/quota/service.go</files>
  <action>
  Implement `LifecycleService.ApplyStripeEvent`, SQL store methods, checkout metadata parsing, payment intent completion, subscription create/update, top-up paid marking, quota crediting, and transition recording. Use transition keys such as `stripe:<event_id>:checkout:<payment_intent_id>`.
  </action>
  <verify_green>
  <automated>cd src/server && go test ./internal/stripe -run 'LifecycleApplyCheckoutSessionCompleted' -count=1</automated>
  <expected>PASS.</expected>
  </verify_green>
</task>

<task type="tdd">
  <name>Task 4: Add invoice, failed-payment, subscription, and refund RED tests</name>
  <files>src/server/internal/stripe/lifecycle_test.go</files>
  <action>
  Add failing tests:
  - `TestLifecycleApplyInvoicePaidAndPaymentFailedTransitions`
  - `TestLifecycleApplySubscriptionUpdatedAndDeletedTransitions`
  - `TestLifecycleApplyRefundRecordsRefundAndAdjustsTopup`
  </action>
  <verify_red>
  <automated>cd src/server && go test ./internal/stripe -run 'LifecycleApply(Invoice|Subscription|Refund)' -count=1</automated>
  <expected>FAIL because only checkout completion behavior exists after Task 3.</expected>
  </verify_red>
</task>

<task type="tdd">
  <name>Task 5: Implement invoice, subscription, plan-change, failed-payment, and refund application</name>
  <files>src/server/internal/stripe/lifecycle.go, src/server/internal/stripe/payment.go</files>
  <action>
  Implement invoice upsert, invoice paid/payment-failed state, subscription update/delete state, scheduled plan-change application, refund upsert, payment intent refund state, top-up quota reversal, and transition records.
  </action>
  <verify_green>
  <automated>cd src/server && go test ./internal/stripe -run 'LifecycleApply(Invoice|Subscription|Refund|CheckoutSessionCompleted)' -count=1</automated>
  <expected>PASS.</expected>
  </verify_green>
</task>

<task type="tdd">
  <name>Task 6: Wire webhook lifecycle application and payment-backed top-up checkout</name>
  <files>src/server/internal/stripe/webhook.go, src/server/internal/stripe/webhook_test.go, src/server/internal/http/billing_handler.go, src/server/internal/http/quota_handler.go, src/server/internal/http/router.go, src/server/internal/http/stripe_handler_test.go</files>
  <action>
  Add failing DB-backed HTTP tests:
  - `TestStripeWebhookRouteAppliesCheckoutCompletedSubscriptionOnce`
  - `TestBillingCheckoutTopupDoesNotCreditQuotaBeforeWebhook`
  - `TestQuotaTopupEndpointNoLongerCreditsWithoutPayment`
  Then wire `LifecycleApplier` into `WebhookHandler`, construct it in `router.go`, support `kind=topup` in checkout, create pending `topup_orders`, and stop direct quota top-up from crediting without payment evidence.
  </action>
  <verify_green>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test ./internal/http -run 'Stripe|Billing|Checkout|Webhook|Topup|Subscription' -count=1</automated>
  <expected>PASS.</expected>
  </verify_green>
</task>

<task type="execute">
  <name>Task 7: Update evidence docs and close PAY-03 only after verification</name>
  <files>docs/release/commercial-gates.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md, .planning/phases/18-subscription-invoice-topup-refund-state-machine/18-01-SUMMARY.md</files>
  <action>
  After all RED/GREEN tests pass, update v06 evidence, mark `PAY-03` complete, move state to Phase 19 ready to plan, and write `18-01-SUMMARY.md` with exact commands and residual Phase 19/20/v07/v08 work.
  </action>
  <verify>
  <automated>cd src/server && go test ./internal/stripe -run 'Lifecycle|CheckoutCompleted|Topup|Invoice|Refund|Subscription' -count=1</automated>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test ./internal/http -run 'Stripe|Billing|Checkout|Webhook|Topup|Invoice|Refund|Subscription' -count=1</automated>
  <automated>cd src/server && TEST_DATABASE_URL="$TEST_DATABASE_URL" OBLIVIOUS_REQUIRE_TEST_DATABASE=true go test ./internal/stripe ./internal/http ./internal/quota -count=1</automated>
  <automated>bash scripts/check.sh docs</automated>
  <automated>GOPROXY=https://mirrors.aliyun.com/goproxy/,direct GOSUMDB=sum.golang.google.cn bash scripts/check.sh all</automated>
  <automated>git diff --check</automated>
  </verify>
</task>

</tasks>

<completion_criteria>
Phase 18 is complete only when PAY-03 has current DB-backed evidence for subscription lifecycle, invoices, failed-payment state, plan changes, payment-backed top-up fulfillment, refunds, and append-only lifecycle transitions. Do not mark v06 complete, do not enable Marketplace settlement, and do not claim final commercial readiness.
</completion_criteria>
