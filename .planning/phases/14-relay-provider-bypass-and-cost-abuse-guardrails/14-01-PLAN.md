---
phase: 14-relay-provider-bypass-and-cost-abuse-guardrails
plan: 01
type: execute
wave: 1
depends_on:
  - 13-relay-endpoint-authority-and-fail-closed
files_modified:
  - src/server/internal/relay/types/types.go
  - src/server/internal/relay/handler/policy.go
  - src/server/internal/relay/handler/policy_test.go
  - src/server/internal/relay/handler/router.go
  - src/server/internal/relay/handler/router_test.go
  - src/server/internal/chat/gateway.go
  - src/server/internal/chat/gateway_test.go
  - src/server/internal/http/router.go
  - src/server/internal/memory/embedder.go
  - src/server/internal/memory/embedder_test.go
  - src/server/internal/memory/service.go
  - src/server/internal/memory/service_test.go
  - scripts/check.sh
  - scripts/verify-relay-security.sh
  - scripts/verify-quality-gates.sh
  - docs/release/relay-route-table.md
  - docs/release/commercial-gates.md
autonomous: true
requirements: [RELAY-10, RELAY-11]
user_setup:
  - "No provider keys are required; provider-bypass checks are static and focused unit tests use httptest."
  - "Use restricted-network Go proxy overrides if module downloads fail: GOPROXY=https://mirrors.aliyun.com/goproxy/,direct and GOSUMDB=sum.golang.google.cn."
---

<objective>
Implement Phase 14 by proving app services cannot bypass Relay for provider calls and by enforcing production identity/auth/rate-limit/audit policy semantics before supported `/v1/*` endpoints can reach provider handlers.

Purpose: close `RELAY-10` and `RELAY-11` without claiming Phase 15 settlement or final commercial readiness.
</objective>

<tasks>

<task type="tdd">
  <name>Task 1: Add supported-route auth, tenant, rate-limit, and audit policy tests</name>
  <files>src/server/internal/relay/handler/policy_test.go, src/server/internal/relay/handler/router_test.go</files>
  <action>
  Add failing tests first:
  - supported billed routes must declare `AuthPolicy`, `TenantIdentityRequired`, `RateLimitPolicy`, and `AuditPolicy`.
  - production supported routes without trusted internal identity must return 401 before handler execution.
  - production supported routes with valid trusted identity must reach the handler, put trusted user/org/request identity into context, and emit an allowed audit event.
  - rejected production requests must emit a rejected audit event with failure reason.
  </action>
  <verify_red>
  <automated>cd src/server && go test ./internal/relay/handler -run 'SupportedRoutePolicies|ProductionSupportedRoutesRequireTrustedIdentity|ProductionSupportedRoutesAttachTrustedIdentityAndAudit' -count=1</automated>
  <expected>FAIL because Phase 13 policy has no auth/rate/audit fields and supported routes do not enforce identity.</expected>
  </verify_red>
</task>

<task type="tdd">
  <name>Task 2: Add Memory Relay metadata and direct-provider bypass RED checks</name>
  <files>src/server/internal/chat/gateway_test.go, src/server/internal/memory/embedder_test.go, src/server/internal/memory/service_test.go, scripts/verify-relay-security.sh, scripts/check.sh</files>
  <action>
  Add failing tests/gate first:
  - `HTTPReplyGenerator` must not call a configured direct provider endpoint; it should remain demo-only and make zero network calls.
  - `RelayEmbedder` must forward trusted user and organization headers plus internal auth when context carries Relay identity.
  - `memory.Service` must add session user/org identity to embedding calls for add/update/search paths.
  - `bash scripts/check.sh relay-security` must fail while app services still contain direct provider fallback wiring.
  </action>
  <verify_red>
  <automated>cd src/server && go test ./internal/chat ./internal/memory -run 'HTTPReplyGenerator|RelayEmbedder|RelayIdentity' -count=1</automated>
  <automated>bash scripts/check.sh relay-security</automated>
  <expected>FAIL before implementation.</expected>
  </verify_red>
</task>

<task type="tdd">
  <name>Task 3: Implement Phase 14 guardrails</name>
  <files>src/server/internal/relay/types/types.go, src/server/internal/relay/handler/policy.go, src/server/internal/relay/handler/router.go, src/server/internal/chat/gateway.go, src/server/internal/http/router.go, src/server/internal/memory/embedder.go, src/server/internal/memory/service.go</files>
  <action>
  Implement the minimal behavior to pass the RED tests:
  - extend `RoutePolicy` with auth, tenant, rate-limit, and audit policy fields.
  - replace the route wrapper with a single policy guard that handles production-disabled and production-supported auth failures before handler dispatch.
  - add injectable route audit sink semantics to `RouteRegistrationOptions`.
  - propagate trusted identity from headers into Relay context for all supported handlers, not only Chat.
  - add trusted request-id context helpers in Relay types.
  - make `HTTPReplyGenerator` demo-only and remove direct provider HTTP calls from app service code.
  - make `memory.RelayEmbedder` forward trusted internal headers and make `memory.Service` seed trusted identity from `auth.Session`.
  </action>
  <verify_green>
  <automated>cd src/server && go test ./internal/relay/handler -run 'SupportedRoutePolicies|ProductionSupportedRoutesRequireTrustedIdentity|ProductionSupportedRoutesAttachTrustedIdentityAndAudit|FailClosed|RoutePolicy' -count=1</automated>
  <automated>cd src/server && go test ./internal/chat ./internal/memory -run 'HTTPReplyGenerator|RelayEmbedder|RelayIdentity' -count=1</automated>
  <automated>bash scripts/check.sh relay-security</automated>
  <expected>PASS.</expected>
  </verify_green>
</task>

<task type="execute">
  <name>Task 4: Update docs, quality gates, and phase evidence</name>
  <files>docs/release/relay-route-table.md, docs/release/commercial-gates.md, scripts/verify-quality-gates.sh, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/14-relay-provider-bypass-and-cost-abuse-guardrails/14-01-SUMMARY.md</files>
  <action>
  Document the new Phase 14 boundary:
  - route table includes auth, tenant, rate-limit, and audit policy columns.
  - commercial gates cite provider-bypass and endpoint policy evidence while keeping settlement/final commercial readiness open.
  - quality gates assert `verify-relay-security.sh`, `relay-security`, and route-policy fields exist.
  - planning files mark `RELAY-10` and `RELAY-11` complete only after verification passes, keep `BILL-01`, `BILL-02`, and `DOC-04` open, and set next step to Phase 15 planning.
  </action>
  <verify>
  <automated>cd src/server && go test ./internal/relay/... ./internal/http -count=1</automated>
  <automated>bash scripts/check.sh docs</automated>
  <automated>bash scripts/check.sh relay-security</automated>
  <automated>git diff --check</automated>
  </verify>
</task>

</tasks>

<completion_gate>
Phase 14 is complete only after focused RED/GREEN evidence, Relay security gate, docs gate, and diff check pass. Do not mark v05 or the thread goal complete; Phase 15-16 and v06-v08 remain required.
</completion_gate>

---
*Plan written: 2026-05-28*
