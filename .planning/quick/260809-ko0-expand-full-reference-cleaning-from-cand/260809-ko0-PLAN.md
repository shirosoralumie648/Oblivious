---
quick_id: 260809-ko0
description: Expand full reference cleaning from candidate-only to all 179669 unfiltered units while reusing current checkpoints
date: 2026-08-09
status: planned
---

# Expand Full Reference Cleaning To Unfiltered Scope

## Goal

Replace the active 91,273-unit candidate-only cleaner with one strict, resumable cleaner covering all 179,669 unfiltered issue, merged-PR, release, and changelog units. Reuse every current `gpt-5.4-mini` / low-effort clean checkpoint and preserve fail-closed aggregation.

## Scope And Boundaries

- Workdir remains `/home/shirosora/.cache/oblivious-reference-intel/full-20260809-v3`.
- Model remains `gpt-5.4-mini`, reasoning effort `low`, and confidence threshold `0.80`.
- The old `run-full-v3.sh` runner and all current raw/clean checkpoints remain intact.
- The replacement runner omits `--implementation-candidates-only`, keeps `--max-clean-errors 1`, and runs strict `clean -> aggregate -> status` without incomplete/error allowances.
- Only one pipeline writer may run against this workdir.
- Completion of this quick task means the all-unit cleaner is durably launched and verified to reuse checkpoints. It does not mean all 179,669 units or the final catalog are complete.
- The resulting catalog, once complete, is upstream metadata only; it is not proof of Oblivious implementation or target/live readiness.

## Task 1: Capture Baseline And Stop Candidate Writer

**Files:** External workdir runtime state only.

**Action:** Record item/error/response counts and candidate progress. Send `C-c` to the named tmux session, wait for the foreground command to exit, and verify no orphan pipeline or Codex cleaner remains for the workdir.

**Verify:** Candidate progress remains readable, all checkpoints remain present, and process inspection finds zero writers before relaunch.

**Done:** The candidate-only process is stopped without deleting or rewriting checkpoints.

## Task 2: Launch Strict Unfiltered Cleaner

**Files:** `/home/shirosora/.cache/oblivious-reference-intel/full-20260809-v3/run-full-v3-unfiltered.sh`.

**Action:** Add a syntax-checked external runner with the same model/effort/confidence and strict error/aggregation behavior, but no candidate-only filter. Reuse the existing named tmux session for the replacement process.

**Verify:** Exactly one writer is active; `clean/progress.json` reports `implementation_candidates_only=false`, `source_records=178377`, and `source_units=179669`; pre-existing item checkpoints remain; successful calls advance with zero errors; `catalog/` remains absent while cleaning is incomplete.

**Done:** The unfiltered cleaner is running persistently and has demonstrated checkpoint reuse plus new-call progress.

## Task 3: Record Repository-Local Evidence

**Files:** `docs/audit/reference-intelligence-pipeline.md`, `.planning/quick/260809-ko0-expand-full-reference-cleaning-from-cand/260809-ko0-SUMMARY.md`, `.planning/quick/260809-ko0-expand-full-reference-cleaning-from-cand/260809-ko0-VERIFICATION.md`, `.planning/STATE.md`.

**Action:** Document the unfiltered launch, exact runtime evidence, remaining completion boundary, monitoring commands, and GSD task status. Run focused pipeline tests and shell syntax checks, inspect the owned diff, and commit only owned files locally.

**Verify:** Focused tests pass; runtime fields and process counts match the documentation; unrelated dirty paths remain unstaged; the final commit contains only owned repository artifacts.

**Done:** The launch is reproducible and auditable without overstating catalog, product, or target/live completion.
