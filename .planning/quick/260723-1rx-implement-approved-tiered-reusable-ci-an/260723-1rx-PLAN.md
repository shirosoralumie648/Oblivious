---
quick_id: 260723-1rx
status: planned
description: Implement approved tiered reusable CI and push it
must_haves:
  truths:
    - Every branch push invokes the quick tier through the repository-owned reusable workflow.
    - Pull requests, main/master pushes, and manual runs additionally invoke the full repository-local tier.
    - Ordinary CI remains read-only, credential-free, and cannot imply target/live commercial readiness.
  artifacts:
    - .github/workflows/ci.yml
    - .github/workflows/_ci-reusable.yml
    - .github/actions/setup-toolchain/action.yml
    - scripts/verify-quality-gates.sh
  key_links:
    - The facade derives run_full only from the GitHub event and protected branch refs.
    - quick-gate and full-gate fail closed over the results of their required jobs.
    - The repository quality verifier owns structural regression checks for all three CI artifacts.
---

# Quick Task 260723-1rx: Implement Tiered Reusable CI

Canonical implementation plan: `docs/superpowers/plans/2026-07-23-tiered-reusable-ci.md`.

## Task 1: Add the structural contract first

**Files:** `scripts/verify-quality-gates.sh`

**Action:** Add facade, reusable workflow, composite action, trigger, permission, tier, strict database, security, fixture, and Playwright artifact assertions before creating the new artifacts. Expose the same assertions through `--ci-contract-only` so quick CI does not invoke the unrelated clean-head release aggregate.

**Verify:** `bash scripts/verify-quality-gates.sh` fails specifically because the approved reusable/action files do not yet exist.

**Done:** The repository-owned quality gate detects the missing CI architecture.

## Task 2: Implement and commit the CI architecture

**Files:** `.github/workflows/ci.yml`, `.github/workflows/_ci-reusable.yml`, `.github/actions/setup-toolchain/action.yml`, `scripts/verify-quality-gates.sh`

**Action:** Replace the current workflow with the approved event facade, add the shared toolchain action and quick/full reusable jobs, and preserve least privilege plus the repository-local evidence ceiling.

**Verify:** The focused structural gate turns green, Docker Compose profiles parse, YAML parses, shell syntax passes, backend compile passes, frontend production build passes, and the staged file set contains only the four implementation files. The default quality gate is rerun from a clean temporary worktree at the implementation commit.

**Done:** A single atomic implementation commit contains the complete approved CI change and no user-owned Phase 31.2 edits.

## Task 3: Record, push, and inspect the hosted quick tier

**Files:** this plan, `260723-1rx-SUMMARY.md`, `docs/superpowers/plans/2026-07-23-tiered-reusable-ci.md`, `.planning/STATE.md`

**Action:** If the quick tier exposes deterministic stale tests, align only those expectations with the newer authority-bearing release projection/OpenAPI contracts in a separate commit. Then record fresh verification evidence and the implementation commit, commit only GSD/planning artifacts, push `commercial-target-release-runner`, and inspect the GitHub Actions run when authentication permits.

**Verify:** The remote branch reaches the local HEAD. A hosted branch run, when observable, contains the stable quick jobs and `quick-gate`; absence of a PR/full run remains explicitly unproven.

**Done:** The CI implementation is pushed with an auditable local evidence record and an honest hosted-evidence boundary.
