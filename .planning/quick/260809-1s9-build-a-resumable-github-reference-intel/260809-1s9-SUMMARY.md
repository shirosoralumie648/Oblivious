---
quick_id: 260809-1s9
status: incomplete
updated_at: 2026-08-09T02:36:43Z
implementation_commit: ccf3ce3
---

# Quick Task 260809-1s9 Summary

## Outcome

已完成可恢复的 GitHub reference intelligence 管线，并提交为 `ccf3ce3`。管线从每个 `reference/*` 嵌套仓库的 Git origin 自动发现项目，保留 issue、merged PR、release、tag 和本地 changelog 的不可变 raw provenance，再按单条 bounded unit 调用 Luna、校验结构化输出、回连 raw hash 并生成 evidence-aware catalog。

全量清洗尚未完成：`newapi -> gpt-5.6-luna` 在显式 `low` effort 下仍超时，但用户指定的 `gpt-5.4-mini` 已通过最小 probe 和 20-unit 真实 smoke。当前没有生成完整功能 catalog，也没有把 timeout 或部分样本当作全量结果。raw 数据和 clean checkpoint 保存在仓库外的 `/home/shirosora/.cache/oblivious-reference-intel/full-20260809`。

## Implemented

- `discover` 自动发现 30 个 GitHub 仓库，跳过数为 0。
- `collect` 使用 `gh api --paginate` 流式写入 raw JSONL、SQLite 索引和每来源 checkpoint。
- 当前 raw manifest：190,299 条记录，包含 changelog 25、issue 88,920、pull request 80,137、release 9,295、tag 11,922，collection failures 为 0。
- `clean` 对候选集提供 94,097 个 unit（source records 92,556）；完整非候选集为 181,878 个 unit（178,377 条可清洗记录）。
- `model_reasoning_effort` 默认为 `low`，显式传入 Codex；它与 model、source hashes 一起构成 clean checkpoint 身份，旧 timeout checkpoint 不会阻塞新配置重试。
- 模型错误以 atomic per-unit checkpoint 保存，不能进入 accepted inventory；聚合保留 upstream-only evidence boundary。

## Verification

- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m unittest discover -s tests -p 'test_reference_intel.py' -v` — 9/9 passed。
- `python3 -m py_compile scripts/reference_intel/pipeline.py tests/test_reference_intel.py` — passed。
- `bash -n scripts/reference-intel.sh` 和 schema JSON parse — passed。
- `python3 scripts/reference_intel/pipeline.py discover --repo-root .` — 30 repositories, 0 skipped。
- Full raw status — collection failures 0；`gpt-5.4-mini` clean successful 20、current errors 0、pending 94,077；catalog 尚未建立。

## Model Status

- 命名 tmux smoke `oblivious-reference-luna-smoke-20260809` 执行了 1 个真实 unit，`--model gpt-5.6-luna --model-reasoning-effort low --model-timeout 35 --max-attempts 1`，结果为 `Luna call timed out after 35s`。
- 独立最小 probe 同样在 30 秒内退出码 124，只有 SessionStart，没有输出文件。
- 将 `service_tier="priority"` 与 `low` 一起显式设置后，30 秒 probe 仍退出码 124，排除了仅由 `fast` tier 选择造成的恢复路径。
- 同一 `newapi` provider 下 `gpt-5.6-sol`、low effort 的最小 probe 退出码 0 并返回 `OK`。不能用 sol 冒充 Luna，因此清洗任务在 provider 恢复前保持 fail-closed。
- `gpt-5.4-mini`、low effort 的独立 probe 在约 6 秒内退出码 0 并返回 `OK`。
- 命名 tmux smoke `oblivious-reference-gpt54mini-smoke-20260809` 完成 20/20 个真实 unit，0 模型错误；18 个 unit 判为 implementation-bearing，共产生 17 条 grounded accepted claims，6 个 unit 同时进入人工复核范围。
- 20-unit smoke 约耗时 5 分 24 秒；串行候选集 ETA 约 16 天。由于调用规模和成本显著，未在“试试”授权下自动启动剩余 94,077 个 unit。

## Resume

确认接受调用规模后，可从已有 raw 数据继续 `gpt-5.4-mini` 候选集：

```bash
run_dir=/home/shirosora/.cache/oblivious-reference-intel/full-20260809
bash scripts/reference-intel.sh clean \
  --workdir "$run_dir" \
  --model gpt-5.4-mini \
  --model-reasoning-effort low \
  --implementation-candidates-only \
  --retry-errors \
  --allow-clean-errors
bash scripts/reference-intel.sh aggregate --workdir "$run_dir"
bash scripts/reference-intel.sh status --workdir "$run_dir"
```

现有 20 个成功 checkpoint 会被复用；不要删除 raw 目录，也不要把 `--allow-clean-errors` 的中间状态当作完成。`gpt-5.6-luna` 的旧错误 checkpoint 按 model 身份隔离，不会混入 mini clean index。

## Evidence Boundary

任何未来生成的 `catalog/features.*` 都只能声明 `evidence_class=upstream-metadata`、`reference_only=true`。它描述上游项目的历史实现证据，不证明 Oblivious 已实现、可部署、可收费或已通过 target/live 验证。
