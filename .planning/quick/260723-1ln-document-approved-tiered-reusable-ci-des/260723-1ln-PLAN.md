---
quick_id: 260723-1ln
status: planned
description: Document approved tiered reusable CI design
must_haves:
  truths:
    - The approved reusable-workflow approach is documented before CI implementation begins.
    - Development branches receive fast repository-local feedback while pull requests and default branches retain the complete gate set.
    - CI cannot imply target/live commercial readiness or consume production credentials.
  artifacts:
    - docs/superpowers/specs/2026-07-23-tiered-reusable-ci-design.md
  key_links:
    - The event facade passes a boolean full-tier decision into one repository-owned workflow_call implementation.
    - Stable quick-gate and full-gate results provide branch-protection contracts over their respective jobs.
---

# Quick Task 260723-1ln: Tiered Reusable CI Design

## Task 1: Record the approved architecture

**Files:** `docs/superpowers/specs/2026-07-23-tiered-reusable-ci-design.md`

**Action:** Document the event facade, reusable workflow, local composite setup action, quick and full job tiers, aggregate gates, permissions, concurrency, caching, failure artifacts, evidence boundary, and acceptance checks approved by the user.

**Verify:** The design contains no unresolved markers, names every planned repository artifact, and keeps target/live evidence outside ordinary CI.

**Done:** The implementation boundary is precise enough to plan without reopening the selected approach.

## Task 2: Review and commit the design independently

**Files:** design document and current Git index

**Action:** Check the design for internal contradictions, ambiguous trigger behavior, privilege escalation, duplicate required-check semantics, and accidental inclusion of unrelated dirty files. Commit only the design document.

**Verify:** `git diff --check` passes and commit inspection shows exactly the design document.

**Done:** The approved specification has an independent reviewable commit.

## Task 3: Record the GSD design slice

**Files:** this plan, the quick-task summary, and `.planning/STATE.md`

**Action:** Record completion and the design commit without changing ROADMAP phase status or treating the design as CI implementation.

**Verify:** The tracking commit contains only the three GSD artifacts and preserves every pre-existing worktree change.

**Done:** The design slice is auditable and ready for user review.
