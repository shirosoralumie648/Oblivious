---
quick_id: 260717-up4
status: planned
description: 审计当前未提交内容，完善 .gitignore，删除确认无关的生成内容，并按逻辑边界逐步提交
must_haves:
  truths:
    - 当前用户改动、产品源码、正式规划文档和目标环境证据不被删除或覆盖
    - 仓库级忽略规则覆盖本项目实际产生的本地 Agent、MCP、Go、Node、Python、IDE 和操作系统产物
    - 已跟踪的可再生研究缓存从版本控制中移除，正式研究结论继续保留
    - 提交按清理实现与 GSD 记录两个逻辑边界保持原子性
  artifacts:
    - .gitignore
    - .planning/research/ARCHITECTURE.md
    - .planning/research/FEATURES.md
    - .planning/research/PITFALLS.md
    - .planning/research/STACK.md
    - .planning/research/SUMMARY.md
  key_links:
    - .gitignore must ignore .planning/research/.cache/ after tracked cache files are removed
    - repository-local ignore rules must replace reliance on .git/info/exclude for local runtime state
---

# Quick Task 260717-up4: Git Ignore And Repository Cleanup

## Task 1: Preserve the audit boundary

**Files:** `.gitignore`, `.git/info/exclude`, current Git index and worktree

**Action:** Record the clean starting state, identify ignored local outputs and tracked generated artifacts, and exclude product code, formal planning evidence, secrets, target evidence, dependencies, and reference checkouts from deletion.

**Verify:** `git status --porcelain=v2` is empty before edits; the only tracked cache/temp pattern is `.planning/research/.cache/*.json`; no repository reference points to that cache directory.

**Done:** The deletion list is limited to the 19 renewable research-cache JSON files.

## Task 2: Harden ignore rules and remove renewable cache files

**Files:** `.gitignore`, `.planning/research/.cache/*.json`

**Action:** Organize `.gitignore` by artifact class, add repository-local rules for actual local runtime/config outputs and common stack caches, preserve safe environment examples, then remove the tracked research-cache files while keeping the five formal research documents.

**Verify:** `git check-ignore -v` identifies representative local artifacts through `.gitignore`; `git ls-files -ci --exclude-standard` is empty after the cleanup is staged; formal research documents remain tracked.

**Done:** Ignore behavior is portable across clones and no renewable cache file remains tracked.

## Task 3: Validate and commit by logical boundary

**Files:** `.gitignore`, `.planning/research/.cache/*.json`, GSD quick-task artifacts, `.planning/STATE.md`

**Action:** Review the exact diff, run whitespace and repository docs checks, commit the cleanup atomically, then create the GSD summary and state record in a separate documentation commit.

**Verify:** `git diff --check`, `bash scripts/check.sh docs`, focused ignore assertions, commit inspection, and final `git status --short --branch` all pass without skips relevant to this cleanup.

**Done:** The branch contains one cleanup commit plus one GSD tracking commit and ends clean.
